# TASKS — `base-repo`

> Действия, которые может совершить **только владелец**: найти, скачать, прислать, решить,
> сходить, подать, спросить. Работа внутри песочницы — в `ROADMAP.md`.
> Статус несёт чекбокс, не время глагола.
>
> Заведён 20.08.2026 вместе с `ROADMAP.md`: база предписывала оба файла каждой репе,
> а сама не имела ни одного. Плюс `TASKS.md` — **принимающая сторона моста** от планировщика
> (`74-planner-bridge.md`), без него доставка задач в базу невозможна.

---

## ✅ Семь реп «за потолком» — ЛОЖНАЯ ТРЕВОГА, снята 04.09.2026 01:25

**Задача прожила пятнадцать минут и снята замером, а не решением.** Оставлена
записью: снятая задача объясняет себя, удалённая неотличима от несуществовавшей.

**Что было заявлено:** семь реп перевалили жёсткий потолок 500 МБ, `self-map`
1110 МБ, читать репу целиком Claude не сможет.

🔴 **Что оказалось: мерилось не то.** Порог 500 МБ существует ради одного —
**Claude не берёт архив целиком**. А `pack_release.py` в архив не кладёт
`node_modules`, `.venv`, `dist`, `__pycache__` и импортированные изображения:

| Репа | на диске | **в архив пойдёт** |
|---|---:|---:|
| `self-map` | 1110 МБ | **245 МБ** |
| `personal-finance-dss` | 617 МБ | **104 МБ** |

**Реп за потолком архива: ноль.** Действий владельца не требуется.

**Что осталось верным из находки:** `self-map/reports` действительно содержит
**5133 JPG** (873 МБ на диске). Это не нарушение порога архива, но это место
на диске, у которого свободно 7 %. Если владелец захочет разобрать —
инструмент есть (`heavy_media_to_note.py`), но повода-порога больше нет.

🔴 **Урок записан в докстроку `system_status.heavy_repos()`:** инструмент
проверки принёс свой счёт вместо счёта проверяемого и стал измерять себя.
**Второй раз за один батч** — часом раньше то же случилось в
`acceptance_audit.py`, где сравнивались разные определения «файла в репе».

## 🔴 `~/Documents/claude` — две строки, из-за которых каталог не может уехать в репу

**Найдено 22.08.2026 вахтой IX, разбор — `PIT-126`.** Кампания переноса унесла папку
`~/Documents/claude` в репу, и **личные инструкции перестали грузиться**: `~/.claude/CLAUDE.md`
подключает их директивой `@` по абсолютному пути:

```
@/Users/vasyaevdokimov/Documents/claude/instruction-files/КОНТЕКСТ_Василий_ЕДИНЫЙ.md
@/Users/vasyaevdokimov/Documents/claude/instruction-files/СТАНДАРТЫ_код_и_карьера.md
```

**Восстановлено:** оба файла лежат на месте, директивы разрешаются (43 459 и 17 039 байт).
Полный комплект (**116** файлов) — в `misc-vault/90-imported/claude-instructions/`.

🔴 **Но каталог `~/Documents/claude` остался на диске**, а по твоему правилу в `~/Documents`
должны быть только `система_репозиториев/`, `base-repo/`, `finpilot/`, `recovery-kits/`.
Сейчас там ровно два файла — те, что грузит `CLAUDE.md`.

**Решение за тобой, потому что это правка твоего глобального конфига:**

| Вариант | Что делать | Цена |
|---|---|---|
| **A. Переписать пути в `CLAUDE.md`** на `…/система_репозиториев/misc-vault/90-imported/claude-instructions/…` | одна правка двух строк | каталог уезжает целиком, `~/Documents` становится чистым |
| **B. Оставить как есть** | ничего | два файла живут вне реп; они дублируются с репой и могут разойтись |

📍 Общее правило из этого случая уже записано (`PIT-126`): перед переносом каталога —
`grep` по точкам входа (`~/.claude/CLAUDE.md`, `settings.json`, `~/.zshrc`) на абсолютные
пути внутрь него. Перенос обрывает не только файл, но и всё, что на него настроено.

---

## ~~🔴 Блокирует раздачу базы (фаза 3 кампании синтеза)~~ — снято 28.08.2026


- [ ] 🔴 **По вахте A (Adam) — до 22.09, иначе повторное списание.**
      Ящик `finpilot.support@proton.me` отключён Proton 23.08 в день заведения; у Claude
      **нет пароля**, вход только кодом на почту → доступ к оплаченной Pro потерян.
      **Два трека, второй от первого не зависит:**
      · апелляция Proton — тикет `5104621`, ответ ждём на `gertab95@proton.me`;
      · **нет ответа к ~30.08 → писать в Anthropic** с доступного адреса, потерянный
        в CC (их документированная процедура): отмена подписки + возврат. Крайний срок
        начать — ~15.09, **последний день отмены 22.09** (правило «минимум за 24 ч»).
      🔴 **Не оспаривать платёж через банк** — у обоих провайдеров это основание
      отключить аккаунт. Разбор — `reports/incidents/vahta_a_email_suspension_incident.md`

- [ ] **По вахте S (Sergey) — платёж не прошёл 18.08**, доступ приостановлен,
      подписка отменена. Четыре месяца до этого оплачивались без сбоев.
      Решить: восстанавливать или оставить. Реестр — `84-claude-accounts.md`

- [ ] **Подтвердить почту вахты M** — `/status` под ней. В реестре стоит
      `gertab95@proton.me` **со знаком вопроса**: выведено из кадра, где рядом
      был профиль «Michael», но командой не проверено

- [ ] **Отозвал токен VK** у провайдера, затем удалил `~/Downloads/vk-graph.env.backup`
      **Разблокирует:** закрытие открытого 🔴 по безопасности. Живой `.env` найден
      в `vk-graph.zip`; из git секрет не удаляется — он **отзывается**, и только потом
      чистятся файлы

---

## ✅ Непубликованные 1.84–1.93 — устарело целиком, снято 28.08.2026 (авто-режим)

Весь блок был про версии 6-дневной давности. `gh release list` подтверждает:
GitHub давно на v3.77.0+ (170+ релизов), вопрос «что делать с 1.84-1.93» снят
самим ходом времени — версии многократно перекрыты. `.git`-вопрос (база без
истории) остаётся верным по существу, но раз тег-нумерация 1.x полностью в
прошлом — не актуален для текущего v3.x потока в том же виде.

---

## ✅ Размещение org ID — РЕШЕНО 21.08.2026

**Решение владельца: в базе.** Живёт в `00-infrastructure/84-claude-accounts.md`.
Первая редакция уехала в `mission-control` из опасения «раздастся в 57 реп» —
владелец опасение снял: **база приватная и раздаётся только приватным репам**.

🔴 **Опасение проверено, и оно закрыто механически:** `sync-base.sh` перед раздачей
спрашивает GitHub `isPrivate` и публичную репу пропускает — предохранитель стоит
**на факте, а не на списке** (PIT-097). Замер: `_base/` нет ни в одной из восьми
публичных реп.

- [ ] **Даты продления — в календарь.** V — 14.09 20:10, S — 18.09 09:00;
      по J и M дат нет, дополнить в `84-claude-accounts.md`. Файл напомнить не может,
      а пропущенное продление обрывает вахту посреди работы

---

## Публикация

Все пункты закрыты — деплой v1.61.0, накопленное 1.84.0–1.89.0, profile README
опубликован. Детали и обоснования — `TASKS_HISTORY.md`.

---

## Решения, которых ждёт работа


      **Готовый выбор, тебе остаётся сказать «да» или назвать своё:**

      1. **`salvation`** — ты его уже называл. MVP работает без сборки
         (`mvp/index.html`). Оговорка честная: кода почти нет, и тема личная —
         под «крутое технологичное прикладное» для международного найма ложится слабо.
      2. **Извлечённый движок `dota-dossier`** — отдельная публичная репа **без личной
         статистики** (твоё решение «личное остаётся приватным» не нарушается).
         Внутри: слепой прогноз, запечатанный `sha256` **до** события, с порогами успеха
         и провала, названными заранее; сборка корпуса на 545 документов; Wilson CI; Elo.
         **Это единственный кандидат, который показывает не код, а методологическую
         честность** — на найме отличает сильнее, чем ещё один сервис.

      Прочие рассмотрены и отклонены с причиной — таблица в `ROADMAP.md` §P2.
      </details>

- [ ] **Подтвердил критерий разделения контуров: по локальности задачи.**
      Разбор — `reports/adr/adr_003_autonomous_contour.md`. Правило применяется одним
      вопросом: *нужен ли задаче доступ к файлам этой машины?* Да — локальный контур,
      нет — Routines. Кампания синтеза читает `~/Documents/система_репозиториев/`,
      значит локальный; ресёрчи уходят в облако.
      **Локальный контур уже собран** (`06-autonomous-mode-kit/bin/`) — подтверждение
      нужно не «прежде чем собирать», а прежде чем считать критерий каноном.
      **Разблокирует:** перевод части задач в Routines; локальная половина уже работает

- [ ] **Дал добро на живой прогон автономного контура** — сутки+ с искусственным обрывом
      сети. До него контур считается несобранным (`71` §7в), и это не формальность:
      сейчас проверено только то, что скрипт отказывается стартовать неправильно.
      От тебя нужно: сказать, **на какой задаче** гонять (предлагаю М-очередь
      `dota-dossier` — она механическая и полностью локальная), и не выключать ноутбук.
      **Разблокирует:** §P1.6a целиком, то есть «уехать на неделю»

- [ ] **Решил про `--max-budget-usd`** — CLI умеет жёсткий потолок трат на прогон
      (найдено 21.08.2026, `51` §5а). Это **единственное стоп-условие, которое исполняет
      сам инструмент**, а не дисциплина агента. Назови сумму на недельный прогон —
      вошью в контур после первой живой приёмки.
      **Разблокирует:** спокойствие: превышение физически невозможно, а не обещано


- [ ] **Решил по репам без `ROADMAP.md`/`TASKS.md`:** заводить ли им принимающую сторону
      сразу или только тем, куда реально пойдут задачи. Число обновлено 28.08.2026
      (`tasks_census.py`): было 11, сейчас **4** — большинство закрылось волной
      наполнения 27.08, вопрос сузился, но не решён.
      **Разблокирует:** полноту моста.

---

## ✅ Решено и исполнено 26.08.2026 — удаление оригиналов после `.md`-выжимки

- `health-vault` (72 PDF, медкарты/анализы) — **не тронуто**, эталон нужен показывать
  врачам, остаётся внутри репы вместе с выжимкой.
- `self-map` (~95 файлов, психологические профили/дневники) — **не тронуто**, та же
  чувствительность, что health-vault.
- `academic-portfolio` — оригиналы удалены (198 файлов), **кроме `03-bochvar-competition/`**
  (81 файл — дипломы Бочвара, эталон сохранён по прямому указанию).
- `it-base` — оригиналы удалены полностью (162 файла) — учебный архив, резюме, решения
  CodeSignal, заменимо.
- `legal-knowledge-base` — оригиналы удалены полностью (365 файлов) — методички/справочники
  (военное право, школа прав человека), не личные документы владельца.
- `base-repo` — удалён 1 файл (публичный PDF-документация Anthropic, легко перекачать).

Освобождено: it-base 182 МБ, academic-portfolio 61 МБ (после уже прошедшего PIT-146),
legal-knowledge-base 36 МБ — размеры после чистки, не объём удалённого.

## 🔴 Секрет найден при доскане PIT-124 — 26.08.2026, требует твоей проверки

- [ ] **Проверить и, если реальный, отозвать пароль БД в экспорте чата.** `secrets_scan.py`
      (доработан `--max-mb`, PIT-124 п.1 — прошлый прогон пропускал большие файлы по
      размеру, не только по заглушкам) нашёл сигнатуру «пароль в строке подключения»
      (`postgresql://…:***@…`) в
      `~/Documents/_heavy-originals/misc-vault/04-data/claude-account-data/
      J/all time all chats (07.07.26 15.48)/conversations.json`, строка 1 —
      внутри скопированной в чат команды активации `.venv` с `DATABASE_URL=`.
      **Не показываю сам секрет** (правило стоп-класса «секрет в материале») — только
      путь и класс. Похоже на локальный dev-пароль FinPilot, но это предположение, не
      факт: 8 символов, схема `postgresql`, хост не проверен (классификатор
      заблокировал дальнейшее чтение файла с секретом мне самому — правильно,
      это твоё решение). Проверь сам: если реальный/боевой — отозвать/сменить у
      провайдера БД; если dev-заглушка на `localhost` — можно закрыть без действия.
      🔴 **Тот же класс найден и во втором экземпляре** —
      `90-imported/claude-account-data/S/all time all chats (07.07.26 15.48)/
      conversations.json:1` (251 МБ, отдельная копия, пропущена первым прогоном
      из-за размера — понадобился `--max-mb 300`). J/ и S/ похожи на копии одного
      экспорта на разные вахты-аккаунты — вероятно, один и тот же секрет
      продублирован, а не два разных, но это тоже стоит проверить тебе, не
      предполагать.

## Быстрое, но требует твоего решения

- [ ] **Отключил атрибуцию в коммитах** — в `~/.claude/settings.json` добавить
      `"attribution": {"commit": "", "pr": "", "sessionUrl": false}`
      **Разблокирует:** чистые контрибьюторы в репах. Сейчас ключ **не задан**,
      то есть каждый коммит ассистента несёт трейлер `Co-Authored-By` с адресом
      `noreply@anthropic.com` — а его зарегистрировал на себя **посторонний человек**,
      и в контрибьюторах твоих приватных реп числится он. Настройка на будущее —
      пять минут; чистка прошлого опасна (перезапись хешей ломает привязку релизов
      к коммитам), детали — `00-infrastructure/58-git-practice.md` §5а

## Хозяйственное

- [ ] **Освободил место на диске**
      **Обновлено 28.08.2026:** было 10.8 ГБ свободного, сейчас **6.6 ГБ** — стало
      теснее, не свободнее, актуальность пункта выросла, не снялась.
      **Разблокирует:** спокойные массовые операции. Синк 57 реп занимает ~1.9 ГБ,
      своп уже используется в покое (`52-hardware-baseline.md`)

- [ ] **Решил, что делать с ре-мержем `character-a-analysis` → `self-map` (Stage 2).**
      🔴 Найдено 22.08.2026 при закрытии `self-map` в кампании синтеза.
      **Обновлено 28.08.2026:** `self-map` теперь на 1.12.0, `character-a-analysis`
      на 3.51.1 — расхождение выросло, не сократилось, вопрос всё ещё актуален.
      План ре-мержа записан в `_chto-eto.md` спин-оффа
      («потом — полный мерж обратно»), стадия 2 не наступила.

      **Почему это владельцу, а не вахте:** слияние необратимо и идёт по личному корпусу.
      Плюс явное предупреждение из того же документа: **не делать `rsync --delete`** —
      снесёт сырьё (`06-diaries`, `08-feedback`, `02-clinical`, `03-competency-reports-rsv`,
      `_knowledge-kit`), которого в срезе нет по построению. Только добавление и обновление.

      **Разблокирует:** единый репозиторий вместо двух расходящихся линий; снимает вопрос
      «какая копия корпуса каноническая» и делает `self-map` пригодной к работе.

- [ ] **Разобрался, один ли это источник: VK-архив в `misc-vault` и VK-источник `character-a-analysis`.**
      🔴 Найдено 22.08.2026 при закрытии `misc-vault`. Оба — **ровно 488 страниц**.
      В `misc-vault/03-archives/anya-vk-correspondence/` лежат **настоящие 36 МБ**
      в четырёх PDF, описанные как переписка. В `character-a-analysis` VK-источник описан
      как экспорт **стены и разделов профиля**, прочитан целиком, а в проектном контейнере
      его копии были **битыми плейсхолдерами по 280–300 байт**.

      **Хеши не совпадают** (проверено против трёх реп) — но материал в `misc-vault`
      помечен «re-encoded for size», так что при пережатии и не совпали бы.

      **Почему не разрешено вахтой:** различить можно только открыв личную переписку.
      Для кампании оба варианта дают одну разметку (**С**), так что работа не блокирована.

      **Разблокирует:** снимает вопрос «где канонический VK-источник» и, если это одно
      и то же, — переезд архива в свой дом (его README прямо предусматривает: «move
      to a better home if one appears»).


- [ ] **Решил, что делать с версиями базы, которых нет в git.**
      🔴 Найдено 22.08.2026. **Перепроверено 28.08.2026 — оба пути из первоначальной
      находки больше не существуют** (`~/Documents/base-repo`, `~/Downloads/_sync-base` —
      оба исчезли миграцией на `~/repos/`), но факт не изменился по сути:
      **у `~/repos/base-repo` по-прежнему нет `.git`.** База сейчас на v3.82.8,
      и вся эта история версий не в git. Из прежних трёх вариантов один отпал сам:
      «догнать `_sync-base` одним коммитом» — нечем, копии больше нет.

      **Осталось два варианта, не равнозначных:**
      - **завести `.git` в рабочей базе** прямо сейчас и вести историю с текущей
        точки (архивы остаются каналом доставки, но перестают быть единственным
        носителем);
      - оставить как есть, приняв, что история базы живёт в `CHANGELOG.md`
        (89 версий описаны текстом), а не в git.

      🔴 Любой из вариантов — операция с git, то есть **стоп-класс автономного
      режима**. Без вашего решения не делается.

      **Разблокирует:** восстановление проверяемой истории базы.


- [ ] **Решил, что делать с 47 МБ сканов коллоквиума в `academic-portfolio`.**
      `10-mephi-2019-2022/scans/kollokvium-matematika-*.pdf` — рукописный конспект,
      28 страниц (мат. индукция, бином Ньютона, комплексные числа). Доставлен из загрузок.

      🔴 **Противоречит политике самой репы:** там одиннадцать `scans-inventory.md`
      со строкой «Сканы (не хранятся); рукописные материалы сняты при slimming».
      Варианты: сжать · вынести в офлайн-архив с описью по их же образцу · оставить.
      Записка о происхождении лежит рядом.

- [ ] 🔴 **СЕКРЕТЫ: решил, где хранить `~/Documents/recovery-kits/`.**
      Найдено 22.08.2026 при уборке. **Содержимое не открывалось**, зафиксированы только
      имена и класс:

      | Файл | Класс |
      |---|---|
      | `github-recovery-codes.txt` | 🔴 коды восстановления доступа |
      | `proton-recovery-kit.pdf` (два файла) | 🔴 ключи восстановления шифрования |
      | `Резервные коды — VK ID.pdf` | 🔴 коды восстановления доступа |
      | `GitHub-Copilot-darwin-arm64.dmg` (233 МБ) | не секрет — установщик, скачивается заново |

      🔴 **В репу не переносятся ни при каких условиях** — `01-repo-standard.md` §2.4:
      «секреты — пароли, приватные ключи, API-токены, данные карт — единственное железное
      исключение, даже в приватную репу». Эти файлы дают **доступ к аккаунтам**, то есть
      ровно тот класс.

      **Что решить вам:** где им жить вне репозиториев — менеджер паролей · зашифрованный
      том · офлайн-носитель. Пока оставлены на месте нетронутыми.

      **Что сделано без вас:** ничего, кроме этой записи. Установщик Copilot (233 МБ)
      предложен к удалению отдельно — он не секрет и скачивается заново.

---

## 🟢 Два 0-байтных husk-дубля — можно удалить (низкий приоритет)

**Найдено 27.08.2026** при втором проходе `docs_to_md.py` (`07-media-to-text-lab/runs/
2026-08-27-docs-to-md-second-pass.md`): проверено на GitHub, не артефакт синка — файлы
0 байт **на самом GitHub**. Оба — устаревшие дубли, содержимое цело под другим именем
рядом:

- `legal-knowledge-base/04-reference/raznoe-import/Шаблон доверенности.docx` (0 Б) —
  рядом `Шаблон доверенности__from-юриспруденция.docx` (24 КБ) и
  `Шаблон доверенности.pdf` (118 КБ, без выжимки).
- `master-admission/achievements/rezultaty-bochvara/01.04.04 Прикладная математика ИТКН.pdf`
  (0 Б) — рядом полная копия того же файла в `achievements/bochvar/` (144 КБ, с выжимкой)
  и ещё одна в том же каталоге под суффиксом `__from-магистратура ` (тоже с выжимкой).

**Что сделано без вас:** ничего — удаление не входило в мандат содержательной ревизии,
решение по чужому контенту (даже 0-байтному) оставлено вам.

**Разблокирует:** чистые репы без пустых файлов; не блокирует ничего срочного.

---

## 🔴 `legal-knowledge-base` — папка «Заявление о годности» несёт чужие медкарты вперемешку

**Найдено 27.08.2026** при попытке пройти очередь изображений (`image_queue.py`,
`METHOD_IMAGES.md`) по `06-conscript-school/voennoe-pravo/Заявление о годности/`
(10 кадров). Открыл первые 8 — это не одна серия про одного человека и один случай,
а вперемешку сфотографированы медицинские документы **минимум трёх разных людей**:
твои собственные, и ещё двух родственниц с другими фамилиями/отчествами (кардиология,
неврология — пожилой возраст одной из них).

**Что сделано без вас:** ничего — синтезирующую заметку по этой папке **не написал**
намеренно. Три причины: (1) это не серия по методу (`METHOD_IMAGES.md` §3) — кадры
не про один предмет; (2) записывать в файл базы диагнозы третьих лиц без вашего
решения о том, где им место (`family`? `health-vault` кого-то из родных отдельной
папкой? вообще не в этой репе?) — не моё решение; (3) `87-file-to-repo-routing.md`
говорит адресовать по содержимому, а не по папке, где случайно оказался файл.

**Что решить вам:** кому какой кадр принадлежит и куда каждый должен переехать —
только тогда лаборатория переводов сможет обработать их правильно, по отдельности.

**Разблокирует:** очередь `image_queue.py` по этой репе (пока пропущена целиком —
не могу отличить, что там ещё в порядке, а что тоже перемешано, без вашей сверки).

---

## 🟡 Отложено намеренно: `portrait-of-taste` и дневник `misc-vault` — с вами вместе

**Решение владельца, 27.08.2026, дословно:** «портрет вк не трогай, там отдельно
разберёмся. Про misc картинки то же забей уже, потом как-нибудь сделаем — слишком
много картинок».

Один блок очереди `image_queue.py` исключён из содержательной ревизии изображений
(`ROADMAP.md` п.2, `METHOD_IMAGES.md`) до вашего участия:

- **`portrait-of-taste/photos*`** — 107 серий / 692 кадра. Фотографии именованных
  живых людей (личный проект «портрет вкуса»). Метод применим технически, но
  писать «дословный захват» чужой внешности без вас на связи — не решение вахты.

**Что сделать вам:** когда будет время — сесть вместе и пройти эту очередь по
методу (`METHOD_IMAGES.md`), либо явно решить свернуть её иначе (эталон без
текстового разбора, отдельная выборка и т. п.).

✅ **`misc-vault/.../Дневник рефлексий Notion/photos/` — закрыт 27.08.2026, не
разбором, а переносом.** 24 серии / 248 кадров (443 файла: вложения-скриншоты
переписки и сторис, не текст). По вашему прямому решению в этой же сессии —
проверено: текстовая часть того же дневника уже полностью проанализирована в
`character-a-analysis/10-life-history/02-notes/notion-diary.md`, вложения
туда никогда не попадали (CSV-источник текстовый, вложения — отдельный канал).
Скопированы (побайтово + sha256-выборкой проверено) в `~/Pictures/Персонаж
А /Дневник рефлексий Notion — фото/`, исходник удалён из `misc-vault`.
Указатель — `misc-vault/01-documents/raznoe/Разное/Дневник рефлексий Notion/
photos-MOVED.md`. Разбор содержания — задача `character-a-analysis`.

**Разблокирует:** закрытие пункта «изображения» кампании ревизии почти целиком
— без `portrait-of-taste` очередь `image_queue.py` по остальной системе
(6 серий / 54 кадра на 27.08.2026, весь остаток в `legal-knowledge-base`
конскрипт-школе — личная переписка и медданные третьих лиц — и `edu-base`,
`07-media-to-text-lab/runs/2026-08-27-image-queue-first-look.md`)
близка к закрытию автономно.

---

## ✅ `_base/` не создавался для четырёх приватных реп — найдено и исправлено 27.08.2026

**Найдено по прямому вопросу владельца** («почему база не раздана в
personal-finance-dss?»): `sync_base_local.py --all` обновляет **только** те
репы, где `_base/` уже существовал (`d.name != "base-repo" and
(d / "_base").is_dir()`) — он не создаёт папку с нуля. Четыре приватные репы
никогда не получали `_base/` вообще (не отставание — полное отсутствие):
`personal-finance-dss`, `character-a-analysis`, `portrait-of-taste`,
`mission-control`.

**Исправлено в этом же заходе:** все четыре получили `_base/` v3.59.4 через
`sync_base_local.py <репа>` (одиночный режим создаёт папку, если её нет).
Проверено: среди оставшихся реп без `_base/` — только публичные
(`algorithms-site`, `claude-usage`, `finpilot`, `game-analytics-engine`,
`salvation`, `vevdokimovm.github.io`, `vevdokimovm`, `vk-graph`), где `_base/`
не раздаётся намеренно по правилу ADR-004 (приватность).

**Ничего не осталось решить** — записано для истории, не как открытая
задача. Стоит проверить в `sync_base_local.py`, не завести ли `--all`
режим, который создаёт `_base/` и для реп без него (сейчас `--all` тихо
пропускает такой случай — тот самый баг, который скрывал эту дыру).

---

<!--
ЗАПОЛНЕНИЕ

ГЛАВНОЕ РАЗДЕЛЕНИЕ (`00-infrastructure/45-roadmap-and-tasks.md`):
  ROADMAP.md — что построить В ПРОЕКТЕ. Это может сделать ассистент.
  TASKS.md   — что сделать ВЛАДЕЛЬЦУ за пределами репы.

Задача, которую может закрыть ассистент, в этом файле — ошибка: её место в ROADMAP.

ФОРМАТ:
  - [ ] <Что сделать, в прошедшем времени — приём владельца, мозгу так проще>
        **Разблокирует:** <что становится возможным после выполнения>

Закрытое переезжает в TASKS_HISTORY.md батчем при релизе — файл истории заводится
в момент первого переноса, не заранее.
-->
