# base-repo — ROADMAP

> **Как читать.** Приоритеты, не вехи (`00-infrastructure/45-roadmap-and-tasks.md`).
> Здесь — **только открытое**. Где стоим — `WATCHLOG.md` §0,
> история сделанного — `CHANGELOG.md`.
>
> **Дисциплина указателя:** закрыл батч → обнови «СЛЕДУЮЩАЯ ЗАДАЧА» здесь и «Следующий шаг»
> в `WATCHLOG` §0. Они обязаны совпадать.
>
> Заведён 20.08.2026: база предписывала `ROADMAP.md` каждой репе, а сама его не имела —
> открытые задачи жили в журнале и внутри плана кампании, то есть были не видны никому,
> кто не знал о существовании кампании.
>
> ### Разделение с планом кампании — чтобы не разъехались
>
> | Файл | Отвечает на вопрос | Горизонт |
> |---|---|---|
> | **этот `ROADMAP.md`** | что открыто **у репы** | переживает кампании |
> | `05-infra-synthesis-lab/runs/<прогон>/PLAN.md` | как устроен **конкретный прогон**: фазы, порядок, обоснование, опись | живёт и умирает вместе с прогоном |
>
> **Правило:** здесь — **одна строка на кампанию** со ссылкой и статусом, а не копия её плана.
> Детализация остаётся в плане прогона. Кампания закрылась — строка уходит в историю,
> план прогона остаётся как документ (`21-revision-protocol.md`: замороженное не переписывается).
>
> Дублировать содержимое плана сюда **запрещено**: две копии задач расходятся молча,
> и через неделю непонятно, какая настоящая (`67-information-compression.md` §2, единый
> источник истины).

**СЛЕДУЮЩАЯ ЗАДАЧА:** ✅ **Заголовок P1 исправлен 28.08.2026** (был «идёт, фаза
2 из 6» при кампании, закрытой ещё 22.08.2026 — счёт 57 из 57, `WATCHLOG.md`
§0). Открытое сейчас — секции `P0`/`P0.5`/`P2` ниже. Общее правило остаётся:
любой раздел старше нескольких дней сверять с диском перед тем, как выполнять
построчно (`PIT-037`) — заголовки и статусы отстают от факта чаще, чем кажется.

> 🔴 **Два шага перед первым замером новой репы, оба из этой вахты:**
> 1. `find ~ -type d -name "<репа>" -not -path "*/.git/*"` — **без списка каталогов**.
>    Копий больше одной → установить канон и записать его в `decisions.md` до счёта
>    (`SYN-017`). Одинаковый `VERSION` идентичности не доказывает — только `diff -rq`.
> 2. **Файлы корня читаются первыми** (`BOARD`, `ROADMAP`, `BACKLOG`, `WATCHLOG`):
>    там лежит то, что репа уже знает о себе. Мерить — после (`SYN-019`).

Метод — `PROCEDURE.md`: вычитание вместо выбора · при сомнении повышать (закон 4) ·
частотный сигнал §3а · **сначала искать готовый инструмент** · **хеш надёжен только
при одинаковой нормализации обеих сторон**.

**P1.5 закрыт 20.08.2026:** мост запущен, проверен на двух репах, доска разрезана
по одной секции на репу, доставленное разобрано. PIT-088 остаётся в §P2, раздачу
не блокирует.

---

## P1 — кампания «большой синтез инфраструктуры» · ✅ СТАТУС: ЗАКРЫТА 22.08.2026

🔴 **Заголовок исправлен 28.08.2026** — до этой правки он всё ещё говорил
«идёт, фаза 2 из 6, пройдено 5 из 57», хотя кампания давно закрыта (см. врезку
в начале файла и `WATCHLOG.md` §0: «все 57 репозиториев пройдены, ноль
пропусков §6а»). Это тот же класс расхождения, что и врезка выше уже
предупреждает — заголовок раздела не обновился вслед за фактом.

Свести служебки всех 57 реп в базу, ничего не потеряв — сделано.

**Прогресс, порядок реп, обоснование и опись — в плане прогона:**
`05-infra-synthesis-lab/runs/2026-08-20-first-synthesis/PLAN.md` §3 P1.3.

Опись решений — в `decisions.md` прогона.

---

## P1.5 — ✅ МОСТ ПЛАНИРОВЩИКА · ЗАКРЫТ 20.08.2026

Решение владельца 20.08.2026: заняться этим **до** продолжения синтеза. Сделано:
мост запущен по-настоящему (`portrait-of-taste`, затем `base-repo`), повторный прогон
даёт `no-op`, два guard'а в скрипте, доска разрезана по одной секции на репу,
доставленное в базу разобрано в §P2. Разбор решений — `decisions.md`, запись
«2026-08-20, вечер». Контракт — `00-infrastructure/74-planner-bridge.md`.

**Осталось открытым, но раздачу не блокирует:** PIT-088 (деплойер стирает файл
доставки) — в §P2; наполнение доски для 38 реп без секции — за еженедельным разбором
планировщика, задачи нельзя сочинять.

- [x] **🔴 Доска — узкое место. ✅ ЗАКРЫТО 26.08.2026.** Замер 2026-08-20 (19 реп из 57,
      три общих среза, 38 без секции) — устарел, сверка на диске не показала ни одной
      из трёх находок: `base-repo`/`mission-control` уже раздельные заголовки,
      `finpilot`/`personal-finance-dss` тоже, `vk-graph` — свой (health-report-generator
      и bron-kerbosch удалены с 25.08, секции закрыты). Проверено дословно: сухой прогон
      `push-inbox.sh` по каждой из **62** секций `BOARD.md` — **0 отказов guard 1**.
      Единственный реальный пробел — `vevdokimovm` (профиль GitHub) без секции — добавлен.
      Похоже, находка была починена кампанией `PIT-142`/манифест-завершения (25–26.08),
      но чекбокс не сняли. Проверка — `mission-control/scripts/push-inbox.sh <репа>`.
- [x] **Прогон моста — сделано 20.08.2026.** Первая реальная доставка: `portrait-of-taste`,
      sha `9321cfad`, `script=1.1.0`. Повторный прогон — `no-op` по хешу среза.
      По дороге поставлены два guard'а (`push-inbox.sh` 1.1.0, опубликован `bdfd0dfa`):
      срез обязан принадлежать одной репе; приёмник обязан существовать. Оба поймали
      реальные случаи — `base-repo`/`mission-control` и `it-base`
- [x] **Принимающая сторона неполна. ✅ Закрыто 26.08.2026.** Заведены из шаблонов
      (`templates/ROADMAP_TEMPLATE.md`, `TASKS_TEMPLATE.md`): 25 `TASKS.md` + 1 репа
      (`vevdokimovm`) без обоих файлов + 1 `ROADMAP.md` (`research-engine`, была
      только `TASKS.md`). Файлы — скелет шаблона с плейсхолдер-пунктом, не выдуманное
      содержимое (шаблон это и предполагает: «завести», не «заполнить»). Единственный
      сознательно пропущенный — `personal-finance-dss` (прямой запрет владельца трогать
      репу, там активная параллельная разработка). `система_репозиториев/base-repo`
      v1.42.0 — не в счёт, живая база оба файла имеет, и на GitHub они есть.
- [x] **Дочитать `mission-control`** — 🏁 **все 21 протокол прочитаны 21.08.2026**,
      плюс 11 ADR и 4 отчёта. Поднято в базу: `76` классы реп · `77` сплошная ревизия ·
      `78` вложение базы · блоки в `04`, `45`, `58`, `59`, `71`, `72`, `73`, `08`, `32`, `44`
- [x] **Свести `mission-control/scripts/` (24 файла)** — закрыто 27.08.2026:
      `deploy.sh`-дубль удалён (подтверждено по диску 28.08: файла больше нет).
      🔴 **Остальные 23 — не дубли, а собственные инструменты mission-control** для
      его роли репы-планировщика синтеза: `sync-base.sh`/`collect-base.sh`
      (раздача/диагностика `_base/` через git, а не локальные файлы — возможно,
      именно это имел в виду ADR-004 под «deploy.sh --sync-base», не отсутствующий
      флаг в `templates/deploy.sh`, см. `PIT-141` — стоит свериться с текстом ADR),
      `collect_to_base.py` (обратное направление — собирает служебки ИЗ реп В базу,
      отвечает на вопрос дампа задач 24.07 «как аккумулировать служебки со всех
      реп»), `repos_map.py` (правила авторства прозы `repos-map.md`), `system_audit.py`/
      `audit_service_coverage.py` (аудит по стандарту mission-control, не по
      стандарту base-repo — другая область), `migrate_old_repos.py` (импорт из
      внешних экспорт-архивов, не из системы реп), `push-inbox.sh`/`build-inbox.py`/
      `import-notes.py`/`todoist_export.py` — собственный inbox/todoist контур,
      без аналога в base-repo.
- [x] **Три копии `mission-control` на диске** — устарело: миграция на
      `~/repos/` (`88-local-repo-location-standard.md`) упразднила все старые
      расположения. Подтверждено 28.08: `~/vault` не существует, `~/Documents/
      mission-control` не существует, единственная копия — `~/repos/
      mission-control`.

---

## P1.6 — АВТОМАТИЗАЦИЯ ВАХТЫ: агенты, скиллы, гейты · СТАТУС: начато 21.08.2026

**Почему в P1, а не в P2.** Впереди **51 репа** с одинаковым механическим входом. Каждая
делегированная механика — это не только дешевле, но и **длиннее вахта**: отчёт субагента
приходит вместо пятисот строк листинга в контексте. Первый прогон на `dota-dossier`
(5148 файлов) это подтвердил и попутно нашёл вложенную legacy-базу с отставанием
на 55 версий, на которую наведён не был.

**Обоснование выбора модели и усилия — `reports/adr/adr_002_model_and_effort_for_campaign.md`.**
Общий стандарт жанра — `69-agents-hooks-and-gates.md` §3 и §4 («правило выбора:
хук / скилл / субагент»).

### Железное правило этого блока

> **Агент перечисляет и считает. Решает вахта.**
> Как только агенту требуется список из головы или вердикт «дубль / не дубль» —
> он не пишется (`PROCEDURE.md` §7). Нарушение этого правила и есть тот способ,
> которым четыре инструмента подряд теряли материал (PIT-074).

### Сделано

- [x] **`repo-inventory`** (sonnet) — механический вход в репу: копии от `~` без списка
      каталогов, четыре листинга, `sha256`, скелет арифметики §6. Заведён 21.08.2026,
      обкатан на `dota-dossier`. Внутрь зашита починка команды §1.3 (`-mindepth 1`).
- [x] **`base-coverage-probe`** (sonnet) — где термины встречаются в базе, структура
      ближайших документов, дословные цитаты, отдельный список «чего не нашлось».
- [x] **Гейт дублей сквозных реестров** + канарейка — `scripts/revision_check.py`.

### 🔴 P1.6a — АВТОНОМНАЯ РАБОТА БЕЗ ВЛАДЕЛЬЦА · заказ 21.08.2026

**Постановка владельца дословно:** *«чтобы я мог уехать на неделю на дачу и не переживать —
один раз сказал „продолжай“, и он просто работает, исчерпав весь недельный лимит; сам
принимает адекватные решения, ничего не спрашивает, все проблемы — провалы интернета,
VPN, чего-то ещё — решает сам, пробует пока не получится или ищет другие варианты».*

> **🔴 Ресёрч по этому пункту УЖЕ СДЕЛАН — не начинать заново** (`PROCEDURE.md` §3а, PIT-072).
> `00-infrastructure/51-autonomous-agent-loop.md` покрывает: §1 headless · §2 разрешения ·
> §3 `settings.json` · §5 сеть, VPN, обрывы (`CLAUDE_CODE_MAX_RETRIES`,
> `CLAUDE_CODE_RETRY_WATCHDOG=1`, `API_TIMEOUT_MS`) · **§6 упор в лимит и продолжение
> после сброса** · §7 Routines · §8 watchdog на `launchd` с `--resume` по `session_id` ·
> §9 таблица выбора. Плюс `06-autonomous-mode-kit/` — восемь законов, `SWITCHES.md`,
> `LAUNCH_BRIEF.md`, `CHECKLIST.md`, `RUNS.md`.
>
> **Пробел не в знании, а в том, что ничего из этого не собрано и не проверено на живом
> прогоне.** Задача — сборка и приёмка, а не исследование.

- [x] **Выбрать контур по таблице `51` §9 и зафиксировать выбор ADR'ом.**
      **Сделано 21.08.2026 — `reports/adr/adr_003_autonomous_contour.md`.** Решение:
      **два контура, разделённые признаком локальности** (нужен ли задаче доступ
      к файлам этой машины). Кампания синтеза — локальный, ресёрчи — Routines.
      В ADR записано и то, чем это решение будет опровергнуто.
      От владельца нужно одно: подтвердить критерий (`TASKS.md`).
      <details><summary>исходная формулировка</summary> Развилка одна:
      **Routines** (облако Anthropic, переживает сон ноутбука и обрыв VPN **по построению**,
      без permission-picker) против **локального watchdog** (`launchd` + `--resume`).
      Критерий из `51` §9: нужен ли задаче доступ к **локальным** файлам этой машины.
      Кампания синтеза читает `~/Documents/система_репозиториев/` — **значит локальный**;
      но часть задач (ресёрчи, разборы) локального доступа не требует и уходит в Routines.
      **Скорее всего ответ — оба контура, разделённые по признаку локальности.**
      </details>

- [x] **Собрать локальный контур** — сделано 21.08.2026:
      `06-autonomous-mode-kit/bin/autonomous_loop.sh` (цикл с `--resume`, ожидание
      сброса лимита по машинному времени, backoff 30/60/120/240/300 с джиттером ±15 %,
      журнал на каждую итерацию, три стоп-условия) · `bin/limits_watch.py` ·
      `bin/com.vasyaevdokimov.claude-autonomous.plist` (`SuccessfulExit=false`,
      свой `PATH` — launchd не читает `~/.zshrc`).
      **Скрипт отказывается стартовать** без брифа и без раздела `## СТОП` в нём.
- [ ] **🔴 ПРОГНАТЬ ВЖИВУЮ — контур без этого не собран.**
      **Приёмка — не «скрипт написан», а прогон длиннее суток с искусственным обрывом сети
      посередине** (`71` §7в: проверка, которую нельзя провалить, — не проверка).
      Что проверять: переживает ли `--resume` разрыв · срабатывает ли ожидание лимита
      на реальном упоре · выходит ли цикл по `STOP` в течение минуты · не засыпает ли
      ноутбук посреди прогона (`caffeinate -i`).
      **Ключи, найденные уже после сборки и в контур пока не вшитые** (`51` §5а):
      `--max-budget-usd` (потолок трат, исполняется самим CLI — единственное
      стоп-условие, не зависящее от дисциплины агента), `--fallback-model` (переживание
      перегруза модели), `--session-id` (задать id вместо парсинга ответа).
      **Вшивать после первого живого прогона**, не до: иначе непонятно, что именно
      сломалось.

- [x] **🔴 Автопродолжение после сброса лимита. ✅ ЗАКРЫТО 26.08.2026 — уже реализовано.**
      Зависимость («файл лимитов» ниже) была закрыта раньше, чем этот пункт проверили —
      сам пункт остался непомеченным. `autonomous_loop.sh` (готов, см. «Собрать локальный
      контур» выше) уже делает ровно это, причём лучше исходного плана: не парсит строку
      `You've hit your... limit`, а спрашивает `limits_watch.py` машиночитаемый `resetsAt`
      из поля `quotaLimits` самого лога сессии — часовой пояс и языковая локализация текста
      не участвуют вовсе. Ждёт ровно `resetsAt`, не дольше, и продолжает `--resume`.
      `launchd` не понадобился: постоянный процесс `autonomous_loop.sh` держит цикл сам,
      пока жив терминал — ровно тот охват, для которого пункт писался (переживание
      закрытия терминала — отдельная, более широкая задача, см. «ПРОГНАТЬ ВЖИВУЮ» выше
      и `night.sh`/Routines, не заявлено этим пунктом).

- [x] **Стоп-условия — пишутся ДО запуска, не после.**
      Сделано: `06-autonomous-mode-kit/STOP_CONDITIONS.md` — пять стоп-классов
      (необратимое вовне · секрет · расхождение, требующее владельца · потеря опоры ·
      выход за мандат) **плюс список того, что стопом НЕ является**. Второе — половина
      ценности: без него агент останавливается «на всякий случай» и неделя простаивает.
      Правило деления, применимое не думая: **останавливаемся там, где ошибка
      не откатывается**; важность оценивается по-разному в 3 часа ночи и утром,
      обратимость — одинаково.
      <details><summary>исходная формулировка</summary> `06-autonomous-mode-kit/STANDARD.md`
      закон 2: «стоп-условия сильнее продуктивности». Автономный прогон обязан
      останавливаться на: необратимом действии вовне (деплой, удаление, публикация) ·
      секрете в материале (`32-…`) · расхождении, требующем решения владельца.
      **Всё остальное — решать самому и не спрашивать**, это прямое требование заказа.
      </details>

- [x] **Журнал автономного прогона. ✅ СДЕЛАНО 26.08.2026.** По закону 5 кита
      («записано по ходу — иначе не было»): `06-autonomous-mode-kit/bin/auto_log.py`
      дописывает в `06-autonomous-mode-kit/runs/auto.log` (tab-separated: время · репа ·
      тип · описание; типы `batch`/`skip`/`stop`/`wait`). Подключено в чек-лист батча
      и в пропуск шага скиллы `auto` (`.claude/skills/auto/SKILL.md`). Журнал начат
      с нуля этой правкой, задним числом не заполнялся — цена восстановления
      прошлых батчей из `CHANGELOG.md` ниже пользы от точной реконструкции.

### P1.6b — ЛИМИТЫ КАК ДАННЫЕ · заказ 21.08.2026

- [x] **Файл, который знает, когда восстанавливаются лимиты** — сделано 21.08.2026:
      `06-autonomous-mode-kit/bin/limits_watch.py` → `limits.json`.
      🔴 **Источник оказался лучше записанного.** Роадмап называл `/usage` — интерактивную
      команду, которой в headless нет. Фактически Claude Code пишет отказ прямо в лог
      сессии полем `quotaLimits` с **`resetsAt` unix-таймстампом**: ни парсинга экрана,
      ни таймзон, ни языка интерфейса. Окна на этой машине — `five_hour` и `seven_day`,
      40 упоров в логах.
      **Две оговорки, обе стоили ошибки в первой редакции:** запись появляется только
      в момент отказа (остатка лимита в логе нет и он не оценивается); **отказ ≠ блокировка
      до `resetsAt`** — в живом логе после отказа шли 149 успешных ходов, поэтому
      блокировка определяется фактом успешного хода, а не временем.
      **Замер расхода — рядом, но отдельно:** `reports/experiments/token-consumption/session_metrics.py`.
      Смешивать нельзя: это разные величины.
      <details><summary>исходная формулировка</summary>
      </details>
      **Разблокирует:** и автопродолжение (§P1.6a), и планирование тяжёлых батчей
      «успею ли до сброса». Смежное — `49-token-economy-and-prompting.md` (замеры расхода),
      `33-token-budget-and-modes.md`.

- [x] **Подробный анализ моделей и усилий + рекомендация ПО КАЖДОЙ РЕПЕ. ✅ ЗАКРЫТО
      26.08.2026.** Раскладка сделана по классам (`76-repo-classes.md` §1), не по 57
      репам поимённо — институт переживает раскладку, а раскладка меняется с каждой
      новой репой, ровно как и предлагалось. Добавлены `66-model-and-effort-selection.md`
      §3б («классы репозиториев → модель по умолчанию», 8 классов × дефолт × почему) и
      §3в («качество выхлопа на токен» — определение, формула `находок / (токены/100K)`,
      способ снять число из уже существующих `auto.log`+`session_metrics.py` без нового
      инструмента). Критерий переключения — одна строка: класс из `.repo-class` даёт
      стартовый дефолт, дерево §3 работает поверх него на конкретной задаче.
- [ ] 🆕 **Автоматизировать «качество на токен»** (`66` §3в) — сейчас снимается вручную
      за несколько минут раз в несколько батчей; собрать в один скрипт (сопоставление
      таймстампов `auto.log` с рядом контекста `session_metrics.py` + подсчёт находок по
      ключевым словам в `note`) имеет смысл, когда ручной подсчёт станет узким местом,
      не раньше — тот же критерий, что решал `/tokens` про вызов агента.

### P1.6c — ИНСТРУМЕНТЫ, КОТОРЫЕ НАДО ИЗУЧИТЬ ПЕРЕД ИНТЕГРАЦИЕЙ

- [x] **Dynamic workflows / `ultracode`** — **выяснено 21.08.2026, интегрировать нечего.**
      В `claude --help` версии **2.1.238** нет ни команды, ни ключа `ultracode`.
      Есть `ultrareview` — облачный многоагентный ревью ветки или PR (он же
      `/code-review ultra`). Слово «workflows» встречается **один раз** — в перечне того,
      что отключает `--safe-mode`, то есть это **категория кастомизации**, а не механизм
      запуска. Разбор — `51` §5а. Перепроверяется одной командой: `claude --help | grep -i ultracode`.

- [x] **`/remote-control` и управление сессией с телефона** — разобрано, `50` §4.
      Ключи есть: `--remote-control [имя]` и `--remote-control-session-name-prefix`
      (по умолчанию hostname). **Граница подтверждена и уточнена:** Remote Control даёт
      **канал управления своей сессией**, а не смену её владельца — запись
      `SWITCHES.md` §4 («передать живую сессию другому аккаунту нельзя») этим не
      отменяется. Для недельного прогона это единственный способ вмешаться,
      не подходя к ноутбуку.

- [x] **Работа в нескольких окнах и «арках»** — разобрано, `50` §4, **с замером**.
      🔴 **Главное: живые сессии перечисляются машинно** — `claude agents --json`
      отдаёт `pid` · `cwd` · `kind` · `sessionId` · `name` · `status`. Значит правило
      против коллизии версий стоит одну команду **перед** стартом вахты по репе,
      а не разбирательство после.
      **Замер 21.08.2026:** шесть живых сессий, по одной на репу, **пять `idle`
      с 19–20.08** — висят сутками, удерживая контекст. Дублей по каталогу нет.
      Развести работу физически: `-w/--worktree` · `--tmux` (нативные панели iTerm2) ·
      `--cloud` / `--teleport` / `--environment`. Имя сессии (`-n`) видно в поле ввода,
      в пикере `/resume` и в заголовке терминала — это и есть дешёвая маркировка «что здесь идёт».

- [ ] **Эстетика Claude Code + iTerm.** 🔴 **Практическая, а не декоративная задача:**
      у владельца тёмный фон, и **тёмные цвета в выводе не видны совсем**. Настроить тему
      Claude Code (`/config`) и профиль iTerm; в собственных скриптах брать только яркие
      коды `91`–`97` (образец — `templates/deploy.sh`, тест `I4` набора базы уже это
      проверяет).

### Очередь — по убыванию окупаемости

- [ ] **`acceptance-auditor`** (sonnet) — контрольная сверка **закрытой** репы: сходится ли
      записанная арифметика §6 с диском сегодня, не появились ли копии, не разъехались ли
      числа. **Первый прогон по пяти закрытым репам запущен 21.08.2026** — оформить
      результат в постоянного агента.
      **Разблокирует:** доверие к «репа закрыта». Сейчас это утверждение проверяется
      только чтением журнала, то есть не проверяется.

- [ ] **`dir-sampler`** (sonnet) — для каталога, размеченного `?`, открыть 2–3 файла
      и **пересказать, что внутри**, не предлагая отметку. `PROCEDURE.md` §2 прямо требует
      разрешать сомнение открытием файла, а не рассуждением о названии; открытие
      механическое, отметка — суждение вахты.
      **Разблокирует:** разметку крупных реп, где каталогов десятки (`dota-dossier` — 500+).

- [ ] **Скилл `/synthesis-pass`** — процедура прохода по репе одним вызовом: порядок шагов,
      что делается субагентом, что вахтой, чем закрывается приёмка. Сейчас порядок
      восстанавливается чтением `PROCEDURE.md` каждый раз, и он **уже дважды менялся**
      за одну вахту (`SYN-017` — сначала копии; `SYN-019` — корень первым).
      **Разблокирует:** одинаковый вход в 51 репу без перечитывания процедуры.

- [ ] **Скилл `/repo-accept`** — ритуал приёмки: арифметика §6, запись в `decisions.md`,
      обновление `PLAN.md` §3 P1.3, строки в `WATCHLOG` §0 и `ROADMAP`. Пять мест,
      которые правятся вместе и **уже расходились**: указатель врал про «3 протокола
      из 21» при прочитанном 21.

- [x] **Хук на `_base/`. ✅ ЗАКРЫТО 26.08.2026.** `.claude/hooks/protect-base-mirror.sh`
      (`PreToolUse` на `Edit|Write|MultiEdit`) блокирует правку любого пути, содержащего
      `_base/`, `exit 2` с объяснением — та же схема, что `block-destructive.sh` у
      `personal-finance-dss`. Проверено тремя синтетическими случаями: путь внутри `_base/`
      блокирован, обычный путь пропущен, непонятный вход (`Bash`-команда без `file_path`)
      пропущен, а не упал. Живой повод — `PIT-147` (симметричный риск: правка ВНУТРИ уже
      розданного зеркала, не порча корня канона, но того же семейства «чужое пространство»).
      🔴 **Охват — только эта сессия (`base-repo` как корень проекта).** Хук ловит путь
      к ЛЮБОЙ репе, но действует, пока хуки читаются из `base-repo/.claude/settings.json` —
      сессия с другим корнем проекта (`mission-control`, `personal-finance-dss`, любая
      сателлитная репа) хук не унаследует, пока не закрыт следующий пункт.

- [x] **Раздать `.claude/` дочерним репам. ✅ ЗАКРЫТО 26.08.2026 — узкий набор v1.**
      Решение: базовый набор v1 — **только** `protect-base-mirror.sh` (хук защиты `_base/`,
      предыдущий пункт) + `settings.json` на него. Агенты/скиллы **намеренно не входят** —
      они кампанейские инструменты синтеза, не универсальная защита; риск конфликта с
      будущей продуктовой настройкой репы у одного узкого хука почти нулевой, у целого
      набора агентов — нет. `scripts/distribute_claude_kit.py` (сухой прогон по умолчанию,
      `--apply` пишет): роздано **57 из 57** репов без своего `.claude/settings.json`
      (`personal-finance-dss` пропущен — уже имеет продуктовый набор, `base-repo` — источник).
      Проверено вживую на `edu-base`: хук блокирует правку `_base/` в её собственном
      `.claude/`, не только в сессии с корнем `base-repo`.
      🔲 **Осталось открытым:** расширение набора (агенты/скиллы по классу `76`) — решение
      будущей правки, эта закрывает только минимальную защиту `_base/` системно.

### Что осознанно НЕ делаем

| Идея | Почему отклонена | Когда пересмотреть |
|---|---|---|
| Агент, решающий «поднимать или дубль» | это суждение по длинному контексту — ровно то, что делегируется хуже всего; и это класс PIT-074 | не пересматривать без смены самого метода |
| Агент, который сам правит базу | «нашёл» и «починил» разводятся намеренно (`69` §3 правило 3): чинящий агент не даёт вахте принять решение | — |
| Свои скрипты там, где есть готовые | `legacy_delta.py`, `drift_versions.py`, `per_repo_audit.py`, `audit_coverage.py` уже покрывают инвентаризацию; PIT-072 | если у готового нашёлся названный потолок |
| Полная автоматизация выбора модели | ручка не на стороне модели (`ADR-002`, приложение); автоматизируется только делегирование | если появится переключение модели из сессии |

---

## P0 — очередь владельца от 22.08.2026 (после закрытия кампании)

Задачи заданы владельцем списком. Порядок — его, кроме случаев, где зависимость обратная
(отмечено).

> ### 🔴 Где какая задача живёт — правило заведено 22.08.2026
>
> Заказ владельца дословно: *«обнови роадмапы, таски, бэклоги, чтобы нахер не путалось
> и не терялось ничего, почему абардак такой»*. Бардак измерен, а не объявлен:
> **950 открытых задач** в четырёх независимых местах, и ни одно не знает про остальные.
>
> | Место | Что там живёт | Замер 22.08 |
> |---|---|---|
> | `base-repo/ROADMAP.md` | работа над инфраструктурой **системы** | 77 |
> | `base-repo/TASKS.md` | то, что требует решения/действия **владельца** | 24 |
> | `mission-control/BOARD.md` | доска планировщика, срез **на репу** | 120 в 55 секциях |
> | `<репа>/ROADMAP.md`, `TASKS.md` | работа **внутри** конкретной репы | 729 в 41 репе |
>
> **Правило:** задача пишется туда, где находится её предмет. Работа над базой — в ROADMAP
> базы; решение, которое может принять только владелец, — в TASKS; работа внутри репы —
> в файлы этой репы, а на доску идёт **срез**, а не дубль.
>
> 🔴 **Измеритель:** `python3 scripts/tasks_census.py` — сводит все четыре места в одну
> картину и показывает **разрывы**, которых иначе не видно:
>
> | Разрыв | Найдено 22.08 | Чем грозит |
> |---|---|---|
> | на доске есть, своих файлов нет | **17 реп** | задача не доедет до репы (мост требует приёмник) |
> | работа идёт, доска не знает | **5 реп** | планировщик не видит загрузку |
> | реп без `ROADMAP`/`TASKS` вовсе | **20** | некуда положить задачу |
> | секций доски без репы на диске | 2 | `finpilot` (живёт рабочей папкой), «Общесистемное» |
>
> Ключи: `--dupes` — похожие формулировки между уровнями, `--repo ИМЯ` — разрез по репе.
> Скрипт **измеряет, но не сливает**: где чему место — суждение вахты (`71` §7г-бис).

- [x] **0. Кампания переноса `~/Documents` → система реп. ✅ ЗАКРЫТА 22.08.2026.**
      Не была в списке владельца, заказана по ходу: *«перенести всю информацию с мака
      в репы и удалить всё»*. `~/Documents` 7.4 → **5.8 ГБ**, `~/Downloads` 163 → **8.7 МБ**,
      43 500+ файлов, 24 папки-источника → 0. Отчёт —
      `reports/migrations/2026-08-22-documents-to-repos.md`.
      🔴 Хвост: раскладка назначалась по имени папки (`PIT-124`) — разбор в §P2.

- [x] **1. Стандарт прогонов + оптимизация скорости. ✅ ЗАКРЫТ 22.08.2026.**
      ✅ Стандарт — `05-infra-synthesis-lab/STANDARD.md` §00, выведен из прогона 57 реп.
      ✅ **Оптимизация — `scripts/close_batch.py`:** ритуал закрытия батча (гейт → `VERSION`
      → секция `CHANGELOG` → `WATCHLOG` §0 → §3 с подрезкой до десяти → статус `README`
      → архив) сведён из 5–7 вызовов **в одну команду**. Замер узкого места подтвердился
      на этой же сессии: **10 версий**, то есть ритуал исполнялся десять раз.
      🔴 «Без потери качества» закрыто устройством: `revision_check.py` внутри скрипта,
      прогон **до** ритуала и **после** правок; DRIFT — батч не закрывается.
      Границы названы: механика машине, содержание вахте.

- [ ] **2. Ревизия базы и каждой репы, протокол общий + по каждой.**
      **Сделано 22–23.08.2026:**

      | Что | Результат |
      |---|---|
      | дубли внутри базы | 29 групп → **4**, два машинных критерия найдены |
      | копии канона в локальных инфраструктурах | 407 → **0** (`ADR-004`) |
      | классы реп | 61 из 61 размечена |
      | 🏁 **импорт кампании переноса** | разобран в **15 репах из 15**, ≈13 500 файлов разложено, 821 дубль снят |
      | мёртвые пути в README реп | 183 находки → **8**, из них 2 намеренные |
      | манифесты | **11 из 61** (+`it-base`, `misc-vault`, `legal-knowledge-base`, `self-map`, `dota-dossier`) |
      | 🆕 **документы → текст** | ✅ **99.9%** (1511 из 1513 pdf/docx/pptx/xlsx, 27.08.2026); было 23 → 543 → теперь +472 вторым проходом; 74 скана без текстового слоя ждут OCR; 2 файла — 0-байтные husk-дубли, не читаются в принципе (`TASKS.md`) |
      | 🆕 **изображения → текст** | метод поднят в `07-media-to-text-lab/METHOD_IMAGES.md`; очередь **173 серии / 1 495 кадров**; свалка скриншотов: 86 из 355 разобрано, 292 → 223 МБ |

      🔴 **Найдено владельцем 23.08 (PIT-138):** ревизия свелась к механике — раскладке,
      дедупу, путям, манифестам, — потому что механика отчитывается числами. Замер:
      5 236 медиа-файлов, выжимку имели 23 (**0.4 %**). Отсюда обе строки «→ текст» выше.

      ✅ **Манифесты — закрыто 26.08.2026.** 58 из 58 сателлитных репозиториев несут
      `MANIFEST.md` (кампания `05-infra-synthesis-lab/runs/2026-08-25-manifest-completion`,
      QUEUE.md 47/47 + 11 уже существовавших на старте кампании; `base-repo` не считается —
      он сама база). Инструменты для машинной части остаются готовы: `repo_revision.py`
      (мёртвые пути, числа в прозе, штамп зеркала), `import_dedup.py` (дубли и баланс
      приёмки), `git_limits_check.py` (лимиты git), `secrets_scan.py` (секреты по сигнатурам).

      ✅ **Дедуп по хешу — закрыто 26.08.2026.** Блокер материализации снят
      (`pack_release.py --force-stubs`, `PIT-145`, прочитал все файлы при сборке архива).
      `import_dedup.py --repo it-base` и `--repo misc-vault` — по 0 дублей в обеих.
      `import_dedup.py --all` по всей системе — чисто. 🔴 **Хвост «7 файлов в
      `research-craft/researches/`» закрыт 28.08.2026:** сверка по размеру/заголовку
      показала — это дубли уже переработанных FINPILOT-методичек
      (`personal-finance-dss/docs/research/`), удалены как дубль, не разложены заново.
      `import_dedup.py --all` подтверждает: остаётся к раскладке **0**.

      ✅ **`base-repo` и `finpilot` перенесены в `система_репозиториев` — 26.08.2026,
      прямое требование владельца** («рабочая оригинал единственная папка репы должна
      быть в система_репозиториев»). Классификатор авто-режима заблокировал `rm -rf`/`mv`
      на пути `~/Documents/base-repo` (это рабочая директория харнесса) — выполнено
      самим владельцем через `!`. `~/Documents/base-repo` и `~/Documents/finpilot`
      теперь симлинки на `система_репозиториев/base-repo` и `.../finpilot`.
      🔴 **Находка по ходу:** `finpilot` и `personal-finance-dss` — **один и тот же**
      GitHub-репозиторий (`vevdokimovm/personal-finance-dss`, обе VERSION 8.28.0) в двух
      локальных копиях: `personal-finance-dss` (87 МБ, чистый кампанейский снимок) и
      `finpilot` (722 МБ, живая dev-копия с `node_modules`/`.venv`/git, там же прямо
      сейчас работала отдельная активная сессия Claude Code — PID подтверждён `lsof`).
      Не консолидировано намеренно — владелец попросил только надбавки без удаления;
      описание/теги подтянуты на GitHub из уже написанного `personal-finance-dss/MANIFEST.md`.

      🔴 **Строка «копии канона в локальных инфраструктурах: 407 → 0» опровергнута
      26.08.2026 (`PIT-142`).** Гейт `base_coverage.py --adr004` матчит копию по точному
      имени файла — переименованный дубль (старые русские имена канона до переезда на
      английский) для него невидим. Найдено на живом примере: `truth-seeking` нёс
      77 файлов дословной копии `base-repo` (включая watchlog/changelog **самой базы**)
      под чужими именами, гейт показывал 0 нарушений. Системный `grep` по маркерной
      строке нашёл тот же паттерн ещё в **11 репах** — из них 8 уже числились
      «отревизированы» в этой самой кампании 22–23.08 (`academic-portfolio`,
      `character-a-analysis`, `christ-walk`, `edu-base`, `family`, `health-vault`,
      `it-base`, `legal-knowledge-base`, `misc-vault`, `self-map`, `portrait-of-taste`).
      ✅ **Почищено 26.08.2026** — все 11 репов (тот же приём, что закрыл `truth-seeking`)
      плюс `dota-dossier` (отдельный случай: не переименованные копии по маркеру, а
      вложенная папка `base-repo/` целиком, версия v1.36.0) — итого **12 репов**, свежий
      `_base/` роздан `sync_base_local.py`. Публикация (версия/архив/GitHub) для
      `academic-portfolio`, `it-base`, `legal-knowledge-base`, `misc-vault` завершена
      26.08.2026 (см. п. 3 ниже).

- [x] **3. Новые архивы каждой репы для деплоя → `~/Downloads`. ✅ Проверено 26.08.2026.**
      🔴 **Вскрылось при сборке:** упаковщик отказал репам без `.repo-id`/`.repo-class` —
      **17 реп** жили без обязательного минимума `01-repo-standard.md`. Проставлено;
      теперь 61 из 61. Второй упаковщик не понадобился: `pack_release.py` принимает
      путь аргументом.
      **Сверка 26.08.2026:** `gh release view` по всем 58 сателлитным репам с `VERSION` —
      **52 уже задеплоены** на текущую версию (владелец вёл `deploy.sh` параллельно
      всю сессию). Реально отставших было **6**: `academic-portfolio`, `it-base`,
      `legal-knowledge-base`, `misc-vault` (архивы через `--force-stubs`, `PIT-145`),
      `ai-relay`, `master-admission` (17% заглушек, тоже `--force-stubs`). Все шесть
      собраны, `testzip()` чист, описание/теги/`_base/` синхронизированы — архивы лежат
      в `~/Downloads`, ждут `deploy.sh` владельца (сам деплой — стоп-класс §5).

- [x] **4. Обновить базу во всех репах** (раздача `_base/`). ✅ **СДЕЛАНО 22.08.2026,
      ПЕРЕСДЕЛАНО 25.08.2026.** Раздано локально (`sync-base.sh` LOCAL=1 — новый режим,
      а не второй скрипт): **51 репа** → v2.51.0, 9 публичных пропущены предохранителем
      по `isPrivate`, зеркало убрано из публичной `salvation`. Отставших зеркал: 42 → **0**.
      Проверка: `python3 scripts/base_coverage.py --adr004`.
      🔴 **`sync-base.sh` не пережило время — найдено 25.08.2026 (`PIT-141`).** ADR-004
      называет механизмом `deploy.sh --sync-base`; флага в коде нет, раздача была
      разовым ручным действием 22.08, дальше никто её не повторял. Итог: **51 репа**
      отстала на `_base/BASE_VERSION` v2.92.0 против канона v3.22+ — три дня и ~30 версий
      молча. Починено вручную новым скриптом `scripts/sync_base_local.py` (тот же паттерн,
      что `bump_repo.py`/`pack_release.py` — узкий инструмент вместо правки живого
      деплойера) — все 51 репа снова CLEAN на v3.23.0, попутно вычищены артефакты
      iCloud-конфликтов (папки с суффиксом ` 2` внутри `_base/`).
      🔲 **Осталось навсегда: встроить `--sync-base` в `deploy.sh` как настоящий
      постоянный шаг**, а не разовое ручное действие, которое забывают повторить —
      иначе цикл «раздали → забыли → отстало на N версий» повторится в третий раз.
      До тех пор `sync_base_local.py --all` — рабочий обходной путь, гонять его в
      начале каждой ревизионной сессии, не только когда владелец спросит про версию.

      🔴 **Уточнение 26.08.2026: механизм ADR-004 существовал, просто не в той репе.**
      `PIT-141` искал `--sync-base` внутри `templates/deploy.sh` — там его действительно
      нет. Но `mission-control/scripts/sync-base.sh` уже несёт режим `LOCAL=1`
      («раздача в рабочие копии на диске, без GitHub», v2.2, 22.08.2026) — ровно то,
      что цитирует ADR-004 строкой «раздача сделана локально (`sync-base.sh` LOCAL=1)».
      `sync_base_local.py` в этой сессии оказался **параллельным изобретением того же
      колеса** в другой репе, не единственным рабочим путём. Не консолидировано:
      оба инструмента делают одно и то же, `sync_base_local.py` доказанно рабочий
      (гонялся десятки раз за эту сессию), `mission-control`-версия не запускалась
      с 22.08 — решение, какой оставить канон, за владельцем.
      🔲 Осталось: раздача на **GitHub** (штатный режим) — по-прежнему стоп-класс.
      🔴 **Это то, чего не сделала кампания** — операция над 56 чужими репами и стоп-класс
      автономного режима. Измеренная точка старта: покрытие **47 из 57**, версии `_base/`
      в репах расходятся (найдены v1.36.0, v1.41.0, v1.42.0 при базе v2.42.0).

- [x] **5. 🔴 АРХИТЕКТУРНЫЙ ВОПРОС о модели раздачи. ✅ ЗАКРЫТ 22.08.2026** — `ADR-004`
      принят и **исполнен**: правила 1–3 доведены до нуля расхождений, правило 4 стало
      режимом гейта. Гипотеза подтвердилась предельно: из 199 «своих» документов реп
      **ни один** не содержал авторского текста — вся «своя инфраструктура» была
      устаревшим каноном. Ниже — исходная формулировка вопроса.

  > _Исходная формулировка вопроса (закрыт выше):_
      Формулировка владельца: база копируется 1-в-1 в каждую репу, и при этом у каждой репы
      своя инфраструктура — не приведёт ли это к тому, что репа смотрит **в первую очередь
      на своё** и не учитывает багаж базы; не потеряется ли информация; не сделать ли одну
      общую инфраструктуру и наследовать от неё.
      Разбор и предложение — `reports/adr/` (заводится отдельным ADR). Требует ресёрча.

- [x] **6. Перенести рабочую `base-repo` в `~/Documents/система_репозиториев/`. ✅ СДЕЛАНО 26.08.2026.**
      🔴 Делается **последним и отдельным заходом**: сессия работает внутри этого каталога,
      все пути, гейты и скиллы смотрят на него.

      **Проверка снимка повторена машинно 22.08.2026** (прежняя была поимённой):

      | Вопрос | Ответ |
      |---|---|
      | файлов в снимке `v1.42.0` | 300 |
      | отличаются от рабочей базы по хешу | 63 |
      | из них содержат **своё** содержимое | **0** |
      | нет в базе даже по имени | 18 — и это ровно те дубли, что удалены из базы 22.08 (лежат под другими именами в `01-claude-context/` и `reports/`) |

      «Своими» 220 строк выглядели у `WATCHLOG.md` (57), `README.md` (32),
      `00-navigator.md` (21) — все оказались **прежними редакциями**: старые записи
      журнала, старое описание витрины. `00-navigator.md` — копия личного
      `00_НАВИГАТОР.md`, который лежит в `misc-vault/90-imported/claude-instructions/`.

      🔴 **Вывод подтверждён: снимок уникального не несёт**, и его удаление ничего не теряет.
      Но само удаление — часть переноса, а не отдельный шаг: пока рабочая база лежит
      в `~/Documents/base-repo`, снимок мешает только тем, что ревизия видит в нём
      мёртвые пути.

      🔴 **Препятствие снято 22.08.2026 — путь вынесен в одно место.** Было **15 файлов**
      с абсолютным путём строкой; исполняемый код переведён на переменную окружения
      `BASE_REPO` с падением на **место самого скрипта** (`__file__`), а не на константу:

      | Файл | Было | Стало |
      |---|---|---|
      | `scripts/drift_versions.py` | константа | `BASE_REPO` → `parent.parent` |
      | `scripts/per_repo_audit.py` | константа | то же |
      | `scripts/pack_release.py` | константа в fallback | то же |
      | `05-infra-synthesis-lab/tools/scan_lessons.py` | константа | `BASE_REPO` → `parents[2]` |

      **Скрипт лежит В базе и знает, где он** — это надёжнее любой константы и переживает
      перенос сам собой. Проверено запуском каждого после правки.

      Осталось с абсолютным путём: `06-autonomous-mode-kit/bin/…plist` (launchd требует
      абсолютный по построению), `LAUNCH_BRIEF.md` и строка команды в скилле `auto`
      (её печатает владелец) — все трогаются одной правкой в момент переноса.

      **Порядок соблюдён.** Перенос выполнен последней операцией, как и планировалось.
      Прямое требование владельца: *«рабочая оригинал единственная папка репы должна
      быть в папке система репозиториев»*. Реальный путь — `система_репозиториев/
      base-repo`, на старом месте (`~/Documents/base-repo`) — симлинк для
      совместимости с харнессом (он жёстко держит рабочую директорию на этом пути).
      Классификатор авто-режима дважды заблокировал `rm -rf`/`mv` на этом пути —
      выполнено самим владельцем через `!`, содержимое сверено побайтово перед
      заменой (`diff -rq`, 0 расхождений).

---

- [x] 🆕 **Гейт: реестр ADR обязан совпадать с составом каталога. ✅ СДЕЛАНО** —
      реализован как часть `check_living_documents` в `scripts/revision_check.py`
      (проверка «реестр ADR совпадает с составом каталога», v2.46.0). Проверено
      22.08.2026: гейт живой, входит в 16 проверок ревизионного контроля.
      Найдено 22.08.2026 — реестр `reports/adr/README.md` отстал на **две** записи
      (`adr_003`, `adr_004`). Лечение, придуманное 21.08 («реестр врёт заметно»),
      само не исполнялось сутки. Поймала не проверка, а **коллизия номера** при заведении.
      **Что сделать:** в `revision_check.py` сверять строки таблицы реестра
      с `ls reports/adr/adr_*.md`. Класс общий: **список, который ведут руками,
      отстаёт всегда** — вопрос только в том, ловится ли отставание.

---

## P2 — усиливает результат

- [ ] 🆕 🔴 **Разобрать раскладку кампании переноса: адреса назначались по имени папки.**
      Заведено 22.08.2026 по замечанию владельца, отложено им же в следующий батч.
      Разбор — `PIT-124`.

      **Что случилось.** `~/Documents/claude` уехала в `claude-usage` целиком, 152 файла.
      Внутри оказались три разные вещи: **142** файла инфраструктуры Claude
      (`Instruction files`, `Skills`, `.agents`, `Методички`), **29** файлов `Account data`
      и **3** случайных (`ADHD.pptx`, «Сетевые протоколы.docx», PDF про обход региона) —
      последним место в `misc-vault`, как и сказал владелец.

      🔴 **Дефект шире одного случая.** Для спорных папок (`аналитика`, `логика`, `видео`)
      содержимое вскрывалось и допущение называлось строкой. Для папок с «очевидным»
      именем — не вскрывалось вовсе. То есть проверка применялась там, где сомнение уже
      возникло, и пропускалась там, где не возникло, — хотя ошибается именно второй случай.

      ✅ **Пункт 1 (Account data) — ЗАКРЫТ 26.08.2026.** Раскладка уже произошла
      органически (не этой задачей): `claude-account-data` лежит в `misc-vault/04-data/`,
      как предписывалось. `secrets_scan.py` доработан (`--max-mb`, было жёстко 2 МБ —
      именно оно, а не заглушки, было настоящей причиной пропуска: `conversations.json`
      весит 45.8–251 МБ) и прогнан **на реальном содержимом**, оба экземпляра:
      `04-data/claude-account-data/` и `90-imported/claude-account-data/` (второй,
      ранее не упомянутый — найден по ходу). **Найдено по одному срабатыванию сигнатуры
      «пароль в строке подключения» в каждом** (вероятно, дубли одного экспорта на
      J/S-копии) — секрет не показан здесь по правилу стоп-класса, путь и класс
      записаны в `TASKS.md` владельцу на проверку/отзыв. Материализация 251 МБ файла
      прошла без сети зависшей дольше обычного — не потребовала `--force-stubs`.

      🔴 **Сверено с диском 28.08.2026 (авто-режим).** Пункты 1, 3, 4 — устарели
      сами собой: `claude-usage` перестал быть свалкой импортированного контента
      и переродился в отдельный публичный продукт (класс `product`, англоязычный
      README, GitHub-релиз, Python-пакет `claude_usage`) — путь `misc-vault/
      90-imported/from-public-repos/claude-usage` (пункт 3) больше не существует
      физически, чинить нечего. Пункт 1 (Account data) уже был закрыт отдельно
      выше 26.08.2026.

      **Реально осталось открытым — только пункт 2**, и он большой: проверить
      **остальные 19 целей** кампании переноса содержимым, не именем (`PIT-124`).
      Крупнейшая — `schizophrenic_things` (33 384 файла по описи 22.08.2026) — **не
      найдена на диске под этим именем 28.08.2026**: либо переименована, либо уже
      разобрана отдельно, не проверено какой из двух вариантов. Это отдельная
      многочасовая задача (систематический проход по 19 целям с реальным чтением
      содержимого), не батч-фикс авто-режима — **осознанно пропущено**, не
      выполнено вслепую.

- [x] 🆕 **Двадцать групп дублей внутри базы: решить, где канон. ✅ ЗАКРЫТО 26.08.2026.**
      Заведено 22.08.2026,
      после того как 9 сирот из 29 удалены машинным критерием (v2.59.0).

      **Критерий, закрывший 9 групп:** копия в `00-infrastructure/` имеет **0 ссылок
      по пути**, а её пара — больше нуля. Тогда копия сирота, и её удаление проверяемо
      (гейт остался CLEAN, ни одной битой ссылки).

      🔴 **Оставшиеся 20 этим критерием не решаются, и вот почему** — три разных случая:

      | Случай | Пример | Что решать |
      |---|---|---|
      | **обе копии по 0 ссылок** (14 групп) | `brand-voice.md`, `analiz-chatov-*.md`, `python-backend.md` | нужен ли документ вообще: на него не ссылается **никто** |
      | **ссылок поровну** | `adr-template.md` 4↔4, `security-audit-template.md` 1↔1 | какое место канон по смыслу, а не по счёту |
      | **сильнее копия в `00-infrastructure`** | `architecture.md` 1↔0 | обратный случай: канон мог переехать |

      **Отдельно `pre-commit`:** `.githooks/pre-commit` (12 ссылок) и `templates/pre-commit`
      (3) — это **не дубль**, а рабочий хук и его шаблон. Не сводить.

      **Порядок:** сначала 14 групп с нулём ссылок — там вопрос «нужен ли», а не «где
      канон»; документ, на который не ссылается ничто, либо включается в навигатор,
      либо удаляется (`21` §4).

      🔴 **Сверено с диском 26.08.2026: большинство групп уже устранено** (видимо,
      попутно PIT-142-кампанией, которая занималась тем же классом проблемы — старыми
      копиями по имени файла). Прямым поиском по названным примерам (`brand-voice.md`,
      `python-backend.md`, `adr-template.md`, `security-audit-template.md`,
      `architecture.md`) — везде осталась только **одна** копия. Общий проход по всем
      `.md`-именам базы (кроме `_base/`) нашёл лишь **2 настоящих оставшихся дубля**:
      `interview-prep.md`, `psychology.md` (00-infrastructure/ ↔ 01-claude-context/
      02-instructions/, содержимое идентично на 99%, различие — одна устаревшая
      кириллическая ссылка на переименованный файл, тот же паттерн миграции имён,
      что PIT-142). **Оба закрыты 26.08.2026** — удалены копии в `00-infrastructure/`
      (устаревшие ссылки), оставлены копии в `01-claude-context/02-instructions/`
      (актуальные ссылки); ни на одну не было ссылок вообще, гейт остался CLEAN.
      Остальные basename-совпадения в дереве (`README.md`, `CHANGELOG.md`,
      `decisions.md`, `SKILL.md`, `00_ROLE.md` и т.п.) — легитимные множественные
      экземпляры по замыслу (один на кампанию/скиллу/роль), не дубли.

- [x] 🆕 🔴 **ЗАКАЗ ВЛАДЕЛЬЦА 22.08.2026 — шесть связанных задач к ревизии реп.
      ✅ ЗАКРЫТ ЦЕЛИКОМ 26.08.2026.**

      **1. ✅ ЗАКРЫТО.** Развести классы сущностей. `00-infrastructure/86-entity-classes.md` —
      шесть классов (инструмент/механизм/библиотека/кит/лаборатория/система), признак
      различения (кто запускает / копится ли состояние / что на выходе), полный перечень
      что чем числится. Написан 22.08.2026, чекбокс не сняли.

      **2. ✅ ЗАКРЫТО.** Лаборатория перевода файлов в `.md`. `07-media-to-text-lab/` —
      `docs_to_md.py` (pdf/docx/pptx/xlsx), `images_to_note.py`+`image_queue.py`,
      `tg_export_to_md.py`, `heavy_media_to_note.py`, метод `METHOD_IMAGES.md`,
      журнал `measurements.csv`. Числится лабораторией в реестре `86` §4. Уже применена
      на `academic-portfolio`/`misc-vault` этой сессией (`PIT-146`).

      **3. ✅ ЗАКРЫТО.** Манифест + описание + теги каждой репе — то же, что `B1`/`B2`
      в §P3, закрыты этим же батчем: 58 из 58 репов, проверено вживую.

      **4. ✅ ЗАКРЫТО — найдено сделанным 26.08.2026.** `00-infrastructure/
      87-file-to-repo-routing.md` — четыре вопроса по порядку (секрет? / правило или
      состояние? / о чём файл ПО СОДЕРЖИМОМУ? / есть своя репа?), таблица маршрутизации
      по 15 предметам, правило для тяжёлого, механизм самоуточнения (журнал правок).
      Уже написан 22.08.2026, чекбокс не сняли — тот же класс, что 1/2/3/B1/B2.

      **5. ✅ ЗАКРЫТО 26.08.2026 — правило расщепилось надвое по прямому решению
      владельца.** «Эталон + выжимка» держится только там, где эталон нужен
      содержательно: `health-vault` (показывать врачам), `self-map` (та же
      чувствительность), `academic-portfolio/03-bochvar-competition` (конкретно
      попросил сохранить). Везде остальном — выжимка замещает оригинал, оригинал
      удалён: `academic-portfolio` (198 файлов, кроме bochvar), `it-base` (162),
      `legal-knowledge-base` (365), `base-repo` (1). `misc-vault` — по прежнему
      правилу `PIT-146`, эталон вынесен в `_heavy-originals`, не удалён.

      **6. ✅ Записано, не открытая задача.** Лаборатории аудио/видео в `ai-relay` —
      `ai-relay/ROADMAP.md` уже несёт Л1 (аудио) и Л2 (видео) с материалом для первых
      прогонов; сам прогон — отдельная работа, не эта запись.

      **Все шесть закрыты.**

- [ ] 🆕 **Разобрать перепись `scan.csv` — 3 280 файлов с признаками метода.**
      Заведено 23.08.2026: `05-infra-synthesis-lab/tools/scan_lessons.py` прошёл
      **10 088 файлов** по всем репам и отранжировал те, где есть следы метода
      (правила, разборы, реестры, ретроспективы) — вывод в
      `05-infra-synthesis-lab/tools/scan.csv`.

      ✅ **Первый кандидат разобран 27.08.2026:** `character-a-analysis/LESSONS.md`
      (score 599, в топ-10 по score, `in_base` было пусто) — 14 уроков семьи
      «выборка решает результат раньше данных» (У-1д и родня) подняты в
      `00-infrastructure/89-corpus-hypothesis-discipline.md`, с перекрёстными
      ссылками на `71-fail-loud-and-sourcing.md` и
      `02-methodology-library/overfitting_and_self_deception.md` (родственные, но
      не дублирующие темы).

      ✅ **Второй кандидат разобран 27.08.2026:**
      `personal-finance-dss/docs/reports/incidents/inc_null_deadline_latent_defects.md`
      (score 619) — правило «ограничение схемы (NOT NULL/CHECK/FK) — предохранитель
      на коде, не описание домена; снимая его, сначала обходишь всех потребителей
      колонки по коду» поднято в `02-methodology-library/engineering_practices.md`
      §3, плюс второй конкретный пример расхождения SQLite/PostgreSQL (сортировка
      с NULL) добавлен в уже существующий раздел про матрицу СУБД.
      `personal-finance-dss/docs/documentation_methodology.md` (score 651) проверен
      и оказался дублем уже поднятого `incident_process_documentation_methodology.md`
      — действия не потребовалось.

      Остальные кандидаты топ-50 (в основном CHANGELOG/
      WATCHLOG — состояние, не правило — и уже помеченные `in_base=yes` копии
      канона) не разбирались в этом заходе.

      **Что это даёт.** Кампания синтеза прошла репы **вручную**; здесь машинный список
      кандидатов с оценкой: `families` (сколько классов признаков), `score`, `fam_detail`
      (`RULE=173`, `REGISTRY=134`, `POSTMOR=5` и т. д.) и `in_base` — есть ли уже в базе.
      Верх списка: `personal-finance-dss/CHANGELOG.md` (7 666 строк, score 699),
      `character-a-analysis/CHANGELOG.md` (2 682 строки).

      🔴 **Это не «что поднять», а «где искать».** Высокий score у `CHANGELOG` означает
      плотность формулировок, а не то, что файл целиком идёт в базу. Решение по каждому
      кандидату — суждение вахты (`77` §3), список лишь снимает вопрос «а всё ли посмотрели».

      **Порядок:** взять верхние 50 по score → для каждого ответить «правило или
      состояние» (`76`) → поднять правило, оставить состояние.

- [x] 🆕 **Кампания обслуживания машины: место и версии. ✅ ЗАКРЫТО 26.08.2026** — обе
      половины дали то, что Claude может дать без риска (место: измерено и очищено;
      версии: полный обзор с рекомендацией по риску); сами обновления, требующие
      проверки живой разработки, — решение владельца, см. ниже. Заказ владельца 22.08.2026:
      *«освобождение памяти на устройстве и обновление старых версий приложений,
      инструментов — с этим у меня на маке проблема»*. Жанр отчёта заведён
      (`00-infrastructure/19-reporting-system.md`, `reports/maintenance/`), сама кампания —
      после переноса.

      **Измеренная точка старта (22.08.2026), только регенерируемое в `~/Documents`:**

      | Класс | Сколько | Чем восстанавливается |
      |---|---|---|
      | `__pycache__` | 801 каталог, **628 МБ** | сам Python при первом импорте |
      | `.venv` / `venv` | 5 каталогов, **462 МБ** | `pip install -r requirements.txt` |
      | `node_modules` | 3 каталога, **272 МБ** | `npm ci` по lock-файлу |
      | `.mypy_cache`, `.pytest_cache` | 2 каталога, 21 МБ | сам инструмент |

      **≈1.38 ГБ удаляется без потери информации** — каждый класс восстанавливается из того,
      что остаётся на диске. Это и есть критерий обслуживания: удаляем только регенерируемое,
      иначе операция — перенос, а не обслуживание.

      🔴 **Стоп-класс, найденный в тот же день:** `finpilot` держит **184** незакоммиченных
      изменения при чистом `origin`. Аргумент «репа есть на гите, снесём и склонируем»
      стоил бы этой работы. `git status --short` **до** любого удаления, без исключений.

      **Вторая половина — версии.** Отдельно от места: обновление ломает чаще, чем удаление,
      поэтому отчёт обязан нести «версия до / после / что сломалось».

      🔴 **Место — сверено с диском 26.08.2026, измерение устарело.** С 22.08 система
      прошла массовую реорганизацию (перенос в `система_репозиториев/`, консолидация
      finpilot/personal-finance-dss) — измеренные тогда 1.38 ГБ регенерируемого
      мусора **больше не существуют в этом объёме**. Реальный текущий остаток
      (исключая `personal-finance-dss` — там живая сессия, трогать нельзя):
      **224 КБ** (`base-repo/scripts/__pycache__`, `claude-usage/…/__pycache__`) —
      очищено этим заходом.

      ✅ **Версии — обзор дан 26.08.2026, обновление НЕ выполнялось.**
      `reports/maintenance/2026-08-26-versions-survey.md`: 90 устаревших пакетов
      `brew`, из них под вниманием — `postgresql@14`/`@15` и `python@3.13`/`@3.14`
      (зависимости FinPilot, обновлять с проверкой миграций/тестов, не молча),
      `gh`/`pyenv`/`starship`/`zoxide` (низкий риск). Находка: активный `node` в
      `PATH` (v22.15.0) — не та установка, что видит `brew` (24.4.1<26.7.0);
      откуда вторая установка, не выяснено. Сами обновления — решение владельца
      (риск сломать живую разработку дороже, чем цена отчёта без действия).

- [ ] 🆕 **Место для секретов: коды восстановления теряются, и это единственный класс,
      которому репа не подходит.** Заказ владельца 22.08.2026, дословно: *«многие приложения
      делают какие-то коды на случай потери пароля, и я не знаю, что с ними делать — я их
      теряю. Их тоже надо куда-то класть»*.

      **Почему нельзя решить так же, как с личными документами.** 22.08 правило про личное
      было переформулировано (`05-infra-synthesis-lab/STANDARD.md` §5): паспорта, медкарты,
      мемуары кладутся в приватные репы штатно. 🔴 **Секреты — исключение, и не из
      осторожности, а по механике:** секрет опасен самим фактом попадания в файл, потому что
      **из git он не удаляется, а отзывается у провайдера**. В `academic-portfolio` живые
      VK- и Telegram-токены уже уезжали в коммит (`00-CLAUDE-STOP.md` §2, `PIT-012`).

      **Предлагаемая форма — приём, который в системе уже работает.**
      `00-infrastructure/31-media-and-photo-storage.md`: *файлы на диске, каталог в git*.
      Здесь то же самое:

      | Слой | Где | Что содержит |
      |---|---|---|
      | сами секреты | менеджер паролей вне git | коды, пароли, seed-фразы |
      | **индекс** | приватная репа | у каких сервисов коды есть, где лежат, когда обновлены |

      Индекс лечит названную боль: «я их теряю» — это отсутствие **описи**, а не хранилища.
      Список видно, не видя самих кодов.

      **Что решить при заведении:** менеджер (KeePassXC — локальная база, ничего не уходит
      наружу; либо Bitwarden) · в какой репе живёт индекс · регламент обновления ·
      судьба `~/Documents/recovery_kits` (223 МБ, сейчас свалка без описи; в репу
      не переносится ни при каких условиях).

- [x] **🔴 Свести лидерборды классов — счётчик размножен и уже разошёлся. ✅ ЗАКРЫТО 26.08.2026**
      (механическая часть; 3 личных репа осознанно оставлены владельцу).
      Найдено 21.08.2026 по замечанию владельца («в разных репах одна и та же ошибка
      находилась»). Замер по всей системе:

      | Файл | Что | Состояние |
      |---|---|---|
      | `base-repo/reports/incidents/PITFALLS.md` | классы `PIT-A…G` со счётчиками | **канон**, 7 классов |
      | `dota-dossier/reports/infrastructure/PITFALLS.md` | те же `PIT-A…F` | **отстал**: нет `PIT-G` |
      | `reports/pitfalls.md` в 9 репах | случаи `PIT-001…012` | одинаковые байт-в-байт — раздача старой базы |
      | `character-a-analysis` (33), `portrait-of-taste` (17), `personal-finance-dss` (17) | случаи | **свои линии**, в базу сведены не полностью |

      **Почему это важнее, чем кажется.** Счётчик, размноженный по репам, перестаёт
      считать: класс сработал в одной репе, счётчик вырос там, в остальных копиях
      молчит — и следующая вахта видит «2 повтора» вместо пяти. **Лидерборд ловит
      дефект, которым сам болеет.**

      **Что делать:** канон один (база), в репах — ссылка, а не копия. Плюс проверить
      три репы со своими линиями случаев: сведено ли в `reports/pitfalls.md` базы.

      🔴 **`dota-dossier` — закрыто 26.08.2026.** Сверено с канона: файл действительно
      отстал (нет `PIT-G`/`PIT-H`, `PIT-E` показывал 2 повтора вместо 3) и нёс
      **идентичный** канону текст (общесистемные уроки, не своя специфика) —
      заменён на короткую ссылку на канон, дублирование убрано.

      🔴 **«9 репов с байт-в-байт старыми копиями» — сверено, уже устарело.**
      Сейчас (26.08.2026) собственный `reports/pitfalls.md` есть только у **двух**
      репов (`master-admission` — 2 карточки, `portrait-of-taste` — 16), и оба —
      **свои** реестры случаев, а не копии канона (заголовок и нумерация свои).
      Похоже, устранено попутно кампанией манифестов или PIT-142. Пункт закрыт.

      **Не тронуто сознательно:** три репы со своими линиями случаев
      (`character-a-analysis`, `portrait-of-taste`, `personal-finance-dss`) —
      решение, что из личного/защищённого контента сводить в общую базу, тот же
      класс суждения, что и `scan.csv` выше, не решается в одностороннем /auto.

- [x] **✅ ЗАКРЫТО 22.08.2026 — три репы, числившиеся пройденными, закрыты по-настоящему.**
      ✅ **`character-a-analysis` закрыта 22.08.2026** (вахта VIII): приёмка
      `337 = база(v2.1.0) 78 + С 68 + прочитано 191`, §6а по всей репе — **0 файлов
      без записи**. Репа дала базе 17 разделов и карточки `SYN-023`, `SYN-024`, `SYN-025`.
      **Счёт кампании: 5 из 57.**

      ✅ **`portrait-of-taste` закрыта 22.08.2026** (та же вахта): приёмка **пересобрана** —
      `1572 = база(v2.9.0) 140 + дельта 25 + С 1271 + прочитано 136`. Прежняя сходилась
      при **73 файлах без решения** (не 36: групповая пометка зачлась за решение, `SYN-026`).
      §6а по всем М-каталогам — **0**. Репа дала базе 6 разделов.
      **Счёт кампании: 6 из 57.**

      ✅ **`personal-finance-dss` закрыта 22.08.2026** (та же вахта):
      `1501 = база(v2.13.0) 48 + С 1195 + прочитано 258`, §6а — **0**.
      Пробел был **138 в М**, а не 436 (436 включало С-каталоги). Репа дала базе
      **13 разделов**, включая лестницу эскалации и разбор «41 день красного гейта».

      🏁 **ВСЕ ТРИ РЕПЫ ЭТОГО ПУНКТА ЗАКРЫТЫ. Счёт кампании: 7 из 57.**
      Пункт закрывается; кампания продолжается по 50 непройденным репам.

      Измерено 21.08.2026
      (`SYN-021`, `SYN-022`, `decisions.md`): поимённая сверка решений против диска
      с вычетом совпавших с базой.

      | Репа | пробел | что именно не так |
      |---|---|---|
      | `personal-finance-dss` | **436** файлов | приёмка прямо гласит «🔶 Репа НЕ закрыта», два слагаемых из четырёх с тильдой |
      | `portrait-of-taste` | **36** | приёмка **сошлась**, но раздел «`docs/` разобрано» покрывал 9 позиций из 138 |
      | `character-a-analysis` | **24** | **раздела приёмки нет вовсе**; разметка каталогов есть |

      **Порядок:** `character-a-analysis` (337 файлов, нужна только приёмка) →
      `portrait-of-taste` (36 дочитать) → `personal-finance-dss` (половина работы по репе).
      Первые две дешёвые и закрывают счёт кампании честно.

      **Не начинать раньше, чем закрыт `dota-dossier`:** вход в него уже оплачен
      (копии найдены, корень прочитан, разметка сделана), бросать на середине —
      платить за вход дважды.

      **Счёт кампании исправить при закрытии:** сейчас честно «пройдено 3 из 57»,
      а не 5 и не 6.

- [x] **Развести нотацию своих и чужих номеров реестров. ✅ Сделано 22.08.2026.**
      Голый номер в `reports/pitfalls.md` означал И свою карточку, И чужую: источники
      штатно цитировались как «`portrait-of-taste` (номер без квалификатора)», где он принадлежит
      **чужой** нумерации. Различить по тексту было нельзя — синтаксис один.

      🔴 **Предсказанный момент наступил в тот же день, и не по расписанию.** Задача
      считалась не срочной («цена сейчас нулевая»), потому что своя нумерация не дошла
      до чужого диапазона `portrait-of-taste:PIT-101…116`. Она дошла не ростом, а **починкой**: `PIT-115`
      и `PIT-116` стояли на неверном уровне заголовка и были невидимы гейту; как только
      их увидели, `max(своих)` прыгнул со 114 до 116 и накрыл чужой диапазон. Гейт
      покраснел на девяти законных цитатах.

      **Урок шире самой нотации:** отложенная задача сработала от постороннего
      изменения, а не от того роста, который её планировали дождаться. Оценка «дорастём
      не скоро» неявно предполагала, что величина меняется только одним способом.

      **Сделано:** форма `репа:PIT-NNN` для чужих, голый `PIT-NNN` — только свой;
      lookbehind в `check_dangling_registry_refs` пропускает квалифицированные;
      замороженный `templates/_archive/` исключён (несёт номера чужих реп и не правится);
      живые цитаты переписаны. Разбор — `PIT-117`.
      Работы на один проход `sed` по девяти вхождениям плюс строка в `72-source-of-truth.md`.

      Найдено 21.08.2026, вахта VII. Родня: PIT-098, `72-source-of-truth.md` §4а.

- [ ] **Критерия выхода из скелета нет ни у одной репы, заведённой до 21.08.2026.**
      Найдено 22.08.2026 при закрытии `ai-relay` и `visual-style`. `83-project-maturity-levels.md`
      требует: «скелет `0.1.0` — идея вне головы **+ критерий выхода записан сразу**».
      Проверено командой: в обоих `START-HERE.md` **ни слова** о том, что должно появиться,
      чтобы репа стала `0.2.0`.

      🔴 **ОБЪЯСНЕНИЕ ИСПРАВЛЕНО 22.08.2026** при закрытии `money`/`productivity`/`career`.
      Записано было «разрыв во времени: репы заведены 20.08, стандарт 21.08». Правда другая
      и хуже: **у реп, заведённых 04.08, `ROADMAP.md` ЕСТЬ** — критерий выхода туда вписывать
      было куда. У заведённых 20.08 `ROADMAP.md` **отсутствует как файл**.

      **Причина не в дате стандарта, а в регрессии генератора скелета:** 04.08 он давал
      17 файлов, 20.08 — 6, и пропали ровно `ROADMAP`, `TASKS`, `WATCHLOG`. Разбор —
      `80` §5.4. Дата стандарта тут ни при чём, и прежняя формулировка увела бы починку
      не туда: дописывать критерий в `START-HERE` вместо того, чтобы вернуть `ROADMAP`.

      **Что сделать, в этом порядке:**
      1. ✅ **Починить генератор — закрыто.** Проверено 26.08.2026 сплошным обходом:
         из 60 репов у 59 есть `ROADMAP.md`+`TASKS.md`+`WATCHLOG.md`; исключение
         (`personal-finance-dss`) держит owner-задачи внутри своего `ROADMAP.md`
         пометкой `👤` — работающая альтернативная конвенция для зрелого продукта,
         не пробел.
      2. ✅ **Вернуть `ROADMAP`/`TASKS`/`WATCHLOG` — закрыто**, см. проверку выше.
      3. 🔲 **Пройти по всем на `0.1.0` и дописать критерий выхода — остаётся
         открытым.** Не тронуто в этом заходе: 50 из 59 репов — чистые скелеты без
         содержания (`owner_blockers.py`, тот же список, что «прислать материалы»),
         и открытый вопрос — применим ли «критерий выхода в MVP» (`83` §1) к
         `satellite`-репам знаний так же, как к `product`, или там нужна другая
         формулировка цели (у знаниевой репы нет естественного «MVP»). Решать
         вместе с наполнением, не отдельно от него.

      Стандарт без применённых экземпляров — обещание (`21` §4г).

- [x] 🆕 **Свести блокировки владельца по всей системе в одно место. ✅ ЗАКРЫТО
      26.08.2026.** `scripts/owner_blockers.py` — сканирует `TASKS.md` всех репов,
      группирует пункты по нормализованному тексту. Первый прогон сразу дал
      содержательный результат: **50 из 59 репов** висят на одной формулировке
      («прислать исходные материалы по теме») — не 50 разных вопросов, а один,
      заданный 50 раз. Свод записан в `mission-control/TASKS.md` (репа-планировщик,
      «одно место» по требованию пункта).
      Найдено 22.08.2026: `money`, `productivity`, `career` — три репы класса `core` —
      **18 дней** стоят на одной и той же строке `TASKS.md` («прислать исходные материалы»).
      Каждая репа знает про свою блокировку; **«три репы ждут одного действия» не видит никто.**

      Форма записи в самих репах правильная — названо, что нужно и что это разблокирует.
      Дефект в отсутствии **свода**: блокировки живут по репам, а решение о них принимает
      один человек в одном месте.

      **Что сделать:** скрипт-сборщик открытых пунктов `TASKS.md` по всем 57 репам
      с группировкой по формулировке — одинаковые строки в одну группу с числом реп.
      Родня: `scripts/base_coverage.py` как образец измерителя по всей системе.

- [x] **✅ ИЗМЕРЕНО 22.08.2026 — покрытие раздачи базы: 47 из 57.**
      Инструмент: `scripts/base_coverage.py` (запуск `python3 scripts/base_coverage.py`,
      ключ `--csv` для машинного вывода). Считает три разных числа на репу: наличие
      каталога инфраструктуры · **побайтовые** совпадения с базой · совпадения **по имени**.
      Три числа намеренно: побайтовое = раздача свежая, по имени = была когда-то,
      отсутствие каталога = не было никогда.

      | Статус | Реп |
      |---|---|
      | полная (≥40 файлов байт-в-байт) | **46** |
      | частичная | 1 (`mission-control`) |
      | **нет раздачи вообще** | **10** |

      🔴 **Гипотеза «раздача покрывает старые репы, а августовские нет» — ОПРОВЕРГНУТА
      измерением.** Десять непокрытых делятся на две группы, и только одна из них дефект:

      **Шесть — НЕ ПОЛУЧАЮТ БАЗУ ОСОЗНАННО**, потому что публичные или витринные:
      `finpilot` (публичное зеркало) · `vk-graph` · `health-report-generator` ·
      `bron-kerbosch` · `vevdokimovm.github.io` · `vevdokimovm` (профиль).
      **База приватна — раздать её в публичную репу значит опубликовать.** Это правильное
      поведение, а не пробел (`59-public-mirror-filter.md`, deny-by-default).

      **Четыре — настоящий пробел**, приватные и без раздачи: `ai-relay` · `visual-style` ·
      `control-panel` · `master-admission`. Все заведены в августе 2026.

      🔴 **Структурная находка, важная для кампании: раздача живёт в `_base/`, а не
      в `00-infrastructure/`.** У сорока шести покрытых реп `_base/` содержит **295 файлов**,
      а `00-infrastructure/` пуст или почти пуст. При этом `PROCEDURE.md` §1 **исключает
      `./_base/*` из листингов по построению** — поэтому проход по репе раздачу не видит
      и правильно её не считает. Вывод «база в эту репу не раздавалась» законен только
      когда **нет ни `_base/`, ни совпадений по хешу** — как у четвёрки выше.

      **Что осталось решением владельца:** раздать ли базу в те четыре приватные репы.
      Заведено отдельной строкой ниже.

- [x] **Раздать базу в приватные репы, которые её не получили. ✅ СДЕЛАНО 22.08.2026.**
      `sync-base.sh LOCAL=1`: **51 репа** получила v2.51.0, затем повторно v2.62.0.
      Публичные пропущены предохранителем по `isPrivate`; зеркало убрано из публичной
      `salvation`. Отставших зеркал **0** при пороге 5 минорных версий.

- [ ] **🔴 Каждая репа кампании существует в нескольких расходящихся копиях — системно.**
      Контрольная сверка пяти закрытых реп 21.08.2026: **все пять записанных чисел
      подтвердились точно**, но ни одна репа не имеет единственной копии:

      | Репа | Копий | Худшее расхождение |
      |---|---|---|
      | `personal-finance-dss` | 2 | `_sync-base` 8.25.0 против 8.28.0, **87 позиций** |
      | `it-base` | 2 | та же версия, 3 лишних файла |
      | `portrait-of-taste` | 3 | `_sync-base` 3.5.7 против 3.6.14, **116 позиций**, на 211 файлов меньше |
      | `character-a-analysis` | 3 | `_sync-base` 3.36.0 против 3.45.1, 24 позиции |
      | `mission-control` | **5** | версии от 0.25.0 до 1.12.0 |

      **`SYN-017` оказался не особенностью `mission-control`, а свойством системы.**
      И структурный перекос: у канонических копий **нет `.git`**, а у отставших
      в `~/Downloads/_sync-base/` — **есть**. То есть git-история живёт у устаревших
      экземпляров.
      **Разблокирует:** доверие к любой сверке. Пока не сведено, результат зависит
      от того, в какой каталог зашли.

- [ ] **🎯 ВИТРИНА — ПЛАН УТВЕРЖДЁН ВЛАДЕЛЬЦЕМ 21.08.2026.** Шесть проектов,
      порядок исполнения от готового к длинному. Каждая ступень заканчивается
      **опубликованной вещью**, а не заделом: недоделанная репа на витрине читается
      как «не доводит до конца» и работает против найма.

      | # | Проект | Состояние | Срок |
      |---|---|---|---|
      | 1 | `finpilot` | ✅ публичный | — |
      | 2 | `vk-graph` | ✅ публичный | — |
      | 3 | `bron-kerbosch` | ✅ публичный | — |
      | 4 | **движок игровой аналитики** | ядро есть в `dota-dossier`, извлечь | недели |
      | 5 | `salvation` | MVP готов, опубликовать | дни |
      | 6 | **сайт по алгоритмам** | с нуля, главная ставка | месяцы |

      **Сверх счёта:** profile README (рамка, не проект) · приватный инстанс движка
      на личной статистике · `health-report-generator` — оставить как есть.

- [ ] **🔴 №4 — ДВИЖОК ИГРОВОЙ АНАЛИТИКИ. Ядро системы, а не «проект про доту».**
      Формулировка владельца 21.08.2026: *«сделать фундаментальной идеей ядром
      не игру одну, а движок игровой аналитики — есть ещё вещи похожие, которые
      будут также анализироваться этим инструментом»*.

      **Контракт домена — то, что превращает слово «движок» в архитектуру.**
      Домен подключается, если умеет отдать **шесть вещей**:

      | | Что | Dota 2 | Шахматы | CS |
      |---|---|---|---|---|
      | 1 | **единица игры** — с исходом | матч | партия | матч/карта |
      | 2 | **актор** | аккаунт | аккаунт | аккаунт |
      | 3 | **сущность выбора** — то, что игрок выбирает и у чего есть своя статистика | герой | дебют | карта, оружие |
      | 4 | **метрики за единицу** — сравнимые между акторами | GPM, LH, урон | ACPL, точность | K/D, ADR |
      | 5 | **референсная популяция** — относительно кого считать перцентиль | все на этом герое | все в этом дебюте | все на этой карте |
      | 6 | **версия правил** — относительно чего считается мета | патч | (нет; вместо неё рейтинговый диапазон) | обновление игры |

      Всё, что выше контракта — **перцентили, доверительные интервалы Уилсона,
      дрейф меты между версиями, профиль стиля, слепой прогноз с предрегистрацией** —
      предметной области не знает вовсе. Всё, что ниже — адаптер на 200 строк.

      **Проверка замысла, которую надо пройти до первой строки кода:** взять шахматы
      и заполнить таблицу выше **до конца**. Если шестая строка не заполняется
      (в шахматах нет патчей) — значит контракт неверен и метрика меты должна
      опираться на что-то другое. Это дешевле выяснить сейчас, чем после адаптера.

      - [ ] Заполнить контракт по шахматам и CS; расхождения — в контракт, не в адаптер
      - [ ] Извлечь ядро из `dota-dossier` **без личной статистики** (решение владельца
            «личное остаётся приватным» не нарушается)
      - [ ] Адаптер Dota (OpenDota) — первый, материал готов
      - [ ] Адаптер Chess (Lichess, открытый API) — **доказательство переносимости**;
            репа `chess` уже есть (16 файлов, knowledge-класс) — она про содержание,
            адаптер про метод, не смешивать
      - [ ] Приватный инстанс на личной статистике — **тот же код**, другие данные.
            🔴 Отдельной кодовой базой не делать: это ровно дубль корпуса из
            `72-source-of-truth.md` §4д, разобранный в этот же день на этой же репе
      - [ ] Витрина: слепой прогноз с хеш-локом (`28` §9б) — вынести на видное место,
            это то, что отличает сильнее кода

- [ ] **№6 — сайт по алгоритмам.** Решение владельца: **пет-проект, не продукт**;
      требование «чем отличается от существующих» **снято владельцем 21.08.2026** —
      делается для себя. Замысел: теория + визуализация + задачи по нарастающей
      сложности + система сдачи; образец мотивации — e-maxx, боль автора в школьные годы.
      Записано как есть, чтобы вахта не переоткрывала вопрос отличия.

- [ ] **№5 — `salvation` в публичные.** MVP работает без сборки (`mvp/index.html`).
      Перед публикацией — прогон `59-public-mirror-filter.md` (что не должно уехать).

- [x] ~~**Profile README — `vevdokimovm/vevdokimovm`.**~~ Закрыто — репа заведена,
      публична, README задеплоен (проверено 28.08.2026, `gh repo view` + `gh api
      .../readme`). Остаток (пины/bio/архивация старых студенческих реп) — чисто
      GitHub UI, во владельца (`mission-control/BOARD.md`).

- [x] **`health-report-generator` — не трогать. ✅ РЕШЕНИЕ ПОЛНОСТЬЮ ОТМЕНЕНО владельцем
      25.08.2026** — **включая GitHub-репу**, а не только локальную папку.
      Прежний запрет («копию в `it-base` не вкладывать, репа публичная и живая, дубль
      корпуса `72` §4д») предполагал, что standalone-репа остаётся живой рядом с копией.
      Заказ владельца: «даже не пет-проекты», отдельная репа избыточна — и когда
      предупреждение «удаление GitHub-репы необратимо» было названо явно, владелец
      подтвердил: *«я именно это и хотел, смысл папки удалять то просто»*.
      Сделано полностью: содержимое (**и `bron-kerbosch`**) слито в `it-base`
      (`02-code-archive/algorithms/bron-kerbosch`, `01-projects/health-report-generator`) —
      актуальным контентом, не устаревшей копией, уже лежавшей там с 20.08 (живой случай
      `72` §4д: два корпуса разошлись без синхронизации за пять дней). Локальные папки
      удалены. **`gh repo delete` выполнен на обоих** — `vevdokimovm/bron-kerbosch`,
      `vevdokimovm/health-report-generator`, имена проверены `gh repo list` перед действием,
      подтверждены `gh repo view` после (`Could not resolve to a Repository`). Счёт публичных
      репозиториев на GitHub: **61 → 59**. `repos-map.md` v3.3.0.

<details><summary>Разбор кандидатов, на котором план построен (21.08.2026)</summary>

- [x] **Витрина: нужно ДВА проекта, а не три.** 🔴 **Запись была неверна, проверено
      по GitHub 21.08.2026** (`gh repo list --json visibility`): публичных реп **пять**,
      из них технических **четыре** — `finpilot`, `vk-graph`, `health-report-generator`,
      `bron-kerbosch`; пятая — визитка `vevdokimovm.github.io`.
      **`salvation` в публичных НЕ значится**, хотя роадмап называл её действующей витриной.

      | Кандидат | Код-файлов | Что показывает | Препятствие |
      |---|---|---|---|
      | `salvation` | 0 `.py` (MVP на `index.html`) | продукт целиком, работает без сборки | тема личная/религиозная — решение владельца, не моё; «технологичность» слабая |
      | **извлечённая часть `dota-dossier`** | 41 `.py` | **слепой прогноз с предрегистрацией и хеш-локом**, корпус на 545 документов, Wilson CI, Elo | владелец записал «личная статистика остаётся приватной» — значит **извлекать движок без данных**, отдельной репой |
      | `academic-portfolio` | 275 | диплом, публикация РИНЦ, конкурс, стажировка VK; **README уже на английском** | дублирует `finpilot`; содержит личные документы (акты, сертификаты) |
      | `control-panel` | 21 | локальный интерфейс к системе реп | в самой репе записано «видимость: приватный, деплоя нет и не будет» (её ADR-002) |
      | `it-base` | 1649 | объём учебного кода | архив, а не продукт |

      **Рекомендация вахты:** `salvation` (уже назван владельцем) + **извлечённый движок
      `dota-dossier`**. Второй — единственный кандидат, показывающий не код, а
      **методологическую честность**: прогноз, запечатанный `sha256` до события, с порогами
      успеха и провала, названными заранее. Для найма это отличает сильнее, чем ещё один CRUD.

      **Решение владельца получено 21.08.2026** — план выше.
</details>

- [x] ~~**Передать в планировщик: архетип «умные через науку оправдывают мораль».**~~
      Закрыто 28.08.2026 — сначала ошибочно добавлен указатель в `mission-control/
      INBOX.md`, затем найдена **настоящая** запись: задача уже стоит П1 в
      `truth-seeking/ROADMAP.md` (перенесена 22.08.2026) — дубль в `INBOX.md` убран,
      уточнение в `truth-seeking` снято (не дублирует ничего закрытого).

- [x] ~~**🔴 `mission-control` существует в ПЯТИ копиях; две самые свежие — без `.git`.**~~
      Закрыто фактом 28.08.2026 — все четыре старых пути (`~/vault/mission-control`,
      `~/Documents/mission-control`, `~/Documents/система_репозиториев/mission-control`,
      `~/Downloads/_sync-base/mission-control`) не существуют на диске; миграция
      на `~/repos/` (25.08.2026) оставила ровно одну копию, канон.

- [ ] **🔴 `deploy_version_gate.py` не видит отставшие копии деплойера — список путей
      вместо поиска по свойству** (`SYN-018`, класс PIT-074). `KNOWN_COPIES` (сейчас
      `~/Documents/mission-control/scripts/deploy.sh`, `~/Downloads/deploy.sh`) —
      пути, вписанные руками; **проверено 28.08.2026: обоих на диске больше нет**
      (миграция на `~/repos/` их убрала), гейт сейчас честно защищает **ноль** копий,
      не три. Конкретный пример устарел, архитектурный пробел — нет: список путей
      вместо поиска по свойству остаётся хрупким при следующем перемещении. Свежий
      `find` по `~/repos`/`~/Downloads`/`~/Documents` (без `_base/`) — копий вне базы
      нет вообще на сегодня, так что почва для правки не горит.
      Порядок: заменить `KNOWN_COPIES` на `find` по свойству (имя + наличие
      `SCRIPT_VERSION`) с явным предикатом исключения `_base/` — тот же переход,
      что `audit_coverage.py` сделал в v1.85.0 → прогнать гейт → тесты 279/279 →
      `--sync-copies`. **Трогает контур `deploy.sh`** — по `00-CLAUDE-STOP.md` §4 это
      отдельный батч с полным прогоном, а не попутная правка.
      **Разблокирует:** защиту от повторения инцидента отката деплойера — сейчас она
      покрывает три пути из ста восьми

- [x] ~~**`mission-control/reports/README.md` врёт четырьмя способами**~~ (найдено
      вахтой VI). Закрыто 28.08.2026 в сессии `mission-control`: дубль строки снят,
      таблица переписана по факту диска (12 файлов, не 4), `disposal/` убран
      (папка так и не завелась), ложное «пусто — норма» заменено правдой. Класс PIT-091.

*Два пункта ниже приехали мостом из планировщика 20.08.2026 (первая доставка в базу,
sha `1b082813`) и разобраны по контракту `74-planner-bridge.md` §3.*

- [x] ~~**🔴 Вычистить плоские legacy-копии базы из 12 реп (PIT-090).**~~ Закрыто —
      `python3 scripts/legacy_delta.py` (28.08.2026) даёт «плоской копии нет,
      пропуск» на всех 12 из списка, итого дублей 0 · старых версий 0 ·
      неизвестных 0.
- [ ] **🔴 Деплойер молча стирает доставленный мостом файл.** `deploy.sh:1470` сносит
      рабочее дерево клона и копирует архив, собранный из локального дерева, где
      `INBOX-FROM-*` нет — он лежит только на GitHub. Порядок «доставка → деплой →
      вахта» теряет задачу без единого сигнала. Закрывается отказом публиковать репу,
      у которой на удалённой стороне есть файл доставки, отсутствующий локально
      (один запрос к API, отказ с объяснением). Разбор — `74-planner-bridge.md` §4б
- [x] ~~**Баг `deploy.sh`: создаёт `finpilot-mirror` вместо публикации в существующую
      `finpilot`.**~~ Закрыто 28.08.2026 — не баг: `REPO_MAP` в `templates/deploy.sh`
      (строки 198-199) уже несёт явный маппинг `finpilot-mirror=finpilot`.
- [x] ~~**Решить по 5 конфликтным служебкам** — `pitfalls.md`, `watchlog.md`,
      `incidents-summary.md`, `investigations-summary.md`, `LESSONS.md`~~ — закрыто
      28.08.2026, по факту диска ни один не требует резолюции: `pitfalls.md` уже
      унифицирован в `reports/pitfalls.md`; `incidents-summary.md`/`investigations-
      summary.md` больше не существуют нигде (вытеснены системой PIT-карточек);
      `watchlog.md`/`LESSONS.md` — по замыслу разные в каждой репе (собственный
      журнал/грабли проекта), находка 21.08 была категориальной ошибкой инструмента
      (не различал «должен быть общим» от «должен быть свой»). Подробный разбор —
      `mission-control/BOARD.md`.
- [ ] **🔴 ADR-006 записан, но не исполнен для `edu-base` — материал в двух местах.**
      Всё ещё открыто, но 28.08.2026 разобрано глубже (не переоценивать как «то же самое
      старое», реальная новая проверка):
      - **`04-linguistics` (5 файлов) — вероятно НЕ конфликт ADR-006.** Имена файлов
        (`chinese.md`, `japanese.md`, `english-grammar-ielts-metodichka.md`,
        `russkiy-yazyk-grammatika-metodichka.md`) — практический языковой материал,
        а не теория лингвистики. Репа `linguistics` (12 файлов, `01-theory`/`02-english`)
        построена по-другому охвату. **Гипотеза для проверки:** это разный контент под
        похожим именем папки, а не непереехавшая тема — не спешить переносить.
      - **`01-learning-how-to-learn` — реальное частичное пересечение подтверждено.**
        `readings/reading-renaissance-learning-and-unlocking-your-potential.md` (207
        строк, сырой транскрипт) и тот же ридинг в `research-craft/05-learning-science/
        …docx.md` (118 строк, сжатая выжимка из оригинального `.docx`, лежащего рядом) —
        **один и тот же источник, две разные обработки**, не идентичные копии: у edu-base
        полнее, у research-craft — обработан через пайплайн извлечения. Резолюция
        требует решения, какая версия канон, не механического дедупа.
        🔴 **Побочная находка:** папка `import/` внутри (мьюинг, гимнастика для глаз,
        IQ-развитие, трекер привычек) — **не курс Learning How to Learn вообще**,
        посторонний материал под неверным именем папки, к ADR-006 не относится,
        нуждается в собственной классификации отдельно.
      - **`03-logic` (1 файл) — не перепроверено в этом заходе**, статус как на 21.08.
      **Исполнены только `it-base/03-machine-learning` и одна папка в `academic-portfolio`**
- [ ] **Чистка публичного зеркала идёт по denylist — против принципа базы.**
      `mirror-clean.sh` планировщика ведёт список исключений, а `59-public-mirror-filter.md`
      требует **allowlist**. Denylist пропускает всё, что в него не внесли: новый тип
      служебного файла утечёт молча, и узнают снаружи. Переписать на allowlist;
      до тех пор список исключений сверяется руками при каждом изменении состава служебок
- [x] ~~**Разобрать 22 кандидата из таблицы 2**, из них **14 конфликтов**.~~ Разобраны
      28.08.2026 (`per_repo_audit.py`, свежий прогон — те же 22/14, но состав другой:
      `revision-2026-08-26.md` датирован уже после 21.08, список пересчитан заново).
      **Ни один не требует резолюции «какая редакция права»** — все 14 попадают
      в уже известную категориальную ошибку инструмента (`LESSONS.md`/`watchlog.md`,
      разобрано ранее в этом же заходе):
      - `raspoznavanie.md` (45 реп/дублей, 23 редакции) — машинный маркер-стаб от
        медиа-лабы, генерируется **на каждый файл-изображение отдельно**, по
        построению разный в каждом месте, не шаблон для унификации.
      - `INFRASTRUCTURE.md`/`METHODOLOGY.md` (`dota-dossier`↔`portrait-of-taste`),
        `RELEASING.md` (`exam-kit`↔`portrait-of-taste`), `GLOSSARY.md`
        (`portrait-of-taste`↔`personal-finance-dss`) — общее имя по конвенции
        документации продукта, содержание у каждого продукта своё по построению,
        продукты никак не связаны.
      - `case-instagram-session-4.md`, `experiment-primary-source.md`,
        `instagram-channel-and-reading.md`, `view-diagnostics.md`,
        `view-experiment-stats.md` (`character-a-analysis`↔`self-map`) — общая
        методология `10-life-history/03-reports/`, экземпляры про разных субъектов
        (анализ чужих людей vs себя) — имя общее, содержание обязано различаться.
      - `DEPLOY.md`/`RELEASES.md` (`finpilot`↔`personal-finance-dss`) — **реальное
        расхождение** (проверено `diff`: `finpilot` откатан к до-SPA состоянию),
        но это **известный, принятый факт**, не находка: решение «не консолидировать,
        только надбавки» уже принято владельцем (см. выше в этом файле, находка
        от 26.08.2026) — `finpilot` намеренно не обновляется вслед за каноном.

- [x] ~~**Три унаследованных провала в `tests/test_deploy.sh`**~~ — закрыто, полный
      прогон 28.08.2026: **295 из 295 зелёных**, ноль провалов (deploy.sh v4.22.0).
- [x] ~~Дописать в `mission-control`, что её `scripts/deploy.sh` — копия, а не канон~~ —
      снято фактом: `mission-control/scripts/deploy.sh` больше не существует, единственная
      копия — `_base/templates/deploy.sh` (стандартная раздача базы, ей положено отставать).
- [x] ~~`templates/pack_release.py` — универсальный упаковщик.~~ Устарело — `scripts/
      pack_release.py` уже универсален, принимает путь к любой репе аргументом
      (`sys.argv[1]`), использовался в этой же сессии на `mission-control`
      напрямую из `base-repo`, без копии в целевую репу.
- [ ] `00-infrastructure/NN-release-readiness.md` — контракт **со стороны репы**:
      что она обязана предъявить, чтобы её можно было публиковать. `deploy-SPEC.md`
      описывает гарантии деплойера, обратной половины контракта нет. 🔴 **Номер
      74 занят** (`74-planner-bridge.md`, проверено 28.08.2026) — при создании
      брать свободный номер, не 74, дальше подтверждено — по-прежнему не сделано.
- [ ] `templates/release_check.py` — самопроверка репы по этому контракту до упаковки
      (по-прежнему не сделано, проверено 28.08.2026)
- [ ] Перенести списки `MIRRORS` / `REPO_MAP` / `VARIANT_REPOS` из тела `deploy.sh`
      в данные: шесть имён реп зашиты в универсальный инструмент, каждое новое зеркало =
      правка кода + новая версия. Дважды уже закрывали этот сценарий (4.10.0, 4.13.0)
- [ ] Закрыть последний молчаливый путь деплойера: «нет секции в CHANGELOG →
      описание сохраняется как есть». Пропускать версию **громко**
- [ ] `tests/test_deploy.sh`: фильтр по зоне и кейсу (`ONLY=C12`, `ZONE=C`) — прогон целиком
      ~3 минуты, медленный набор начинают пропускать (PIT-039)
- [x] **Навигатор `00-infrastructure/README.md` отстал на 36 документов. ✅ УСТАРЕЛО,
      закрыто 26.08.2026** — гейт `revision_check.py` («Навигатор полон: N из N
      документов») проверяет это на каждом батче этой сессии, стабильно CLEAN.
      Похоже, устранено раньше (сам гейт и есть механизм, не дающий отставать снова).
- [x] ~~Оформить `per_repo_audit.py` постоянным инструментом~~ — снято фактом: живёт
      в `scripts/`, задокументирован в `77-system-wide-audit.md`/`76-repo-classes.md`,
      использовался в этой же сессии для проверки конфликтов служебок.
- [ ] Ревизия базы после синтеза — отсечь лишнее, набранное по закону 4

---

## P3 — бэклог вехи синтеза (после сведения базы)

Заведён владельцем 20.08.2026, подробности — `PLAN.md` §4б.

- [x] **B1. ✅ ЗАКРЫТО — найдено сделанным 26.08.2026.** 58 из 58 репов несут `.repo-meta`,
      проверено вживую (`gh repo view`): описание развёрнутое, не одно слово, топики
      расставлены (пример — `edu-base`: полное описание + 8 топиков). Сделано кампанией
      манифестов (`05-infra-synthesis-lab/runs/2026-08-25-manifest-completion`), чекбокс
      не сняли — тот же класс, что доска `BOARD.md` и автопродолжение лимита в этой сессии.
- [x] **B2. ✅ ЗАКРЫТО — найдено сделанным 26.08.2026.** 58 из 58 репов несут `MANIFEST.md`
      (сверено на диске: `find … -name MANIFEST.md | wc -l` = 58). Та же кампания, что B1.
- [ ] **B3.** Философская часть манифеста базы: Зуко (Аватар), Кратос (God of War),
      «Атака титанов» — **по каждому персонажу отдельный ресёрч**, потом синтез.
      Задел: `00-MANIFEST-attack-on-titan.md` (22 КБ)
- [ ] **B4.** Стандарт выпуска со стороны репы — раскрыт в P2

---

## Ожидается от владельца (блокирует P1.3)

- [x] ~~**🔴 Ключ из Anthropic Console**, `export ANTHROPIC_API_KEY` в `~/.zshrc`~~ —
      снято архитектурно: работа идёт через подписку/`/login`, не через API-ключ
      (проверено 28.08.2026 — `ANTHROPIC_API_KEY` нет в `~/.zshrc`, и вся практика
      сессии строится вокруг `/login`, не ключа).
- [x] ~~**🔴 `~/claude-workspace/`** — склонировать туда только открытую зону~~ —
      снято архитектурно: каталог не создан, модель работы прямо в `~/repos/`
      (миграция 25.08.2026) заменила идею изолированного клона.
- [ ] **🔴 Отозвать токен VK** — открытый пункт; живой `.env` найден в `vk-graph.zip`
      (тот же пункт уже в `TASKS.md` этой репы).
- [x] ~~**Задеплоить v1.62.0**~~ — устарело, база на v3.82.1.
- [ ] Освободить диск — **обновлено 28.08.2026: было 10.8 GB, сейчас 6.6 GB
      свободно** (стало теснее, не свободнее).

---

## Отложено

- [ ] Обкатать `04-product-dev-kit` на живом проекте
- [ ] Калибровать `hypothesis.md` §5 в `reports/experiments/token-consumption/`
- [ ] Записи `[4.3.2]` в `templates/deploy-CHANGELOG.md` нет — восстанавливать не из чего,
      дыра отмечена

---

## P0.5 — разбор дампа задач владельца от 24.07.2026, занесён 26.08.2026

Владелец скинул целиком список задач месячной давности (24.07.2026, ~300 пунктов,
личные/по репам/инфраструктурные вперемешку) с прямым указанием: разнести по
ROADMAP, делать то, что зависит от меня. Дамп **месячной давности** — часть пунктов
уже закрыта позже (проверено по факту, не по галочке в дампе), часть устарела.
Ниже — только то, что (а) ещё актуально на 26.08.2026, (б) в зоне ответственности
Claude, а не только владельца. Остальные ~280 пунктов дампа — личные дела владельца
(кровь сдать, загранпаспорт, военкомат и т.д.) или адресные задачи внутри конкретных
тематических реп (дота-гайды, шахматы, self-map...) — не разнесены построчно, это
отдельная большая задача на будущее (систематический проход по дампу репа-за-репой).

- [x] **Переместить все репы в систему** — сделано 26.08.2026 в этой же сессии
      (`base-repo`, `finpilot`→`personal-finance-dss`)
- [x] **Слить `finpilot`/`personal-finance-dss`** — сделано 26.08.2026, см. запись
      в п.2 выше
- [x] **«Деплой создаёт finpilot-mirror репу-сироту»** — проверено 26.08.2026:
      `REPO_MAP` в `deploy.sh` (`finpilot-mirror=finpilot`) уже решает это, живых
      орфан-репозиториев на GitHub нет (`gh repo list` — только `finpilot` и
      `personal-finance-dss`). Пункт дампа устарел, проблема уже была починена
      раньше 24.07 или между.
- [x] **«Большие файлы, картинки — через лабу медиа→текст»** — сделано 26.08.2026:
      см. находку про превышение 500 МБ (`misc-vault`, `academic-portfolio`) в п.2 —
      прямой повод для этого же пункта дампа, оба разобраны в этом же заходе.
- [x] **Опубликовать `research-engine`** — закрыто, подтверждено 28.08.2026:
      `gh release list` — v0.2.1 «Latest», релиз реально на GitHub.
- [x] **Токен-лаба: закрыть разрыв с панелями интерфейса** — закрыто, `panel_probe.py`
      существует с 25.08.2026 (подтверждено на диске 28.08), автоматизирует
      Status/Stats/Usage без экрана владельца — ровно то, что просил дамп.
- [ ] **Синтез лабы токенов с другими репами** — дамп: «как в других репах
      синтезировать работу с лабой по токенам? можно ли соединить чтобы сразу
      в базу писал?». Открытый архитектурный вопрос: /tokens сейчас пишет только
      в `base-repo/reports/experiments/token-consumption/`, у топик-реп своей лабы
      токенов нет. Решить: нужна ли она вообще на уровне репы, или сводка по всей
      системе — задача самой базы (вероятно последнее — экономика session-scoped,
      не repo-scoped).
- [ ] **Аудит: чего не хватает в медиа-лабе** (`07-media-to-text-lab/`) — владелец
      просит подумать, что доделать/докрутить, пока есть время. Не сделано в этой
      сессии (нашлись 2 живых бага в существующих инструментах — `docs_to_md.py` и
      `heavy_media_to_note.py` не переживали PIT-145/сетевые таймауты на заглушках,
      оба пофикшены/обойдены сегодня, но структурный аудит «чего не хватает» отдельно
      не проводился).
- [ ] **Аудит: каких лаб/инструментов/китов не хватает в базе** — тот же вопрос
      шире, чем медиа-лаба. Отдельная сессия на «посмотреть на всю систему реп,
      подумать какой обслуживающий персонал/инфраструктура нужны ещё».
- [ ] **Светлая тема iTerm** — локальная настройка приложения, не код репы; можно
      дать команду/инструкцию владельцу, но это его действие, не моё (граница
      ROADMAP/TASKS — `45-roadmap-and-tasks.md`).

## Архив закрытых

История закрытого — `CHANGELOG.md`, по версиям. Отдельный `ROADMAP_HISTORY.md` заводится
в момент первого переноса, а не заранее (`45-roadmap-and-tasks.md` §5).
