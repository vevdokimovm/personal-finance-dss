# Архитектура — <ИМЯ ПРОДУКТА>

> Заполнить при старте, держать актуальным при значимых изменениях (значимое изменение → отдельный
> ADR в `reports/adr/`, этот файл — снимок текущего состояния, не история).

## 1. Что это и для кого

<Одним абзацем: что делает продукт, кто пользователь, какую задачу решает.>

## 2. Компоненты и границы

<Схема/список: из каких сервисов/модулей состоит система, где границы ответственности каждого.
Диаграмма (Mermaid) приветствуется, но не обязательна на старте.>

## 3. Ключевые технические решения

<Список с датами и ссылками на ADR — не пересказ, а указатель: "почему Postgres, не Mongo — ADR-003".
Не дублировать содержимое ADR здесь.>

## 4. Данные

<Модель данных верхнего уровня: основные сущности и связи. Ссылка на подробную схему/миграции, если есть.>

## 5. Внешние зависимости

<Сторонние API, платёжные системы, инфраструктурные сервисы — что продукт не контролирует.>

## 6. Известные ограничения

<Что сейчас сознательно не решено/отложено — не путать с багами, это архитектурный долг с обоснованием.>

---

Связанные файлы: `reports/adr/` (история решений), `SECURITY.md` (модель угроз),
`../00-infrastructure/architecture.md` (общие паттерны стека, если Python/FastAPI).
