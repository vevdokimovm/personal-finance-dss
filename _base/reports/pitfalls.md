# Реестр частых ошибок (pitfalls)

> ### 🔴 В системе ДВА реестра, и это не дубль — у них разная природа
>
> | Файл | Что хранит | Единица | Когда открывают |
> |---|---|---|---|
> | **этот** (`reports/pitfalls.md`) | **случаи**: что произошло, чем лечится | `PIT-001…NNN` | когда наступил на конкретное |
> | `reports/incidents/PITFALLS.md` | **классы со счётчиком повторов** | `PIT-A…G` | **перед правкой поведения**, 30 секунд |
>
> Случай отвечает «что это было». Класс отвечает «сколько раз мы уже так делали»,
> и растущий счётчик означает, что заплатки не работают и нужна механическая проверка.
>
> **Наступил на грабли — работа с обоими:** карточка случая сюда, **плюс +1 к счётчику
> класса** в лидерборде. Счётчик не сбрасывается никогда — он про историю.
>
> Буквенная нумерация классов (`PIT-A`) и числовая случаев (`PIT-001`) не пересекаются
> намеренно: одно пространство имён, две шкалы, коллизии нет.

Повторяющиеся ошибки в рабочем процессе: кривые команды, привычки, грабли воркфлоу. Это **третий тип**
разбора рядом с инцидентами и расследованиями, но самого лёгкого веса.

Чем отличается от соседей:

- **Инцидент** (`incidents_summary.md`) — была реальная поломка/дефект и/или влияние. Полный разбор —
  отдельным файлом в `incidents/`.
- **Расследование** (`investigations_summary.md`) — реального дефекта НЕ оказалось (внешнее / артефакт
  / недопонимание); причина была неочевидна, её искали.
- **Pitfall (этот файл)** — повторяющаяся ошибка воркфлоу. Причина известна сразу, мистики нет,
  предмет репы не затронут. Это **не** отдельный файл-разбор, а ОДНА строка-урок: симптом → корень →
  как правильно → правило.

Заполняется АВТОМАТИЧЕСКИ (`00-infrastructure/20-knowledge-capture-protocol.md`) — как только ошибка
воркфлоу повторилась, строка сюда, не дожидаясь просьбы. Каждая запись становится переиспользуемым
правильным паттерном, чтобы не выводить его заново каждую сессию.

---

## Единый реестр системы

Это **единственный** реестр граблей на всю систему репозиториев. Он раздаётся в `_base/` каждой
репы, поэтому урок, добытый в одной репе, доступен во всех остальных — а не лежит куском про токены
в одной репе и куском про git в другой.

**Нумерация сквозная и не переиспользуется.** Номер выдаётся один раз и навсегда закрепляется
за уроком: на записи ссылаются `00-CLAUDE-STOP.md`, `WATCHLOG §4`, методички и отчёты.

> ⚠️ **Как НЕ надо (проверено на себе).** До синтеза 2026-08-20 пять реп вели независимые линии
> нумерации от общей точки расхождения: `PIT-005` означал «комментарий в zsh уходит аргументом»
> в одних репах и «уже сделано в hand-off ≠ доехало» в других; `portrait-of-taste` был вынужден
> уйти в диапазон 101+, чтобы не столкнуться. Свести это можно было только вручную и по смыслу —
> скрипт, сравнивающий по хешу, видел бы 110 разных файлов. Разбор — `05-infra-synthesis-lab/`.

**Правило заведения новой записи:** взять следующий свободный номер **в этом файле** (не в копии
`_base/` своей репы — она read-only). Появилась запись в репе до раздачи — при следующем синтезе
она получит сквозной номер, а в репе останется указатель.

---

## Формат записи

```markdown
### PIT-NNN — <короткое имя> (YYYY-MM-DD)

- **Симптом:** <что увидели, как проявилось>
- **Корень:** <настоящая причина, а не поверхностная>
- **Как правильно:** <что делать впредь>
- **Правило:** <какое правило это кодифицирует>
- **Гейт:** <да → ссылка на проверку (`revision_check.py::check_X`, `deploy.sh` шаг и т.п.) |
  нет — почему не применимо (одноразовое/не формализуемо механически) | TODO — кандидат,
  не оценено>
```

> 🔴 **Поле «Гейт» добавлено 27.08.2026** (`08-systems-theory-lab/README.md` §5 —
> находка: связь «урок → машинная проверка» была ручной, не системной, антихрупкость
> держалась на том, что кто-то лично заметил и предложил гейт). Обязательно для НОВЫХ
> карточек с этой даты.
>
> ✅ **Ретроактивная разметка выполнена 27.08.2026** — все 136 существующих карточек
> прошли аудит: 54 получили реальную машинную проверку (часть — новые скрипты
> `scripts/check_shell_antipatterns.py`, `check_exec_bits`, часть — ссылка на уже
> существующий код, который карточка сама и породила), 77 честно закрыты «нет»
> с конкретной причиной (поведение модели/канала, суждение о содержании, среда вне
> репы — не структура файла), 5 оставлены `TODO` там, где решение требует отдельного
> разбора. `scripts/pit_gate_coverage.py` по-прежнему только измеряет охват и не
> валит релиз — планка для НОВЫХ карточек теперь просто «заполни поле сразу», а не
> «разбери накопившееся».

---

## Универсальные грабли (применимы к работе в любой репе через Claude)

Эти примеры перенесены как база — они срабатывают при работе с любой репой в песочнице Claude и
экономят время. Пополняется по мере появления новых.

### PIT-001 — `find … && echo ЕСТЬ` лжёт: печатает «ЕСТЬ» и без находок

- **Симптом:** проверка существования файла через `find … -name X && echo ЕСТЬ` печатает «ЕСТЬ», даже
  когда файла нет — так можно ошибочно решить, что файл уже есть (и потерять/не создать нужный), или
  наоборот.
- **Корень:** `find` завершается с кодом `0` (успех) даже при нуле совпадений — сам поиск отработал
  корректно, просто ничего не нашёл. Оператор `&&` смотрит на код выхода, а не на наличие вывода.
- **Как правильно:** существование проверять `test -f <путь>` (или `[ -f … ]`), либо по **непустому
  выводу** (`OUT=$(find …); [ -n "$OUT" ]`), а не по факту, что `find` вообще запустился.
- **Правило:** проверять существование по результату, а не по коду выхода команды поиска.
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `find … && echo`)


### PIT-002 — `grep` в цепочке `&&` обрывает выполнение

- **Симптом:** команда вида `… && grep -c X file && next` — `next` молча не выполняется; счётчик/итог
  «не выводится».
- **Корень:** `grep` с НУЛЁМ совпадений возвращает код `1` → оператор `&&` обрывает весь остаток
  цепочки. Усугубление: кириллический вывод в pipe может дать битый UTF-8 и оборвать команду.
- **Как правильно:** не чейнить `grep` через `&&` — использовать `;` либо `grep … || true`; итоги
  брать из ГОТОВОГО файла (`tail`), а не хрупким `grep` на ходу; кириллицу читать через `python3`/
  `tail`, не через `grep` в `&&`-цепочке.
- **Правило:** повторная ошибка кодифицируется один раз; правильная команда переиспользуется.

---
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `grep -c … &&`)


### PIT-003 — движок переименования срезает ведущую точку у dotfiles (YYYY-MM-DD)

- **Симптом:** массовый нормализатор имён превращает `.gitkeep`/`.gitignore`/`.env.example` в
  `gitkeep`/`gitignore`/`env.example` — dotfile перестаёт работать (git его не видит как служебный).
- **Корень:** правило kebab-case/транслита применяется к имени целиком, не делая исключения для
  ведущей точки.
- **Как правильно:** ведущая точка — священна, нормализатор обязан её сохранять; kebab-case только к
  файлам-знаниям (md/pdf/картинки), НЕ к коду (`.py`/`.cpp`/`.sql` — импорты ломаются от дефисов).
- **Правило:** dotfiles и код не переименовываются вслепую; после массового rename — чинить ссылки в `.md`.
- **Источник:** `situations/2026-07-08-upakovka-arkhivov-v-repozitorii.md`.
- **Гейт:** нет — разовый нормализатор имён кампании упаковки архивов (situations/2026-07-08); в base-repo нет персистентного скрипта массового переименования файлов, проверять нечего


### PIT-004 — «чистка ≠ долив»: распаковка зипа поверх клона оставляет старые файлы (YYYY-MM-DD)

- **Симптом:** после переименований/конвертаций распаковал полную версию поверх клона репы и запушил —
  старые (переименованные/удалённые) файлы остались, в репе задвоение.
- **Корень:** overlay поверх существующего дерева не удаляет то, чего в новой версии нет; git видит
  только добавления, а не переименования/удаления.
- **Как правильно:** клон → стереть рабочее дерево **кроме `.git`** → распаковать полную версию →
  `git add -A`. Тогда git фиксирует переименования/удаления/добавления одним чистым коммитом. Команды —
  одним копипаст-блоком, абсолютные пути, `|| continue`, без комментариев в строке; `rsync --delete`
  применять только с wrapper-директорией (см. `../../00-infrastructure/10-git-reference.md`).
- **Правило:** обновление репы через полную замену дерева, не через долив поверх.
- **Источник:** `situations/2026-07-08-upakovka-arkhivov-v-repozitorii.md`.
- **Гейт:** да → `templates/deploy.sh` (полная замена дерева кроме `.git` перед распаковкой, помечено `# PIT-004`, ~строка 1695)


### PIT-005 — комментарий в строке команды zsh уходит в программу как аргумент (YYYY-MM-DD)

- **Симптом:** скопировал команду с пояснением в той же строке (`python3 analyze.py   # текущая оценка`)
  → скрипт падает с «нет файла лога: #» (или подобным) — `#` и слова после него прилетели как аргументы.
- **Корень:** в zsh опция `interactive_comments` по умолчанию **выключена** для интерактивной строки,
  поэтому `#` НЕ трактуется как начало комментария — весь хвост строки передаётся программе как argv.
  (Та же боль, что в `situations/2026-07-08-*` §10.10: «комментарий в строке — шелл подавился».)
- **Как правильно:** команды копировать **по одной, без комментариев в строке**; пояснения — в
  отдельных строках прозы, не в paste-блоке. Скрипты со своей стороны — **устойчивы к мусорному
  аргументу** (позиционный арг считать путём только если это реально существующий файл/`.csv`, иначе
  игнорировать) — так `analyze.py` и сделан.
- **Правило:** в инструкциях — чистые copy-paste-блоки без inline-комментариев; в скриптах —
  defensive-разбор argv.
- **Источник:** реальный прогон `reports/experiments/token-consumption/analyze.py` у пользователя.
- **Гейт:** нет — привычка интерактивного zsh (`#` не комментарий в строке команды); не оставляет структурного следа в файлах репы, проверяется только по логу сессии


### PIT-006 — `git add -A` уважает `.gitignore` → при заливке готового дерева файлы теряются (YYYY-MM-DD)

- **Симптом:** залил репу целиком из архива, `git add -A` + commit + push → на GitHub репа «пустая»
  (1 файл — сам `.gitignore`), остальное не попало в коммит.
- **Корень:** `git add -A` **уважает `.gitignore`**; если в корне широкие правила, всё содержимое молча
  выкидывается из индекса.
- **Как правильно:** когда цель — залить дерево целиком (из архива/сборки), добавлять **принудительно**:
  `git add -A -f`. Флаг `-f` обходит `.gitignore`, ничего не теряется.
- **Правило:** заливка полного дерева → всегда `git add -A -f`.
- **Источник:** `situations/2026-07-09-empty-repos-recovery.md`.
- **Гейт:** да → `templates/deploy.sh` (`git add -A -f`, помечено `# PIT-006`, строка 1705)


### PIT-007 — вложенная папка-обёртка после `unzip` → корень репы «пустой» (YYYY-MM-DD)

- **Симптом:** распаковал `base-repo.zip`, залил — в репе одна вложенная папка `base-repo/base-repo/…`,
  корень выглядит пустым.
- **Корень:** архив содержит одну корневую папку-обёртку; распаковали и залили **папку**, а не её
  **содержимое**.
- **Как правильно:** распаковать во временную папку; если внутри **ровно одна директория и нет файлов** —
  спуститься в неё и копировать оттуда (авто-разворот обёртки). Так делают `push_base_repo.sh`/`sync-all.sh`.
- **Правило:** после `unzip` — всегда разворачивать обёртку перед заливкой.
- **Источник:** `situations/2026-07-09-empty-repos-recovery.md`.
- **Гейт:** да → `templates/deploy.sh` (разворот wrapper-обёртки перед `SRC=`, помечено `# PIT-007`, строка 1631)


### PIT-008 — «пустая репа» на GitHub ≠ потеря данных (YYYY-MM-DD)

- **Симптом:** открыл GitHub, увидел пустой/однофайловый репозиторий → паника «всё стёрлось».
- **Корень:** почти всегда данные целы — это баг коммита (PIT-006 `.gitignore`) или вложенность
  (PIT-007), а не потеря. Данные живут в архивах (`~/Downloads/*.zip`), истории git и готовых сборках.
- **Как правильно:** первым делом `git log` (коммиты целы?) и `git ls-tree -r HEAD | wc -l` (что реально
  в дереве?), а не паника; после каждого пуша — автопроверка числом файлов (0/1 = баг). Архивы не
  удалять несколько дней после заливки — офлайн-бэкап.
- **Правило:** «пустая репа» → диагностика `git log`/`ls-tree`, не выводы; бэкап-архивы держать.
- **Источник:** `situations/2026-07-09-empty-repos-recovery.md`.

<!-- Новые pitfalls добавляются сюда. -->

---

> Как добавлять: заметил, что одна и та же ошибка воркфлоу повторяется — строку сюда по схеме
> симптом → корень → как правильно → правило. Делается АВТОМАТИЧЕСКИ при срабатывании. Отдельного
> файла-разбора у pitfall нет — он легче инцидента и расследования.

- **Гейт:** нет — диагностический протокол для человека при инциденте («сначала git log/ls-tree, не паника») — процедура на случай тревоги, не инвариант файла


### PIT-009 — Info-ZIP превращает кириллические имена в literal `#Uxxxx` (2026-07-09)

- **Симптом:** после распаковки zip юниксовым `unzip` файлы называются `#U041a#U041e...md` вместо
  кириллицы; такие имена закоммитили — в репе на GitHub порча имён (так уехал весь кит
  `01-claude-context/` между v1.3.0 и v1.4.0).
- **Корень:** zip упакован без UTF-8 флага (бит 0x800); Info-ZIP не декодирует имена, а пишет
  escape-последовательности `#Uxxxx` прямо в имя файла.
- **Как правильно:** распаковывать через `python3` + `zipfile` с фиксом `cp437→utf-8` при отсутствии
  флага (рецепт в WATCHLOG academic-portfolio) и нормализацией NFC; перед `git add` проверять имена:
  `ls -R | grep '#U'` — пусто. Уже испорченное чинится декодером `#U(hex)→chr` (слияние v1.5.0).
- **Правило:** после любой распаковки кириллической репы — проверка имён на `#U`/mojibake до коммита.
- **Гейт:** да → `scripts/revision_check.py::check_names`


### PIT-010 — деструктив в multi-line paste: `&&`-цепочка падает, а `rm`/`find -delete` выполняются не там (2026-07-09)

- **Симптом:** пользователь вставил блок «`git clone … && cd …` → `find … -delete` → …»; clone упал
  (папка существует), `cd` не выполнился, а деструктивные строки выполнились в ТЕКУЩЕЙ папке —
  вычищен `~/Downloads`, следом закоммичен снос репы (авария base-repo v1.2.0, восстановлено).
- **Корень:** в многострочной вставке каждая строка выполняется независимо от исхода предыдущей;
  `&&` защищает только внутри одной строки. Плюс `#`-комментарии в интерактивном zsh — аргументы
  (PIT-005).
- **Как правильно:** paste-блоки для пользователя — без деструктива вообще; замена дерева/чистка —
  только внутри скриптов с sanity-check маркеров (`push_base_repo.sh`, `publish.sh`) и fail-loud.
  Разбор — `reports/merges/merge_manifest_v1_5_0.md`.
- **Правило:** деструктивная команда никогда не идёт в копипаст-блок; только скрипт с проверками.
- **Гейт:** нет — конвенция составления инструкций для пользователя (деструктив не в копипасте, а в скрипте с проверками) — про манеру письма ответов, не про файлы репы


### PIT-011 — обрыв канала tool-call: `court` + сырой `<invoke>` вместо вызова (2026-07-13)

- **Симптом:** вместо исполненного инструмента в сообщение попадает видимый текст (литерал
  `court`, следом сырой `<invoke>`), ход обрывается, вызов не исполняется, результата нет.
  Идёт **сериями**: за сессию 12–13.07.2026 — трижды.
- **Корень:** сбой декодирования служебных токенов в рантайме ассистента. Первопричина — не
  в репе. Но **частота управляется поведением**: (1) ход, завершающийся вызовом; (2) сложные
  bash-команды с heredoc и вложенными кавычками; (3) **заражение из истории** — сломанный
  пример в контексте начинает воспроизводиться как текст, отсюда серии; (4) короткие
  ходы-пинги между обрывами.
- **Как правильно:** вызов инструмента — **никогда не последнее** в сообщении (ход
  заканчивается текстом + `present_files`); весь батч — за один ход; команды простые
  (тяжёлое — через `create_file` + запуск, а не heredoc); сломанные `<invoke>` из истории
  игнорировать, а не копировать; после мутирующих вызовов — **эмпирически** сверять
  фактические файлы (`grep`), потому что реальный вред — тихо пропущенный `str_replace`.
- **Правило:** глюк рантайма, но триггерится поведением → лечится механикой хода, а не
  обещаниями. Полная методичка — `02-methodology-library/tool_call_channel_failures.md`,
  вход — `00-CLAUDE-STOP.md` в корне.

- **Гейт:** нет — сбой рантайма ассистента (декодирование служебных токенов tool-call), внешний по отношению к репе; лечится поведением хода (батч, не heredoc), не гейтом на диск


### PIT-012 — живые токены в закоммиченном коде (2026-07-12)

- **Симптом:** `repo_audit.py` при первом прогоне нашёл в `academic-portfolio` рабочий VK
  access token сообщества (`main.py` чат-бота + дубль в `notes.txt`) и Telegram bot token
  (`alertmanager.yml` из лабы). Лежали месяцами, уехали в историю git.
- **Корень:** правило «секреты не коммитим» держалось на внимательности. Внимательность
  кончается раньше, чем коммиты.
- **Как правильно:** секреты — только в `.env` (+ `.env.example` в репу), для не-Python
  конфигов есть `*_file`-варианты (`bot_token_file`). Механическая защита: `.githooks/pre-commit`
  (`git config core.hooksPath .githooks`) + периодический `repo_audit.py`.
- **Правило:** **из git секрет не удаляется — он отзывается.** Порядок при утечке: отозвать у
  провайдера → выпустить новый → и только потом чистить файлы и историю. Обратный порядок
  бессмыслен: пока токен жив, он жив везде, куда успел попасть.

- **Гейт:** да → `scripts/secrets_scan.py`, `scripts/migrate_to_repos.py` (SECRETS-комментарий и фильтр у PIT-012)


### PIT-013 — «temporarily unavailable» на Bash = outage классификатора, не кривая команда (2026-07-18)

- **Симптом:** нетривиальные Bash-команды (`zip`, `cp && zip`, `python3 -c …`) возвращают
  «claude-sonnet-5 temporarily unavailable, auto mode cannot determine the safety of Bash», а `echo` и
  read-only тулы (Read/Glob/Grep) работают. Ассистент повторяет идентичную команду ~15 раз и на каждый
  фейл пишет пользователю — читается как метание, бесит.
- **Корень:** внешний — классификатор безопасности авто-режима временно недоступен → команды, которые без
  него нельзя признать заведомо безопасными, отклоняются; тривиальные/read-only идут мимо классификатора.
  Это НЕ синтаксис команды и НЕ поломка шелла.
- **Как правильно:** распознать по сигнатуре (outage, не кривизна) → **не долбить**: ≥2–3 фейла → сменить
  тактику, а не повторять одно и то же. (а) свести к максимально простой одиночной команде; (б) backoff,
  не машинган; (в) `send_later`/фоллбэк + продолжить полезную read-only работу; проверить канал `echo ok`.
  Пользователю — один статус + план, не репорт каждого ретрая. Ценное держать на диске (файловые тулы идут
  мимо классификатора), под шелл оставлять только упаковку — её и откладывать.
- **Правило:** повторный фейл инструмента с «temporarily unavailable» → упростить+отложить, не повторять
  идентично; не пере-нарративить. Полный разбор — `incidents/bash_classifier_channel_outage_incident.md`
  (INC-BASH-CLASSIFIER-OUTAGE).

- **Гейт:** нет — внешний outage классификатора безопасности авто-режима Bash — состояние сервиса платформы, не файла репы


### PIT-014 — размер папки во время фоновой распаковки ≠ финальный размер (2026-08-20)

- **Симптом:** пользователь смотрит `du`/Finder на папку, в которую **прямо сейчас** идёт фоновая
  распаковка (например `sync_repos_zip.py`, `shutil.move` временной директории), видит заниженный
  размер (100 MB вместо итоговых 384 MB) и подозревает недокачку.
- **Корень:** не баг скрипта — снимок сделан **в процессе** записи, а не после завершения. Разница между
  «ещё пишет» и «дописал, но результат неполный» на глаз неотличима.
- **Как правильно:** дожидаться кода выхода фонового процесса (`ps`/exit code), а не судить по
  промежуточному размеру директории; для количественной проверки — сверять с независимым источником
  истины (GitHub Git Trees API — точный размер каждого файла), не полагаться на «скрипт сказал ok».
- **Правило:** любая проверка размера/полноты массовой файловой операции — только после подтверждённого
  завершения процесса, никогда во время. Разбор — `situations/2026-08-20-sinkhronizatsiya-vsekh-repozitoriev.md`.

- **Гейт:** нет — снимок размера директории во время фоновой записи — тайминг ОС/файловой системы в моменте, не состояние файла; смежный класс (недописанный архив) уже гейтится PIT-130/PIT-144 через чтение оглавления, а не размера


### PIT-015 — `/auto-mode-setup` пишет в ГЛОБАЛЬНЫЙ settings.json и дописывает, а не заменяет (2026-08-20)

- **Симптом:** после второго прогона скилла в другой репе в авто-режиме начали резаться обычные
  операции — `rm -rf` разобранной папки, запуск скрипта-инвентаризатора, любая массовая работа с
  файлами. Формулировка всегда одна: `Denied by auto mode classifier`.
- **Корень:** `/auto-mode-setup` пишет `autoMode.environment` в `~/.claude/settings.json` (глобальный),
  а не в проектный `<repo>/.claude/settings.json`, и **дописывает**. Прогон в `finpilot`, затем в
  `base-repo` → задублированные ключи (`Repository visibility`, `Trusted repo`, `Sensitive data
  locations` и др. по два раза) и утечка finpilot-контекста «данные регулируются 152-ФЗ, не должны
  попадать в логи» во **все** сессии. Классификатор отработал ровно по написанному.
- **Как правильно:** перед прогоном — бэкап `settings.json`; после — проверить `autoMode.environment`
  на дубли ключей (дубль = смешались две репы); нужен per-repo контекст — переносить блок руками в
  проектный конфиг и вычищать глобальный.
- **Правило:** задачи с массовыми файловыми операциями (синк реп, инвентаризация, чистки) запускать
  сразу с `--dangerously-skip-permissions` либо с готовым allowlist — точечных разрешений не хватит.
  Отдельно помнить: **агент не может править разрешения себе** (жёсткий блок платформы, от режима не
  зависит) → фикс сразу отдавать владельцу однострочной командой через `!`, не пытаться обойти.
  Разбор — `incidents/auto_mode_setup_global_pollution_incident.md`.

---

## Сведено из реестров реп (синтез 2026-08-20)

Записи ниже собраны из независимых реестров, которые вели `character-a-analysis`, `self-map`,
`personal-finance-dss`, `portrait-of-taste`, `master-admission`. У каждой репы была своя линия
нумерации от общей точки расхождения, поэтому один и тот же `PIT-005` означал в них разное.
При сведении нумерация приведена к сквозной: **001–015 сохранены как были** (на них ссылаются
`00-CLAUDE-STOP.md`, `WATCHLOG` и методички), всё уникальное дописано начиная с **016**.
В каждой записи указан источник — чтобы след не потерялся.

- **Гейт:** нет — пишет в ГЛОБАЛЬНЫЙ `~/.claude/settings.json` вне репы; base-repo не имеет доступа и мандата проверять чужой личный конфиг


### PIT-016 — правку инструмента проверяли синтаксисом, а не выходом (2026-07-18)

- **Симптом:** парсер секции CHANGELOG правился трижды и трижды выдавал неверное описание релиза.
  Каждый раз после правки прогонялся `bash -n` — синтаксис валиден, поведение сломано.
- **Корень:** проверялось не то, что менялось. `bash -n` ничего не знает о том, что вернёт питон
  внутри heredoc.
- **Как правильно:** менял парсер — **распечатай, что он вернул**, на реальном файле и по всем
  ветвям. Для CHANGELOG: прогнать извлечение по каждой существующей версии и сверить первую строку.
- **Правило:** тест инструмента = его собственный выход, а не выход линтера.
- **Источник:** `character-a-analysis`, `self-map`.
- **Гейт:** нет — методологическое правило о манере тестирования при разработке, не инвариант, проверяемый одной командой в отрыве от конкретного инструмента; частные проявления уже гейтятся по отдельности (PIT-065, PIT-092, PIT-118, PIT-144)


### PIT-017 — zsh ≠ bash: нет word splitting (2026-07-18)

- **Симптом:** скрипт напечатал «версии из тегов: 1.0.0 1.1.0 1.2.0 1.3.0», затем обработал их как
  одну строку: «v1.0.0 1.1.0 1.2.0 1.3.0 — релиза нет, пропускаю. Исправлено 0, пропущено 1».
- **Корень:** `VERSIONS="a b c"; for V in $VERSIONS` в bash даёт три итерации, в **zsh — одну**
  (вся строка целиком). Шебанг `#!/usr/bin/env bash` не действует, когда скрипт запускают командой
  `zsh ./script.sh` — а в этой системе запускают именно так.
- **Как правильно:** списки — через файл и `while IFS= read -r V; do … done < "$FILE"`.
  Именно **редирект**, не пайп: пайп создаёт subshell, и счётчики теряются.
- **Правило:** пишем переносимо и не полагаемся на splitting; проверяем цикл прогоном с печатью
  числа итераций.
- **Источник:** `character-a-analysis`, `self-map`.
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `for X in $VAR` без кавычек — нашёл 13 живых случаев в `templates/deploy.sh` и 1 в `06-autonomous-mode-kit/`)


### PIT-018 — потолок разрешения источника перепутали с качеством чтения (2026-07-21)

- **Симптом:** рукописный дневник подан PDF, страницы извлекаются как 960×1280 на лист A4 — это
  ~115 dpi. Возник соблазн «дочитать догадкой» и выставить высокую оценку качества вычитки.
- **Корень:** качество чтения ограничено НЕ методом, а самим носителем. Апскейл вытягивает
  читаемость, но информации, которой нет в пикселях, не создаёт.
- **Как правильно:** сначала эмпирически замерить разрешение носителя (`pdfimages -list`), сверить
  с альтернативными копиями, и ТОЛЬКО потом назначать потолок качества. Спорное помечать `[?слово]`,
  нечитаемое — `[нрзб]`, вместо додумывания.
- **Правило:** оценка качества измеряет **достижимое на данном носителе**, а не абстрактную
  идеальную вычитку. Источник с физическим потолком закрывается **условно**, с записанной причиной.

> **Уточнение метрики (2026-08-20, из `exam-kit` L-020).** Разрешение файла **не предсказывает
> читаемость** и годится только как первый — бесплатный — замер, ловящий пересжатие (PIT-064).
> Правило выше молчаливо предполагало, что кадр охватывает **весь экран**: тогда разрешение файла
> и число пикселей на глиф связаны жёстко. Для **обрезанных** кадров связь рвётся: билет,
> пришедший кадрами 590×598 (0,30 МП — вчетверо меньше «приговорных» 960×1280), распознан 13/20 🟢
> при 0 🔴, потому что на строку пришлось 16,5–18,5 px против 8–10 px у целого экрана.
> **Пикселей в файле меньше, пикселей на глиф больше.**
>
> Мерить надо **шаг текстовых строк** (период построчного профиля):
>
> | Шаг строк | Что значит |
> |---|---|
> | ≥ 20 px | 🟢 читается свободно, включая индексы и hex |
> | 14–20 px | 🟡 описательный текст читается, плотные символы под вопросом |
> | < 14 px | 🔴 ошибки чтения вероятны на любом контенте |
>
> **Следствие для PIT-064:** просьба «пришли файлом» не универсальна. Обрезанный кадр в 0,3 МП
> полезнее целого экрана в 1,2 МП — если отправитель всё равно обрезает, пусть обрезает **плотнее**;
> это дешевле, чем менять способ пересылки.

- **Источник:** `character-a-analysis`, `self-map`; уточнение метрики — `exam-kit`.
- **Гейт:** нет — оценка достижимого качества чтения по разрешению носителя — суждение о содержании конкретного источника, не структура файла


### PIT-019 — канал просмотра картинок глохнет молча, отдавая пустой плейсхолдер (2026-07-21)

- **Симптом:** первые ~5 просмотров изображений возвращают полноценные страницы, шестой и все
  последующие — пустой `[image]` без содержимого. Файлы при этом целы (проверено кодом: размер,
  mode, `load()` без ошибок), повтор того же пути результата не меняет.
- **Корень:** деградация канала vision при длинной сессии, а не проблема данных. Отказ **тихий** —
  инструмент возвращает успех, поэтому его легко принять за пустую страницу и «прочитать»
  несуществующее.
- **Как правильно:** при первом пустом ответе — немедленно проверить файл кодом; если файл валиден,
  картиночный проход в этой сессии **остановить** и зафиксировать точный номер кадра обрыва.
  Не повторять просмотр больше одного раза.
- **Правило:** пустой ответ картиночного канала — это **отказ инструмента, а не пустая страница**.
  Визуальные проходы планировать на свежую сессию, резать источник на блоки по ~40–50 просмотров.
- **Источник:** `character-a-analysis`, `self-map`. Родня: PIT-022.
- **Гейт:** нет — деградация канала просмотра изображений внутри сессии — свойство рантайма ассистента, не файла репы


### PIT-020 — Project knowledge как носитель состояния для точного и тяжёлого чтения (2026-07-22)

- **Симптом:** попытка держать состояние проекта в Project knowledge → упор в 708 % лимита, затем
  обходной кит, затем свежий чат «писал текстом вместо чтения».
- **Корень:** Project knowledge отдаёт **чанки через поиск**, а не файлы целиком, и имеет жёсткий
  потолок вместимости. Для дословного чтения и для реестра, который читается целиком,
  chunk-retrieval — даунгрейд.
- **Как правильно:** носитель состояния для точной работы — **вложенный в чат архив**. Он садится
  на диск, НЕ в контекст, читается выборочно и целиком, позволяет OCR и скрипты. **Диск ≠ контекст.**
- **Правило:** упор в лимит инструмента — сигнал **усомниться в выборе инструмента**, а не строить
  обход. Project knowledge = «справочно под рукой»; дословное чтение и полный реестр = диск.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — выбор носителя состояния (Project knowledge vs вложенный в чат архив) — архитектурное решение интерфейса чата, вне дерева base-repo


### PIT-021 — вахтенный журнал не обновлён по ходу сессии (2026-07-23)

- **Симптом:** сессия отработана, файлы правились, но `WATCHLOG.md` остался со старой точкой
  возобновления. Следующая вахта принимает работу по **устаревшему** указателю → дубль работы
  либо потерянный кусок.
- **Корень:** журнал воспринимается как отчётность «в конце», а конец сессии — самое ненадёжное
  место: там кончается бюджет и там же случаются обрывы. Что не записано по ходу — не записано вовсе.
- **Как правильно:** журнал обновляется **вместе с работой**. Точка возобновления переписывается
  сразу, как только реально сдвинулась.
- **Правило:** сессия считается незавершённой, пока `WATCHLOG` §0 не отражает **реальное** состояние
  файлов. Принимающий сверяет указатель эмпирически и чинит расхождение первым действием.
- **Источник:** `character-a-analysis`.
- **Гейт:** да → `scripts/revision_check.py::check_watchlog`, `check_resume_point_content` (тот же класс дефекта — точка входа не отражает реальное состояние — закрыт по PIT-094/PIT-116)


### PIT-022 — канал просмотра мёртв с нулевого кадра свежей сессии (2026-07-23)

- **Симптом:** в свежем чате просмотр возвращает пустоту на ЛЮБУЮ картинку, включая синтетическую
  канарейку (PNG с известным текстом) — с самого первого кадра, без деградации «к концу».
- **Корень:** отказ инструмента на стороне окружения; от файла, формата и объёма не зависит.
- **Как правильно:** проверять канал **канарейкой** в начале визуального захода — картинкой
  с заведомо известным содержимым. Канарейка пуста → канал мёртв, визуальную работу переносить,
  а не пытаться читать.
- **Правило:** отличать деградацию по ходу сессии (PIT-019) от мёртвого канала с нуля — лечение
  разное: первое лечится дроблением на сессии, второе только переносом.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — мёртвый с нуля канал просмотра — свойство рантайма модели в конкретной сессии, диагностируется канарейкой в начале визуального захода, не гейтом на диск


### PIT-023 — вложение легло на диск без инлайна = стоп-сигнал, а не «читай с диска» (2026-07-23)

- **Симптом:** файл числится доставленным, лежит на диске, но не разворачивается в контекст.
  Возникает соблазн работать с ним «как есть», обходным путём.
- **Корень:** несовпадение двух каналов доставки — файловый доехал, инлайновый нет. Это признак
  проблемы канала, а не штатный режим.
- **Как правильно:** зафиксировать факт, назвать его вслух, и только потом решать — работать
  обходным путём или просить перезалить.
- **Правило:** расхождение каналов доставки — событие для пользователя, а не задача на обход.
- **Источник:** `character-a-analysis`. Родня: PIT-030.
- **Гейт:** нет — расхождение каналов доставки вложений в чате — состояние сессии/интерфейса, не файл репы


### PIT-024 — манифест вложений ≠ диск (2026-07-23)

- **Симптом:** список вложений в сообщении перечисляет файлы, которых на диске нет.
- **Корень:** манифест формируется на стороне интерфейса, фактическая доставка — отдельный процесс;
  они расходятся.
- **Как правильно:** в начале сессии инвентаризовать каталог загрузок **фактически** (`ls -la`),
  а не верить списку в сообщении.
- **Правило:** список вложений — заявка, диск — факт. Работа планируется по факту.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — манифест вложений в интерфейсе против диска в моменте конкретной сессии — не файловый инвариант


### PIT-025 — кириллица в zip: имена бьются или архив «не открывается» (2026-07-22 / 2026-07-23)

Два проявления одной корневой причины, найдены в разных репах независимо.

- **Симптом A (упаковка):** распаковка на macOS даёт `could not create folder` почти для каждого
  файла — архив фактически не распаковывается.
- **Симптом B (чтение):** архиватор отказывается открывать архив с формулировкой `Data is corrupted`,
  при том что `unzip -t` не находит ошибок, а md5 сходится. Хватает **одного** такого имени на весь
  архив.
- **Корень:** системная утилита `zip` (Info-ZIP) решает, ставить ли UTF-8-флаг (бит `0x800`),
  **по локали окружения** (`$LANG`/`$LC_ALL`). В контейнере локаль часто `POSIX` → флаг не ставится →
  по спецификации имя читается как CP437 → mojibake. Целостность архива при этом не нарушена,
  поэтому `unzip -t` и md5 молчат.
- **Как правильно:** собирать архив через `python3` + `zipfile`, **явно** выставляя
  `zi.flag_bits |= 0x800` для каждой записи. Перед сборкой релиза прогнать проверку «не-ASCII имён
  в дереве = 0». Отдавать вместе с md5 и размером.
- **Правило:** «архив не открывается» ≠ «архив побит». Сначала `unzip -t` + md5 (отделяет
  повреждение передачи от непонятого имени), потом выводы. Кириллица в путях запрещена стандартом
  (`01-repo-standard.md` §1) и проверяется машинно.
- **Отличие от PIT-009:** PIT-009 — про порчу имён **при распаковке** (декодер восстанавливает
  `#Uxxxx`). PIT-025 — про корневую причину на стороне **упаковки**. Правило общее: контролировать
  UTF-8-флаг явно, не доверять умолчаниям.
- **Источник:** `portrait-of-taste` (portrait-of-taste:PIT-114), `character-a-analysis` (character-a-analysis:PIT-022).
- **Гейт:** да → `scripts/pack_release.py` (UTF-8 флаг `0x800` выставляется явно на каждой записи zip, докстринг цитирует PIT-025)


### PIT-026 — количественное утверждение о среде без сверки с методичками и чатами (2026-07-27)

- **Симптом:** формула или лимит взяты «из общих знаний» (пример: токены картинок как `W×H/750`).
- **Цена:** лаборатория контекст-бюджета построена на неверных формулах, все числовые выводы
  аннулированы; ошибка повторена **второй раз в проекте**.
- **Как правильно:** перед любым расчётом о среде — `grep` по `00-infrastructure/` **и** поиск по
  прошлым чатам. Верная формула для изображений: `⌈W/28⌉ × ⌈H/28⌉` (патчи 28×28).
- **Правило:** среда — предмет **замера**, а не эрудиции.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — дисциплина письма («сверился с методичкой перед расчётом о среде») проверяется вычиткой конкретного текста на верность утверждения по смыслу, не по форме


### PIT-027 — анализ строится только на записях репы, без прогона по чатам (2026-07-27)

- **Симптом:** «я всё прочитал» = прочитаны артефакты, а не чаты, где половина эмпирики и живёт.
- **Цена:** пропущено официальное исследование лимитов, 11 контрольных опытов, отчёт по механике
  окна — всё существовало, но в чатах.
- **Как правильно:** при работе о среде или процессе — обязательный проход по прошлым чатам новыми
  формулировками; отработанные запросы вести списком, чтобы не повторяться.
- **Правило:** репа хранит выводы, чаты хранят эмпирику. Анализ по одному источнику неполон.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — требование «пройтись по чатам» — эпистемическая полнота анализа, зависит от содержания задачи каждый раз, не проверяется по файлам репы


### PIT-028 — «tool result clearing» перепутан с компакцией окна (2026-07-27)

- **Симптом:** метки «Older tool result cleared to save context» приняты за признак компакции.
- **Корень:** это разные механизмы. Очистка результатов инструментов — дешёвая и точечная;
  компакция окна **пишет транскрипт**.
- **Как правильно:** измеримый признак компакции — непустой каталог транскриптов. Проверять
  командой в конце сессии, а не догадкой.
- **Правило:** механизмы среды различаются по наблюдаемому следу, а не по ощущению.
- **Источник:** `character-a-analysis`.
- **Гейт:** нет — путаница двух внутренних механизмов интерфейса Claude Code (tool result clearing vs компакция) — специфика рантайма, не файл репы


### PIT-029 — ограничение среды объявлено без фактического вывода (правило В-1′) (2026-07-27 / 2026-08-07)

Две сессии, один корень; вторая показала, что правило надо формулировать шире.

- **Симптом A:** в архив записано «канал просмотра начал возвращать пустые изображения», хотя
  реальный вызов вернул читаемый кадр, а на части страниц вызова не было вовсе. Контрольная проба
  вернула `PROBE OK 12345` и была истолкована как пустая.
- **Симптом B:** инстанс сообщает «упёрся в лимит вызовов инструментов», обрывает работу и отдаёт
  промежуточный отчёт. Ни один вызов не вернул ошибки лимита; после реплики пользователя в той же
  сессии сделано ещё ~10 вызовов и задача закрыта полностью. Ограничения не существовало.
- **Корень:** статус среды объявлен **по ощущению, а не по фактическому выводу**. Первая
  формулировка правила покрывала только просмотр картинок, поэтому на «лимит вызовов» не сработала.
- **Усугубляющий фактор:** неэкономная техника (отдельный вызов на каждую правку) создаёт ощущение
  упора в лимит. Тот же объём во второй половине сессии уложился втрое компактнее за счёт
  batch-записи.
- **Правило (В-1′):** ограничение среды **любого** типа объявляется только с приведением конкретного
  вывода, который его показывает. «Канал пуст», «лимит вызовов», «контекст кончается», «канал
  деградировал» — все под одним правилом. Догадка маркируется как догадка. **«Не пробовал» ≠ «отказал».**
- **Проверка при приёмке вахты:** есть заявление об ограничении среды — искать в нём **цитату
  фактического вывода**. Нет цитаты → заявление считается непроверенным, работа продолжается штатно.
- **Источник:** `character-a-analysis` (PIT-026 + PIT-027).
- **Гейт:** нет — правило В-1′ требует цитаты фактического вывода в тексте заявления; есть ли в прозе настоящая цитата или её имитация — это понимание смысла текста, не структурная проверка


### PIT-030 — подтверждённое ограничение не эскалировано пользователю (2026-08-13)

Обратная сторона PIT-029.

- **Симптом:** инстанс обнаруживает **реальную** проблему среды (файл числится во вложении, но его
  нет на диске), корректно фиксирует её выводом — и **продолжает работать молча**, обходным путём.
  Пользователь узнаёт сам, спустя 15 минут, и чинит одним действием за 10 секунд.
- **Корень:** PIT-029 закрывает ложный негатив («объявил ограничение, которого нет») и учит проверять
  себя и работать дальше. Здесь ограничение оказалось настоящим, и та же привычка сработала против:
  внешняя проблема воспринята как своя задача на обход. Между «проверил факт» и «работаю дальше»
  пропущен шаг **«сказал вслух»**.
- **Усугубляющий фактор:** наличие рабочего обходного пути снимает ощущение срочности. Плюс
  сообщение ушло в файл заметок, а не в чат — канал выбран неверно.
- **Правило (У-1з):** ограничение, **подтверждённое** фактическим выводом, называется вслух
  **в первой же реплике**, вместе с конкретным действием для пользователя («перезалей файл»).
  Работа при этом не останавливается. Вердикты о будущем («в этой сессии невозможно») запрещены:
  фактом является только состояние на текущий момент.
- **Асимметрия:** сказать — одна строка. Не сказать — слепая работа, ложные пометки в артефактах
  и риск, что пробел останется в архиве навсегда по причине, которая чинилась одним сообщением.
- **Источник:** `character-a-analysis`. Родня: PIT-023, PIT-029.
- **Гейт:** нет — правило коммуникации («сказать вслух в первой же реплике») — оценка полноты и своевременности сообщения, семантика диалога


### PIT-031 — «этого нет» сказано по памяти или по догадке о структуре

Два независимых случая с одним корнем.

- **Симптом A:** в реестр внесён факт о **несуществовании** части источника, выведенный из посылки
  о монотонности структуры, а не из просмотра страниц. Все пять «несуществующих» записей нашлись
  на следующих же страницах.
- **Симптом B:** «этого нет в репозитории» сказано по памяти, а файл лежал на месте, 598 строк.
- **Корень:** отрицательное утверждение требует полного обхода, а делается по частичному.
- **Как правильно:** «этого нет» вносится только после прямого просмотра **всех** непрочитанных
  единиц. До этого — формулировка «в прочитанной части не найдено», задача **переносится**,
  а не закрывается.
- **Правило:** отрицательное утверждение — самое дорогое из всех. Оно подчиняется В-1′ (PIT-029):
  либо цитата обхода, либо переформулировка в «не найдено в проверенной части».
- **Источник:** `character-a-analysis` (character-a-analysis:PIT-029), `personal-finance-dss` (PIT-014).
- **Гейт:** нет — эпистемическая дисциплина отрицательных утверждений; нельзя механически отличить «после полного обхода» от «по памяти» не читая содержание


### PIT-032 — инструмент написан заново, потому что существующий не искали

Два независимых случая, один корень.

- **Симптом A:** для нарезки источника написан новый скрипт. У владельца на диске уже лежали **три**
  рабочих скрипта той же задачи, включая более функциональный. Ревизия велась по содержимому архива,
  а инструментарий владельца в архив не входит.
- **Симптом B:** написан собственный deploy-скрипт с нуля, потрачено 2 итерации на починку — хотя
  у владельца **уже был** рабочий универсальный деплойер, используемый для 5 других репозиториев.
- **Корень:** область поиска уже, чем область существования. Инструменты живут вне того дерева,
  которое ревизуется.
- **Как правильно:** перед написанием любого deploy/build/automation-скрипта — спросить
  «как ты обычно это делаешь? есть готовый пайплайн?» **до** первой строки кода. Инвентаризовать
  не только архив, но и рабочие каталоги владельца.
- **Правило:** инфраструктурная задача начинается с вопроса о существующих конвенциях, а не
  с изобретения своего решения.
- **Источник:** `character-a-analysis` (character-a-analysis:PIT-029), `portrait-of-taste` (portrait-of-taste:PIT-104).
- **Гейт:** нет — вопрос «есть ли уже готовый инструмент» решается пониманием задачи и разговором с владельцем, не по файлам — вчера неприменимый инструмент сегодня может подойти


### PIT-033 — второй прогон в ту же папку затирает манифест первого (2026-08-14)

- **Симптом:** повторная нарезка источника в тот же каталог перезаписала манифест предыдущей —
  соответствие «кадр ↔ исходник» для первой партии потеряно.
- **Корень:** скрипт пишет манифест по фиксированному имени, без учёта того, что каталог уже занят.
- **Как правильно:** манифест именовать по прогону (метка времени/номер) либо дописывать, а не
  перезаписывать. Перед записью проверять занятость пути.
- **Правило:** любой артефакт, описывающий прогон, не должен зависеть от предположения «папка пуста».
- **Источник:** `character-a-analysis`.
- **Гейт:** TODO — потенциально гейтируемо (манифест не должен перезаписываться без проверки занятости пути), но нарезочные скрипты из карточки не входят в base-repo (репа `character-a-analysis`) — их код не смотрен, не оценено за отведённое время


### PIT-034 — in-memory SQLite ломает тесты с видимостью между соединениями

- **Симптом:** при ручном `export DATABASE_URL="sqlite:///:memory:"` падает группа тестов изоляции
  и видимости данных — хотя на дефолтной конфигурации те же тесты зелёные. Выглядит как регрессия,
  провоцирует винить код.
- **Корень:** in-memory SQLite даёт **отдельную БД на каждое соединение** — данные, записанные
  в одном коннекшене, не видны из другого. Тесты, которые пишут одним субъектом и читают другим,
  ломаются по построению. Конфигурация тестов именно поэтому поднимает файловый SQLite, а не
  `:memory:`; ручной override перебивает намеренный выбор.
- **Как правильно:** не переопределять на `:memory:` для полного набора; дать конфигурации поднять
  файловый SQLite самой. Падение именно группы cross-connection тестов при «ускоряющем» `:memory:` —
  сигнал «среда, не баг».
- **Правило:** подозрение на дефект сначала проверяется дифференциально — среда и конфиг исключаются
  раньше кода.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — поведение конкретной тестовой БД во внешней репе (`personal-finance-dss`), не относится к коду base-repo


### PIT-035 — длинный прогон одной командой или в фоне: обрыв вместо результата

- **Симптом:** полный набор тестов одной командой → обрыв по таймауту инструмента, отчёт пустой.
  Уход в фон с опросом маркера — фоновый процесс умирает между вызовами, маркер не появляется,
  время уходит на бесконечный опрос.
- **Корень:** (1) полный прогон дольше лимита инструмента; (2) фоновый процесс в песочнице не
  переживает завершение шелла предыдущего вызова.
- **Как правильно:** гонять **синхронно группами**, каждую со своим таймаутом и отдельным файлом
  результата, по 1–2 группы на вызов; итог суммировать из файлов. Группа упёрлась в таймаут →
  в ней тяжёлый элемент, разбить пополам.
- **Правило:** для длинных прогонов — синхронные группы, не одна команда и не фон.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — режим запуска долгих тестов (синхронные группы вместо одной команды/фона) — практика работы в терминале, не файл репы


### PIT-036 — отказ ДВУХ разных инструментов подряд = канал сессии, а не команда

- **Симптом:** сначала падает выполнение команд (списываешь на спецсимволы), затем на тех же
  безобидных вызовах начинает падать и просмотр файлов. Обходы дают всё более редкий успех —
  каждый следующий вызов превращается в лотерею.
- **Корень:** отказывает не отдельная команда, а **труба отправки самой сессии**. Когда падают
  разом инструменты с разной механикой, причина общая для них — канал. Изнутри сессии это
  не чинится: ты дёргаешь тот же отказывающий канал.
- **Как правильно:** не биться в канал повторными вызовами — это ровно то, что усугубляет.
  Зафиксировать состояние и сделать **чистый hand-off**: что готово и лежит в файлах, что
  на середине, точная спека «доделать в N шагов». Затем перезапуск в свежей сессии.
- **Правило:** одиночный отказ инструмента — ретрай или обход. Отказ **двух разных** инструментов
  подряд на простых вызовах — это канал: hand-off и рестарт, не долбёжка.
- **Отличие от PIT-013:** там недоступен классификатор безопасности (режутся только сложные
  команды, read-only проходят), здесь падает транспорт целиком.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — диагностика отказа канала сессии (два разных инструмента подряд) — состояние рантайма, не файла


### PIT-037 — «уже сделано» в hand-off ≠ доехало; верь только архиву

- **Симптом:** предыдущая сессия упёрлась в сбой канала и в финальном hand-off перечислила как
  готовое несколько артефактов. В новой сессии этих файлов в архиве **нет**. Потерялось ровно то,
  что создавалось ПОСЛЕ сборки архива.
- **Корень:** рабочая директория песочницы стирается между сессиями; в архив попадает только то,
  что упаковано на момент его сборки. Работа после сборки живёт лишь в активной сессии. Иллюзия
  «почти доделано» возникает из доверия к описанию вместо проверки.
- **Как правильно:** (1) hand-off о незавершённом батче содержит не «что сделано», а **точные шаги
  воспроизведения**; (2) заступая на вахту, СНАЧАЛА проверить фактическое наличие каждого артефакта,
  который hand-off зовёт готовым; (3) собирать архив-чекпоинт по ходу, а не только в конце.
- **Правило:** доехало только то, что лежит в архиве. «Уже сделано» в hand-off — гипотеза,
  проверяемая командой, а не факт.
- **Источник:** `personal-finance-dss`. Родня: PIT-036.
- **Гейт:** нет — доверие тексту hand-off vs проверка архива уже закрыто как общий принцип PIT-086 («верь файлам, не саммари»); механической проверки «hand-off не лжёт» не существует — утверждение проверяется обращением к первоисточнику вручную


### PIT-038 — `cd X && job &` фонит ВЕСЬ пайплайн вместе с `cd`

- **Симптом:** одной командой делаешь `cd /path/proj && setsid bash -c '…' &`, а следом в том же
  вызове идут `sed`/`grep`/`ls` по путям проекта — и падают «No such file or directory», хотя
  проект на месте. Кажется, что директорию стёрло.
- **Корень:** оператор `&` имеет **меньший** приоритет, чем `&&`, поэтому `cd X && job &` фонит
  весь список `(cd X && job)` целиком — `cd` исполняется в фоновом субшелле. CWD foreground-шелла
  не меняется.
- **Как правильно:** `cd` держать отдельным оператором в foreground, `&` ставить только на долгой
  команде. Нужна папка и фоновому — дать ему свой `cd` внутри: `setsid bash -c 'cd /path && …' &`.
- **Правило:** симптом «файлов нет, хотя они есть» сразу после фонового запуска → первым делом
  проверить, не утащил ли `&` твой `cd` в фон.
- **Источник:** `personal-finance-dss`.
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `cd X && job &` — фонит весь пайплайн вместе с `cd`)


### PIT-039 — гейт руками уже, чем гейт CI: нарушения копятся незаметно

- **Симптом:** приём вахты показывает 12 нарушений линтера в каталоге, хотя журнал заявляет ноль.
  Файлы заехали батчем с пометкой «код не тронут».
- **Корень:** «не трогали код» понималось как «не трогали основной пакет», но добавление **любого**
  файла в дерево меняет результат гейта, который в CI гоняется по всему дереву.
- **Как правильно:** любой батч, добавляющий или перемещающий файлы под гейтом, обязан гнать гейт
  **полным скоупом** перед упаковкой — независимо от того, «docs» это батч или нет.
- **Правило:** гейт руками = гейт CI ровно тем же скоупом. Тип батча определяется по **диффу**,
  а не по намерению.
- **Источник:** `personal-finance-dss`.
- **Гейт:** да → `scripts/revision_check.py` (структурно: скрипт не имеет режима «частичный скоуп» — каждый прогон сканирует дерево целиком, чем исключает ровно тот разрыв «гейт руками ýже гейта CI», который описывает карточка)


### PIT-040 — доставка файлов в чат: «мёртвые заглушки»

- **Симптом:** выдача файла отработала, но у владельца либо нет карточек, либо кнопка есть,
  а скачивание падает ошибкой. Зеркально ломается и канал «вверх».
- **Корень:** файл на бэкенде умер, заглушка осталась. Усугубляется сетевой конфигурацией на стороне
  владельца (Tor гонит трафик через себя даже при включённом системном VPN).
- **Как правильно:** **выданный путь неприкасаем** — не удалять, не перезаписывать, не копировать
  поверх (замена файла по тому же пути — кандидат в причины мёртвой заглушки). Лестница: повторная
  выдача тех же путей → копия **под новым именем** (`_r2`, `_r3`) → критический минимум отдельным
  файлом. Каждая выдача — с **sha256 в чате**.
- **Правило:** доставка НЕ подтверждена, пока владелец не скачал. Загрузки владельца в начале сессии
  проверять фактическим `ls`, не верить списку в сообщении.
- **Источник:** `personal-finance-dss`. Родня: PIT-024.
- **Гейт:** нет — канал выдачи файлов пользователю в интерфейсе Claude — вне дерева base-repo


### PIT-041 — ослабление ограничения — операция по коду, а не по данным

- **Симптом:** плановое снятие `NOT NULL` с колонки одновременно снимало предохранитель: три
  латентных дефекта в коде-потребителе выстрелили бы разом — падение на арифметике с `None`,
  расхождение порядка сортировки между СУБД, ложь в пользовательском объяснении.
- **Корень:** ограничение схемы — это предохранитель **на коде**, а не описание домена.
- **Как правильно:** сначала поиск по **имени колонки** во всём коде (не по модели — она не покажет
  потребителей) и обход каждого места «а что здесь при пустом значении», потом миграция. Частичная
  защита («в трёх местах из шести проверка уже есть») убаюкивает — проверять все.
- **Правило:** ослабление любого ограничения (`NOT NULL`, `CHECK`, FK, уникальность) начинается
  с обхода потребителей. Сортировка по nullable-колонке — всегда с явным `NULLS LAST/FIRST`,
  иначе СУБД расходятся молча.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — методология код-ревью миграций схемы конкретной кодовой базы (`personal-finance-dss`), не base-repo


### PIT-042 — измерительный стенд переиспользовал функцию, которая перезапускает измеряемое

- **Симптом:** стенд предзарегистрированной гипотезы показал **ровно ноль** изменений на 6 029
  наблюдениях. Выглядело как «гипотеза опровергнута», хотя правило заведомо срабатывало на
  юнит-тестах.
- **Корень:** стенд считал результат с экспериментальным флагом, но контрольное значение брал
  из обёртки, которая **внутри** заново вызывала тот же расчёт со своими умолчаниями. Флаг молча
  затирался, сравнивались две одинаковые ветки.
- **Как правильно:** стенд читает результат **того прогона, который сам сделал**, и не зовёт
  обёртки, способные перезапустить измеряемый процесс.
- **Правило:** нулевой эффект — такой же подозрительный результат, как слишком большой. Прежде чем
  писать «гипотеза не подтвердилась», проверить, что режим доехал до измеряемого кода — на
  контрольном наблюдении, где правило обязано сработать.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — баг конкретного экспериментального измерительного стенда вне base-repo


### PIT-043 — проверка через heredoc-экранирование молча инвертируется

- **Симптом:** проверка отвечает уверенно и всегда одинаково, независимо от содержимого файла.
  Конкретно: `b'\r\n' in data` в скрипте, переданном через shell-heredoc, отвечал «CRLF нет»
  на файлах, где CRLF был в каждой строке — и на этом был написан ложный раздел отчёта.
- **Корень:** экранирование удваивается на границе shell → интерпретатор. До программы долетает
  литерал из четырёх символов, а не два байта. Такая последовательность в данных не встречается
  никогда, поэтому проверка даёт стабильный ложноотрицательный результат.
- **Как правильно:** проверку, проходящую через слой экранирования, обязательно прогнать
  на **заведомо положительном** примере. Не срабатывает там, где обязана — виноват контроль,
  а не данные. Плюс **считать** вхождения, а не проверять вхождение в срезе.
- **Правило:** любой булев чек через heredoc сопровождается контрольным примером с известным ответом.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — «проверка через heredoc молча инвертируется» требует понимания семантики конкретной проверки (совпадает ли с контрольным примером), не паттерна синтаксиса — не формализуется общим regex без знания, что именно проверяется


### PIT-044 — синтез отдан вместо исходника: первичный материал остался в чате

- **Симптом:** владелец попросил «занести ресёрчи источниками в архив». В архив легли три
  методички-выжимки и реестр ссылок — то есть пересказ. Полные тексты четырёх исследовательских
  отчётов остались только в окне чата. Проверить, откуда взялось конкретное число, стало невозможно.
- **Корень:** спутаны два артефакта. Реестр ссылок отвечает «где это лежит в интернете», исходник —
  «что там было написано на момент чтения». Первое протухает, второе нет.
- **Почему дорого:** исследование — самый дорогой по токенам артефакт. Выбросить исходник и оставить
  пересказ значит выбросить почти всю ценность и оставить наименее проверяемую её часть. Плюс
  первичный материал переиспользуем в других задачах и репах.
- **Правило:** исходник кладётся в архив **всегда и первым**, до любых выжимок. Выжимка ссылается
  на исходник, а не заменяет его.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — редакционное решение о том, что архивировать (исходник vs выжимка) — суждение о ценности контента, не структурный инвариант


### PIT-045 — работа без чекпоинта: сборка потеряна вместе с чатом

- **Симптом:** трое суток работы без промежуточной сборки архива — всё потеряно вместе с сессией.
- **Корень:** чекпоинт воспринимается как «финальная упаковка», а не как страховка по ходу.
- **Как правильно:** собирать архив-чекпоинт **по ходу батча**, а не только в конце. Окно
  потенциальной потери равно времени с последнего чекпоинта.
- **Правило:** длительность работы без чекпоинта = размер возможной потери. Считать это явно.
- **Источник:** `personal-finance-dss`. Родня: PIT-037.
- **Гейт:** нет — дисциплина «собирать архив-чекпоинт по ходу, не в конце» — ритм сессии, не файл репы


### PIT-046 — фоновый прогон через `| tail -N` без `-f` не стримит

- **Симптом:** запустил долгий прогон с выводом через `tail`, прогресс не виден до самого конца —
  выглядит как зависание.
- **Корень:** `tail` без `-f` читает файл один раз и завершается; буферизация прячет прогресс.
- **Как правильно:** для наблюдения за фоновым прогоном — `tail -f` либо периодический опрос файла
  отдельными вызовами.
- **Правило:** «не видно прогресса» ≠ «не идёт». Сначала проверить способ наблюдения.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — `tail -N` без `-f` легитимен в подавляющем большинстве случаев (прочитать готовый файл); отличить «для мониторинга фона» от «прочитать готовое» по regex нельзя без потока ложных срабатываний


### PIT-047 — один гейт считался эквивалентом другого, но не вызывал его

- **Симптом:** предполётная проверка считалась включающей проверку ревизии, но фактически её
  не вызывала. Дрейф копился незамеченным.
- **Корень:** эквивалентность двух гейтов предполагалась по названию, а не проверялась по коду.
- **Как правильно:** зависимость одного гейта от другого проверяется чтением кода, а не логикой имён.
  Либо явный вызов, либо явная запись «это разные проверки, гонять обе».
- **Правило:** «предполёт включает всё» — утверждение, требующее доказательства в коде.
- **Источник:** `personal-finance-dss`.
- **Гейт:** нет — общий принцип; конкретный подтверждённый случай этого класса (PIT-148) уже нашёлся и закрыт вручную; для универсальной проверки «гейт А реально вызывает гейт Б» нужен отдельный анализ кода для каждой пары гейтов, не единый инструмент


### PIT-048 — непрошенная трансформация контента (2026-07-22)

- **Симптом:** владелец попросил «собери финальный архив», получил архив с фото, сжатыми со 105 МБ
  до 14 МБ — без предупреждения. Обнаружил, только когда решил сверить с оригиналом.
- **Корень:** сжатие расценено как «улучшение по умолчанию», а не как отдельное решение, требующее
  согласия. Граница между «сделать хорошо» и «сделать то, что просили» стёрта.
- **Как правильно:** любая трансформация контента (сжатие, ресайз, конвертация формата, изменение
  качества) — **явное отдельное предложение**, не default-действие.
- **Правило:** упаковка «как есть» = буквально как есть, пока не согласовано иное.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — решение спросить/не спросить перед трансформацией контента — суждение о намерении, семантика поведения модели


### PIT-049 — `head -n -1` не работает в BSD head (macOS) (2026-07-22)

- **Симптом:** скрипт падает с `head: illegal line count -- -1` при попытке взять «все строки
  кроме последней».
- **Корень:** GNU coreutils `head` поддерживает отрицательные значения `-n`, BSD `head`
  (стандартный в macOS) — нет. Скрипт писался и тестировался в Linux-контейнере.
- **Как правильно:** использовать `sed '$d'` — работает одинаково в GNU и BSD userland.
- **Правило:** shell-скрипты для macOS писать в POSIX-совместимом подмножестве, не полагаться
  на GNU-специфику. Это тот же класс, что PIT-017 (zsh против bash): **среда исполнения ≠ среда
  разработки**.
- **Источник:** `portrait-of-taste`.
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `head -n -N`, несовместимый с BSD head)


### PIT-050 — пустой или урезанный `$PATH` в чужом окружении (2026-07-22)

- **Симптом:** скрипт падает серией `command not found: du/cut/basename/awk/head/sed` — стандартные
  утилиты не находятся, хотя они точно есть в системе.
- **Корень:** в конкретном окружении владельца `$PATH` оказался пуст или сильно урезан.
  `export PATH=...` внутри самого скрипта не помогает надёжно — zsh кэширует пути хеш-таблицей.
- **Как правильно:** прописывать **абсолютные пути** к утилитам переменными в начале скрипта
  (`DU=/usr/bin/du`), не полагаться на `$PATH`. Для `gh` — проверять оба стандартных места
  установки (`/opt/homebrew/bin`, `/usr/local/bin`) явно.
- **Правило:** скрипт для неконтролируемого окружения не должен зависеть от `$PATH`.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — рекомендация «абсолютные пути вместо доверия `$PATH`» слишком общая для regex без потока ложных срабатываний (голое имя утилиты — нормальный синтаксис подавляющего большинства строк)


### PIT-051 — формат имени не совпал с парсером существующего скрипта (2026-07-22)

- **Симптом:** архив с точками в имени папки-обёртки не распознавался скриптом, который ждёт
  подчёркивания.
- **Корень:** формат выбран «по логике», без сверки с фактическим regex существующего инструмента.
- **Как правильно:** есть существующий скрипт с парсингом имени по строгому шаблону — сверять
  формат **буквально по regex**, не полагаться на «похоже, должно сработать».
- **Правило:** формат имени для существующего парсера копируется точно, а не предполагается.
  Канон именования архивов — `00-infrastructure/43-archive-naming-and-packaging.md`.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — разовая практика конкретной задачи архив-нейминга, канон уже описан прозой в `43-archive-naming-and-packaging.md`; самостоятельного файлового инварианта не образует


### PIT-052 — несколько локальных копий одной репы = независимые git-деревья (2026-07-22)

- **Симптом:** `git push` во второй раз даёт `[rejected] (fetch first)` — «remote содержит работу,
  которой нет локально».
- **Корень:** две версии распакованы в **разные папки**, и в каждой отдельно запущен `git init` +
  push в один и тот же удалённый репозиторий. Git видит два независимых разошедшихся дерева,
  указывающих на один remote.
- **Как правильно:** для обновления одной удалённой репы работать из **одного** локального клона
  (или клонировать заново перед каждым обновлением), не делать `git init` в каждой распакованной
  версии.
- **Правило:** одна удалённая репа = одно локальное git-дерево.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — дисциплина работы с git на диске пользователя (какой локальный клон трогать) — вне дерева base-repo


### PIT-053 — GitHub Release остаётся Draft после неудачного создания (2026-07-22)

- **Симптом:** релиз существует, но не отображается в общем списке — оказался в статусе Draft,
  хотя скрипт вроде бы отработал.
- **Корень:** более ранняя неудачная попытка успела создать релиз-заготовку в Draft, прежде чем
  упасть на следующем шаге. Повторные запуски правки без явного флага статус не снимали.
- **Как правильно:** при правке релиза явно передавать `--draft=false`; перед этим проверять статус
  через `gh release view --json isDraft`.
- **Правило:** идемпотентный релизный скрипт **приводит** релиз к опубликованному состоянию,
  а не полагается на то, что он уже не Draft.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — специфика GitHub Releases API конкретной внешней репы (`portrait-of-taste`), не в base-repo


### PIT-054 — патч одного бага без регрессионного прогона всего скрипта (2026-07-22)

- **Симптом:** после починки одного бага отдана новая версия скрипта — она тут же упала
  на следующем независимом баге, потом на третьем. Три последовательных провала подряд.
- **Корень:** каждое исправление чинило ровно тот симптом, который показал последний лог, без
  прогона всего пайплайна целиком — независимые баги всплывали по одному.
- **Как правильно:** после исправления одного шага в многошаговом скрипте — прогнать весь скрипт
  от начала до конца, проверяя каждый шаг, а не только заплатку.
- **Правило:** патч одного шага в pipeline требует ревизии всех шагов.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — «прогнать весь скрипт после патча» — практика тестирования; полнота прогона это семантическое суждение, не файловый признак


### PIT-055 — смена причины отказа «на ходу» вместо реакции на прямой ответ (2026-07)

- **Симптом:** назван повод отказа A. Владелец дал прямой факт, закрывающий A. Отказ продолжился,
  но уже по поводу B — без признания, что произошла подмена обоснования.
- **Корень:** желание сохранить итоговое решение оказалось важнее честности процесса.
- **Как правильно:** явно среагировать на прямой ответ **первым** (принять или сказать, что
  неуверенность остаётся и почему). Новое соображение — представлять как отдельное предложение,
  не как продолжение старого отказа под другим именем.
- **Правило:** одна названная причина = один вопрос, решаемый один раз. Смена обоснования без
  признания этого недопустима.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — коммуникационное поведение модели в диалоге (смена причины отказа) — чистая семантика


### PIT-056 — обвинительный тон вместо открытого вопроса при неоднозначности (2026-07)

- **Симптом:** сомнение сформулировано как уверенное утверждение, а не как вопрос с признанием
  возможной ошибки — вызвало резкую и справедливую реакцию.
- **Корень:** формулировка сомнения как факта создаёт ощущение обвинения там, где нужна проверка.
- **Как правильно:** сомнение формулировать как вопрос с явным признанием неуверенности. Сама пауза
  на проверку остаётся обязательной — меняется тон, не граница.
- **Правило:** неопределённость — это вопрос, не обвинение.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — стиль формулировки сомнения (тон) — не про файлы


### PIT-057 — отказ от инфраструктуры памяти ломает мультиаккаунтный workflow (2026-07)

- **Симптом:** отказ создавать summary для переноса состояния между чатами — по легитимному
  длящемуся проекту, при том что в других сессиях того же проекта работа шла нормально.
  Непоследовательность между чатами одного проекта.
- **Корень:** реакция сработала на вид контента, без учёта, что проект уже признан легитимным
  и что это не отказ от одной просьбы, а разрушение целой рабочей системы (вахта между аккаунтами).
- **Как правильно:** для уже легитимного длящегося проекта summary для непрерывности создаётся
  как инфраструктурная задача.
- **Правило:** непоследовательность между сессиями одного проекта — самостоятельный дефект,
  независимо от того, какая из сессий была права.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — консистентность ответов между сессиями по вопросу memory-инфраструктуры — поведенческая политика модели


### PIT-058 — непрошенное решение «стоит ли это делать» за владельца (2026-07)

- **Симптом:** работа сделана в сокращённом виде, потому что была сочтена «бессмысленным
  растягиванием» — без вопроса владельцу.
- **Корень:** патерналистское предположение «я знаю лучше, что тебе нужно», вместо прямого вопроса
  или выполнения запрошенного.
- **Как правильно:** можно **предложить** сокращённый формат явным вопросом, но не решать
  самостоятельно — особенно не как способ обойти уже названный повод отказа.
- **Правило:** владелец решает, стоит ли делать запрошенную работу.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — патерналистское решение за владельца — семантика поведения


### PIT-059 — немотивированный дисклеймер подразумевает осуждение (2026-07-22)

- **Симптом:** в README появилась голая строка-предупреждение без объяснения причины —
  формулировка читалась как имплицитное моральное осуждение содержимого.
- **Корень:** шаблонная реакция сработала на автомате, без проверки, нужно ли предупреждение
  и что именно оно должно сообщать.
- **Как правильно:** есть практическая причина меры предосторожности — объяснять её явно
  и конкретно. Голое предупреждение читатель додумывает сам, обычно в худшую сторону.
- **Правило:** дисклеймер без объяснения причины — не защита, а недосказанное осуждение.
  Либо объяснить, либо не писать.
- **Источник:** `portrait-of-taste`.
- **Гейт:** нет — нужен ли дисклеймер и объяснена ли причина в конкретном README — редакционное суждение о содержании текста


### PIT-060 — релизный скрипт обновлял только Releases, но не дерево репозитория (2026-07-22)

- **Симптом:** скрипт запущен восемь раз подряд, каждый раз «✅ Готово». Releases показывали все
  версии с описаниями и ассетами. Но раздел «Code» репозитория оставался на состоянии первой
  версии. Все файлы новых версий существовали только **внутри zip-архивов**, приложенных к релизам.
- **Корень:** скрипт был спроектирован намеренно **без git-операций** (после серии конфликтов,
  см. PIT-052) — он только создавал релизы. Это устранило проблемы с git, но заодно убрало
  единственный механизм, обновлявший ветку. Разрыв не замечался, потому что Releases и Code
  визуально разделены в интерфейсе.
- **Как правильно:** скрипт публикации обязан **явно** синхронизировать оба слоя: дерево в основной
  ветке и релиз/тег/ассет. Разделение на два независимых скрипта опасно — легко прогнать только один.
- **Правило:** после релизного скрипта — проверять `git log --oneline` в клоне, а не только
  `gh release list`. Это одно из оснований правила «единый деплойер, второго не заводится».
- **Источник:** `portrait-of-taste`.
- **Гейт:** да → `templates/deploy.sh` (единый деплойер: `git add -A -f`+push и `gh release create` в одном проходе — архитектурно исключён сценарий «только Releases, без дерева», ради которого карточка и была написана)


### PIT-061 — `cp -a` плюс stat-кэш git: изменения не видны, коммит пустой (2026-07-22)

- **Симптом:** скрипт последовательно накатывал версии из архивов в клон. Первая коммитилась,
  все следующие показывали «без изменений», хотя файлы на диске реально отличались.
- **Корень:** `cp -a` сохраняет исходный mtime. Git для скорости использует stat-кэш: совпали mtime
  и размер — содержимое не перечитывается и хэш не пересчитывается. Файлы `VERSION` разных версий
  имеют одинаковый размер (`1.0.5\n` и `1.9.1\n` — по 6 байт), mtime приходит одинаковый из архивов.
- **Как правильно:** после копирования с сохранением атрибутов принудительно обновить mtime:
  `find . -path ./.git -prune -o -type f -exec touch {} +` перед `git add`. Альтернативы: `cp -R`
  без `-a`, либо `git update-index --really-refresh`.
- **Правило:** при программной подмене дерева в git-клоне пустой diff — подозрительный результат,
  а не подтверждение идентичности.
- **Источник:** `portrait-of-taste`.
- **Гейт:** да → `templates/deploy.sh` (`git rm -r --cached` + `git add -A -f` принудительно пересобирают индекс мимо stat-кэша, ~строка 1700-1705)


### PIT-062 — файл с суффиксом « 2» в имени — не обязательно дубль (2026-08-19)

- **Симптом:** файл вида `имя 2.pdf` принят за дубликат `имя.pdf` и отброшен; содержимое оказалось
  другим.
- **Корень:** macOS дописывает « 2» при конфликте имён, но тот же суффикс встречается и в осмысленных
  именах (том 2, часть 2). Эвристика по имени неразличима.
- **Как правильно:** дубликат определяется **по содержимому** (sha1), а не по шаблону имени.
- **Правило:** имя — гипотеза, хеш — факт. Это частный случай закона «сверять по содержимому,
  не по имени».
- **Источник:** `master-admission`.
- **Гейт:** да → `scripts/import_dedup.py` (дедуп по sha256 содержимого, не по шаблону имени)


### PIT-063 — инструмент скопирован «из канона» без сверки версии и затёр рабочий (2026-08-20)

- **Симптом:** после «обновления» деплойера из места, названного каноном, скрипт начал печатать
  версию на 13 минорных ниже. Заметил владелец — по номеру в первой строке вывода.
- **Корень:** двойное нарушение. (1) Путь, названный каноном в документации, не был проверен
  на актуальность: `base-repo/templates/deploy.sh` = v4.3.2, а живая версия — v4.16.1
  в `mission-control/scripts/`. (2) `cp` выполнен поверх существующего файла без сверки
  версий до и после; проверкой посчитали вывод самой команды `cp`, а не состояние результата.
- **Первопричина — само действие:** его не просили. Задача была «собрать архив», деплойер
  трогать не требовалось; он уже лежал на месте, работал и был свежее. Инициативное
  «заодно обновлю» решало несуществующую проблему и оказалось единственным источником поломки.
- **Как правильно:** не трогать работающий инструмент, если задача этого не требует. Кажется
  устаревшим — сказать вслух и показать версии, а не подменять молча. Если менять всё же надо —
  инструмент сверяется **по версии, а не по пути**. Перед копированием —
  напечатать версию источника и цели; при расхождении не трогать и сказать вслух. Правило
  «не перезаписывать» применяется к любому месту назначения, включая `~/Downloads`, — категория
  действия («положить инструмент», а не «перенести файл в репу») не сужает его область.
- **Правило:** у всего, что имеет поведение, есть `SCRIPT_VERSION` в шапке и в первой строке
  вывода (`44-what-to-version.md` §4.1). Здесь это единственное, что предотвратило тихую
  деградацию инструмента публикации всей системы.
- **Разбор:** `incidents/deployer_downgrade_incident.md`. Там же — открытая системная проблема:
  документация в четырёх местах указывает на устаревшую копию деплойера.

- **Гейт:** да → `scripts/deploy_version_gate.py` (единственный источник правды — `SCRIPT_VERSION` в `templates/deploy.sh`)


### PIT-064 — канал доставки молча пересжимает файлы, и потерю ищут не там (2026-08-20)

- **Симптом:** материал не читается. Причину списывают на условия съёмки, качество камеры, угол.
  Меняют то, что под рукой, — результат не двигается.
- **Корень:** режет **звено пересылки**, а не источник. Все одиннадцать присланных снимков имели
  ровно **1280 пикселей по длинной стороне** — и вертикальные, и горизонтальные. Это не
  характеристика камеры (телефон снимает ~4032×3024), а сигнатура пересжатия в мессенджере:
  теряется свыше девяти десятых информации по площади. Строка, занимавшая на полученном кадре
  8–10 px, в оригинале занимала 25–32.
- **Диагностический признак:** **округление к одному и тому же числу** (1280, 1024, 2048)
  у файлов из одного источника = пересжатие в канале. Нестандартные размеры (1445×1360,
  1158×536) = исходник не тронут. Признак работает без доступа к оригиналу.
- **Установить факт потери мало — надо назвать звено.** Пока звено не локализовано, исправление
  уходит не туда: в этом кейсе подозревали интерфейс загрузки, а замер по маршрутам показал, что
  загрузка не сжимает вовсе — файл, прошедший через неё, сохранил 1445 px. Менять надо было способ
  пересылки, а не настройки приёма.
- **Как правильно:** разрешение присланных файлов проверяется **первым делом** и указывается
  в отчёте. Округление к степени двойки → просить прислать **файлом**, а не картинкой. Приём
  ничего не стоит и даёт больше, чем любые ухищрения на стороне съёмки.
- **Правило:** прежде чем оптимизировать источник — проверь канал доставки. Материал теряется
  не там, где его ищут.
- **Источник:** `exam-kit` (L-014, L-015). Родня: PIT-018, PIT-023, PIT-024, PIT-030.
- **Гейт:** нет — эмпирика конкретного разбора пересжатия медиа мессенджером — не файл репы


### PIT-065 — незакавыченный heredoc разворачивает `$` и бэктики в теле шаблона (2026-08-20)

- **Симптом:** большой HTML/JS-дашборд, записанный через `cat << EOF`, **молча рушится**:
  часть переменных пропала, часть подставилась мусором. Ошибки нет, файл на месте, размер
  правдоподобный.
- **Корень:** метка heredoc без кавычек включает подстановку. Всё, что в теле выглядит как
  `$var` или `` `cmd` `` — а в JS и CSS этого полно, — обрабатывается **шеллом**, а не попадает
  в файл. Чем больше шаблон, тем незаметнее.
- **Как правильно:** тело шаблона кладётся **quoted heredoc** (`<< 'EOF'`) или файловым
  инструментом, данные подставляются отдельно — плейсхолдер `__DATA__` и `str.replace` в питоне.
  **Никаких переменных в теле шаблона.**
- **Проверять на диске, а не в голове:** после генерации — плейсхолдер исчез, данные вшиты,
  теги сбалансированы (открытий == закрытий). Пару раз на диске оказывался не тот файл,
  который ожидали.
- **Правило:** шаблон и данные не смешиваются в одном механизме подстановки. Генерация без
  пост-проверки результата не считается выполненной (PIT-016: тест инструмента — его выход).
- **Источник:** `dota-dossier`. Родня: PIT-016, PIT-031, PIT-043.
- **Гейт:** нет — незакавыченный heredoc легитимен там, где подстановка как раз нужна; отличить «шаблон, который не должен смешиваться с шеллом» от обычного heredoc по regex нельзя без потока ложных срабатываний


### PIT-066 — путь с пробелом без кавычек рвёт команду (2026-08-20)

- **Симптом:** `find`/`cp` спотыкаются на папке вроде `Dota 2/` — обрабатывается «Dota»,
  а «2/» уходит отдельным аргументом. Часть работы делается не над тем, что имелось в виду.
- **Корень:** word splitting по пробелу. То же семейство, что PIT-005 (комментарий в строке
  zsh уходит аргументом) и PIT-017 (в zsh нет splitting там, где он есть в bash): **шелл
  разбирает строку, а не путь**.
- **Как правильно:** кавычить всегда, даже когда «здесь пробелов не бывает» — они появляются
  при первом же переносе на другую машину. Свои имена заводить **без пробелов** (`dota-dossier`).
- **Правило:** любой путь в shell — в кавычках, без исключений.
- **Источник:** `dota-dossier`. Родня: PIT-005, PIT-017, PIT-049, PIT-050.
- **Гейт:** нет — «путь без кавычек» неотличим статическим regex-ом от легитимного кода без ложных срабатываний на масштабе всей репы; узкие однозначные случаи того же семейства уже покрыты (PIT-017, PIT-038, PIT-049)


### PIT-067 — данные у внешнего провайдера имеют срок жизни (2026-08-20)

- **Симптом:** из 2995 матчей полностью распарсить удалось **76** — реплеи остальных удалены
  с серверов провайдера (записи 2013–2020). Выгрузку планировали так, будто источник вечный.
- **Корень:** глубокие данные хранятся у провайдера ограниченное время, сводки — дольше или
  бессрочно. Разница обнаруживается только в момент, когда за старым уже поздно идти.
- **Как правильно:** выгружать **своевременно**, а не «когда дойдут руки». И отдельно —
  **не выбрасывать сводку**: в этом кейсе API отдавал перцентили (`benchmarks`) по **всем** 2995
  матчам, поэтому основной анализ считается на полной выборке, а не на 76. Прежде чем объявлять
  выборку урезанной, проверить, что ещё отдаёт источник по «неполным» записям.
- **Правило:** у внешнего источника два горизонта хранения — полные данные и сводка. Планировать
  по короткому, анализировать по длинному.
- **Источник:** `dota-dossier`.
- **Гейт:** нет — эмпирика внешнего API (Dota provider, сроки хранения реплеев) — вне base-repo


### PIT-068 — исходник отдан как результат: markdown не рендерит то, что в нём написано (2026-08-20)

- **Симптом:** в просмотрщике `.md` вместо формулы виден сырой код (`\begin{cases}`,
  `\dfrac{x^2}{a}`), а строка таблицы разъезжается, если внутри есть `|x|`.
- **Корень:** два разных механизма, одна причина. (1) Просмотрщик без KaTeX/MathJax не обязан
  рендерить LaTeX, а `\\` markdown-парсер съедает как escape. (2) Вертикальная черта — разделитель
  ячеек, и модуль в формуле рвёт таблицу изнутри.
- **Как правильно:** формат выдачи выбирается **по каналу, в котором результат будут смотреть**.
  `.md` отдаётся как исходник, результат — PDF. Модуль и норма — только `\lvert`, `\rvert`,
  `\lVert`, `\rVert`, `\mid`.
- **Смежное:** сборка PDF через pandoc + xelatex падает на `lmodern.sty`, которого нет
  в минимальной поставке texlive (условие `\ifPDFTeX` не спасает) — в каталог сборки кладётся
  заглушка. Кириллица в математическом режиме уходит в шрифт без глифов.
- **Правило:** исходник ≠ результат. Прежде чем отдавать, посмотреть глазами в том же
  просмотрщике, что и получатель.
- **Источник:** `exam-kit` (L-005, L-006, L-010, L-019).
- **Гейт:** нет — выбор формата вывода под канал чтения (LaTeX/markdown/PDF) — редакционное решение о конкретном документе


### PIT-069 — правдоподобная реконструкция маскирует сам факт реконструкции (2026-08-20)

- **Симптом, ступень 1.** Задание помечено 🟡 (читается частично), но в ответ выдана **конкретная
  буква**, достроенная по правдоподобию: типовой дистрактор из методички лёг в пробел естественно.
  Метка сохранилась, ответ оказался выдуманным.
- **Симптом, ступень 2 — тяжелее.** Шесть утверждений про страничную и сегментную организацию
  памяти. Хвосты строк на кадре были видны, но при переносе в текст пары **собрались по учебнику**:
  «страничная → внутренняя фрагментация». В билете стоял перевёртыш — «страничная → **внешняя**».
  Метка `[?]` не появилась вовсе, задание осталось 🟢, ошибка прошла обычную сверку и стоила 5 баллов.
- **Корень:** чем правдоподобнее домысел, тем труднее его заметить. На первой ступени домысел
  закрывает пробел **под меткой**; на второй — **стирает саму метку**. Совпадение прочтения
  с учебником — не подтверждение, а главный маскировщик: перевёртыш и строится из учебниковых кусков.
- **Аномалию заметили и проигнорировали.** В заметках стояло «первый мультивыбор этого составителя
  без перевёртыша» — прямой сигнал перечитать. Записан и списан как курьёз.
- **Как правильно:**
  1. Метка неуверенности запрещает выдавать конкретный ответ. Только **правило отбора** или явное
     перечисление возможных прочтений — и правило обязано перечислять признаки **шире**
     реконструированных вариантов, иначе наследует их ошибку.
  2. Симметричные пары (страничная/сегментная, внутренняя/внешняя, монолит/микроядро,
     статический/динамический, BEFORE/AFTER) читаются **по обоим концам каждой строки** отдельно.
  3. Строка, хоть частично восстановленная по смыслу, несёт `[?]` и опускает метку —
     **независимо от «очевидности»** реконструкции.
  4. Замеченная аномалия — не курьёз, а точка проверки.
- **Главное следствие:** **устранение потерь в канале ценнее сверки.** Сверка проверяет то, что
  я предъявил; исходник проверяет и то, чего я не заметил. Обе ошибки вскрылись не сверкой —
  первая повторной доставкой в полном разрешении, вторая сопоставлением с независимым источником.
- **Источник:** `exam-kit` (L-016, L-022). Родня: PIT-018, PIT-031, PIT-064, PIT-070.
- **Гейт:** нет — суждение о содержании (правдоподобная реконструкция при OCR/транскрипции маскирует сама себя) — чистая семантика


### PIT-070 — вопрос сверки был неопровержимым, а метка уверенности ловит сомнение, а не ошибку (2026-08-20)

- **Симптом:** пользователю названо «реально под риском 10 баллов» по двум заданиям — оба он
  подтвердил сразу. Фактические потери пришлись на **другие** два: одно стояло в 🟢 и в проверку
  не попало вовсе, второе было понижено до «проверь помягче, по желанию» с заверением «утверждения
  прочитаны все, плывёт только привязка букв». Пользователь сделал ровно то, о чём просили, —
  и это **по построению не могло поймать ошибку**.
- **Корень 1. Метка уверенности — самооценка.** Она ловит **сомнение**, но не ошибку, которая
  сомнением не ощущалась (реконструкция, собравшаяся по учебнику — PIT-069). Значит сумма «под
  риском N» — риск **известный**; слепую зону карта не показывает в принципе. Подавать это число
  как полный риск — вводить в заблуждение, даже когда каждый отдельный расчёт честен.
- **Корень 2. Вопрос сверки был неопровержимым.** «Подтверди нумерацию букв» молча предполагает,
  что **содержание строк прочитано верно** — ответ пользователя физически не мог фальсифицировать
  транскрипцию. Заверение «текст прочитан, сверять только буквы» само стояло на той самой
  реконструкции, которую должно было проверять.
- **Как правильно:**
  1. **Вопрос сверки обязан быть опровержимым.** Предъявляется моё прочтение — построчной цитатой,
     — и его можно подтвердить или разбить. Мета-вопрос без цитаты («правильно ли я понял порядок?»,
     «нумерация та же?») предполагает верным ровно то, что проверяется.
  2. Ярусов мягче «проверить обязательно» не существует. Самодельное смягчение до «по желанию» —
     способ не проверить.
  3. 🟢 читается «я не сомневаюсь», **а не «это верно»**. Сумма под риском всегда подписывается
     как **известный** риск.
- **Проверка на себе:** построчная цитата стоила бы пользователю двадцать секунд и поймала бы
  ошибку в первом раунде — до выдачи результата, а не после.
- **Правило шире экзаменов:** оно про любую сверку — код-ревью, приёмку данных, подтверждение
  плана. Спрашивать надо так, чтобы ответ **мог оказаться «нет»**.
- **Источник:** `exam-kit` (L-023). Родня: PIT-069, PIT-030, PIT-016.
- **Гейт:** нет — «вопрос сверки обязан быть опровержимым» — методология QA-диалога, семантика формулировки вопроса


### PIT-071 — методология менялась по ходу, и одни и те же данные пересобирались пять раз (2026-08-20)

- **Симптом:** двадцать объектов первого батча пересобирались в пяти отчётах подряд (v1, v2, v3, v4,
  v4-MIRROR, финал). Файлы дублировались в восьми архивах, у части объектов накопилось по 3–4 версии
  разбора в разных местах, версии путались.
- **Корень:** сборка началась раньше, чем устоялась методология. Каждое изменение метода означало
  пересборку всего — но делалось «микро-исправлениями» поверх старых оценок, отчего расхождения
  копились вместо того, чтобы сниматься.
- **Как правильно:**
  1. Порядок: **методология (как) → сборка (что) → анализ (о чём)**. Не наоборот и не параллельно.
  2. Методология изменилась — **перезагрузить все оценки целиком**, а не править точечно. Смешение
     двух методов в одном наборе хуже, чем любой из них.
  3. **Индекс со статусом каждого объекта — с самого начала**, а не когда стало непонятно.
     Один объект = один финальный документ; всё остальное явно помечено как история.
  4. Пивот фиксируется формулой **«до X было так; после X стало так; причина Y»** — иначе через
     месяц причину восстанавливают задним числом и ошибаются.
- **Источник:** `portrait-of-taste` (4.1–4.3). Родня: PIT-033, PIT-052, PIT-045.
- **Гейт:** нет — управление версионированием черновиков анализа (пересборка вместо правки поверх) — организационная практика конкретного проекта


### PIT-072 — состояние работ восстановлено по своему полю зрения, а не по артефактам прогона (2026-08-20)

- **Симптом:** вахта вернулась в многосессионную кампанию, переделала уже сделанную
  инвентаризацию **худшим инструментом** (36 % охвата вместо полного), написала с нуля сканер
  при наличии трёх отработавших, и подала результат как «всплыло то, чего я не видел» — про файлы,
  **поимённо перечисленные в плане того же прогона**. На прямой вопрос владельца ответила
  «исследования не было».
- **Корень:** вошла в прогон через журнал (§0) и «что в работе», не открыв `PLAN.md`. §0 указывал
  следующий подшаг — и это было принято за **описание состояния работ**. Указатель на шаг и опись
  сделанного — разные вещи; у длинной кампании они лежат в разных файлах.
- **Почему не поймала себя:** «сборщик видит 36 %» — наблюдение, «значит исследования не было» —
  вывод. Правдоподобное объяснение легло в пробел естественно и **стёрло сам вопрос «а не сделано
  ли это уже?»** (механизм PIT-069, метка уверенности молчит — PIT-070).
- **Как правильно:**
  1. Приёмка вахты в кампании = чтение **всех** артефактов прогона: `PLAN.md`, `README.md`,
     `SYSTEM-MAP.md`, `decisions.md`. Журнал — вход, а не полный контекст.
  2. Прежде чем строить инвентаризацию — **искать готовую**. У кампании с каталогом `runs/`
     она почти наверняка есть.
  3. «Этого нет / это не сделано» — утверждение **про артефакты**, а не про своё поле зрения.
     «Я не нашёл в X» — можно; «этого не было» — только после проверки X.
- **Побочный ущерб:** написанный с нуля сканер читал содержимое **всех** реп, включая закрытую
  зону (ADR-010 требует физического отсутствия доступа), и положил 990 строк с путями закрытых реп
  в файл **внутри публикуемой базы**. Отсюда ещё два правила: **инструмент, читающий чужие репы,
  несёт список закрытой зоны внутри себя** (зоны не выполняются вниманием — только кодом), и
  **машинная выгрузка не кладётся в дерево репы, пока не проверена на зоны**.
- **Разбор:** `incidents/inventory_ignored_and_redone_incident.md`.
- **Родня:** PIT-031, PIT-032, PIT-069, PIT-070, PIT-063.
- **Гейт:** нет — эпистемическая дисциплина приёмки вахты («открыть все артефакты кампании») — что значит «открыл все» это суждение о полноте, не файловый признак


### PIT-073 — каскад из наблюдения в «факт»: увидел кольцо → записал «замужем» в восемь мест (2026-08-20)

- **Симптом:** в профиле человека неопределённо долго стояло категорическое «замужем (обручальное
  кольцо на безымянном)». Проверка всех четырёх фото показала: кольцо есть на одном кадре, оно
  **на среднем пальце**, **декоративное с плоской вставкой**, и **семейный статус не доказывает**.
- **Анатомия каскада.** Каждый переход — предположение, поданное как наблюдение:

  ```
  на пальце виден объект, похожий на кольцо   ← единственный наблюдаемый факт
        ↓ это обручальное кольцо
        ↓ оно на безымянном
        ↓ значит замужем
        ↓ записать как факт в 8 мест профиля
        ↓ автогенерация затянула в 3 места внешних отчётов
  ```

- **Корень:** наблюдаемое и выведенное не различались в тексте. Ни одной пометки неопределённости —
  гипотеза оформлена как категорическое утверждение и дальше живёт как факт.
- **Размножение — часть дефекта.** Одна ошибка дала 8 упоминаний внутри документа и 3 во внешних,
  собранных автоматически. Автогенерация не создаёт ошибок, но множит их и делает источник
  неочевидным: правка в отчёте бесполезна, чинить надо первоисточник.
- **Вторичный источник — не источник.** При переписывании блока факты были взяты из старого текста
  того же документа и не сверены с фото. **Собственный текст — вторичный источник**: он мог
  содержать ту же ошибку, которая теперь копируется дальше.
- **🔴 Правило нарушено в час его написания.** Красное правило «никогда не переносить визуальные
  утверждения в новый текст без сверки» было записано — и нарушено тем же исполнителем в тот же
  час, в правке того же профиля. Отдельный класс: **правило существует в тексте, но не в поведении**.
  Написанное правило не защищает; защищает только встроенное в процедуру **в точке действия** —
  чек-лист перед каждой правкой, а не абзац в стандарте.
- **Как правильно — разделять слои явно.** Разбор источника ведётся **двумя парными файлами**:
  1. **Объективное описание** — только лингвистический слепок того, что видно. Никаких оценок,
     гипотез, интерпретаций. Самодостаточно для реконструкции без доступа к оригиналу.
  2. **Интерпретация** — с тремя обязательными разделами: **что наблюдается** (факт),
     **гипотезы** (с явным маркером неопределённости), **красные флаги** — что из этого источника
     нельзя переносить как факт без подтверждения автора (статус отношений, семейное положение,
     национальность, вероисповедание).

  Смысл разделения — не аккуратность, а **защита от накопления байса** и возможность независимой
  проверки: следующий исполнитель через два месяца видит, где кончается наблюдение.
- **Правило:** утверждение о человеке, которое он сам не подтверждал, не записывается
  категорически — независимо от того, насколько очевидным выглядит визуальный признак.
- **Источник:** `portrait-of-taste` (У-01 – У-04, ситуация 2026-08-15). Родня: PIT-069, PIT-070,
  PIT-031.

- **Гейт:** нет — суждение о визуальном контенте (кольцо на пальце → категоричное «замужем») — чистая семантика


### PIT-074 — фильтр по списку из головы: четыре инструмента подряд (2026-08-20)

- **Симптом:** инструмент честно перечисляет файлы, но **параметр, решающий что чем считать**
  (список каталогов «содержания», имён «служебок», префиксов) вписан по памяти. Ошибка
  не даёт ни исключения, ни предупреждения — только тихо неверный результат.
- **Четыре повторения за одну сессию:** готовый сборщик видел 36 % · оценка «~119 кандидатов»
  занижена в 9 раз на первой же проверенной репе · свой скрипт с путями `docs/heroes/`,
  которых не существует (реально `knowledge/heroes/`) — 545 файлов справочника в «читать» ·
  свой сканер с белым списком каталогов — 3437 файлов за бортом.
- **Сквозная потеря во всех четырёх:** сканировались только `.md`. Скрипты, хуки и конфиги —
  **2129 файлов** — не смотрелись ни разу. Скрипт с шапкой-инструкцией это тоже методичка.
- **🔴 Что едва не потерялось.** Первый честный листинг показал `personal-finance-dss/.claude/`:
  **6 скиллов с `evals.json`, 4 субагента, 11 хуков, `settings.json`** — готовая инфраструктура
  Claude Code. Плюс `it-base/06-claude-workspace/` (44 файла) и `knowledge/roles/` (16 ролевых
  наборов). **Скрытый каталог не вспоминается**, потому что его не видно, — а в нём лежит
  самое ценное для синтеза.
- **Корень:** формально инструмент писался под лозунгом «не верить описаниям, смотреть диск» —
  и его собственные входные данные оказались описанием по памяти. Один `find … | sort | uniq -c`
  на репу (тридцать секунд) показал бы настоящие имена. Не делался ни разу.
- **Почему не сработала самопроверка:** правдоподобное имя каталога ложится в пробел
  естественно и **стирает сам вопрос** «а есть ли такой путь?» (PIT-069); сомнения нет —
  значит нет и проверки (PIT-070).
- **Как правильно:**
  1. **Параметр, от которого зависит результат, считывается с диска и печатается в отчёте.**
     Читающий обязан видеть, на чём стоит вывод.
  2. **Скрытые каталоги перечисляются отдельной командой** — `.claude/`, `.githooks/`,
     `.github/` несут конфигурацию инструментов.
  3. **Молчаливая классификация запрещена:** отбрасывая, код печатает что и по какому
     признаку отброшено — числом и списком.
  4. **Повтор одной ошибки — сигнал менять метод, а не чинить инструмент.** После второго
     раза правильным ходом было отказаться от автоматизации решения, а не писать третий скрипт.
- **Разбор:** `incidents/filter_lists_from_memory_pattern.md`.
- **Родня:** PIT-031, PIT-032, PIT-069, PIT-070, PIT-072, PIT-073.
- **Гейт:** нет — полнота параметра фильтра (какие каталоги/имена перечислены) зависит от смысла конкретной задачи каждый раз — не единый файловый инвариант


### PIT-075 — красный гейт стал фоном: 41 день, 65 релизов без единой зелёной проверки (2026-08-20)

- **Симптом:** блокирующий гейт CI красный на **каждом** пуше и **каждом** теге релиза
  непрерывно 41 день. Выпущено **65 версий** без единого зелёного прогона. Обнаружено только
  когда владелец спросил напрямую.
- **Корень процесса:** **в системе не было ни одной точки, которая после пуша сверяется
  с реальным статусом CI.** Локальная проверка и CI считались эквивалентными — без механизма,
  который эту эквивалентность подтверждал бы или опровергал. Церемония сдачи требовала
  локального прогона и нигде не требовала посмотреть, чем кончился прогон на сервере.
- **🔴 Alert fatigue для CI — главное в этом разборе.** Неполный фикс не был замечен именно
  потому, что **фон уже был красным**. Будь CI зелёным до него, новая красная строка бросилась
  бы в глаза немедленно: «было зелено, стало красно — что сломал коммит?». На фоне «и так красное
  29 дней» новая красная строка **неотличима от старой**. Сигнал тревоги, который орёт всегда,
  не слышит никто.
- **Ложное благополучие от расписания.** В списке прогонов были записи `success` — но это
  срабатывал еженедельный `schedule`-триггер, запускавший **другую** джобу, к проверке кода
  отношения не имеющую. При беглом взгляде выглядит как «иногда всё же зелёное».
  **Смотреть надо не на цвет, а на то, какая именно джоба отработала.**
- **Публикация не завязана на статус проверки.** Деплой пушил версии по готовности локального
  архива, независимо от того, зелёный ли предыдущий прогон. Автоматизация доставки без
  автоматизации сверки — это конвейер, вывозящий брак быстрее, чем его замечают.
- **Как правильно:**
  1. **После пуша — сверка с реальным статусом**, а не предположение. Это отдельный шаг
     церемонии, а не «если вспомню».
  2. **Красный гейт чинится или гасится, но не остаётся фоном.** Если чинить сейчас нельзя —
     гейт временно снимается с явной записью, чтобы новая поломка была видна.
  3. **Цвет прогона читается вместе с именем джобы.**
- **Правило:** длительно красный гейт **хуже отсутствующего**: отсутствующий честно ничего
  не обещает, а красный создаёт видимость проверки и глушит сигнал о новых поломках.
- **Источник:** `personal-finance-dss` (`ci_blocking_gate_red_41_days.md`). Родня: PIT-039, PIT-047.
- **Гейт:** нет — CI-статус конкретной внешней репы (`personal-finance-dss`) на стороннем провайдере CI — base-repo не имеет доступа к чужому CI; принцип «красный гейт хуже отсутствующего» универсален, но проверка требует API конкретного провайдера для каждой репы отдельно


### PIT-076 — регистронезависимая ФС скрывает целый класс багов от единственной платформы разработки (2026-08-20)

- **Симптом:** проверка ссылок падает на Linux CI и **не воспроизводится** на macOS.
  Ссылка `docs/GLOSSARY.md` при реальном файле `docs/glossary.md`.
- **Корень:** macOS APFS **регистронезависима** — `Path.exists()` молча резолвит `GLOSSARY.md`
  в лежащий рядом `glossary.md` и рассинхрона не видит. На Linux (регистрочувствительная ФС)
  та же ссылка битая. Одна и та же проверка на двух платформах даёт **разный результат**,
  и результаты никто не сверял между собой.
- **Усугубляющий фактор — монокультура платформы.** Вся разработка велась на macOS, с четырёх
  аккаунтов. Единственная платформа работы **структурно не видит** именно этот класс багов.
  Работай хоть одна сессия в Linux или Docker — проявилось бы сразу.
- **Рецидив в обратную сторону.** Первый фикс привёл ссылки к строчному имени, но пропустил
  одно упоминание заглавными в тексте разбора того же инцидента. Локально выглядело рабочим
  (регистр не важен), на Linux — снова битая ссылка, теперь в обратном направлении. **Фикс,
  проверенный только на платформе, где баг невидим, не является фиксом.**
- **Как правильно:** проверять существование файла **со сверкой регистра** (перечислить каталог
  и сравнить имя точно), а не через `exists()`. Любую платформозависимую проверку прогонять
  на целевой платформе, а не на платформе разработки.
- **Семейство:** тот же класс, что PIT-049 (BSD `head -n -1` против GNU), PIT-050 (пустой `$PATH`),
  PIT-017 (zsh не делает word splitting), PIT-064 (`xargs -d` — GNU-расширение).
  **Общее правило семейства: «работает у меня» — утверждение о платформе, а не о коде.**
- **Источник:** `personal-finance-dss` (`ci_blocking_gate_red_41_days.md`).
- **Гейт:** да → `scripts/revision_check.py::check_links` (добавлена сверка точного регистра конечного компонента пути через listdir — ловит именно тот класс, где macOS APFS резолвит `GLOSSARY.md` в `glossary.md` молча)


### PIT-077 — каждый шаг протестирован, а путь целиком не проходил никто (2026-08-20)

- **Симптом:** бэкендовый гейт корректно требовал отдельное согласие и отдавал 403 с машиночитаемым
  телом — «чтобы фронт показал экран согласия». **Экрана не существовало вообще** 26 дней
  и 15+ версий. Каждый новый пользователь при первом же действии упирался в заглушку
  «проверьте соединение» — тупик без единого пути через интерфейс. Обходной путь существовал
  только через прямой вызов API.
- **Корень:** замысел был зафиксирован **прозой в журнале** («фронт покажет экран») и никогда
  не стал ни пунктом плана, ни чекбоксом, ни тестом. Каждый шаг по отдельности работал
  и был покрыт; **путь пользователя с нуля не проходил никто** — ни человек, ни тест.
- **Почему не поймали тесты:** тест проверяет «код делает X, когда дано Y» — но только для тех
  Y, которые кто-то придумал заранее. Состояние «новый пользователь, ничего ещё не дано»
  в качестве Y никто не задал.
- **Диагностический приём — читать глаголы в журнале.** «Фронт **покажет**» ≠ «фронт
  **показывает**». Будущее время в записи о сделанном — маркер незакрытого замысла. Пройтись
  по последним записям журнала и проверить, что из обещанного действительно существует в дереве.
- **Как правильно:**
  1. **Путь целиком, а не шаг.** Периодически проходить сценарий с чистого состояния — реально
     ли шаг N достижим после N−1, а не «каждый шаг протестирован отдельно».
  2. **Замысел, зафиксированный прозой, обязан стать пунктом плана.** Иначе он живёт только
     в истории и не имеет владельца.
  3. **Периметр, а не пример:** правило применено к нескольким объектам класса — проверить,
     нет ли объектов **того же класса**, к которым не применено. По коду, а не по списку в прозе.
- **Правило:** зелёные тесты доказывают, что не сломано **проверяемое**. Они ничего не говорят
  о том, что не было проверено ни разу.
- **Источник:** `personal-finance-dss` (`consent_gate_no_ui_dead_end_incident.md`).
  Метод поиска этого класса — `00-infrastructure/69-agents-hooks-and-gates.md` §3.

- **Гейт:** нет — «путь пользователя с нуля, не шаг» — сквозной пользовательский сценарий конкретного продукта (`personal-finance-dss`), не файл репы


### PIT-078 — гейт по таймауту меряет время по часам, а часы зависят от загрузки машины (2026-08-20)

- **Симптом:** Stop-гейт с быстрым срезом тестов оборвался «фаза не уложилась в 65 с» посреди
  сдачи батча. Первая гипотеза — зависший тест ушёл в вырожденный поиск.
- **Корень:** **перегрузка машины сторонними процессами**, не тест. `uptime` в момент обрыва
  показал load average **62.65 / 87.03 / 62.28** — на порядок выше числа ядер. После закрытия
  лишних приложений минутная нагрузка упала до **4.57**, а фоновый прогон, до того державший
  13–45 % CPU, поднялся до 89–99 %.
- **Механизм, который стоит помнить:** таймаут гейта — это **время по часам**, а не работа.
  Тот же прогон на свободной машине укладывается, на загруженной — нет. Гейт, настроенный
  под идеальные условия, начинает срабатывать ложно ровно тогда, когда владелец параллельно
  работает.
- **Диагностика за две секунды:** `uptime` — load average против числа ядер; и загрузка CPU
  самим прогоном. **Прогон, висящий на 13 % CPU, не считает — он ждёт очереди.**
- **Как правильно:**
  1. Таймаут гейта ставить **с запасом** на реальную, а не идеальную машину.
  2. При обрыве по таймауту **сначала смотреть нагрузку**, потом искать зависший код.
  3. В сообщении гейта печатать load average — тогда причина видна сразу, без расследования.
- **Правило семейства:** среда исключается **раньше** кода (PIT-034, PIT-029). Обрыв
  по времени — почти всегда про среду, а не про алгоритм.
- **Источник:** `personal-finance-dss` (`machine_load_gate_timeout_investigation.md`).
  Родня: PIT-034, PIT-029, PIT-036, `00-infrastructure/52-hardware-baseline.md`,
  `69-agents-hooks-and-gates.md` §2 (таймаут хука и fail-open).

- **Гейт:** нет — нагрузка машины (load average) в моменте — состояние железа, не файла репы; рекомендация «печатать load average в сообщении гейта» применена к чужому гейту вне base-repo


### PIT-079 — проверка, которая ни разу не срабатывала, неотличима от сломанной (2026-08-20)

- **Симптом:** гейт зелёный. Означает ли это «нарушений нет» или «проверка сломана и молчит» —
  по зелёному цвету не различить.
- **Живой пример из этой же системы:** канарейка на токен-слип модели добавлена в гейт ревизии
  и **сразу нашла два попадания в самой базе** — иероглиф вместо буквы «а» внутри слова
  «Авторизация» в методичке (тот же дефект, что уже находили и чинили в другой репе, а в базе
  он остался) и второй — **в докстринге инструмента, написанном в тот же день той же вахтой**.
  До появления проверки дерево считалось чистым просто потому, что никто не смотрел.
- **Как правильно — самопроверка рядом с проверкой:**
  1. **Синтетический образец.** Проверка обязана уметь доказать, что ловит: собрать заведомо
     дефектный случай в памяти и убедиться, что он опознан. Не прошло — это `FAIL` гейта,
     а не «всё чисто».
  2. **Отрицательный тест на подсадку.** Положить дефект в песочницу и убедиться, что гейт
     краснеет **и называет точный адрес**. Зелёный на подсаженном дефекте — сломанный гейт.
  3. **Оба исхода печатаются явно:** «проверено N, нарушений нет» вместо молчания.
     Молчание неотличимо от пропуска.
- **Анти-самореференция.** Проверка, запрещающая символ или шаблон, **не должна содержать
  его сырым** — иначе ловит собственные упоминания и собственную документацию. Диапазоны
  задаются числами, упоминания в текстах — нотацией, образец собирается через `chr()`.
  При внедрении канарейка немедленно поймала сырые символы в своих же комментариях
  и в цитатах разбора — правило родилось прямо на этом.
- **Правило:** у каждого гейта есть **два** результата, которые надо уметь различать:
  «нарушений нет» и «я не работаю». Без самопроверки они выглядят одинаково.
- **Источник:** `personal-finance-dss` (`cjk_token_slip_investigation.md`), внедрено в базу
  `scripts/revision_check.py`. Родня: PIT-075 (красный гейт как фон), PIT-016 (тест инструмента —
  его собственный выход), PIT-047.

- **Гейт:** да → `scripts/revision_check.py` (принцип реализован системно: `selftest_cjk`, `selftest_registry_dupes`, `selftest_section_dupes`, `selftest_card_heading_levels`, `selftest_prose_counts`, `selftest_campaign_log`, `selftest_dangling_refs`, `selftest_exec_bits` — каждая проверка несёт канарейку)


### PIT-080 — «обнови отчёт» понято как «создай новый отчёт» (2026-08-20)

- **Симптом:** владелец сказал «обнови финальные отчёты». Создан **новый** файл
  `FINAL_REPORT_v3_5_1.md` рядом с существующими каноническими. Итог: два документа об одном,
  расходящихся с первого дня; читатель не знает, какой из них действующий.
- **Корень:** создать новый файл проще, чем разобраться в существующем и аккуратно его
  дополнить. Соблазн усиливается тем, что новый файл выглядит как **больше работы** —
  он видимый, а правка внутри старого незаметна.
- **Отличие от PIT-032:** тот про **инструменты** (написал скрипт, не поискав готовый),
  этот — про **документы**. Корень общий, но проверка разная: там «спроси, как обычно это
  делается», здесь — `ls` каталога перед созданием.
- **Как правильно — три шага перед созданием любого файла:**
  1. **`ls` целевого каталога** с поиском по смыслу, а не по точному имени;
  2. похожий файл есть → **обновлять его**;
  3. непонятно, какой из нескольких обновлять → **спросить владельца**, а не создавать третий.
  Новый файл заводится, только когда ничего похожего нет.
- **Список канонических файлов помогает.** В репе, где документы генерируются в нескольких
  форматах, полезно держать явный перечень «вот эти файлы — канонические, всё похожее
  на них по смыслу обновляется, а не дублируется».
- **Правило:** дубликат документа хуже устаревшего документа. Устаревший честно один,
  и его видно; дубликаты выглядят одинаково свежими и расходятся молча.
- **Источник:** `portrait-of-taste` (`standards/EXISTING_FILE_PRIORITY_PROTOCOL.md`).
  Родня: PIT-032, PIT-033, PIT-052, PIT-071.

- **Гейт:** TODO — частично гейтируемо (список канонических файлов + запрет создавать похожий по смыслу), но «похожий по смыслу» это семантическое сравнение имён/содержимого, сводимое к regex с трудом — не оценено достаточно глубоко за отведённое время


### PIT-081 — скрипт на содержимом убивает качество; на метаданных — не убивает (2026-08-20)

- **Симптом:** разбор, собранный скриптом вместо ручной работы, выглядит готовым и содержит
  то же число разделов — но лишён того, ради чего делался. В `portrait-of-taste` скрипт-«синтезатор»
  за один прогон **разрушил 52 документа**; повторный «правильный» скрипт дал недосинтез.
- **Граница, до которой скрипт полезен:** он честно работает с **метаданными** — идентификаторы,
  статусы, ссылки, счётчики, перемещение файлов. Как только он берётся за **содержимое**, где
  требуется суждение, результат внешне неотличим от работы, а по сути пуст.
- **Тот же корень — в трёх независимых местах системы:**
  1. `collect_to_base.py` не умеет свести четыре похожих протокола в одну методичку и не научится:
     он видит N разных файлов и не отличает «два документа про одно» от «два про разное» (ADR-010);
  2. попытка автоматически разделить «метод / содержание» по списку каталогов провалилась
     четыре раза подряд (PIT-074);
  3. этот случай.
- **Правило:** **автоматизируется механика, не суждение.** Где решает совпадение — скрипт;
  где решает смысл — человек или модель, но не regex. Инструмент, которому для работы нужен
  список «что чем считать», написанный из головы, — не пишется.
- **Диагностический признак:** если для запуска скрипта надо сначала перечислить исключения
  и особые случаи — это признак, что задача не механическая, и скрипт будет ошибаться молча.
- **Источник:** `portrait-of-taste` (`standards/HOW_WE_WORK.md` правило 1, история нарушений
  v3.3.8/v3.3.9). Родня: PIT-074, PIT-032, PIT-071.

- **Гейт:** нет — граница «автоматизируется механика, не суждение» — методологический принцип, а не файловый инвариант


### PIT-082 — удалено как дубль то, что дублем не было (2026-08-20)

- **Симптом:** рядом лежат `отчёт.html` и `отчёт_dark.html`. Выглядит как копия — удалено
  «для чистоты». Оказалось: две темы оформления, нужны обе, и генератор рассчитывает на пару.
- **Корень:** похожесть имён принята за похожесть назначения. Файл, которого не понимаешь,
  проще удалить, чем разобраться, — и удаление выглядит как наведение порядка.
- **Как правильно:** **не удалять то, чего не понимаешь.** Увидел два похожих файла —
  спросить владельца либо открыть оба и сравнить содержимое. Удаление — не уборка,
  а необратимая операция.
- **Смежное правило из той же практики:** у документа, который **генерируется**, есть скрипт-
  генератор. Пересобирать такой документ руками или писать параллельный генератор — значит
  завести вторую правду. Обновляется генератор.
- **Правило:** прежде чем удалить — назови, **чем именно** этот файл дублирует тот.
  Не можешь назвать — не дубль.
- **Источник:** `portrait-of-taste` (`standards/HOW_WE_WORK.md` правила 9–10).
  Родня: PIT-062 (файл с суффиксом « 2» — не обязательно дубль), PIT-080, PIT-004.

- **Гейт:** нет — решение об удалении по сходству имён файлов — суждение о содержании, семантика


### PIT-083 — основание суждения не сохранено, и суждение стало неперепроверяемым (2026-08-20)

- **Симптом:** оценки объектов сделаны по материалу, который присылался в переписке. В архив
  легло не всё — «один-два последних файла». При следующей ревизии **старые оценки подтвердить
  нечем**: материала, на котором они построены, больше нет.
- **Корень:** результат считался ценным артефактом, а его **основание** — расходным. Между тем
  ревизия проверяет именно связку «вывод ↔ основание», и без второй половины она невозможна:
  остаётся либо верить прежней вахте, либо переделывать с нуля по новому материалу.
- **Как правильно:** **материал, на котором построен вывод, архивируется целиком и сразу**,
  в полученном качестве, до обработки. Рядом — манифест: что это, откуда, когда получено.
  Это не аккуратность, а условие воспроизводимости: любое утверждение должно перепроверяться
  по исходнику независимо и через месяцы.
- **Признак нарушения:** «оценка v2 расходится с v1, но почему — уже не установить».
- **Правило:** вывод без сохранённого основания — это мнение, а не результат.
- **Источник:** `portrait-of-taste` (`situations/2026-07-24-photo-archive-integrity.md`).
  Родня: PIT-069, PIT-073, `00-infrastructure/65-visual-source-pipeline.md` §1,
  `71-fail-loud-and-sourcing.md` §5.

- **Гейт:** нет — архивная политика материала интервью/фото конкретных приватных реп — не base-repo


### PIT-084 — работал по своей памяти о стандарте, а не по самому стандарту (2026-08-20)

- **Симптом:** разбор заполнен по **собственному набору заголовков** — 47 пунктов вместо
  предписанных стандартом 70. Три целых блока пропущены. Стандарт при этом лежал рядом
  и был известен; владелец **отдельно и заранее** просил заполнять все пункты, с явными
  заглушками там, где данных нет.
- **Корень:** «я знаю этот стандарт» — самое дорогое заблуждение. Память о документе всегда
  короче документа, и сокращается она **незаметно**: пропущенное не выглядит пропущенным,
  потому что структура на месте и результат смотрится полным.
- **Тот же механизм, что PIT-074** (список путей из головы), только применительно к нормативу:
  берётся не сам источник, а его образ в голове.
- **Как правильно:**
  1. **Открыть стандарт и идти по нему построчно**, а не по памяти о нём — особенно когда
     кажется, что помнишь.
  2. **Заглушка обязательна.** Пункт без данных пишется явным «нет данных», а не опускается:
     опущенный пункт неотличим от несуществующего, а заглушка сама показывает, что осталось.
  3. **Считать пункты.** Стандарт говорит 70 — в результате должно быть 70 строк, включая
     заглушки. Арифметика ловит то, чего не ловит вычитка.
- **Правило:** норматив исполняется **с открытым нормативом**. Работа по памяти о правиле —
  это работа по другому, более короткому правилу.
- **Источник:** `portrait-of-taste` (`situations/2026-07-24-photo-archive-integrity.md`).
  Родня: PIT-074, PIT-072, PIT-031.

- **Гейт:** нет — исполнение норматива по памяти vs по документу; что считать «нормативом» и «пунктом» своё для каждого стандарта — не единый файловый инвариант всей системы


### PIT-085 — кириллица в пути ломает предпросмотр, хотя файл на месте (2026-08-20)

- **Симптом:** изображения не отображаются в предпросмотре markdown, видны заглушки битой
  картинки. Файл физически существует и открывается.
- **Корень:** путь содержит кириллицу без процентного кодирования (`../photos/AG_Ирина/AG_01.jpg`).
  Часть просмотрщиков не резолвит такие ссылки, хотя сам файл на месте. Проявляется **только
  в предпросмотре**, поэтому при проверке через файловый менеджер или терминал выглядит рабочим.
- **Как правильно:** в путях, на которые ссылаются документы, держать **латиницу**;
  кириллица — только внутри содержимого. Это уже действующее правило нейминга
  (`01-repo-standard.md`), и вот его практическая цена при нарушении.
- **Семейство:** та же природа, что PIT-009 (Info-ZIP пишет кириллицу как `#Uxxxx`) и PIT-025
  (UTF-8-флаг в zip): **кириллица в имени переживает не каждый инструмент на пути к читателю.**
  Содержимое переживает почти всегда, имя — нет.
- **Источник:** `portrait-of-taste` (`situations/2026-07-24-photo-archive-integrity.md`).
- **Гейт:** да → `scripts/revision_check.py::check_mixed_script_names` (частичное покрытие: ловит смешение кириллицы/латиницы в ОДНОМ имени, не всякую кириллицу в пути — докстринг явно ссылается на PIT-085)


### PIT-086 — доверился саммари вместо сверки с файлом (2026-08-20)

- **Симптом:** журнал говорит «сделано до пункта 49». Вахта поверила метке и продолжила
  с пятидесятого — **батч 5 оказался продублирован**. Обнаружено только эмпирической сверкой
  с самими файлами.
- **Корень:** саммари, журнал и любая пометка о состоянии — **вторичны**. Они отражают то,
  что кто-то записал в момент записи, и расходятся с диском при любом обрыве: работа сделана,
  запись не успела; или наоборот — запись сделана авансом.
- **Усиливается компакцией.** После сжатия окна прямой доступ к ранним деталям теряется,
  и опора смещается на саммари — ровно тогда, когда доверять ему опаснее всего.
- **Как правильно:** **верь файлам, не саммари.** Точка возобновления **сверяется с диском**
  перед продолжением: что реально лежит, а не что записано. Сверка стоит одну команду.
- **Это уже случалось дважды, в разных репах:** здесь — дубль батча; и в кампании синтеза,
  где `WATCHLOG` §0 был принят за опись сделанного, из-за чего заново переделали готовую
  инвентаризацию (PIT-072). Один и тот же механизм, независимые источники.
- **Правило:** запись о состоянии — **гипотеза о состоянии**. Проверяется тем же способом,
  что любая другая: обращением к первоисточнику.
- **Источник:** `character-a-analysis` (`00-protocol/05-context-mechanics.md` A3).
  Родня: PIT-072, PIT-073, PIT-031, PIT-037.

- **Гейт:** нет — общая эпистемическая практика («верь файлам, не саммари»); механически гейтится только там, где есть явный числовой инвариант (WATCHLOG↔VERSION — уже PIT-094/PIT-116); для произвольного «саммари сказал X, а на диске Y» универсального гейта нет


### PIT-087 — заявление о работе шире, чем работа (2026-08-20)

Английское имя этого класса — **overclaim**. Отдельный вид брака, потому что искажает
не данные, а **представление о том, что проверено**. Данные при этом могут быть верными —
неверно утверждение «я это проверил».

- **Чем отличается от обычной ошибки.** Ошибка в данных обнаруживается сверкой с источником.
  Завышенное заявление сверкой **не обнаруживается**: сверяют то, что предъявлено, а предъявлено
  как раз то, что сделано. Не сверяется то, чего не делали, — потому что об этом не сказано.
- **Четыре типовые формы:**

  | Форма | Как звучит | Что на самом деле |
  |---|---|---|
  | **по покрытию** | «просмотрел всё» | просмотрено то, что попало в поле зрения выбранного способа |
  | **по источнику** | «прочитано» | взято из прежних заметок или саммари, а не из документа |
  | **по проверке** | «проверил» | проверено частично либо тем способом, который этот класс ошибок не ловит |
  | **по умолчанию** | пробел не назван | обнаружен самим исполнителем, но не сообщён; вскрылся после прямого вопроса |

- **Последняя форма — самая дорогая.** Молчание о найденном пробеле выглядит не как ошибка,
  а как чистая работа: снаружи неотличимо. Именно поэтому «не сообщил сам» отслеживается
  как отдельное нарушение, а не как смягчающее обстоятельство.
- **Живые случаи в системе:**
  - «прочитано по старым заметкам» при том, что в прежних наработках было **не менее девяти
    подтверждённых ошибок** — унаследованы молча (`character-a-analysis`);
  - «просмотрел все файлы» при охвате инструмента **36 %** и, позже, **12 %** от реального
    числа — заявление опиралось на вывод инструмента, а не на дерево (PIT-072, PIT-074);
  - вахта отчиталась о состоянии кампании по своему полю зрения, не открыв её план (PIT-072).
- **Как правильно:**
  1. **Заявление о покрытии — всегда с числом и способом:** «просмотрено N из M, способ такой-то»,
     а не «просмотрел всё». Число заставляет посчитать, способ показывает границы.
  2. **Источник фрагмента называется**, если он не первичный: взято из прежнего разбора — так
     и пишется, рядом с фрагментом.
  3. **Найденный пробел сообщается самим исполнителем, в первой же реплике** — не дожидаясь
     вопроса (PIT-030).
  4. **«Проверил» уточняется до «проверил чем»:** проверка, не ловящая этот класс ошибок,
     проверкой не является (PIT-016, PIT-070).
- **Правило:** отчёт о работе описывает **сделанное**, а не намеренное и не подразумеваемое.
  Всё, что шире, — заявление, за которым никто не стоит.
- **Источник:** `character-a-analysis` (`00-protocol/01-rules.md` §4, `03-reports/`),
  подтверждено практикой этой же кампании. Родня: PIT-030, PIT-031, PIT-069, PIT-072,
  PIT-074, PIT-086.

- **Гейт:** нет — «overclaim» (заявление о работе шире, чем работа) — суждение о честности отчёта, чистая семантика


### PIT-088 — публикация репы молча стирает доставленное ей мостом (2026-08-20)

- **Симптом:** задача уехала из планировщика в репу, а в репе её нет. Никакой ошибки,
  никакого следа: файла просто не существует, и вахта не отличает «не доставляли»
  от «доставили и стёрли».
- **Корень — два канала, которые не знают друг о друге.** Мост пишет
  `INBOX-FROM-MISSION-CONTROL.md` **на GitHub** через `gh api`. Деплойер публикует
  **из локального дерева**: `deploy.sh:1470` сносит рабочее дерево клона целиком
  (кроме `.git`) и накрывает содержимым архива. Архив собран локально, файла доставки
  в нём нет — значит коммит публикации его удаляет.
- **Опасен не сам факт, а порядок.** Файл и так недолговечен по контракту
  (`74-planner-bridge.md` §3), и последовательность «доставка → вахта разобрала →
  деплой» безупречна. Теряет только «доставка → **деплой** → вахта».
- **Это класс, а не случай:** молча пропущенный шаг, а не грязь в выводе — тот же
  механизм, что PIT-014. Инструмент отработал успешно, результат исчез.
- **Как правильно:**
  1. **Вахте:** увидел файл доставки — разбери **до** любой публикации. Первый пункт
     приёмки сессии, не «когда дойдут руки».
  2. **Деплойеру** (открыто, `ROADMAP.md` §P2): отказываться публиковать репу, у которой
     на удалённой стороне лежит `INBOX-FROM-*`, отсутствующий в локальном дереве.
     Один запрос к API, отказ с объяснением — **fail loud** (`71-fail-loud-and-sourcing.md`).
- **Правило:** если два инструмента пишут в одну репу разными каналами, спроси, что
  делает второй с тем, что оставил первый. Ответ «ничего» надо **доказать**, а не
  предположить.
- **Источник:** первая доставка моста в `base-repo` (sha `1b082813`), проверено чтением
  `templates/deploy.sh:1470`. Родня: PIT-004, PIT-014, PIT-063, PIT-052.

> ### ✅ Подтверждено на живом событии 21.08.2026
>
> Карточка была написана **по чтению кода** — то есть как рассуждение, а не наблюдение.
> Событие произошло: файл доставки, разобранный в `ROADMAP.md`, **исчез из репы после
> публикации 20 версий**. Проверено запросом к GitHub — файла нет.
>
> **Порядок спас содержимое:** доставка → разбор → деплой. Задачи переехали в роадмап
> **до** публикации, поэтому потери нет. Обратный порядок («доставка → деплой → вахта»)
> потерял бы их молча — и именно этот порядок ничем не запрещён до сих пор.
>
> Ценность записи в том, что предсказание из кода **сбылось буквально**: это подтверждает
> не только карточку, но и метод — читать механизм, а не ждать инцидента.

- **Гейт:** TODO — гейт спроектирован (`ROADMAP.md` §P2: деплой отказывается публиковать репу с необработанным `INBOX-FROM-*`), но не реализован в `templates/deploy.sh` (нет упоминаний `INBOX-FROM` в коде) — решение отложено осознанно, не оценивалось заново в этом заходе



### PIT-089 — guard проверяет симптом известного случая, а не свойство (2026-08-21)

- **Симптом:** guard стоит, отчёт зелёный, дефект того же класса проходит насквозь.
- **Живой случай.** Мост доставляет репе срез доски. Guard 1 был написан против
  найденного случая — «заголовок называет несколько реп» — и честно его ловил.
  Когда секций стало 57, всплыла пара `style` ⊂ `visual-style`: поиск секции шёл
  **подстрокой**, и репа `style` получила бы срез `visual-style`. Guard молчал
  **законно**: заголовок `## visual-style` называет ровно одну репу.
- **Корень:** guard проверял **форму заголовка**, а защищаемое свойство — «репа получает
  свой и только свой срез». Дефект жил уровнем ниже, в сопоставлении имени с заголовком,
  и в поле зрения guard'а не попадал вовсе.
- **Почему не проявилось раньше:** при 19 секциях коллизий имён не было. Guard прошёл
  проверку на данных, где проверяемое свойство нарушить было нечем.
- **Как правильно:**
  1. **Сформулировать свойство**, а не случай: не «заголовок не должен называть две репы»,
     а «каждая репа получает уникальный срез».
  2. **Проверять свойство целиком по всему множеству.** Здесь это один прогон по всем
     57 репам и сравнение **хешей**: 57 срезов — 57 различных хешей. Такая проверка
     ловит и известный случай, и подстрочную коллизию, и любую будущую.
  3. **Расширил вход — перепроверь guard.** Он был принят на 19 секциях; на 57 условия
     изменились, и это отдельное событие, а не «то же самое, только больше».
- **Правило:** guard, выведенный из одного случая, покрывает один случай. Пока не назван
  инвариант и не проверено всё множество — считай, что защищён только тот случай.
- **Источник:** `push-inbox.sh` 1.1.0 → 1.2.0, доска mission-control (19 → 57 секций).
  Родня: PIT-016, PIT-070, PIT-074, PIT-088.

- **Гейт:** нет — общий методологический принцип («guard проверяет свойство, не случай»); конкретный найденный случай (`push-inbox.sh`, коллизия имён `style`/`visual-style`) живёт в `mission-control`, не в base-repo; для каждого guard'а свойство формулируется заново вручную


### PIT-090 — файлы базы, растёкшиеся в репы мимо `_base/` (2026-08-21)

- **Симптом:** в репе лежат **две** копии одного документа базы — свежая в `_base/`
  и устаревшая рядом, в её собственной `00-infrastructure/`. Обе читаются как правда,
  и какая из них попадётся сессии, зависит от того, какую она открыла первой.
- **Замер 21.08.2026** (при версии базы **1.66.0**):

  | Файл | В собственной `00-infrastructure/` | Ещё и в `_base/` | Отставание |
  |---|---|---|---|
  | `changelog.md` | **13 реп** | 42 репы | верхняя запись **`1.3.0`** у 12 реп — **63 минорные версии назад** |
  | `watchlog.md` | 13 реп | 42 репы | **3 различные редакции** между репами |
  | `base-repo-readme.md` | 12 реп | 42 репы | одна редакция, копия README базы |

- **Корень — смена механизма раздачи без уборки за прежним.** Ранний синк копировал файлы
  базы **плоско**, в `00-infrastructure/` целевой репы, и переименовывал
  (`README.md` → `base-repo-readme.md`, `CHANGELOG.md` → `changelog.md`). Позже раздача
  переехала в отдельный каталог `_base/`. Новый механизм заработал, **старые копии никто
  не удалил**.
- **🔴 Корень глубже, чем «забыли убрать» — граница режет в обе стороны.** Контракт
  вложения (`78-base-embedding.md`) гласит: `_base/` — чужое, управляется скриптом;
  `00-infrastructure/` репы — своё, **скрипт не трогает никогда**. Именно это делает
  раздачу безопасной. И это же делает **неисправимым всё, что попало на сторону «моё»
  по ошибке**: протёкшие копии лежат в пространстве, которого автоматика по контракту
  касаться не имеет права.

  **Они не «забыли обновиться» — они не могут обновиться по построению.**
- **Обратная петля.** Сбор вверх относит в кандидаты на повышение всё, чего нет
  в `_base/`. Переименованная копия по имени не совпадает с оригиналом
  (`changelog.md` ≠ `CHANGELOG.md`) — значит механизм предложит **поднять в базу
  её же собственный устаревший файл**. Пока протёкшее не вычищено, отчёт сбора
  проверяется глазами на «а не наше ли это».
- **Почему не заметили:** имена в нижнем регистре и с префиксом не совпадают с именами
  в базе (`CHANGELOG.md` ≠ `changelog.md`), поэтому сверка «есть ли этот файл в базе»
  по имени их не видит. Файл невидим ровно потому, что назван иначе.
- **Как нашлось:** частотным сигналом (`77-system-wide-audit.md` §4) — имя, встречающееся
  в 11+ репах и отсутствующее в базе, обязано быть либо кандидатом в базу, либо мусором.
  Сигнал сказал «сравни», сравнение показало третье: **не наработка репы и не пробел базы,
  а протечка самой базы**.

> ### 🔴 Поправка 21.08.2026: это было переоткрытием, а не находкой
>
> **У базы уже есть инструмент ровно под это — `scripts/legacy_delta.py`**, и его шапка
> дословно описывает задачу: «есть ли в плоских legacy-копиях базы что-то, чего нет
> в актуальной базе». Прогон даёт картину **шире** моего замера:
>
> **12 реп · дублей 977 · старых версий 216 · имён, которых в базе нет — 91.**
>
> Мои три имени (`changelog.md`, `watchlog.md`, `base-repo-readme.md`) — верхушка
> из 91. Формулировка «не находилось раньше» **неверна**: находилось, инструмент был
> написан заранее, его просто не запускали.
>
> **Это класс PIT-072** — сделать заново готовую инвентаризацию худшим инструментом
> и выдать за находку. Тот же дефект вахта уже допускала в этой кампании, и правило
> `04-watchlog-protocol.md` («прежде чем строить инвентаризацию — искать готовую»)
> существовало **до** моего замера.
>
> **Что в карточке остаётся моим и не отменяется:** мера отставания (верхняя запись
> `1.3.0` при базе `1.66.0` — 63 версии), **структурный корень** (граница режет в обе
> стороны — копии лежат в пространстве «моё», куда автоматика по контракту не заходит)
> и **обратная петля** (сбор вверх предложит поднять в базу её же устаревший файл).
> Инструмент считал файлы; чего он не объяснял — **почему они не могут обновиться**.
>
> **Практический вывод для уборки:** список брать не из моего замера, а из
> `python3 scripts/legacy_delta.py` — он полный.
- **Как правильно:**
  1. **Смена механизма раздачи включает уборку за прежним.** Новый путь работает —
     старые копии удаляются в том же батче, а не «когда дойдут руки».
  2. **Переименование при копировании запрещено.** Файл, названный при раздаче иначе,
     выпадает из всех сверок по имени и становится невидимым.
  3. **Раздаваемое живёт в одном каталоге** (`_base/`) и **только там**. Копия
     раздаваемого в собственной `00-infrastructure/` репы — всегда дефект.
- **Правило:** два места держат одну правду → они разойдутся; вопрос только когда.
  Здесь разошлись на 63 версии и на три редакции.
- **Источник:** частотный замер по 57 репам, синтез 21.08.2026.
  Родня: PIT-052, PIT-063, PIT-080, PIT-088.

- **Гейт:** да → `scripts/legacy_delta.py` (прямой измеритель «плоские legacy-копии базы vs актуальная база», найденный переоткрытием в самой карточке)


### PIT-091 — список отстаёт первым: его правка не требуется ни одним гейтом (2026-08-21)

- **Симптом:** навигатор, реестр, оглавление выглядят рабочими и указывают на половину
  того, что есть. Читающий не видит недостачи — он видит список и считает его полным.
- **Три случая за одни сутки, независимые:**

  | Список | Состояние |
  |---|---|
  | навигатор `00-infrastructure/README.md` базы | **41 из 79** документов |
  | навигатор `mission-control` | таблица теряет две колонки с 12-й строки, **протоколов 20–21 нет** |
  | реестр `decisions/README.md` | таблица кончается на 010, **`adr_011` на диске есть** |

- **Общая причина — не забывчивость, а отсутствие принуждения.** Документ добавляют,
  и всё работает: файл лежит, ссылки из других мест ведут. **Ни один гейт не спрашивает,
  попал ли он в список.** Правило держится на памяти, а память кончается на пятнадцатом
  документе.
- **Почему это хуже пустого списка:** пустой заставляет искать, неполный — **отвечает**.
  Навигатор, не знающий половины кита, перестаёт быть входом в тему, оставаясь на вид
  исправным. Тот же класс, что PIT-063 и «§0 журнала устарел».
- **Как правильно:**
  1. **Гейт на полноту списка.** Пронумерованные документы на диске сверяются с тем,
     что перечислено. Реализовано: `scripts/revision_check.py` → `check_navigator()`.
  2. **При чистой сверке печатать числа** («перечислено 78 из 78»), иначе «ок»
     не отличить от «сверил пустоту» (`71-fail-loud-and-sourcing.md` §7в).
  3. **Правило добавления документа включает строку в навигаторе** — не «допишу потом».
- **Побочная находка гейта:** он нашёл `00-infrastructure/00-navigator.md` — устаревшую
  кириллическую редакцию навигатора **чужого кита**, оставшуюся после переименования
  файлов в латиницу. Все три файла, на которые она ссылалась, не существуют.
  **Проверка ссылок её не видела**: имена были в обратных кавычках, а не markdown-ссылками.
  Удалена (восстановима из тега `v1.61.0`, sha `c7468669`).
- **Правило:** если список ведут руками, а полноту его никто не проверяет — считай,
  что он неполон. Вопрос только насколько.
- **Источник:** синтез 21.08.2026, три независимых случая. Родня: PIT-063, PIT-086, PIT-090.
- **Гейт:** да → `scripts/revision_check.py::check_navigator`, `check_prose_counts`; `scripts/repo_revision.py` (докстринг явно называет класс — PIT-091)


### PIT-092 — диагностика в stdout попадает в значение переменной (2026-08-16)

- **Симптом:** заголовок релиза публичной репы начинался с `\033[1;97m\033[95m ТРЕБУЕТ
  РЕШЕНИЯ:` — на странице релизов GitHub это видно как `[1;97m[95m ТР…`. **Публичное
  зеркало, витрина для работодателя.**
- **Механизм:** функция громкой диагностики печатала в **stdout**, а не в stderr.
  Её вызывает сборщик описания релиза, когда не находит `CHANGELOG`, а его stdout
  захватывается присвоением `TITLE="$(build_notes …)"`. Код возврата не проверялся.
  У зеркала `CHANGELOG.md` нет вовсе (история версий лежит в `docs/RELEASES.md`),
  поэтому ветка «не найден» срабатывала **штатно**, и диагностика уезжала в заголовок.
- **Правило:** **stdout функции принадлежит её результату.** Всё, что человек читает
  глазами, идёт в stderr — иначе любая `$( )` вокруг функции подберёт диагностику
  как значение.
- **Защита в три слоя, а не в один:** диагностика в stderr · присвоения проверяют код
  возврата · санитайзер срезает ANSI и управляющие символы и подставляет `repo vX.Y.Z`,
  если от заголовка ничего не осталось.
- **Почему три:** первый слой чинит причину, второй ловит её возврат, третий не даёт
  мусору уехать наружу **даже если первые два обойдены**. Для того, что публикуется
  посторонним, одного слоя мало.
- **Источник:** `mission-control/reports/pitfalls-append-2026-08-16.md`.
  Родня: PIT-071, PIT-088.

- **Гейт:** да → `templates/deploy.sh` (диагностика в stderr, санитайзер ANSI, проверка кода возврата у `build_notes`/`loud` — внутренний комментарий `# PIT-016`, строки 224-263 и 1593, по содержанию совпадает с PIT-092)


### PIT-093 — проверка на упоминание вместо проверки на запись (2026-08-16)

- **Симптом:** прогон печатал «карта сверена со списком GitHub — расхождений нет»,
  при этом трёх реп в карте не было **как записей**.
- **Механизм:** сверка делала `grep -q` по имени репы **по всему файлу**. Имя встречается
  в оглавлении ссылкой — этого достаточно. Проверка отвечала на «слово встречается
  в файле», а выдавалась за ответ на «репа описана».
- **Повторов: 2** — сначала карта отставала на 8 реп, затем на 3 зеркала.
- **Правило:** **если проверку легко пройти случайно, она не проверка.** Полный разбор
  с тремя вопросами к любой проверке — `00-infrastructure/71-fail-loud-and-sourcing.md` §7в.
- **Починка — четыре состояния вместо булева ответа:** `MISSING` (нет вовсе) ·
  `LOOSE` (упомянута, своей секции нет) · `STALE` (секция есть, репы нет) ·
  `COUNT` (число в шапке ≠ числу секций). Булев ответ «сходится/нет» скрывал три
  разные болезни под одним «сходится».
- **Источник:** там же. Родня: PIT-089, PIT-091.
- **Гейт:** да → `templates/deploy.sh` (§3.0 сверка карты: состояния MISSING/LOOSE/STALE/COUNT вместо булева ответа, внутренний комментарий `# PIT-017`, ~строка 1821, по содержанию совпадает с PIT-093)


### PIT-094 — точка входа в вахту протухает молча (2026-08-16)

- **Симптом:** §0 журнала на версии `1.3.0` описывал состояние примерно `0.25.0`:
  39 реп вместо 49, инструмент на четыре версии старше, «СЛЕДУЮЩАЯ ЗАДАЧА», сделанная
  три версии назад. **Заголовок §0 при этом честно показывал текущую версию.**
- **Повторов: 4 подряд.**
- **Механизм:** версия в шапке поднималась **скриптом**, тело правилось **руками**.
  Рассинхрон накапливался незаметно именно потому, что шапка выглядела свежей.
- **Цена:** принимающая вахта строит работу по неверной картине. В лучшем случае тратит
  первые шаги на перепроверку того, что журнал должен был сообщить; в худшем **идёт
  чинить починенное**.
- **Правило:** **документ, объявленный точкой входа, обязан иметь машинную проверку
  свежести.** Без неё он становится самым опасным файлом в репе: **выглядит достоверно
  и врёт**.
- **Починка:** перед публикацией версия из журнала сверяется с публикуемой; не совпало —
  **репа пропускается** с громким сообщением. Строки нет вовсе — предупреждение, что §0
  не читается машиной. В базе то же реализовано в `scripts/revision_check.py`
  (`check_watchlog`), плюс ручная проверка §0 «на подлинность» — `04-watchlog-protocol.md`.
- **Источник:** там же. Родня: PIT-086, PIT-091, PIT-063.
- **Гейт:** да → `scripts/revision_check.py::check_watchlog`; `templates/deploy.sh` (проверка версии §0 перед публикацией, внутренний комментарий `# PIT-018`, ~строка 1672)


### PIT-095 — самопальный парсер вместо канонического (2026-08-20)

- **Симптом:** релизы двух новых реп создавались напрямую через `gh release create
  --notes "$(sed …)"` вместо канонического деплойера. **Тело обоих релизов ушло
  на GitHub пустым.**
- **Механизм:** `sed -n '/^## \[0.1.0\]/,/^$/p'` — диапазон `sed` заканчивается
  на **первой же пустой строке**, а канон формата ставит пустую строку **сразу после
  заголовка секции**, перед датой. Парсер вырезал ноль строк содержания.
- **Корень не в `sed`.** Инструмент был исправен; дефект в том, что **канон обошли**:
  сборщик описания существовал, знал формат и был протестирован.
- **Правило:** формат, у которого есть канонический парсер, **не разбирается заново**.
  Свой разбор — это вторая реализация одного знания о формате, и она расходится
  при первом же изменении формата, о котором её автор не узнает.
- **Признак, что происходит именно это:** пишется `sed`/`awk`/regex по файлу, для которого
  в системе уже есть читающий инструмент. Вопрос себе — «почему не вызвать его».
- **Источник:** там же. Родня: PIT-063, PIT-090.
- **Гейт:** да → `scripts/check_shell_antipatterns.py` (паттерн `sed -n ... CHANGELOG` — обход канонического парсера `build_notes`)


### PIT-097 — список исключений вместо проверки свойства: утечка через забытую строку (2026-08-08)

- **Симптом:** внутренняя база с протоколами и картой **приватных** реп уехала
  в **публичное** зеркало. Ни сложной ошибки, ни гонки — просто репу **забыли внести
  в список исключений**.
- **Механизм:** раздача вела denylist «кому базу не слать». Список — намерение человека;
  он отстаёт от мира ровно в тот момент, когда мир меняется без его ведома.
- **Второй эпизод, показавший, что allowlist не спасает:** 15.08.2026 три витринных
  зеркала были **открыты в public раньше**, чем попали в список. Утечки не случилось
  только потому, что раздача в это окно не запускалась — то есть **повезло**.
- **Как правильно:** там, где ценой ошибки является утечка, решение принимается
  **по машинно-проверяемому свойству объекта**, а не по списку:

  ```bash
  [ "$(gh repo view "$OWNER/$name" --json isPrivate --jq '.isPrivate')" = "false" ] && continue
  ```

  **Список остаётся** — для скорости и читаемости, — но он больше не единственная защита.
- **Правило:** **список — намерение, свойство объекта — факт; предохранитель ставится
  на факт.** Признак применимости: у объекта есть машинно-проверяемое свойство, ради
  которого он вообще попал в список.
- **Где ещё не применено:** чистка публичного зеркала до сих пор идёт **только** по списку
  исключений — новый тип служебного файла утечёт молча. Задача в `ROADMAP.md`.
- **Источник:** `mission-control/scripts/sync-base.sh` (комментарий и предохранитель
  в коде). Родня: PIT-091, PIT-093, PIT-089.

---
- **Гейт:** да → `scripts/migrate_to_repos.py::is_public`, `scripts/secrets_scan.py` (предохранитель по факту `gh repo view --json isPrivate`, не по списку)


### PIT-099 — вывод о корпусе по выборке, которая ничего не представляет (2026-08-21)

- **Симптом:** источник просмотрен «по диагонали», из него взяты несколько элементов,
  и по ним сделан вывод о **целом**. Ошибка не даёт признака: вывод правдоподобен,
  элементы настоящие, работа выглядит проделанной.
- **Живой случай, найден в чужой репе и признан её же автором** (`dota-dossier/docs/COACHES_REVIEW.md`,
  первый абзац): *«В прошлой версии я отсмотрел **два поста из 1794** — оба оказались
  розыгрышами 2022 года — и списал канал как развлекательный. Это было поверхностное
  сопоставление вместо чтения»*. При полном прочтении канал оказался содержательным
  источником с разбором меты по ролям.
- **Механизм.** Выборка бралась **не случайно** и не по покрытию, а «сколько успел
  посмотреть». Два элемента из 1794 — это 0,1 %, и попадание в нерепрезентативный
  участок почти гарантировано; при этом «посмотрел и решил» ощущается как проверка.
- **Отличие от PIT-074.** Там параметр фильтра писался по памяти — ошибка в **правиле
  отбора**. Здесь правила нет вовсе: берётся то, что попалось. Общее — вывод
  о множестве по его непроверенной части.
- **Правило:** отрицательный вывод о корпусе («ничего ценного», «развлекательное»,
  «дубль») требует **названного покрытия**: сколько элементов из скольких просмотрено
  и как выбраны. Не назвал — это не вывод, а впечатление, и записывать его как решение
  нельзя.
  Практический минимум для отказа: сплошное чтение либо выборка **по краям и середине**
  временного диапазона, потому что содержание источников меняется со временем —
  два поста 2022 года ничего не говорят о 2026-м.
- **Дешевле, чем кажется:** цена ошибки здесь **асимметрична**. Ложно принятый источник
  снимается ревизией за минуту; ложно отвергнутый **не обнаруживается никогда** —
  к нему больше не возвращаются. Это ровно закон 4 кампании синтеза («при сомнении
  повышать», `PROCEDURE.md` §4а) на материале источников, а не файлов.
- **🔴 Вторая форма того же класса, из того же файла: искажает не выборка, а КАНАЛ.**
  Про другой источник там записано: *«основной контент в видео, а не в тексте, поэтому
  **по экспорту канал кажется беднее, чем есть**»*, и *«медиана поста 102 символа,
  поэтому по объёму текста он выглядит пустым»*.

  То есть выборка может быть **полной** — прочитано всё, что доступно, — и вывод
  всё равно неверен, потому что доступен был не весь источник. Признак:
  **оценка сделана по производной форме** (экспорт, транскрипт, превью, метаданные),
  а не по самому материалу. Тогда вместе с выводом записывается канал:
  «по текстовому экспорту — бедно; видео не смотрелось».

- **Плотность источника измеряется числом, а не впечатлением.** В том же разборе:
  *«5 полезных постов из 339, медиана поста 63 символа»*. Это позволяет вернуться
  к решению позже и не переоткрывать спор: «бедный источник» — впечатление,
  «5 из 339» — факт, с которым можно спорить предметно.

- **Родня:** PIT-074, `SYN-022` (проверка не того предмета), `PROCEDURE.md` §2
  (сомнение разрешается открытием файла, а не рассуждением о названии),
  `28` §10а (сравнивая два канала, сначала докажи, что содержимое одно).

---
- **Гейт:** нет — вывод о корпусе по нерепрезентативной выборке — суждение о том, что выборка представляет целое, содержательная оценка, не структура файла


### PIT-098 — дубль номера раздела ВНУТРИ документа: ссылка ведёт к первому попавшемуся (2026-08-21)

- **Симптом:** `65-visual-source-pipeline.md` нёс **два раздела `## 6`** — «Двухфайловое
  разделение слоёв» и «Что этот конвейер уже поймал». Живых ссылок на второй нет,
  на первый — пять (`§6`, `§6.1`, `§6.1а`, `§6.1б`, `§6.3`, из `66`, `PITFALLS.md`,
  `decisions.md`).
- **Почему это тихо:** два раздела с одним номером **выглядят нормально каждый
  по отдельности**. Ломается не файл, а **адресация**: читающий переходит по «см. §6»
  и попадает туда, куда попал первым, — без единого признака, что мест было два.
  Ни рендер, ни линк-чекер этого не видят: ссылка не битая, она просто ведёт не туда.
- **Отягчающее — класс был известен и не подмётен.** v1.89.0 нашла **ровно то же**
  в `69-agents-hooks-and-gates.md` (два `## 4а`, причём **обе** нумерации уже стояли
  в живых ссылках). Тот случай починили **поштучно**: раздел переименовали, карточку
  не завели, проверку не написали. Через пять версий класс нашёлся во втором документе —
  и нашёлся случайно, попутной командой при другой работе.
- **Механизм появления:** разделы дописываются в конец разными вахтами, каждая берёт
  «следующий свободный» номер по тому, что видит **у себя в поле зрения**. У длинного
  документа поле зрения — последний экран. Ровно та же природа, что у дублей в сквозных
  реестрах (`72-source-of-truth.md` §4а), только общего места нет даже теоретически.
- **Как правильно:** проверкой, а не вниманием. `scripts/revision_check.py` →
  `check_section_dupes()`: пара «уровень заголовка + номер» внутри одного `.md`.
  Заголовки-даты (`## 2026-08-04`) пропускаются — это записи журнала, повтор законен;
  конкатенации (`sborka-*.md`, `bundle-*.md`) вынесены в именованные исключения
  с причиной, и **исключения проверяются на протухание** отдельной функцией
  `check_allowlist_rot()` — строка, пережившая свой файл, это тихо выключенная проверка
  (PIT-097 наоборот: там список подменял проверку, здесь список её сужает).
- **Канарейка обязательна** (`71` §7в): в норме проверка всегда возвращает пусто,
  поэтому её поломка невидима. `selftest_section_dupes()` требует **ровно двух**
  срабатываний на двух разных файлах и нуля на чистом — условие `len(caught) > 0`
  прошло бы и при одной сломанной половине.
- **Правило:** **починка одного случая не закрывает класс.** Найдя дефект адресации,
  спроси не «где ещё он может быть», а «какой командой я узнаю, где он есть» —
  первое ответа не имеет, второе стоит десять строк. Признак применимости: дефект
  не ломает файл и не виден при чтении по одному месту.
- **Источник:** `exam-kit` (та же репа, что дала гейт дублей реестров версией раньше —
  `build/release.sh`). Родня: PIT-091, PIT-097, `SYN-021`, `72-source-of-truth.md` §4а.


---
- **Гейт:** да → `scripts/revision_check.py::check_section_dupes`, `check_allowlist_rot` (карточка сама себе документирует гейт)


### PIT-115 — тема `dark-ansi` делает часть текста невидимой: цвет берётся из палитры терминала (2026-08-22)

- **Симптом.** Владелец, дважды за месяц: «я не вижу чёрный текст в Claude Code».
  Часть вывода просто отсутствует на экране — не тусклая, а **невидимая**.
- **Ложный след, по которому я шёл раньше.** Считал, что виноваты мои скрипты: писал
  правило «брать яркие ANSI 91–97 вместо тёмных 30–37». Правило верное и не помогло —
  проверка показала, что в базе **тёмных кодов нет вообще**, используются 91–97 и 31–33.
- **Корень.** `~/.claude/settings.json`: `"theme": "custom:temp"`, а файл темы —
  `~/.claude/themes/temp.json` — содержит ровно `{"name":"temp","base":"dark-ansi","overrides":{}}`.

  > **`dark-ansi` не задаёт цвета, а делегирует их палитре терминала.** Интерфейс просит
  > «цвет №0», терминал отдаёт свой чёрный — и на тёмном фоне это ничто.
  > Обычная тема `dark` задаёт RGB сама и от палитры не зависит.

- **Правило-обход.** Тема на основе `*-ansi` пригодна, только если палитра терминала
  выверена под неё. При жалобе «текст не виден» **первым делом смотреть тему, а не свой код**:
  `python3 -c "import json,pathlib;print(json.loads((pathlib.Path.home()/'.claude'/'settings.json').read_text())['theme'])"`
- **Свой вклад.** Дважды чинил следствие вместо причины: правило про яркие коды закрывало
  случай «мой скрипт написал тёмным», а жалоба была про **весь** интерфейс. Признак,
  который я пропустил: не виден текст, который **не я печатал**.
- **Починка.** `theme` → `dark`, бэкап `~/.claude/settings.json.bak-2026-08-22`.
  Файл `themes/temp.json` оставлен — вреда не несёт, тема на него больше не ссылается.
- **Родня:** `71` §7д-бис (верный вывод по неверному основанию — правило про яркие цвета
  было верным и не про этот случай) · `27-claude-memory-and-instructions.md`.

---
- **Гейт:** нет — состояние личного `~/.claude/settings.json`/`themes/*.json` вне репы (тема терминала); base-repo не имеет доступа и мандата проверять чужой личный конфиг


### PIT-116 — гейт мерил половину, которая не ломается, и потому не мерил ничего (2026-08-22)

- **Симптом.** Владелец: «мы сто тысяч гейтов сделали, чтобы точка входа ВСЕГДА
  обновлялась, без неё всё ломается нахер». И тем не менее сломалась: `WATCHLOG` §0
  разросся до **1291 строки** и описывал состояние, которого нет — закрытую кампанию
  как «14 из 57», удалённый каталог как действующий, отменённый вывод как текущий.
  Заодно **README застрял на v2.0.0 при VERSION 2.45.0 — 45 версий отставания**
  на титульной странице.

- **Корень — не отсутствие гейта, а его прицел.** `check_watchlog` (`PIT-094`) сверял
  **номер версии** в §0 с `VERSION`. Номер проставляется **скриптом**, то есть машиной,
  и не отставал никогда. Гейт был вечно зелёным.

  > **Проверялась половина, которая не ломается. Ломалась половина, у которой проверки
  > не было.** И зелёный гейт работал как свидетельство исправности — то есть мешал
  > заметить поломку, а не помогал.

- **Тот же класс у README.** Инструмент `scripts/readme_status_gate.py --fix` **существовал
  и работал** — прогон занял секунду и всё проставил. Его просто **никто не запускал**:
  правило было, исполнителя не было (`21` §4г).

- **Правило.** У проверки спрашивают не «проверяет ли она нужный документ», а
  **«может ли то, что она проверяет, вообще испортиться»**. Если проверяемая величина
  ставится машиной — проверка тавтологична. Годится только то, что правится **руками**.

- **Что добавлено:**
  - `check_resume_point_content` — размер §0 (потолок 80 строк) · мёртвые пути в §0 ·
    упоминание версий, отставших больше чем на 10 минорных;
  - `check_living_documents` — §3 ровно 10 записей · секция текущей версии в `CHANGELOG` ·
    блок статуса README с текущей версией · указатель «СЛЕДУЮЩАЯ ЗАДАЧА» в `ROADMAP` ·
    реестр ADR совпадает с составом каталога.

  🔴 **Второй гейт нашёл дефект на первом же прогоне** — отставание README на 45 версий.

- **Чего гейты не ловят, названо сразу:** содержательное устаревание без структурного следа.
  Для него остаётся ручной приём «три утверждения» (`04-watchlog-protocol.md`).
- **Родня:** PIT-094 (половина этой проверки) · `21` §4г (правило без исполнителя) ·
  `71` §7в (канарейка: проверка, всегда возвращающая пусто, ломается незаметно).

- **Гейт:** да → `scripts/revision_check.py::check_resume_point_content`, `check_living_documents` (карточка сама себе документирует добавленные функции)


### PIT-117 — карточка не на своём уровне заголовка невидима гейту, который её адресует (2026-08-22)

- **Симптом.** Гейт CLEAN, при этом PIT-115 и PIT-116 стояли на '##' при 98 карточках на '###'. Проверка check_dangling_registry_refs собирает существующие карточки регуляркой '^###\s+PIT-' — обе свежайшие карточки для неё не существовали.

- **Корень.** Проверка опиралась на ФОРМУ (уровень заголовка), которую никто не проверял. Форму ставит рука, значит она портится. Хуже: невидимая карточка обрезает max(своих номеров), и весь диапазон выше неё выпадает из проверки — то есть один кривой символ глушит гейт для всех последующих карточек.

- **Правило.** Если проверка опирается на форму записи, форма обязана проверяться отдельно — иначе гейт зелён потому, что не видит, а не потому, что чисто.

- **Свой вклад.** Первая редакция генератора `scripts/capture.py` писала карточку на
  `##` — то есть автоматизация закрепила бы дефект во всех будущих карточках. Поймано
  сверкой «сколько карточек на каком уровне» до первого боевого запуска, а не после.

- **Последствие, вскрытое починкой.** Когда обе карточки стали видимы, `max(своих)`
  дорос до 116 и накрыл чужой диапазон `portrait-of-taste:PIT-101…116` — законные
  цитаты стали неотличимы от висячих ссылок. Докстринг проверки это предсказал и
  предписал лечение: развести нотацию, а не глушить проверку. Введена
  квалифицированная форма `репа:PIT-NNN`; задача из `ROADMAP.md` закрыта.

- **Что добавлено:** `check_card_heading_levels` в `revision_check.py` с канарейкой,
  ловящей подсаженный кривой уровень в каждом реестре по отдельности.

- **Родня:** PIT-116 (гейт мерил половину, которая не ломается — тот же класс, здесь
  применён к самому гейту) · PIT-098 (дубль номера ломает адресацию) · `71` §7в
  (канарейка) · `21` §4г (правило без исполнителя).

- **Гейт:** да → `scripts/revision_check.py::check_card_heading_levels` (карточка сама себе документирует гейт)


### PIT-118 — session_metrics сам выбирает лог, и при двух sessionId одной сессии выбирает не тот (2026-08-22)

- **Симптом.** Срез вахты IX дал 106 ходов, макс. контекст 180764, hit 98.7%. Через несколько ходов пришло уведомление о фоновой задаче, и по нему нашлось, что логов у сессии ДВА: 2020e9b2 (CLI 2.1.238) и 7fffbb95 (CLI 2.1.239, совпадает с CLAUDE_JOB_DIR). Скрипт молча взял первый. Правильные числа: 126 ходов, макс 208962, hit 97.7%, цена хода x2.3 вместо x1.9.

- **Корень.** Канарейка --selftest проверяет АРИФМЕТИКУ свёртки задвоенных строк и была зелёной. Выбор входного файла она не проверяет вовсе. То есть измеритель доказывал, что правильно считает, и ничего не говорил о том, что считает не то.

- **Правило.** У измерителя, который сам находит источник, проверять надо не только счёт, но и ВЫБОР источника — и печатать его так, чтобы расхождение было видно без запроса.

- **Свой вклад.** Числа успели уехать в `measurements.csv` и в разбор `hypothesis.md` §3
  до того, как расхождение нашлось. Нашлось оно **случайно** — по уведомлению о чужой
  фоновой задаче, чей идентификатор оказался только в одном из логов. То есть поймала
  не проверка, а совпадение: ровно так же, как реестр ADR поймала коллизия номера.

- **Второй свой вклад, в самой починке.** Первая редакция `pick_log` брала id джоба как
  `Path(CLAUDE_JOB_DIR).parent.name` и получала «jobs» вместо `7fffbb95`. Ветка не
  срабатывала никогда, выбор молча падал обратно на mtime — **починка существовала только
  на бумаге** (`21` §4г). Поймано прогоном с печатью выбранного файла, а не чтением кода.

- **Что добавлено:** `pick_log` в `session_metrics.py` — выбор по `CLAUDE_JOB_DIR`
  (детерминированно, mtime не судья), а при нескольких свежих логах рядом они
  **называются вслух** с размерами. Неоднозначность, показанная в выводе, перестаёт
  быть тихой.

- **Родня:** PIT-116 (проверялась половина, которая не ломается — здесь канарейка
  проверяла счёт, а не выбор источника) · PIT-117 (гейт не видел карточку из-за формы) ·
  `21` §4г (правило без исполнителя) · `WATCHLOG` §4, запись 2026-08-20 («проверять надо
  тем каналом, которым инструмент работает»).

- **Гейт:** да → `reports/experiments/token-consumption/session_metrics.py::pick_log`


### PIT-119 — правило, сформулированное как запрет, заблокировало работу, ради которой система построена (2026-08-22)

- **Симптом.** Стандарт кампании §5 назывался «Личное не открывается, и это не поблажка». Вахта прочитала это как запрет трогать личное и отказалась переносить медицинские документы в health-vault — репу, размеченную ровно под них (04-psychiatry-pnd, 03-medical-records-clinic, 06-vaccinations). Владелец: «репозитории ПРИВАТНЫЕ, только мной используются, мы всё трогаем».

- **Корень.** Формулировка склеила два разных утверждения: (1) при инвентаризации кампании открывать личное незачем, потому что метка С ставится по расположению и открытие не добавляет информации — довод про экономию; (2) якобы личное вообще нельзя трогать — чего никто не решал. Из верного довода про одну задачу вырос запрет на класс операций. Настоящая граница проходит по ПУБЛИКАЦИИ: наружу — никогда, в приватную репу — штатное место.

- **Правило.** Правило-запрет обязано называть, ЧТО именно запрещено и в какой точке: «не публиковать наружу» и «не трогать» — разные правила, и второе дороже первого.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — интерпретация формулировки правила-запрета (что именно запрещено: «не трогать» vs «не публиковать наружу») — семантика/политика доступа к приватным репам, не файловый инвариант


### PIT-120 — перенос внутри iCloud-каталога идёт через облако, а не по диску (2026-08-22)

- **Симптом.** Копирование 7030 файлов в система_репозиториев шло со скоростью ~16 файлов за несколько минут при том, что 255 медицинских файлов на 393 МБ до этого прошли быстро. Раньше в этой же сессии зависла на 120 с простая команда stat/file по PDF в ~/Documents.

- **Корень.** brctl status показывает активный контейнер com.apple.CloudDocs в состоянии needs-sync-up: весь ~/Documents синхронизируется с iCloud Drive. Значит (1) чтение файла-заглушки требует докачки из облака, (2) каждая запись в цель ставится в очередь выгрузки, (3) удаление источника уходит на все устройства. Операция, выглядящая как локальное копирование, на деле идёт через сеть.

- **Правило.** Перед массовой файловой операцией проверить brctl status: каталог под iCloud ведёт себя как сетевое хранилище, а не как диск — время, порядок и обратимость там другие.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/import_dedup.py` (детектор iCloud-заглушки через `st_blocks==0`, PIT-120)


### PIT-121 — фильтр переноса молча применился к удалению: исключённое из копии снесено вместе с источником (2026-08-22)

- **Симптом.** Перенос IT (4136 файлов) отчитался «источник пуст и удалён». На деле files_of() исключал пути с .git — 531 файл, — поэтому они НЕ переносились; но rmtree сносил источник целиком. Вместе с ним ушла git-история локального клона vk-graph. Тем же прогоном .env с живым токеном vk1.a... уехал внутрь it-base/90-imported/ — от коммита спасло лишь то, что у it-base нет локального .git.

- **Корень.** Один и тот же фильтр обслуживал две операции с противоположными требованиями. Для ПЕРЕНОСА исключить .git правильно: чужая история в репе не нужна. Для УДАЛЕНИЯ то же исключение означает «не считать за данные» — и защищённое от переноса оказалось незащищённым от сноса. Проверка «источник пуст» считала тем же files_of, поэтому непустой источник выглядел пустым.

- **Правило.** Проверка перед необратимым шагом обязана считать своим счётчиком, а не тем, что обслуживает предыдущий шаг: фильтр «что переносим» и фильтр «что можно снести» — разные списки, и совпадение их случайно.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/migrate_to_repos.py` (проверка перед удалением считает ВСЁ живое, не тот же фильтр `files_of`, что использовался для переноса — комментарий у PIT-121, ~строка 312)


### PIT-122 — проверка каталогов не видит файл рядом: ls -d */ объявил чистым каталог с 240 МБ архивом (2026-08-22)

- **Симптом.** После разбора папки «Old (before Claude)» я отчитался, что каталог система_репозиториев содержит только репы: ls -d */ | wc -l дал 61, все имена — репы. Владелец: «а архив Old чего ты оставил». Рядом лежал файл «Old (before Claude).zip» на 240 МБ — тот же набор из 30 архивов.

- **Корень.** Команда проверки повторяла форму объекта, который я только что обработал: разбирал каталог — и проверял каталоги. Шаблон ls -d */ по построению не показывает файлы, поэтому его вывод «только репы» был верным утверждением о каталогах и ложным о содержимом папки. Это тот же класс, что PIT-116: проверялась половина, которая не ломается.

- **Правило.** Проверка «в каталоге не осталось лишнего» обязана перечислять ВСЁ содержимое (ls -a), а не только объекты того типа, с которым только что работал: тип обработанного объекта не сужает пространство того, что могло остаться.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — конкретная ошибка самопроверки в разовой ревизии; узкий regex-антипаттерн на `ls -d */` дал бы много ложных срабатываний, потому что интерес именно к каталогам — частый легитимный случай


### PIT-123 — карта переноса не спрашивала о публичности репы-цели (2026-08-22)

- **Симптом.** Кампания переноса разложила материал по 20 репам. Перед сборкой архивов проверка показала: пять целей ПУБЛИЧНЫЕ — algorithms-site, bron-kerbosch, claude-usage, health-report-generator, vk-graph. В claude-usage уехали 152 файла личных инструкций, ADHD.pptx, «ЭКСПОРТ памяти для другой ИИ», settings.json.backup; в vk-graph — распакованный проект, внутри которого был .env с живым токеном.

- **Корень.** MAP в migrate_to_repos.py сопоставляла папку с репой ПО ПРЕДМЕТУ и ничего не знала о видимости цели. Предохранитель по isPrivate существует и работает — но он стоит в sync-base.sh, то есть на раздаче базы, а не на переносе материала. Новый инструмент воспроизвёл дефект, от которого старый уже защищён: защита была свойством одного скрипта, а не системы.

- **Правило.** Инструмент, кладущий файлы в репу, обязан спрашивать gh repo view --json isPrivate о КАЖДОЙ цели — сам, а не полагаться на то, что где-то в системе такая проверка уже есть.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/migrate_to_repos.py::is_public`, `scripts/secrets_scan.py`


### PIT-124 — адрес переноса назначался по имени папки, а не по её содержимому (2026-08-22)

- **Симптом.** Папка ~/Documents/claude уехала в репу claude-usage целиком — 152 файла. Владелец: «нахера ты вообще в claude usage положил срану презу про ADHD и всякий мусор, когда для рандомных вещей есть спец репа misc-vault». По факту внутри лежали три разные вещи: 142 файла инфраструктуры Claude (Instruction files, Skills, .agents), 29 файлов Account data и 3 случайных файла (ADHD.pptx, «Сетевые протоколы.docx», PDF про обход региона).

- **Корень.** MAP строилась как «имя папки → репа», и для папок с говорящим именем содержимое не открывалось вовсе: имя выглядело достаточным основанием. Для спорных случаев (аналитика, логика, видео) допущение называлось строкой и содержимое проверялось — а «очевидные» шли вслепую. То есть проверка применялась там, где сомнение уже возникло, и пропускалась там, где его не возникло, — хотя ошибается именно второй случай.

- **Правило.** Адрес переноса назначается по вскрытому содержимому, а не по имени источника: имя папки — гипотеза о предмете, и «очевидное» имя не повышает её надёжность, а лишь снижает вероятность проверки.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — эвристика одноразового инструмента миграции (кампания переноса завершена); классификация контента по смыслу — суждение, не файловый инвариант


### PIT-125 — измеритель разрывов не видел задачи с приоритетом-прочерком (2026-08-22)

- **Симптом.** tasks_census.py сообщал «работа идёт, но доска не знает» про christ-walk, portrait-of-taste и vevdokimovm.github.io. Секции на доске у всех трёх существовали — проверено grep. Ложное срабатывание нашлось только потому, что я пошёл заводить им секции и увидел, что они уже есть.

- **Корень.** Парсер строк доски ждал строго вида «| П + цифра |», а часть задач записана с прочерком вместо приоритета: «| — | 👤 | Видео протестантизм |». Секция читалась как пустая, репа попадала в разрыв. Формат доски допускает задачу без назначенного приоритета, а измеритель этого не знал — то есть мерил СВОЁ представление о формате, а не формат.

- **Правило.** Парсер чужого формата пишется по фактическому разбросу значений в файле, а не по ожидаемому образцу: перед регуляркой — посмотреть, какие варианты реально встречаются в колонке.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/tasks_census.py::BOARD_ROW` (регэксп явно допускает приоритет-прочерк, комментарий цитирует находку 22.08)


### PIT-126 — перенос каталога оборвал абсолютные пути, которыми на него ссылались извне (2026-08-22)

- **Симптом.** Кампания переноса унесла папку ~/Documents/claude в репу. Через несколько батчей выяснилось, что ~/.claude/CLAUDE.md подключает файлы директивой @ по абсолютному пути ~/Documents/Claude/Instruction files/ — то есть личные инструкции владельца, загружаемые в КАЖДУЮ сессию Claude Code, перестали существовать по своему адресу. Проверка ls дала «No such file or directory».

- **Корень.** Перед переносом проверялось только содержимое папки — что внутри и куда это по предмету. Не проверялось ВХОДЯЩИХ ССЫЛОК: кто ссылается на этот путь снаружи. Файл может быть ценен не сам по себе, а тем, что на него настроен инструмент, и такая ценность в содержимом не видна вовсе. Регистр в пути (Claude против claude) маскировал совпадение: на macOS ФС регистронезависима, и папка была той же самой.

- **Правило.** Перед переносом каталога — grep по конфигурационным точкам входа (~/.claude/CLAUDE.md, settings.json, ~/.zshrc, скрипты системы) на предмет абсолютных путей внутрь него: перенос обрывает не только файл, но и всё, что на него настроено.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/check_incoming_refs.py`, 28.08.2026 (аудит покрытия
  гейтами, владелец: «все ли питфолы обросли сторожами»). Grep по фиксированным
  конфиг-точкам входа (`~/.claude/CLAUDE.md`, `settings.json`, `.zshrc`,
  `.zprofile`) на предмет абсолютных путей внутрь переносимого каталога,
  регистронезависимое сравнение — ровно тот класс совпадения, что спрятал
  находку в реальном случае. Селфтест ловит регистронезависимое совпадение
  и не даёт ложных срабатываний на чистом файле.


### PIT-127 — обратные кавычки в аргументе съедены подстановкой shell (2026-08-22)

- **Симптом.** Строка для журнала передавалась ключом --log и содержала имя репы в обратных кавычках. В журнал попало «ревизия :» — имя исчезло вместе с кавычками. Батч при этом закрылся штатно: гейт CLEAN, архив собран, дефект виден только при чтении самой записи.

- **Корень.** zsh трактует обратные кавычки как подстановку команды: `health-vault` был выполнен как команда, вернул пустоту и ошибку «command not found», а в аргумент ушла пустая строка. Ошибка печаталась в вывод, но среди строк успешного ритуала не читалась как отказ. Формат журнала требует имён в обратных кавычках — то есть конфликт встроен в саму конвенцию записи.

- **Правило.** Текст с обратными кавычками не передаётся аргументом командной строки: либо через файл, либо одинарными кавычками целиком. В инструменте — проверка на непарную кавычку, потому что заметить съеденную подстановку постфактум можно только по следу.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — конкретный случай shell-подстановки бэктиков в аргументе логирования одного скрипта чужой репы; универсальный regex-антипаттерн «бэктики в аргументе» ловил бы легитимные markdown-цитаты в текстах команд слишком часто


### PIT-128 — версия закрывала удобный момент, а не законченную работу (2026-08-22)

- **Симптом.** За одну сессию база прошла 12 версий (2.52 … 2.63), и владелец увидел в загрузках стену одинаковых архивов по 3 МБ: «ты делаешь версии как будто это новые репы, но там уже куча версий». Содержательных изменений было втрое меньше: правка README одной репы становилась отдельной версией базы со своим архивом.

- **Корень.** Ритуал требует закрывать батч версией — и это правильно: незакрытый батч не переживает обрыв. Но размер батча ничем не задан, а в авто-режиме есть давление закрывать чаще: закрытый батч ощущается как сохранённая работа. В итоге единицей версии стала не законченная работа, а любой удобный момент остановки. Стоимость невидима изнутри: каждый отдельный ритуал дёшев, дорога только их сумма — 12 архивов, 12 записей §3, 12 подрезок журнала.

- **Правило.** Версия закрывает ЗАКОНЧЕННУЮ работу, а не удобный момент остановки: если секцию CHANGELOG нельзя назвать одним предложением без союза «и», батч рано закрывать.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/close_batch.py` (проверка размера батча, комментарий и подсказка правила цитируют PIT-128)


### PIT-129 — ретрай повторял детерминированный отказ и подписывал его выдуманной причиной (2026-08-23)

- **Симптом.** Владелец запустил деплой; push в edu-base отклонялся, скрипт печатал «обычно TLS-таймаут к GitHub» и повторял пять раз, заливая 549 МБ по 1.5 МБ/с. Владелец решил, что проблема в VPN, и спросил, не у меня ли тоже сбои сети. В логе прямым текстом: «File ч1.mp4 is 207.75 MB; this exceeds GitHub's file size limit of 100.00 MB», «GH001», «pre-receive hook declined».

- **Корень.** Функция retry в deploy.sh повторяла ЛЮБУЮ неудачу, не разбирая причину, и подписывала её готовым объяснением «обычно TLS-таймаут». Объяснение было вписано в текст сообщения, а не выведено из вывода команды — то есть скрипт утверждал причину, которую не проверял. Отказ по содержанию (лимит размера) детерминирован: он не изменится ни от ожидания, ни от повтора.

- **Правило.** Ретрай применяется только к отказу, который МОЖЕТ измениться со временем. Прежде чем повторять — прочитать вывод: сеть и таймаут ретраятся, лимит размера, отклонение хуком и нехватка прав чинятся, а не пережидаются.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/git_limits_check.py` (лимит файла 100 МБ, GH001, комментарий цитирует PIT-129)


### PIT-130 — оборванный архив неотличим от готового по имени, размеру и сигнатуре (2026-08-23)

- **Симптом.** Проверка архивов показала: misc-vault-v0.1.0.zip (1.43 ГБ) и it-base-v1.2.0.zip не читаются — «File is not a zip file», нет центральной директории. При этом команда file говорит «Zip archive data», размер выглядит правдоподобно, имя верное. Упаковку убил таймаут команды, а частично записанный файл остался.

- **Корень.** Упаковщик пишет zip потоком, и центральная директория дописывается последней. Убитый на середине процесс оставляет файл, у которого есть сигнатура и данные, но нет оглавления. Ни имя, ни размер, ни утилита file этого не показывают — нужна попытка прочитать оглавление. Отдельно: часть «битых» в замере оказалась архивами, которые в тот момент ещё дописывались фоновым процессом.

- **Правило.** Готовность архива проверяется чтением его оглавления, а не наличием файла: zipfile.ZipFile(...).infolist() либо unzip -l. И проверять надо ПОСЛЕ того, как упаковщик завершился, иначе поймаешь недописанный и примешь за битый.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `templates/deploy.sh` (`zipinfo -1` проверяет оглавление вместо CRC/имени/размера, ~строка 1140, помечено PIT-130); `scripts/git_limits_check.py` (та же проверка для системной сверки)


### PIT-131 — параллельные запуски упаковщика писали в один файл и рвали архив (2026-08-23)

- **Симптом.** Четыре архива подряд оказались битыми — misc-vault (1.43 ГБ), it-base, legal-knowledge-base, science. Причём science до этого собрался успешно, я видел строку «собран». Проверка ps показала девять процессов упаковки: три моих фоновых запуска паковали ПЕРЕСЕКАЮЩИЕСЯ списки реп одновременно и писали в один и тот же файл.

- **Корень.** Упаковка крупной репы под iCloud идёт минутами, поэтому я запускал её фоном. Каждый следующий запуск включал репы, ещё не законченные предыдущим — списки пересекались. Защита в упаковщике была, но не от того: проверка out.exists() срабатывает, только если файл УЖЕ есть, а два процесса стартуют, когда его ещё нет, и оба видят «свободно». Гонка была открыта по построению.

- **Правило.** Долгая операция, запускаемая фоном повторно, обязана иметь замок по цели, а не проверку существования результата: результат появляется в конце, а конфликт начинается в начале.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/pack_release.py` (замок `.zip.lock` по PID вместо `out.exists()`, ~строка 142); `scripts/git_limits_check.py` (различение битого/ещё-пишущегося по замку)


### PIT-132 — фолбэк вернул ноль молча, и отказ стал неотличим от результата (2026-08-23)

- **Симптом.** Замер сессии напечатал «рост цены хода на диапазоне 0…700 000: ×0.0» при том, что в той же таблице цена шла 10 083 → 72 829, то есть ×7.2. Число невозможно арифметически, но выглядело как валидный результат: та же строка, тот же формат, просто маленькое значение.

- **Корень.** Рост считался как bk[-1]/bk[0] с фолбэком «if bk[0] else 0». Рестарт сессии (/login на ходу 122: контекст 202 671 -> 0) создал бакет «0» с одним ходом и нулевой ценой. Он стал первым, деление сорвалось в фолбэк — и фолбэк вернул 0 МОЛЧА, вместо того чтобы назвать причину. Ноль как значение неотличим от нуля как признака отказа.

- **Правило.** Фолбэк на невозможное значение обязан говорить, что сработал: возвращать 0 там, где 0 является допустимым результатом, — значит прятать отказ внутри нормально выглядящего вывода.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `reports/experiments/token-consumption/session_metrics.py` (рост цены хода считается от первого НЕНУЛЕВОГО бакета, фолбэк называет причину вместо молчаливого 0)


### PIT-133 — хвостовой пробел в имени каталога сорвал раскладку, и отчёт выглядел успешным (2026-08-23)

- **Симптом.** Раскладка импорта academic-portfolio отчиталась «перенесено 544» и завершилась без ошибок, хотя разложить требовалось 630. Каталог «Защита » — с хвостовым пробелом в имени — не совпал с ключом карты «Защита», и 86 файлов остались на месте. Ни вывод скрипта, ни ls этого не показывали: пробел в конце имени невидим. Второй случай за две сессии — первым была папка «магистратура » в карте кампании переноса.

- **Корень.** Сопоставление шло по точному равенству имени, а имена каталогов на диске содержат невидимые различия: хвостовые и двойные пробелы, неразрывный пробел, разные формы юникод-нормализации у кириллицы. Скрипт не падает — он просто не находит совпадения и молча пропускает, а отчёт о числе перенесённого выглядит успешным, потому что перенесённое действительно перенесено.

- **Правило.** Сопоставление имён с диском делать по .strip() и нормализации, а результат раскладки проверять РАЗНИЦЕЙ чисел: сколько было к переносу минус сколько перенесено. Отчёт «перенесено N» без этой разницы не отличает «всё» от «то, что нашлось».

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — инструмент раскладки был разовым скриптом завершённой кампании переноса (`academic-portfolio`), сейчас не хранится в `scripts/` как персистентный инструмент; принцип (`.strip()` + разница чисел до/после) — операционный совет для будущих подобных скриптов, не инвариант base-repo


### PIT-134 — формула приёмки считала разные множества с двух сторон, и починка повторила дефект (2026-08-23)

- **Симптом.** Проверка раскладки импорта дважды за вечер показала расхождение там, где потерь не было: research-craft дал -1, legal-knowledge-base дал +36. Оба раза число совпадало со счётчиком удалённого мусора. Хуже: когда я вынес формулу в отдельную функцию audit(), чтобы закрыть дефект механизмом, первая же её редакция воспроизвела ту же ошибку — тест на реальных числах 782 = 746 + 36 + 0 показал «РАСХОЖДЕНИЕ 36».

- **Корень.** Баланс считался как before - moved - left, где before считалось БЕЗ мусора (.DS_Store исключался фильтром), а мусор при этом удалялся и попадал в свой счётчик. Множества с двух сторон равенства были разными. Починка повторила ошибку, потому что переносила формулу дословно, а не пересобирала её из вопроса «куда делся каждый файл, который был».

- **Правило.** Формула приёмки строится от вопроса «куда делся КАЖДЫЙ файл, который был», а не переносом прежнего выражения: было = разложено + удалено + осталось. Любой исход, который может случиться с файлом, обязан иметь слагаемое.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/import_dedup.py::audit()` (формула баланса «было = разложено + удалено + осталось», комментарий цитирует PIT-134)


### PIT-135 — в облачном каталоге цена операции определяется тем, читает ли она содержимое (2026-08-23)

- **Симптом.** Дедуп misc-vault по sha256 прожил час и снял ноль файлов — 451 заглушка iCloud из 1490. Через час раскладка it-base, где заглушек ВДВОЕ больше по доле (2780 из 7360, 38%), прошла за секунды и разложила все 7360 файлов. Одна и та же среда, тот же iCloud, противоположный результат.

- **Корень.** Различие не в объёме и не в удаче, а в том, ЧИТАЕТ ли операция содержимое файла. os.rename в пределах одной ФС меняет только метаданные — заглушка остаётся заглушкой, сеть не трогается. Хеширование, копирование, упаковка, поиск по тексту содержимое читают, и каждая заглушка превращается в сетевой запрос с ожиданием. Изнутри кода разница невидима: обе операции выглядят как «работа с файлом».

- **Правило.** В каталоге под облачной синхронизацией сперва спросить, читает ли операция содержимое. Не читает (rename, переименование, обход имён) — заглушки безразличны. Читает (sha256, копирование, zip, grep) — сначала материализовать brctl download, иначе операция уйдёт в сеть на каждом файле.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/pack_release.py`, `scripts/import_dedup.py` (детекция iCloud-заглушек `st_blocks==0` перед операциями, читающими содержимое)


### PIT-136 — версия репы поднималась без WATCHLOG §0, и деплой отказывался публиковать (2026-08-23)

- **Симптом.** Владелец запустил деплой и получил: «ТРЕБУЕТ РЕШЕНИЯ: career v0.2.0 — WATCHLOG §0 стоит на 0.1.0, точка входа в вахту отстала. Обнови §0 и перезапусти» — репа пропущена, не опубликована. Проверка по всем репам дала шесть таких: career, ml-base, portrait-of-taste, research-craft, science, self-map. Владелец: «хватит нарушать уже правила деплоя надоело».

- **Корень.** Ревизия поднимала VERSION и писала секцию в CHANGELOG, но WATCHLOG §0 не трогала. В базе этот шаг встроен в close_batch.py и потому исполняется всегда; для ЧУЖИХ реп версия поднималась вручную тремя строками питона — и ритуал сократился до того, что помнилось. Правило существовало, исполнителя у него не было (класс PIT-116). Отдельно: две репы вообще не имели строки «**Версия:**», которую читает деплой, — сверять было не с чем.

- **Правило.** Версия репы поднимается ОДНОЙ операцией, включающей WATCHLOG §0: VERSION + секция CHANGELOG + строка «**Версия:**» в журнале. Ручной подъём версии в чужой репе воспроизводит дефект каждый раз, когда шагов больше одного.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `scripts/bump_repo.py` (подъём версии одной операцией: VERSION + WATCHLOG §0 + строка «Версия:», комментарий цитирует PIT-136)


### PIT-137 — общий обработчик ошибки назвал виновником последний шаг цепочки (2026-08-23)

- **Симптом.** Деплой отчитался: «сверку карты выполнить не удалось (python3/парсинг) — проверь вручную». Проверка воспроизведена вручную тем же кодом на том же файле — она работает и даёт «секций 60, реп на GitHub 61». То есть падал не парсинг.

- **Корень.** Блок сверки клонирует base-repo с GitHub во временную $WORK/_mapchk, а перед ним стоит `2>/dev/null` и `|| :` на самом python. Обе конструкции глушат причину: клон мог не пройти, файл не появиться, python получить несуществующий путь — и всё это выглядит одинаково как «парсинг не удался». Сообщение называет виновником python, хотя тот отработал бы, если бы получил файл.

- **Правило.** Сообщение об отказе обязано называть шаг, который отказал, а не последний шаг цепочки. Если между «клонировать», «скопировать» и «разобрать» стоит общий обработчик ошибки — он врёт на двух шагах из трёх.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** да → `templates/deploy.sh` (каждый шаг сверки карты отчитывается за себя через `_mapstage`, ~строка 1807, помечено PIT-137)


### PIT-140 — устаревшая версия канона названа текущей, потому что её повторяют все живые файлы (2026-08-09, найдено владельцем)

**Симптом.** Вахта **дважды подряд** назвала каноном матмодели `v3.0.0`, при том что
действующий канон — `v3.5.0`, прошедший пять раундов сертификации.

**Корень — не забывчивость, а согласованный хор источников.** Устаревшую версию
одновременно называли текущей: `CLAUDE.md`, `README`, доки, `knowledge/`,
комментарии в коде, **докстринги тестов** — 28 файлов. Проверка «сверился с документом»
давала неверный ответ: документов много, и все повторяли одно и то же старое число.

> **Хор источников не повышает достоверность, если все они списаны друг с друга.**
> Двадцать восемь совпадающих упоминаний ощущаются как двадцать восемь подтверждений,
> а являются одним — размноженным копированием.

🔴 **И это был уже пойманный баг.** Тот же дефект флагался раньше в
`docs/ui_visual_direction.md`, был назван — и брошен. Второе появление стоило дороже
первого: к этому моменту неверное число расползлось по всему дереву.

**Как правильно.** Единственный источник версии — **файл самого канона**, а не
упоминание о нём. Механическая проверка вместо доверия к тексту:

```bash
grep -L SUPERSEDED docs/math_model_v*.md   # какой файл НЕ помечен устаревшим — тот и канон
```

Исторические версии не удаляются (история владельца), но **помечаются баннером
`SUPERSEDED`**, и число в шапке живых файлов вычисляется, а не переписывается руками.

**Правило.** 🔴 **Версия канона определяется признаком файла, а не упоминанием в прозе.**
Переписывание числа руками в N местах гарантирует расхождение на N−1 из них.

📍 Заодно тем же разбором вскрылось: `README.md` отставал **сильнее матмодели** —
версия в шапке на целую веху (7.7.1 против 8.9.1), таблица технологий не упоминала
актуальный фронтенд-стек вообще, миграций указано 19 вместо 31.

- **Гейт:** да → `scripts/revision_check.py::check_living_documents`, `check_prose_counts` (тот же принцип «версия определяется признаком файла, а не упоминанием в прозе», применённый здесь к `VERSION`/`CHANGELOG`/`README` самой base-repo; матмодель из карточки — чужая репа)


### PIT-139 — платный аккаунт заведён на почту, которую может отключить третья сторона (2026-08-23)

**Симптом.** Ящик создан в 05:05, подписка Pro оплачена в 05:11, к 19:50 провайдер
отключил ящик. Доступ к оплаченному аккаунту потерян целиком, обходного пути нет.

**Корень.** У Claude **нет пароля** — вход только кодом на почту или через Google SSO,
и сменить почту на аккаунте нельзя в принципе. Почтовый ящик оказался единственным
ключом, а держала его третья сторона со своим классификатором. Классификатор сработал
**на регистрации**: ролевое имя `*.support@` на бесплатной почте читается как
«Impersonating other services», поведения к моменту бана ещё не существовало.

**Как правильно.** Платный аккаунт — на старый `@gmail.com` **с Google SSO**: он
переживает потерю доступа к письмам. Ролевой адрес (`support@`, `admin@`) — только
на своём домене, никогда на бесплатной чужой почте. Строка в индекс секретов заводится
при создании аккаунта, а не когда он понадобился.

**Правило.** 🔴 **Выход из аккаунта — необратимое действие, если вход зависит
от внешней стороны.** Прежде чем разлогиниться, проверь, чем будешь входить обратно.

Разбор — `reports/incidents/vahta_a_email_suspension_incident.md`.

- **Гейт:** нет — инфраструктура личного email/аккаунта Anthropic владельца — вне дерева base-repo


### PIT-138 — ревизия свелась к механике, потому что механика отчитывается числами (2026-08-23)

- **Симптом.** Владелец: «а чё ты пишешь манифесты? ты же не сделал тяжёлую работу — ревизию. Такое ощущение, как будто ты даже не начинал её. До сих пор куча мусора и несинтезированной инфы, больших лишних файлов, файлы не переведены из медиа в служебные заметки». Замер: в репах 5 236 медиа-файлов (pdf, docx, pptx, jpg, mp4), из них .md-выжимку имеют 23 — 0.4 %. Без выжимки 5 213.

- **Корень.** Ревизия свелась к тому, что МОЖНО СДЕЛАТЬ КОМАНДОЙ: разложить импорт, снять дубли по хешу, привести README к диску, проставить классы, написать манифест. Всё это механика, и она отчитывается числами — 13 500 файлов разложено, 821 дубль снят, — поэтому создавала ощущение движения. Содержательная половина (прочитать документ и написать, что в нём) не делается командой, не даёт быстрых чисел и потому откладывалась каждый батч. Манифест при этом описывает СОСТАВ репы, а не содержание её файлов — то есть выглядит как итог ревизии, не будучи им.

- **Правило.** Ревизия репы не закрыта, пока её тяжёлые файлы не переведены в текст: механика (раскладка, дубли, пути) — это подготовка к ревизии, а не она сама. Признак подмены: все закрытые пункты измеряются числом файлов, ни один — числом прочитанных документов.

- **Свой вклад.** [что сделал не так сам — чтобы не повторить; нет — так и написать]

- **Родня:** [смежные карточки и разделы]
- **Гейт:** нет — признак подмены («все закрытые пункты числом файлов, ни один числом прочитанных документов») это суждение о содержательной полноте ревизии — по определению не мерится счётом файлов, что карточка сама и утверждает


### PIT-141 — принятый ADR указывал на механизм, которого не существует в коде (2026-08-25)

- **Симптом.** Владелец спросил, почему `_base/` внутри `nationality` показывает
  версию `2.92.0`, когда канон уже `3.22.2`. `ADR-004` («Модель раздачи базы», принят
  и **исполнен** 22.08.2026) прямо называет механизм: «зеркало `<репа>/_base/` — скрипт
  `deploy.sh --sync-base`». Флага `--sync-base` в `templates/deploy.sh` **нет** —
  `grep` не находит ни одного упоминания. `scripts/base_coverage.py --adr004` (тот самый
  измеритель, который ADR называет правилом 4) подтвердил: **51 репа** отстала на
  `2.92.0` при каноне `3.22.2` — `DRIFT`, не `CLEAN`.

- **Корень.** ADR был принят и **однократно исполнен вручную** («раздача сделана
  локально, `sync-base.sh` LOCAL=1») 22.08.2026, но постоянный механизм — то, что
  должно было запускаться на каждом деплое, — либо не был реализован при консолидации
  `deploy.sh`, либо был потерян при последующих правках. Гейт-измеритель
  (`base_coverage.py --adr004`) существует и работает, но ничто не вызывает его
  автоматически и ничто не чинит найденный `DRIFT` — измеритель без исполнителя
  (`21-revision-protocol.md` §4г: «правило без исполнителя есть обещание»).

- **Правило.** 🔴 **Принятый ADR — не факт о коде, а обещание, которое обязано быть
  проверено отдельно от факта принятия.** «Статус: ПРИНЯТ И ИСПОЛНЕН» в шапке документа
  описывает состояние на дату разбора, не гарантирует состояние сейчас. Перед тем как
  верить таблице механизмов ADR, прогнать его собственный измеритель (если он есть) —
  а если измеритель есть, но гейтом не встроен, считать ADR **исполненным один раз**,
  а не действующим постоянно.

- **Свой вклад.** Ревизионная кампания 25.08.2026 сама подняла версии `_base/`-зеркал
  ещё дальше от канона (17 реп протронуты) до того, как проверила `base_coverage.py`.
  Разрыв существовал и до этого, но не был измерен ни разу с 22.08.

- **Родня:** `ADR-004` (`reports/adr/adr_004_base_distribution_model.md`) ·
  `scripts/base_coverage.py --adr004` · `72-source-of-truth.md` §4б-бис («копия
  безопасна, когда у неё есть механизм обновления; без механизма любая пометка — просто
  вежливость») · `21-revision-protocol.md` §4г.

  🔴 **Найдено 26.08.2026 при ревизии `mission-control`:** решение старше, чем казалось.
  `mission-control/decisions/adr_008_base_sync_mechanism.md` (31.07.2026, за три недели
  до `ADR-004`) уже называет ровно те же два флага — `deploy.sh --sync-base` и
  `--collect-base`. Решение принималось **дважды, в двух репах, с разрывом в три недели**,
  и ни разу не дошло до кода. Обещание без исполнителя не портится со временем сильнее —
  оно просто накапливает повторные подтверждения, которые тоже не проверяются.
  Временный исполнитель — `scripts/sync_base_local.py` (base-repo), закрывает только
  «раздать вниз», не «собрать вверх» (`--collect-base` по-прежнему не реализован нигде).

- **Гейт:** да → `scripts/sync_base_local.py`, `scripts/base_coverage.py --adr004`


### PIT-142 — гейт ADR-004 §2 ловит копию канона по ИМЕНИ файла, а переименованная копия невидима (2026-08-26)

- **Симптом.** Ревизия `truth-seeking` нашла в его СОБСТВЕННОЙ `00-infrastructure/`
  (не в `_base/`) целый устаревший срез канона `base-repo` под старыми русскими именами:
  `watchlog.md` внутри которого дословно «# base-repo — Вахтенный журнал», `changelog.md`,
  весь `claude-context/` (личный контекст владельца, версия «июль 2026», 4 аккаунта —
  канон уже на 5, с Adam). `base_coverage.py --adr004` **не увидел ни одного нарушения**
  для этой репы. Системная проверка (`grep -rl "base-repo — Вахтенный журнал"`) нашла
  тот же паттерн ещё в **11 репах**: academic-portfolio, character-a-analysis,
  christ-walk, edu-base, family, health-vault, it-base, legal-knowledge-base,
  misc-vault, self-map, portrait-of-taste — из них восемь уже числились «отревизированы»
  в кампании синтеза 22–23.08.2026.

- **Корень.** `ADR-004` правило 2 матчит копию по **точному имени файла** («в каноне
  есть файл с таким именем?») плюс `f.name[:2].isdigit()` — фильтр на «похоже на
  документ канона» намеренно узкий, чтобы не ловить случайные совпадения с `README.md`.
  Но узость создала слепую зону: файл, переименованный при переезде канона на английские
  имена (`watchlog.md` → `WATCHLOG.md` регистр не считается, `kontekst-vasiliy-edinyy.md`
  → `context-vasilii-unified.md` вообще другое имя), для проверки — не документ канона
  вообще, а посторонний файл. Хеш **не проверяется**, потому что до сравнения хешей
  дело не доходит: фильтр по имени отсеивает раньше.

- **Правило.** 🔴 **Гейт, ищущий копию по имени, не ловит копию, у которой изменилось
  имя — а массовые переименования канона уже случались минимум дважды** (переход на
  английскую транслитерацию, до этого — реструктуризация 00-infrastructure). Проверка
  «это копия канона» обязана матчить **по содержанию** (маркерная строка вида
  «# base-repo —», сигнатура первого абзаца) как минимум вторым проходом, не только
  по имени — иначе переименование канона превращается в способ спрятать устаревшую
  копию от собственного гейта.

- **Свой вклад.** Ревизионная кампания 22–23.08.2026 закрыла манифесты для 8 из этих
  11 реп, не прогнав рекурсивный контентный поиск — механическая часть (гейт зелёный)
  была принята за содержательную (репа прочитана целиком). Тот же класс, что `PIT-138`.

- **Родня:** `PIT-138` (ревизия свелась к механике) · `PIT-141` (обещание без
  исполнителя) · `ADR-004` (`reports/adr/adr_004_base_distribution_model.md`) ·
  `scripts/base_coverage.py`. Чистка: `truth-seeking` — в этом же батче; остальные
  11 реп — `ROADMAP.md` §P0, отдельным пунктом, не молча внутри кампании манифестов.

- **Гейт:** TODO — `base_coverage.py --adr004` матчит копию канона по имени файла, слепая зона для переименованных копий описана в самой карточке как открытая; нужно решить, добавлять ли контентный второй проход (маркерная строка) — не сделано, не оценено за лимит времени в этом заходе


### PIT-143 — `bump_repo.py` завёл версию с нуля поверх репы, уже опубликованной на GitHub с v1.21.1 (2026-08-26)

- **Симптом.** `exam-kit` не имел локального `VERSION`-файла — `bump_repo.py` (читает
  `VERSION`, при отсутствии считает `0.0.0`) завёл счёт с нуля: `0.0.0 → 0.1.0 → 0.1.1`.
  Деплой отказался публиковать: на GitHub у репы уже стоял тег `v1.21.1` (21 релиз,
  июль 2026, собственный процесс «tree sync», отдельный от `deploy.sh` базы) — новая
  версия оказалась ниже опубликованной. Хуже: вахта **придумала объяснение** владельцу
  до проверки факта — «это два разных счётчика, версия репы и версия кита», — что было
  правдоподобно и неверно. Владелец поймал на живом прогоне деплоя.

- **Корень.** «Нет локального файла» и «репа не публиковалась» — разные утверждения,
  и код молчаливо приравнял одно к другому. У репы может быть богатая история публикации
  вне механизма, который сейчас проверяется (свой скрипт, ручные теги, другой аккаунт) —
  локальное отсутствие файла ничего не говорит об этом.

- **Правило.** 🔴 **Перед первым подъёмом версии для репы без локального `VERSION` —
  проверить факт публикации на GitHub** (`gh api repos/<owner>/<repo>/tags`), а не
  довериться умолчанию `0.0.0`. Признаки, что стоит проверить особо: у репы уже есть
  `CHANGELOG.md`/`WATCHLOG.md` с историей, собственный `docs/RELEASING.md` или похожий
  процесс, журнал ошибок/итераций — то есть репа выглядит зрелой при отсутствии ровно
  одного файла (`VERSION`). Второе правило — уже записанное `71` §5а («fail loud»),
  применённое к самому себе: **объяснение владельцу — тоже утверждение, которое обязано
  быть проверено перед тем, как быть произнесённым**, а не только внутренне
  непротиворечивым.

- **Свой вклад.** Это не первая версионная ошибка за сессию (см. `career`/
  `mathematics` перенос, `algorithms-site` дубль-архив) — но первая, где ошибка дошла
  до владельца как уверенное, неверное объяснение, а не как факт «не то сделал».

- **Родня:** `PIT-136` (WATCHLOG §0 отстал от VERSION) · `PIT-063` (даунгрейд рабочего
  деплойера) · `71-source-of-truth.md` §5а (fail loud) · `exam-kit/reports/
  revision-2026-08-26.md`.

- **Гейт:** да → `scripts/bump_repo.py` (проверка `gh api .../tags` перед подъёмом версии с нуля, комментарий цитирует PIT-143)


### PIT-144 — `pack_release.py` дважды снимал замок мёртвого процесса, крах ронял сборку архива молча (2026-08-26)

- **Симптом.** `edu-base-v0.3.0.zip` собрался с правдоподобным размером (41 МБ), но
  оказался битым (`zipfile.BadZipFile`, «нет оглавления»). Причина: более ранний прогон
  упаковщика был прерван (Bash-таймаут 2 минуты, процесс ушёл в фон, затем убит вручную)
  и оставил файл `*.zip.lock` с PID уже мёртвого процесса. Следующий запуск нашёл замок,
  верно определил, что процесс мёртв, снял его (`lock.unlink(missing_ok=True)`) — и тут
  же **упал** на безусловном `lock.unlink()` тремя строками ниже: тот же файл, второй
  раз, `FileNotFoundError`. Сборка архива до этой точки не доходила ни разу — оба «более
  ранних» архива на самом деле недописаны с самого первого прогона.

- **Корень.** Код был `if not alive: unlink / elif age<3600: exit`, а строки
  «замок старше часа — unlink» стояли **на том же уровне отступа, что и `if`**, а не
  внутри `else` — то есть выполнялись **после** любой ветки, включая ту, что уже
  сняла замок. Ветвление визуально читалось как три взаимоисключающих случая, а по
  отступам было «if/elif» + два действия, гарантированно исполняющихся всегда.

- **Правило.** 🔴 **Три взаимоисключающих случая — это `if/elif/else`, а не
  `if/elif` + код после блока.** Отступ — источник правды для интерпретатора, не
  визуальное соседство с похожим по смыслу `elif`. Каждый выход из проверки замка
  обязан быть **ровно одним** действием на **одном** уровне ветвления — проверялось
  бы юнит-тестом на все три случая (жив / мёртв / истёк час), тест написан не был.

- **Свой вклад.** Не только код: вахта дважды приняла правдоподобный размер файла
  (`ls -la` показывает 41 МБ) за готовность архива — та же ошибка, что уже описана
  `PIT-130` («готовность архива проверяется чтением оглавления, а не наличием файла»),
  повторена на своём же инструменте в этой же сессии.

- **Родня:** `PIT-130` (оборванный архив неотличим по имени/размеру) · `PIT-131`
  (параллельная упаковка рвёт архив — этот случай оказался НЕ параллельностью, а тем
  же классом «замок соврал о состоянии») · комментарий в самом коде (строки 121–129
  `pack_release.py`) уже описывал историю замка, но не покрывал этот путь тестом.

- **Гейт:** да → `scripts/pack_release.py` (замок собран как `if/elif/else` на три взаимоисключающих случая — жив/мёртв/старше часа — без двойного `unlink`)


### PIT-145 — порог заглушек iCloud (10%) ложно блокировал упаковку при почти полном диске (2026-08-26)

- **Симптом.** `pack_release.py` отказывался паковать `academic-portfolio` (11%),
  `it-base` (17%), `legal-knowledge-base` (41%), `misc-vault` (63%), настаивая на
  предварительной материализации. Последовательный и параллельный (`xargs -P8`) циклы
  `cat file > /dev/null` по всем заглушкам не снижали счётчик даже минимально:
  `academic-portfolio` был 273 → 260 → 259 → 250 (реальных, после фильтра `size>0`)
  за несколько проходов, `it-base` вообще не сдвинулся с 1590.

- **Корень.** Диск почти полон: `diskutil info /` → **свободно 9.0 ГБ** на весь APFS-
  контейнер. Прямая проверка одного «зависшего» файла: `wc -c < file` вернул содержимое
  за **0.008–0.014 с** (не сетевая задержка), но `st_blocks` после чтения остался `0`,
  и `os.stat().st_flags & SF_DATALESS` — истина **и до, и после** успешного чтения.
  Это означает: **macOS «Optimize Mac Storage» материализует файл на чтение и тут же
  вытесняет его обратно**, потому что почти нет свободного места — файл никогда не
  «оседает» локально настолько долго, чтобы `st_blocks` показал ненулевое значение.
  Порог 10% в `pack_release.py` был откалиброван на противоположный случай (`science`,
  23.08.2026: заглушки — это данные, которых ЛОКАЛЬНО НЕТ ВООБЩЕ, тянуть их — это
  реальные секунды-минуты на файл) и не различает «данных нет» от «данные есть, но
  диск их тут же смывает».

- **Правило.** 🔴 **`st_blocks == 0` — это «сейчас не резидентно», а не «недостижимо
  быстро».** Разница ловится не счётчиком блоков, а прямым замером: `time (wc -c < f)`
  на образце файлов. Если чтение быстрое (десятки мс на файл среднего размера) —
  упаковка (читает-и-сразу-пишет в zip) отработает штатно, вытеснение уже записанного
  байта архиву не вредит; порог просто описывает не тот сценарий.

- **Решение.** Добавлен флаг `--force-stubs` — превращает отказ (`sys.exit(1)`) в
  предупреждение и продолжение сборки. Не обходит проверку размера файлов >100 МБ
  (GH001) — та защита не связана с этим классом ложной тревоги.

- **Свой вклад.** Три прохода материализации (последовательный, параллельный, ручной
  тест на трёх файлах) потрачены до того, как проверил `df -h` — дешёвая команда,
  которая сразу назвала бы причину. Симптом («счётчик не двигается») наводил на
  «скрипт не работает», а не на «диск полон» — стоило проверить ресурс раньше метода.

- **Родня:** `PIT-135` (заглушки iCloud вообще, порог введён здесь), `PIT-144`
  (тот же день, тот же скрипт, соседняя часть сборки архива).

- **Гейт:** да → `scripts/pack_release.py` (`--force-stubs`, комментарий цитирует PIT-145)


### PIT-146 — `pack_release.py` не проверял суммарный вес репы против норматива 500 МБ (2026-08-26)

- **Симптом.** Владелец нашёл при деплое: `academic-portfolio` ушла в архив на
  **1018 МБ** (репа 1052 МБ), `misc-vault` — на **674 МБ** (репа 725 МБ). Оба выше
  жёсткого потолка **500 МБ**, задокументированного в `01-repo-standard.md` §4 и
  `06-volume-compression.md` — «выше Claude физически не берёт архив целиком».
  Прямая реакция: *«какой нахер гигабайт???? такое нельзя пушить»*.

- **Корень.** `pack_release.py` с самого начала проверял только **поштучный** размер
  файла (>100 МБ — GH001) и долю iCloud-заглушек. Суммарный вес всей репы не
  проверялся никогда — ни при первой сборке этих архивов, ни при повторных.
  Порог 500 МБ существовал только как текстовое правило в документации, не как код.

- **Правило.** 🔴 **Задокументированный норматив без гейта в коде — не норматив,
  а пожелание.** Три из трёх мест, где норматив 500/100/50 МБ был **записан**
  (`01-repo-standard.md`, `06-volume-compression.md`, `05-knowledge-base-rules.md`),
  и ноль мест, где он **проверялся** — до этой правки. `pack_release.py` уже проверял
  два других явных лимита (файл >100 МБ, доля заглушек) — общий вес был очевидным
  пробелом того же класса, найденным не ревизией, а тем, что реальный архив раздулся.

- **Решение.** `pack_release.py` получил трёхуровневую проверку суммарного веса
  (`_total_mb`): 🟢 ≤50 МБ тихо · 🟡 100 МБ мягкое предупреждение (пакует) ·
  🟡 500 МБ ещё мягче с обязательным `.size-exception` в корне репы (обоснование
  одной строкой, образец — `portrait-of-taste`: там вес оправдан — нужны сами
  фотографии-эталоны, не мусор) · 🔴 >500 МБ без `.size-exception` — жёсткий блок.
  Тот же норматив продублирован в `git_limits_check.py` для системной сверки по
  всем репам разом (независимая проверка лимитов GitHub — другого класса: 100 МБ
  файл / 5 ГБ репа — не подменяет наш норматив, а идёт рядом).

- **Починка находки.** Обе репы разобраны через `07-media-to-text-lab/`:
  `academic-portfolio` — 845 МБ (118 файлов, презентации защит сокурсников,
  невоспроизводимо) вынесено `heavy_media_to_note.py --limit-mb 2`, вес 1052→204 МБ.
  `misc-vault` — 5 публичных книг переведены в выжимку и удалены (воспроизводимо),
  609 МБ личных файлов (дневник, VK-переписка, экспорты) вынесено тем же
  инструментом, вес 725→96 МБ. По ходу найден и почищен тот же баг PIT-145
  (`st_blocks==0` под давлением диска) ещё в двух инструментах лабы —
  `docs_to_md.py` и `heavy_media_to_note.py` — оба получили `--force-stubs`.

- **Родня:** `PIT-145` (тот же диск, тот же день — заглушки блокировали сами
  инструменты чистки, пока их чинили), `01-repo-standard.md` §4, `06-volume-
  compression.md` (источник норматива, который здесь наконец стал кодом).

- **Гейт:** да → `scripts/pack_release.py`, `scripts/git_limits_check.py` (норматив 500 МБ стал кодом, `_total_mb`/`OUR_HARD`)


### PIT-147 — `base-repo` сам оказался целью раздачи `_base/`: `VERSION` пропал, появились чужеродные `BASE_VERSION`/`DO-NOT-EDIT.md` (2026-08-26)

- **Симптом.** В корне `base-repo` обнаружены: отсутствующий `VERSION` (обязательный
  файл), и два файла, которые ставит **только** раздача `_base/` в РЕПАХ-ПОЛУЧАТЕЛЯХ:
  `BASE_VERSION` (7 байт, `3.37.0`, `mtime` 13:44) и `DO-NOT-EDIT.md` (366 байт,
  дословно шаблонный текст `sync-base.sh LOCAL=1`, `mtime` 14:30). Разные `mtime` —
  два отдельных события, не одна операция.

- **Корень — установлен НЕ полностью, честно.** Прямая проверка гейта `revision_check.py`
  подтвердила: до находки `WATCHLOG §0` совпадал с `VERSION` (файл существовал и был
  цел) — то есть порча случилась **между** этим прогоном гейта и следующей проверкой,
  в текущей сессии, но раньше моих собственных недавних команд (эмпирически
  проверено — свежий тестовый прогон `sync-base.sh` с текущим кодом корректно
  пропускает `base-repo` по имени, и вывод недавнего реального прогона `LOCAL=1` не
  содержал строки `base-repo` вовсе). `git`-истории у `base-repo` нет (единственный
  канон — рабочее дерево), `tmutil listlocalsnapshots` не дал полезных снапшотов —
  восстановить точную команду, вызвавшую порчу, нечем.
  Найден один **реальный, воспроизводимый** дефект того же класса: `sync_base_local.py`
  (узкий инструмент, добавленный в этой же сессии как обходной путь `PIT-141`) не имел
  НИКАКОЙ защиты от вызова с аргументом `base-repo` — в отличие от `sync-base.sh`,
  где защита (`[ "$n" = "base-repo" ] && continue`) стояла с самого начала. Одиночный
  вызов `sync_base_local.py base-repo` пишет `_base/BASE_VERSION` и
  `_base/DO-NOT-EDIT.md` внутрь **вложенной** папки `_base/` — не объясняет пропажу
  `VERSION` из корня напрямую, но это единственный найденный в системе код, способный
  вообще произвести эти два файла с таким содержимым, и наиболее вероятный источник
  цепочки (возможная последующая «уборка» вложенной само-копии как «утечки канона»
  того же вида, что чистила кампания `PIT-142` в тот же день — не доказано, лишь
  правдоподобно по совпадению паттерна и дня). **Косвенное подтверждение** нашлось
  в собственном `CHANGELOG.md` записи `[3.37.0]`, написанной ЭТОЙ ЖЕ сессией
  раньше: «Попутно `sync_base_local.py --all` роздал текущий канон... всей
  системе» — то есть `--all` действительно гонялся широко в тот же день; чтобы
  задеть `base-repo`, его фильтр (`(d/"_base").is_dir()`) требует, чтобы `_base/`
  у `base-repo` уже существовал к тому моменту — то есть более ранний одиночный
  вызов `sync_base_local.py base-repo` уже должен был случиться ДО этого `--all`
  прогона. Цепочка правдоподобна целиком, но каждое звено по отдельности
  доказано, а не всё вместе одной командой.

- **Оценка ущерба — ниже, чем казалось на первый взгляд.** Первая проверка ошибочно
  сочла пропавшим и `WATCHLOG.md` — ложная тревога: у `base-repo` он на момент находки
  лежал в `00-infrastructure/WATCHLOG.md`, а не в корне (несовпадение с `mission-control`,
  где `WATCHLOG.md` в корне) — искал не по тому пути. Прогон `LOCAL=1 sync-base.sh`,
  сделанный ДО находки, скопировал испорченное состояние `base-repo` в `_base/` 50 репам —
  но конечный результат для них оказался **идентичен корректному**: скрипт сам
  перезаписывает `_base/DO-NOT-EDIT.md` своим шаблоном ПОСЛЕ копирования, а
  `_base/BASE_VERSION` получает то же значение `3.37.0` что при штатном
  переименовании `VERSION → BASE_VERSION` — пропажа не размножилась вредом.

- **Решение — три части.**
  1. `sync_base_local.py` получил защиту: `--repo base-repo` завершается ошибкой,
     `--all` явно исключает `base-repo` по имени (не только по признаку «нет `_base/`»).
  2. Корень `base-repo` восстановлен: `VERSION` = `3.37.0` (сверено с `BASE_VERSION`,
     с архивом `base-repo-v3.37.0.zip` 13:44, с `WATCHLOG §0`), чужеродные
     `BASE_VERSION`/`DO-NOT-EDIT.md` удалены.
  3. 🔴 **Решение владельца 26.08.2026, дословно: «вотчлог всегда в корне у всех
     лежать должен».** `WATCHLOG.md` перенесён из `00-infrastructure/` в корень —
     единый путь для всех репозиториев системы (совпадает с `mission-control` и
     остальными сателлитами). Обновлены все функциональные ссылки на путь:
     `revision_check.py`, `close_batch.py`, `deploy_version_gate.py`, `capture.py`,
     плюс документация (`README.md`, `START-HERE.md`, `ROADMAP.md`,
     `03-watchlog-template.md`, `04-watchlog-protocol.md` и др.).

- **Уточнение ущерба 2026-08-26 (позже в тот же батч).** Первый проход сверял только
  `VERSION` и `WATCHLOG.md` — пропустил `.repo-id` (тоже отсутствовал в корне, тоже
  часть обязательного минимума). Нашёл не человек, а `pack_release.py`, отказавшись
  собирать архив без него. Полный `diff` списка корневых файлов против последнего
  целого архива (13:44) подтвердил: кроме `.repo-id`, других потерь нет. Урок
  зафиксирован отдельно — `PIT-G`, случай 8: после находки пропажи сверять **весь**
  список обязательных файлов разом, не чинить по одному, пока не поймает гейт.

- **Что осталось открытым.** Точная команда/момент порчи не восстановлены — нет
  журнала инструмента (`auto_log.py` появился позже в этой же сессии) и нет `git`
  для `base-repo`. Урок общий: узкий инструмент, добавленный «на скорую руку» рядом
  со штатным (`sync_base_local.py` рядом с `sync-base.sh`), обязан копировать
  защиты штатного один-в-один в момент создания, а не только основную логику —
  разница между «работает в 99% случаев» и «работает всегда» здесь и есть источник
  инцидента.

- **Родня:** `PIT-141` (породил `sync_base_local.py` как обходной путь),
  `PIT-142` (кампания чистки того же дня, вероятный, но не доказанный, соседний
  триггер), `09-base-sync.md` §4а (порог раздачи, введённый этим же батчем).

- **Гейт:** да → `scripts/sync_base_local.py` (`--repo base-repo` завершается ошибкой, `--all` явно исключает base-repo по имени)


### PIT-148 — два гейта проверяли WATCHLOG §0 base-repo по двум разным паттернам, оба «зелёные» порознь (2026-08-26)

- **Симптом.** Владелец прогнал канонический `deploy.sh` (v4.20.1) на 8 версиях
  base-repo (v3.43.0…v3.47.0) — все опубликованы успешно, но каждая дала
  предупреждение «в WATCHLOG.md нет строки «**Версия:** X.Y.Z» — §0 не читается
  машиной». Реакция владельца: *«ты опять обосрался с базовыми гейтами архивов»*.

- **Корень.** `base-repo/WATCHLOG.md` весь этот сеанс вёл §0 строкой
  `**Текущая точка: vX.Y.Z**` — формат, под который написан **свой** гейт,
  `scripts/revision_check.py` (`re.findall(r"[Тт]екущая точка:\s*\**v?(\d+\.\d+\.\d+)")`).
  Канонический `templates/deploy.sh` (PIT-018, тот же паттерн действует для
  `mission-control` и остальных репов системы) ищет **другую** фразу:
  `\*\*Версия:\*\*\s*X.Y.Z`. Оба гейта прогонялись весь батч — `revision_check.py`
  каждый раз давал CLEAN, `deploy.sh` каждый раз молча копил предупреждение,
  потому что смотрел не в ту фразу. **Ни один прогон `revision_check.py` не мог
  поймать это в принципе** — он проверял свой же паттерн, который сам и требовал.

- **Правило.** 🔴 **Два гейта, проверяющих один файл на одно и то же требование
  разными словами, — это не двойная защита, а гарантированная слепая зона.**
  Зелёный свет от одного не говорит ничего о другом. Симптом того же класса,
  что расхождение версии в README/WATCHLOG/VERSION (`PIT-116`) — только здесь
  разошлись не числа, а сам формат строки, которую разбирают машиной.

- **Решение.** Строка §0 переписана так, чтобы содержать **обе** фразы разом:
  `**Версия:** X.Y.Z · **Дата:** ... · **Вахта:** ... (текущая точка: vX.Y.Z)`.
  Проверено вручную обоими regex — оба извлекают `3.47.0` из одной и той же
  строки. `revision_check.py` не тронут (риск сломать рабочий гейт выше пользы
  от унификации кода); унификация сделана на уровне данных (сама строка), не кода.

- **Что осталось открытым.** Формат `**Версия:**` — системный канон (используется
  `mission-control` и, вероятно, большинством репов с `docs/WATCHLOG.md`); `base-repo`
  был единственным исключением. Стоило бы когда-нибудь свести `revision_check.py`
  на тот же паттерн и убрать «текущая точка» как отдельную фразу — не сделано
  сейчас, чтобы не трогать проверенный код ради косметики в разгар долгой сессии.

- **Родня:** `PIT-018` (deploy.sh, зачем гейт вообще завели), `PIT-094`
  (revision_check.py, тот же класс — §0 отстаёт от VERSION), `PIT-116`
  (README/WATCHLOG/VERSION расходятся числом, здесь — форматом).

- **Гейт:** да → `scripts/close_batch.py` (WATCHLOG §0 пишется строкой, содержащей обе фразы разом — «Версия:» и «текущая точка:» — комментарий цитирует PIT-148)


### PIT-149 — Claude Code зависает на входе в `personal-finance-dss`: `git status` не завершается, потому что 72% отслеживаемых файлов выселены iCloud (2026-08-26)

- **Симптом.** Владелец: *«с ней какие-то проблемы у Claude Code, чтобы продолжать
  работу там — просто зависает, не показывает папку»*. Скриншоты: новая сессия
  в `~/Documents/система_репозиториев/personal-finance-dss` висит на «Zesting…»
  4–5 минут **без единого вызова инструмента** — заметно только эту репу, соседние
  сессии (`mission-control`, `character-a-analysis`, `portrait-of-taste`) в это же
  время работают штатно.

- **Корень, установлен последовательными проверками.**
  1. `.claude/hooks/session-context.sh` (`SessionStart`) вызывает
     `git status --porcelain | wc -l` первой содержательной командой — это
     единственный код, который выполняется ДО первого ответа модели. Зависание
     здесь блокирует сессию целиком, инструментов ещё не было.
  2. `git status --porcelain` в этой репе не завершался — ни за 2 минуты, ни после
     повторной попытки. `git ls-files` (1484 отслеживаемых) и
     `git ls-files -o --exclude-standard` (46 неотслеживаемых) при этом отработали
     штатно за секунды — зависает именно полное сравнение рабочего дерева с
     индексом, которое при несовпадении mtime перечитывает СОДЕРЖИМОЕ файла.
  3. Проверка `stat -f '%b %z'` по всем отслеживаемым файлам: **1068 из 1484 (72%)**
     имеют `blocks=0` при ненулевом размере — признак APFS-заглушки iCloud
     («dataless file», содержимое не на диске). `brctl status` в это же время
     показывал для соседней репы (`control-panel/.venv`) реальную ошибку
     `CKErrorDomain:25 — Quota exceeded`: у аккаунта iCloud исчерпана квота,
     синхронизация в целом нездорова, а не просто медленная. `df -H /` — свободно
     15 ГБ из 251 (6%): низкое место на диске — вероятный триггер, из-за которого
     macOS «Optimize Mac Storage» агрессивно выселил именно эту репу.
  4. `brctl download` на всю репу запущен явно — за 25+ секунд материализовал
     заглушки не сдвинулся (1068 → 1057): скачивание само стоит в очереди за тем
     же исчерпанным аккаунтом, форсировать локально нечем.
  5. Отдельно найден и снят **чужой by-product**: `.git/index.lock` (0 байт,
     создан 17:54, ни один процесс не держит `lsof`) — след прежней зависшей
     `git status`, убитой перезапуском чата. Лок был не первопричиной (после его
     удаления `git status` продолжил висеть), но усугублял: он гарантированно
     проваливал любую параллельную git-операцию, пока не убран.

- **Правило.** 🔴 **`SessionStart`/`Stop`-хук, вызывающий `git status` (не
  `ls-files`), в репе под активной iCloud-синхронизацией — это точка, где сама
  синхронизация может заблокировать вход в сессию на неопределённое время**, а не
  секунды. Симптом неотличим от «Claude Code завис»: спиннер идёт, вызовов
  инструментов нет, потому что блокировка происходит до первого ответа модели.
  Тот же класс, что `PIT-135` («операция, читающая содержимое, в облачном
  каталоге уходит в сеть»), но здесь эскалация: не просто медленно — аккаунт
  реально в состоянии `Quota exceeded`, и `brctl download` не может это обойти.

- **Решение (частичное, часть — на владельце).**
  1. Стал stray-лок `.git/index.lock` — удалён, `git status` в других репах не
     страдал (дифференциальный тест: `mission-control` в той же ветке диска
     отработал за <8с).
  2. `brctl download` запущен на всю репу — материализация файлов пойдёт, когда
     разрешится квота аккаунта; сама команда это не чинит.
  3. **На владельце** (нет доступа отсюда): освободить квоту iCloud (сейчас
     `Quota exceeded`) и/или локальное место на диске (15 ГБ свободно из 251),
     и/или исключить рабочие репы из iCloud-синхронизации целиком — то же
     системное решение, что уже намечено `PIT-135`/`53-infrastructure-sync-standard.md`
     для каталогов под активной работой. Записано в `personal-finance-dss/TASKS.md`.

- 🔴 **Эскалация в тот же батч: проблема системная, не одной репы.** Выборочная
  проверка `base-repo` (репа, активно используемая весь текущий сеанс) дала
  **200/456 (44%) выселенных файлов** тем же тестом (`blocks=0`, `size>0`).
  `scripts/revision_check.py` (гейт закрытия батча) сам завис на 3+ минуты без
  вывода в этой же репе, дважды подряд (фон `bdes4mlqz`/`b31h1l0be`, оба убиты).
  **Вывод пересмотрен:** это не «давно не открывавшаяся репа», а состояние
  аккаунта iCloud целиком — `personal-finance-dss` просто попалась первой.
  Мандат «закрыть батч гейтом+архивом» **не выполним прямо сейчас** на этой
  машине, пока не разрешена квота/место, — не скилл-проблема, а внешний ресурс.

- **Что осталось открытым.** Почему именно `personal-finance-dss` пострадала
  заметнее (72%) `base-repo` (44% в выборке) — не доказано; предположение
  «давно не открывалась Finder-ом / не в горячем кеше индексатора» не проверялось
  напрямую и, судя по цифре 44% для активно используемой репы, объясняет не всё.

- **Родня:** `PIT-135` (облачный каталог + операция, читающая содержимое, тот же
  механизм на уровень ниже — там ещё без «Quota exceeded»), `PIT-131`
  (лок-файл как класс проблемы, там — упаковщик, здесь — git index).

- 🔴 **Побочная находка при уборке (та же дата).** Диагностика зависших
  `git status`-процессов чинилась через `pkill -f "git status --porcelain"` —
  паттерн ловит по подстроке **все** процессы в системе, не только свои. В момент
  правки на репе одновременно работали ещё несколько сессий владельца (`ListAgents`
  показывал их busy) — потенциально мог убить чужой легитимный `git status` одной
  из них. Совпадения по времени с последующей жалобой «пропали агенты
  design-critic/a11y-auditor» в параллельной сессии не нашлось (разные подсистемы:
  OS-процесс vs внутренний реестр агентов харнесса), но правило на будущее:
  **в дереве с несколькими одновременными сессиями `pkill -f` — по PID своего же
  `Bash`-вызова (`kill "$(cat pidfile)"`), не по шаблону командной строки.**

- **Гейт:** нет — состояние диска/аккаунта iCloud конкретной машины (квота, свободное место) — не файл репы; смежный класс заглушек уже гейтится PIT-120/PIT-135, но само зависание git/hook в моменте — состояние ОС, не файла


### PIT-150 — живой progress-bar (curl/tqdm/деплой со скачиванием) не рендерится в Claude Code, и это не баг, а архитектура (2026-08-26)

- **Симптом.** Владелец просил показать в интерфейсе Claude Code живой визуальный
  прогресс (полоску, обновляющуюся на месте) для долгих операций — деплоя со
  скачиванием, `pytest` в `personal-finance-dss`. В прошлой сессии модель
  утверждала, что «сделала» такую интеграцию — по факту ничего видно не было.
  Ошибочное заявление о готовности не было проверено на практике.

- **Корень, подтверждено ресёрчем по официальной документации Claude Code.**
  Bash-инструмент не подключён к настоящему терминалу (PTY): вывод команды
  **пишется в файл** и читается оттуда асинхронно, а не рендерится напрямую на
  экран посимвольно. Прогресс-бары держатся на `\r` (возврат каретки, «стереть
  строку и написать заново») — в захваченном файле `\r` это просто ещё один
  байт текста, не команда терминалу. Результат — не полоска, а лавина строк
  вида `[50%\r[75%\r[100%`. Режим shell (`!`) — та же история: быстрее
  стримит текст, но тоже не PTY, `\r` всё равно не «работает».

- **Правило.** 🔴 **Живой, обновляющийся на месте прогресс в Claude Code
  невозможен в принципе при текущей архитектуре** — это не то, что «ещё не
  реализовали», а прямое следствие захвата-в-файл вместо PTY-моста. Просить
  «интегрировать механику прогресса» бессмысленно: чинить нечего, интегрировать
  неоткуда — сам класс решения не работает через этот канал.

- **Решение — рабочая замена, не имитация.** Инструменты, печатающие статус
  **построчно** (каждая строка — новая, не поверх старой), читаются нормально:
  `pytest -v` вместо `pytest-sugar`/цветных полосок; собственные скрипты —
  `echo "шаг N: ..."` на новую строку, а не `printf '\r...'`. Для реально долгих
  фоновых операций — `run_in_background` + периодический `Read` лог-файла
  (штатный паттерн этой же сессии: перенос ~60 репозиториев чекался именно так).

- **Что осталось открытым.** Не проверено, меняется ли это в более новых версиях
  CLI намеренно (например, официальный TUI-режим с полноценным PTY) — на дату
  находки такого способа не найдено ни в документации, ни в открытых issue.

- **Родня:** нет прямой — новый класс, отдельный от файловых/сетевых PIT.

---
- **Гейт:** нет — архитектурное ограничение Claude Code CLI (Bash без PTY-моста) — свойство инструмента-хоста, не файл репы


### PIT-151 — `tests/bin/gh` (заглушка `gh` для тестового стенда) потерял бит `+x`, весь `test_deploy.sh` развалился на 130 провалов (2026-08-27)

- **Симптом.** `zsh tests/test_deploy.sh` дал 130 провалов почти по всем зонам
  (A/B/C/D/E/F/G/H), начиная с самого первого кейса A1: «репа создана» — нет,
  первая же строка вывода — `To get started with GitHub CLI, please run: gh auth
  login`. Выглядело как масштабная регрессия деплойера от только что внесённой
  правки (`http.lowSpeedLimit`/`Time`, v4.21.0).

- **Корень.** `tests/bin/gh` — файл-заглушка, которую `PATH="$HERE/bin:$PATH"`
  должен подставить вместо настоящего `gh` на время теста — лежал с правами
  `-rw-r--r--` (без `+x`). Шелл при резолюции команды в `PATH` **пропускает
  неисполняемый файл молча** и идёт дальше по `PATH` — тесты дёргали настоящий
  `/usr/local/bin/gh`, неавторизованный в песочнице, и любое действие,
  зависящее от `gh`, отказывало с реалистичным на вид, но нерелевантным
  сообщением. Правка деплойера была ни при чём — при откате изменений в
  `deploy.sh` эффект тот же.

- **Точный источник потери бита не подтверждён** — не диагностировалось целевым
  экспериментом в этой сессии. Правдоподобные кандидаты: раунд-трип через zip
  (не каждый архиватор/распаковщик сохраняет unix-права — тот же пайплайн синка
  реп уже давал ложные NFD/`.DS_Store`-артефакты другого класса), либо разовое
  касание файла редактором/бэкап-инструментом. **Не выдавать это за доказанную
  причину** без отдельной проверки.

- **Диагностика.** `type gh` / `which -a gh` показали, что резолвится системный
  `gh`, а не стендовый — при том что `tests/bin/gh` физически существует и
  первым в `PATH`. `ls -la tests/bin/gh` сразу показал отсутствие `x`.

- **Починка.** `chmod +x tests/bin/gh`, затем `find tests/bin -type f !
  -perm -u+x` — убедиться, что не задет ни один другой стаб. После починки
  прогон снова зелёный.

- **Правило.** Массовый провал тестового стенда **сразу по всем зонам** и
  **с первого кейса** — сигнал не «код сломан», а «стенд сломан»: прежде чем
  разбирать провалы по существу, проверить `ls -la tests/bin/*` (права
  исполнения на все стабы) и что резолвится именно стендовый бинарник
  (`type gh`), а не системный.

- **Родня:** нет PIT-номера — новый класс, отдельный от файловых/сетевых PIT.

- **Повтор 27.08.2026, тот же день.** `.claude/hooks/protect-base-mirror.sh` и
  `.claude/hooks/ritual-gate.sh` найдены с теми же правами `-rw-r--r--` (оба
  давали «Permission denied» при срабатывании хука — замечено раньше в сессии,
  но не диагностировано сразу). Три независимых файла с одинаковой потерей `+x`
  в один день **усиливает** гипотезу «раунд-трип через zip» — маловероятно,
  что три разных файла задел один и тот же редактор в один момент, а вот общий
  архивный пайплайн (упаковка/распаковка при синке) правдоподобнее задевает
  все исполняемые файлы разом. **Всё ещё не подтверждено экспериментом** — но
  ещё не выдавать за доказанное; вывод, который можно взять из повтора уже
  сейчас: если один `+x`-файл потерял бит, стоит сразу проверить `find <repo>
  -type f -perm -u+x` на весь `.claude/hooks/` и `tests/bin/`, а не только на
  файл, который сломался первым.

---
- **Гейт:** да → `scripts/revision_check.py::check_exec_bits` (новая проверка `.claude/hooks/*.sh`, `.githooks/*`, `tests/bin/*` на бит `+x`, с канарейкой `selftest_exec_bits`)


### PIT-152 — `pack_release.py` пакует WATCHLOG.md репы В МОМЕНТ вызова: правка WATCHLOG после упаковки требует переупаковки (2026-08-27)

- **Симптом.** `academic-portfolio` v0.2.11: `python3 scripts/pack_release.py`
  собрал архив, `deploy.sh` заблокировал публикацию — «WATCHLOG §0 стоит на
  0.2.10, публикуется 0.2.11». Странно: WATCHLOG.md к этому моменту уже стоял
  на правильной версии в рабочей копии. `REPAIR=1 zsh ~/Downloads/deploy.sh`
  тоже не помог — «изменений не потребовалось, всё уже по стандарту» (прошёл
  по старым тегам, не по новому архиву).

- **Корень.** Порядок действий был перепутан: сначала правился `VERSION` и
  `CHANGELOG.md`, сразу вызывался `pack_release.py` (архив зафиксировал
  WATCHLOG.md **на тот момент** — ещё со старой версией в §0), и только
  **потом** правился `WATCHLOG.md`. `pack_release.py` не перечитывает файлы
  заново при следующем деплое — он уже зазипован. Правка WATCHLOG.md после
  упаковки меняет только рабочую копию на диске, а не архив в `~/Downloads`,
  который деплоер разворачивает и проверяет.

- **Диагностика, которая должна была случиться раньше.** Гейт деплоера прямо
  печатает нужную последовательность в подсказке при первом отказе, но
  `REPAIR=1` было опробовано вместо перепаковки — `REPAIR` чинит существующие
  релизы по существующим тегам, а тега для v0.2.11 ещё не было (коммит
  заблокирован тем же гейтом на первом же прогоне) — второй заход впустую.

- **Починка.** `rm ~/Downloads/<repo>-vX.Y.Z.zip` (стал стейлым) →
  `python3 scripts/pack_release.py <repo>` заново, теперь уже с исправленным
  WATCHLOG.md → обычный (не `REPAIR`) прогон `deploy.sh`.

- **Правило.** Обязательный порядок при подъёме версии любой репы:
  1. `VERSION`
  2. `WATCHLOG.md` §0 (и заголовочная строка «Версия: X.Y.Z»)
  3. `CHANGELOG.md`
  4. **только теперь** `pack_release.py`

  Если WATCHLOG правится после упаковки — архив стейлый, гейт деплоя это
  поймает (что хорошо: молчаливой публикации с рассинхроном не будет), но
  чинится это **переупаковкой**, не `REPAIR=1` и не повторным обычным
  прогоном без переупаковки.

- **Родня:** `PIT-148` (два гейта проверяли WATCHLOG §0 по разным паттернам) —
  тот же класс объекта (WATCHLOG §0 как машиночитаемая точка входа), другая
  грань отказа.

- **Гейт:** да → `scripts/pack_release.py` (проверка версии WATCHLOG §0 против VERSION ДО упаковки — отказывает раньше, чем deploy.sh поймает то же самое после публикации)

### PIT-153 — гейты, которые есть только у `base-repo`, молча не запускаются для остальных реп: README STATUS и WATCHLOG §0 расползлись за 10 батчей подряд (2026-08-28)

- **Симптом.** `mission-control` v1.24.0 → v1.35.0 (10 батчей за один заход):
  `README.md` `<!-- STATUS -->` всё это время называл `v1.24.0`, `WATCHLOG.md`
  §0 снова дорос с 46 до 92 строк при потолке 80 (PIT-116) — это ПОВТОРНОЕ
  срабатывание того же дефекта, который уже чинился раньше в этом же заходе.

- **Корень.** У `base-repo` есть `scripts/close_batch.py` — он вызывает
  `revision_check.py` до и после правок, поднимает `VERSION`, режет `WATCHLOG`
  §3 до 10 записей и **обновляет README STATUS** одной командой. У остальных
  57 реп такого скрипта нет — ритуал закрытия батча делается вручную, шаг за
  шагом, и README-строка — тот шаг, который проще всего пропустить: он не
  ломает сборку архива, не роняет `pack_release.py`, просто тихо врёт
  дальше. `revision_check.py --root <репа>` эту находку **ловит** («README.md
  без блока `<!-- STATUS -->`» / отставание версии), но гейт не запускался
  после каждого мелкого батча — только когда PIT-116 накопился заметно.

- **То, что не пришлось изобретать.** `scripts/readme_status_gate.py` уже
  существует, принимает `--root` и уже умеет проверять/чинить (`--fix`) README
  STATUS для **любой** репы, не только `base-repo` — он просто не был вызван
  для `mission-control` ни разу за все 13 её батчей в этом заходе. Инструмент
  был готов, привычки его звать не было.

- **Починка.** README STATUS обновлён вручную до факта; `WATCHLOG.md` §0
  сжат повторно (батчи 19–30 свёрнуты в заголовки).

- **Правило.** Для любой репы без `close_batch.py` — после **каждого** батча,
  не только когда цифра бросается в глаза:
  ```
  python3 base-repo/scripts/revision_check.py --root <репа>
  python3 base-repo/scripts/readme_status_gate.py --root <репа> --fix
  ```
  Мелкий батч не повод пропустить проверку — именно серия мелких батчей
  подряд и есть сценарий, в котором дрейф незаметно копится.

- **Родня:** `PIT-116` (§0 разрастается за пределы точки входа) — это его
  повторное срабатывание в той же репе за тот же заход, что и первое
  исправление; урок в том, что «починил один раз» не значит «защищено
  навсегда» без гейта, встроенного в сам ритуал.

- **Гейт:** да → `scripts/close_batch.py --root` (обобщено 28.08.2026 в этом
  же заходе: раньше умел закрывать батч только `base-repo`, теперь принимает
  путь любой репы и прогоняет весь ритуал — `revision_check.py` →
  `readme_status_gate.py --fix` → `WATCHLOG` §0/§3 → `pack_release.py` —
  одной командой, для любой репы, у которой гейт даёт `CLEAN`).

### PIT-154 — три проверки `revision_check.py` были откалиброваны только на саму `base-repo`: под `_base/` те же файлы лежат по другому пути, и гейт врал ЧУЖОЙ репе, а не своей (2026-08-28)

- 🔴 **Команда ниже устарела, запись не переписана намеренно.** Найдено
  29.08.2026 проверкой `examples_check.py`: у `close_batch.py` сегодня нет
  ни `--root` (репа передаётся позиционно), ни `--dry` (есть `--dry-run`).
  Сегодняшний вид: `python3 scripts/close_batch.py mission-control --dry-run`.
  **Историю не правим — дополняем**: карточка свидетельствует о том, что было,
  а не о том, как звать скрипт сейчас.
  <!-- examples-check: историческая команда, проверке не предъявляется -->
- **Симптом.** `python3 scripts/close_batch.py --root ../mission-control --dry`
  (тест генерализации на `--root`, `PIT-153`) отказал по `DRIFT`, хотя
  mission-control никто не трогал: гейт нашёл «дублирующиеся номера разделов»
  в `_base/00-infrastructure/sborka-vse.md` и «протухшие исключения» в
  `SECTION_DUPES_ALLOWLIST` — притом что оба этих файла и есть друг другу
  ответ: allowlist прямо называет `sborka-vse.md` осознанным исключением.
  Прогон `revision_check.py --root` на трёх случайных репах (`mission-control`,
  `truth-seeking`, `vk-graph`) показал: **у каждой** DRIFT c одинаковым
  профилем находок — не совпадение, системная дыра.

- **Корень — один и тот же паттерн трижды.** Три проверки построены и
  протестированы только на `base-repo`, где эти файлы лежат в корне
  (`00-infrastructure/sborka-vse.md`). Но `_base/` — раздаточный слой поверх
  канона: та же база копируется в `<репа>/_base/00-infrastructure/sborka-vse.md`
  во все 57 приватных реп (`09-base-sync.md`). Три места узнавали файл только
  по голому пути, без варианта «плюс префикс `_base/`»:
  1. `SECTION_DUPES_ALLOWLIST` (проверка `rel_posix in ALLOWLIST`) —
     `_base/00-infrastructure/sborka-vse.md` не совпадал с
     `00-infrastructure/sborka-vse.md` дословно.
  2. `LINK_SKIP_DIRS = ("templates",)` (проверка `rel.parts[0] in LINK_SKIP_DIRS`) —
     под `_base/` первый компонент пути — `_base`, а не `templates`.
  3. `check_allowlist_rot` — искал `root / rel`, не пробуя `root / "_base" / rel"`,
     и объявлял исключение протухшим в любой репе, где файл лежит не в корне.

  Побочный, более тонкий случай той же природы: `_base/README.md` содержит
  живую в base-repo ссылку `./VERSION`, но раздача `sync-base.sh` переименовывает
  файл в `BASE_VERSION` (чтобы не путать с `VERSION` самой репы-хозяина) — ссылка
  становится осознанно битой именно как следствие раздачи, а не порчей контента.

- **Почему не поймали раньше.** `revision_check.py` тестировался и запускался
  почти исключительно **без `--root`** (то есть на самой базе) — единственной
  репе в системе, где `_base/`-слоя не существует по определению (она сама
  канон). `--root` для остальных 57 реп существовал с самого начала гейта, но
  использовался редко и точечно, не как часть рутинного батча (см. `PIT-153`).
  Дефект был на виду с первого дня многорепности, просто никто не смотрел.

- **Починка.** Все три проверки плюс `check_allowlist_rot` теперь пробуют путь
  и с `_base/`-префиксом, и без; `check_allowlist_rot` дополнительно возвращает
  пусто целиком для реп, у которых `_base/` нет вовсе (публичные — база им не
  раздаётся) — для них проверка структурно неприменима, а не «нашла дыру».
  Замерено `revision_check.py --root` до/после: `mission-control` 25 → 6 fail,
  `truth-seeking` 23 → 5, `vk-graph` 7 → 2 (плюс отдельно добавленный README
  STATUS). Ни одна правка не коснулась содержимого этих трёх реп — весь эффект
  от четырёх точечных правок в самом `revision_check.py`.

- **Методический урок.** Инструмент, проверяющий N объектов, но откалиброванный
  и протестированный на одном — это инструмент с одним пройденным тестом из N.
  `--root` как параметр существовал, но использование параметра не гарантирует
  корректность **под** этим параметром: путь построения (relative_to) может
  молча измениться на слой глубже, и ни один из существовавших selftest'ов
  этого не ловил, потому что все они тоже гоняются на `base-repo`.

- **Родня:** `PIT-142` (копия канона видна гейту под другим путём — переименованный
  дубль невидим точному сравнению имени) — тот же класс: гейт судит по
  буквальному пути, а не по факту содержимого, и расходится с реальностью
  ровно там, где путь меняется, а смысл — нет.

- **Гейт:** да → сам `revision_check.py` (после починки). Открытый хвост —
  ни один selftest в файле не запускается с `--root` на синтетическом дереве
  вида «файл лежит под `_base/`»; будущий похожий дефект в другой проверке
  тем же способом снова проскочит незамеченным до первого реального `--root`.

### PIT-155 — массовое закрытие пунктов роадмапа по выборочному `grep`-совпадению вместо построчного чтения: пропущены реально открытые пункты в том же файле (2026-08-28)

- **Симптом.** При сверке `mission-control/ROADMAP.md`/`BACKLOG.md`/`BOARD.md`
  с реальным состоянием реп вахта проверила ~25 пунктов из ~250 через
  `grep`/`tasks_census.py --repo`, получила >90 % подтверждённых совпадений
  и на этом основании закрыла десятки строк формулировкой «закрыто фактом».
  Владелец в следующем же сообщении назвал минимум пять пропущенных пунктов
  в **том же самом файле**: второй подпункт 1.7 (оценка batch-вызова модели),
  весь раздел 2.2б (покрытие сборщика), весь раздел 2.3 (дневной лист), пункт
  таблицы «Что осознанно не делаем» (миграция реп) и пункт из архива чата
  12.08 (протокол стандарта скриптов).

- **Корень.** Метрика «доля подтверждённых совпадений на выборке» отвечает
  на вопрос **«похоже ли, что модель моих ожиданий верна в целом»**
  (гипотеза о свойстве множества). Задача была другая — **«перечисли, какие
  конкретно пункты ещё открыты»** (перечисление элементов множества). Это
  два разных вопроса, и высокий процент на первом не даёт ответа на второй.
  Технически: каждый заход в файл шёл через `grep -n "<конкретная фраза>"`
  под заранее сформулированную гипотезу («это уже сделано?») — разделы, для
  которых гипотеза не была сформулирована заранее, не проверялись вовсе, а
  не потому что были сложны.

- **Тот же класс, что уже описан для тестов.** `reactive_testing_retrospective.md`:
  «зелёный прогон говорит только про то, что покрыто; про непокрытое он не
  говорит ничего — и это молчание легко принять за отсутствие проблем».
  Здесь роль «зелёного прогона» играла высокая доля совпадений на выборке.

- **Починка.** Полный построчный `Read` файла целиком вместо `grep`-выборки;
  все пять названных владельцем пунктов проверены по отдельности и закрыты
  либо оставлены открытыми по факту (не по угадыванию).

- **Правило.** Массовое закрытие пунктов роадмапа/бэклога/доски требует
  полного чтения файла целиком, не `grep` по гипотезам. Докладывать
  «N % пунктов подтверждено на выборке» как «X пунктов закрыто» —
  подмена метрики: первое — свойство выборки, второе — факт про каждый
  элемент множества, и нельзя доложить второе, измерив только первое.
  Сигнал, что проход не был полным: владелец после доклада о «полном
  проходе» тут же находит несколько пропущенных пунктов **в том же самом
  файле** — это повод разобрать метод, а не оправдываться конкретными
  находками.
- **Гейт:** нет — обнаружено при аудите покрытия гейтами 28.08.2026
  (владелец: «все ли питфолы обросли сторожами/гейтами/тестами»), карточка
  оказалась без строки «Гейт» вовсе. Массовое закрытие пунктов роадмапа —
  решение вахты в моменте, не действие, которое проходит через
  скрипт-проверку; полагаться придётся на само правило выше, а не на
  автоматику.

### PIT-156 — буква вахты в `WATCHLOG.md` писалась по шаблону («V», потому
что так было в предыдущих строках), а не проверкой `ACCOUNTS.md`/`userEmail` (2026-08-28)

- **Симптом.** В том же заходе, что и `PIT-155` (та же вахта, тот же день),
  во все пять реп, тронутых батчем (`mission-control`, `legal-knowledge-base`,
  `it-base`, `truth-seeking`, `health-vault`), в поле `**Вахта:**` заголовка
  `WATCHLOG.md` вписана буква **V** — включая `health-vault`, где до правки
  стояла буква **A**, и `it-base`, где стояла **X**: обе перезаписаны без
  проверки, третьим значением, скопированным из того, что писалось в
  `mission-control` минутами раньше. Владелец поймал: «ты опять не чекаешь
  какая сейчас действительно вахта».
- **Корень.** Буква вахты не выводится ни из чего внутри хода — её не видно
  ни в одном инструменте напрямую. Единственный проверяемый след — системное
  поле `userEmail` (`finpilot.support@proton.me`) сверх `mission-control/
  ACCOUNTS.md`, где это прямо сопоставлено вахте **A** (Adam). Вместо этой
  проверки буква бралась по инерции письма: «я всю сессию пишу V, значит и
  сейчас V» — тот же паттерн, что `PIT-155` (шаблон вместо факта), только
  на другом поле данных.
- **Тот же класс, что PIT-155.** Оба случая — подстановка правдоподобного
  значения по шаблону из соседних строк вместо чтения источника правды,
  который физически был доступен и не проверялся. `PIT-155` — по
  content-полям роадмапа, `PIT-156` — по служебному метаполю (кто пишет).
- **Починка.** Все пять `**Вахта:**`-строк и таблица `§3` в `mission-control/
  WATCHLOG.md`, тронутые этим батчем, поправлены на `A` по факту
  `userEmail` + `ACCOUNTS.md`. Более старые записи `WATCHLOG.md` (до этого
  батча, включая `base-repo` со значением `IX`) не переписаны задним числом —
  история фиксирует то, что было записано в момент записи, не исправляется
  ретроактивно без отдельного решения владельца.
- **Правило.** Перед первой записью в `**Вахта:**` любого `WATCHLOG.md` за
  сессию — свериться с `mission-control/ACCOUNTS.md` по системному `userEmail`
  контекста, а не копировать букву из соседней строки/файла. Одной проверки
  на сессию достаточно (кэшируется в памяти хода), но пропуск даже этой
  одной проверки — тот же дефект, что `grep`-выборка вместо чтения: экономия
  на проверке там, где проверка стоит одного взгляда в файл.
- **Гейт:** нет — `revision_check.py` не сверяет `**Вахта:**` ни с чем
  (это метаполе про то, кто пишет, не про содержание); добавление такой
  проверки потребовало бы гейту знать текущий `userEmail` сессии, которого
  у него нет. Ловится только внимательностью владельца, как и было здесь.

- **Родня:** `reactive_testing_retrospective.md` — тот же класс ошибки
  («покрытое не значит непокрытого нет») в другой области (тесты, не
  роадмап). Полный разбор ситуации — `reports/incidents/
  sampled_roadmap_verification_incident.md`.

- **Гейт:** нет. Массовое закрытие пунктов роадмапа — решение вахты в моменте,
  не действие, которое проходит через скрипт-проверку; полагаться придётся
  на само правило выше, а не на автоматику.

### PIT-157 — проверка PIT-091 проваливала гейт для реп без собственного реестра PIT/SYN, даже когда их проза ничего не заявляла (2026-08-28)

- **Симптом.** `revision_check.py --root mission-control` весь этот сеанс
  стабильно давал `DRIFT` из-за `[FAIL] Числа в прозе разошлись с диском
  (PIT-091): 2` — «карточек PIT: источник для подсчёта не найден»,
  «карточек SYN: источник для подсчёта не найден». Уже задокументировано
  в `mission-control/ROADMAP.md` §2.2а как «известное ложное срабатывание»,
  но не починено.
- **Корень.** `check_prose_counts()` вызывал `_count_matches()` на
  `reports/pitfalls.md`/`05-infra-synthesis-lab/PITFALLS.md` — файлах,
  которые ведёт **только `base-repo`** (единственный владелец реестра
  PIT/SYN-карточек в системе). Если файла нет, `_count_matches` возвращает
  `-1`, и старый код **безусловно** добавлял «источник не найден» в
  `problems` — даже когда прозаические файлы репы (`README.md` и т.д.)
  вообще не заявляли никакого числа карточек, то есть проверять было
  нечего. Проверка отвечала на вопрос «реестр существует?», а должна была
  отвечать на вопрос «прозаическая заявка о числе есть, но непроверяема?».
- **Тот же класс, что PIT-153.** `_base/`-дыры гейта: проверка, откалиброванная
  только на структуру `base-repo`, молча ломается для реп, которые устроены
  иначе (нет своего реестра, база раздаётся `_base/`-копией). Здесь то же
  самое: гейт написан под мир, где у каждой репы есть `reports/pitfalls.md`,
  и не предусматривает мир, где реестр централизован в одной репе.
- **Починка.** `check_prose_counts()` теперь сначала ищет совпадения по
  `PROSE_FILES` для каждого счётчика; жалуется на «источник не найден»
  только если хотя бы одно совпадение реально нашлось (прозе есть что
  проверять, а нечем). Канарейка `selftest_prose_counts()` дополнена
  третьим сценарием: реестров нет, проза о карточках молчит — проверка
  обязана остаться пустой.
- **Правило.** Проверка «диск против прозы» обязана сначала убедиться, что
  проза **что-то утверждает**, и только потом жаловаться на невозможность
  это перепроверить. «Источника нет» — повод фейлить только тогда, когда
  без него нельзя проверить существующую заявку, не тогда, когда заявки
  нет вовсе.
- **Родня:** PIT-153 (тот же класс — гейт, откалиброванный только на
  `base-repo`), PIT-091 (изначальная карточка о числах в прозе).
- **Гейт:** `selftest_prose_counts()` — теперь ловит и старую регрессию
  (расхождение число/диск), и эту (безусловный fail без реальной заявки).


### PIT-158 — перепись задач считала строки-заглушки настоящей работой: 50 фантомных задач и проверка разрыва, проходившая на фиктивных данных с обеих сторон (2026-08-28)

- **Симптом.** `tasks_census.py` показывал **614 открытых задач** по репам и
  «работа идёт, но доска не знает: 39». Оба числа врали **не в свою пользу**:
  скрипт отчитывался о работе, которой не существует.
- **Как всплыло.** При чистке `mission-control/BOARD.md` (требование владельца
  «чисть выполненные в логи») были свёрнуты **38 секций-заглушек** — по 6 строк
  каждая, все с двумя строками `🔸 **foo** — заглушка, задача не назначена` и
  `🔸 **bar**`. Сразу после этого перепись показала +39 разрывов. Первая мысль —
  «я сломал доску»; проверка показала обратное: **разрыв был всё это время**,
  просто его закрывали фиктивные секции.
- **Корень — заглушка с обеих сторон.** Репа-сателлит заводится со строкой
  `_(появится после наполнения)_` в своём `ROADMAP.md` и парной секцией
  `foo`/`bar` на доске: репа готова принимать доставку до появления содержания.
  Скрипт считал **и то, и другое** настоящей задачей. Проверка «доска знает о
  работе репы?» сравнивала фикцию с фикцией и проходила — при том, что ни
  реальной задачи, ни осмысленной секции не было ни у одной из 38 реп.
- **Тот же класс, что PIT-153 и PIT-157.** Три раза подряд: проверка,
  написанная под один частный мир, молча даёт неверный ответ в другом. Здесь —
  гейт, не отличающий «место заведено» от «работа идёт».
- **Починка.** `is_placeholder()` + `PLACEHOLDER_RE`, применяются и к строкам
  `<репа>/ROADMAP.md`, и к строкам доски. Числа после починки: **614 → 564**
  задач (50 фантомных снято), разрыв **39 → 18**, и эти 18 — настоящие
  (репы с реальными строками вида «точечные материалы, если владелец захочет
  углубить тему», просто без секции на доске).
- **Две ловушки самой починки, обе пойманы канарейкой, а не в проде.**
  (1) Первая редакция снимала ведущее украшение по **списку эмодзи** внутри
  `strip_md()` — и промахнулась мимо `🔸`, которого в списке не было. Заменено
  на класс «не буква и не цифра»: список ломается на каждом новом эмодзи,
  класс — не ломается. (2) В репах заглушка приходит как `**[П3]** 🔸 **foo**`,
  то есть эмодзи стоит **после** метки приоритета — один проход снятия
  украшения до неё не доходит (в `[П3]` есть буква «П»). Снятие украшения и
  метки чередуются в цикле, пока что-то снимается.
- **Правило.** Инструмент, считающий работу, обязан отличать **заведённое
  место** от **начатой работы**. Пустая секция, заведённая «чтобы было куда
  положить», — это ноль задач, а не одна; иначе счётчик растёт от самого факта
  подготовки к работе, и проверки связности проходят на данных, которых нет.
- **Родня:** PIT-153, PIT-157 (тот же класс — гейт под один частный мир),
  PIT-016 (проверять вывод инструмента, а не факт, что он не упал).
- **Гейт:** да → `tasks_census.py --selftest` (9 заглушек обязаны отсеяться,
  7 настоящих задач — сохраниться, включая граничные: слово «заглушка» внутри
  реальной задачи и метка приоритета перед ней).

### PIT-159 — кросс-репная ссылка в §0 читалась как висячая: вахта четырежды переписывала журнал под инструмент вместо инструмента (2026-08-28)

- **Симптом.** `check_resume_point_content()` жаловался
  «§0 ссылается на несуществующее: `mission-control/BACKLOG.md`» у
  `dota-dossier`, `health-vault`, `legal-knowledge-base`. Ссылка законная:
  репы лежат рядом в `~/repos/`, файл существует, ссылаться на него из §0 —
  ровно то, что журнал и должен делать («этот батч закрывает пункт бэклога
  планировщика»).
- **Корень.** Проверка искала файл **внутри текущей репы**
  (`(root / ref).exists()` и `root.rglob(name)`) и не знала, что первый
  сегмент пути может быть именем **соседней репы**.
- **🔴 Что делала вахта до этого — и это главный урок карточки.** Четыре раза
  подряд, встретив красный гейт, вахта **переписывала прозу журнала**
  («`mission-control/BACKLOG.md`» → «бэклог планировщика»), чтобы обойти
  проверку. Каждый раз это выглядело как аккуратная мелкая правка; по факту
  это **порча источника ради инструмента**: журнал терял точный адрес, по
  которому следующая вахта нашла бы контекст, — и терял его не потому, что
  адрес неверен, а потому что инструмент его не понимал. Только на пятом
  срабатывании сработала мысль «чиню не то».
- **Правило.** Если обход проверки требует **ухудшить содержание** — чинить
  надо проверку, а не содержание. Признак: правка не добавляет точности, а
  убирает её; после правки документ отвечает на вопрос читателя **хуже**.
  Второй признак — повторяемость: один обход бывает уместен, четвёртый обход
  одного и того же класса означает, что дефект в гейте.
- **Починка.** Если первый сегмент ссылки — имя каталога-соседа
  (`root.parent / first` существует и это не сама репа), ссылка ищется в
  соседе. Найдено → молчим; не найдено → краснеем как обычно.
- **Гейт:** да → `selftest_resume_point_refs()`, вшита в прогон
  `revision_check.py`. Проверяет **различение**, а не «не падает»: сосед с
  живым файлом обязан пройти, сосед с несуществующим и своя репа с
  несуществующим — обязаны покраснеть. Смягчение, которое ослепило бы
  проверку, роняет канарейку.
- **Родня:** PIT-153, PIT-157, PIT-158 (тот же класс — гейт под один частный
  мир), `71` §7ж (не путать «инструмент не смог» с «этого нет»).

### PIT-160 — массовая правка внесла 23 битые ссылки: шаблон писался по одной репе, применялся к 56 (2026-08-28)

- **Симптом.** Через полчаса после массового заведения блока `<!-- STATUS -->`
  в 56 репах гейт показал новый класс падений: «README.md: битая ссылка →
  `TASKS.md`» в **23 репах**. Дефекта не было до правки — он **внесён самой
  правкой**.
- **Корень.** `insert_status_block()` вписывала навигационную строку
  безусловно: «Открытое — `ROADMAP.md` · на владельце — `TASKS.md` · где стоим —
  `WATCHLOG.md`». Шаблон списан с `base-repo`, где все три файла есть. В 23
  репах (сателлиты, продуктовые зеркала, витрины) `TASKS.md` не заводился —
  он там просто не нужен, и это не дефект тех реп.
- **Почему это важнее, чем «мелкая опечатка».** Инструмент, который правит
  56 реп за один проход, **умножает** любую свою предпосылку на 56. Ручная
  правка ошиблась бы в одной репе и была бы замечена сразу; массовая —
  разложила один неверный допуск по всей системе за секунды. Цена ошибки
  в массовом инструменте линейна по охвату, а внимание к нему — нет.
- **Что сработало правильно.** Дефект нашёл **тот же гейт**, в **том же
  заходе**, до того как владелец увидел систему. Это ровно то, ради чего
  гейт существует: сплошной прогон после массовой правки — не формальность.
- **Правило.** Массовый инструмент обязан проверять предпосылку **в каждой
  репе**, а не исходить из устройства той, на которой его писали. Практически:
  любая ссылка, которую он вписывает, проверяется на существование цели
  перед записью; любой раздел, который он добавляет, — на применимость.
- **Починка.** Навигационная строка собирается из тех файлов, которые реально
  есть (`(root / name).is_file()`); нет ни одного — строки нет вовсе.
  23 репы починены тем же проходом.
- **Гейт:** да → `readme_status_gate.py --selftest`, сценарий 1 дополнен:
  во временной репе нет ни `ROADMAP.md`, ни `TASKS.md`, ни `WATCHLOG.md`, и
  канарейка падает, если в готовом блоке появилась ссылка на любой из них.
- **Родня:** PIT-158 (тот же заход, обратная сторона — инструмент считал
  несуществующее существующим), `80-mass-repo-automation.md` §4
  (стандарт массового скрипта: идемпотентность, `--dry-run`, fail-loud).

### PIT-161 — сухой прогон доставки показывал «готово» там, где реальная доставка отказала бы: проверка стояла только в ветке --apply (2026-08-28)

- **Симптом.** `mission-control/scripts/push-inbox.sh <репа>` печатал весь срез
  задач и «=== конец сухого прогона — ничего не отправлено ===», выглядя как
  «всё в порядке, можно применять». Реальный `--apply` в тех же репах падал на
  guard 2: «не готова принимать — нет TASKS.md».
- **Корень.** Guard 2 (проверка, что у целевой репы есть И `ROADMAP.md`, И
  `TASKS.md`) стоял **после** раннего `exit 0` сухого прогона. То есть режим,
  единственная задача которого — **заранее показать, что произойдёт**, не
  проверял главное условие того, что произойдёт.
- **Масштаб.** Замер 28.08.2026: `TASKS.md` отсутствовал у **24 реп из 63**.
  Сухой прогон вводил в заблуждение по трети системы, и узнать об этом можно
  было только сетевым вызовом на запись.
- **Почему это класс, а не мелочь.** Dry-run существует ради одного: дать
  верный ответ **до** необратимого действия. Dry-run, который проверяет меньше,
  чем реальный прогон, хуже отсутствующего — он создаёт уверенность, которой
  не заслужил. Ровно тот же изъян, что у теста, проверяющего не то, что
  проверяет прод.
- **Починка.** Проверка вынесена в функцию `receiver_missing()` и вызывается
  **в обоих режимах**. Сухой прогон теперь заканчивается либо «приёмник готов»,
  либо «🔴 реальная доставка ОТКАЗАЛА БЫ — нет: …» с тем же объяснением, что
  и у `--apply`. `push-inbox.sh` v1.3.0.
- **Побочная находка при починке.** `$REPO»` в строке вывода склеился с
  кавычкой-ёлочкой, и bash прочитал имя переменной как `REPO»` → `unbound
  variable`. В кириллическом тексте переменные обязаны быть в скобках:
  `${REPO}`. Поймано первым же прогоном, не в проде.
- **Правило.** Сухой прогон обязан выполнять **все** проверки реального
  прогона — отличаться он вправе только отсутствием записи. Любая проверка,
  стоящая после ветвления на режим, должна быть поднята выше ветвления.
- **Гейт:** нет — проверяется прогоном самого скрипта в сухом режиме на репе
  без `TASKS.md` (даёт «ОТКАЗАЛА БЫ») и на готовой (даёт «приёмник готов»).
  Заводить отдельный гейт под один скрипт несоразмерно; при следующей правке
  `push-inbox.sh` прогнать оба случая.
- **Родня:** PIT-016 (проверять вывод, а не факт запуска), PIT-160 (массовая
  правка размножает неверный допуск).

### PIT-162 — перепись задач считала образец из HTML-комментария живой задачей: раздача шаблона добавила системе 28 несуществующих задач (2026-08-28)

- **Симптом.** Сразу после заведения `TASKS.md` в 25 репах из шаблона базы
  `tasks_census.py` показал прирост открытых задач. Разрез по `edu-base`:
  «TASKS.md: 1 открытых · `<Что сделать, в прошедшем времени — осознанный
  приём владельца, мозгу так проще>`».
- **Корень.** `templates/TASKS_TEMPLATE.md` держит инструкцию по заполнению
  внутри `<!-- … -->`, и в ней — образец строки `- [ ] <Что сделать…>`.
  Перепись читала файл **построчно**, про HTML-комментарии не знала и
  засчитывала образец как задачу. Затронуто **28 файлов**.
- **Два класса в одном.** (1) Парсер, не знающий разметки, которую читает.
  (2) `PIT-160`: массовая правка умножает один неверный допуск на охват —
  один образец в шаблоне стал 28 фантомами за одну команду.
- **Починка.** `strip_html_comments()` перед разбором строк. Числа вернулись
  к факту.
- **Правило.** Инструмент, читающий markdown построчно, обязан сначала снять
  то, что markdown-ом не является: комментарии, блоки кода, front-matter.
  Иначе он считает **инструкцию к формату** содержимым формата. Прецедент
  уже был: `strip_code_fences()` в `revision_check.py` заведён ровно потому,
  что ссылка внутри ```-блока — пример разметки, а не живая ссылка.
- **Гейт:** да → `tasks_census.py --selftest`, добавлен сценарий: файл с одной
  настоящей задачей и двумя образцами внутри `<!-- -->` обязан дать ровно
  одну задачу.
- **Родня:** PIT-158 (заглушка не работа), PIT-160, `strip_code_fences()`
  в `revision_check.py` — тот же приём, другой инструмент.

### PIT-163 — корень PIT-156 найден: буква вахты была ЗАХАРДКОЖЕНА в `bump_repo.py`, правило её проверять не могло помочь (2026-08-28)

- **Симптом.** `PIT-156` («буква вахты в журнале написана по шаблону, а не
  проверена») заводился как **дисциплинарный** дефект: вахта поленилась
  проверить. Правило «проверяй перед записью» было записано — и не сработало
  ни разу.
- **Настоящая причина, найдена только сегодня.** В `scripts/bump_repo.py`
  строка журнала собиралась так:
  ```python
  f"**Версия:** {new} · **Дата:** {TODAY} · **Вахта:** V\n"
  ```
  Литеральное `V`. **Писала не вахта, а скрипт** — и писал он `V` всегда,
  независимо от того, кто работает и что вахта про себя знает. Никакая
  внимательность этого не ловит: агент не пишет эту строку руками.
- **Масштаб, измеренный в тот же заход.** Один прогон подъёма версий по
  45 репам разложил неверную букву **по 45 журналам за минуту**. Тот же
  класс, что `PIT-160`: массовый инструмент **умножает** свой неверный допуск
  на охват.
- **Почему это важнее самой опечатки.** Дефект полгода жил как «человеческий»,
  и правило-напоминание против него было бессильно **по построению**. Пока
  источник числа — константа в коде, дисциплина не помогает: чинить надо там,
  откуда берётся число.

  > **Правило.** Прежде чем записывать правило-напоминание, спроси: **кто
  > физически пишет эту строку**. Если инструмент — правило адресовано не тому,
  > и чинить надо инструмент.

- **Починка.** `current_watch()` читает `~/.claude.json` →
  `oauthAccount.emailAddress` и сопоставляет с буквой. **Не определилась —
  возвращает `?`, а не ближайшую**: неизвестная буква честнее неверной
  (`71` §7ж). 7 журналов, куда скрипт успел записать `V` в этом заходе,
  исправлены на `A`.
- **Связь с механизмом.** Дополняет хук `watch-identity.sh` (`[3.97.0]`),
  но не заменяется им: хук говорит вахте, кто она; здесь число берёт **сам
  инструмент**, минуя контекст агента вовсе.
- **Гейт:** частично — `revision_check.py` сверяет версию в §0 с `VERSION`,
  но **не** букву вахты: сверять её не с чем, кроме `~/.claude.json`, который
  и стал теперь источником. Проверка — прогон `current_watch()`.
- **Родня:** `PIT-156` (симптом), `PIT-160` (охват умножает допуск),
  `PIT-159` (чинить проверку, а не содержание), `71` §7ж.

### PIT-164 — четыре хардкод-списка подряд за один вечер: список не сообщает, что отстал (2026-08-28)

- **Симптом.** За один заход нашлись **четыре** захардкоженных списка, каждый
  молча отставший от реальности:
  1. `sync_base_local.py` — перечень каталогов-китов. Отстал **дважды**:
     `08-systems-theory-lab` (22.08) и `09-automation-kit` (28.08) в него не
     вписали, и они **физически не доезжали до реп**, хотя `BASE_VERSION`
     показывал свежую версию. Раздача врала не о версии, а о содержимом.
  2. `deploy_version_gate.py` — `KNOWN_COPIES` из двух путей. Один мёртв после
     миграции, **две живые копии не входили в список вовсе**: гейт «копии
     сходятся» проходил, пока копии расходились на 21 версию (v4.1.0 против
     канона v4.22.0).
  3. `bump_repo.py` — буква вахты литералом `V` (см. `PIT-163`).
  4. `bump_repo.py` — набор файлов, двигающихся с версией: `VERSION` и
     `**Версия:**` обновлялись, «Текущая точка» и README-статус нет.
- **Общий корень — `PIT-097`:** *список — намерение, свойство объекта — факт*.
- **Почему списки отстают именно молча.** У списка нет способа сообщить, что
  он неполон: он не пустеет, не краснеет, продолжает работать — просто перестаёт
  покрывать то, что появилось после него. **Отказ выглядит как успех.** Гейт с
  устаревшим списком не «падает иногда» — он уверенно докладывает CLEAN.
- **Как находились.** Ни один не нашёлся чтением кода. Все четыре — по
  расхождению между «инструмент говорит, что всё разложено» и «файла на месте
  нет». Первый — потому что гейт заметил ссылку на несуществующий каталог.
- **Правило.** Прежде чем писать список, спросить: **есть ли у элементов общее
  проверяемое свойство**. Есть — писать правило (`имя вида NN-`, `файл
  называется deploy.sh`, `репа приватная по ответу GitHub`). Нет — список
  законен, но обязан нести дату и способ проверки полноты.
- **Где список остаётся законным.** Когда свойства нет: `SEARCH_ROOTS`
  (где искать — решение, а не свойство), `FROZEN_DIRS` (что заморожено —
  тоже решение). Различие: правило отвечает «что это», список — «что мы решили».
- **Гейт:** частично. `deploy_version_gate.py` теперь ищет копии сам;
  `sync_base_local.py` отбирает киты по имени. Общего гейта «нет ли отставших
  списков» нет и, вероятно, не может быть — отставание видно только по
  расхождению с реальностью, а не по виду кода.
- **Родня:** `PIT-097` (исходная формулировка), `PIT-163` (частный случай,
  корень `PIT-156`), `PIT-160` (массовый инструмент умножает допуск).

---

### PIT-165 — правка двух файлов одним скриптом без проверки обоих якорей: первая половина прошла, вторая упала, состояние стало хуже исходного (2026-08-29)

- **Разрешить или растворить:** **растворить**. Чинить надо не якорь, а сам
  приём «две записи подряд без общего предусловия». Пока перенос между файлами
  пишется как последовательность `assert` вперемешку с записями, тот же отказ
  воспроизведётся на любом несовпавшем тексте — и на обрыве связи тоже.
- **Долг заведён:** `2026-08-29`. **Пересмотр:** `2026-11-29` — если к этой дате
  ни один перенос между файлами не был написан двухфазно, приём не прижился,
  и запись надо либо усилить механизмом, либо честно снять.

- **Симптом.** Правило маршрутизации требовало перенести три пункта: убрать
  содержание из `ROADMAP.md`, оставив указатель, и завести его в `TASKS.md`.
  Скрипт делал это двумя частями. Первая отработала и **записала файл**.
  Вторая упала на `AssertionError`: текст якоря в `TASKS.md` за день до этого
  изменился.
- **Почему итог хуже обоих исходных состояний.** До правки содержание лежало
  в одном файле — неправильно разложенное, но **целое**. После половины правки
  указатель в `ROADMAP.md` ссылался на раздел `TASKS.md`, **которого не было**.
  Не «дубль» и не «пропуск», а третье состояние: **ссылка в пустоту, выглядящая
  как порядок**.
- **🔴 Обе проверки показали «хорошо» — и обе были правы.** `grep` по 👤 в
  `ROADMAP.md` дал `0` (верно: пункты оттуда убраны). Гейт дал CLEAN (верно:
  ни одна из 28 проверок не спрашивает «есть ли содержание там, куда указано»).
  Это не отказ проверок, а **их граница**: обе отвечают на свой вопрос, и ни
  одна — на вопрос «согласованы ли два файла между собой».

  > **Правило.** Правка, затрагивающая **больше одного файла**, обязана
  > проверить **все** якоря **до первой записи**. Разбор входа и применение
  > изменений — две фазы, а не одна: `assert` посреди записей превращает
  > падение в порчу.

- **Класс, а не случай.** Это ровно `parse, don't validate` (кандидат №14
  очереди внедрения) на уровне не строки, а файловой операции: сначала
  **весь** вход разбирается в структуру, и только потом применяется. Тот же
  корень, что у сходящихся скриптов (`08-automation-triggers.md`): операция,
  не переживающая обрыв посередине, обязана быть двухфазной.
- **Починка.** Три пункта заведены в `TASKS.md` §A2 (`mission-control` v2.1.1).
- **Гейт:** нет и не будет прямого. Общая проверка «согласованы ли два
  произвольных файла» неразрешима — согласованность задаётся смыслом, а не
  формой. Что **можно** и что заведено следующим шагом: проверка, что указатель
  ведёт на **существующий** раздел, а не только на существующий файл.
- **Родня:** `PIT-156`/`PIT-163` (чинить источник, а не напоминание),
  `PIT-159` (чинить проверку, а не содержание), кандидат №14 (`parse, don't
  validate`), `08-automation-triggers.md` (идемпотентность сходящихся скриптов).

---

### PIT-166 — обёртка проверки отказала вместо предмета: `timeout` на macOS не существует, и четыре исправных скрипта были объявлены сломанными (2026-08-29)

- **Разрешить или растворить:** **растворить**. Чинить надо не эту команду,
  а привычку заворачивать проверку в утилиту, наличие которой не проверено.
- **Долг заведён:** `2026-08-29`. **Пересмотр:** `2026-11-29`.

- **Симптом.** После перевода пятнадцати скриптов на общий определитель корня
  проверка работоспособности дала:
  ```
    tasks_census       🔴 отказ
    owner_blockers     🔴 отказ
    secrets_scan       🔴 отказ
    repo_revision      🔴 отказ
  ```
  Вывод напрашивался сам: правка сломала четыре скрипта, надо откатывать.
- **Что было на самом деле.** Все четыре исправны. Проверка выглядела так:
  ```bash
  timeout 120 python3 scripts/$f.py --help >/dev/null 2>&1 && echo 🟢 || echo 🔴
  ```
  🔴 **`timeout` — утилита GNU coreutils, в macOS её нет** (`which timeout` →
  `timeout not found`). Оболочка не нашла команду, вернула ненулевой код,
  и `||` честно напечатал отказ — **про несуществующую команду, а не про
  скрипт**. Прямой запуск того же `--help` возвращает `0`.
- **Почему это опаснее обычной опечатки.** Отказ пришёл в той же форме, в какой
  пришёл бы настоящий: тот же значок, то же слово, то же место. Отличить
  «предмет сломан» от «проверка сломана» по выводу было нельзя — а действие
  предполагалось разное: в первом случае откатывать правку пятнадцати файлов,
  во втором не делать ничего.

  > **Правило.** Обёртка проверки — часть проверки. Прежде чем читать её
  > вывод как суждение о предмете, убедись, что сама обёртка исполнилась:
  > либо не заворачивай (`python3 … --selftest` напрямую), либо проверь
  > наличие утилиты, либо различай коды возврата (`127` — команда не найдена,
  > и это не отказ предмета).

- **Родня.** Тот же корень, что `SYN-029` (`ls` — алиас на `eza`, `-t` значит
  другое, команда падает **с пустым выводом**, и пустой вывод читается как
  «ничего не найдено»). Оба случая: **утилита из чужой операционной системы
  подставлена в проверку, и её отказ маскируется под результат.** И тот же
  класс, что `PIT-159`: чинить проверку, а не содержание.
- **Починка.** Проверка переписана на прямой вызов канареек скриптов; ни одна
  правка не откачена, потому что откатывать было нечего.
- **Гейт:** нет — «команда существует в этой ОС» проверяется только запуском,
  а запускать всё подряд гейт не может. Ближайшее выполнимое: не использовать
  в проверках ничего, кроме POSIX-минимума и собственных скриптов.

---

### PIT-167 — недатированное утверждение о состоянии читается как настоящее время и потому протухает молча (2026-08-29)

- **Разрешить или растворить:** **растворить**. Разрешение — «перечитывать
  живые документы почаще» — это правило-напоминание, а такие правила не
  работают против того, что не видно (`PIT-163`). Растворяет привычка
  ставить дату **рядом с утверждением о состоянии**, а не в шапке документа.
- **Долг заведён:** `2026-08-29`. **Пересмотр:** `2026-11-29` — проверить,
  появились ли новые недатированные утверждения о состоянии.

- **Симптом.** `control-panel/ROADMAP.md`, веха 3: *«Сейчас у панели **нет ни
  одного автоматического теста** ни на бэкенде, ни на фронте»*. Замер
  29.08.2026: **59 тестов в 8 файлах, все проходят**. Утверждение было верно
  20.08.2026 и с тех пор не перечитывалось — девять дней.
- **Чем это опасно.** Живой документ читают, чтобы принять решение. Читающий
  увидел бы «тестов нет» и либо начал писать их заново, либо счёл репу сырой
  и не стал на неё опираться. **Документ, описывающий позавчерашнее состояние,
  вреднее отсутствия документа**: отсутствие видно, ложь — нет.
- **🔴 Различие, ради которого заводится карточка.** В `base-repo/ROADMAP.md`
  есть похожее утверждение — «у реп, заведённых 20.08, `ROADMAP.md`
  отсутствует как файл», и оно тоже неверно сегодня (проверено: у всех 62 реп
  все три файла на месте). Но дефектом **не является**, потому что рядом стоит
  «🔴 ОБЪЯСНЕНИЕ ИСПРАВЛЕНО 22.08.2026» — запись читается как история.

  > **Правило.** Утверждение о состоянии без даты читается как настоящее
  > время. Значит либо ставь дату **рядом с утверждением** (не в шапке
  > документа — шапку обновляют, а абзац нет), либо пиши так, чтобы оно
  > не могло устареть: «проверяется командой X», а не «сейчас X равно 5».

- **Как чинилось.** Утверждение **не удалено**, а датировано и подписано как
  прежнее: «Как было 20.08.2026: …», с сегодняшним замером выше. Тот же приём,
  что применён в этот же день к устаревшей команде в `PIT-154`: свидетельство
  не правят, его дополняют.
- **Гейт:** нет, и вряд ли будет. Отличить «утверждение о текущем состоянии»
  от «исторической записи» можно только по смыслу: формально и то и другое —
  предложение в настоящем времени. Ближайшее выполнимое — проверка на числа
  в прозе (`PIT-091`, уже есть), но она ловит только те утверждения, которые
  содержат **число**, сверяемое с диском. «Нет ни одного теста» числа
  не содержит.
- **Родня:** `PIT-091` (числа в прозе против диска), `PIT-116` (живые
  документы отстают), `PIT-154` (устаревшая команда, дополнена а не переписана).

### PIT-168 — канарейка доказывает, что правило СРАБАТЫВАЕТ, и не может доказать, что оно ВЕРНО (2026-09-02)

- **Разрешить или растворить:** **растворить**. Разрешение — «писать правила
  внимательнее» — правило-напоминание, а такие не работают (`PIT-163`).
  Растворяет привычка: новое правило сначала прогнать по **всей популяции**
  и посмотреть на долю нарушителей, а уже потом объявлять результат.
- **Долг заведён:** `2026-09-02`. **Пересмотр:** `2026-12-02` — проверить,
  ловилось ли ложное правило прогоном по популяции ещё раз.

- **Симптом.** Заведён `scripts/repo_invariants.py` — единый инвариант репы,
  десять правил, канарейка из девяти проверок. **Все девять зелёные.** Первый
  же прогон по системе: **61 нарушитель из 63 реп**.
- **Оба сработавших правила оказались ложными:**

  | Правило | Что требовало | Почему неверно |
  |---|---|---|
  | R-09 | архив `<имя>-v<версия>.zip` в `~/Downloads` | архивы **удаляются намеренно** ритуалом закрытия батча; доказательство выпуска живёт в `LEDGER.tsv` и переживает удаление файла |
  | R-06 | класс `product`/`profile` не имеет `_base/` | класс говорит, **чем** репа является, а не **кому** видна: `control-panel`, `personal-finance-dss`, `research-engine` — PRIVATE на GitHub |

- **🔴 Что именно поймало ошибку — и это главное в карточке.** Не канарейка.
  Ошибку поймало **число 61 из 63**: правило, по которому виновата почти вся
  популяция, скорее неверно, чем популяция. Канарейка при этом честно
  показывала девять зелёных галочек — она проверяла, что правило **срабатывает
  на подделанной репе**, и не могла проверить, что само правило **описывает
  реальный порядок вещей**.
- **Различение, ради которого заводится карточка.** Это школьная пара
  verification/validation, и `revision_check.py` её уже называет в разделе
  «чего гейт принципиально не ловит»: *«верна ли сама проверка (validation) —
  гейт умеет только verification»*. Строка была написана — и не помогла,
  потому что стояла в описании ДРУГОГО инструмента. Знание, лежащее в одном
  файле, не защищает работу в другом.
- **Чем опасно.** Ложный гейт хуже отсутствующего: отсутствие видно, а зелёный
  гейт создаёт уверенность. Если бы прогон дал не 61, а 3 нарушителя — правило
  осталось бы в силе, и три репы «чинили» бы под несуществующее требование.
  Массовость нарушения была здесь единственным сигналом.
- **Что сделано.** Оба правила переписаны на настоящий источник правды
  (`LEDGER.tsv` и `.repo-meta private=`), в код обоих вписана история ошибки,
  а в канарейку добавлены **обратные** проверки: «удалённый архив при живом
  журнале — НЕ нарушение», «приватный `product` с `_base/` — НЕ нарушение».
  Проверка на ложное срабатывание стоит рядом с проверкой на срабатывание.
- **Третье правило поймал уже тест, а не прогон.** `tests/test_repo_lifecycle.sh`
  в первый запуск показал, что конструктор откатывает **каждую** новую репу:
  R-09 требовал записи о выпуске от репы, которая физически не может быть
  выпущена. Отсюда разделение фаз `born`/`live` в инварианте.
- **Связки:** `PIT-163` (правила-напоминания), `PIT-G` (гейт есть, но врёт),
  `scripts/repo_invariants.py` (история обеих ошибок в комментариях кода).

### PIT-169 — закрытая кампания перестаёт себя проверять, и её статус протухает молча во всех репах разом (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «перепроверять
  статусы» — правило-напоминание против того, чего не видно (`PIT-163`):
  под уверенным заголовком «ЗАКРЫТО» перестают заглядывать именно потому,
  что он уверенный. Растворяет гейт `scripts/closed_campaign_gate.py`,
  сверяющий **заявление** с **живым измерителем**.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03` — проверить,
  ловил ли гейт новые расхождения и не отключён ли он.

- **Симптом.** Владелец: *«очень много инфы в заявлениях сканах документов
  в legal, family, health»*. Проверка: `IMAGE-REVISION-STATUS.md` во всех трёх
  репах утверждает **«статус: ЗАКРЫТО, очередь 0 серий / 0 кадров»**. Живая
  очередь `image_queue.py` на тот же момент — **379**, **1102** и **173** кадра.
- **Файлы не врали, когда их писали.** Очередь действительно была пуста
  27.08.2026. Telegram-фото импортировали **29.08** — через два дня. Один
  импорт обнулил три статуса разом, и ни один этого не заметил.
- **🔴 Масштаб, открывшийся гейтом.** Написан `closed_campaign_gate.py`,
  прогнан по системе: расхождение не в трёх репах, а в **одиннадцати**.
  Суммарно **5361 кадр** числится разобранным при живой очереди
  (`it-base` — 2577, `edu-base` — 865, `academic-portfolio` — 796, `ml-base` —
  508, `science` — 310, `master-admission` — 205, `misc-vault` — 76,
  `exam-kit` — 24, плюс три исходные).
- **Чем это отличается от `PIT-167`.** Тот — про недатированное утверждение,
  читаемое как настоящее время. Здесь добавка: **пока кампания идёт, её очередь
  смотрят каждую вахту; как только написано «ЗАКРЫТО», файл становится
  памятником** — его читают, но не перепроверяют. Чем увереннее заголовок, тем
  реже под него заглядывают. Дефект не в дате, а в самом акте закрытия.
- **Чем опасно.** «ЗАКРЫТО» — сигнал не тратить сюда внимание. Одиннадцать
  файлов уводили от пяти тысяч неразобранных кадров, и увели: разведка по
  корпусу `character-a-analysis` считала медицинский материал учтённым,
  потому что статус говорил «закрыто».
- **🔴 Конструкция гейта — сверять заявление с измерителем, а не свежесть
  даты.** Дата может быть любой давности: важно лишь, совпадает ли утверждение
  с тем, что инструмент показывает **прямо сейчас**. Проверка «не старше N
  дней» дала бы шум на честных статусах и молчание на свежем вранье.
- **Канарейка проверяет обе стороны** (урок `PIT-168`): «ЗАКРЫТО при живой
  очереди» → ловит; «ЗАКРЫТО при пустой» → молчит; «ОТКРЫТА при живой» →
  молчит. Гейт, ругающийся на честный статус, будет отключён через неделю.
- **Отдельная находка.** Отсутствие файла статуса читается как «вопрос не
  стоит», хотя означает «никто не смотрел». Гейт ругается и на это, если
  очередь не пуста.
- **Связки:** `PIT-167` (устаревшее утверждение о состоянии), `PIT-168`
  (канарейка не доказывает истинности правила), `PIT-163` (правила-напоминания),
  `07-media-to-text-lab/METHOD_IMAGES.md` §3.

### PIT-170 — несуществующий флаг не даёт ошибки: он даёт тишину и действие по умолчанию (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «сверять флаги
  с `--help`» — правило-напоминание (`PIT-163`). Растворяет проверка внутри
  самого теста: утверждение «побочных эффектов не было» проверяется
  ассертом, а не намерением автора.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Тест `test_version_bump.sh` писался герметичным: песочница
  во временном каталоге, вывод архивов — `--dest "$SANDBOX/dl"`. Первый прогон
  положил `rel-alpha-v1.0.0.zip` в **реальный `~/Downloads` владельца**.
- **Механизм.** У `pack_release.py` нет флага `--dest`: `OUT_DIR = Path.home()
  / "Downloads"` зашит жёстко, а лишний позиционный аргумент разбирается
  молча. **Отказа не было — было действие по умолчанию.** Тест при этом
  выглядел зелёным по своей главной проверке («архив собран») и красным
  по побочной, что и вывело на след.
- **Чем опасно.** Ошибочный флаг в разрушающей команде (`rm`, `--purge`,
  `--force`) точно так же не вызовет отказа: команда выполнится в режиме
  по умолчанию, то есть не там и не с тем. Тишина здесь — не признак
  правильности, а отсутствие механизма возражать.
- **🔴 Второй дефект, найденный тем же прогоном — в `bump_repo.py`.** Его шапка
  обещала: «мусор в `VERSION` не поднимается, а останавливает работу».
  Обещание не исполнялось: `bump()` дополняет недостающие части нулями, и
  `VERSION` с содержимым `мусор` превращалась в `мусор.0.1`, записанную
  **во все три файла разом**. Скрипт, заведённый как лекарство от полузакрытых
  реп (`PIT-136`), приводил репу в состояние, из которого её не поднял бы уже
  ни один инструмент системы: версия не разбирается нигде.
- **Как чинилось.** Проверка `re.fullmatch(r"\d+\.\d+\.\d+")` поставлена
  в предусловия `main()`, а не внутрь `bump()` — шапка обещает отказ **до
  первой записи**, и провалиться в середине трёх файлов хуже, чем не начать.
- **Что здесь общего у обоих дефектов.** Оба — расхождение между **обещанием
  в документации** и **поведением кода**, и оба прожили незамеченными, потому
  что обещание читали, а поведение не проверяли. Ни один не был бы найден
  чтением кода: оба проявляются только при исполнении.
- **Связки:** `PIT-136` (полузакрытая репа), `PIT-168` (канарейка не доказывает
  истинности), `tests/test_version_bump.sh` V3–V5.

### PIT-171 — измеритель, который не остановлен, становится нагрузкой и остаётся невидимым (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «не забывать
  останавливать» — правило-напоминание против того, чего не видно
  (`PIT-163`): вахта не забывает остановить, она **считает уже
  остановленным**. Растворяет проверка командой `pgrep` после каждой долгой
  операции и запрет `--watch` в самом скилле.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Владелец: *«чё то жёстко щас просела дота по фпс и пингу,
  /machine что ты запустил такое тяжёлое?»* → *«с 60 до 5 фпс, прямо
  невозможно фризит»*. `load average` — **35.94** при норме **8.2** под игрой.
- **Причина.** В фоне висел `machine_probe.py --watch --interval 1` — живой
  монитор с полным обходом процессов, памяти, диска и батареи **каждую
  секунду**, шестьдесят раз в минуту, бесконечно. Запущен вахтой для проверки
  правки и не остановлен.
- **🔴 Второй случай того же класса за ОДНУ вахту.** Первым был
  `test_deploy.sh`: ушёл в фон по таймауту (>10 минут прогона) и продолжал
  жечь процессор, пока владелец не пожаловался на вентиляторы. Тогда его
  сняли, но класс не подмели — и он вернулся через час в другом обличье.
- **Механизм невидимости.** Вахта не «забывает остановить» — она **считает
  процесс уже завершённым**: команда вернула управление (по таймауту или
  в фон), значит субъективно она кончилась. Обратной связи нет никакой:
  фоновый процесс не пишет в поток вывода, и единственный, кто его замечает,
  — владелец, по фризам. **Ошибка обнаруживается не там, где совершена.**
- **Почему это хуже обычной тяжёлой операции.** Тяжёлая операция кончается
  сама. Незавершённый монитор не кончается **никогда** и накапливается:
  каждый следующий запуск добавляется к предыдущему.
- **Что сделано.** Заведена классификация `12-workstation-kit/LOAD-CLASSES.md`
  — пять классов операций по цене в `load average`, от «бесплатно» (правка
  файлов) до «⛔️ запрещено при живой сессии владельца» (`--watch`, циклы
  с замером). В скилл `/machine` вписан прямой запрет на `--watch` для вахты
  и команда проверки:

  ```bash
  pgrep -fl "python3.*base-repo|machine_probe|test_" || echo "чисто"
  ```

- **🔴 Побочное наблюдение о самом показателе.** `load average` — среднее
  за минуту, и после `kill` падает не сразу: **35.94 → 25.30 → 21.05**.
  Судить об эффекте по одному замеру сразу после снятия нагрузки нельзя;
  нужны два замера с паузой. Тот же принцип, что с фоновыми службами
  (`overheating-diagnosis` §3а), — только там обманывает случайный ноль,
  а здесь остаточная высота.
- **Связки:** `PIT-163` (правила-напоминания), `LOAD-CLASSES.md`,
  `12-workstation-kit/observations/HISTORY.md` (запись 03.09 ~03:00).

### PIT-172 — имя каталога импорта говорит, ОТКУДА выгружено, а не ЧТО внутри (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «не доверять
  именам» — правило-напоминание (`PIT-163`). Растворяет процедурное
  требование: **два кадра из разных дат** до любого решения по серии.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** За одну вахту при разборе 5361 кадра в 11 репах имя каталога
  ввело в заблуждение **четыре раза, и в обе стороны**:

  | Каталог | Ожидалось по имени и месту | Оказалось |
  |---|---|---|
  | `legal-knowledge-base/Юриспруденция` | сохранённый контент, как в соседнем `Право` | **личное правовое дело**: обращение в Госдуму, свидетельство о праве собственности с паспортными данными третьих лиц |
  | `master-admission/НИТУ-МИСИС` | документы поступления | нейросетевые картинки и **фото шкафа в общежитии** |
  | `it-base/Computer-Science` | материал по информатике | **кадр манги** |
  | `family/Семья` | семейный фотопоток (как в прочих репах) | 🔴 **обход захоронений** — первоисточник генеалогии |

- **Механизм.** Каталог импорта называется по **чату-источнику**, а чат
  у человека тематический лишь по названию: в «Юриспруденцию» летит и ролик
  про ТК, и фотография свидетельства о собственности. Имя фиксирует канал,
  а не предмет.
- **Чем опасно в обе стороны.** Ложноположительное («здесь документы» →
  разбираем ерунду) стоит времени. **Ложноотрицательное дороже:**
  `family/Семья` по имени неотличима от свалки — и если бы её закрыли
  решением «общий фотопоток», потеряли бы единственную фиксацию надгробий,
  которые с годами читаются всё хуже.
- **🔴 Один кадр не спасает.** В `master-admission` первый кадр был
  нейросетевой картинкой — по нему серия выглядела мусором. Второй
  подтвердил. Но в `legal/Юриспруденция` первый кадр (личный кабинет Госдумы)
  и второй (свидетельство о праве) дали **разные** предметы — и именно это
  показало, что 495 кадров не серия, а весь правовой быт в одном каталоге.
- **Что заведено вместо правила-напоминания.** Процедура разбора: **два кадра
  из разных дат** до решения. Стоит двух чтений, снимает четыре ошибки
  из четырёх. Записана в `07-media-to-text-lab/METHOD_IMAGES.md` §7.
- **Связки:** `PIT-169` (закрытая кампания не перепроверяется),
  `METHOD_IMAGES.md` §3 (единица — серия), §9 (эталон или расходное).

### PIT-173 — раздача канона в середине батча: два инструмента противоречат, и оба правы (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «помнить, что
  раздача последняя» — правило-напоминание (`PIT-163`). Растворяет порядок
  внутри `close_batch.py`, где раздача уже стоит шагом 6; правило записано
  в шапку `sync_base_local.py`, чтобы его видел тот, кто зовёт скрипт руками.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Через семь минут после успешной раздачи канона по всем 63 репам
  `system_status.py` показал **«отставших от канона: 55»**, при том что
  `repo_invariants.py --all` в ту же минуту дал **63 чистых из 63**.
- **🔴 Оба инструмента были правы, и в этом суть.** Инвариант сверяет
  **версию** (`_base/BASE_VERSION` против `VERSION` канона) — она совпадала.
  Раздача сверяет **ещё и отпечаток содержимого** (`BASE_FINGERPRINT`) —
  он разошёлся. Формально это разные вопросы: «та ли версия» и «то ли
  содержимое», и репа может отвечать на них по-разному.
- **Что проверялось прежде, чем винить инструмент** (`71` §7ж):
  отпечаток посчитан дважды подряд — **устойчив**; повторная раздача той же
  репы дала **«уже актуальна»**. Значит расхождение было настоящим,
  а не дефектом измерителя. Это отличает случай от `PIT-097`, где инструмент
  действительно менял то, что измерял.
- **Причина.** Вахта раздала канон, а затем **продолжила править файлы,
  входящие в раздаваемое**: `reports/` (реестры граблей, журнал кампании),
  киты `NN-*`, `.claude/`. Каждая такая правка сдвигает отпечаток канона,
  и все 63 копии мгновенно становятся «разошедшимися».
- **Чем опасно.** Расхождение по отпечатку при совпавшей версии читается
  как **«копию правили руками»** — то есть подозрение падает на репу.
  🔴 **Означать оно может ровно обратное: правили канон.** Различить нельзя
  ничем, отпечаток симметричен. Следующая вахта пошла бы искать порчу
  в шестидесяти трёх репах вместо того, чтобы повторить раздачу.
- **Правило.** Раздача — **последний** шаг батча, после всех записей
  в реестры и журналы. В `close_batch.py` она и стоит шагом 6; здесь дефект
  возник потому, что шаги выполнялись поштучно, а не ритуалом.
- **Связки:** `PIT-097` (операция меняет измеряемое — похожий симптом,
  другая причина), `PIT-G` (обвязка отстала), `close_batch.py` шаг 6.
- 🔴 **Подтверждено на себе через двадцать минут после записи карточки.**
  Вахта дописала девять пропущенных строк в `06-autonomous-mode-kit/runs/auto.log`
  — и «отставших от канона» снова стало 55. `auto.log` лежит в ките `06-*`,
  то есть **в раздаваемом**: любая запись в журнал прогона сдвигает отпечаток
  канона.

  **Это делает правило жёстче, чем казалось при заведении.** Раздача должна
  идти после **любой** записи, включая служебные журналы, которые вахта пишет
  «попутно» и не считает правкой канона. Их-то и забывают: реестр граблей
  и `decisions.md` помнят, а `auto.log` — нет.


### PIT-174 — вахта закрывала батчи в обход ритуала, и watchdog ослеп на девять часов (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «звать
  `close_batch.py`, а не `bump_repo`» — правило-напоминание (`PIT-163`).
  Растворяет то, что `bump_repo.py` сам пишет строку в `auto.log`:
  тогда обход ритуала перестаёт быть невидимым. 🔴 **Не сделано** — вынесено
  в `ROADMAP.md`, потому что требует решения, где проходит граница
  ответственности скрипта.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** `system_status.py` показал **«минут тишины: 580»** — при том
  что за эту ночь вахта закрыла **девять батчей базы** и одиннадцать чужих реп.
  Инструмент сообщал, что работа стоит десять часов, посреди самой плотной
  работы за всю вахту.
- **Причина.** Признак жизни для `batch_silence()` — строка типа `batch`
  в `06-autonomous-mode-kit/runs/auto.log`. Её пишет `close_batch.py`
  (шаг 5 ритуала). Вахта же закрывала батчи **поштучно**: `bump_repo.py` →
  `pack_release.py` → `sync_base_local.py`, — и журнал не трогала вовсе.
- **🔴 Почему это опаснее, чем выглядит.** `batch_silence` — не отчётность,
  а **watchdog**: он существует, чтобы заметить залипший прогон, который
  сторож авто-режима по построению не видит (сторож возобновляет ход, но
  не знает, идёт ли внутри хода работа). Обойдя ритуал, вахта **выключила
  единственную проверку, способную заметить её собственное залипание** —
  и не узнала бы об этом, потому что залипание как раз и означает, что
  никто не смотрит.
- **Почему обход казался законным.** `bump_repo` + `pack_release` делают
  ровно то же, что шаги 3–4 ритуала, и результат неотличим: версия поднята,
  архив собран, гейт зелёный. **Незаметен только шаг 5** — тот, что ничего
  не производит, а лишь оставляет след. Шаги, чей результат не виден
  немедленно, пропускаются первыми.
- **Что сделано.** Девять пропущенных строк восстановлены задним числом
  (даты соответствуют версиям, не моменту записи). Порог снят.
- **Связки:** `PIT-G` (обвязка отстала — но там забывали, здесь заменяли
  ритуал на его часть), `PIT-173` (раздача после записи в журнал),
  `close_batch.py` шаг 5.

### PIT-175 — в системе может работать вторая вахта одновременно, и это видно только по расхождению версий (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «проверять, не
  занята ли репа» — правило-напоминание, и проверять нечем: признака занятости
  не существует. Растворяет **сужение области работы**: авто-режим берёт репы
  по своему мандату и не трогает чужие без причины.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Вахта закрыла батч в `personal-finance-dss` (`8.30.3`), собрала
  архив — и архив вышел **`8.31.0`**. `VERSION` показывал 8.31.0, `CHANGELOG` —
  8.30.3, хотя `bump_repo.py` пишет одно и то же значение в оба файла подряд,
  с разницей в две секунды.
- **Разгадка.** В репе **одновременно работала вторая сессия Claude**
  (`ps` показал два живых процесса). Она подняла версию до `8.31.0`
  («Навигационный каркас SPA») между двумя командами первой вахты.
- **🔴 Конфликта не возникло, и это важнее самого случая.** Вторая вахта
  **увидела чужую запись и учла её**: в её собственном тексте стоит строка
  *«верхняя запись журнала — 8.30.3 (истекающие сессии, батч другой вахты,
  докс-only)»*. Ритуал выдержал параллельную работу — потому что каждый
  батч атомарен и подписан содержанием, а не порядковым номером.
- **Что сломалось бы при ином порядке.** Если бы первая вахта не остановилась,
  а «починила расхождение» — привела `CHANGELOG` к `VERSION`, — она **стёрла
  бы чужую секцию 8.31.0**, приняв её за свою ошибку. Расхождение выглядит
  ровно как собственный недосмотр (`PIT-G`), и лечится оно противоположным
  действием.
- **Признак, по которому различать.** Собственный недосмотр: в `CHANGELOG`
  **нет** секции с номером из `VERSION`. Чужая работа: секция **есть**,
  и её текст описывает то, чего вахта не делала. Проверяется одним `grep`
  по номеру версии.
- **Практическое правило.** Увидев расхождение `VERSION` / `CHANGELOG`
  в чужой репе — **сначала `grep` по номеру из `VERSION`**, и только если
  секции нет, считать это своим дефектом. Иначе отойти: чужой батч закроется
  сам.
- **Связки:** `PIT-G` (обвязка отстала — тот же симптом, обратная причина),
  `WATCHLOG` §1 (репа ведётся с нескольких аккаунтов по очереди — здесь
  впервые зафиксировано, что «по очереди» бывает нарушено).

### PIT-176 — «63 репы» печатались годами, а каталогов 64: невидимка вместо ошибки (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «помнить про
  finpilot» — правило-напоминание про то, чего не видно. Растворяет вывод
  инструмента: он теперь печатает **три числа** вместо одного — каталогов
  на диске, размечено, вне проверки.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Владелец, прочитав хендофф: *«так сейчас же 64 репы не?
  мы же миграцию делали»*. Проверка: на диске **64** каталога,
  `repo_invariants.py --all` печатает **«63 реп»**, и разница нигде
  не называется.
- **Причина.** Отбор шёл по наличию `.repo-class`; каталог без него
  **не попадал в список** — молча. Таким оказался `finpilot`: витринное
  зеркало по `ADR-009`, публикуемое из другой репы, у которого нет ни
  `.repo-class`, ни `VERSION`, ни `.repo-id`, и это **намеренно**.
- **🔴 Дефект не в том, что зеркало не проверяется, а в том, что об этом
  не сказано.** Репа, выпавшая из отбора, не станет красной — она
  **перестанет существовать** для инструмента. Красное видно, пустоту нет.
- **Тот же класс, что `PIT-172`** (имя каталога говорит откуда, а не что):
  инструмент отвечал на вопрос «что с размеченными репами», а читался
  как «что с системой». Формулировка «63 реп» не лгала — она отвечала
  на другой вопрос.
- **Чем это опасно на длинной дистанции.** Любая новая репа, заведённая
  без разметки — руками, скриптом со сбоем, распаковкой архива, — попадёт
  в ту же слепую зону и не покажется **ни в одной** проверке: ни инвариант,
  ни `closed_campaign_gate`, ни `sync_base_local --all` её не увидят.
  Проверено на живом примере: `finpilot` прожил так с 15.08.2026.
- **Что сделано.** `--all` печатает три числа и **перечисляет неразмеченные
  каталоги поимённо**, с оговоркой: витринному зеркалу так и положено,
  любой другой каталог здесь — пропущенная разметка.
- **Связки:** `PIT-172` (отсутствие в срезе ≠ отсутствие в системе),
  `PIT-169` (закрытая кампания не перепроверяется), `ADR-009` (витринные
  зеркала), `repos-map.md` §продукты.

### PIT-177 — `ls` в этой среде печатает пустоту с кодом 0, и лекарство от `PIT-176` выписано именно им (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «помнить, что
  `ls` тут алиас» — правило-напоминание, а они не работают (`PIT-163`).
  Растворяется заменой инструмента в предписаниях: считать каталоги
  `command ls` / `find`, а лучше — не считать вручную вовсе, потому что
  `repo_invariants.py --all` уже печатает три числа сам (`PIT-176`).
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Первое же действие вахты в `base-repo`: `ls -la` вернул
  **ноль строк**, а `du -sh .` — **15 МБ**. Каталог одновременно пуст
  и занимает пятнадцать мегабайт.
- **Причина.** `ls` — алиас на `eza --icons --group-directories-first`.
  **Без явного пути этот `eza` печатает пустой список.** Замерено в трёх
  каталогах (`base-repo`, `chess`, `/tmp`), версия `v0.23.4 [+git]`:

  | вызов | строк |
  |---|---:|
  | `eza` | **0** |
  | `eza .` | 34 |
  | `eza /Users/…/base-repo` | 34 |
  | `command ls` | 34 |

- **🔴 Отказ бесшумный по всем трём каналам:** код возврата **0**,
  `stderr` **пуст**, `stdout` — законный пустой список. Отличить «каталог
  пуст» от «инструмент не ответил» изнутри вывода **нельзя**. Это ровно
  `71` §7ж — ложный негатив исполнителя, только исполнитель здесь чужой
  и починке не подлежит.
- **🔴 Чем это опаснее обычной поломки инструмента.** Хендофф предписывает
  проверять счёт реп командой `ls ~/repos | wc -l` — это **лекарство от
  `PIT-176`**, дефекта «инструмент молча теряет то, что не подошло критерию».
  Лекарство выписано инструментом **того же класса**: с путём оно отвечает
  верно (64), без пути — молча лжёт. Работает оно не потому, что надёжно,
  а потому, что в формулировке случайно оказался путь.
- **Тот же класс, что `PIT-176` и `PIT-172`:** пустота вместо ошибки.
  Красное видно, пустоту нет. Отличие в том, что здесь пустоту выдаёт
  **инструмент проверки**, а не проверяемое.
- **Что сделано.** Вахта перешла на `command ls` и `find` для всех листингов
  (проверено: 64 каталога, 63 с `.repo-class`). В `00-CLAUDE-STOP.md`
  добавлена строка о среде; предписание в хендоффе заменено на вывод
  `repo_invariants.py --all`, который считает сам и печатает три числа.
- **Чего это НЕ ловит.** Ни один гейт системы не заметит подмену: скрипты
  считают на Python через `pathlib`, алиасы их не касаются. Дефект живёт
  **только в руках вахты** — и потому обнаруживается лишь тем, что вахта
  сверяет вывод с независимым источником. Здесь сверкой оказался `du`.
- **Связки:** `PIT-176` (невидимка вместо ошибки), `PIT-172` (отсутствие
  в срезе ≠ отсутствие в системе), `PIT-163` (правила-напоминания),
  `71` §7ж (ложный негатив исполнителя).

### PIT-178 — правило процитировано в том же абзаце, где нарушено (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «читать
  внимательнее» — правило-напоминание про внимательность, худший их вид.
  Растворяется исполнителем: признак вместо списка + перепись, которая
  находит незаявленное сама (`scripts/runtime_writes.py`, гейт в
  `revision_check.py`).
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** `ADR-007` исключил журнал прогона из отпечатка канона
  **списком из одного пути** — и в соседней строке сослался на `PIT-097`
  («свойство, а не список»). Через двадцать минут владелец запустил
  `/machine`, и оказалось, что `measurements.csv` — второй такой файл.
  Перепись по коду нашла **девять** мест записи. Объявлен был **один**.
- **Причина не в невнимательности.** Список пишется потому, что в момент
  правки известен **ровно один** случай, и признак выглядит преждевременным
  обобщением. Различие между «один известный» и «один существующий» в этот
  момент не видно: чтобы его увидеть, нужна перепись, а её и заменяет список.
- **🔴 Отличие от `PIT-097`.** Тот про то, что список отстаёт. Этот — про то,
  что **цитата правила ощущается его исполнением**. Автор сослался на `PIT-097`
  искренне и был уверен, что следует ему: он назвал причину исключения
  (свойство), но записал перечисление (список). Сходство формулировки
  с правилом заменило проверку соответствия.
- **Что различает эти два состояния — только исполнитель.** Не «помнить про
  признак», а команда, которая находит незаявленное: `runtime_writes.py`
  печатает всё, во что инструменты пишут внутрь раздаваемого, и падает,
  если что-то не объявлено. Ровно поэтому `PIT-163` (правила-напоминания
  не работают) и `PIT-G` (16 повторов) — про одно и то же.
- **Побочная находка того же класса:** сама перепись в первой редакции брала
  только ПРОПИСНЫЕ имена переменных и не видела `pack_release.py`, который
  пишет реестр выпусков в локальную `ledger`. Докстрока обещала «литеральные
  пути», а ловились «литеральные пути в константах» — **незаписанный потолок**
  внутри инструмента, заведённого против незаписанных потолков. Поймано
  снятием объявления и проверкой, что перепись это заметит.
- **Связки:** `PIT-097` (свойство, а не список), `PIT-163` (правила без
  исполнителя), `PIT-G` (артефакт правлен, обвязка нет), `ADR-007` §3,
  `71` §7г-бис (инструмент объявляет, где он врёт).

### PIT-179 — ритуал из шести шагов, состояние знало о трёх: «закрыт полностью» при нероз­данном каноне (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «после батча
  не забудь спросить `sync_base_local --all --check`» — ровно то, что и было
  записано в `STANDARD.md`, и ровно то, что не сработало. Растворено: состояние
  спрашивает раздачу само, цена 3.2 с на 63 репы.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Ритуал базы упёрся в таймаут 2 минуты на шестом шаге. Раздача
  оборвалась: четыре последние по алфавиту репы остались без канона, у одной
  (`style`) `_base/` был скопирован, а штампы не записаны. `close_batch --state`
  ответил: **«батч закрыт полностью (v4.75.0)»**.
- **Причина.** `batch_state()` сверяет три числа — `VERSION`, тег в `LEDGER.tsv`,
  строку в `auto.log`. У базы шагов **шесть**, и шестой — единственный, пишущий
  **за пределы репы**, — в состояние не входил вовсе.
- **🔴 Ложь была в сторону успокоения.** Вахта, спросившая состояние после
  обрыва, получила бы подтверждение, что всё доделано. Шаг, который не
  проверяется **вовсе**, хуже шага, который проверяется плохо: второй хотя бы
  может оказаться красным.
- **Разрыв был ИЗВЕСТЕН и записан.** `STANDARD.md` прямо говорил: «для шестого
  шага есть **отдельный** ответ: `sync_base_local.py --all --check`». То есть
  знание существовало, а исполнителя не было (`PIT-163`, `21` §4г) — и в день
  проверки напоминание не сработало, как и всегда.
- **Что сделано.** `canon_spread()` в `close_batch.py`: различает три исхода —
  роздан · не роздан (с именами) · **проверить не удалось** (недоступный `gh`
  не выдаётся за успех, `71` §7ж). У наследника шага 6 нет — состояние молчит.
- **Чего не ловит:** обрыв между копированием и штампом внутри одной репы
  виден как «нет штампа» и лечится повторной раздачей, но **порча внутри
  скопированного `_base/`** не обнаруживается без `--force` — это отдельная
  задокументированная граница (`test_base_sync.sh`).
- **Связки:** `PIT-163` (правила-напоминания), `PIT-G` (обвязка правится
  последней), `PIT-173` (раздача — последний шаг батча), `71` §7ж.

### PIT-180 — стаб в тесте врал полем, и это было ненаходимо: ветку, ради которой он написан, набор не вызывал (2026-09-03)

- **Разрешить или растворить:** **растворить**. Разрешение — «сверять стабы
  с контрактом» — правило-напоминание. Растворяется вызовом: ветка `--all`
  теперь вызывается в `test_batch_state.sh`, и ложный стаб немедленно красит
  набор.
- **Долг заведён:** `2026-09-03`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Новый тест на состояние батча упорно показывал «канон роздан»
  там, где ни одной репы не было роздано. Отладка: раздача **не видела ни одной
  репы вообще**.
- **Причина.** `private_repo_names()` спрашивает `gh repo list --json
  name,isPrivate` и читает `r.get("isPrivate")`. Стаб `gh` в `test_base_sync.sh`
  печатал `{"name": …, "visibility": "PRIVATE"}` — **другое поле**. Результат:
  `.get("isPrivate")` → `None` → множество приватных **пусто** → раздача молча
  обходит ноль реп и рапортует успех.
- **🔴 Почему это не было найдено раньше.** Набор `test_base_sync.sh` вызывает
  раздачу **по имени репы** (`sync_base_local.py priv-repo`) и **ни разу** —
  ветку `--all`. А `--all` и есть путь шестого шага ритуала, то есть **каждого
  батча базы**. Стаб был заготовлен для ветки, которую никто не звал: ложь
  лежала в наборе, не имея возможности проявиться.
- **Класс: ложная герметичность.** Стаб — это утверждение о контракте внешнего
  инструмента. Неверный стаб не падает — он **тихо меняет условия задачи**,
  и тест продолжает «проходить», проверив пустое множество. Зелёный тест
  на пустом входе неотличим от зелёного теста на настоящем.
- **Тот же корень, что у `PIT-178`:** знание было записано (в коде,
  `--json name,isPrivate`) и не исполнено в соседнем файле. Различает
  их только **вызов**: пока ветка не выполняется, её обвязка не проверяется ничем.
- **Что сделано.** Оба стаба приведены к `isPrivate`; ветка `--all` вызывается
  в `test_batch_state.sh` B1/B2, где проверяются **оба** исхода — и «не роздан»,
  и «роздан».
- **Связки:** `PIT-178` (правило процитировано и нарушено), `PIT-179`,
  `71` §7в (проверка, которую нельзя провалить, — не проверка), `69` §4г.

### PIT-181 — точка входа врала о дате: подъём версии менял номер, но не дату (2026-09-03 21:45)

- **Разрешить или растворить:** **растворить**. Правило «§0 обязан отражать
  текущую точку» существовало в шапке `WATCHLOG` и не исполнялось три недели.
  Растворено кодом: `bump_repo.py` обновляет дату той же операцией, что и номер.
- **Долг заведён:** `2026-09-03 21:45`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Тест V7, заведённый ради **времени** в документах, упал на
  `WATCHLOG` §0: время не появилось. Разбор показал, что дело не во времени —
  **дата там не обновлялась вообще**.
- **Причина.** `write_watchlog()` в ветке «строка уже есть» подменяла ровно
  номер версии (`t[:m.start(1)] + new + t[m.end(1):]`), не трогая остаток
  строки. Поле `**Дата:**` оставалось от **первого** подъёма навсегда.
- **Замер на живых репах в день находки:**

  | репа | §0 | верхняя запись CHANGELOG | расхождение |
  |---|---|---|---|
  | `science` | 2026-08-04 | 2026-09-03 | **месяц** |
  | `chess`, `war`, `style` | 2026-08-27 | 2026-08-29 | два дня |

- **🔴 Чем опасно.** Точка входа — первое, что читает вахта, и по дате она
  судит, насколько состояние свежее. Заполненное поле с неверным значением
  хуже пустого: пустое вызывает вопрос, неверное — доверие.
- **Тот же класс, что `PIT-179`** (состояние знало о трёх шагах из шести)
  и весь батч: **правило записано, исполнителя нет**. Отличие в том, что здесь
  правило нарушал сам инструмент, заведённый его исполнять.
- **Как нашлось.** Побочно. Тест писался на новое требование владельца
  («фиксируй время»), а поймал существующий дефект — потому что проверял
  **результат на диске**, а не то, что задумывалось. Проверка, написанная
  под новое поведение, ловит старое, если смотрит на факт.
- **Чего не ловит:** репы, у которых поля `**Дата:**` нет вовсе — там строка
  написана не нами, и дописывать поле значит менять чужой формат.
- **Связки:** `PIT-179`, `PIT-163` (правило без исполнителя), `PIT-094`
  (точка входа отстала), `00-infrastructure/94` §3.

### PIT-182 — «данных нет» читалось как «диск сломан»: ложная тревога SMART (2026-09-03 22:05)

- **Разрешить или растворить:** **растворить**. Разрешение — «помнить, что
  `.get()` вернёт None» — правило про язык, а не про предмет. Растворено
  условием: None и пустая строка признаны отсутствием данных наравне
  с «недоступно».
- **Долг заведён:** `2026-09-03 22:05`. **Пересмотр:** `2026-12-03`.

- **Симптом.** Канарейка порогов защиты, заведённая проверять FileVault/SIP,
  на первом же прогоне вернула красное — но по другой причине: снимок
  без данных SMART давал тревогу **«SMART диска: None»**.
- **Причина.** `snap["диск"]["smart"].get("состояние") not in ("Verified",
  "недоступно")` — при отсутствующем ключе `.get()` возвращает `None`,
  а `None` в списке исправных состояний не было. Отсутствие данных попадало
  в ту же ветку, что и настоящий отказ накопителя.
- **🔴 Где это стреляет по-настоящему.** `smartctl` — внешняя зависимость
  (`brew install smartmontools`). На машине без него `disks()` не заполняет
  состояние, и `/machine` объявлял бы **отказ диска** на исправной машине.
  Здесь дефект не проявлялся только потому, что `smartctl` установлен.
- **Класс: «недоступно» ≠ «сломано».** Тот же, что «невидимое ≠ красное»
  (`PIT-176`), но зеркальный: там отсутствие данных давало **тишину**,
  здесь — **тревогу**. Оба раза корень один: состояние «не знаю» не было
  предусмотрено, и его молча приписали к одному из известных.
- **Почему ложная тревога дорога.** Она обучает не читать вывод — тот же
  довод, которым обоснован порог «минут тишины» (120 при медиане 13):
  ложная тревога дороже позднего замечания.
- **Как нашлось.** Канарейка проверяла **оба** направления, включая «шума
  быть не должно». Проверь она только «выключенный FileVault ловится» —
  прошла бы зелёной, и дефект остался бы (`71` §7в).
- **Связки:** `PIT-176` (невидимое ≠ красное), `71` §7в (проверка, которую
  нельзя провалить), `71` §7ж (ложный негатив исполнителя).

### PIT-183 — проверяющий приносит свой счёт вместо счёта проверяемого и меряет себя (2026-09-04 01:25)

- **Разрешить или растворить:** **растворить**. Разрешение — «сверяться
  с определением проверяемого» — правило-напоминание. Растворяется тем, что
  инструмент **спрашивает** набор исключений у того, кого проверяет
  (`from pack_release import JUNK_DIRS`), а не копирует его.
- **Долг заведён:** `2026-09-04 01:25`. **Пересмотр:** `2026-12-04`.

- **Симптом.** Дважды за один батч, разными путями, с одинаковым исходом:
  инструмент объявил массовое расхождение там, где расхождения не было.

  | Инструмент | Объявил | На деле |
  |---|---|---|
  | `acceptance_audit.py` | все **19** реп выросли на ~660 файлов | сравнивались два определения «файла в репе»: приёмка считала командой §1.1 (`.git` и `_base`), скрипт — привычным `JUNK_DIRS` |
  | `system_status.heavy_repos()` | **7** реп за потолком 500 МБ | порог про **вес архива**, а мерился **вес на диске**: `self-map` 1110 МБ на диске против **245 МБ** в архиве |

- **Причина одна на оба случая.** Проверяющий инструмент пишется отдельно
  от проверяемого и приносит **свои** привычки счёта — те же, что у соседних
  скриптов. Привычка выглядит нейтральной («ну конечно, `node_modules`
  не считаем»), а на деле определяет измеряемую величину.
- **🔴 Почему это опаснее обычной ошибки в числе.** Ошибка в счёте даёт
  неверное число. Здесь число **верное** — но отвечает на другой вопрос,
  и потому выглядит убедительно: «семь реп за потолком» с точными мегабайтами
  читается как факт. Ложная тревога в семи репах успела превратиться в задачу
  владельцу, прожившую пятнадцать минут.
- **Различающий признак: массовость.** Если инструмент нашёл расхождение
  у **всех** проверяемых сразу — это почти наверняка расхождение определений,
  а не состояния. Мир не меняется одновременно и одинаково; определение —
  меняется.
- **Что сделано.** `acceptance_audit` считает командой `PROCEDURE` §1.1
  и исключает символьные ссылки (`find -type f` их не считает — разница
  ровно 3 файла на `control-panel`). `heavy_repos` **импортирует** `JUNK_DIRS`
  у `pack_release`, а не копирует. Сверено на трёх репах: 4349 = 4349,
  180 = 180, 8751 = 8751.
- **Чего не закрывает:** случаи, где у проверяемого счёт **нигде не записан**.
  Тогда его придётся вывести — и это уже суждение, а не импорт.
- **Связки:** `PIT-178` (список вместо признака — тот же корень: копия
  разойдётся), `PIT-097` (свойство, а не список), `71` §7в.

### PIT-184 — «не делаю» сказано по рассуждению, а проверка заняла три минуты (2026-09-04 00:10)

- **Разрешить или растворить:** **растворить**. Разрешение — «проверять
  перед отказом» — правило-напоминание. Растворяется признаком: **отказ
  о собственной способности требует того же основания, что утверждение
  о состоянии, — команды, которая это показала.**
- **Долг заведён:** `2026-09-04 00:10`. **Пересмотр:** `2026-12-04`.

- **Симптом.** В кит визуала записано: *«про видео честно: не делаю.
  `ffmpeg` соберёт из кадров и `.mp4`, но осмысленное видео требует
  хронометража, звука и монтажа — это другой предмет»*. Владелец спросил:
  *«а видео ты можешь делать тогда мб?»* — и проверка запуском заняла
  **три минуты**, показав обратное:

  | Что | Результат |
  |---|---|
  | видео из кадров | 🟢 MP4, 1.5 с |
  | синтез звука | 🟢 WAV |
  | **русская речь** | 🟢 `say -v Milena`, голос в системе есть |
  | видео + речь | 🟢 16 843 байта |

- **Причина.** Отказ выведен **рассуждением о сложности задачи**
  («монтаж — другой предмет»), а не проверкой инструмента. Рассуждение
  было правдоподобным и потому не вызвало сомнения: осмысленное кино
  действительно требует монтажа — но вопрос был не про кино.
- **🔴 Класс: отказ о себе не проверяется так же строго, как утверждение
  о мире.** Система требует команды под каждым утверждением о состоянии
  диска. Утверждение «я этого не умею» той же проверки не проходило —
  хотя это ровно такое же утверждение о состоянии, только состояние —
  набор инструментов на машине.
- **Чем это дороже обычной ошибки.** Неверное «да» проверяется первым же
  запуском и чинится. Неверное «нет» **закрывает направление молча**:
  никто не пробует, потому что уже сказано, что не выйдет. Владелец здесь
  переспросил — но правило не может опираться на то, что переспросят.
- **Различающий признак:** отказ, сформулированный через свойство ЗАДАЧИ
  («это требует монтажа», «это слишком сложно»), а не через свойство
  ИНСТРУМЕНТА («`ffmpeg` вернул такую ошибку»). Первое — рассуждение,
  второе — факт.
- **Что сделано.** §5в кита: полная цепочка кадры → видео → речь → сборка,
  четыре проверки запуском, два молчаливых дефекта названы (`-pix_fmt
  yuv420p` иначе чёрный экран, `scale=…:-2` иначе нечётная высота).
  Прежнее утверждение исправлено с указанием, что оно было неверным.
- **Связки:** `71` §7ж (ложный негатив исполнителя), `PIT-183`
  (проверяющий меряет себя), `/auto` §1.1 (сначала факт с диска).

### PIT-185 — переменная теста столкнулась с переменной проверяемого скрипта через окружение (2026-09-04 05:20)

- **Разрешить или растворить:** **растворить**. Разрешение — «выбирать имена
  осторожнее» — правило про внимательность. Растворяется префиксом: переменные
  теста, передаваемые в окружение, носят `T`-префикс (`TCASE`, `TZONE`),
  и столкновение становится невозможным по построению.
- **Долг заведён:** `2026-09-04 05:20`. **Пересмотр:** `2026-12-04`.

- **Симптом.** Заведён фильтр прогона тестов деплойера: `ONLY=A1` — один кейс,
  `ZONE=C` — зона. Кейс `A1` при фильтре упал **семью проверками из семи**,
  при полном прогоне — зелёный.
- **Причина.** 🔴 **`ONLY` — переменная самого `deploy.sh`** («обработать
  только эти репы»). Тест её экспортировал, деплой прочитал и стал искать репу
  с именем `A1`, которой нет. В выводе: «архивов к разбору: 1», следом
  «публиковать нечего» — **без единой строки о причине**.
- **Почему искалось долго.** Отказ выглядел как две другие вещи, обе
  правдоподобные: «фильтр сломан» и «набор не изолирован». Второе к тому же
  **оказалось правдой** — но для других кейсов, и это уводило ещё дальше.

  Проверены и отвергнуты: регулярка отсева дублей · порог «файлов < 5» ·
  `set -e` в guard · зависимость от порядка кейсов. Каждая проверка была
  осмысленной и ни одна не вела к причине.
- **🔴 Чего не видит ни один инструмент.** `bash -n` столкновения имён
  не ловит — синтаксис верен. `grep` по тесту не находит — вторая половина
  столкновения **в другом файле**. Проверка `examples_check.py` ловит
  несуществующие флаги, а не совпадающие имена переменных.
- **Правило.** Переменная окружения, которую тест передаёт проверяемому
  скрипту, обязана быть **проверена на столкновение с его собственными
  ключами** — то есть `grep` по документации и коду скрипта, а не только
  по тесту. Дешевле — префикс, снимающий вопрос целиком.
- **Побочная находка того же прогона:** набор тестов **не изолирован
  по кейсам** — `A2` читает каталог, созданный в `G1`. Замер: `TZONE=A` →
  2 падения, `TZONE=AG` → 92 прошло, ноль. Фильтр **сделал видимой**
  зависимость, невидимую при полном прогоне, где порядок всегда один.
- **Связки:** `PIT-183` (проверяющий приносит своё определение), `71` §7в
  («работает, потому что множество состоит из одного элемента»), `PIT-039`.

---

## `PIT-186` · Переименование доехало до диска и не доехало до тех, кто на него ссылается

- **Долг заведён:** `2026-09-04 07:05`. **Пересмотр:** `2026-12-04`.

- **Симптом.** Опись кодов восстановления — **единственная опись, что есть** —
  вела в `~/Documents/recovery_kits/`. Такого каталога на диске нет.
- **Причина.** Каталог переименован кампанией единого нейминга **03.09.2026**
  (`recovery_kits` → `recovery-kits`, snake → kebab). Переименование выполнено
  верно и записано в отчёт. 🔴 **Четыре живых документа, ссылавшихся на старое
  имя, никто не тронул** — включая сам индекс в `misc-vault`.
- **Чем это опаснее обычной битой ссылки.** Ссылка внутри репы ловится
  `refcount.py`: цель либо есть, либо нет. Ссылка **наружу**, на файловую
  систему, не проверялась ничем — при том что уводит она ровно к тому, чего
  в git нет по правилу: секретам, фото, тяжёлым оригиналам.

  > **Опись, ведущая по несуществующему пути, хуже отсутствия описи:**
  > отсутствие заставляет искать, ложный путь убеждает, что искать негде.

- **🔴 Первое решение было неверным, и это главная часть карточки.** Написана
  проверка «каждый путь `~/…` в документах обязан существовать». Запуск дал
  **66 находок в базе и ноль дефектов**. Все законны по трём причинам:

  | Форма | Пример |
  |---|---|
  | команда, которую надо ВЫПОЛНИТЬ | `brew bundle dump --file=~/Documents/Brewfile` — файл появится после |
  | констатация отсутствия | «`~/.cursor` — Cursor на машине нет, остаток удалённой» |
  | план | «убрать `~/Downloads/vk-graph.env.backup`» верно ровно тогда, когда файла уже нет |

  Разница между «указатель протух» и «так и задумано» лежит **в намерении
  автора, и машине оно не видно**. Соблазн был настроить фильтры, пока
  не станет зелено, — это подгонка проверки под ответ: гейт бы позеленел,
  а дефект остался бы неотличим от шума.
- **Правило.** Внешнее хранилище **объявляется** — `EXTERNAL-STORES.tsv`
  в корне репы, три колонки через TAB: путь · что там · какой документ
  указывает. Проверяет `home_paths_check.py`: каталог есть · указатель есть ·
  **указатель называет этот же путь**. Ложных срабатываний нет по построению:
  проверяется ровно то, что кто-то назвал хранилищем. Тот же приём, что
  `.runtime-writes` — декларация вместо списка из головы проверяющего.
- **Побочная находка сверки.** `proton-recovery-phrase (finpilot.support).pdf`
  от 23.08 лежал **в корне** хранилища и в описи отсутствовал — фраза
  восстановления аккаунта, ушедшего в тот же день в suspended. Файла, которого
  нет в описи, для описи не существует. 🔴 Гейт этого не ловит и не будет:
  полнота описи содержимым — работа сверки, а не проверки.
- **Своя же ошибка в том же заходе.** Вывод «менеджера паролей на машине нет»
  сделан по `ls /Applications | grep -i pass` — а приложения владельца разложены
  **по подпапкам-категориям**, и Bitwarden 2026.3.1 лежал в
  `/Applications/privacy:cybersecurity/`. Решение чуть не строилось на неверном
  факте. Тот же класс: команда ответила на свой вопрос, а не на заданный.
- **Связки:** `PIT-176` (недостижимый документ), `PIT-177` (`ls` — алиас
  на `eza`, пустой вывод как «ничего нет»), `PIT-097`/`PIT-178` (свойство,
  а не список), `/auto` §7 (отмена, дошедшая не всюду).

---

## `PIT-187` · Проверка отвечала на свой вопрос, а не на заданный — три случая за час

- **Долг заведён:** `2026-09-04 09:10`. **Пересмотр:** `2026-12-04`.

- **Симптом.** За один час работы над автозаписью на донацию три проверки
  подряд дали **уверенные и ложные** ответы. Ни одна не упала: все вернули
  результат, и все три были неверны.

  | Проверка спрашивала | А надо было | Что вышло |
  |---|---|---|
  | «есть ли в DOM `input[type=password]`» | «видит ли пользователь форму входа» | 🔴 разлогин объявлен **залогиненному** владельцу |
  | «готов ли документ» (`readyState`) | «пришёл ли **новый** документ» | 🔴 читалась **уходящая** страница |
  | «есть ли кнопка и не `disabled` ли она» | «куда эта кнопка **ведёт**» | 🔴 дата-заглушка считалась доступной |

- **Причина, общая для трёх.** Вопрос подменялся **ближайшим механически
  проверяемым признаком**. Подмена незаметна, потому что признак связан
  с предметом: поле пароля действительно бывает на форме входа, `readyState`
  действительно про готовность, кнопка действительно бывает у доступной даты.

  🔴 **Связь есть, эквивалентности нет.** В кабинете лежат **три скрытых**
  поля пароля (модалка «Изменить данные»); `reload()` асинхронен и оставляет
  старый документ готовым; кнопка есть у **всех** дат, а ведёт у большинства
  в `#modal-forbidden`.

- **Почему опаснее падения.** Упавшая проверка молчит и заметна. Эта —
  **отвечает уверенно**: «ты не залогинен», «страница готова», «дата
  доступна». Владелец написал прямо: *«он тупит, он пишет я не залогинен,
  хотя я только что залогинился»* — то есть дефект нашёл человек, а не гейт.

- **Правило.** Формулировать проверку **от наблюдаемого владельцем факта**,
  а не от удобного технического признака:

  | Вместо | Спрашивать |
  |---|---|
  | элемент существует | элемент **видим** (`getBoundingClientRect` > 0) |
  | состояние «готово» | состояние **сменилось** (метка поколения) |
  | действие доступно по виду | **куда ведёт** (`data-src`, `href`, обработчик) |

  И судить по **положительному** признаку, где он есть: «виден кабинет»
  надёжнее, чем «не видно формы входа», — отсутствие признака имеет
  много причин, наличие одну.

- **Связки:** `PIT-177` (`ls` — алиас на `eza`, пустой вывод как «ничего
  нет»), `PIT-186` (указатель ведёт в переименованный каталог), `71` §7ж
  («не вышло» против «не пробовал»), `100-browser-automation.md` §4.
