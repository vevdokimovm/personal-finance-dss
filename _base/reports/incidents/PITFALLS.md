# PITFALLS — лидерборд повторяющихся дефектов

> **Зачем этот файл, а не ещё один репорт.** Разборы отдельных случаев уже есть
> (`unreachable_code_pattern.md` и другие). Проблема в том, что репорт читают один раз
> и он тонет: следующий похожий баг совершается снова, потому что в момент правки кода
> никто не открывает архив разборов.
>
> Здесь другое: **короткий список классов с счётчиком повторов**, отсортированный по частоте.
> Смотреть перед каждой правкой поведения. Растёт счётчик — значит класс не побеждён,
> и нужна не ещё одна заплатка, а механическая проверка.

> ### 🔴 Этот файл существует в НЕСКОЛЬКИХ копиях, и они уже разошлись
>
> Измерено 21.08.2026: тот же лидерборд лежит в
> `dota-dossier/reports/infrastructure/PITFALLS.md` — с теми же классами `PIT-A…F`
> и теми же счётчиками, **но без `PIT-G`**, заведённого здесь.
>
> **Счётчик, размноженный по репам, перестаёт считать.** Класс сработал в одной репе,
> счётчик вырос там — в остальных копиях он молчит, и следующая вахта в другой репе
> видит «2 повтора» вместо пяти. Это ровно тот дефект, который лидерборд и должен ловить.
>
> **Канон — этот файл.** Копии сводить отдельной задачей (`ROADMAP.md` §P2),
> не по ходу другой работы.
>
> Смежное — реестр **случаев** `reports/pitfalls.md` (`PIT-001…NNN`): другая природа,
> другая шкала, не путать.

**Правило ведения:** поймал дефект → нашёл его класс в списке → **увеличил счётчик**
и дописал случай одной строкой. Класса нет → добавил с счётчиком 1. Счётчик не сбрасывается
никогда: он про историю, а не про текущее состояние.

### 🔴 Лестница эскалации: счётчик обязан что-то запускать

Формулировка выше — «растёт счётчик, значит нужна механическая проверка» — верна и
**не исполняется**, потому что не называет, **при каком числе** и **что именно**.
Порог, взятый из `personal-finance-dss` (`tools/preflight.py`, правило эскалации §1):

| Счётчик | Что обязано появиться |
|---|---|
| **1** | разбор случая: симптом, корень, правило |
| **2** | правило переносится в **точку применения** — шапку документа, чек-лист закрытия, шаблон. Не в общий свод |
| **3** | 🔴 **проверка кодом, а не текстом.** Гейт, хук, тест — что-то, что запускается само |
| **4+** | проверка существует и **не сработала** → разбирается **она**, а не исходный дефект |

**Принцип за порогами — тип контрмеры выбирается по числу рецидивов, а не по тяжести
ошибки.** Инстинкт обратный: чем страшнее последствие, тем серьёзнее меры. Но тяжесть
говорит о цене **одного** случая, а рецидив — о том, **что именно не работает**.

> «Если проза не сработала дважды, **третий текст не сработает тем более**.»

Доказательство из репы-источника — таблица, где у каждой строки разбор **существовал**
и ошибка всё равно повторилась: готовый рецепт в пост-мортеме · точно описанная карточка
граблей · записанный урок. Вывод оттуда:

> **Разбор, который надо вспомнить и открыть, не является мерой.** Он работает как
> объяснение постфактум и не работает как предохранитель.

> **«Существует потому, что проза не работает.»** Формулировка автора того инструмента,
> и она точна: реестр рецидивов фиксирует случаи, где разбор **был написан**, правило
> **было сформулировано**, и ошибка всё равно повторилась. Третий повтор — это доказательство,
> что текст исчерпан как средство.

**Как это читать по текущему лидерборду.** Классы с счётчиком **3 и выше** обязаны иметь
названного исполнителя — гейт или хук — и **имя этого исполнителя пишется в карточке класса**.
Класс с счётчиком 4+ без исполнителя — это не «известная проблема», а **невыполненная работа**,
и она видна прямо из таблицы.

**Ступень 4+ важна отдельно.** Когда проверка уже поставлена, а класс повторился снова —
чинить надо **проверку**: она либо не запускается (`69` §4в), либо запускается и падает
незаметно (§4г), либо проходится случайно (`71` §7в). Ставить вторую проверку поверх
неработающей первой — самый частый способ удвоить обвязку и не изменить исход.



---

## 🥇 PIT-A · Код недостижим в состоянии пользователя — **7 повторов**

**Симптом:** «сделано, покрыто тестом, у пользователя не работает».

**Механизм:** шаг написан под переходное состояние (публикуем сейчас, ассета нет, архив
на месте), а пользователь почти всегда в устоявшемся (уже опубликовано, архив удалён).
Три подвида: мёртвая ветка · порядок объявлений · флаг, который никто не ставит.

**Проверка перед выпуском:** сработает ли это на **втором** запуске подряд, когда входных
данных для шага уже нет, а результат его прошлой работы всё ещё лежит на диске?

**Механическая защита:** утверждение о состоянии после второго прогона обязательно
(`tests/README.md`), плюс инвариант «последнее объявление функции до `# --- ШАГ 1`».

| # | Случай | Дата |
|---|---|---|
| 1 | `DROP_LEGACY_ASSETS` в ветке «ассета нет» — у обработанных релизов мертва | 29.07 |
| 2 | `VERIFY` вызывал функцию раньше объявления → всегда «ДЕРЖАТЬ» | 29.07 |
| 3 | `DELETE_AFTER` в ветке публикации — опубликованное раньше не убиралось | 29.07 |
| 4 | Автоудаление за флагом, который владелец не ставит | 29.07 |
| 5 | Зеркала искались по списку архивов прогона | 29.07 |
| 6 | Уборка не выполнялась на раннем выходе «архивов нет» | 29.07 |
| 7 | `cleanup_mirrors` вызывалась до создания рабочей папки → `set -u`, код 1 | 30.07 |

Разбор: `unreachable_code_pattern.md`.

---

## 🥈 PIT-B · Различие окружений: стенд ≠ боевая машина — **4 повтора**

**Симптом:** зелёные тесты, падение у пользователя на первой же команде.

**Механизм:** тесты идут в **bash на Linux**, скрипт запускают в **zsh на macOS**.
Различия синтаксиса и утилит тестами не ловятся в принципе.

**Проверка перед выпуском:** использую ли я glob, `xargs`, `tr`, `sed -E`, `cut`, `date`,
`stat`? Если да — проверить поведение в zsh и в BSD-версии, а не только в bash/GNU.

**Механическая защита:** `setopt no_nomatch` в шапке (снимает фатальность globs в zsh);
зона `I` в тестах; правило «первый прогон на новых данных — `DRY=1`».

| # | Случай | Дата |
|---|---|---|
| 1 | `cut: Illegal byte sequence` — BSD против не-UTF-8 имён | 28.07 |
| 2 | `xargs -d` — ключа нет в BSD | 29.07 |
| 3 | `tr '\0'` — BSD требует `\000` | 29.07 |
| 4 | Несовпавший glob в zsh — фатальная ошибка, скрипт оборвался | 30.07 |

---

## 🥈 PIT-G · Артефакт правлен, его обвязка — нет — **17 повторов**

**Симптом:** «я это починил» — а у владельца по-прежнему старое поведение.

**Механизм.** У каждого живого артефакта есть **обвязка**: копии на диске, журнал версий,
спека, число в прозе, строка в карте. Правка самого артефакта ощущается как завершённая
работа, потому что результат виден **у себя**; обвязка не даёт обратной связи вообще,
и её пропуск не ощущается никак.

**Повторы (счётчик не сбрасывается):**

1. **2026-07/08 — деплойер откатили на 13 версий.** Копия в базе три недели стояла
   на 4.3.2, пока рабочий скрипт ушёл до 4.16.1, и документация в четырёх местах
   называла каноном именно базу. Разбор — `deployer_downgrade_incident.md`, PIT-063.
2. **v1.87.0 — тело README врало шестью числами.** Блок статуса под гейтом был свеж,
   и именно это создавало ощущение, что защищён весь файл.
3. **v1.89.0 — два раздела `## 4а`** в `69-agents-hooks-and-gates.md` при живых ссылках
   на обе нумерации. Починили поштучно, класс не подмели — вернулось в `65` через пять версий.
4. **2026-08-21 — правка `deploy.sh` 4.17.1 не доехала до владельца.** Канон
   `templates/deploy.sh` правлен, тест написан — а `deploy-CHANGELOG`, `deploy-SPEC`
   и **копии на диске** остались на 4.17.0. Владелец запустил `~/Downloads/deploy.sh`
   и увидел ровно тот дефект, который был «починен». Ритуал `00-CLAUDE-STOP` §Четвёртое
   описан подробно, гейт `deploy_version_gate.py` существует — **и не был запущен**.
5. **2026-08-25 — v3.5.0: VERSION и CHANGELOG подняты, README-витрина осталась на v3.4.0.**
   Ход завершался «версия поднята, архив собран» — а блок статуса README (первое, что видит
   владелец) до Stop-хука никто не перечитал. Гейт `revision_check.py` поймал сам:
   `README.md: блок статуса не назвал v3.5.0 — витрина отстала от VERSION (PIT-116)`. Починено
   `readme_status_gate.py --fix` после ручной правки текста (гейт версию/дату проставляет,
   описание — нет), архив пересобран `pack_release.py` уже с CLEAN-деревом.
6. **2026-08-25 — v3.6.0: тот же дефект повторился в ТОЙ ЖЕ сессии, следующим батчем.**
   VERSION/CHANGELOG/WATCHLOG подняты до 3.6.0, архив собран — README снова остался
   на предыдущем номере (3.5.0). Гейт поймал сам, тем же сообщением PIT-116, ~15 минут
   спустя после починки случая 5. Значит однократная починка в моменте **не встраивается
   в привычку** — обвязка правится последней и в следующем батче забывается заново,
   даже когда предыдущий пропуск только что стоил внимания. Починено тем же путём.
7. **2026-08-25 — конвенция «бриф авто-режима в `~/Downloads`» правилась только в `SKILL.md`,
   остальные три копии молчали.** Владелец потребовал: мандат живёт **внутри репы**
   (ROADMAP/TASKS/mission-control), не в Downloads. Исправил адрес поиска в
   `.claude/skills/auto/SKILL.md` §2 — и на этом чуть не остановился. `grep` по всей базе
   нашёл ту же неверную конвенцию ещё в трёх местах: `06-autonomous-mode-kit/bin/night.sh`
   (default-путь **и** комментарий к нему отдельно) и `RUNS.md` (описание умолчания).
   Ни один из них не всплыл бы в `revision_check.py` — гейт проверяет числа и ссылки,
   не совпадение путей в прозе между документами. Правка одного канона без `grep` по
   остальным — это ровно то, чем PIT-G и является.
8. **2026-08-26 — v3.38.0: VERSION/CHANGELOG/WATCHLOG подняты (починка `PIT-147`),
   `pack_release.py` не запущен следом в том же ходе.** Между правкой канона и сборкой
   архива легло ожидание фонового `revision_check.py` (CLEAN) — и на этом ход
   остановился без финального шага. Stop-хук поймал сам: `VERSION=3.38.0, но архива
   ~/Downloads/base-repo-v3.38.0.zip нет и тега на GitHub тоже нет`. Заодно вскрылась
   ещё одна жертва того же `PIT-147` — `.repo-id` тоже пропал из корня, но не был
   замечен при первой проверке (сверял только `VERSION`/`WATCHLOG.md`, не весь список
   обязательных файлов); нашёл его отсутствие сам `pack_release.py`, отказавшись
   собирать архив. Урок: после инцидента с потерей файлов сверять **весь** список
   обязательных корневых файлов разом (`diff` против последнего целого снимка), а
   не по одному по мере всплытия — тот же паттерн, что случай 7 (грепать все места
   сразу, не чинить по одному, пока не поймает гейт).
9. **2026-08-26 — v3.41.0: следующий батч в той же сессии, тот же дефект.** VERSION/
   CHANGELOG/WATCHLOG подняты (закрытие ROADMAP-пункта про модель/усилие по классам
   реп), `pack_release.py` снова не запущен следом — фоновый `revision_check.py`
   опять лёг между правкой и сборкой, ход остановился без финального шага. Stop-хук
   поймал сам, тем же сообщением, что случай 8, в тот же час. Значит починка случая 8
   («записать урок») не встроилась в привычку за один батч — тот же вывод, что уже
   был у случаев 5→6 («однократная починка в моменте не встраивается в привычку»).

10. **2026-08-26 — v3.52.0: VERSION/CHANGELOG/WATCHLOG подняты (закрытие кампании
    `_heavy-originals` + находка `PIT-149`), `pack_release.py` не запущен следом.**
    Между правкой и сборкой легло большое, не связанное с батчем отвлечение —
    диагностика зависания `personal-finance-dss` и перенос всего дерева репов из
    iCloud, начатые в том же ходе. Ход продолжился на несколько тем вперёд, обвязка
    осталась висеть. Stop-хук поймал сам, тем же сообщением, что случаи 8–9, плюс
    впервые заодно поймал `PIT-091` (число карточек PIT в README отстало на одну —
    132 вместо 133) и `PIT-116` (§0 разросся до 89 строк при потолке 80). Урок,
    отличный от случаев 8–9: здесь причина не «гейт лёг между правкой и сборкой»,
    а **посторонняя работа внутри того же хода, начатая до завершения ритуала
    закрытия батча.** Правило на будущее: смена темы (даже по прямой просьбе
    владельца) — не повод откладывать `pack_release.py` для уже поднятой версии;
    закрывать ритуал ДО переключения, а не «на потом».

11. **2026-08-27 — v3.61.1: VERSION/CHANGELOG/WATCHLOG/README подняты (фикс
    `pack_release.py` node_modules-исключения), `pack_release.py` для самой базы
    не запущен.** Причина — масштаб отвлечения хуже случая 10: между правкой базы
    и её собственной сборкой легла volume-работа на 30+ других репозиториях
    (мега-волна MVP: 28 доменных реп до v1.0.0, каждая со своим `pack_release.py`).
    Внимание было полностью на чужих архивах — «архив собран» ощущалось верным,
    потому что архивы **действительно** собирались, просто не архив самой базы.
    Stop-хук поймал сам, тем же сообщением, что случаи 8–10. Урок, отличный от
    случая 10: там отвлечение было на **одну** посторонюю тему, здесь — на серию
    из тридцати однотипных действий над **другими** репами, которая создавала
    иллюзию «я как раз этим и занимаюсь, обвязка идёт» — хотя базы среди тех
    тридцати не было.

12. **2026-08-27 — v3.64.0: серия правок `deploy.sh` (4.21.1-4.21.3) в ответ на
    живую жалобу владельца в реальном времени, `pack_release.py` для базы не
    запущен трижды подряд внутри одной серии.** Контекст отличается от случаев
    8-11: здесь не было отвлечения на другую тему — было **давление срочности**:
    владелец активно ждал, каждая правка синхронизировалась в `~/Downloads/
    deploy.sh` немедленно (это правильно, это критичная обвязка), но параллельная
    обвязка самой базы (VERSION/CHANGELOG/WATCHLOG базы, архив `pack_release.py`
    для base-repo) откладывалась «на потом» три раза подряд, потому что «потом»
    означало «после того как перестану быть нужен прямо сейчас» — а нужен
    оставался всю серию. Stop-хук поймал сам, тем же сообщением. Урок: срочность
    пользователя — законная причина откладывать необязательную работу, но
    обвязка базы **не необязательная** (PIT-G и есть про это) — правило нужно
    применять особенно внимательно именно в моменты, когда кажется, что сейчас
    не время для ритуала.

13. **2026-08-30 — v4.44.0 закрыта, а журнал кампании смолчал о новых сутках.**
    Класс здесь в чистом виде: правка (ADR-006, модуль диаграмм, разбор второй
    выгрузки) сделана и проверена, а **обвязка — след в журнале кампании
    синтеза** — отстала. Причина ровно та, что описана в механизме: артефакт
    даёт обратную связь сразу (гейт зелёный, тесты проходят), журнал кампании
    не даёт никакой.

    🔴 **Отличие от предыдущих двенадцати: сменилась дата.** Работа шла
    непрерывно с 29 на 30 августа, и в журнале кампании запись за 29-е стояла.
    Ощущение «журнал в порядке» было верным вчера и стало неверным
    в полночь — **без единого действия с моей стороны**.

    Урок: обвязка бывает привязана не к правке, а к **времени**. Такой пропуск
    нельзя заметить, перечитывая свою работу, — только запуском проверки.
    Stop-хук поймал сам, четвёртый раз подряд именно он.

14. **2026-09-02 — отчёт исследования записан без раздела о процессе.**
   Заведён `reports/research/torrents-explained-2026-09-02.md`: содержание
   полное, источники расставлены, противоречия названы — а **обязательного
   раздела «Как это исследовалось» нет**. Правило записано в
   `templates/RESEARCH_REPORT_TEMPLATE.md` и проверяется гейтом
   `check_research_reports`; гейт **не был запущен перед завершением хода**.

   🔴 **Отличие от четырёх предыдущих случаев:** обвязкой здесь оказался
   не файл-спутник, а **раздел внутри того же файла**. Правка ощущалась
   завершённой, потому что содержание было готово — а недостающее было
   не рядом, а внутри, и потому невидимо при взгляде «файл написан».

   Поймано **Stop-хуком**, а не мной: проверка запускается сама именно
   потому, что четырежды пропускалось не знание правила, а его применение.

15. **2026-09-02 — репа `migration` заведена без `_base/`; найдено владельцем.**
    Дословно: *«ты не положил в репу миграции базу почему то. гейта на этого
    нет что ли?»*. Единственная из 63 реп простояла шесть часов без канона.

    🔴 **Гейт был, и он честно отказывал** — в этом отличие от случаев 4–13,
    где проверку просто не запускали. `new_repo.py` звал раздачу канона, а
    `sync_base_local.py` её отклонял: предохранитель приватности считал
    «репы нет в списке `gh repo list`» = «репа публичная». У репы, ТОЛЬКО ЧТО
    заведённой на диске, состояние третье — GitHub про неё ещё не знает.
    Отказ выглядел как срабатывание защиты, а был ложноположительным.

    Первое объяснение вахты («никто не запустил гейт») тоже оказалось неверным
    и держалось два часа — пока не был написан тест жизненного цикла
    (`tests/test_repo_lifecycle.sh`), который воспроизвёл отказ в песочнице.

    **Починено тремя правками, каждая закрыта тестом:**
    · `sync_base_local.py` — источник правды о приватности перенесён
      в `.repo-meta` (его пишет сам конструктор и он же идёт в `gh`);
      `gh` остаётся авторитетом для реп, которые там уже есть;
    · `new_repo.py` — **сильная гарантия**: нарушен инвариант → каталог
      откатывается целиком. Половина объекта хуже его отсутствия: отсутствие
      видно сразу, а половина мимикрирует под целое;
    · `repo_invariants.py` — единый список инвариантов вместо четырёх гейтов,
      каждый из которых знал свой кусок правды и ни один — целое.

    **Урок, отличный от всех предыдущих случаев класса.** Раньше PIT-G звучал
    как «запусти проверку, которая есть». Здесь проверка запускалась и врала.
    Значит одного лишь наличия гейта недостаточно: **гейт сам обязан быть под
    тестом**, иначе он становится источником ложной уверенности — худшим, чем
    его отсутствие. Конструктор и деструктор реп до 02.09.2026 не имели тестов
    вовсе, при том что именно они создают объекты для всей остальной системы.

16. **2026-09-03 — версия базы 4.66.0 поднята, архив НЕ собран. Вахта решила
    так НАМЕРЕННО, и в этом всё отличие от случаев 1–15.**

    Все прошлые пятнадцать раз обвязка отставала по недосмотру: шаг забывали,
    гейт не запускали, отвлекались. **Здесь вахта решила не собирать архив** —
    сославшись на собственную же классификацию
    `12-workstation-kit/LOAD-CLASSES.md`, где `pack_release.py` отнесён
    к классу C, «не запускать при игре владельца».

    Довод выглядел взвешенным: владелец играл, часом раньше вахта уже уронила
    ему FPS с 60 до 5 (`PIT-171`), осторожность казалась уместной.

    🔴 **Ошибка не в осторожности, а в том, ЧТО было отложено.** Отложить можно
    работу; **закрытие батча отложить нельзя** — оно и есть граница между
    сделанным и полусделанным. Версия без архива — репа, которая **выглядит
    выпущенной, не будучи ею**, то есть ровно тот дефект, что во всех
    пятнадцати предыдущих случаях, только достигнутый рассуждением,
    а не забывчивостью.

    **Третье решение вахта не увидела:** не поднимать версию вовсе, пока
    нельзя закрыть батч. Подъём версии и выпуск — одна операция, и разрывать
    её нельзя ни по какой причине.

    **Stop-хук заблокировал ход и оказался прав против рассуждения вахты.**

    🔴 **Довод был вдобавок фактически неверен.** Замер сразу после
    разблокировки: `pack_release.py` для базы (600 файлов, 4.5 МБ) —
    **2.9 секунды, `load average` не изменился ни на сотую** при Доте на 321 %.
    Класс C был присвоен **команде**, а не команде с данными: та же команда
    для `portrait-of-taste` (2203 файла, 396 МБ) дороже в сто раз. Исправлено
    в `LOAD-CLASSES.md` §4б.

    **Урок, которого не было в прошлых пятнадцати случаях:** правило,
    заведённое вахтой для себя, становится основанием пропустить ритуал.
    Чем разумнее правило, тем убедительнее исключение, которое оно оправдывает.
    **Ритуал закрытия батча не подчиняется другим правилам — он их граница.**

**Почему заплатки не работают.** Все двенадцать раз правило существовало и было записано.
Отказывает не знание, а **момент**: обвязка правится последней, когда работа уже
ощущается сделанной, и ровно тогда внимание кончается.

**Механическая защита:**

- `scripts/deploy_version_gate.py` — версия скрипта против журнала, спеки и **копий**.
- `scripts/revision_check.py` → `check_prose_counts`, `check_section_dupes`,
  `check_campaign_log`, `check_allowlist_rot`.
- 🔴 **`.claude/hooks/ritual-gate.sh`** (2026-08-21) — Stop-хук: не даёт закончить ход,
  если артефакт правлен, а обвязка отстала. Заведён именно потому, что все проверки
  выше **надо не забыть запустить**, а хук запускается сам.

**Признак применимости класса:** у правки есть *производные* — копия, число, запись
в журнале, строка в карте. Есть производные — есть этот дефект.

---


- **2026-09-03 23:45 — случай 17. 🔴 Класс поймал сам себя: сторож этого класса и оказался
  нарушителем.** Артефакты системы переехали `~/Downloads` → `~/Developer`. Переведены
  на общий источник пути пять скриптов Python, `deploy.sh` (v4.24.0 с полным ритуалом),
  тест и спека. **Хук `ritual-gate.sh` — нет:** он остался искать `~/Downloads/base-repo-v*.zip`,
  не нашёл при существующем архиве и **заблокировал ход** сообщением «архива нет».

  Отличие от шестнадцати предыдущих: обвязку не «отложили на потом» — её **не увидели
  в списке потребителей**. Перепись делалась `grep` по `scripts/` и `templates/`,
  а `.claude/hooks/` в неё не входил. Список мест оказался короче множества мест —
  ровно `PIT-178`, но применительно к переписи, а не к исключениям.

  🔴 **Цена именно ложной тревоги.** Не молчание, а блок с неверной причиной: ход
  потрачен на разбор пустого, и такой сигнал обучает не читать вывод — дороже, чем
  пропущенное замечание. Починено переводом хука на ту же `BASE_ARTIFACTS`, что читают
  Python и shell: три способа переопределить путь стали одним.

## 🥉 PIT-C · Отсутствие данных подменяется правдоподобным результатом — **3 повтора**

**Симптом:** отчёт об успехе там, где данных не было.

**Механизм:** вместо отказа подставляется заглушка или игнорируется код возврата.
Плохой артефакт выглядит готовым и потому живёт годами; отсутствующий чинится за минуту.

**Проверка:** что происходит, когда данных для шага нет — отказ или «что-нибудь»?

| # | Случай | Дата |
|---|---|---|
| 1 | Заглушка «Синхронизация дерева» вместо описания → 6 пустых релизов | 28.07 |
| 2 | `cp` упал по нехватке места, код возврата не проверен → залит обрезанный ассет | 29.07 |
| 3 | Пустая папка подана как «ОШИБКА» с ненулевым кодом (обратный случай: успех как отказ) | 29.07 |

---

## PIT-D · Сравнение имён как регулярного выражения — **2 повтора**

**Симптом:** проверка «этот файл уже есть» ложно срабатывает.

**Механизм:** имя файла подставляется в `grep` как шаблон. Точка в версии матчит любой
символ: `v1.0.1` совпадает с `v1_0_1`.

**Проверка:** сравнение имён только буквальное — `grep -F`, `[ "$a" = "$b" ]`.

| # | Случай | Дата |
|---|---|---|
| 1 | Канон «найден» в легаси-ассете → загрузка пропущена, легаси удалён, релиз без ассета | 29.07 |
| 2 | Та же ловушка в `VERIFY` (копия логики) | 29.07 |

---

## PIT-F · Маркер взят из вложенной репы, а не из корня — **2 повтора**

**Симптом:** архив опознан как другая репа; проверка «свой/чужой» срабатывает ложно.

**Механизм:** в дереве лежит вложенный репозиторий со своими служебными файлами
(`base-repo/` внутри `dota-dossier/`). Поиск маркера идёт по всему дереву и `head -1`
берёт **первый найденный**, а не корневой.

**Проверка:** может ли этот файл существовать во вложенной репе? Если да — искать строго
в корне либо перебирать уровни сверху вниз.

| # | Случай | Дата |
|---|---|---|
| 1 | `*/VERSION` склеивал версии base-repo и dota-dossier: «1.13.0»+«2.13.0» | 28.07 |
| 2 | `.repo-id` взят из вложенной base-repo → дота отклонена как «чужой архив» | 30.07 |

**Отдельно горько:** случай 2 сделан при том, что в трёх строках выше в коде стоял мой же
комментарий про случай 1 — «VERSION берём СТРОГО корневой, глоб ловил вложенные репы».
Комментарий-предупреждение на месте преступления не спас. Отсюда вывод: предупреждение
в комментарии слабее проверки в тесте, и писать надо второе.

---

## PIT-E · Дублирование логики вместо общей функции — **3 повтора**

**Симптом:** починил в одном месте, в другом осталось.

**Механизм:** одна и та же проверка написана дважды. Расходятся не сразу.

**Проверка:** есть ли вторая копия этой логики? Вынести до правки, не после.

| # | Случай | Дата |
|---|---|---|
| 1 | «Полностью опубликовано» отдельно в `VERIFY` и в автоудалении | 29.07 |
| 2 | Разбор имени архива отдельно для zip и для каталогов-зеркал | 30.07 |

---

**Повтор №3 — 21.08.2026, `dota-dossier`, найдено проходом синтеза.**
`match_archiver.py` и `match_watcher.py` делают одно и то же; второй помечен
«ранняя лёгкая версия, оставлена для совместимости». Измерено:

- обоснование в `docs/TOOLS.md` повторено **дословно дважды** (`grep -c` → 2);
- расписания в одном файле расходятся: `0 */3` и `0 */6`;
- в «оставленной для совместимости» версии живёт **ровно тот дефект**, который
  новая чинила: два состояния вместо трёх, вечная метка «мёртв» после одного
  неудачного опроса.

**Опасность не в лишнем файле.** «Оставлено для совместимости» звучит безобидно —
и следующий читатель ставит в расписание старую версию, получая починенный дефект
обратно. Счётчик достиг **3**: по правилу этого файла заплатки больше не работают,
нужна проверка — например, запрет на два скрипта с пересекающейся ответственностью
в одном `src/` без явной записи «кто из них канон».

## 🥈 PIT-H · Строка лога принята за единицу учёта — **3 повтора**

**Симптом:** агрегат по логу завышен, и завышен правдоподобно — порядок величины верный,
поэтому число не вызывает подозрений и уходит в отчёт.

**Механизм:** источник пишет **несколько строк на одну сущность** (реплика с текстом
и двумя вызовами инструментов — три строки, и **каждая несёт одинаковый `usage`**),
либо пишет строки, которые сущностью не являются вовсе (отказ по лимиту приходит
с `type: assistant` и заполненным `usage`, хотя ход не состоялся). Скрипт считает строки,
отчёт называет их ходами.

**Почему не ловится глазом:** ошибка не в разах-порядках, а в множителе 1.6–2.2.
«648 ходов» и «300 ходов» одинаково правдоподобны для длинной сессии, а «расход
232 миллиона» не с чем сравнить — эталона нет, потому и мерили.

**Проверка:** у каждой строки источника спросить — **это сущность или её часть?**
Если у сущности есть идентификатор (`message.id`), агрегат считается по нему,
а не по строкам. И отдельно: **есть ли строки, которые выглядят как сущность,
но ею не являются** (отказы, служебные, синтетические).

| # | Случай | Дата |
|---|---|---|
| 1 | `limits_watch.py`: запись об отказе по лимиту (`type: assistant` + `usage`) принята за ход → «лимит идёт» на работающей сессии | 21.08 |
| 2 | `session_metrics.py`: тот же дефект в том же виде, найден отдельно через день работы | 21.08 |
| 3 | `session_metrics.py`: три строки одной реплики приняты за три хода → **все агрегаты лаборатории завышены ×1.64…×2.24**, число ходов и суммы расхода в `measurements.csv` неверны | 21.08 |

**Счётчик дошёл до 3 → поставлена механическая проверка** (§3 порядка работы):
`session_metrics.py --selftest` — канарейка на заведомо задвоенном логе, падает,
если дедупликацию по `message.id` убрать. Проверена снятием фикса.

---

## Что видно из счётчиков

**PIT-A — 7 повторов, больше половины всех дефектов сессии.** Отдельные заплатки его
не победили: пять раз чинил конкретный случай, шестой и седьмой пришли тем же путём.
Победить может только механическая проверка, которая срабатывает без участия памяти —
поэтому «утверждение о втором прогоне» сделано условием приёмки, а не советом.

**PIT-B неустраним, пока стенд не гоняется на боевой машине.** Все четыре случая пришли
с macOS/zsh, стенд линуксовый и в bash. Смягчается `setopt no_nomatch`, зоной `I`
и обязательным `DRY=1`. **Закрывается только одним действием: прогнать
`zsh base-repo/tests/test_deploy.sh` на маке перед выпуском.** Внесено в `tests/README.md`
как обязательный шаг.

**PIT-F — 2 повтора, и второй сделан рядом с комментарием о первом.** Это самый прямой
аргумент за механические проверки: текст, который надо прочитать и вспомнить, не работает.
Закрыт тестом `C13`.

**PIT-H — 3 повтора за один день, и все три в измерительном коде.** Класс опасен тем,
что портит не поведение, а **числа, которыми поведение оценивают**: неверный агрегат
не падает, а тихо становится основанием для выводов. Закрыт канарейкой `--selftest`.

**PIT-C, D, E — по 2–3 повтора, но у каждого есть однозначное правило.** Они, скорее всего,
закрыты: правило простое и проверяется чтением диффа.

---

## Порядок работы с этим файлом

1. **Перед правкой поведения** — прочитать заголовки классов. Это 30 секунд.
2. **После обнаружения дефекта** — найти класс, увеличить счётчик, дописать строку.
3. **Если счётчик класса дошёл до 3** — заплатки не работают, нужна механическая проверка:
   тест, инвариант или запрет на уровне структуры кода.
4. **Класс с механической защитой не удалять из списка.** История нужна, чтобы через
   полгода не «упростить» защиту, не понимая, от чего она стоит.
