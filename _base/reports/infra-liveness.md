# Признак живости — инфра-скрипты

> Не «стоит ли в расписании», а «когда реально запускался последний раз».
> Принцип — `00-infrastructure/08-automation-triggers.md`, раздел «Автоматизация
> отваливается молча». Порог для триггера в той же таблице — ~14 дней.
>
> Обновляется каждым скриптом при запуске (последняя строка на скрипт — источник
> правды, старые строки не трогаются, история не нужна — это не журнал, а щуп).

| Скрипт | Последний прогон | Итог |
|---|---|---|
| `scripts/owner_blockers.py` | 2026-08-28 | 4 реп в крупнейшей группе, 1 групп |
| `scripts/sync_base_local.py --all` | 2026-08-29 | канон v3.115.0, 54 реп |
