# START HERE — `<repo-name>`

**Класс:** `<infra | core | satellite | temp:<родитель> | product>`
**Состояние:** `<скелет, наполнение не начато | в работе | зрелая>`

## Что это

<Одна фраза: зона ответственности репы. Берётся из `repos-map.md` — там же прописаны границы
с соседними репами, чтобы содержимое не расползалось.>

## Порядок входа в сессию

1. `WATCHLOG.md` §0 — где остановились
2. `ROADMAP.md` — что дальше
3. `_base/` — общие правила системы (зеркало `base-repo`, **не редактировать здесь**)

## Правила

Общесистемные правила наследуются из `_base/` — это зеркало `base-repo`, раздаётся
скриптом `deploy.sh --sync-base`. Локальные правила этой репы, если появятся, живут
в `00-infrastructure/` и в `_base/` не попадают.

Планирование верхнего уровня — репа `mission-control`.

<!--
ЗАПОЛНЕНИЕ

Меняются ровно три места: имя репы в заголовке, класс, описание в «Что это».
Остальное одинаково во всех репах намеренно — вахта, заходящая в любую репу, видит
один и тот же порядок входа и не переучивается.

Класс берётся из `.repo-class` в корне и обязан совпадать с ним и с `repos-map.md`
(три места, все машиночитаемые — расхождение означает, что правде верить нельзя нигде).
Для `temp` родитель указывается сразу: `temp:it-base`. Без него временная репа через год
становится вечной.

Репа доросла до собственных протоколов — они идут в `00-infrastructure/` рядом, а не
внутрь `_base/`: всё, что в `_base/`, затирается целиком при следующей раздаче.

Стандарт: `00-infrastructure/01-repo-standard.md` · карта: `repos-map.md` ·
классы: `00-infrastructure/76-repo-classes.md`
-->
