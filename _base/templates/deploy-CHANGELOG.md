# CHANGELOG — `deploy.sh`

## [4.25.0] — 2026-09-04 04:55 — предохранитель доставки: отказ вместо молчаливой потери (MINOR)

**Закрыт пункт `ROADMAP` §P2** и `74-planner-bridge.md` §4б.

### Что стирало задачу молча

Публикация **сносит рабочее дерево клона** и кладёт содержимое архива. Архив
собран из **локального** дерева, а файл доставки `INBOX-FROM-*` лежит только
на GitHub — мост кладёт его туда напрямую.

Значит порядок «доставка → деплой → вахта» уничтожал задачу **без единого
сигнала**: в репе просто нет файла, и вахта не отличает «не доставляли»
от «доставили и стёрли».

🔴 Это не грязь в выводе, а **молча пропущенный шаг** — тот же класс, что
`PIT-014`, и он же был предметом всей вчерашней работы.

### Что сделано

Перед публикацией — один запрос `gh api repos/…/contents`. Есть на удалённой
стороне `INBOX-FROM-*`, которого нет в архиве → **отказ с объяснением**
и строка в отчёт прогона; остальные версии и репы не страдают.

**Отказ ставится ДО распаковки:** не начатая публикация чинится повторным
запуском, начатая и оборванная оставляет дерево в промежуточном виде.

### 🔴 Дефект первой редакции, пойманный до запуска

Проверка ссылалась на `$SRC_TREE` — **переменную, которой в этой точке
не существует**: дерево появляется только после распаковки, а она ниже.
`bash -n` этого не ловит: имя синтаксически верно.

Исправлено чтением **архива** (`unzip -Z1`) — проверять надо ровно то,
что ляжет в репу.

### Проверено обоими направлениями

| Вход | Поведение |
|---|---|
| архив **без** файла доставки, а на GitHub он есть | 🔴 отказ |
| архив **с** файлом доставки | 🟢 публикация идёт |

**Когда проверки нет:** `gh` недоступен (`HAVE_GH=0`) — предохранитель молчит.
Это осознанно: без сети нельзя узнать, что лежит на удалённой стороне,
а блокировать публикацию из-за отсутствия сети дороже, чем пропустить
редкий случай. Граница названа здесь, а не оставлена читателю.


## [4.24.0] — 2026-09-03 23:20 — папка артефактов по умолчанию `~/Developer` (MINOR)

**Заказ владельца:** *«сделать папку типо developer, куда будут вместо downloads
класться все артефакты системы, в том числе архивы новых версий. Чтобы всё это
было отдельно, система от остального. А то в downloads вообще всё скачивается»*.

### Что изменилось

```
было:  DIR="${1:-${SELF_DIR:-$HOME/Downloads}}"
стало: DIR="${1:-${BASE_ARTIFACTS:-$HOME/Developer}}"
```

### 🔴 `SELF_DIR` убран из цепочки — он давал сюрприз

Запуск **канона** (`templates/deploy.sh`) искал архивы в `templates/`
и честно отвечал «публиковать нечего» — при полной папке архивов рядом.
Поймано 03.09.2026 при публикации `photo-archive`: первый прогон не нашёл
ничего, второй с явной папкой нашёл шесть архивов.

Копия в `~/Downloads` работала лишь потому, что **случайно лежала там же,
где архивы**. Умолчание, верное по совпадению, ломается при первом переносе
файла — что и произошло, когда артефакты переехали в `~/Developer`.

### Разделение по смыслу, а не по удобству

| Папка | Что там |
|---|---|
| `~/Developer` | что система **производит**: архивы релизов, выгрузки |
| `~/Downloads` | что пришло **извне**: браузер, почта, выгрузка Telegram |

Смешение стоило видимости: шесть архивов системы лежали среди случайных
`.jpeg` и `.pdf`, и «убрать лишнее из загрузок» означало риск снести релиз.

### Переопределение — одной переменной, общей с Python

`BASE_ARTIFACTS=/путь` — та же переменная, что читает `scripts/_roots.py`.
**Один способ переопределить путь, а не два расходящихся** (`PIT-178`:
второй механизм разойдётся с первым на первой же правке).

🔴 **Копия деплойера в `~/Downloads` по-прежнему проверяется гейтом.**
Артефакты переехали, копия осталась: владелец запускает её оттуда годами,
и перестать её сверять значило бы вернуть `PIT-063` — откат единственного
инструмента публикации на 13 версий, потому что смотрели не туда.


> Собственная история деплойера. Скрипт живёт своей жизнью внутри репы: он меняется
> чаще, чем сама база, и его поведение нужно уметь откатывать и объяснять отдельно.
> Правило — `00-infrastructure/44-what-to-version.md` (у меняющихся инструментов
> обязательны версия и журнал).
>
> Версия скрипта — в переменной `SCRIPT_VERSION` в шапке файла.
> Формат записей — `00-infrastructure/42-release-description-standard.md`.
>
> **Разрыв 4.3.2 → 4.16.1 восстановлен задним числом (2026-08-20).** Три недели скрипт
> развивался в `mission-control/scripts/deploy.sh`, а копия в базе стояла на 4.3.2 —
> при том, что документация в четырёх местах называла каноном именно базу. Записи
> `[4.4.0]`–`[4.16.1]` ниже собраны из `mission-control/CHANGELOG.md` (релизы 0.9.0–1.5.0),
> где каждая правка деплойера описывалась, и сверены с кодом 4.16.1. Разбор —
> `reports/incidents/deployer_downgrade_incident.md`. **Впредь деплойер релизится
> через базу**, `mission-control` получает копию.

---

## [4.23.0] — 2026-08-29 — Проверка WATCHLOG идёт по указателю (MINOR)

🔴 **Найдено живым деплоем владельца 29.08.2026.** Прогон остановился на
`personal-finance-dss v8.29.4` с сообщением «в WATCHLOG.md нет строки
**Версия:** X.Y.Z — §0 не читается машиной». Остальные репы опубликовались.

**Строка была** — в `docs/WATCHLOG.md`. У этой репы своя продуктовая конвенция:
вся документация живёт в `docs/`, а корневой `WATCHLOG.md` — **указатель**,
заведённый ради guard'а доставки (`74-planner-bridge.md` §4а требует файл
именно в корне). В тот же день указатель был очищен от состояния как второй
источник правды (`revision_check.py` теперь это проверяет) — и деплойер,
читающий строку версии в корне, перестал её находить.

**Починка:** если корневой `WATCHLOG.md` короткий и несёт ссылку на
одноимённый файл в подкаталоге — версия читается оттуда. Признак тот же,
что уже используют `revision_check.py` и `bump_repo.py`: три инструмента
теперь понимают указатель одинаково.

> Класс дефекта: **изменение адреса, не доехавшее до всех, кто по этому адресу
> ходит.** Гейт и `bump_repo.py` научились ходить по указателю в тот же день,
> деплойер — нет, потому что его никто не запускал до вечера.

---

## [4.22.0] — 2026-08-27 — Заливка ассета: живой heartbeat + отдельный больший таймаут (MINOR)

Тот же прогон, четвёртый и последний баг того же класса в этой серии: владелец
сообщил «на релизы просто висит, ничего не показывает» — `self-map` (~140 МБ ассет)
на шаге `→ релизы` был тих несколько минут подряд.

### Fixed
- **`GH_TIMEOUT=120` (2 минуты) — общий для лёгких API-вызовов и заливки
  многомегабайтных ассетов.** На 40-60 КБ/с 120с дают ~7 МБ — недостаточно для
  140 МБ, гарантированный таймаут на каждой из 3 попыток `gh_try()`, полностью
  тихий (весь вывод буферизуется через `_o="$(...)"`, ничего не печатается до
  завершения попытки). Новая переменная `GH_UPLOAD_TIMEOUT` (по умолчанию 1800с,
  30 минут) — только для заливки, лёгкие вызовы не затронуты.
- **Новая функция `gh_try_upload()`** — тот же ретрай-паттерн, что `gh_try()`, но
  запускает `gh` в фоне и печатает живой отсчёт времени (`\r заливаю... Nс`) вместо
  полной тишины. Побайтовый процент недоступен (`gh` пишет прогресс-бар только в
  реальный терминал, не в перенаправленный поток) — heartbeat подтверждает, что
  процесс жив, а не завис.
- Оба места заливки ассета (`ensure_release()` — создание релиза с ассетом и
  дозаливка канонического ассета к существующему релизу) переведены на
  `gh_try_upload()`, с явным сообщением размера файла перед стартом.

### Added
- Косметика: строка `[N/40] repo` (обновляется через `\r` во время сканирования
  архивов) теперь завершается явным переводом строки — раньше первая репа сводки
  версий печаталась впритык к хвосту этой строки на одной визуальной строке,
  а не с новой, как остальные репы списка.

## [4.21.3] — 2026-08-27 — Дочищена симптоматика 4.21.2: спам ошибок + повторная защита в retry() (PATCH)

Тот же прогон продолжился: несмотря на 4.21.2, владелец увидел ту же картину на `family` —
десятки повторов `no such file or directory: .../clone_family.log`, затем `retry()` всё
равно вошёл в 5 бессмысленных попыток («Unable to read current working directory») прежде
чем сдаться. Два отдельных недочёта в самой защите 4.21.2, не новый класс дефекта.

### Fixed
- **Цикл опроса прогресса спамил нечитаемой ошибкой.** Раз в секунду читал
  `$_clone_log` без проверки существования; если файл (или вся `$WORK`) пропадал
  посреди клона, `< "$_clone_log"` на отсутствующий файл кричит "no such file or
  directory" **прямо в терминал** — эмпирически подтверждено: `2>/dev/null` эту
  ошибку не гасит ни в bash, ни в zsh, потому что это ошибка открытия редиректа
  самим шеллом, а не ошибка команды, чей stderr перенаправляется. Добавлена
  проверка `[ -f "$_clone_log" ]` перед каждым чтением.
- **`retry()` не проверял `$WORK` на своей точке входа.** Guard из 4.21.2 стоял
  только ДО первой попытки клона — если `$WORK` пропадала уже ПОСРЕДИ этой первой
  попытки (ровно случай `family`: 74% готовности, 475 КБ/с — сеть была жива),
  скрипт всё равно уходил в `retry()` и повторял идентичный провал 5 раз (~2 минуты
  чистого ожидания на одну репу). Тот же fail-loud guard добавлен второй раз — прямо
  перед входом в `retry()`, а не только перед первым клоном.

Точная причина, почему `$WORK` пропадает посреди активного клона без Ctrl+C, **всё ещё
не установлена** (VPN-клиент/диск/файловая система — кандидаты, не подтверждено
экспериментом) — 4.21.3 не устраняет первопричину, только её самые болезненные
симптомы (спам в терминал, бессмысленные ретраи).

## [4.21.2] — 2026-08-27 — Гонка Ctrl+C с фоновым клоном + fail-loud на исчезнувшей рабочей папке (PATCH)

Тот же прогон, что нашёл 4.21.1: владелец, дожидаясь исправления порога, продолжал
наблюдать `fatal: Unable to read current working directory: No such file or directory`
и `fatal: remote helper 'https' aborted session` — не разово, а **на каждой следующей
репе подряд** в одном и том же прогоне (health-vault → history → linguistics →
mathematics, все с идентичной ошибкой). Разбор вскрыл два отдельных дефекта.

### Fixed
- **Гонка cleanup_work / фоновый `git clone &`.** Клон запускается в фоне (для живого
  прогресс-бара), PID нигде не отслеживался за пределами локальной переменной. При
  Ctrl+C трап `cleanup_work` (`EXIT INT TERM`) удалял `$WORK` немедленно, а фоновый
  git-процесс мог пережить это на секунду-две и продолжал писать в уже стёртую папку —
  ровно та ошибка, что видел владелец. Теперь PID активного клона хранится в
  `ACTIVE_CHILD_PID`, и `cleanup_work` сначала убивает и дожидается его смерти, потом
  чистит `$WORK` — гонка исключена по конструкции, не по таймингу.
- **Fail-loud, если `$WORK` пропала БЕЗ Ctrl+C.** Второй, более тревожный случай:
  рабочая папка исчезла посреди активного клона (health-vault, 85% готовности,
  686 КБ/с — сеть была живой) без единого нажатия Ctrl+C. Без проверки скрипт слепо
  уходил в `retry()` — 5 попыток клонирования в несуществующую папку, гарантированно
  проваливающихся одинаково, — **на каждой следующей репе**, пока владелец не прервал
  прогон вручную. Добавлена проверка `[ -d "$WORK" ]` перед каждым клоном: если папка
  пропала, скрипт останавливается сразу с понятным объяснением («среда нестабильна,
  перезапусти»), вместо того чтобы жечь ретраи там, где чинить нечего. Точная причина
  исчезновения `$WORK` в этом случае не установлена (VPN-клиент/диск/ФС — кандидаты,
  не подтверждено экспериментом) — это защита от последствий, не устранение первопричины.

## [4.21.1] — 2026-08-27 — Порог зависания перекалиброван: 1024 → 51200 байт/с (PATCH)

В тот же день, первый реальный прогон 4.21.0 на живой VPN-нестабильности (волна деплоя
40 реп): владелец наблюдал зависшие строки прогресса `git clone` и был вынужден вручную
прерывать (`Ctrl+C`) раз за разом — ровно то, что 4.21.0 должна была предотвращать.

**Причина.** `CLONE_LOW_SPEED_LIMIT=1024` (1 КБ/с) — порог «соединение почти мертво».
Реальный симптом на нестабильной VPN — не смерть соединения, а устойчивое ползание:
лог показал минуты передачи на 46–150 КБ/с, что технически **выше** старого порога.
Механизм ни разу не сработал за весь наблюдаемый прогон — не потому что не нужен, а
потому что порог не совпадал с реальностью канала, которую он должен был ловить.

### Changed
- `CLONE_LOW_SPEED_LIMIT` по умолчанию: `1024` → `51200` (50 КБ/с) — теперь ловит
  «мучительно медленно», не только «мертво».
- `CLONE_LOW_SPEED_TIME` по умолчанию: `30` → `15` — быстрее реакция, меньше времени
  теряется перед тем, как механизм вообще заметит проблему.

## [4.21.0] — 2026-08-27 — Зависшая на TCP-уровне передача обрывается сама (MINOR)

Владелец: на нестабильном канале (в т.ч. при переключении VPN на лету) скорость `git
clone`/`git push` может упасть с ~10 МБ/с до долей кбит/с, но соединение при этом не
рвётся — git ждёт молча, без кода ошибки, который мог бы увидеть `retry()`. Прежде
единственным выходом было прервать процесс вручную (`Ctrl+C`, иногда `kill -9`, что
могло оставить недочищенную папку клона и сломать следующий заход) и перезапускать
скрипт заново, теряя прогресс текущей репы.

### Added
- `CLONE_LOW_SPEED_LIMIT` (по умолчанию 1024 байт/с) и `CLONE_LOW_SPEED_TIME`
  (по умолчанию 30 с) — новые переменные окружения, как `RETRIES`/`RETRY_SLEEP`.
  Передаются в `git clone`/`git push` через `-c http.lowSpeedLimit=… -c
  http.lowSpeedTime=…` (скоуп только на вызов, глобальный `git config` не трогается).
  Если средняя скорость держится ниже порога дольше указанного времени, git сам
  обрывает передачу штатным кодом ошибки — с корректной подчисткой частичного клона —
  и она уходит в уже существующий `retry()` с экспоненциальной паузой. Применено к
  обеим точкам клонирования (первая попытка и повтор после сетевого сбоя) и к
  `pushb`/`pusht`.
- `retry()` — при паузе между попытками печатает подсказку, что сейчас безопасно
  переключить VPN: следующая попытка откроет соединение заново на новом интерфейсе.

## [4.20.1] — 2026-08-25 — Сообщение MISSING repos-map не звучит как блокер, которым не является (PATCH)

Владелец на живом прогоне: сообщение «ТРЕБУЕТ РЕШЕНИЯ: repos-map: нет в карте вовсе —
research-engine» напечаталось, а следующим шагом скрипт **сам** добавил строку в карту
(шаг 3) — противоречие того же класса, что чинил `v4.17.1` для итогового блока, но
здесь про **inline**-сообщение по ходу прогона, не про сводку в конце.

**Причина.** Текст сообщения (`loud "repos-map: нет в карте вовсе — ..."`) не различал
«репа не зарегистрирована в карте вообще» (это чинится автоматически шагом 3) и «зона
ответственности не описана» (это остаётся за человеком). Оба смысла жили в одной фразе
«нет в карте вовсе», и она читалась как полный блокер.

**Правило.** Текст `loud`-сообщения обязан называть именно ту часть, которую скрипт
**не** почини́т сам — если часть проблемы устраняется следующим шагом того же прогона,
сообщение обязано это сказать, а не выглядеть цельным блокером.

## [4.20.0] — 2026-08-23 — Сверка карты называет отказавший шаг (MINOR)

🔴 **`PIT-137`, найдено на живом прогоне владельца.** Деплой печатал
«сверку карты выполнить не удалось (python3/парсинг) — проверь вручную».

Проверка воспроизведена вручную тем же кодом на том же файле: она **работает** и даёт
«секций 60, реп на GitHub 61». Падал не парсинг.

**Причина.** Блок клонирует `base-repo` с GitHub во временную папку, и над всей цепочкой
стоял общий `2>/dev/null || :`. Клон не прошёл, файл не появился, python получил
несуществующий путь — три разных отказа выглядели одинаково, и сообщение винило
последний шаг цепочки.

> **Общий обработчик ошибки врёт на двух шагах из трёх.** Сообщение обязано называть
> шаг, который отказал, а не тот, на котором цепочка оборвалась.

Добавлена переменная `_mapstage`: «клон base-repo» → «копирование repos-map.md» →
«разбор карты». Отказ теперь называет своё место.

## [4.19.0] — 2026-08-23 — Шаг 1 перестал молча читать все архивы целиком (MINOR)

🔴 **Найдено владельцем:** *«что-то странное происходит со скриптом деплоя, я его
запускаю и он стоит на месте долго»*. Скрипт печатал «Шаг 1. Ищу версионные архивы»
и замолкал; владелец прерывал прогон дважды.

**Зависания не было — было молчание.** Замер 23.08.2026: **47 секунд** на 47 архивах
(1.2 ГБ суммарно), из них 3.8 с на один архив в 268 МБ.

**Причина — `unzip -t` в предполётной проверке.** Комментарий рядом с ним утверждал
«дёшево — по списку файлов, без распаковки». Утверждение неверно: `-t` проверяет CRC
**каждого файла**, то есть распаковывает архив целиком.

> **Проверка делала не то, что от неё требовалось.** Нужно было отличить оборванный
> архив от целого; вместо этого пересчитывался CRC всего содержимого. Центральная
> директория дописывается **последней** (`PIT-130`) — её наличия достаточно.

**Заменено на `zipinfo -1`:** 47 с → **1.2 с**, в 40 раз быстрее, поведение то же.

**Добавлен прогресс** — `[N/47] имя-архива`. Молчание на десятки секунд неотличимо
от зависания, и это стоило двух прерванных прогонов.

## [4.18.0] — 2026-08-23 — Ретрай перестал бить в стену (MINOR)

🔴 **Найдено владельцем на живом прогоне.** `git push` в `edu-base` отклонялся
`pre-receive` хуком: `ч1.mp4` — **207.75 МБ** при жёстком лимите GitHub **100 МБ**.
Ретрай счёл это сетевым сбоем и повторил **пять раз**, каждый раз заливая 549 МБ
по ~1.5 МБ/с. Около получаса и несколько гигабайт трафика впустую — отказ был
**окончательным** и не прошёл бы никогда.

> **Ретрай осмыслен только для временного отказа.** Отказ по содержанию — лимит
> размера, отклонение хуком, нет прав — повторять нельзя: ожидание ничего не меняет.
> Прежний `retry` не смотрел на причину и печатал «обычно TLS-таймаут» на любой сбой.

**Добавлено:**

- `FATAL_PATTERNS` — распознавание окончательных отказов (`GH001`, `exceeds GitHub's
  file size limit`, `pre-receive hook declined`, `Large files detected`,
  `remote rejected`, `Permission denied`, `403`, `authentication failed`,
  `repository not found`). Совпало — выход сразу, с причиной и командой починки;
- **предполётная проверка размеров** перед push (`find -size +100M`): дешевле узнать
  о лимите до заливки, чем после отказа.

📍 Причина появления самих файлов — кампания переноса свалила медиа в репы «как есть».
Лечится лабораторией `07-media-to-text-lab/`: 18 файлов на 2.1 ГБ вынесены из-под git
и заменены служебками с паспортом.

## [4.17.2] — 2026-08-21 — MIRRORS дополнен четырьмя публичными репами (PATCH)

### Fixed
- **Четыре репы стали публичными в тот же день и в `MIRRORS` не попали:**
  `algorithms-site`, `game-analytics-engine`, `claude-usage`, `salvation`.
  Массовые режимы пропускают репы из этого списка — без записи они считались бы
  обычными приватными.

### Почему это не было утечкой
Раздача базы (`mission-control/scripts/sync-base.sh`) держит предохранитель
**на свойстве объекта, а не на списке**:

```bash
[ "$(gh repo view "$O/$n" --json isPrivate --jq '.isPrivate')" = "false" ] && continue
```

Публичная репа базу не получает, даже если её забыли внести. Это исполнение PIT-097
(«список — намерение, свойство объекта — факт»), и оно сработало: `_base/` нет
ни в одной из восьми публичных реп — проверено.

**Список остаётся** для скорости и читаемости, но единственной защитой не является.
Рядом с ним записана команда сверки списка с GitHub.

### Почему PATCH
Поведение не менялось ни в одном режиме — правится значение по умолчанию и комментарий.

---

## [4.17.1] — 2026-08-21 — Итоговая сводка не противоречит сама себе (PATCH)

### Fixed
- **Сводка `ТРЕБУЕТ ТВОЕГО РЕШЕНИЯ` печатала устаревший вердикт.** Сверка `repos-map`
  идёт **до** шага 3, который новые репы в карту и добавляет. К моменту печати
  финального блока вердикт устаревал, и прогон выдавал две строки подряд:

  ```
  algorithms-site — repos-map  добавлена (требует описания)
  ТРЕБУЕТ РЕШЕНИЯ: repos-map: нет в карте вовсе — algorithms-site …
  ```

  Вторая опровергает первую. Владелец справедливо спросил, не сломан ли скрипт:
  публикация-то отрабатывала верно. **Молчаливое противоречие в итоговом блоке хуже
  отсутствия блока** — читающий перестаёт доверять всему списку, включая настоящие
  пункты.

- **Как починено:** функция `map_resolve` вычёркивает из строки `MISSING` имена,
  которые шаг 3 зарегистрировал в этом же прогоне; если вычеркнулись все — строка
  уходит целиком. Остальные пункты сводки не трогаются.

### Added
- Тест `H9` в `tests/test_deploy.sh` — четыре проверки: разрешённое вычёркивается,
  неразрешённое остаётся, при полном разрешении строка исчезает, пустой список
  ничего не ломает. Плюс посторонний пункт сводки обязан уцелеть.

### Почему PATCH, а не MINOR
Поведение публикации не изменилось ни в одном режиме — правится только печать
итогового блока. Новых ключей нет, `deploy-MODES.md` не трогается.

---

## [4.17.0] — 2026-08-21 — Тег без релиза больше не тупик; настоящая ошибка вместо догадки (MINOR)

**Боевой случай.** Публикация 20 версий `base-repo`: релизы 1.64–1.72 создались,
на 1.73–1.74 пришло «GitHub недоступен», 1.75 прошла, прогон прерван. Повторные запуски
печатали «новых версий нет» и **«→ релизы» без единой строки** — при том что в блоке
уборки честно значилось «не хватает: релиз» у **десяти** версий.

### Fixed

- **Диагноз без команды починки — половина работы.** По умолчанию релизы делаются только
  для версий **текущего** прогона (`ALLTAGS="$PUBLISHED"`), и тег, уехавший без релиза,
  обычным прогоном не чинится **никогда**. Механизм починки существовал (`REPAIR=1`),
  но владелец видел проблему и не видел выхода. Теперь блок уборки, обнаружив нехватку
  релиза, печатает **точную команду**.
- **`release_state` возвращала вердикт и глотала причину.** На экран уходила зашитая
  фраза «GitHub недоступен» — догадка вместо факта; настоящий текст `gh` терялся.
  Причин у кода 2 минимум шесть (502/503/504 · таймаут · сброс соединения · DNS),
  и лечатся они по-разному. Теперь текст кладётся в `RELEASE_STATE_ERR` и **печатается**,
  а сообщение переформулировано в наблюдение: «не смог узнать состояние релиза».
  Разбор класса — `00-infrastructure/71-fail-loud-and-sourcing.md` §7г.

### Changed

- В список признаков «ответить невозможно» добавлены **`429`, `rate limit`,
  `secondary rate`**. Двадцать релизов с загрузкой ассетов подряд упираются во вторичный
  лимит GitHub, и прежде такой ответ трактовался как «релиза нет» — то есть скрипт
  пытался создать существующий релиз вместо того, чтобы подождать.

## [4.16.2] — 2026-08-20 — Переменная вплотную к «кавычке» убивала прогон под `set -u` (PATCH)

Первый релиз деплойера из базы после возврата канона. Дефект найден набором тестов, который
три недели не видел рабочего кода: копия в базе стояла на 4.3.2, а разработка шла в другой репе.

### Fixed
- **`«$_ridrepo»` и `«$_aid»`: шелл приклеивал первый байт закрывающей кавычки к имени
  переменной.** У «» это 0xC2 0xBB; при разборе имени байт 0xC2 попадал внутрь идентификатора,
  получалось несуществующее имя, и под `set -u` прогон **умирал** — причём ровно в той строке,
  которая должна была назвать проблему. Наружу вместо диагноза выходило
  `line 992: _ridrepo?: unbound variable`.
- **Последствие было тяжелее самой опечатки.** Обе строки — диагностические:
  «`.repo-id` указывает на другую репу, пропускаю» (предполёт) и «публикуем не туда — СТОП»
  (последний рубеж перед заливкой). Вместо пропуска одного архива падал **весь батч**, нарушая
  инвариант И8 («сбой на одной версии не роняет остальные»). Триггер — любое имя репы
  с не-ASCII в `.repo-id`.
- Лечение — фигурные скобки: `${_ridrepo}`, `${name}`, `${_aid}`, `${REPO}`.

### Added
- **Тест I5 закрывает класс, а не два случая.** Статическая проверка: в скрипте не должно быть
  `$var` вплотную к `«»„“”‘’`. Плюс живой прогон — архив с `.repo-id` на кириллице обязан дать
  диагноз, а не `unbound variable`, и репу не создать.

### Fixed (в самом наборе тестов)
- **`xargs -d '\n'` — GNU-расширение, которого нет в BSD-xargs на macOS.** Проверка G4 падала
  не из-за деплойера, а из-за себя самой. Переписана на NUL-разделитель (`tr '\n' '\0' | xargs -0`),
  он есть в обоих. Тот же класс различия BSD/GNU, что уже ловили с `sed` и с `nomatch` в zsh.

### Verified
- **278 проверок, всё зелено** (было 271 из 274 — два провала в зоне C от дефекта выше,
  один в G от бага теста). Зон девять, кейсов 73.

---

## [4.16.1] — 2026-08-16 — Строгая сверка карты не узнавала эмодзи в заголовках (PATCH)

### Fixed
- **Выражение строгой сверки `repos-map.md` не распознавало заголовки записей.** Заголовок
  содержит эмодзи между решёткой и именем (``## 🧩 `base-repo` ``), прежнее выражение такие
  строки не считало записями — и объявило бы `LOOSE` все 54 записи разом.

### Verified
- Прогнано на живой карте — чисто.

---

## [4.16.0] — 2026-08-16 — Цветовые коды в заголовке релиза, строгая сверка карты, гейт по WATCHLOG (MINOR)

Три дефекта, найденные эмпирической сверкой (PIT-016 – PIT-018).

### Fixed
- **PIT-016. В заголовок публичного релиза уехали цветовые коды.** `loud()` печатала
  диагностику в stdout, и её подхватывал `TITLE="$(build_notes …)"` без проверки кода
  возврата. У зеркала `finpilot` нет `CHANGELOG.md` — ветка «не найден» срабатывала штатно,
  и escape-последовательности попадали в заголовок. Теперь диагностика идёт в stderr,
  код возврата проверяется в обоих местах, добавлен санитайзер `safe_title()`.
- **PIT-017. Сверка карты проходила на любом упоминании имени.** Имя репы искалось
  подстрокой, поэтому витринные зеркала «были в карте» ссылкой в оглавлении. Исходы
  разведены: `MISSING` / `LOOSE` / `STALE` / `COUNT`.

### Added
- **PIT-018. Гейт по журналу перед публикацией.** `WATCHLOG` §0 протух четвёртый раз
  подряд (шапка на одной версии, тело на другой). Перед публикацией скрипт сверяет версию
  в §0 с публикуемой и пропускает репу при расхождении.

---

## [4.15.0] — 2026-08-15 — Ключ `MIRRORS_ONLY` (MINOR)

### Added
- **`MIRRORS_ONLY=1` — обработать все зеркала из `MIRRORS` одной командой.** Массовый
  прогон зеркала пропускает намеренно, и перечислять их руками каждый раз — прямой путь
  к тому, что однажды забудешь одно и витрина разъедется по версиям. Ключ раскрывается
  в `ONLY` со списком `MIRRORS`, дальше работает та же проверенная ветка: новой логики
  публикации не появилось, а значит и нового способа уронить что-то в публичную репу.

### Changed
- **`MIRRORS_ONLY=1` вместе с `ONLY` — выход с кодом 2.** Молча выбирать за владельца,
  какой из двух списков он имел в виду, нельзя.
- Формы `ONLY=1` быть не может: `ONLY` — список имён, и `1` было бы понято как репа
  с именем `1`.

---

## [4.14.0] — 2026-08-15 — Карта сверяется на КАЖДОМ прогоне (MINOR)

### Changed
- **Сверка `repos-map.md` отвязана от события «создали репу в этом прогоне».** Репу можно
  завести руками через `gh` — и тогда в карту она не попадала никогда. Теперь список
  активных реп с GitHub сверяется с картой всегда, расхождение печатается в раздел
  «требует твоего решения». Ручная дисциплина здесь не работает: за месяц карта отстала
  на восемь реп именно потому, что «допишу потом».

---

## [4.13.0] — 2026-08-15 — Три зеркала витрины под защиту, шаг 3 не объявляет успех провалом (MINOR)

### Fixed
- **Шаг 3 печатал «push не прошёл» при полном успехе.** Проверялся код возврата цепочки
  `add && commit && push`; когда все новые репы уже были в карте, `commit` падал
  с «nothing to commit», цепочка рвалась, и рядом оседала мусорная копия
  `~/Downloads/repos-map-updated.md`. Теперь сначала `git status --porcelain`, и три исхода
  разведены: правок не требуется / коммит не удался / push не прошёл.

### Added
- **`MIRRORS` дополнен зеркалами витрины** — `vk-graph`, `health-report-generator`,
  `bron-kerbosch`. Без этого массовые режимы и раздача базы добираются до публичных реп.
  Зеркала были открыты в public раньше, чем попали в список: окно, в котором `sync-base.sh`
  залил бы в них `_base/`. Проверено — не успел.

---

## [4.12.0] — 2026-08-14 — Номер сожжён: редакция не доехала до рабочей папки

Версии 4.12.0 в рабочей линии **не существует**. Номер был выдан редакции, которая
содержала `finpilot-public-mirror` в `MIRRORS`/`REPO_MAP` и поддержку `RELEASES.md`,
но до рабочей папки не доехала. Содержимое вошло в 4.13.0, номер оставлен пустым —
запись стоит здесь, чтобы следующая вахта не искала пропажу.

Первопричина выдачи номера: приложенная владельцем редакция шла под номером 4.11.0 —
тем же, что архивная **без** этих изменений. Ровно тот класс дефекта, что закрывался
в 4.5.0 (версия в переменной не соответствует содержимому файла) и повторился как
PIT-063.

---

## [4.11.0] — 2026-08-09 — Ремонтные режимы больше не создают репозитории (MINOR)

### Fixed
- **`ALL_REPOS=1 CHLOG_FILL=1` создал заново удалённую `finpilot-mirror`.** Цепочка:
  репу удалили с GitHub, строку в `repos-map.md` не убрали, через сутки `ALL_REPOS` взял
  список реп **из карты**, не нашёл репу на GitHub и создал её — ветка создания
  не различала обычный прогон и ремонт. Теперь `REPAIR`, `ASSETS_ONLY`, `CHLOG_FILL`,
  `AUDIT`, `ALL_REPOS` чинят только существующее: отсутствие репы для них — повод громко
  пропустить, а не создать.

### Changed
- `MIRRORS` расширен до `finpilot finpilot-mirror`.

---

## [4.10.0] — 2026-08-09 — Массовые режимы не трогают публичные зеркала (MINOR)

### Fixed
- **`ALL_REPOS=1` и `CHLOG_FILL=1` дошли бы до публичной `finpilot` и вернули туда
  служебные файлы.** `SKIP_MIRRORS` закрыл только `sync-base.sh`, а у деплойера был
  свой путь к тем же репам.

### Added
- **Переменная `MIRRORS`** (по умолчанию `finpilot`) фильтрует и список из архивов,
  и список из `repos-map`. Названная явно через `ONLY` репа обрабатывается — осознанная
  публикация в зеркало остаётся возможной.

---

## [4.9.0] — 2026-08-08 — Живой прогресс клонирования (MINOR)

### Added
- **Проценты и объём при клонировании, размер клона по завершении.** У крупных реп клон
  идёт минутами, и без вывода нельзя отличить работу от зависшей сети.

---

## [4.8.0] — 2026-08-08 — `CHLOG_FILL` приводит журнал к §2 целиком (MINOR)

### Added
- **Разряд проставляется в заголовки, где его нет.** Считается по номерам версий —
  однозначно следует из SemVer, выдумывать нечего. Тезис у заголовков, где нет и его,
  берётся из темы коммита. У `portrait-of-taste` это 25 строк предупреждений §2 за прогон.

### Fixed
- **Предшественник версии берётся из списка тегов, а не из порядка в журнале.** Журнал
  может пропускать версии, и тогда сосед по файлу — не тот, с кем сравнивать: `3.4.0`
  рядом с `2.0.0` в файле давал MAJOR вместо MINOR.
- **Новая секция вставлялась перед первой существующей.** Для добора свежих версий это
  работало, но версия из середины истории уезжала наверх. Журнал пересобирается целиком
  с сортировкой по номеру, тела существующих секций не трогаются.

---

## [4.7.0] — 2026-08-08 — Ремонтные режимы глохли на пустой папке (MINOR)

### Fixed
- **`ASSETS_ONLY` / `REPAIR` / `CHLOG_FILL` выходили ни с чем, если архивов нет.** Список
  реп собирался из индекса локальных архивов, и проверка «архивов нет» стояла раньше
  разбора режима. Выходило наоборот смыслу: эти режимы и запускают тогда, когда архив уже
  опубликован и удалён. Хуже того, выход был тихим и с кодом 0 — владелец видел
  «публиковать нечего» и считал, что починка отработала. Теперь при пустом индексе
  ремонтный режим берёт список из `ONLY`, а без `ONLY` — из `repos-map`.

### Verified
- `art` и `philosophy` развёрнуты: репы созданы, описания и топики из `.repo-meta`
  проставились при создании, релизы с ассетами, обе добавлены в `repos-map`.

---

## [4.6.1] — 2026-08-08 — В ветке REPAIR не передавался клон (PATCH)

### Fixed
- **Шестой аргумент `ensure_release` — путь к клону — в REPAIR не передавался**, без него
  канонический zip не собирается из тега (`git archive`), и версии с удалённым архивом
  получают релиз без ассета. Так вышло с `portrait-of-taste` 3.4.3–3.4.5: релизы созданы,
  ассетов нет.
- **Парсер CHANGELOG понимает оба диалекта заголовка.** Канон §2 требует разряд в скобках
  в конце, но генератор скелетов с самого начала писал `— MINOR: тезис`, и в этом диалекте
  живут журналы почти всех реп. Раз файлов больше, чем правил, парсер приводит к канону сам.
  На прогоне `portrait-of-taste` это давало 35 строк предупреждений подряд.
- **Архивы именуются по канону — с точками.** Прежние поставки уходили как
  `mission-control-v0_10_0.zip`; скрипт их обрабатывал, но каждый раз писал «имена
  не по стандарту».

---

## [4.6.0] — 2026-08-08 — Режим `CHLOG_FILL` (MINOR)

### Added
- **`CHLOG_FILL=1` дописывает пропущенные секции журнала по данным самого git** — дата
  тега, тема коммита, статистика диффа с предыдущим тегом — и сразу переходит в `REPAIR`,
  создавая релизы. Нужен там, где тег и коммит уже на GitHub, релиза нет из-за отсутствия
  секции, а локальный архив удалён: единственный оставшийся источник о версии —
  репозиторий. Ассет при отсутствии архива собирается из тега (`git archive`).
- Ничего не выдумывается: в секцию попадает только читаемое из репозитория, и каждая
  помечена как восстановленная.
- **Отдельного скрипта не заведено — это режим внутри `deploy.sh`**, по железному правилу
  файла (`templates/README.md`).

---

## [4.5.1] — 2026-08-08 — REPAIR ронял прогон после успешной работы (PATCH)

### Fixed
- **`ensure_release:51: ASSET_OK: parameter not set`.** Накопитель `ASSET_OK` объявлялся
  в блоке 2Б, а ветка REPAIR на шаге 2А вызывает `ensure_release` раньше. Под `set -u`
  обращение к необъявленной переменной валит прогон — причём уже ПОСЛЕ создания релиза:
  работа сделана, а выход с ошибкой. Объявлены глобально + защитное раскрытие
  `${ASSET_OK:-}` внутри функции.

### Замечено на прогоне
- В режиме REPAIR CHANGELOG читается из **клона репы**, а не из архива: REPAIR
  по определению не перекоммичивает дерево. Добавить CHANGELOG в локальный zip и запустить
  REPAIR — бесполезно, файла в дереве на GitHub от этого не появится.

---

## [4.5.0] — 2026-08-08 — `.repo-id` старше `REPO_MAP` (MINOR)

### Fixed
- **Архив с явно указанным владельцем не мог доехать никуда.** Карта
  (`finpilot=personal-finance-dss`) переписывала целевую репу по имени архива, после чего
  проверка ниже отбраковывала архив за то, что `.repo-id` не совпал с уже переписанным
  именем. Теперь карта применяется только когда `.repo-id` в архиве отсутствует.
- **`SCRIPT_VERSION` отставал от содержимого файла.** Двумя правками подряд менялся только
  комментарий в шапке, а баннер прогона печатается из переменной — скрипт работал
  пропатченный, но представлялся версией 4.3.2. Для системы, где версия решает, что
  накатано, это прямая дезинформация. Переменная приведена к факту.
  *(Тот же класс дефекта позже дал сожжённый номер 4.12.0 и PIT-063.)*

---

## [4.4.0] — 2026-08-08 — Скрипт переехал в репу, описание и топики из `.repo-meta` (MINOR)

### Added
- **Сам скрипт теперь живёт в репе**, а не только патч к нему.
- **Описание и топики берутся из `.repo-meta`.** Раньше описание при создании репы
  равнялось её имени — отсюда `finpilot-mirror` с описанием `finpilot-mirror`. Теперь
  читается из `.repo-meta` самого свежего архива и обновляется на каждом прогоне,
  не только при создании.
- **Проверка целостности архива** через `unzip -t` с повтором: отличает битый архив
  от того, который ещё дописывается на диск.

### Fixed
- **Реальная ошибка `git clone`** вместо зашитой догадки «обычно TLS-таймаут» — печатаются
  три последние строки вывода git.

---

## [4.3.1] — 2026-07-30 — `.repo-id` брался из вложенной репы (PATCH)

`dota-dossier-v2.14.25.zip` отклонён как «чужой архив»: внутри лежит `base-repo/` со своим
`.repo-id`, и поиск через `head -1` взял вложенный маркер вместо корневого. Рецидив ловушки,
уже закрытой для `VERSION` — причём новый код написан в трёх строках от собственного
комментария об этом.

### Fixed
- **`.repo-id` в предполёте читается СТРОГО из корня архива** (с учётом уровня обёртки),
  как уже сделано для `VERSION`.
- **`find_repo_id` и `find_version_file` перебирают уровни сверху вниз** (1 → 2 → 3) и берут
  самый верхний маркер. Раньше `find -maxdepth 3 | head -1` возвращал произвольный —
  порядок обхода не гарантирован.

### Added
- **Тест C13** — архив с вложенной репой, у которой свои `.repo-id` и `VERSION`: корневой
  маркер выигрывает, версия публикуется, вложенная репа не создаётся; при уборке зеркала
  тоже побеждает корневой.
- **Класс PIT-F** в лидерборде с двумя случаями и выводом: комментарий-предупреждение
  рядом с кодом слабее проверки в тесте.

### Verified
- 274 проверки зелёные, 72 кейса.

---

## [4.3.0] — 2026-07-30 — Опознание репы изнутри: `.repo-id` (MINOR)

Скрипт опознавал репу по **имени архива** — внешнему признаку, который можно переименовать.
Из-за этого `REPO_MAP` (finpilot → personal-finance-dss) держался на конфиге, а не на
содержимом, а распакованные копии без `VERSION` не опознавались вовсе: у FINPILOT этого
файла нет, практика новее самого репозитория.

### Added
- **Проверка `.repo-id`** — файл в корне со строкой `owner/repo`. Читается **прямо из zip**,
  до создания репозитория: при несовпадении с целевой репой публикация версии отклоняется,
  и репа-сирота не заводится. Сравнение идёт с репой **после `REPO_MAP`** — у finpilot имя
  архива и репа намеренно разные, это не ошибка. Файла нет — публикация идёт, в выводе
  напоминание.
- **`find_repo_id` и `find_version_file`** — поиск маркеров на глубине до 3 уровней.
  Распаковщик macOS добавляет уровни обёрток, и угадывать структуру по именам бессмысленно.
- **Уборка зеркал опознаёт каталог по `.repo-id`**, а не по имени папки. Нет ни одного
  маркера — каталог не трогается, и сообщение говорит, что искали оба и до какой глубины.
- **`00-infrastructure/48-repo-identity.md`** — стандарт для рассылки по репам: зачем,
  таблица «без маркера / с маркером», внедрение в 6 шагов, частые ошибки, что будет если
  не внедрять.
- **`.repo-id` в самой base-repo** и шаблон `templates/repo-id.template`.
- **Тесты C12** (несовпадение → отказ и репа не создана; правильный → публикация без
  напоминания; отсутствие → напоминание, но публикация идёт) и **G14** (зеркало опознано
  по `.repo-id` на глубине 3; без маркеров каталог цел).

### Verified
- 269 проверок зелёные, 71 кейс.

---

## [4.2.1] — 2026-07-30 — Скрипт падал в zsh на несовпавшем шаблоне (PATCH)

Боевой прогон оборвался на строке `cleanup_mirrors:25: no matches found`. Причина —
различие оболочек: **zsh считает несовпавший glob фатальной ошибкой и прекращает скрипт**,
bash оставляет шаблон строкой. Тесты идут в bash, владелец запускает в zsh. Четвёртый
случай класса PIT-B.

### Fixed
- **`setopt no_nomatch` в шапке** — снимает фатальность несовпавших шаблонов, когда скрипт
  запущен в zsh. Заодно `sh_word_split` для единообразия разбиения слов.
- **Создание рабочей папки и регистрация `trap` перенесены выше первого шага.** Уборка
  зеркал на раннем выходе «архивов нет» обращалась к `$WORK`, которого ещё не существовало
  → при `set -u` ошибка → код возврата 1 на успешном исходе. Случай №7 класса PIT-A.

### Added
- **`reports/incidents/PITFALLS.md`** — лидерборд повторяющихся дефектов со счётчиками:
  5 классов, 18 случаев. Отсортирован по частоте, с проверочным вопросом и механической
  защитой для каждого класса. Правило: счётчик дошёл до 3 → нужна проверка, а не заплатка.
- Ссылка на лидерборд в шапке скрипта, в `tests/README.md` и нулевым пунктом порядка
  изменения поведения в `deploy-SPEC.md`.

### Verified
- 259 проверок зелёные, 68 кейсов. Код возврата на пустой папке — 0.

---

## [4.2.0] — 2026-07-30 — VERSION на глубине 2, ревизия порядка объявлений (MINOR)

Каталог `finpilot_v6_25_0_intl/` не убирался: `VERSION` лежал уровнем глубже. macOS
распаковывает `foo.zip` в папку `foo/`, а у архива внутри уже есть обёртка — файл
оказывается на глубине 2. Ревизия скрипта нашла ещё одно: функция-обработчик `trap`
объявлялась после первого шага, из-за чего правило «все функции до шагов» было
непроверяемым.

### Fixed
- **`VERSION` и `.git` ищутся в корне И на уровень глубже.** Требование наличия `VERSION` —
  осознанная защита (владелец подтвердил), но у распакованных архивов с обёрткой файл
  лежит глубже. `.git` тоже: рабочий клон внутри обёртки теперь распознаётся.
- **`cleanup_work` перенесена в блок функций.** Работала корректно (объявлялась до
  регистрации `trap`), но нарушала инвариант порядка и делала его непроверяемым —
  а именно этот класс дефекта убил режим `VERIFY`.

### Added
- **Тесты G13 п.6б/6в** — зеркало с `VERSION` на глубине 2 убирается; git-клон на глубине 2
  остаётся. Обе фикстуры используют репы с разбираемыми именами (первая попытка провалилась
  как раз потому, что имена вида `mir-repo-v1.0.0-deep` версией не парсятся).
- **Тест G10** дополнен: `DELETE_AFTER=0` сохраняет архив — флаг оставлен для совместимости.

### Changed
- **Инвариант порядка объявлений внесён в ревизию** как проверяемый одной командой:
  последнее `^func(){` должно идти до `# --- ШАГ 1`.

### Verified
- 259 проверок зелёные, 68 кейсов. `shellcheck -S warning` чисто. Без тестов осознанно
  остаются `BRANCH`, `RETRIES`, `GH_TIMEOUT`.

---

## [4.1.1] — 2026-07-30 — Зеркала убираются независимо от наличия архива (PATCH)

Уборка зеркал шла по списку архивов, найденных **в этом прогоне**. Каталог
`portrait-of-taste-v3_3_7/` остался, потому что его zip удалили прошлым запуском —
и в список этого прогона версия не попала. Четвёртый случай подряд одного класса:
код достижим только в части сценариев.

### Fixed
- **Каталоги ищутся в папке напрямую**, а не через индекс архивов. Имя разбирается тем же
  правилом, что и у архивов: точки, подчёркивания, постфикс сборки (`_intl`), `REPO_MAP`.
  Наличие архива больше ни на что не влияет.
- **Уборка работает и на раннем выходе «архивов нет».** Раньше скрипт в этой ветке выходил
  до блока уборки — то есть когда все архивы уже удалены, зеркала не убирались никогда.
  Логика вынесена в `cleanup_mirrors` и вызывается на обоих путях.

### Added
- **Тест G13, пункт 7** — зеркало версии, чей архив удалён предыдущим прогоном, убирается.
  Это ровно тот случай, который у владельца не работал.

### Verified
- 256 проверок зелёные, 68 кейсов.

### Notes
- Признак этого класса дефектов, который стоит проверять до выпуска: «работает ли это,
  если входных данных для шага нет, но результат его работы всё ещё лежит на диске».

---

## [4.1.0] — 2026-07-30 — Уборка распакованных зеркал (MINOR)

Архивы убирались, а распакованные каталоги рядом с ними оставались: `base-repo-v1.26.0/`,
`portrait-of-taste-v3_3_5/` и подобные. Содержимое то же, что в репе, а место занимают.

### Added
- **Уборка распакованных зеркал** в том же проходе, что и архивы. Пять условий, все
  обязательны: имя разбирается как `<repo>-vX.Y.Z` (точки или подчёркивания), внутри
  `VERSION` совпадает с версией из имени, внутри **нет** `.git`, версия полностью
  на GitHub, репа обработана в этом прогоне.
- **Инвариант И13** в контракте.
- **Тест G13** — шесть проверок: зеркала с точками и с подчёркиваниями убираются;
  git-клон, каталог без `VERSION`, каталог с чужой версией внутри и рабочая папка без
  версии в имени остаются нетронутыми.

### Changed
- Каждый неудалённый каталог называется с причиной (`это git-клон`, `нет VERSION`,
  `VERSION не совпал`, `на GitHub не всё`) — молча ничего не пропускается.

### Notes
- Удаление каталога опаснее удаления архива: под именем без версии может лежать рабочая
  копия с несохранённой работой. Поэтому условий пять, а не одно, и каждое проверено
  отдельным утверждением в тесте.

---

## [4.0.0] — 2026-07-29 — Уборка опубликованных архивов стала поведением по умолчанию (MAJOR)

Удаление сидело за флагом `DELETE_AFTER=1`, а владелец запускает скрипт без флагов
в 99% случаев — то есть функция была, но не работала там, где нужна. Классическая ошибка:
сделал возможность вместо правильного поведения.

### Changed
- **MAJOR: `zsh deploy.sh` сам убирает архивы, полностью уехавшие на GitHub.** Критерий
  не изменился — тег, релиз и канонический ассет должны быть подтверждены. Архив, доехавший
  до GitHub, дубликат того, что уже в репе; держать его на диске незачем.
- **`KEEP_ARCHIVES=1`** — отключить уборку. `DELETE_AFTER` оставлен для совместимости.
- Подсказка про `VERIFY=1` печатается только когда уборка отключена: при включённой она
  бессмысленна.

### Added
- **Тест A7** — дефолтный прогон без флагов убирает архив и печатает блок уборки;
  с `KEEP_ARCHIVES=1` архив остаётся и подсказка про `VERIFY` появляется.
- Тестовый харнесс по умолчанию сохраняет архивы (кейсы переиспользуют их после публикации);
  проверки уборки отключают это явно.

### Verified
- 248 проверок зелёные, 67 кейсов.

### Notes
- Третий раз за сессию правильным решением оказалось «сделать поведением, а не флагом».
  Признак: пользователь просит функцию, которая уже есть — значит она за флагом, о котором
  надо помнить, а помнить он не должен.

---

## [3.4.0] — 2026-07-29 — Автоудаление работает для всех опубликованных архивов (MINOR)

`DELETE_AFTER=1` удалял только то, что опубликовал **этот же** прогон. Архив, уехавший
на GitHub раньше, оставался лежать: тег уже есть → версия пропускается → до кода удаления
дело не доходит. Ровно тот же класс дефекта, что был с чисткой ассетов — код в ветке,
недостижимой в самом частом сценарии.

### Fixed
- **Автоудаление вынесено в отдельный проход** в конце прогона, по **всем** архивам папки.
  Критерий один: версия полностью на GitHub. Кто и когда её туда положил — неважно.
- **Проверка «полностью опубликовано» стала общей функцией** `missing_parts` для `VERIFY`
  и `DELETE_AFTER`. Раньше это были две копии логики, которые неизбежно разъехались бы.
- **`grep -qx` → `grep -Fxq`** и в проверке `VERIFY`: там оставалась та же ловушка с точкой
  в имени, из-за которой раньше терялись ассеты.

### Changed
- Блок автоудаления показывает, что удалено и что оставлено — с указанием, чего не хватает.
- Подсказка про `VERIFY=1` не печатается, когда автоудаление уже отработало.

### Added
- **Тест G12** — архив, опубликованный **прошлым** прогоном, удаляется следующим запуском
  с флагом; архив без релиза остаётся, причина названа.

### Verified
- 245 проверок зелёные, 67 кейсов.

### Notes
- Третий случай подряд одного класса: рабочий код в недостижимой ветке. Признак — «сделал,
  но у пользователя не сработало». Проверять надо не наличие кода, а достижимость пути
  в том сценарии, ради которого он писался.

---

## [3.3.1] — 2026-07-29 — Пустая папка — это успех, а не ошибка (PATCH)

После автоудаления архивов повторный прогон писал «ОШИБКА: версионных архивов
к публикации нет» и возвращал ненулевой код. Но пустая папка — ровно тот результат,
к которому шли: всё опубликовано и подчищено. Формулировка вдобавок врала — про
«отброшено как копии», хотя отбрасывать было нечего.

### Fixed
- **Пустая папка больше не отказ.** Зелёное «Публиковать нечего», перечень того, что
  в папке осталось (служебные загрузки, копии, архивы без версии), путь папки и напоминание
  про канон имени. Код возврата `0` — важно для расписаний и автоматизации.

### Added
- **Тест C11** — после `DELETE_AFTER=1` повторный прогон говорит спокойно, без слова
  «ОШИБКА», и возвращает `0`. Кейс C8 дополнен проверкой кода возврата.

### Verified
- 239 проверок зелёные, 66 кейсов.

### Notes
- Отдельный класс дефектов: **нормальный конечный результат, поданный как отказ.** Ломается
  доверие к выводу — человек ищет проблему там, где всё сделано правильно.

---

## [3.3.0] — 2026-07-29 — Защита от переполненного диска и обрыва, автоудаление архивов (MINOR)

Прошлый инцидент дал цепочку: забытые рабочие папки съели диск → `cp` оборвался → скрипт
не проверил код возврата → на GitHub уехал недокопированный ассет. Закрыты все три звена.
Плюс архив теперь можно удалять сразу после публикации, не дожидаясь отдельного прогона.

### Added
- **`MIN_FREE_MB`** (по умолчанию 2048) — прогон не стартует на переполненном диске
  и объясняет, чем это грозит. Порог настраивается.
- **Проверка размера залитого ассета**: после загрузки размер на GitHub сверяется с локальным.
  Обрыв связи и нехватка места дают частичный файл, который выглядит как успешная загрузка.
  Размер сверяется, только если пришёл числом — «не смог узнать» не превращается в отказ.
- **`DELETE_AFTER=1`** — локальный архив удаляется сразу после публикации, но **только**
  когда подтверждены три вещи: тег на remote, релиз и ассет. Версия без релиза (нет секции
  в CHANGELOG) архив не теряет.
- **Инвариант И12** в контракте.
- **Тесты G10** (удаление только при полном комплекте; без флага и без релиза — не удаляет),
  **G11** (печатаемая команда переносима и работает с пробелами в путях),
  **C10** (прогон не стартует без места и ничего не публикует).

### Fixed
- **`cp` без проверки кода возврата** — источник недокопированного ассета. Теперь при
  неудачном копировании ассет не трогается вовсе.
- **Печатаемая команда удаления не работала третий раз подряд.** `xargs -d` нет в BSD,
  `tr '\0'` BSD тоже не понимает (нужно `\000`). Заменено на `while IFS= read -r`, который
  работает везде и корректно обрабатывает пробелы в путях. Проверено запуском.
- **Ассет только что созданного релиза не помечался подтверждённым** — ветка создания
  выходит из функции раньше, и автоудаление для новых версий не срабатывало.

### Verified
- 232 проверки зелёные, 65 кейсов.

---

## [3.2.0] — 2026-07-29 — Рабочая папка больше не оседает на диске (MINOR)

Скрипт складывал клоны всех реп в `~/Downloads/repo_deploy_<дата>` и удалял их **последней
строкой**. Любое прерывание — и папка оставалась. У владельца накопилось три штуки на ~5 ГБ,
диск переполнился прямо посреди прогона, и загрузка ассета прошла с оборванным файлом.

### Fixed
- **Рабочая папка переехала во временную директорию** (`mktemp -d` с учётом `TMPDIR`).
  В `Downloads` больше ничего не создаётся.
- **Уборка через `trap` на `EXIT INT TERM`** — срабатывает при нормальном завершении,
  при ошибке и при `Ctrl+C`. Раньше стояла последней строкой, и до неё не доходило;
  ранние выходы (`DRY`, `VERIFY`) не убирали за собой вовсе.

### Added
- **`KEEP_WORK=1`** — осознанно оставить папку для отладки, путь печатается в выводе.
- **Тест G9** — после обычного прогона, после `DRY`, после `VERIFY` не остаётся ни папки
  в `Downloads`, ни мусора во временной директории; с `KEEP_WORK=1` путь назван.
- **Инвариант И11** в контракте.

### Verified
- 220 проверок зелёные, 62 кейса.

### Notes
- Побочный ущерб этого дефекта — обрыв `cp` по нехватке места: скрипт не проверил код
  возврата и залил недокопированный файл. Проверка `cp` — следующая правка.

---

## [3.1.1] — 2026-07-29 — Контракт поведения отдельным документом (PATCH)

Поведение скрипта было описано в трёх местах вперемешку с реализацией: в ТЗ времён
консолидации, в справочнике режимов и в записях этого журнала. Чтобы менять поведение
осознанно, нужен один документ, который отвечает **что гарантируется**, не касаясь **как**.

### Added
- **`templates/deploy-SPEC.md`** — контракт чёрного ящика: назначение одной фразой, вход,
  пять обязательных пунктов на выходе, **десять инвариантов** (каждый закреплён тестом
  и объяснён случаем, из-за которого появился), таблица решений по всем входным ситуациям,
  раздел «чего не делает никогда», поведение при отказах, порядок изменения поведения.
- Ссылка на контракт в шапке скрипта и таблица служебных файлов в `templates/README.md`.

### Changed
- **`reports/requirements/deploy_script_spec.md`** помечен как исторический: он ценен
  постановкой задачи и обоснованием решений, но действующий контракт теперь в `SPEC`.

### Notes
- Порядок изменения поведения: сначала контракт → тест → код → версия → `DRY=1` на боевой
  папке. Если правка не выражается таблицей решений, она не продумана — и пользователю
  её тоже не объяснить.

---

## [3.1.0] — 2026-07-29 — Потеря ассетов из-за точки в regex, коммиты с тезисом (MINOR)

Разбор боевого прогона вскрыл потерю данных: у `portrait-of-taste` часть релизов осталась
вообще без zip-ассета. Причина — точка в имени файла трактовалась как метасимвол регулярки.
Плюс закрыты ещё три дефекта того же прогона.

### Fixed
- **Точка в имени ассета матчила что угодно.** Проверка «канонический ассет уже есть» шла
  через `grep -qx "$_aname"`, а `portrait-of-taste-v1.0.1.zip` как шаблон совпадает
  с `portrait-of-taste-v1_0_1.zip` — точка матчит подчёркивание. Итог: канон считался
  присутствующим, загрузка пропускалась, а легаси-ассет удалялся как дубль. Релиз
  оставался с двумя авто-архивами GitHub и без своего. Заменено на `grep -Fxq`
  (буквальное сравнение).
- **Команда удаления не работала на macOS.** Печатали `xargs -d '\n'`, но BSD-версия
  ключа `-d` не знает — у владельца команда падала с `invalid option`. Теперь переносимый
  вариант: `tr '\n' '\0' | xargs -0 rm --`.
- **Существующая репа считалась отсутствующей при сбое связи.** Скрипт лез создавать
  `family` и получал `GraphQL: Name already exists`. Введено три состояния (есть / нет /
  связь не отвечает), как уже сделано для релизов.
- **Сообщение коммита было техническим** (`release: v1.2.3 — tree sync`).

### Changed
- **Коммит версии несёт `<repo> vX.Y.Z — <тезис>`** — то же, что заголовок релиза, из той же
  секции CHANGELOG. `git log --oneline` теперь читается как оглавление проекта.

### Added
- **Тест G7** — канона нет, есть только легаси с подчёркиваниями: канон обязан появиться,
  релиз не остаётся без ассета.
- **Тест A8** — в сообщении коммита есть репа, версия и тезис; нет «tree sync».
- **Тест G8** — печатаемая команда удаления не содержит `-d` и использует `-0`.

### Verified
- 215 проверок зелёные, 61 кейс.

### Notes
- Урок: имя файла — не регулярное выражение. Любое сравнение имён должно быть буквальным
  (`grep -F`, `[ "$a" = "$b" ]`), иначе точка в версии тихо превращает проверку в ложное «да».

---

## [3.0.1] — 2026-07-29 — Только яркие цвета (PATCH)

Тёмный текст на чёрном фоне не виден. Прошлые правки убирали конкретные проявления —
сначала переменную, потом функцию, — но не запрещали сам класс. Теперь запрет описан
как белый список и проверяется тестом.

### Fixed
- **Все цвета переведены на яркие (91–97).** Было: `31–36` — тусклые варианты палитры,
  `1m` без цвета у `bld()` и печать вообще без кода у `plain()` — оба брали цвет терминала
  по умолчанию, а он на тёмной теме тёмный. Стало: каждая функция печати несёт свой
  яркий код, `bld()` — `1;97`, `plain()` — `97`.

### Added
- **Тест I4 переписан на белый список.** Допустимы только `0m`, `1;97m`, `91–97m`;
  любой другой код валит тест. Плюс проверка, что каждая из семи функций печати
  действительно печатает со своим цветом, а не полагается на терминал.

### Verified
- 208 проверок зелёные, 58 кейсов.

---

## [3.0.1] — 2026-07-29 — Только яркие цвета (PATCH)

Прошлые две правки цвета били мимо. Убрал `dim`, но не заметил главного: две функции
печатали **без кода цвета вообще** — `bld()` только жирностью, `plain()` вообще ничем.
Обе брали цвет терминала по умолчанию, а он на тёмной теме тёмный. Плюс остальные
использовали тусклые варианты палитры (31–36), которые на чёрном фоне тоже плохо читаются.

### Fixed
- **Вся палитра переведена на яркие коды**: `91` красный, `92` зелёный, `93` жёлтый,
  `95` пурпурный, `96` голубой, `97` белый, `1;97` жирный белый.
- **`bld()` и `plain()` больше не полагаются на цвет терминала** — каждая строка несёт
  собственный яркий код.
- Запрещены и проверяются тестом: `30m` (чёрный), `90m` (серый), `2m` (тусклый),
  голая жирность `1m`, печать без кода.

### Added
- **Тест I4 ужесточён до белого списка**: в скрипте допустимы только `0`, `97`, `1;97`,
  `91`, `92`, `93`, `95`, `96`. Любой другой код — падение. Плюс проверка, что каждая
  из семи функций печати выводит со своим цветом.

### Verified
- 208 проверок зелёные, 58 кейсов.

### Notes
- Три захода на один баг, потому что дважды чинил симптом: сначала обнулил переменную,
  потом удалил функцию, и только на третий раз проверил **каждую** точку вывода. Правильный
  порядок был обратный — сразу перебрать все места, где что-то печатается.

---

## [3.0.0] — 2026-07-29 — Цвет, чистка ассетов и подсказка об удалении (MAJOR)

Три претензии владельца, все обоснованные: тусклый текст не виден на тёмном фоне; чистка
дублей-ассетов требовала спецфлага, о котором надо помнить; про освобождение диска скрипт
не говорил вообще ничего, хотя режим для этого есть.

### Changed
- **MAJOR: чистка дублей — часть приведения к стандарту, а не отдельный флаг.** Ассет той
  же версии под старым именем нарушает §42 («ровно три ассета»), поэтому снимается при
  обычной ревизии (`AUDIT`/`FORCE`/`ALL_REPOS`). Владелец не обязан помнить спецкоманду,
  чтобы получить релиз по стандарту. Страховка — `KEEP_LEGACY_ASSETS=1`.
- **Подсказка об освобождении места печатается в конце каждого прогона**, а не ждёт,
  пока про режим `VERIFY` вспомнят.

### Fixed
- **Тусклый цвет вырезан из скрипта полностью.** Прошлая правка обнулила переменную,
  но оставила функцию `dim()` и само понятие. Теперь нет ни функции, ни переменной,
  ни атрибута `2m` нигде в файле — второстепенное печатается обычным цветом терминала.
  Проверяется тестом по исходнику **и** по выводу.

### Added
- **Тест I4** — в скрипте нет `2m`, нет `30m`, нет `C_DIM`, и в реальном выводе нет dim.
- **Тест A7** — подсказка про `VERIFY=1` есть в каждом прогоне.

### Verified
- 202 проверки зелёные, 58 кейсов.

### Notes
- Урок: «обнулил переменную» ≠ «убрал возможность». Пока функция существует, ею
  воспользуются — надо удалять сущность, а не значение.

---

## [2.2.0] — 2026-07-29 — Полная ревизия: покрытие считается, а не оценивается (MINOR)

Баги находились только тогда, когда на них показывал владелец. Значит дело не в отдельных
багах, а в способе тестирования: тесты писались под каждый найденный дефект, то есть
покрывали лишь те пути, по которым уже прошёл человек. Проведена системная ревизия —
инвентаризация всех режимов, статический анализ, перечень деструктивных операций.
Отчёт: `reports/testing/deploy_audit.md`.

### Fixed
- **`VERIFY` не работал вообще.** Блок стоял в файле раньше объявления `gh_try` и
  `release_state` — каждый вызов падал с `command not found`, ошибка глушилась, и режим
  всегда отвечал «ДЕРЖАТЬ». То есть инструмент, отданный для решения об удалении архивов,
  не мог сказать «безопасно» ни при каких условиях. Все функции подняты выше первого
  исполняемого шага.
- **Список к удалению ломал `xargs`.** Пути писались с отступом, как в выводе на экран.
  Отступ срезается; тест проверяет не формат, а что `xargs` реально находит файлы.
- **Мёртвый код.** `CL_USED` и `IS_NEW` удалены. `SCRIPT_VERSION` не использовалась —
  теперь печатается первой строкой вывода, чтобы по логу было видно, какая ревизия отработала.

### Added
- **Тесты 52–56**: `VERIFY` (полный комплект / нет ассета / нет релиза / никогда не
  публиковался), `PRIVATE` (приватность по умолчанию и `PRIVATE=0`), переопределение
  `SERVICE_RE` / `REPO_MAP` / `VARIANT_REPOS` / `MIN_FILES`, режим `ASSET=0`.
- **`reports/testing/deploy_audit.md`** — методика ревизии, найденное, проверенное-и-исправное,
  команда инвентаризации покрытия для будущих версий.

### Verified
- 197 проверок зелёные, 56 кейсов.
- `shellcheck -S warning`: чисто. Предупреждения SC2221/SC2222 проверены **запуском**
  на пяти входах и признаны ложными — shellcheck не разбирает перенос строки в списке
  паттернов `case`.
- Режимов без тестов: 3 из 20 (`BRANCH`, `RETRIES`, `GH_TIMEOUT`) — низкий риск, осознанно.

### Notes
- Правило: новый флаг не считается готовым, пока по нему нет теста. Код, который не
  выполнялся ни разу ни в одном тесте, — не рабочий код, а предположение о том, что он рабочий.

---

## [2.1.2] — 2026-07-29 — Чистка ассетов приведена в безопасный вид и покрыта тестами (PATCH)

Разрушительная операция (удаление ассетов) была написана, но **ни разу не выполнялась**:
в стабе `gh` не было `delete-asset`, а тестов на неё не существовало. Проверка по прямому
вопросу владельца вскрыла в этом коде два содержательных дефекта — оба закрыты, путь
покрыт тестами, включая проверки того, что удалять **нельзя**.

### Fixed
- **Удаление шло ДО загрузки канонического ассета.** При обрыве сети между удалением и
  загрузкой релиз оставался вообще без ассета — необратимая потеря. Теперь чистка идёт
  отдельным блоком **после** загрузки и только если канонический ассет подтверждён
  на релизе; иначе пропускается с пояснением.
- **Чистка была недостижима в реальном сценарии.** Код сидел внутри ветки «канонического
  ассета нет». У уже обработанных релизов ассет есть, значит ветка не выполнялась и флаг
  не делал ничего. Вынесен в самостоятельный блок.

### Added
- **Предупреждение о бесполезном флаге.** `DROP_LEGACY_ASSETS=1` без `AUDIT`/`ALL_REPOS`
  не обходит опубликованные релизы — скрипт говорит об этом прямо, а не молчит.
- **`DRY=1` покрывает и чистку**: показывает, что снял бы, ничего не удаляя.
- **Тесты 49–51.** Что снимается: дубль той же версии в обоих написаниях
  (`v6.6.0` и `v6_6_0`). Что **не** снимается: канонический ассет, zip другой версии,
  не-zip вложения. Что не происходит: без флага — ничего, при `AUDIT+FORCE` без флага —
  ничего, в `DRY` — ничего, без канонического ассета — ничего.
- **Стаб `gh` научился `delete-asset`** — до этого путь физически не мог выполниться в тестах.

### Verified
- 174 проверки зелёные, 51 кейс.

---

## [2.1.1] — 2026-07-29 — Читаемый вывод и точечная чистка ассетов (PATCH)

Второстепенные строки печатались атрибутом «тусклый» — на тёмной теме терминала это
чёрное на чёрном, текст не виден вообще. Плюс уточнено, что именно снимает чистка
легаси-ассетов, чтобы флаг не мог задеть лишнее.

### Fixed
- **Невидимый текст.** Атрибут `\033[2m` (dim) убран совсем: второстепенное печатается
  обычным цветом терминала и читается на любой теме — светлой и тёмной.
- **`DROP_LEGACY_ASSETS` бил слишком широко.** Раньше снимал любой `.zip`, кроме
  канонического. Теперь — только архив **той же версии** под старым именем
  (`finpilot_v6_6_0_intl.zip` при канoне `personal-finance-dss-v6.6.0.zip`), в обоих
  написаниях версии. Прочие вложения не трогаются: удаление ассета необратимо.

### Added
- **`templates/deploy-MODES.md`** — справочник по всем режимам и ключам: пять команд на
  все случаи, таблицы по области и глубине работы, рецепты, «чего скрипт не делает
  никогда». Ссылка на него — в шапке самого скрипта.

---

## [2.1.0] — 2026-07-29 — Ревизия всех реп системы, чистка легаси-ассетов (MINOR)

`AUDIT=1` брал только те репы, для которых в папке нашлись архивы, — репу без свежей
зипки было не проверить вообще. Теперь список можно взять из `repos-map.md`: ревизия
охватывает всю систему, а не только то, что лежит в Downloads.

### Added
- **`ALL_REPOS=1`** — список реп из `repos-map.md` (фолбэк — `gh repo list`), включает
  `AUDIT` автоматически. Репозиторий проверяется, даже если архива в папке нет.
- **`DROP_LEGACY_ASSETS=1`** — снимает с релиза ассеты со старым неканоничным именем.
  По §42 ассетов ровно три, а после бэкфилла у части релизов их четыре: канонический
  и легаси рядом. Удаление необратимо, поэтому только по явному флагу.
- **Тест 48** — репа из карты без архива в папке: без флага не трогается, с `ALL_REPOS=1`
  подхватывается и приводится к стандарту.

### Fixed
- **Имена реп из карты извлекались не полностью.** Шаблон требовал бэктик сразу после
  решётки, а авто-регистрация пишет `## 🆕 \`repo\`` — то есть ровно те репы, которые
  завёл сам деплойер, в список не попадали. Теперь берётся первое имя в бэктиках из
  любой строки-заголовка.

### Verified
- 162 проверки зелёные, 48 кейсов.

---

## [2.0.0] — 2026-07-28 — Режимы, ретраи, проверка перед удалением архивов (MAJOR)

Первый полный боевой прогон по всем репам занял около двух часов, потому что скрипт
на каждом запуске проходил ревизию всех релизов подряд. Теперь по умолчанию он делает
только то, ради чего запускается: публикует новые версии из папки. Ревизия истории
вынесена в отдельный режим. Заодно закрыты три дефекта, которые вскрыл прогон на
нестабильной сети, и добавлена проверка «что уже безопасно удалить локально».

### Changed
- **MAJOR: дефолт изменён.** `zsh deploy.sh` = коммит + тег + релиз только для новых
  версий из папки. Проверка старых релизов больше не запускается автоматически.
- **`AUDIT=1`** — полная ревизия всех релизов всех реп (прежнее поведение по умолчанию).
  `REPAIR=1` и `FORCE=1` включают её автоматически.

### Added
- **`SCRIPT_VERSION`** в шапке + этот журнал.
- **`VERIFY=1`** — проверка перед удалением локальных архивов: для каждого архива
  сверяет наличие тега, релиза и канонического ассета на GitHub. Безопасные пишет
  в `~/Downloads/safe_to_delete.txt`, остальные показывает с причиной.
- **`GH_TIMEOUT`** (по умолчанию 120 с) — предел на один вызов `gh`.
- **`gh_try`** — ретраи с нарастающей паузой, но **только** на признаки сбоя связи
  (таймаут, обрыв, 502/503/504, rate limit). «Не найдено» и отказы прав не ретраятся.
- **`release_state`** — отличает «релиза нет» от «запрос не прошёл». При сбое связи
  версия помечается и пропускается, а не считается отсутствующей.

### Fixed
- **`FORCE=1` валил версии без секции в CHANGELOG.** Пустой notes-файл уходил в
  `gh release edit --notes-file`, и версия падала с «не удалось обновить». Теперь тело
  правится только когда есть чем; при отсутствии секции описание и заголовок остаются
  как были, а версия называется в выводе.
- **Утечка `TITLE` между итерациями.** Если notes-файл уже был создан на этапе публикации,
  `build_notes` не вызывался — и версия получала заголовок предыдущей. Так тег `v1.17.0`
  base-repo получил заголовок v1.18.0. Теперь заголовок считается заново для каждой версии.
- **Разнобой шапок в теле релиза.** Секции вида `## vX.Y.Z — …` (без скобок) уезжали
  в релиз как есть, и такие релизы выглядели иначе. Шапка нормализуется к канону
  `## [X.Y.Z] — дата — тезис (BUMP)`; содержимое секции не трогается.
- **Зависание на нестабильной сети.** Вызовы `gh` шли без таймаута — прогон мог стоять
  десятками минут на одной версии.

### Verified
- 158 проверок зелёные, 47 кейсов. Новые: 45 (FORCE без секции), 46 (нормализация шапки),
  47 (дефолт не запускает ревизию, `AUDIT=1` запускает).

---

## [1.x] — до 2026-07-28

История версий скрипта до введения собственного журнала — в `CHANGELOG.md` base-repo,
записи v1.12.0 … v1.18.0: консолидация восьми предшественников, стандарты нейминга и
упаковки, поиск CHANGELOG по всему дереву, запрет пустых релизов, встроенный аудит.
