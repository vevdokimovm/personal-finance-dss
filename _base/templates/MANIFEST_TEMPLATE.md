# Манифест репы `<имя>`

> **Шаблон.** Заполняется при ревизии репы — из **одного чтения** вместе с описанием
> и классификацией (`_base/00-infrastructure/87-file-to-repo-routing.md`).
> Заказ владельца 22.08.2026: *«раз ты каждую репу будешь изучать досконально и вручную —
> напиши каждой манифест подробно, обнови описание, чтобы оно было не одно-два слова,
> а нормальное достаточно подробное, и теги поставь»*.
>
> 🔴 **Шаблон заведён до того, как написан первый манифест** — намеренно. 28 реп
> получили `START-HERE.md` без шаблона и разъехались; повторять это на 61 манифесте
> дороже вдвое (`ROADMAP.md` §P3 B2).

---

## 1. Что это за репа — в трёх предложениях

<Первое: предмет. Второе: чем отличается от соседней репы, с которой её путают.
Третье: что сюда НЕ кладут.>

**Класс:** `<core | satellite | temp:<родитель> | product | infra | profile>`
(`_base/00-infrastructure/76-repo-classes.md`)
**Видимость:** `<приватная | ПУБЛИЧНАЯ>` — 🔴 если публичная, личное сюда не кладётся
никогда, даже если предмет совпал (`PIT-123`)
**Версия на момент манифеста:** `<X.Y.Z>`

## 2. Описание для `.repo-meta` и GitHub

> Одно-два предложения, которыми репа представляется снаружи. **Не одно слово** —
> описание должно давать понять предмет тому, кто видит репу впервые.

```
<описание>
```

**Теги:** `<5–8 тегов через запятую: предмет, тип материала, язык, статус>`

## 3. Что внутри — по предмету, а не по каталогам

| Раздел | Файлов | Что там по существу |
|---|---|---|
| `<01-…/>` | <N> | <не «документы», а что именно и зачем> |

**Всего:** <N> файлов. <Если есть `90-imported/` — сказать отдельно, что оно
не проверено ревизией по построению.>

## 4. Классификация: что сюда идёт, а что нет

> Это часть общей таблицы маршрутизации (`87-file-to-repo-routing.md`), уточнённая
> для конкретной репы. **Уточняется от работы:** прошли репу — дописали строку.

**Сюда идёт:**

- <предмет и типичные типы файлов>

**Сюда НЕ идёт — и куда вместо:**

| Что | Куда | Почему не сюда |
|---|---|---|
| <тип материала> | `<репа>` | <граница одной фразой> |

**Пограничные случаи, решённые при ревизии:**

| Файл или тип | Решение | Основание |
|---|---|---|

## 5. Тяжёлое: эталоны и выжимки

🔴 Правило владельца: **эталон остаётся, рядом появляется `.md`-выжимка** — «и», а не «или».
Метод — `_base/07-media-to-text-lab/README.md`.

| Что | Сколько | Воспроизводимо? | Состояние |
|---|---|---|---|
| <тип> | <N файлов, МБ> | <да/нет> | <эталон + выжимка / только эталон / свёрнуто> |

## 6. Связи с другими репами

| Репа | Связь |
|---|---|
| `<имя>` | <граница или зависимость одной фразой> |

## 7. Состояние на момент манифеста

| | |
|---|---|
| ревизия пройдена | <дата> |
| мёртвых путей в README | <N> (`repo_revision.py`) |
| `_base/` версия | <X.Y.Z> |
| открытых задач | <N> (`ROADMAP.md`, `TASKS.md`) |
| секреты | <проверено `secrets_scan.py`: находок N> |

---

**Как заполнять.** Не по памяти и не по именам каталогов — командами:
`find` для чисел, `repo_revision.py` для путей, `secrets_scan.py` для секретов,
открыть и прочитать для предмета. Числа в манифесте сверяются гейтом с диском (`PIT-091`).
