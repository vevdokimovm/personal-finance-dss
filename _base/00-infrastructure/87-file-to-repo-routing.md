# 87 — Классификация: какой файл в какую репу

> **Заказ владельца 22.08.2026, дословно:** *«какая-то классификация по типу — какого типа
> файлы в какую репу идут. Это вдобавок к манифесту, чтобы было проще определять, что куда
> делегировать, а не на рандом или по наитию. Чем больше будем работать с конкретной репой,
> тем точнее будет классификация становиться»*.

🔴 **Зачем это понадобилось — из инцидента, а не из красоты.** Кампания переноса
22.08.2026 разложила 43 500 файлов по репам, назначая адрес **по имени папки-источника**:
`~/Documents/claude` → `claude-usage`. Внутри оказались три разные вещи, и одна из них —
`ADHD.pptx`, которой место в `misc-vault`. Владелец: *«нахера ты вообще в claude usage
положил срану презу про ADHD»*. Разбор — `PIT-124`.

> **Правила адресации не существовало — поэтому адрес брался из имени.**
> Имя папки это гипотеза о предмете, а не предмет.

Отличие от `76-repo-classes.md`: там ось **«база ↔ репа»** (правило или состояние).
Здесь ось **«какая именно тематическая репа»** — вопрос, который возникает уже после того,
как ясно, что файл не в базу.

---

## 1. Порядок решения — четыре вопроса, строго по очереди

Каждый следующий имеет смысл, только если предыдущий не дал ответа.

### Вопрос 0 (отсекающий). Это секрет?

Токен, ключ, коды восстановления, пароль → **не в репу вообще**,
`~/Documents/recovery-kits/` (`85-secrets-and-recovery-codes.md`).
Ответ «да» закрывает разговор: никакая тематическая репа не подходит.

### Вопрос 1. Это правило или состояние?

Правило (не изменится от смены целей) → **база**. Состояние → дальше
(`76-repo-classes.md` §«Два проверочных вопроса»).

### Вопрос 2. О чём файл ПО СОДЕРЖИМОМУ, а не по имени папки?

🔴 **Открыть и посмотреть.** Это тот самый шаг, пропуск которого дал `PIT-124`.
Папка `claude` содержала инструкции Claude (142 файла), выгрузки чатов (29) и случайное (3) —
три адреса, а не один.

### Вопрос 3. У предмета есть своя репа?

Есть → туда. Нет → `misc-vault` **осознанно**, а не «пока положу сюда».

> **`misc-vault` — не свалка, а решение «предмет не тянет на свою репу».**
> Записывается так же, как любое другое: с причиной.

---

## 2. Таблица маршрутизации — по предмету, не по расширению

Расширение почти ничего не говорит: `.pdf` бывает медкартой, законом и научной статьёй —
и это три разные репы. Поэтому таблица по **предмету**, а тип файла лишь уточняет,
что с ним делать дальше.

| Предмет | Репа | Типичные типы | Что делать с тяжёлым |
|---|---|---|---|
| анализы, медкарты, ПНД, прививки | `health-vault` | `.pdf` сканы, `.xlsx` динамика | 🔴 эталон остаётся **+** `.md`-выжимка |
| законы, повестки, доверенности, призыв | `legal-knowledge-base` | `.pdf`, `.docx` | эталон + выжимка |
| ВКР, курсовые, публикации, дипломы | `academic-portfolio` | `.pdf`, `.docx`, `.pptx`, `.tiff` | эталон сданного + выжимка |
| код, алгоритмы, учебные проекты, гисты | `it-base` | `.py`, `.sql`, `.ipynb` | 🔴 `.venv`/`node_modules` не переносятся |
| ML, нейросети, датасеты | `ml-base` | `.py`, `.ipynb` | датасет — служебкой, если публичный |
| вера, богословие, исповедь | `christ-walk` | `.md`, `.docx` | личное — как есть |
| психологические тесты, дневники, «кто я» | `self-map` | `.md`, `.pdf` | личное — как есть |
| мемуары, фото родных, семейные документы | `family` | `.pdf`, `.jpg`, `.docx` | эталон остаётся всегда |
| разборы утверждений, проверка фактов | `truth-seeking` | `.pdf`, `.md` | материал без разбора — служебкой |
| курсы, лекции, методички обучения | `edu-base` | `.pdf`, `.mp4`, `.docx` | видео → `ai-relay` на перевод |
| поступление в магистратуру | `master-admission` | `.pdf`, `.docx` | — |
| метод познания, логика, научный метод | `research-craft` | `.md`, `.pdf` | — |
| работа с Claude: замеры, лимиты, приёмы | `claude-usage` | `.md`, `.json` | 🔴 **публичная** — личное туда нельзя |
| звук и видео, требующие перевода в текст | `ai-relay` | `.mp3`, `.mp4`, `.wav` | это её предмет, а не балласт |
| всё остальное, осознанно | `misc-vault` | любые | по общему правилу |

🔴 **Публичные репы в этой таблице — особый случай.** `claude-usage`, `vk-graph`,
`algorithms-site`, `bron-kerbosch`, `health-report-generator`, `salvation`,
`vevdokimovm*` — витрины. Личное туда не кладётся **никогда**, даже если предмет совпал:
22.08.2026 в `claude-usage` уехали 152 файла личных инструкций, поймано перед сборкой
архивов (`PIT-123`). Проверка — свойством объекта, а не памятью:
`gh repo view … --json isPrivate`.

---

## 3. Что делать с тяжёлым — общее правило поверх таблицы

| Файл | Решение |
|---|---|
| **воспроизводим** (публичный датасет, канал, дистрибутив) | свернуть в служебку, оригинал удалить |
| **невоспроизводим** (скан, съёмка, рукопись) | 🔴 **эталон остаётся** + `.md`-выжимка рядом |
| регенерируемое (`.venv`, `node_modules`, `__pycache__`) | не переносится вовсе |
| архив `.zip` внутри репы | распаковать, содержимое — по таблице |

Метод перевода и требования к служебке — `../07-media-to-text-lab/README.md`.

---

## 4. Классификация уточняется от работы — как именно

Владелец назвал это прямо: *«чем больше будем работать с конкретной репой, тем точнее
будет классификация становиться»*. Механика:

1. Прошли репу ревизией → в её **манифест** пишется, что в ней уже лежит **по предмету**.
2. Случай, который таблица не покрыла, → строка сюда, с датой и примером.
3. Случай, где таблица **ошиблась**, → строка сюда **и** карточка `PIT`: ошибка адресации
   повторяема по построению.

> **Таблица, которую не правят после работы, через месяц описывает прошлую систему.**
> Признак живой классификации — дата последней правки не старше последней ревизии.

**Журнал уточнений:**

| Дата | Что уточнено | Из чего |
|---|---|---|
| 2026-08-22 | таблица заведена, 15 предметов | кампания переноса, 43 500 файлов, `PIT-124` |
| 2026-08-26 | правило «тяжёлое → эталон + выжимка» подтверждено на практике, не только в теории: `academic-portfolio` 1052→204 МБ, `misc-vault` 725→96 МБ | `PIT-146` |
| 2026-08-26 | экспорты чатов (`conversations.json`) — секреты внутри реальны, не гипотетичны: найден пароль БД в скопированной команде | `PIT-124`, доскан `secrets_scan.py --max-mb` |
| 2026-08-26 | `health-vault` (строка таблицы «эталон + выжимка») — правило записано, но **не применено**: PDF результатов анализов не пройдены. Открытый долг, не ошибка таблицы | п.5 «Чистка тяжёлого» этого же заказа |

## 5. Связки

`76-repo-classes.md` (классы реп и ось «база ↔ репа») · `85-secrets-and-recovery-codes.md`
(вопрос 0) · `07-media-to-text-lab/` (что делать с тяжёлым) ·
`06-volume-compression.md` (пороги по типам) · `repos-map.md` (что за репа по предмету).

---

## 6. Ось «вопрос → репа» (не «файл → репа»)

> 🆕 **Заведена 27.08.2026**, по заказу владельца: роль-диспетчер «быстрые ответы» нуждалась
> в опоре для маршрутизации **вопросов**, а не только файлов — до этой правки ось у таблицы
> была только одна (§1-2, куда положить файл по предмету). Вопрос и файл — разные объекты
> маршрутизации: у файла есть содержимое, которое можно открыть и посмотреть (Вопрос 2, §1);
> у вопроса в моменте есть только формулировка, и решение нужно **до** того, как появится
> артефакт для проверки.

**Как использовать.** Роль `03-role-kit/roles/quick-answer-router/` (или прямой разбор без
роли) — вопрос пользователя классифицируется по предмету, ниже называется репа-эксперт с
накопленным по теме материалом, и, если формат позволяет, даётся краткий прямой ответ по
общим знаниям с пометкой «для глубины — см. `<репа>`».

**Личное и приватное — сначала, ниже общие темы.** Личные данные (медицина, финансы,
психология, карьера, семья) идут вперёд общих тем — совпадение по предмету не означает
совпадение по чувствительности, и приватная репа с личными данными не заменяется публичным
общим знанием, даже если тема формально та же.

| Область вопроса | Репа | Комментарий |
|---|---|---|
| анализы, здоровье, медицина конкретного человека | `health-vault` | приватно, личные данные — не общий медицинский конспект |
| финансы личные, бюджет, кредиты конкретного человека | `personal-finance-dss` (продукт FINPILOT) / `money` | первое — личный трекер, второе — как устроены деньги вообще |
| психология, дневники, история жизни конкретного человека | `self-map` | приватно, личный анамнез — не общая психология |
| вера, богословие, исповедь | `christ-walk` | личное духовное — не общее религиоведение |
| семья, генеалогия, родные | `family` | приватно |
| карьера, работодатели, релокация конкретного человека | `career` | личный трек-рекорд + общие принципы оценки офера |
| философия, этика, эпистемология | `philosophy` | школы, тексты, этические фреймворки, теория познания |
| наука о продуктивности, обучении, привычках | `productivity` | доказательный подход, не мотивационная вода |
| метод познания: логика, научный метод, критическое мышление | `research-craft` | «как» познавать, не «что» конкретно проверить |
| проверка конкретных утверждений/мифов | `truth-seeking` | применение метода `research-craft` к конкретным заявлениям |
| STEM: алгоритмы, биология, химия, физика, математика | `algorithms`/`biology`/`chemistry`/`physics`/`mathematics` | по конкретному предмету вопроса |
| гуманитарные: история, лингвистика, политика, война, нации | `history`/`linguistics`/`politics`/`war`/`nationality` | по конкретному предмету; `war`/`nationality`/`politics` — держать нейтрально, без оценки текущей политики |
| коммуникация: риторика, продажи, актёрство, бизнес, стиль | `rhetoric`/`sales`/`acting`/`business`/`style` | по конкретному предмету |
| игры/спорт/безопасность: шахматы, CS:GO, спорт, кибербезопасность, tradecraft | `chess`/`csgo`/`sport`/`cybersecurity`/`spycraft`/`security-forces` | по конкретному предмету |
| скорочтение, learning science прикладной | `speed-reading` | узкий навык — не путать с общей `productivity` |
| визуальный/дизайн-вкус | `visual-style` | вкусовые критерии и референсы, не личный имидж (см. `style`) |
| аудио/видео → текст, интеграция ИИ-инструментов | `ai-relay` | маршрут перевода, не готовые расшифровки |
| код, IT-инженерия, ML | `it-base`/`ml-base` | практический код против учебного `algorithms` |
| методология разработки (TDD, CI/CD, инженерные решения) | `02-methodology-library/engineering_practices.md` (в базе, не отдельная репа) | философия «почему так», не конкретный проект |
| вопрос не попал ни в одну тему выше | — | не выдумывать репу под ответ; честно сказать, что специализированного материала нет, ответить по общим знаниям |

**Что роль-диспетчер НЕ делает:** не читает содержимое найденной репы за пользователя
автоматически — называет адрес и по возможности отвечает по общим знаниям сама; если
нужен ответ **из** содержимого репы (не общее знание, а то, что там реально записано) —
это уже не быстрый ответ, а обычная работа с конкретным репозиторием, роль за пределы
маршрутизации не выходит (см. `quick-answer-router/00_ROLE.md` «Границы»).
