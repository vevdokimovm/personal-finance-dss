# 🗂️ Карта репозиториев — переехала

**Найдено устаревшим 28.08.2026.** Этот файл — ранний черновик карты (11 реп),
не обновлялся с 26.08.2026, ни один скрипт и протокол на него не ссылается
(`grep` по всей `base-repo` — ноль вхождений `00-infrastructure/repos-map`).

**Актуальная карта — `../repos-map.md`** (корень `base-repo`, 63 репы, машинно
сверяется гейтом `check_repos_map_sync()`).

Протокол git push/pull, который раньше жил в конце этого файла, уже дублирован
и поддерживается в `01-repo-standard.md` — там актуальная версия.

Файл оставлен пустым указателем, а не удалён: у `base-repo` нет `.git`-истории,
удаление было бы необратимым.
