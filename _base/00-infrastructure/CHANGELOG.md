# CHANGELOG — переехал в корень

История инфраструктуры теперь в **`/CHANGELOG.md`** (корень репозитория).

Причина: журнал должен быть виден сразу при открытии репы, а инструменты должны находить его
без догадок. Правило записано в `46-changelog-placement.md`.

Этот файл оставлен указателем, чтобы старые ссылки не ломались.
