# 84. Аккаунты Claude: идентификаторы, границы, что даёт Team

> **Решение владельца 21.08.2026: файл живёт здесь, в базе.** Обоснование его же —
> **база приватная и раздаётся только приватным репам**. Первая редакция лежала
> в `mission-control` из опасения «раздастся в 57 реп»; владелец это опасение снял.
>
> 🔴 **Опасение было проверяемым, и предохранитель существует — не список, а факт.**
> `mission-control/scripts/sync-base.sh` перед раздачей спрашивает GitHub:
> ```bash
> [ "$(gh repo view "$O/$n" --json isPrivate --jq '.isPrivate')" = "false" ] && continue
> ```
> Публичная репа базу **не получает**, как бы ни выглядел список исключений.
> Это исполнение PIT-097 («список — намерение, свойство объекта — факт»).
>
> **Замер 21.08.2026:** `_base/` нет **ни в одной** из восьми публичных реп —
> `finpilot`, `vk-graph`, `bron-kerbosch`, `health-report-generator`,
> `algorithms-site`, `game-analytics-engine`, `claude-usage`, `salvation`.
> Предохранитель работает, а не только описан.
>
> **Org ID — не секрет:** сам по себе доступа не даёт, в отличие от токена
> (`00-CLAUDE-STOP.md` §2 не нарушается).

---

## Идентификаторы

🔴 **Вахт стало пять — 23.08.2026.** Владелец завёл новый аккаунт; данные сняты
командой из `~/.claude.json`, а не со слов.

🔴 **Буква вахты — это ИМЯ ПРОФИЛЯ, а не почта.** Названо владельцем 23.08.2026,
и это снимает догадку: первая редакция этой строки вывела букву из почты
(`finpilot.support@` → «F») и ошиблась.

| Вахта | Имя | Почта | Org ID | Заведён | Продление |
|---|---|---|---|---|---|
| **V** | **Vasilii** | `vevdokimovm@gmail.com` | `7cc76e8d-50ee-4d9e-a37d-e66972d7a846` | 23.01.2026 | 14.09, 20:10 |
| 🔴 **S** | **Sergey** | `gertab95@gmail.com` | `a583ff32-acec-4f10-90cd-d506845d9e6a` | — | **платёж не прошёл 18.08, доступ приостановлен** |
| **J** | **Jesus** | `vevdokimovm3@proton.me` | `bc43a107-d24a-4365-9b9e-ec7f590f6932` | — | — |
| **M** | **Michael** | — | `eb2988ac-e9ba-4fb0-9b05-29ea9cd46abb` | — | — |
| 🔴 **A** | **Adam** | `finpilot.support@proton.me` **(ЗАБЛОКИРОВАНА)** | `8b74743b-4ac3-4095-b681-6b07dc9020ee` | 23.08.2026, 05:05 | 23.09 — **риск повторного списания** |

`accountUuid` вахты A — `5432c8f1-2719-4ac3-b9cb-001479490541`, роль `admin`,
тип `claude_pro`.

### 🔴 Вахта A потеряна в день заведения — почтовый провайдер отключил ящик

**23.08.2026.** Аккаунт заведён в 05:05, подписка Pro оплачена в 05:11, к 19:50 Proton
отключил ящик: *«This account has been suspended due to a potential policy violation»*.
Апелляция подана, тикет `5104621`, ответ ждём на `gertab95@proton.me`.

**Почему аккаунт не восстановить своими силами** — у Claude **нет пароля**. Справка
дословно: *«It's not possible to create a dedicated password for your Claude account
at this time»*. Вход только двумя способами:

| Способ | Состояние |
|---|---|
| код на почту | ❌ ящик отключён |
| Google SSO | ❌ аккаунт заведён на Proton, не на Google |
| живая сессия | ❌ владелец вышел везде до того, как это выяснилось |

🔴 **И сменить почту на аккаунте нельзя в принципе:** *«it's not directly possible
to change the email on an existing Claude account»*. Документированный путь один —
отменить подписку, удалить аккаунт, завести новый.

### Вероятная причина блокировки — имя ящика

В форме апелляции Proton триггеры названы прямо, и один совпадает: **«Impersonating
other services»**. Адрес `finpilot.support@` для их автомата — ролевой адрес поддержки
коммерческого сервиса. Плюс это третий аккаунт Proton с одного устройства
(`vevdokimovm3@`, `gertab95@`), плюс регистрация из-под VPN.

> **Сервис без пароля делает почтовый ящик единственным ключом, и ключ этот держит
> третья сторона.** Отключение ящика обнуляет доступ к оплаченному аккаунту мгновенно,
> и обходного пути нет — ни пароля, ни резервного кода, ни смены адреса.

### 🔴 Правило: платный аккаунт — только на почту, которую нельзя потерять

| Плохо | Хорошо |
|---|---|
| свежий ящик у провайдера, который банит по эвристике | старый `@gmail.com` с историей |
| вход только по коду на почту | **Google SSO** — переживает потерю доступа к письмам |
| ролевое имя (`*.support`, `*.admin`, `*.noreply`) | личное имя |
| оплата в первые минуты жизни ящика | ящик прожил хотя бы неделю |

### 🔴 Найдена шестая почта, не сопоставленная ни с одной буквой

Разбор снимков 14.08.2026 (`07-media-to-text-lab/runs/2026-08-23-screenshots.md`) дал
вывод `/status` из сессии в `~/Documents/finpilot/frontend`:

```
Organization:   gertab95@proton.me's Organization
Email:          gertab95@proton.me
```

В том же кадре в браузере открыт claude.ai под профилем **Michael · Pro** — и
владелец подтвердил напрямую 27.08.2026: **`gertab95@proton.me` — почта вахты M
(Michael)**, вопрос закрыт.

📍 Тот же адрес `gertab95@` фигурирует в восстановлении Steam (`misc-vault/00-secrets-index/`)
как контактная почта — то есть он живой и используется вне Claude.

| Вахта | Почта | Как узнана |
|---|---|---|
| V | `vevdokimovm@gmail.com` | `/status`, снимок 20.08 |
| J | `vevdokimovm3@proton.me` | профиль «Jesus · Pro» в браузере, снимок 20.08 |
| **M** | `gertab95@proton.me` | `/status` 14.08 + профиль «Michael» в том же кадре; **подтверждено владельцем напрямую 27.08.2026** |
| A | `finpilot.support@proton.me` | `~/.claude.json`, 23.08 |
| S | `gertab95@gmail.com` | счёт Anthropic + профиль «Sergey · Pro» на том же экране, снимок 18.08 |

🔧 **Не обязательно вручную и не только для M.** С 25.08.2026 есть `panel_probe.py`
(`reports/experiments/token-consumption/`, скилла `/tokens`) — снимает `email` из
Status-панели без скриншота и вопроса владельцу. Сверить с таблицей выше — и вахта
известна точно, а не по контексту разговора. Найдено 27.08.2026: вахта весь батч
писала в `WATCHLOG §0` чужих реп «Вахта: V» по инерции из старого контекста, хотя
`panel_probe.py` сразу дал `finpilot.support@proton.me` = **A**. Команда:
```bash
python3 reports/experiments/token-consumption/panel_probe.py   # секция Status → email
```

### 🔴 И седьмая почта — в счёте Anthropic

Разбор снимка 18.08.2026 08:47 (`invoice.stripe.com`): счёт **Anthropic, PBC на US$20.00**,
инвойс `QUMWYXOI-0005`, выставлен на **`gertab95@gmail.com`'s Organization**.

Итого в обороте **семь адресов**, и почтовые домены у них разные при одинаковом логине:

| Логин | `@gmail.com` | `@proton.me` | `@mail.ru` |
|---|---|---|---|
| `vevdokimovm` | ✅ вахта V | — | — |
| `vevdokimovm3` | — | ✅ вахта J | — |
| **`gertab95`** | ✅ **счёт Anthropic $20** | ✅ вахта M | ✅ контакт Steam |
| `finpilot.support` | — | 🔴 вахта A, заблокирована | — |

> **Один логин на трёх доменах — три разных аккаунта, и различить их можно только
> по домену.** `gertab95@gmail.com` ≠ `gertab95@proton.me`: первый платит за подписку,
> второй ведёт переписку с поддержкой Proton. Реестр, хранящий «gertab95», не хранит
> ничего.

### 🟢 Вопрос закрыт следующим же кадром: это вахта S (Sergey)

Снимок 18.08 08:55, `claude.ai` → Settings → Billing, профиль в углу — **Sergey · Pro**.
Тот же экран, где выставлен счёт `gertab95@gmail.com`.

| | |
|---|---|
| карта | **Mastercard ···· 9618** |
| история | Apr 17 · May 17 · Jun 17 · Jul 17 — **US$20, все Paid** |
| 🔴 состояние на 18.08 | *«Your subscription is past due so your access has been paused»* |
| 🔴 | *«Your subscription will be canceled on Aug 18, 2026»* |

**Вахта S потеряла доступ 18.08 из-за непрошедшего платежа**, а не по решению владельца:
инвойс `QUMWYXOI-0005` остался неоплаченным, доступ приостановлен, подписка отменяется
в тот же день. Четыре предыдущих месяца оплачивались без сбоев.

> **Подписка кончается не тогда, когда её отменили, а тогда, когда не прошёл платёж.**
> Уведомление приходит на почту аккаунта — и если она недоступна или не читается,
> первым признаком становится отказ в доступе.

📍 **Это второй потерянный доступ за неделю, и по разным причинам:** S — просроченный
платёж 18.08, A — заблокированный почтовый ящик 23.08. Общего между ними только то,
что оба обнаружились по факту отказа, а не по предупреждению.

**Реестр дополнен:** у вахты S теперь есть почта — `gertab95@gmail.com`.

**Имена были известны с июля** — таблица в `sborka-vse.md` §«Сводка» (анализ 334 чатов)
называет V/S/J/M поимённо и даёт их роли:

| Вахта | Роль по анализу чатов | Чатов | Период |
|---|---|---|---|
| **V** Vasilii | домашняя база — самый широкий и личный | 174 | 07.02 → 06.07 |
| **S** Sergey | систематизатор — инструкции, стандарты, портфолио | 121 | 01.04 → 07.07 |
| **J** Jesus | рабочая смена — чистый код | 24 | 27.06 → 07.07 |
| **M** Michael | рабочая смена — чистый код | 15 | 27.06 → 05.07 |
| 🆕 **A** Adam | заведена 23.08.2026, роли пока нет | — | — |

🔴 **И всё же буква была выведена из почты, а не взята отсюда.** Реестр `84`
и анализ чатов жили порознь: первый знал org ID, второй — имена, и ни один
не ссылался на другой.

> **Два документа об одном предмете, не связанные ссылкой, — это один документ
> с дырой и один с ответом, который в дыру не попадёт.**

> **Почта и имя профиля расходятся, и опознаётся вахта по имени.** У Adam почта
> `finpilot.support@`, у Jesus — `vevdokimovm3@`: вывести букву из адреса нельзя
> ни в одном из двух случаев. Имя видно в интерфейсе браузера («Jesus · Pro»),
> почта — в выводе `/status`. Реестр обязан держать оба.

**Даты продления — внешние сроки.** Пропущенное продление обрывает вахту посреди
работы. По S и M дат нет — дополнить. Дублировать в календарь: файл не напомнит.

🔴 **Продление A приходится на 23.09** — через месяц после заведения. Это первая
подписка, у которой известна точная минута старта (`subscriptionCreatedAt`
`2026-08-23T05:11:11Z`), а значит и точный момент списания.

### Почты вахт восстановлены не из памяти, а из снимков экрана

Почты V и J подтверждены разбором скриншотов 23.08.2026: вывод `/status` в терминале
и профиль «Jesus · Pro» в браузере (`07-media-to-text-lab/runs/2026-08-23-screenshots.md`).
До этого реестр хранил **только org ID** — идентификатор, который нельзя ввести
в форму входа.

> **Реестр аккаунтов без почт отвечает «сколько их», но не «под каким я сейчас».**
> Второй вопрос и есть рабочий: именно он стоил разбора 404 на своём же артефакте.

### 🔴 Лимит у `claude.ai` и Claude Code — один на аккаунт, и это печатает сам интерфейс

Найдено 25.08.2026 разбором скриншотов **12.08.2026** (`07-media-to-text-lab/runs/2026-08-23-screenshots.md`).
Четыре кадра одного дня, три разных окна и три разные вахты — и везде одна строка над полем
ввода: **`Usage limit reached • Resets HH:MM • limits shared with Claude Code`**.

| Время | Где | Вахта | Модель | Что написано |
|---|---|---|---|---|
| 10:23 | Safari, `claude.ai`, проект FINPILOT | **M** (Michael · Pro) | Opus 5 **Max** | `Resets 3:00 PM · limits shared with Claude Code` |
| 11:15 | DuckDuckGo, `claude.ai`, проект portrait-of-taste | **V** (Vasili · Pro) | Opus 4.7 **Extra** | `Resets 3:00 PM · limits shared with Claude Code` |
| 11:23 | Claude Code, `~/Documents/finpilot` | — (терминал) | Sonnet 5 | `You've used 93% of your session limit · resets 3pm (Europe/Moscow)` |
| 14:52 | Firefox, `claude.ai`, проект Dota 2 | **J** (Jesus · Pro) | — | `Resets 7:40 PM · limits shared with Claude Code` |

**Что из этого следует прямо:**

1. **Веб и терминал не дают двойного бюджета.** Пул один на аккаунт; уйти из упёршегося
   Claude Code в браузер под той же вахтой — не обход лимита, а тот же счётчик с другой стороны.
   Обходится он только **сменой вахты** (кадр 14:52: J работает, пока пул 3 PM исчерпан).
2. **Окно сброса привязано к аккаунту, а не к часу.** У J сброс в **19:40** — некруглое время,
   то есть окно отсчитывается от первого запроса вахты, а не от общесистемной границы.
3. **Совпадение 3:00 PM у M и V — наблюдение, а не доказательство общего пула.** Две вахты
   независимо могли начать окна в один и тот же час. Кадр 11:23 (Claude Code, `resets 3pm
   Europe/Moscow`) сцеплен с одной из них, но **с какой именно — по кадрам не видно**:
   терминал вахты не показывает. Записано как есть.

> **Строка «limits shared with Claude Code» — свидетельство интерфейса, а не вывод.**
> Всё остальное в таблице — тоже показания экрана; там, где начинается интерпретация
> (совпадение 3 PM), это названо словом «наблюдение».

**Побочно с тех же кадров — какая вахта на какой модели сидела 12.08:** M — Opus 5 Max,
V — Opus 4.7 Extra, терминал — Sonnet 5 в авто-режиме. Выбор модели под задачу —
`66-model-and-effort-selection.md`.

---

## Что достаётся с машины автоматически

Снято 21.08.2026 командой `claude auth status` и из `~/.claude.json` → `oauthAccount`.
**Только по залогиненному аккаунту** — остальные четыре так не проверяются.

🔴 **Обновлено 23.08.2026:** `claude auth status` в текущей версии CLI отсутствует —
данные снимаются `/status` (интерактивная, набирает **владелец**) или из
`~/.claude.json` → `oauthAccount` командой:

```bash
python3 -c "import json,pathlib;d=json.loads((pathlib.Path.home()/'.claude.json').read_text());print(d['oauthAccount'])"
```

| Поле | Значение (вахта V) | Откуда |
|---|---|---|
| `orgId` | `7cc76e8d-50ee-4d9e-a37d-e66972d7a846` | `claude auth status` |
| `email` | `vevdokimovm@gmail.com` | `claude auth status` |
| `authMethod` | `claude.ai` | `claude auth status` |
| `subscriptionType` | `pro` | `claude auth status` |
| `organizationType` | `claude_pro` | `.claude.json` |
| `organizationRole` | **`admin`** | `.claude.json` |
| `organizationRateLimitTier` | `default_claude_ai` | `.claude.json` |
| `accountCreatedAt` | `2026-01-23T17:35:45Z` | `.claude.json` |
| `subscriptionCreatedAt` | `2026-03-10T18:59:06Z` | `.claude.json` |
| `billingType` | `stripe_subscription` | `.claude.json` |
| `hasExtraUsageEnabled` | `false` | `.claude.json` |

Проверить сейчас:

```bash
claude auth status
python3 -c "import json,os;print(json.dumps(json.load(open(os.path.expanduser('~/.claude.json')))['oauthAccount'],ensure_ascii=False,indent=1))"
```

### 🔴 Даты продления среди этих полей НЕТ

`subscriptionCreatedAt` — дата **создания** подписки, и она её не заменяет:
подписка создана **10.03 в 18:59**, а в таблице выше стоит **14.09 в 20:10** —
не сходится ни числом, ни временем. Значит дата взята из письма Stripe или из
`claude.ai/settings/billing`, и **выводить продление из даты создания нельзя**.
Единственный источник — биллинг, CLI его не знает.

🟢 **Источник найден и подтверждён 25.08.2026** — разбором скриншота 14.08.2026, 20:02:12
(`07-media-to-text-lab`, свалка скриншотов). Кадр — открытая вкладка `invoice.stripe.com`,
«Pay Anthropic, PBC Invoice»: **`US$20.00`, To: `vevdokimovm@gmail.com's Organization`,
From: `Anthropic, PBC (@anthropic)`, Invoice No. `MZ4OITO2-0021`**. Это счёт вахты **V**
за августовский цикл. Дата и время кадра (**14.08, 20:02**) почти совпадают с записанным
«Продление 14.09, 20:10» — расхождение в 8 минут между реальным списанием и записанной
датой **следующего** цикла согласуется с ежемесячным продлением (14 число каждого месяца,
время дрейфует на минуты между циклами у Stripe). **Догадка «дата взята из письма Stripe
или биллинга» подтверждена предметно**, а не осталась предположением.

### Что подтвердилось само

- **Org ID вахты V сверен машинно** — совпал с записанным. Раньше это было
  переписанное вручную число, теперь проверенное.
- **`hasExtraUsageEnabled: false`** согласуется с `overageStatus: rejected`
  из логов лимитов: при упоре работа **встаёт**, а не идёт платно.
  Два независимых источника сошлись.

---

## 🔴 Объединить пять аккаунтов на Pro НЕЛЬЗЯ — и вот граница

Ресёрч 21.08.2026 по документации Anthropic. Вопрос владельца: *«для чего он вообще
тогда? как-то можно объединить в общую систему?»*

### Что говорит документация дословно

> **The Admin API is unavailable for individual accounts.**
> To collaborate with teammates and add members, set up your organization in
> **Console → Settings → Organization**.

`organizationType` = `claude_pro` — это **личная** организация Pro-подписки.
`organizationRole: admin` в ней означает «админ самого себя» и **Admin API не открывает**.

### Что закрыто вместе с Admin API

| API | Что даёт | Доступно на Pro |
|---|---|---|
| **Admin API** | участники организации, роли, приглашения, рабочие пространства | ❌ |
| **Claude Code Analytics** | **сессии, токены, стоимость, принятия/отказы инструментов — по каждому участнику** | ❌ |
| **Usage & Cost** | расход и деньги по всей организации | ❌ |
| **Rate Limits API** | сконфигурированные лимиты организации | ❌ |
| **Compliance API** | аудит и лента активности | ❌ |

### Чем org ID полезен НА PRO — честно, три вещи

1. **Опознание вахты.** `claude auth status` отдаёт `orgId` — по нему видно,
   какой аккаунт ведёт сессию, без вопросов человеку. Единственное применение,
   которое работает сегодня.
2. **Сверка записи с фактом.** Записанный ID можно подтвердить командой, а не памятью.
3. **Ключ на будущее.** Если появится Team — эти ID и станут точками, по которым
   организации сводятся.

**Всё остальное — нет.** Ни общего контекста, ни передачи сессии, ни чужих лимитов:
`claude agents --json` показывает сессии **только текущего пользователя ОС**
(`base-repo/00-infrastructure/50` §4).

---

## Что откроет Team — если решишь переходить

Это **единственный** способ получить то, что владелец описывал как «объединить
в общую систему». Не «может быть», а прямо документированный путь:
Console → Settings → Organization.

**Claude Code Analytics API** после перехода даёт **по каждому участнику за день**:

- `num_sessions` — число сессий
- `lines_of_code.added / removed`
- `commits_by_claude_code`, `pull_requests_by_claude_code`
- принятия и отказы по каждому инструменту (`edit`, `multi_edit`, `write`, `notebook_edit`)
- **разбивку по моделям:** `tokens.input / output / cache_read / cache_creation`
  и `estimated_cost.amount` **в центах**
- `terminal_type` — где именно работали (`iTerm.app`, `vscode`, `tmux`)

Данные с задержкой до часа, хранятся без срока, **API бесплатен** для всех,
у кого есть доступ к Admin API.

### Почему это стоит рассмотреть отдельно от цены

Половина того, что делает `claude-usage` (`base-repo`, свой инструмент), на Team
приходит готовым и **по всем четырём вахтам сразу**, а не по одной машине.
Своё останется ценным там, где официального нет: **рост контекста по ходам,
цена хода по бакетам, TTL записи в кэш, машинное время сброса лимита**.

**Чего Team не отменяет:** передачу живой сессии между аккаунтами.
Сессия принадлежит аккаунту CLI (`base-repo/06-autonomous-mode-kit/SWITCHES.md` §4),
и это ограничение продукта, а не плана.

**Решение не принято.** Записано, чтобы вопрос «а можно ли объединить» больше
не переоткрывался: **на Pro — нет, на Team — да, вот ровно что откроется.**

---

## Связки

`50-parallel-accounts-and-admin-orchestration.md` §2а (граница Admin API, правило) ·
`06-autonomous-mode-kit/SWITCHES.md` §4 (сессия принадлежит аккаунту CLI) ·
`reports/experiments/token-consumption/` (свои замеры расхода) ·
`59-public-mirror-filter.md` (что не уезжает в публичное) ·
`mission-control/scripts/sync-base.sh` (предохранитель раздачи по `isPrivate`).
