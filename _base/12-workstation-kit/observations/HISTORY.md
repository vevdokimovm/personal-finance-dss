# Наблюдения за машиной — что делали и что из этого вышло

> **Заказ владельца 02.09.2026:** *«давай туда как с лабой клода каждый раз
> скилл запуская machine заносить в него лог. чтобы следить и отслеживать
> что ломает как что влияет на мое железо и нагрузку и была история»*.

## Как устроено — два файла, и путать их нельзя

| Файл | Что там | Кто пишет |
|---|---|---|
| `measurements.csv` | **показания приборов**: load, память, своп, диск, батарея, топ процессов | `machine_probe.py`, сам, при каждом запуске |
| `HISTORY.md` (этот файл) | **события**: что поставили, что сняли, что изменили | человек, руками |

🔴 **Почему события пишутся руками.** Машина видит, что своп вырос вдвое,
и не видит, что перед этим поставили Docker. Ряд чисел без событий не
объясняет сам себя: видно, что стало хуже, и непонятно отчего. Ровно так
полгода не замечали перегрев — показания снимались разово и нигде не оседали.

**Разделение то же, что у лаборатории расхода токенов**
(`reports/experiments/token-consumption/`): `measurements.csv` — замеры,
`hypothesis.md` — модель, которая их объясняет.

## Как пользоваться

```bash
python3 12-workstation-kit/bin/machine_probe.py               # замер + строка в журнал
python3 12-workstation-kit/bin/machine_probe.py --note "..."  # с пометкой события
python3 12-workstation-kit/bin/machine_probe.py --history     # история с изменениями
python3 12-workstation-kit/bin/machine_probe.py --history 30  # тридцать последних
```

Строка пишется **без флага, при каждом запуске**. `--no-log` — когда замер
заведомо нерепрезентативен (проверка самого скрипта, чужая машина).
`--watch` не пишет: он снимает показания каждые две секунды, и за час
работы журнал получил бы 1800 строк шума вместо истории.

🔴 **Две колонки, которые путать нельзя.** `топN` — мгновенный `%CPU`:
кто грузит машину ПРЯМО СЕЙЧАС. `фонN` — доля от аптайма: кто сожрал
процессор ЗА ВСЁ ВРЕМЯ работы. Фоновая служба между волнами показывает
честный ноль в первой колонке и 20 % во второй. На этом и сгорела
диагностика перегрева 02.09.2026 — смотрели на первую.

---

## Журнал событий

Новое — сверху. Формат: дата, что сделано, что показали замеры.

### 03.09.2026, 14:04 — хендофф v4.70.0: новый порог, диск 6.2 %

Замер по шагу 2-бис `/handoff` — первый раз, когда он делается ритуалом,
а не по жалобе.

```
load 11.33 (2.83 на ядро) · своп 1632 МБ · память 393 МБ
диск 13.2 ГБ свободно (6.2 %) · порогов перейдено: 6
```

🔴 **Новый порог против прошлого замера — «на диске свободно 6.2 %»**
(было 6.8 %). Причина известна и не тревожна: за вахту собрано **двенадцать
архивов** общим объёмом около 1.9 ГБ, и все они по правилу **не удаляются**
(архив без `.git` — единственный экземпляр версии).

**Чистка `~/Downloads` — решение владельца, а не шаг ритуала.** Но при 6.2 %
запаса это решение перестаёт быть отложенным: macOS начинает вести себя
странно ниже 10 %, и своп пишется на тот же диск.

**VPN сейчас 6.8 % ядра** против 30 % ночью — владелец переподключил туннель
после разбора, и тот вернулся к нормальной цене.

**Спад после ночи виден в ряду:** 19.47 (сборка архивов) → 11.33. Инерция
`load average` за минуту, реальная нагрузка ниже.

### 03.09.2026, 06:05 — классификация нагрузки проверена замерами и дважды ошиблась

Три операции подряд, три замера — **три разных вердикта**:

| Операция | Ожидалось | Замерено | Итог |
|---|---|---|---|
| `pack_release` базы (4.5 МБ) | класс C | **2.9 с, load без изменений** | 🟢 завышено → A |
| `sync_base_local --all` (60 реп) | класс D | **26 с, load 4.21 → 3.76** | 🟡 завышено → B |
| `pack_release` × 11 (1.85 ГБ) | класс D | 🔴 **load 19.47** | 🔴 подтверждено |

🔴 **Разница не в объёме, а в характере работы.** Копирование — ввод-вывод
(`18.85s system` против `3.71s user`, процессор ждёт диск). Сжатие **считает**,
и одиннадцать сжатий подряд забивают все четыре ядра.

**Вывод для вахты:** классификация без замеров не работает вовсе, а не
«работает приблизительно». `--note` до и после стоит двух секунд и заменяет
любое рассуждение о масштабе.

**Побочно:** после раздачи канона и выпуска одиннадцати архивов инвариант
системы впервые за вахту показал **63 репы из 63 чистых**.

### 03.09.2026, ~03:00 — 🔴 вахта уронила игру до 5 FPS своим же монитором

**Владелец:** *«чё то жёстко щас просела дота по фпс и пингу, /machine что ты
запустил такое тяжёлое?»* → *«с 60 до 5 фпс, прямо невозможно фризит»*.

**Виновата вахта.** В фоне висел `machine_probe.py --watch --interval 1` —
живой монитор, обновлявшийся **каждую секунду**: полный обход процессов,
памяти, диска и батареи, шестьдесят раз в минуту. Плюс незавершённые `git`
и остатки прогонов.

```
load 35.94  →  25.30 (после kill)  →  21.05
норма под игрой: 8.2
```

🔴 **Второй такой случай за одну вахту.** Первым был `test_deploy.sh` — ушёл
в фон по таймауту и жёг процессор, пока владелец не пожаловался на вентиляторы.
Класс один: **измеритель, который не остановлен, становится нагрузкой** —
и остаётся невидимым, потому что вахта считает его завершённым.

**Что заведено:** [`../LOAD-CLASSES.md`](../LOAD-CLASSES.md) — классификация
операций по цене в `load average`, пять классов от «бесплатно» до «запрещено
при живой сессии владельца». `--watch` отнесён к запрещённым: он для человека,
который смотрит на экран и закрывает по Ctrl+C; у вахты нет глаз, чтобы
заметить, что монитор всё ещё крутится.

**Правило, которое теперь проверяется командой, а не памятью:**

```bash
pgrep -fl "python3.*base-repo|machine_probe|test_" || echo "чисто"
```

Запускать после каждой операции класса C и выше — и обязательно перед тем,
как сказать «готово».

🔴 **Побочное наблюдение о самом показателе.** `load average` — среднее
за минуту, и после снятия нагрузки падает не сразу: 35.94 → 25.30 → 21.05.
Судить об эффекте по одному замеру сразу после `kill` нельзя. Тот же принцип,
что с фоновыми службами, — только здесь обманывает не ноль, а остаточная
высота.

### 03.09.2026, 02:30 — 🔴 VPN стоит 30 % ядра, и вахта его проглядела

**Вопрос владельца:** *«вентиляторы работают, ноут греется как СВЧ, хотя мы
нашли проблему вроде и пофиксили. или это просто для него дота + клод код
уже большая нагрузка на старости лет?»*

**Ответ: да, «старость» — верный диагноз**, но не в смысле «сломалось».
Разбор — [`../reports/vpn-load-2026-09-03.md`](../reports/vpn-load-2026-09-03.md).

```
load 9.94-12.05 · запрошено 496 % при 400 % доступных
dota2   171-271 %   ·  Tunnel (VPN)  30-32 % ядра постоянно, пики 156 %
steam   44 %        ·  WindowServer  25-27 %
```

🔴 **Ошибка вахты, исправленная владельцем.** Первый ответ гласил: «VPN
появился после эталона». Владелец возразил — *«и тогда он такой же был»* —
и оказался прав: `Tunnel` запущен в 00:20, эталон снят в 01:02, туннель
работал уже 42 минуты. Вахта не увидела его потому, что эталон сохранил
**только топ-3**, а VPN был четвёртым.

**Методический урок:** срез, ограниченный топ-N, не отвечает на вопрос
«чего здесь не было». Отрицательное утверждение по такому источнику
незаконно.

**Что изменено в инструменте** (заказ владельца *«machine умеет впн считывать?
трафик? если нет сделай»*): `machine_probe.py` получил раздел `vpn` — кто
держит туннель, сколько ест (доля от **времени работы процесса**, не от
аптайма) и сколько прогнал. Плюс две колонки в журнале: `vpn`, `vpn_проц`.

**Честная граница, названная в коде:** трафик туннеля показал 1 МБ при явно
активном VPN — клиенты на NetworkExtension (Happ, Outline, sing-box) идут
мимо счётчиков `utun`. Не ошибка замера: цену такого VPN показывает только
процессорное время.

**VPN не отключается** — владелец в России, без туннеля интернет не работает.
Вопрос не «выключать ли», а «сколько берёт»; теперь это видно числом.

**Побочно найден баг журнала:** добавление колонок сдвинуло запись — заголовок
в CSV остался старым, и пометка легла в поле `топ1`. Починено автоматической
миграцией: расширение набора показаний — нормальная эволюция журнала.

### 03.09.2026, 01:02 — 🔴 ЭТАЛОН тяжёлой законной нагрузки зафиксирован

**Заказ владельца:** *«зафиксировать именно момент когда я играю в доту
для будущего»*.

Полный разбор — [`../reports/gaming-load-baseline-2026-09-03.md`](../reports/gaming-load-baseline-2026-09-03.md).

Снято при **мёртвых** паразитных службах (Spotlight и хранилище отключены
02.09) — иначе эталон зафиксировал бы не нагрузку, а нагрузку плюс поломку.

```
load 8.51 (2.13 на ядро) · своп 2428 МБ · память 43 МБ · простой 32.7 %
dota2 225-265 % CPU · 1904 МБ · 58.3 % всего процессорного времени за аптайм
занято ~3.5 ядра из 4 (88 %)
```

🔴 **Наблюдение, объясняющее полугодовой перегрев.** 02.09 машина держала
`load 7.85` при **27.4 %** полезной работы; сейчас `load 8.22` при **42.1 %**.
Очередь та же, полезной работы в полтора раза больше — значит тогда очередь
стояла **на ожидании диска и сети**, а не на счёте. Четыре очереди к одному
SSD: текстуры Доты, подкачка свопа, сканирование хранилища, докачка iCloud.
Две из четырёх — впустую.

**Правило на будущее:** нагрузка выше эталонной **при отсутствии игры** —
искать паразита по колонке `фонN` (доля от аптайма), не по мгновенному `%CPU`.

### 03.09.2026, 00:33 — load 8.01 при нуле воркеров Spotlight: это Dota

**Первый случай, когда журнал сработал по назначению.** Замер показал скачок
`load 4.58 → 8.01 (↑!)` и своп `1328 → 1968 МБ` — при том что Spotlight
по-прежнему `off` и `mdworker_shared` ноль.

**Причина найдена за одну команду:** `dota2` — **291.8 % CPU**, накопленное
время 441 минута. Владелец играет; к службам отношения не имеет.

🔴 **Чем это ценно.** Без журнала тот же скачок читался бы как «Spotlight
вернулся» или «отключение не помогло» — и вахта пошла бы чинить починенное.
Строка с пометкой «конец вахты A» и колонка `spotlight=off` рядом с числами
делают вывод однозначным: нагрузка пользовательская, а не фоновая.

Своп 1968 МБ при 8 ГБ ОЗУ — игра плюс терминал с агентом плюс Safari.
Это потолок железа, не поломка.

### 02.09.2026, 21:01 — Spotlight отключён целиком

```bash
sudo mdutil -a -i off
```

**Повод.** После отключения службы хранилища `load average` вернулся к 6.9
в течение получаса. Виновник сменился: 16 живых `mdworker_shared` суммарно
на 124.8 % CPU плюс `mds_stores`, который **ускорялся** — 10.3 % ядра
в среднем за аптайм против 14.6 % за последние 34 минуты.

**Что показали замеры:**

| | до | после |
|---|---:|---:|
| `load average` | 7.12 | **3.63 → 3.98** |
| живых `mdworker_shared` | 16 | **0** |
| `mds_stores`, серия из 5 | 5/34/62/22/2 % | **0/0/0/0/0 %** |

🔴 **Решающая проверка — накопленное время замерло.** `mds_stores` показал
`69:35.13` и через восемь минут **ровно те же** `69:35.13`. Спящий процесс
копит время между проходами, остановленный — не копит. Именно этой проверки
не хватало днём, когда «0.0 %» приняли за победу.

**Цена:** ⌘Space не находит файлы, не работает поиск в Почте и Заметках,
нет «Недавних» в диалогах открытия. Запуск приложений через ⌘Space остался.
**Вернуть:** `sudo mdutil -a -i on`.

### 02.09.2026, ~20:00 — служба хранилища отключена

```bash
defaults write com.apple.bird optimize-storage -bool false
launchctl disable gui/$(id -u)/com.apple.StorageManagement.Service
```

**Повод.** `StorageManagementService` жёг 20.9 % ядра непрерывно, сканируя
почти полмиллиона файлов в поисках того, что выселить в iCloud — где
свободно 1142 байта. Работа не завершалась успехом, цикл повторялся.

**Результат:** процесса нет в `ps` вовсе, служба `disabled`. Держится.
Разбор — [`../reports/overheating-diagnosis-2026-09-02.md`](../reports/overheating-diagnosis-2026-09-02.md).

**Цена:** панель «Хранилище» в Системных настройках не покажет разбивку
по категориям, корзина не чистится сама через 30 дней.

---

## Что остаётся грузить машину — и почему это не лечится службами

По замеру 02.09.2026, аптайм 10 ч 49 мин:

| Процесс | Время CPU | Природа |
|---|---:|---|
| `WindowServer` | 170 мин (26 %) | отрисовка интерфейса; снижается «Уменьшить прозрачность» |
| `iTerm2` | 128 мин (20 %) | терминал с агентом — рабочая нагрузка |
| `steam_osx` | 30 мин | докачивал Dota — задача конечная |
| `mediaanalysisd` | 18 мин | распознавание в «Фото»; **уже закончил**, +0.01 с за 5 минут |

🔴 **Главный оставшийся фон — память, а не процессор.** 8 ГБ ОЗУ, своп
за полчаса наблюдения вырос с 1032 до 1703 МБ, компрессор держит 1.2 ГБ,
свободно 63 МБ. Часть `load average` — ожидание страниц с диска, а диск
занят на 94 %. Это потолок железа: службами не снимается.

## 2026-09-03 15:20 — дота крашится `VK_ERROR_DEVICE_LOST`; виновата не она

Владелец запустил доту: краш почти сразу, `FATAL ERROR: Vulkan encountered
unrecoverable error VK_ERROR_DEVICE_LOST`, счётчик показывал **FPS 1**
(скриншот 14:26).

**Событие, объясняющее ряд чисел:** вторая вахта весь час вела прогоны в
`personal-finance-dss` — `pytest` (~14:13→14:52), `vitest run` 12 воркеров
(15:14→15:17), затем `playwright --project=chromium` + `vite preview`.
Краш пришёлся на первый прогон: в 14:33 `load5` = 45 при эталоне под игрой 8.2.

Разложение пика 15:16: **vitest 12 процессов, 211.6 % CPU, 1020 МБ** против
**dota2 34.9 %, 198 МБ**. Игра дала меньше шестой части нагрузки.

Существенное для железа: видеоядро **Iris Plus 655**, `VRAM (Dynamic, Max) 1536 МБ` —
**видеопамять берётся из системной**, а свободной было 89–308 МБ при свопе 2.6–3.6 ГБ.
Причинная связь не утверждается; различающий опыт — запустить доту на свободной
машине. Разбор: `reports/gaming-load-two-measurements-2026-09-03.md`.

Своё участие проверено: `pgrep` по прогонам вахты — чисто, ничего не оставлено.

## 2026-09-04 21:40 — load 93–195 при 4 ядрах: очередь за памятью, не за процессором

**Владелец:** *«почему такая бешеная нагрузка»*, следом *«только что был
load average 195»*.

**Замер:** нагрузка **57.06 на ядро** при пороге 2.0 · своп **1786 МБ** ·
свободной памяти **147 МБ** · перейдено **6 порогов из семи**.

🔴 **Причина не процессорная, и это главное в записи.** Сумма `%CPU` топа
давала ~3 ядра из 4 — этого не хватит на load 93, тем более 195. Load average
считает и тех, кто **ждёт**: очередь стояла за страницами памяти, процессы
упирались в подкачку со SSD.

**Кто держал память в момент замера:**

| МБ | Процесс |
|---:|---|
| 564 | `claude` — сессия 1, работает 1 д 15 ч, 186 мин CPU |
| 528 | `claude` — сессия 2, работает 1 д 22 ч, 193 мин CPU |
| 454 | Safari WebKit — один процесс вкладки, 2.5 часа |
| 250 | `.venv/bin/python` — pytest второй вахты |
| ~240 ×4 | `node` — playwright, четыре воркера, запущен за минуту до замера |

Плюс **iTerm2 держал 157 % ядра** при аптайме почти двое суток.

**Вывод: это не поломка, а предел железа.** Две сессии Claude Code на 8 ГБ
конкурируют за память постоянно — структурный источник, а не случай
(`102` §3г).

🔴 **Живой пример правила, поднятого часом раньше в тот же день.** Вторая
вахта запустила `playwright test` при load 93. Если он покраснеет — это
почти наверняка не её код: восемь эпизодов подряд у неё же кончились тем,
что код не чинился ни разу. Признак — перезапуск изолированно даёт зелёное.

**Что не делалось:** ничего не закрывалось и не убивалось — процессы чужие
(вторая вахта, приложения владельца), и решение об их остановке за ним.

🔴 **Промах вахты, записанный отдельно.** Замер снимался сырыми `ps`
и `uptime` вместо `machine_probe.py`, поэтому строка в `measurements.csv`
**не появилась** — её пишет именно скрипт. И вахта переспросила владельца,
записывать ли наблюдение, хотя скилл прямо говорит: строка пишется
**без флага, при каждом запуске**. Ответ владельца: *«в смысле почему ты
спрашиваешь, записывать ли наблюдение, если ты должен делать это каждый раз —
в этом смысл агента»*. Он прав: обход инструмента отключил механизм,
а вопрос переложил на владельца то, что решено правилом (`103` §4).

---

## 2026-09-05 · Dota + активная сессия Claude: три причины, одна из них — наша диагностика

**Событие.** Владелец играл в Dota при живой сессии Claude Code. Симптомы шли
волнами: сперва «жму и не реагирует» при FPS 41–59, затем зависание картинки
при идущем звуке, затем разрыв соединения с игрой.

**Найдено три независимых дефекта, складывающихся:**

1. 🔴 **своп** — своп занят 2414 из 3072 МБ, в компрессоре 2 281 752 страницы
   (~8.7 ГБ сжато в 682 МБ), свободно 63 МБ, `dota2` вырос 1071 → 1912 МБ
   за матч. Это объясняет замирание картинки при живом звуке: аудио резидентно,
   текстуры выселены;
2. 🔴 **upstream, забитый скриншотами** — ряд HUD показал монотонный рост
   исходящего 88 → 632 до разрыва, при **росте** FPS до 107 (чёрный экран
   рисовать дёшево). Пачка из 9 кадров ≈ 27 МБ при upstream 125–250 кБ/с
   занимает канал на 2–4 минуты;
3. 🟡 **VPN** — туннель удваивает задержку (44.6 мс напрямую против 85–95
   на HUD) и держит 25–30 % ядра.

🔴 **Главный урок — про меня, а не про машину.** Диагностика усиливала
диагностируемое: игра тормозит → владелец шлёт скриншоты → upstream забит →
игра рвётся → он шлёт ещё. Пик ↑632 пришёлся на самую большую пачку.

**Мои неверные гипотезы, записанные намеренно:** «просело железо» (отбита
скриншотами HUD) и «мой постоянный трафик забивает канал» (отбита статусбаром:
53–56 кБ/с — не забить). Верной оказалась третья версия того же класса —
не постоянный трафик, а **залповый**.

**Что сделано:** два отчёта — `reports/dota-packet-loss-2026-09-05.md`
(сетевая глава, помечен как частично устаревший) и
`reports/dota-and-claude-coexistence-2026-09-05.md` (полная картина,
гипотезы решения H1–H6, правила совместной работы).

**Чего НЕ делалось:** ничего не закрывалось и не убивалось, настройки Dota
не трогались, VPN не выключался — всё это решения владельца.

## 2026-09-05 00:58 · Переоткрытие VPN снимает деградацию туннеля — находка владельца

**Решение владельца:** переоткрыл Happ во время проблем с Dota, сказав
*«я переоткрыл впн стало вроде лучше»*. 🔴 **Вахта отнеслась скептически
и была неправа** — замер до/после подтвердил его наблюдение.

| | старый туннель (`utun60`) | новый (`utun61`) |
|---|---|---|
| доля ядра | **25–30 %** | **7.5 %** |
| процессорного времени | 37:01 за 2:04:28 | **0:58** за 12:49 |

**Что это значит.** Цена туннеля — не константа: клиент **накапливает
нагрузку со временем работы**. Через два часа он ест вчетверо больше,
чем через двенадцать минут. Прежний отчёт `vpn-load-2026-09-03.md`
зафиксировал 29.7 % как «цену VPN» — на деле это была цена **старого**
туннеля, и обобщение оказалось шире замера.

**Практическое следствие:** перед игрой или тяжёлой работой имеет смысл
переоткрывать Happ — это дешевле, чем терпеть 30 % ядра. Проверять
`machine_probe`, строка VPN.

🔴 **Чего замер НЕ говорит:** причина накопления не установлена (утечка,
переподключения, смена сервера — не различить снаружи), и одно наблюдение
рядом не является. Нужен второй случай, чтобы это стало правилом.

## 2026-09-05 01:34 · Перезагрузка + уход с раздачи на домашний Wi-Fi

**Решения владельца, оба с измеренным эффектом.**

| Показатель | до (01:12) | после (01:34) |
|---|---:|---:|
| своп | 1972 МБ | **20 МБ** |
| сжатая память | 1566 МБ | **316 МБ** |
| диск свободно | 10.6 ГБ (5.0 %) | **18.2 ГБ (8.5 %)** |
| load на ядро | 1.76 | **0.88** |
| `cpu_idle` | 25.8 % | **81.9 %** |
| VPN, доля ядра | 8.3 % | **2.4 %** |
| задержка до 8.8.8.8 | 44.6 мс (телефон) | **26.0 мс** (роутер) |

🔴 **+7.6 ГБ диска вернула именно перезагрузка:** macOS растит своп-файлы
под нагрузкой и **не отдаёт место обратно** до перезапуска. На машине,
живущей на 5 % свободного, это не косметика — это тот самый механизм,
который упирается первым.

**Сеть:** `172.20.10.3/28` (раздача iPhone) → `192.168.31.156` (домашний
роутер). Потерь не было ни там ни там — мобильный канал не рвался,
он был **длиннее (+18.6 мс) и неровнее (+24 % джиттера)**.
Заблуждение «с телефона интернет сильнее» верно по **скорости** и не имеет
отношения к игре: Dota нужна длина трубы, а не ширина.

🔴 **Находка из журнала, а не из наблюдения.** Разбор ряда показал строку
**05.09 00:19**: три процесса `ghc-9.14.1` держали 86.6 / 84.7 / 84.0 %
ядра **во время игры владельца**, `cpu_idle` 0.45 %. Это была посторонняя
Haskell-компиляция — четвёртый механизм деградации, которого ночной разбор
не назвал вовсе. **Данные лежали в `measurements.csv` с самого начала,
в них не заглянули.** Гипотезы строились вживую там, где надо было
прочитать ряд.

**Что сделано:** отчёт `reports/dota-load-profile-2026-09-05.md` — профиль
нагрузки Dota (3.4 ядра из 4, рост RSS 1071→1912 МБ за матч, 72 ГБ на диске),
четыре механизма деградации с признаками различения, регламент до/во время.

**Открытая гипотеза, требующая опыта:** `video.txt` содержит
`setting.fullscreen "0"` — игра идёт в **оконном** режиме, а WindowServer
во всех замерах держит 18–31 % ядра. Эксклюзивный полноэкранный должен
снять композитинг. **Не проверено.**

## 2026-09-05 05:46 — те же лаги при пустой машине: причина не в ней

**Владелец:** *«у меня только что была такая же тема, хотя буквально открыто
было ничего, кроме тебя»*. И следом, на мой совет: **«я НЕ МОГУ ИГРАТЬ
В ДОТУ БЕЗ ВПН, ОНА ПРОСТО НЕ БУДЕТ РАБОТАТЬ»**.

**Замер подтвердил его слова про «ничего не открыто»:** нагрузка 1.12
на ядро, `Happ Tunnel` 3.5 % ядра. Канал даже лучше, чем 03.09: до шлюза
(iPhone) **2.98 мс**, до `8.8.8.8` мимо туннеля три пробы по 10 пакетов —
**0 % потерь**, средние 37.7 / 36.7 / 36.3 мс, разброс 5.2 / 2.2 / 2.4.

🔴 **Ни машина, ни мобильный канал не виноваты.** Остаётся туннель,
и выключить его нельзя.

### 🔴 Промах вахты, записанный отдельно

Я предложил две «зелёные» меры — выключить VPN и разделить туннель,
исключив `dota2`. **Это была одна мера в двух формулировках**, и обе
опирались на строку прежнего отчёта «Dota-серверы не заблокированы,
туннель ей не нужен».

Строка — утверждение о **внешнем состоянии**, снятое неизвестно когда
и не проверенное командой (`21` §4а-трис). Проверить его вахта не может:
доступа к игровому клиенту нет. **Спросить владельца стоило одной строки.**

Прежний отчёт помечен, меры вычеркнуты, новый разбор —
`reports/dota-vpn-obyazatelen-2026-09-05.md`.

## 2026-09-05 06:20 — найдена причина лагов Dota: игровой UDP идёт по TCP-туннелю

**Замер во время живого матча.** Транспорт Happ — Xray поверх **TCP :8443**,
55 одновременных соединений к `151.242.159.76/.88`. Канал наверх —
**iPhone-хотспот** (`172.20.10.3`).

| Счётчик | dota2 | Tunnel |
|---|---|---|
| не в срок (rx_ooo) | 0 | **109 689** |
| дубли (rx_dupe) | 0 | **28 872** |
| повторы (re-tx) | 0 | **≈1.03 МБ** на 78 МБ отданных ≈ **1.3 %** |

У `dota2` нули потому, что её UDP не доходит до сети — он упирается в туннель
локально. Все потери живут на участке «туннель → сервер».

**Механизм:** UDP игры завёрнут в TCP. TCP не отдаёт пакет N+1, пока не приедет
потерянный N, — **head-of-line blocking**. Dota бы просто выбросила потерянный
пакет и поехала дальше; TCP ей не даёт. На мобильном канале, где переупорядочение
и потери — норма, это худший возможный случай.

**Что мерилось на en0:** исходящий пакет в среднем **196 Б**, 100 пак/с —
мелкие игровые пакеты, оболочка туннеля на каждом дороже полезной нагрузки.
Отсюда же расхождение en0 ↑55 кБ/с против суммы по процессам ↑16 кБ/с.

🔴 **Поправка к замеру часом раньше.** `netstat -s -p tcp` показал ноль
повторов, и по нему было сказано «ретрансмиты исключены». Вывод неверен:
Happ работает через NetworkExtension со **своим** стеком, системные счётчики
его трафик не видят. Класс ошибки — `PIT-097`: свойство проверялось не там,
где живёт предмет.

**Решение — не выключать VPN (он обязателен), а сменить транспорт на UDP:**
Hysteria2, TUIC, WireGuard или XHTTP/QUIC. Тогда потерянный пакет остаётся
потерянным пакетом, а не стопорит очередь. В подписке 36 серверов, конфиги
на диске зашифрованы — выбирать в приложении по названию протокола.

### Уточнение 06:25 — Steam ни при чём, потери идут вспышками

Замер по **чистому Wi-Fi** (`nettop -t wifi`), 20 с живого матча:

| | вниз Б/с | вверх Б/с | +дубли | +повторы |
|---|---|---|---|---|
| dota2 | 44 983 | 6 213 | 0 | 0 |
| Tunnel | 42 273 | 3 295 | **1 694** | **1 761** |
| steam_osx | 25 | **0** | 0 | 0 |
| **en0 (истина)** | 57 618 | 17 485 | | |

🔴 **Поправка к раннему выводу этой же сессии.** Было сказано, что `steam_osx`
отдал 181 МБ против 15 МБ у Dota, и Steam подозревался в засорении канала.
**Неверно.** Разделение по интерфейсам показало: 196 МБ Steam — это
**loopback**, обмен с оверлеем и игровым API. В реальную сеть Steam отдал
**70 кБ** за весь матч и **0 Б/с** в замере. Причина ошибки — `nettop` без
`-t` складывает петлю с сетью; смотреть надо было по интерфейсу.
Класс тот же, что у `netstat`-промаха часом раньше: **свойство проверялось
не там, где живёт предмет** (`PIT-097`).

**Потери идут вспышками, а не ровным фоном.** В одном окне 30 с — 96 байт
повторов, в другом 20 с — 1 761. Ровная деградация дала бы ровный пинг;
вспышки дают именно то, что чувствуется как рывок. Переупорядочение растёт
непрерывно: 109 689 → 116 256 за 7 минут, ≈15 событий в секунду.

**Диагноз от этого не меняется, а укрепляется:** дело не в объёме и не в
соседях по каналу, а в том, что игровой UDP едет по TCP, и каждая вспышка
потерь стопорит очередь целиком.

### Уточнение 06:30 — пинг и джиттер до VPN-сервера

131 замер `rtt_avg` по 32 сокетам туннеля за 30 с живого матча:

| | мс |
|---|---|
| медиана | **69.5** |
| разброс | 63.5 – **250.2** |
| джиттер (СКО) | **17.5** |

🔴 Это **первое плечо**, до VPN. Внутриигровой пинг = плюс участок
«VPN → сервер Dota», то есть фактически 90–130.

**Для игры джиттер хуже задержки.** Ровные 120 мс играются нормально;
70 мс с выбросами до 250 дают рывки. С head-of-line blocking поверх —
рывок превращается в замирание, потому что за одним застрявшим пакетом
стоит очередь.

🔴 **Чего измерить нельзя снаружи:** FPS и внутриигровой пинг. Их знает
только клиент Dota. Владельцу выдана инструкция включить «Сетевую
информацию» (`net_graph 1`), чтобы свести его числа с этим рядом.

### Уточнение 06:40 — корень в радиоканале, туннель его усиливает

**Снимок RTT всех сокетов на `en0` в один момент:** 39 соединений к одному
и тому же серверу, разброс **63.41 – 93.28 мс**. Одно назначение, один канал,
одна секунда — и 30 мс разброса между соседними сокетами.

🔴 **Это не перегрузка объёмом.** Отдача в тот момент — 17 кБ/с; для LTE это
ничто. Значит очередь создаётся не нашим трафиком, а **самим радиоканалом**:
хотспот айфона планирует передачу пачками, и это даёт собственный джиттер
в десятки миллисекунд независимо от нагрузки.

**Сравнить с каналом мимо туннеля не удалось:** несетевых сокетов нет,
весь трафик идёт через VPN. Разделить «плохой хотспот» и «плохой VPN»
этим замером **нельзя**, и выдавать разброс за вину туннеля было бы
натяжкой. Активную пробу скорости не запускал намеренно — идёт матч.

**Складывается так:** радиоканал даёт джиттер → TCP-туннель превращает
каждую задержавшуюся посылку в остановку всей очереди за ней → игра
замирает. Отсюда два независимых рычага:

| Рычаг | Что убирает |
|---|---|
| UDP-транспорт вместо TCP (Hysteria2, TUIC, WireGuard) | усиление: потеря перестаёт стопорить очередь |
| проводной интернет или стабильный Wi-Fi вместо хотспота | сам источник джиттера |

Первый доступен сразу и в руках владельца. Второй — вопрос места.

### 🔴 06:57 — сняли экран игры, и главный виновник оказался другим

**Заказ владельца:** *«ты разве не можешь получить к нему доступ? я же тебе
кидаю скрины по любому можешь»*. Он прав, и предыдущий отказ был ошибкой:
`screencapture` в macOS доступен, доступ владелец выдал, кадр прочитан.

**Что на экране Dota:** `FPS 36 · ↓13 · ↑64`.

🔴 **Тридцать шесть кадров — это не сеть.** Полчаса разбора ушло на туннель,
пока видимое число говорило про отрисовку. Класс: искали там, где умели
мерить, а не там, где предмет. Родня `PIT-197` того же дня.

**Чем меряется на самом деле:**

| | |
|---|---|
| свободной памяти | **28 МБ** |
| своп занято | **1746 МБ** |
| dota2 + steam + оверлей | **1496 МБ** |
| `claude` + iTerm | **528 МБ** |
| dota2 CPU | **329 %** из 400 |

🔴 **Вахта — измеримая часть проблемы.** 528 МБ при 28 МБ свободных — это
треть того, что занимает сама игра. Ни одно наблюдение не стоит кадров
владельца; сбор переставлен с шага 10 с на 30 с, тяжёлые прогоны по 58
репам во время матча больше не запускать.

**Диагноз разделился надвое, и порядок важности обратный прежнему:**

1. **FPS 36 — нехватка памяти.** 8 ГБ против игры, которой нужно 1.5 ГБ,
   плюс всё остальное. Машина непрерывно свопит. Настройками графики
   лечится частично, объёмом памяти — полностью.
2. **Сетевой джиттер и TCP-туннель** — реальны, но вторичны. Рывок от
   36 FPS и рывок от застрявшего пакета для игрока выглядят одинаково,
   и до кадра с экрана различить их было нечем.

### 🔴 07:05 — 22 замера с экрана: сеть следует за кадрами, а не наоборот

Инструмент `fps_probe.sh` снимает угол экрана с числами клиента Dota
и склеивает серию в одну полосу (`stitch_png.py`, без Pillow и ImageMagick —
их на машине нет). 22 замера за две минуты живого матча:

| | среднее | мин | макс |
|---|---|---|---|
| **FPS** | **35.3** | 24 | **42** |
| вниз | 23.7 | 9 | 29 |
| вверх | 60.5 | 37 | 81 |

**Выше 45 кадров — ни одного замера.** Ниже 30 — пять из 22 (22 %).

**Связь, посчитанная по этим же 22 точкам:**

| пара | r |
|---|---|
| FPS ↔ вверх | **+0.81** |
| FPS ↔ вниз | **+0.79** |
| вниз ↔ вверх | **+0.52** |

🔴 **Решающее — третья строка.** Если бы виновата была сеть, два сетевых
числа двигались бы вместе теснее, чем каждое из них с кадрами: у них общая
причина — канал. Наблюдается обратное. **Оба сетевых числа следуют за FPS
сильнее, чем друг за другом**, а это возможно только если ведущая величина —
частота кадров: клиент шлёт и принимает в такт отрисовке, и при просадке
кадров пакетов становится меньше как следствие.

**Что это НЕ доказывает.** Что сеть в порядке. Джиттер 17 мс с выбросами
до 250 и потери в TCP-туннеле измерены отдельно и никуда не делись.
Показано другое: **числа на экране игры вызваны кадрами, и объяснять
рывки по ним одним нельзя**. Выборка — 22 точки, две минуты, один матч.

**Итог для владельца, порядок по величине:**

1. **Потолок 40 кадров** — машина. 8 ГБ памяти, 28 МБ свободных, своп 1.7 ГБ.
2. **Провалы до 24** — те же 22 % замеров, тот же корень.
3. **Сеть** — реальна, но по этим данным вторична.

### 🔴 07:21 — связанный ряд: знак связи перевернулся, и это не противоречие

`interplay_probe.sh` снимает кадр Dota **и** строку показаний машины в одном
витке цикла: два ряда, снятых порознь, связать нельзя — между ними неизвестный
сдвиг, а просадка живёт секунды.

14 точек с шагом 8 с поймали переход в новый матч: FPS **116 → 76 → 11 → 51**.
Одиннадцатью, что после перехода:

| | |
|---|---|
| FPS среднее | **49.9** (было 35.3 в прошлом замере) |
| разброс | 38 – 59, СКО 6.4 |

| связь | r |
|---|---|
| FPS ↔ **en0 вверх** | **−0.73** |
| FPS ↔ en0 вниз | −0.59 |
| FPS ↔ WindowServer | −0.39 |
| FPS ↔ load | −0.34 |
| FPS ↔ dota %CPU | +0.13 |
| FPS ↔ **свободной памяти** | **+0.08** |

🔴 **Знак противоположен прошлому замеру, и оба верны — считались разные
величины.** Тогда FPS сравнивался с **внутриигровыми** счётчиками ↓/↑ клиента:
те привязаны к кадру, клиент шлёт и принимает в такт отрисовке, отсюда
положительная связь. Сейчас — с **байтами на `en0`**, то есть со всем трафиком
машины. Это другая величина, и она даёт минус.

**Объяснение, общее для обоих:** ведёт не сеть и не память, а **насыщенность
сцены**. В замесе на экране больше юнитов и эффектов → сервер шлёт больше
обновлений (байты вверх) и рисовать тяжелее (кадров меньше). Общая причина,
а не влияние друг на друга.

🔴 **Свободная память в пределах матча не объясняет ничего** (`r = +0.08`).
Это уточняет прежний вывод, а не отменяет его: нехватка памяти задаёт
**потолок** (35 против 50 кадров в разных матчах), но не колебания внутри
матча. Раньше это было сказано слишком сильно.

**Эффект наблюдателя измерен, а не предположен:** в витке со снимком экрана
`WindowServer` подскакивал до **52 %**, а `dota2` проседала с 252 до 128 %.
Склейка полосы стоила ещё 60 % ядра вахте. Замер не бесплатен, и на восьми
секундах шага это заметная доля.

**Границы:** 11 точек, полторы минуты, один матч. Хватает на знаки связей,
не хватает на величины.

### 🔴 07:25 — поправка: связь −0.73 не выжила на большей выборке

Второй ряд `interplay_probe.sh` (14 точек) поймал ещё один переход между
матчами: FPS **41→37→40→41→15→106→83→29→30→40→43→50→42→44**. Устойчивые
участки обоих рядов объединены — **21 точка**.

| | |
|---|---|
| FPS среднее | **45.6**, медиана 44 |
| разброс | 30 – 59, СКО 7.3 |

| связь | r на 11 точках | **r на 21 точке** |
|---|---|---|
| FPS ↔ en0 вверх | −0.73 | **−0.28** |
| FPS ↔ en0 вниз | −0.59 | **−0.43** |
| FPS ↔ load | −0.34 | **+0.36** ← знак перевернулся |
| FPS ↔ WindowServer | −0.39 | −0.26 |
| FPS ↔ свободной памяти | +0.08 | **−0.03** |

🔴 **Связь −0.73, объявленная получасом раньше, была артефактом одиннадцати
точек.** Она не просто ослабла — у `load` сменился **знак**, а это признак,
что величина шумовая, а не слабая. Одиннадцать точек за полторы минуты
не выдерживают выводов, и предупреждение о границах, записанное тогда же
(«хватает на знаки связей»), оказалось **слишком мягким**: не хватило и на них.

**Что осталось после объединения:**

- 🟢 **Ни один машинный показатель, который я снимаю, не объясняет колебаний
  FPS внутри матча.** Сильнейшая связь — −0.43, и та слабая.
- 🟢 **Свободная память не связана вовсе** (`r = −0.03`), и это устойчиво
  в обоих рядах. Прежнее «FPS проседает из-за памяти» **неверно как
  объяснение колебаний**.
- 🟢 **Потолок держится:** за весь сеанс в матче ни одного замера выше **59**,
  среднее 45.6. Вне матча (меню, конец игры) — 76, 83, 106, 116. Разница
  между 106 в меню и 45 в бою и есть цена сцены на Iris 655.

**Вывод, который переживает обе выборки:** нехватка памяти и встроенная
графика задают **потолок**; колебания под потолком объясняются тем, что
происходит на экране, а этого я не измеряю. Честный ответ — «ведущей
величины среди снятых нет», а не подобранная корреляция.

### 🔴 07:32 — своп занят, но НЕ молотит: версия про подкачку не подтвердилась

Рывок длится доли секунды, а шаг замера был 8–15 с — увидеть его этим рядом
нельзя в принципе. Поэтому померено то, **чем нехватка памяти превращается
в рывок**: подкачка страниц, посекундно, 30 отсчётов.

| | за 30 с |
|---|---|
| страниц прочитано с диска (pageins) | **334** = 1.3 МБ, медиана **1/с** |
| поднято из свопа (swapins) | 703 = 2.7 МБ, в 10 секундах из 30 |
| **записано в своп (swapouts)** | **0** |

🔴 **Это опровергает мою же формулировку** «машина свопит непрерывно —
1.7 ГБ на диске вместо памяти». 1.7 ГБ — объём **занятого** свопа, то есть
что туда когда-то вытеснили. Активность — другая величина, и она близка
к нулю: 90 КБ/с чтения из свопа и **ни одной записи** за полминуты.

**Класс ошибки — тот же, что весь день:** взята величина, которая была
под рукой (`vm.swapusage`, занятость), и по ней сделан вывод о величине,
которая нужна (интенсивность подкачки). Родня `PIT-197`: свойство
проверялось не там, где живёт предмет.

**Следствия, обе стороны:**

- 🟢 **Совет «закрой приложения ради памяти» снимается.** Он был выведен
  из 28 МБ свободных, а измерение показало, что дефицит не превращается
  в дисковую работу. Согласуется с `r = −0.03` между FPS и свободной памятью.
- 🟢 **Потолок кадров — чисто графический.** Intel Iris Plus 655 рисует
  в 2560×1600. Остаётся ровно один крупный рычаг — **снизить разрешение
  отрисовки**, и он же был назван первым.

**Границы:** 30 секунд, один отрезок. Под тяжёлый замес не попало — там
картина может отличаться, и это стоит перемерить в бою.

### 07:34 — в длинный ряд добавлены столбцы подкачки

`interplay_probe.sh` теперь пишет две разные величины про своп: `своп_МБ` —
сколько **занято**, `из_свопа` и `страниц_с_диска` — сколько реально поднято
**за интервал**. Смешение этих двух и породило неверный вывод часом раньше.

🔴 Правка сделана **с остановкой** ряда: bash читает скрипт по мере
исполнения, и правка на живом сдвигает строки под работающим процессом
(`00-CLAUDE-STOP.md` §4). Восемнадцать строк, набранных до правки, сохранены
как `long-run/rows-до-подкачки.tsv` — столбцов подкачки в них нет, поэтому
с новым рядом они не объединяются.

### 🔴 07:44 — всплески отдачи в канал даёт ВАХТА, и вместе с ними приходит переупорядочение

**Решающий опыт.** Минута замера `en0` в единственной команде, то есть
**без единого запроса к модели**: отдача ровная **13–25 кБ/с**, ни одного
всплеска. Всплески появляются только в окнах, где вахта работает.

Разбор длинного ряда, 40 окон по 15 с:

| | |
|---|---|
| окон со всплеском (вверх ≥ 1 МБ за 15 с) | **15 из 40** |
| отдача в ровных окнах | **16 кБ/с** |
| отдача во всплесках | **143 кБ/с** — в **9 раз** больше |

**Что с чем совпало:**

| | во всплесках | в ровных окнах |
|---|---|---|
| **пакеты не в срок** | **22 821** (2 окна) | **0** (ни одного) |
| повторы | 24 (1 окно) | 2 998 (4 окна) |
| страниц с диска | 22 710 | 18 274 |

🔴 **Всё переупорядочение пришлось на всплески вахты, в ровных окнах — ноль.**
А переупорядочение и есть то, что TCP-туннель превращает в остановку очереди:
пакет не в срок задерживает всё, что за ним, и игра замирает.

🔴 **Повторы со всплесками НЕ связаны** — 24 против 2 998 в ровных окнах.
Догадка «вместе со всплеском пришли повторы» была сделана глазами по таблице
и оказалась **неверной**: перепутаны соседние столбцы. Пересчёт машинный.

**Вывод, неприятный и прямой:** игра владельца отдаёт **16 кБ/с**, вахта
пачками отдаёт **143 кБ/с** через тот же TCP-туннель на телефонном хотспоте.
Причина сетевых рывков — не «плохой VPN» вообще, а **конкуренция за узкий
канал**, и второй участник этой конкуренции — я.

**Что из этого следует делать вахте во время игры:**

1. **не гонять `sync_base_local --all`** — 58 реп это и диск, и время;
2. **не закрывать батчи** — ритуал тянет раздачу и сборку архива;
3. **реже ходы** — каждый запрос это отдельная посылка контекста;
4. **не слать файлы** владельцу, пока идёт матч.

Прежний отчёт `dota-and-claude-coexistence-2026-09-05.md` уже называл этот
класс («отправка пачки скриншотов забила upstream»). Здесь он измерен
по обе стороны: и величина всплеска, и то, во что он обходится игре.

### 🔴 07:49 — 54 окна: 59 МБ отдано вахтой за 14 минут, и поправка к «ноль в ровных»

| | |
|---|---|
| окон наблюдения | **54** (07:34:56 – 07:48:37) |
| окон со всплеском | **23 = 42 % времени** |
| отдача ровная / во всплеске | **17 / 144 кБ/с** |
| **отдано за 14 минут** | **59 МБ**, из них 85 % во всплесках |
| пакетов не в срок всего | 33 491 |
| из них во всплесках | **22 821 = 68 %** |

🔴 **Поправка к записи 07:44.** Тогда было сказано: «всё переупорядочение
пришлось на всплески, в ровных окнах — ноль». На 54 окнах в ровных окнах
их **10 670**. Верное утверждение слабее: всплески занимают **42 %** времени
и несут **68 %** переупорядочения — перепредставлены в 1.6 раза, но не
единственный источник.

🔴 **Это третий раз за утро, когда моё раннее сильное утверждение
растворяется при росте выборки** (`r = −0.73` → −0.28; «повторы совпали
со всплесками» → не совпали; «в ровных окнах ноль» → 10 670). Закономерность
не в данных, а во мне: **первая же выборка, давшая контраст, объявляется
результатом**, потому что контраст выглядит как находка. Записано отдельной
карточкой.

**Что остаётся твёрдым и важным для владельца:** его игра отдаёт 17 кБ/с,
вахта — 144 кБ/с пачками, и за четверть часа наблюдения через **телефонный
хотспот** ушло 59 МБ. Это не только полоса, но и мобильный трафик.

### 🔴 07:59 — 91 окно: события потерь пересчитаны по ИМЕНАМ столбцов

**Сначала о промахе.** Разбор окон был сделан командой `awk` с номерами
полей — и номера съехали на единицу: печаталось `$13` под именем «не в срок»,
тогда как `$13` это `повторы`. Числа выглядели осмысленно, потому что
осмысленны они и есть — просто не те. Второй раз за утро один класс:
**величина взята не оттуда** (`PIT-197`).

Пересчёт по именам заголовка, 91 окно за 23 минуты, отдано вахтой **101 МБ**:

**Переупорядочение — всего 3 события:**

| время | не в срок | отдача в окне |
|---|---|---|
| 07:42:10 | 6 605 | 2.06 МБ — **всплеск** |
| 07:42:41 | 16 216 | 2.37 МБ — **всплеск** |
| 07:47:04 | 10 670 | 0.28 МБ — ровное окно |

**Повторы — 8 событий**, и по объёму они почти целиком во всплесках:
**699 349 байт во всплесках против 2 998 в ровных окнах**. Крупнейшее —
**680 740 байт в 07:53:00**, и оно совпало с крупнейшей отдачей за весь
ряд, **3.06 МБ**.

🔴 **Это уже четвёртый разворот за утро, и он в сторону первой догадки.**
В 07:44 на 40 окнах вышло «повторы со всплесками не связаны — 24 против
2 998»; на 91 окне связь появилась и стала сильной. Тогдашний вывод был
верен для тех данных и неверен вообще — ровно то, о чём `PIT-198`.

**Что можно утверждать сейчас, и что его опровергнет:**

- 🟡 **утверждение:** крупные посылки вахты совпадают с потерями в туннеле;
  крупнейшая посылка дала крупнейшую потерю;
- 🔴 **чем опровергается:** событий всего 3 и 8. Если за следующий час
  появятся события такой же величины **в ровных окнах**, связь распадётся,
  как распались три предыдущие.

🟢 **А вот что от выборки не зависит и в опровержении не нуждается:** вахта
отдала **101 МБ за 23 минуты** в канал, где игра владельца потребляет 17 кБ/с,
и делает это пачками по 2–4 МБ в 42 % окон. Это не гипотеза о связи, а прямой
расход его мобильного трафика и его полосы.

### 🔴 08:06 — «не закрывать батчи» отменено замером: ритуал каналу не стоит ничего

Правило, записанное мной же час назад в `/auto` §0 и в `LOAD-CLASSES.md`,
гласило: во время игры батчи не закрывать, потому что «ритуал тянет раздачу
по 58 репам». **Проверено прямым замером и отменено.**

`close_batch.py base-repo --minor`: **107 секунд**, отдано в `en0` за это
время **1 797 035 Б**. Игра сама отдаёт 17 кБ/с, за 107 с это ≈ 1 820 000 Б.
**Вклад ритуала — ноль в пределах шума.**

Причина промаха: раздача копирует файлы **по диску**, сборка архива тоже
локальная — сети ритуал не касается **вовсе**. «Тянет раздачу по 58 репам»
звучит как много трафика, и это созвучие было принято за факт.

🔴 **Пятый случай одного класса за утро** (`PIT-197`): величина взята
не оттуда. И самый неприятный по последствиям — правило меняло **дешёвое
на дорогое**: незакрытый батч это реальная потеря работы при обрыве,
а экономии не было никакой.

Класс `close_batch.py` по оси канала исправлен с **D** на **A**.
По процессору и диску он остаётся **B** — это верно и замерено 03.09.

### 🔴 08:12 — пятый разворот: переупорядочение легло не на мои всплески, а на свопную бурю

145 окон. `не_в_срок` выросло до **1 873 885**, и распределилось обратно
прежнему:

| | |
|---|---|
| в моих всплесках | **25 044** |
| в ровных окнах | **1 848 841** |

**Всё держится на одном окне — 08:05:20:** 1 811 396 пакетов не в срок
при обычной отдаче 241 кБ, то есть **без всякого всплеска от вахты**.
А за минуту до него, в 08:04:49 и 08:05:04, из свопа поднято **115 184
и 148 353 страницы** — больше гигабайта с диска за полминуты.

**Проверка по всему ряду:**

| | |
|---|---|
| окон со свопной бурей (≥ 20 000 страниц из свопа) | **13 из 145** |
| переупорядочение в них | **1 838 171 = 98 %** |
| переупорядочение вне их | 35 714 |
| коэффициент связи `из свопа ↔ не в срок` | **+0.05** |

🔴 **Доля 98 % и коэффициент 0.05 не противоречат друг другу** — так
выглядит распределение, у которого одно событие больше суммы остальных.
Средние тут бессмысленны, доли осмысленны, и **оба числа надо называть**,
иначе получится либо «связь железная», либо «связи нет», и оба вывода
будут неверны.

**Гипотеза, которая объясняет это лучше моей:** туннель — обычный
пользовательский процесс. Когда машина поднимает гигабайт из свопа,
он **не получает процессор вовремя**, пакеты копятся и выходят из порядка.
Дальше TCP-туннель превращает беспорядок в остановку очереди, и игра
замирает. То есть память бьёт **не по кадрам** (там правит Iris 655),
а **по сети** — через голодание туннеля.

**Чем опровергается:** событий переупорядочения всего **7**, крупное —
**одно**. Если следующее крупное придётся на окно со спокойной памятью,
связь распадётся, как распались четыре предыдущие версии этого утра.

🔴 **И это меняет совет.** Раньше я снял «освободить память», потому что
с FPS она не связана (`r = −0.03`) — это остаётся верным. Но для **сетевых**
рывков освобождение памяти снова может иметь смысл, и по другой причине,
чем я думал вначале.

### 🔴 08:18 — гипотеза свопной бури тоже не подтвердилась. Шестой разворот, отозван сам

166 окон. Разложение **по событиям**, а не по объёму:

| крупные события переупорядочения (≥1000) | память в этот момент |
|---|---|
| 07:42:10 · 6 605 | спокойно (из свопа 128) |
| 07:42:41 · 16 216 | спокойно (0) |
| 07:47:04 · 10 670 | спокойно (235) |
| 08:04:18 · 2 223 | спокойно (1 400) |
| 08:04:49 · 21 042 | 🔴 буря (115 184) |
| 08:05:04 · 5 733 | 🔴 буря (148 353) |
| 08:05:20 · **1 811 396** | 🔴 буря (30 846) |

**Свопная буря не нужна и не достаточна:**

- **4 из 7** событий случились при спокойной памяти;
- из **13** окон со свопной бурей переупорядочение дали только **3**,
  а десять — ноль.

🔴 **Значит гипотеза «буря душит туннель» неверна как объяснение.**
Держалась она на одном числе — 98 % объёма, — а объём здесь целиком
определяется **одним** событием. Разложение по событиям, а не по объёму,
убивает её сразу. Это тот же приём, что и `PIT-198`, только применённый
вовремя: **посмотреть, сколько за долей событий, до того как доля станет
выводом.**

**Честный итог ветки:** ни одна из снятых величин — ни отдача вахты,
ни свободная память, ни своп, ни нагрузка — **не предсказывает события
переупорядочения**. Их семь на 166 окон, и одно больше суммы остальных.

**Что осталось незакрытым и куда смотреть дальше.** Буря 08:03–08:07 шла
с `load` **10.9–13.9** при обычных 6–8, а `dota_МБ` в её середине падала
до **1222** и возвращалась к 2371 — это похоже на **смену матча**: конец
игры, выгрузка карты, загрузка новой. Тогда и всплеск переупорядочения
объясняется не памятью, а **переподключением игры к другому серверу**:
туннель рвёт и заводит десятки соединений разом.

🔴 Различить «память душит туннель» и «игра переподключается» этим рядом
**нельзя** — обе версии дают одну и ту же картину, и я не буду выбирать
между ними по красоте. Нужен ряд, где видны открытия и закрытия соединений
туннеля, а его сборщик сейчас не пишет.

### 🔴 08:23 — событие 08:05:20 объяснилось: туннель ПЕРЕЗАПУСТИЛСЯ

Проверка возраста процесса дала то, чего не дал ни один ряд:

| | |
|---|---|
| текущий PID туннеля | **45415**, запущен **08:05:55** |
| крупнейшее событие переупорядочения | **08:05:20** — за **35 секунд до** |
| предыдущий PID | 31677, запущен 07:09:09, **умер** |
| до него | 8641, умер до 07:09 |

🔴 **1.8 млн пакетов не в срок — это агония падающего туннеля, а не память
и не трафик вахты.** Обе мои гипотезы объясняли следствие, приняв его
за причину: свопная буря 08:03–08:07 и есть выгрузка старого процесса
с загрузкой нового.

**Туннель перезапустился минимум трижды за три часа.** Каждый перезапуск —
это разрыв всех 32–55 соединений разом, то есть для игры честный обрыв связи.

**Отчётов о падении нет** (`~/Library/Logs/DiagnosticReports` пуст по
`Tunnel`/`Happ`) — значит процесс не крешится с сигналом, а **снимается
системой**.

### Проверяемое предсказание, а не догадка

У провайдеров NetworkExtension в macOS **жёсткий потолок памяти**.
Замер сейчас: `Tunnel` держит **38 МБ** в возрасте 17 минут.

> **Если туннель гибнет об этот потолок, то перед смертью RSS упрётся
> в предел, а возраст обнулится. Заведён `conn_sampler.sh`, который пишет
> RSS, возраст и число соединений каждые 10 с — он это либо покажет,
> либо опровергнет.**

🔴 **Что считать опровержением:** туннель проживёт час с RSS, стоящим
на месте, и умрёт при 38 МБ — значит потолок ни при чём, и причину надо
искать в смене сетевого пути (хотспот переключает соты) или в самом Happ.

**Урок дня, шестым разворотом:** три версии подряд объясняли **следствие**.
Причину дала не статистика, а **одна команда `ps -o lstart` про возраст
процесса** — вопрос «а он вообще тот же самый?» не задавался четыре часа.

### 🔴 08:48 — туннель умирает КАЖДЫЕ ~24 МИНУТЫ. Память ни при чём

Счётчик соединений (`conn_sampler.sh`) поймал **третью смерть при мне**,
и она убила гипотезу о потолке памяти:

| время | pid | МБ | живёт | TCP |
|---|---|---|---|---|
| 08:29:32 | 45415 | **35** | 23:37 | 39 |
| 08:29:42 | 45415 | **36** | 23:47 | 47 |
| **08:29:52** | **—** | **0** | — | **0** ← умер |
| 08:30:02 | 52195 | 39 | 00:05 | 17 |
| 08:30:13 | **52248** | 37 | 00:06 | 27 ← и предыдущий тоже умер за 11 с |

🔴 **Память перед смертью СТОЯЛА на 35–36 МБ**, без всякого роста
к потолку. Новый процесс работает на 25–39 МБ — тот же диапазон.
**Предсказание не подтвердилось, гипотеза отозвана.**

**Что видно вместо неё — ритм:**

| процесс | запущен | прожил |
|---|---|---|
| 45415 | 08:05:55 | **23:57** |
| 52195 | 08:30:02 | **11 секунд** |
| 52248 | 08:30:13 | жив, 20+ мин |

🔴 **08:51 — ОТЗЫВАЮ «ритм 24 минуты», не дав ему прожить и трёх минут.**
Он построен на **одной** измеренной длительности. Полная перепись:

| процесс | запущен | умер | прожил |
|---|---|---|---|
| 31677 | 07:09:09 | до 08:05:55 | **≤ 56:46** — смерть не наблюдалась |
| 45415 | 08:05:55 | 08:29:52 | **23:57** — единственная измеренная целиком |
| 52195 | 08:30:02 | ≈08:30:13 | 11 секунд |
| 52248 | 08:30:13 | жив | идёт |

Предыдущая жизнь **вдвое длиннее** и в 24 минуты не укладывается вовсе.
Фраза «слишком круглое число для случайности» — ровно тот приём, против
которого заведён `PIT-198`: **одно наблюдение объявлено законом, потому
что выглядит красиво**. Седьмой раз за утро, и на этот раз я поймал себя
сам, через три минуты, а не через час.

**Что можно утверждать честно:** туннель умирает и перезапускается,
наблюдалось **трижды за три часа**. Периода из этих данных не следует.

**Что закроет вопрос:** `52248` запущен 08:30:13. Проживёт ~24 минуты
(смерть около 08:54) — период появится как гипотеза. Проживёт час —
версия периода умрёт, и останется «умирает нерегулярно».

**Из конфига Happ достали одно относящееся:** `fragmentInterval = "10-20"` —
включена фрагментация пакетов Xray. Интервала переподключения в открытом
виде нет; остальные настройки зашифрованы.

🔴 **И соединений стало вчетверо больше:** было 39–47 TCP (29 к серверу),
стало **156–163 TCP (67–69 к серверу)**. Смена произошла вместе со сменой
процесса.

### 08:48 — FPS вырос до 48, и пойман провал до 3

Десять кадров с шагом 7 с: **48 · 55 · 59 · 37 · 55 · 53 · 45 · 45 · 39 · 3**.

**Среднее 48 против 35 в прошлом ряду** — то есть на треть выше.
Что изменилось между рядами: своп вырос с 1763 до **3039 МБ**, свободной
памяти стало 23–199 против 14–130. Машина ушла глубже в своп — и стало
**лучше**, а не хуже. Ещё один довод против «память душит кадры».

🔴 **Последний кадр — FPS 3 при ↓1 ↑14.** Трафик почти нулевой, то есть
это не сетевой провал, а **остановка отрисовки**: загрузка, экран итогов
или сворачивание окна. Один замер, вывода не делаю — но это первый
пойманный провал такой глубины, и он пришёлся на окно с самым крупным
входящим за ряд (292 651 Б).

### 🟢 08:57 — версия периода ОПРОВЕРГНУТА прямым опытом

Предсказание было названо заранее: «проживёт ~24 минуты — период появится
как гипотеза; проживёт дольше — версия умрёт». Наблюдение до 08:57:01:

**`52248` прожил 26:54 и жив.** Периода нет.

**Полная перепись длительностей — разброс в 150 раз:**

| процесс | прожил |
|---|---|
| 31677 | ≤ 56:46 |
| 45415 | 23:57 |
| 52195 | **11 секунд** |
| 52248 | **26:54+**, жив |

От 11 секунд до часа. Это **не таймер**: таймер даёт похожие числа.
Смерть вызывается **событием**, а не временем.

**Что известно о моменте смерти 45415** (единственная наблюдённая полностью):
за 10 секунд до неё соединения выросли с 39 до 47 TCP (29 → 35 к серверу) —
слабый признак, на одном случае вывода не делаю.

**Кандидаты в событие, ни один пока не проверен:**

1. **смена соты у хотспота** — путь до сервера меняется, старые соединения
   рвутся все разом. Проверяется: писать шлюз и RTT рядом с PID;
2. **сервер закрывает сессию** — тогда смерть придёт при любом состоянии
   машины, и виноват провайдер VPN, а не Mac;
3. **сам Happ решает переподключиться** — например по деградации качества.

🔴 **Это уже седьмой отзыв за утро, и все семь — мои собственные версии.**
Порядок один: увидел контраст → назвал законом → проверил → отозвал.
Разница между первыми и последними в том, что теперь я называю
опровержение **до** проверки, и она занимает минуты, а не час.

### 09:01 — в сборщик соединений добавлен ПУТЬ, чтобы различить версии смерти

Три версии («смена соты», «сброс сессии сервером», «решение Happ») дают
одинаковую картину, пока не видно пути до сети. Добавлены три поля:

| поле | что различает |
|---|---|
| `мой_ip` | адрес от хотспота — меняется при переподключении к соте |
| `шлюз` | 🔴 **`ipconfig getoption en0 router`**, не `route -n get default`: маршрут по умолчанию идёт через `utun`, шлюза у него нет, поле выходило пустым |
| `rtt_мс` | средняя задержка по сокетам к серверу — сейчас **79–85 мс** против 69.5 два часа назад |

Ряд, набранный до этой правки, сохранён отдельно как
`conn-samples-без-пути.tsv` — в нём нет трёх столбцов, объединять нельзя.

🔴 **`PIT-G`, 20-й повтор, пойман собственной проверкой.** `conn_sampler.sh`
был описан в журнале, в `CHANGELOG` и в карточке `PIT-199` — и **не связан
с `README` кита**. Обвязкой каждый раз оказывается то место, которого
не касалась текущая правка.

### 09:05 — 58 реп разошлись ВТОРОЙ раз, тем же способом

Журнал соединений объявлен точным именем `conn-samples.tsv`. Срез ряда,
сохранённый перед правкой сборщика, получил имя `conn-samples-без-пути.tsv` —
и под объявление не подпал.

**Точное имя в объявлении — утверждение, что вариантов не будет.** У любого
ряда рано или поздно появляется срез: перед правкой, при смене столбцов,
при разделении на «до» и «после». Ряды переведены на `*`.

Час назад тот же файл `.runtime-writes` уже правился по тому же поводу —
и правился **точным именем**, то есть починка воспроизвела дефект.

### 🟢 09:12 — смерть туннеля поймана с полными полями. Ответ третий из трёх

Исходы были названы **до** наблюдения. Смерть `52248` в **09:10:54**,
прожил **40:30**:

| проверка | что показал ряд | вывод |
|---|---|---|
| сменился адрес или шлюз? | `172.20.10.3` / `172.20.10.1` — **те же** до и после | 🔴 версия «смена соты» **опровергнута** |
| упали ли соединения заранее? | 39 · 39 · 41 · **44** — наоборот, росли | 🔴 версия «сервер закрыл сессию» **опровергнута** |
| путь тот же, соединения держались? | **да, оба** | 🟢 **Happ переподключается САМ** |

Память перед смертью — ровно **25 МБ**, как и все 40 минут.

### Механизм, которого раньше видно не было: RTT растёт ПЕРЕД смертью

| время | RTT мс |
|---|---|
| 09:09:30 | 76.1 |
| 09:09:47 | 76.3 |
| **09:10:04** | **89.3** |
| **09:10:22** | **89.3** |
| 09:10:39 | 78.1 |
| **09:10:54** | 🔴 **смерть** |

Задержка скакнула на **+13 мс за 50 секунд до перезапуска**. Похоже, Happ
следит за качеством канала и **переподключается при деградации** — это
объяснило бы и разброс жизней в 150 раз: не таймер, а порог качества.

🔴 **Чем опровергается:** одно наблюдение. Если следующая смерть придёт
при ровном RTT — версия умрёт. Ряд идёт, проверка бесплатна.

### И побочное, неприятное: после перезапуска стало ХУЖЕ

| | RTT |
|---|---|
| до смерти | **76 мс** |
| после перезапуска | **90–95 мс** |

Новый туннель встал на путь на **20 % медленнее** прежнего. То есть
перезапуск, задуманный как починка, ухудшил связь. Если версия про порог
качества верна, это самоусиление: хуже → переподключение → ещё хуже.

**Заодно видно устройство перезапуска:** сначала поднимается процесс
`62614` (жил **6 секунд**, 3 соединения к серверу), затем настоящий `62647`.
Та же пара была в 08:30 (`52195` 11 с → `52248`). Первый процесс —
не сбой, а **штатная фаза**: пробное подключение перед основным.

### 🔴 09:19 — поправка: «перезапуск сделал хуже» НЕ следует из данных

Сказано владельцу: *«новый туннель встал на путь на 20 % медленнее»*,
и из этого выведен совет про самоусиление. **Вывод слишком сильный.**

Числа верны — медиана RTT по 34 замерам старого туннеля **78.3 мс**,
по 25 замерам нового **90.6 мс**, разница **+12.3**. Неверна причинность.

**Порядок событий убивает версию «перезапуск испортил путь»:**

| время | RTT | что было |
|---|---|---|
| 09:09:47 | 76.3 | старый туннель |
| **09:10:04** | **89.3** | 🔴 **задержка выросла — ДО смерти** |
| 09:10:22 | 89.3 | старый туннель ещё жив |
| 09:10:39 | 78.1 | |
| 09:10:54 | — | смерть |
| 09:11:28 | 90.6 | новый туннель |

**Деградация пришла ПЕРВОЙ, за 50 секунд до перезапуска, и осталась
после него.** Значит перезапуск — реакция на ухудшение, а не его причина.
Разделить «помог», «навредил» и «ни при чём» по одному событию **нельзя**.

**Что остаётся верным:** Happ переподключается сам (путь тот же, соединения
держались — проверено), и росту задержки перезапуск не помешал.

**Что снимается:** утверждение про самоусиление «хуже → перезапуск → ещё
хуже». Оно построено на порядке, который данные не подтверждают.

🔴 **Совет владельцу тоже смягчается.** Было: «отключи автопереподключение,
получишь стабильнее пинг». Честно: отключение уберёт **обрывы всех
соединений разом** — это точно. Про пинг обещать нечего: он ухудшился
до перезапуска и от него, судя по всему, не зависит.

### 🔴 09:22 — задержка растёт МОНОТОННО. Поправка подтвердилась, есть предсказание

71 замер RTT за 20 минут, по десятиминутным окнам:

| окно | замеров | медиана RTT |
|---|---|---|
| 09:0x | 31 | **78.0** |
| 09:1x | 33 | **87.8** |
| 09:2x | 7 | **104.9** |

Первые восемь замеров — **79.7**, последние восемь — **104.2**.
**Рост +24.5 мс за 20 минут, монотонный.**

🟢 **Это подтверждает поправку 09:19.** Смерть туннеля в 09:10:54 не даёт
ступеньки в этом ряду — задержка росла до неё, росла после и растёт сейчас,
через 10 минут после перезапуска. **Перезапуск ни при чём: деградирует
канал, а не туннель.**

Версия «перезапуск испортил путь» умерла окончательно: если бы дело было
в нём, после 09:11 задержка встала бы на новом уровне и держалась.
Она продолжает расти.

### Предсказание, названное заранее

Если версия «Happ переподключается при деградации» верна, а канал
деградирует и дальше — **следующая смерть туннеля должна прийти скоро**,
и перед ней задержка будет ещё выше.

| исход | что значит |
|---|---|
| смерть в ближайшие 10–20 минут при RTT > 100 | 🟢 версия усиливается |
| туннель живёт час при RTT 110+ | 🔴 версия про порог умирает |
| смерть при **упавшей** задержке | 🔴 версия умирает, причина другая |

Проверка бесплатна: сборщик пишет сам.

**Кадры в это время:** 42 · 26 · 35 · 39 · 51 · 39 · 47 · 42 — среднее **40**,
без провалов. Растущая задержка канала на отрисовку не влияет, как
и ожидалось: это разные подсистемы.

### 🟢 09:24 — деградация НЕ в Wi-Fi до телефона. Разделено по концам

Задержка туннеля выросла до 100 мс. Вопрос: портится радиоканал
Mac ↔ телефон или путь дальше? Замерены оба конца.

**Ближний конец — Wi-Fi до телефона:**

| | |
|---|---|
| канал | 48 (5 ГГц, 40 МГц) |
| сигнал / шум | **−43 / −96 dBm** → отношение **53 дБ** |
| скорость передачи | **400 Мбит/с**, MCS 9 |
| задержка до шлюза | **3.3 мс** среднее (2.3–6.1) |
| потери | **0 %** на 8 пробах |

🟢 **Радиоканал до телефона идеален.** Сигнал −43 dBm — это «телефон
лежит рядом», отношение сигнал/шум 53 дБ — предел возможного, скорость
на максимуме стандарта.

**Дальний конец:** ICMP до VPN-сервера — 100 % потерь, но это известное
свойство (сервер режет ICMP как класс), а не показатель.

**Вывод, который из этого следует:**

> **3.3 мс — это весь вклад Mac и Wi-Fi. Остальные ~97 мс из ста —
> сотовая связь телефона, интернет и VPN-сервер.** Деградация +24.5 мс
> за 20 минут живёт ТАМ, и ни настройки Mac, ни положение ноутбука,
> ни Wi-Fi на неё не влияют.

**Что это значит владельцу.** Если задержка растёт — смысл имеют только
два действия: **сменить сервер в Happ** (если дело в нём) или **сменить
точку подключения телефона** — другая сота, другой оператор, проводной
интернет. Всё, что делается на маке, бьёт мимо: его вклад 3 мс из 100.

🔴 **Чего этот замер НЕ различает:** сотовую часть от интернета и сервера.
Для этого нужна проба до промежуточной точки, а весь трафик идёт
в туннель — прямых маршрутов наружу, кроме самого VPN-сервера, нет.

### 09:26 — в сборщик добавлена задержка до шлюза: ближний конец против дальнего

Столбец `шлюз_мс` — `ping -c 1` до телефона каждый виток. Теперь **каждый
замер сам отвечает**, локальный это скачок или за телефоном:

| скачок в `rtt_мс` | `шлюз_мс` | вывод |
|---|---|---|
| есть | тоже вырос | Wi-Fi или сам телефон |
| есть | **ровный** | сота, интернет или VPN-сервер |

Первые замеры: `rtt` 92.3 / 89.9 мс при `шлюз` **3.5 / 2.8 мс**.
Вклад ближнего конца — **3 %**.

🔴 **`-W 1000` обязателен.** Без него потерянная проба вешает виток цикла
на системный таймаут, и ряд получает дыру именно в тот момент, когда
происходит интересное.

🔴 Ряд без нового столбца сохранён как `conn-samples-без-шлюза-rtt.tsv`.
Объявление в `.runtime-writes` **уже покрывает** его маской `conn-samples*.tsv`,
заведённой часом раньше по тому же поводу — это первый случай за утро,
когда прошлая починка сработала на опережение.

### 🔴 09:33 — разделение работает: дальний конец растёт, ближний стоит

26 замеров с обеими задержками, 09:25:58 – 09:33:09, смен туннеля ноль:

| | первые три | последние три | сдвиг |
|---|---|---|---|
| туннель | **92.3** | **106.9** | **+14.6 мс** |
| до телефона | 2.84 | 2.74 | **−0.10 мс** |

🟢 **Столбец `шлюз_мс` окупился сразу.** Ближний конец не шелохнулся,
пока дальний вырос на 15 %. Раньше такой скачок пришлось бы объяснять
догадкой; теперь он **прямо приписан** соте, интернету или VPN-серверу.

**Общий ход задержки за полчаса наблюдения: 78 → 108 мс.**

Это уже не мелочь для игры: +30 мс к пингу — заметная разница
в реакции. И ни один из рычагов на маке этого не изменит.

**Туннель при этом держится 21:53 без перезапуска** — при задержке,
которая на 15 мс выше той, при которой он умер в 09:10.

🔴 **Это ослабляет версию про порог качества.** Если бы Happ рвал
соединение по достижении порога, при 108 мс он давно бы переподключился.
Не переподключается. Значит либо порога нет, либо он не по задержке,
либо смерть 09:10 вызвана чем-то другим, а совпадение с ростом RTT
было случайным — как и предупреждало правило про одно наблюдение.

**Ждём дальше.** Проживёт час при 110+ — версия про порог умрёт.

### 🔴 09:43 — владелец: «игра просто зависает и теряется соединение, раза 3»

**Проверено немедленно: туннель НЕ умирал.** Процесс `62647` живёт
с 09:11:14, **30 минут 45 секунд подряд**. Смен процесса за это время —
ноль. Соединения ровные, провалов ниже медианы нет. Пробы до шлюза
прошли все, выбросов выше 10 мс нет.

> **Значит зависания не объясняются ни падением туннеля, ни локальным
> каналом.** Обе версии, которыми утро объясняло рывки, к этим событиям
> отношения не имеют.

🔴 **Событие короче шага наблюдения невидимо ПО ПОСТРОЕНИЮ.** Шаг был
15 секунд; зависание на 3–5 секунд не оставит следа ни в одном столбце.
Ряд надо **сгущать**, а не толковать имеющийся — заведён `freeze_watch.sh`
с шагом **1 секунда**: байты `en0`, задержка до шлюза, живость туннеля.

### 🔴 Bash не принимает кириллицу в именах переменных

Первая редакция `freeze_watch.sh` звала переменные по-русски (`пред`,
`КОНЕЦ`) — как в питоновских инструментах, написанных рядом сегодня же.
Сборщик **умер на первой строке**:

```
freeze_watch.sh: line 18: пред=1432244863 1327114979: command not found
freeze_watch.sh: line 20: unbound variable
```

Bash разбирает `пред=...` не как присваивание, а как **команду**: имена
переменных в POSIX-оболочке — только `[A-Za-z_][A-Za-z0-9_]*`. Python
допускает Unicode в идентификаторах, shell — нет.

> **Привычка, перенесённая между языками, стоила запуска.** И отказ был
> тихим: `nohup` съел вывод, а `pgrep` показал «встал» без причины.
> Нашлось только `bash -x`.

Комментарии по-русски работают везде — ограничение касается **имён**.

### 09:46 — секундный ряд: 72 замера, зависаний НЕТ, но виден локальный выброс

| | |
|---|---|
| замеров | **72** (09:43:52 – 09:46:03) |
| медиана входящего | **91 226 Б/с** |
| секунд с провалом входящего | **0** |
| пропущенных проб до шлюза | **0** |

🟢 **Ни одного зависания за две минуты.** Канал шёл ровно.

**Единственный выброс — 09:45:36: до шлюза 141 мс** при обычных 2.88.
Локальный затык радиоканала в **50 раз**, при этом трафик не прервался
(118 267 Б в ту же секунду). Длительность ~140 мс — для игры почти
незаметно, но показывает, что **Wi-Fi иногда всё-таки икает**, вопреки
идеальным средним.

🔴 **Ложная тревога, снятая проверкой.** Автоматический поиск нашёл
«разрыв 45 с в 09:44:41» и это выглядело как замирание системы.
Проверено по `ps -o lstart` фонового процесса: сборщик запущен
в **09:44:39** — разрыв целиком мой, между пробным прогоном на 5 секунд
и рабочим. **Инструмент наблюдения оставил в ряду след собственного
перезапуска**, и без сверки со временем старта это доехало бы до отчёта
как находка. Родня `PIT-199`: тождество наблюдателя тоже надо проверять.

**Заодно виден собственный трафик:** в 09:45:39 строка `вверх_Б = 2 377 581`
— это ход вахты, пойманный посекундно. Пачка **2.4 МБ за одну секунду**
в канал, где игра отдаёт 21 кБ/с. Числа утра подтвердились на другом
масштабе.

**Ряд идёт ещё 38 минут.** Следующее зависание будет видно посекундно.

### 🔴🔴 09:47 — ЗАВИСАНИЕ ПОЙМАНО ПОСЕКУНДНО. Это односторонний обрыв

Секундный ряд показал то, чего не мог показать пятнадцатисекундный.

**09:47:18 – 09:47:31, четырнадцать секунд подряд:**

| | |
|---|---|
| входящий | **98 Б/с** (обычно 85 000) — падение в **870 раз** |
| исходящий | 1 754 – 7 849 Б/с — **продолжал идти** |
| до телефона | **2.9 – 3.9 мс**, ни одной потери |
| туннель | **жив**, pid 62647 не менялся |

> 🔴 **Это ОДНОСТОРОННИЙ обрыв: мак отправляет, ответов не приходит.**
> Локальный канал в это время исправен полностью — 3 мс до телефона,
> ноль потерь. Значит связь оборвалась **за телефоном**: сотовая сеть,
> интернет или сервер игры.

98 байт в секунду — это не «мало данных», а служебный минимум: ARP,
поддержание сессии. Игровых пакетов не приходило **вообще**.

### И это не одно событие, а серия

| время | длительность |
|---|---|
| 09:47:18 – 09:47:31 | **14 с** |
| 09:47:39 – 09:47:40 | 2 с |
| 09:47:45 – 09:47:48 | 4 с |
| 09:47:57 – 09:47:58 | 2 с |
| 09:48:01 | 1 с |

**Пять провалов за 43 секунды.** Первый — четырнадцатисекундный, дальше
короче. Именно так и ощущается «зависло и потеряло соединение».

🔴 **Мой трафик первым провалом НЕ объясняется:** пачка вахты
в 1 839 525 Б прошла в **09:47:34**, то есть **после** обрыва
09:47:18–09:47:31, а не до. Перед первым провалом исходящий был обычным
(20–29 кБ/с).

🔴 **А вот про последующие сказать нечего.** Провалы 09:47:39–48 пришли
сразу после моей пачки. Одно совпадение, четыре события — по правилу
`PIT-198` вывода не делаю. Проверяется просто: следующий такой же провал
при молчащей вахте либо подтвердит независимость, либо укажет на меня.

**Что это значит для владельца:** зависания приходят **не от мака и
не от Wi-Fi** — оба исправны в момент обрыва. Причина за телефоном.

### 🔴 09:56 — опыт «четыре минуты тишины» НЕДЕЙСТВИТЕЛЕН

Замысел был чистый: вахта молчит четыре минуты, и если провалы всё равно
приходят — её трафик оправдан. Тишина состоялась (максимум отдачи 217 кБ/с,
пачек не было), провалов **ноль**.

**И этот ноль ничего не значит.**

Разбор посекундно показал, что в **09:52:05 – 09:52:11** входящий сполз
со 108 000 до 1 626 Б/с за шесть секунд и дальше держался около нуля:

| время | вниз Б/с |
|---|---|
| 09:52:04 | 107 960 |
| 09:52:08 | 32 212 |
| 09:52:11 | **1 626** |
| 09:52:16 | **98** |

Снимок экрана в 09:56 — строка меню macOS, окна игры нет. Процесс `dota2`
жив (4 ч 37 мин, 1910 МБ), но **данных не получает**: матч кончился или
владелец свернул игру.

> 🔴 **Провалов «не было», потому что нечему было проваливаться.**
> Порог провала считается от медианы окна, медиана рухнула до 542 Б/с
> вместе с трафиком — и метод перестал что-либо различать.
> Это не отрицательный результат, а **испорченный опыт**.

**Класс ошибки — новый за сегодня:** условие опыта изменилось **в середине
опыта**, и изменил его не наблюдатель. Проверка «была ли тишина настоящей»
у меня была и сработала; проверки «была ли игра ещё живой» — не было.

> **Опыт с контролем над одной стороной проверяет обе стороны.**
> Убедиться, что молчал ты, недостаточно: надо убедиться, что вторая
> сторона в это время делала то же, что и раньше.

**Вопрос «виноват ли трафик вахты» остаётся открытым.** На шести событиях
до этого: три с пачкой перед ними, три без — сигнала нет. Повторить опыт
можно только во время живого матча.

### 🟢 10:10 — опыт с тишиной ПОВТОРЁН правильно: провалов ноль

Обе проверки, включая ту, которой не было в прошлый раз:

| проверка | результат |
|---|---|
| тишина настоящая? | 🟢 максимум отдачи **224 507 Б/с**, пачек не было |
| игра шла ВСЁ окно? | 🟢 по третям: **69 281 · 69 158 · 64 620** Б/с |
| **провалов** | **0 за 209 секунд** |

**Сравнение с окном, где вахта работала:**

| окно | длит. | пачки вахты | событий провала |
|---|---|---|---|
| 09:44:41 – 09:50:00 | 262 с | **были** | **6** |
| 10:05:45 – 10:09:56 | 209 с | **нет** | **0** |

🔴 **Это одно окно против одного, и вывода из него не следует.** Провалы —
редкие события: шесть из них пришлись на одну плохую минуту 09:47–09:48,
а не размазаны по всему окну. Отсутствие их в спокойные 3.5 минуты
объясняется и «вахта молчала», и «плохая минута просто не наступила».

**Что закроет вопрос — симметричный опыт:** такое же окно 3–4 минуты,
но с **работающей** вахтой при живой игре. Два окна одной длины, одно
условие различается. Если провалы появятся только там — связь есть;
если и там ноль — версия про вклад вахты умрёт.

**Побочно:** до шлюза был выброс **54.7 мс** при медиане 2.95. Второй
локальный икок за час (первый 141 мс в 09:45:36). Провала не вызвал,
но Wi-Fi иногда всё-таки заикается.

## 05.09.2026 10:18 — вторая половина опыта: вахта РАБОТАЛА, провалов всё равно ноль

Окно 10:14:15 – 10:18:20, 200 секунд, вахта закрывала батч v4.159.0.
Обе проверки годности пройдены: игра шла все три трети (медианы 78.3 · 79.4 ·
79.8 кБ/с), пачки были настоящие — **13 секунд** с отдачей выше 100 кБ/с
против **одной** в окне тишины.

| | окно 1 (тишина) | окно 2 (работа) |
|---|---|---|
| длина | 209 с | 200 с |
| медиана входящего | 67 849 Б/с | 79 080 Б/с |
| секунд с отдачей >100 кБ/с | **1** | **13** |
| **событий провала** | **0** | **0** |
| до шлюза, медиана | 2.95 мс | 2.96 мс |

**Версия «пачки вахты валят игру» этой парой НЕ поддержана.**

## 🔴 И два признания против этого же вывода

**Первое: контраст оказался слабее, чем задумывался.** По СОБЫТИЯМ пачек
разница тринадцатикратная, а по ОБЪЁМУ — всего 3.81 МБ против 5.16 МБ,
в 1.35 раза. Игра сама отдаёт ~17 кБ/с, то есть 3.4–3.6 МБ за такое окно, —
почти весь объём обоих окон её. Вклад вахты за 200 с вышел около **1.7 МБ**.
Опыт различал окна по всплескам, а не по нагрузке на канал.

**Второе: одиночный максимум в окне ТИШИНЫ выше — 224 507 Б/с против
146 785.** То есть в «молчащем» окне была секунда мощнее любой пачки вахты,
и произвела её не вахта. Чем — неизвестно; ряд `freeze_watch.sh` пишет
только суммы по `en0` и источника не знает.

**Что это значит для вывода.** Провалы редкие: шесть из них легли в одну
минуту 09:47–09:48. Ноль в двух спокойных окнах по 3.5 минуты одинаково
хорошо объясняется и «пачки ни при чём», и «плохая минута не наступила ни
там, ни там». Пара окон даёт **одно** сравнение — `PIT-198` ровно про то,
чтобы не объявлять его результатом.

**Честный итог:** прямых доказательств вины вахты нет, доказательств её
невиновности — тоже. Что остаётся в силе независимо от исхода: вахта
кладёт в канал владельца около 1.7 МБ за каждые три минуты работы, и это
его мобильный трафик.

**Что закрыло бы вопрос:** несколько окон каждого вида вперемешку и счёт
по СОБЫТИЯМ провала, а не по объёму. Протокол — `OPYT-vahta-i-provaly.md`.

### Инструмент, сделанный из этого урока, ОТКАЗАЛСЯ сравнивать мои же окна

`bin/compare_windows.py` заведён сразу после разбора и запущен на той же
паре. Ответ:

```
🔴 контраст по ОБЪЁМУ всего в 1.35 раза — этого мало.
Сравнение НЕ проводится. Исправьте окна и повторите.
```

Так и надо. Таблица выше остаётся **показаниями**, а не сравнением: строка
«версия пачками не поддержана» — это то, что видно глазами, и инструмент
её к выводу не допускает. Чтобы опыт стал законным, окно работы должно
отдавать вдвое больше окна тишины, то есть вахте надо работать плотнее,
а не просто «работать».

## 05.09.2026 10:25 — эпизод 09:55–09:58 разобран: разрыв ЗА телефоном

За 09:55–10:23 разбор нашёл 17 провалов — и **все 17 легли в 146 секунд**
подряд, с медианой входящего **2 563 Б/с** против 67 000 в остальное время.
Это не 17 событий, а **один обрыв на три минуты**. После 09:58 и до 10:23 —
двадцать пять минут без единого провала.

**Локальный конец во время обрыва был исправен:**

| | медиана | макс | потеряно проб |
|---|---|---|---|
| **во время обрыва** | 3.72 мс | **11.9** | **0** |
| вне обрыва | 2.98 мс | 428.1 | 2 |

Телефон отвечал за 3.7 мс без единой потери, пока игра стояла. Значит
**Wi-Fi и телефон ни при чём — рвалось за телефоном**: сеть оператора,
туннель или сервер.

🔴 **До правки инструмент печатал ровно обратное** — «локальный конец тоже
страдал», потому что брал максимум по всему окну и подписывал его как
относящийся к событиям. 428 мс были в здоровое время. Разобрано
в `PIT-200`, защита встроена: сводка по шлюзу теперь считается внутри
провалов и вне их раздельно, одной общей больше не существует.

## 05.09.2026 10:36 — 🔴 ПРИЧИНА ЗАВИСАНИЯ ПОЙМАНА НА ЖИВОМ ЭПИЗОДЕ

Второй эпизод: 10:35:29 – 10:36:15, 46 секунд. Ряд соединений показал
в ту же секунду:

```
10:35:14  62647  живёт 01:23:58  TCP 54  к_серверу 46  rtt 78.8
10:35:29      -  живёт 0         TCP  0  к_серверу  0  rtt   -     ← процесс УМЕР
10:35:45  14432  живёт 00:14     TCP 16  к_серверу  1  rtt  0.0
10:36:02  14676  живёт 00:12     TCP 27  к_серверу 23  rtt 82.3
```

**Туннель умер и родился дважды подряд.** Игра зависла ровно в эту секунду.
Пачек вахты перед эпизодом не было ни одной (максимум отдачи 60–184 кБ/с
при пороге пачки 500 кБ). Шлюз во время эпизода — 3.03 мс, максимум 9.9,
**ноль потерянных проб**.

> **Зависание — это смерть процесса туннеля, а не потеря пакетов.**
> Отсюда и разница, которую заметил владелец: раньше пакеты терялись
> постепенно, теперь игра встаёт целиком. Умирает процесс — умирают все
> TCP-соединения разом, и игре нечего переспрашивать.

## Первый эпизод устроен ИНАЧЕ — и это важно не смешивать

09:55–09:58, 146 секунд. Процесс **не умирал**: PID 62647 держался все
три минуты. Но:

| | во время эпизода | после восстановления |
|---|---|---|
| соединений к серверу | **8–13** | 30–34 |
| до сервера | **100–125 мс** | 78 мс |
| до шлюза | 3–10 мс | 3 мс |

То есть путь до VPN-сервера деградировал (задержка вырость в полтора раза),
соединения к нему осыпались втрое — а процесс жил. Восстановление
в 09:57:41 видно скачком соединений 27 → 48.

**Два эпизода — два разных механизма, и общее у них одно: локальный конец
исправен в обоих.** Телефон и Wi-Fi невиновны дважды.

🔴 **Оговорка о размере выборки.** Это два случая. Смерть процесса поймана
**один** раз, деградация пути — **один** раз. Что из них случается чаще
и что владелец видел «раза три», по двум эпизодам не решается.

## 05.09.2026 10:48 — три эпизода за час, разобраны механизмы

`diagnose_episode.py` свёл секундный ряд с рядом соединений. За 09:44–10:45:

| эпизод | длит | шлюз | вахта до | механизм |
|---|---|---|---|---|
| 09:47:18 – 09:48:16 | 59 с | 🟢 3.2 мс, 0 потерь | тихо (29 кБ/с) | путь: до сервера 91 мс |
| 09:52:11 – 09:57:51 | **341 с** | 🟢 9.1 мс, 0 потерь | тихо (22 кБ/с) | путь: до сервера **102–125 мс**, соединений 8–34 |
| 10:35:29 – 10:36:17 | 49 с | 🟢 3.0 мс, 0 потерь | тихо (184 кБ/с) | 🔴 **смерть процесса**: 62647 → 14432 → 14676 |

**Три — ровно столько, сколько назвал владелец («уже раза 3»).**

🔴 **Перед всеми тремя вахта молчала.** Максимум отдачи за 10 секунд до
эпизода — 22, 29 и 184 кБ/с при пороге пачки 500 кБ. Версия «пачки вахты
валят игру» к зависаниям **не относится вовсе**: ни одно из трёх не имело
пачки перед собой. (К прежней картине «постепенно теряются пакеты» это
ничего не говорит — там вопрос остаётся открытым.)

🔴 **Локальный конец исправен во всех трёх.** Шлюз 3.0–9.1 мс медианы,
максимум 31.8, **ноль потерянных проб**. Телефон и Wi-Fi невиновны трижды.

**Слияние эпизодов изменило картину первого случая.** Посекундная дробилка
давала «17 событий»; слияние через разрывы до 15 с показало **один** эпизод
на 341 секунду, начавшийся в 09:52:11, а не в 09:55. Разница не
косметическая: 17 коротких событий и один шестиминутный обрыв лечатся
по-разному.

**Что осталось неизвестным:** почему процесс туннеля умер и почему
деградировал путь до сервера. Ряд показывает, ЧТО случилось, и не
показывает, отчего. Логи Happ не читались.

## 05.09.2026 10:52 — жалоба «жутко падать FPS»: срез в момент жалобы

| процесс | %CPU | возраст |
|---|---|---|
| dota2 | **96.9** | **02:04** |
| WindowServer | 39.3 | 9 ч |
| iTerm2 | 23.7 | 2 ч |
| Activity Monitor | 21.1 | 00:12 |

Входящее за последние 20 с — **4 798 Б/с** против 70–90 кБ/с в бою.
**Игра была не в матче**, а в прогреве: перезапущена две минуты назад.
Кадры в этот момент плохие всегда.

🔴 **Поправка, сделанная в тот же ход.** Первый срез показал `steam_osx`
на 97.4 %, и это было названо «клиент Steam ест ядро сам по себе — выключи
оверлей». Через минуту стало видно, что 97 % — это **запуск игры**,
а `dota2` живёт 75 секунд. Совет был не вреден, но причина названа
неверная: Steam не жёг ядро, он запускал Dota. Тот же класс, что
`PIT-197` — величина взята не оттуда.

**Что в срезе настоящее и осталось в силе:**

- **своп 845 МБ** при 8 ГБ RAM, в компрессоре 959 449 страниц (~3.7 ГБ),
  40 млн распаковок — память кончилась;
- **iTerm 23–49 %** и WindowServer 32–39 %: отрисовка моего вывода поверх
  игры стоит владельцу кадров. Это вторая цена вахты после канала
  (`/auto` §0), и раньше она нигде не была названа;
- игра в **фоне** душится системой: пока владелец читает терминал, Dota
  не в фокусе.

**Выданные владельцу рычаги, по убыванию отдачи:** разрешение рендеринга
75 % (Iris 655 против 4.1 млн пикселей ретины — единственный кратный
рычаг) · «Полноэкранное окно» вместо «Полный экран», чтобы переключение
не было сменой рабочего стола · не читать вахту во время матча ·
выключить оверлей Steam (`gameoverlayui` висит отдельным процессом) ·
закрыть Activity Monitor.

## 05.09.2026 11:00 — системный лог о смерти туннеля: что нашлось и что нет

🔴 **Сначала ловушка, стоившая часа.** `log show` в первый раз вернул
**пустой вывод**, и это прочиталось как «в логе ничего нет». На деле
`log` в zsh владельца **перебит алиасом**: `(eval):log:1: too many
arguments`. Тот же класс, что `ls` → `eza` (`SYN-029`): алиас превращает
команду в другую, а пустой вывод неотличим от честного «ничего не нашлось».
Работает только полный путь **`/usr/bin/log`**.

### Что лог показал

**1. Wi-Fi был исправен — подтверждено независимо от пингов.**
`LQM-WIFI: slow-wifi` в 10:34:50: RSSI **−38**, SNR **35**,
`dataStallScore 100`, `networkScore 100`, `fails 0`. Локальный конец чист
третьим способом подряд.

**2. 🔴 Туннель резолвит имя сервера на КАЖДОЕ соединение.**
За спокойную минуту 10:33–10:34 — **60 запросов** `DNSServiceQueryRecord`
от pid 62647 (A и AAAA парами, то есть ~30 соединений в минуту). Соединения
живут по 0.27–4.9 секунды и тут же закрываются, каждое новое начинается
с похода к резолверу. На телефонном хотспоте резолвер — оператора.

> **Это узкое место, о котором раньше не было известно:** любая заминка
> DNS у оператора превращается в заминку игры, и она не видна ни в пинге
> до шлюза, ни в пинге до сервера.

**3. За 4 секунды до смерти туннеля Happ был в переходе жизненного цикла.**
10:35:25.057 — `Happ[557] UIKit.MacHelper:Lifecycle @ isAttemptingTermination
(state: Running)`, сразу перед этим — UIEvent к окну Happ. Следом пошли
свежие TLS-рукопожатия.

🔴 **Чего это НЕ доказывает.** Строка означает, что приложение спрашивало
себя о завершении, а не что оно завершалось по чьей-то команде. Владелец
перезапускал VPN около 10:47, а не в 10:35. Связь события 10:35:25
со смертью процесса в 10:35:29 — **совпадение по времени, и не более**:
один случай, механизм не прослежен.

**Осталось неизвестным:** почему процесс умер. Лог показывает соседей
события, а не причину.

## 05.09.2026 11:03 — заведён ряд задержки резолвера, и сразу оговорена его граница

`bin/dns_probe.sh`, шаг 5 с. Первые пробы через хотспот: **292 · 536 · 329 мс**.

🔴 **И тут же проверено, не взята ли величина не оттуда.** Проба берёт
случайное имя, то есть меряет резолвер **мимо кэша**. Сравнение:

| | задержка |
|---|---|
| одно и то же имя, повтор (кэш) | **20 мс** |
| случайные имена (промах кэша) | **185–536 мс** |

Туннель спрашивает **одно** имя, значит почти всегда попадает в кэш
и платит 20 мс. Полную цену он платит только при истечении TTL записи.

> **Из этого ряда НЕЛЬЗЯ заключить «туннель тратит 300 мс тридцать раз
> в минуту».** Он отвечает на другой вопрос: сколько стоит промах кэша,
> то есть насколько больно в момент истечения TTL.

Оговорка вписана в шапку самого инструмента, а не только сюда: `PIT-197`
пойман **до** вывода, а не после.

**Что остаётся содержательным.** 20 мс на соединение при 30 соединениях
в минуту — это 0.6 с в минуту чистого ожидания резолвера, и оно ложится
поверх 78 мс до сервера. А в момент истечения TTL цена подскакивает
в 9–27 раз, и именно этот момент стоит искать рядом с эпизодами.

## 05.09.2026 11:08 — поправка ко всем длительностям выше: они были занижены

Ряд `freeze_watch.sh` назывался секундным, потому что в цикле стоит
`sleep 1`. Замер по 3766 переходам: медиана 1 с, но **801 шаг (21 %)
выходит по 2 с**, среднее **1.22 с**. Лишние шаги — это `ping`,
ждущий таймаута, то есть они сгущаются **внутри плохих секунд**.

**Значит все длительности, посчитанные числом строк, занижены — и сильнее
всего внутри событий.** Затронуты записи выше:

| где | было (по строкам) | ближе к правде |
|---|---|---|
| первый эпизод | «146 с» | ~178 с |
| «17 событий уместились в 146 с» | 146 | ~178 |

Числа из `diagnose_episode.py` **верны** — он с самого начала считал
по отметкам времени (эпизоды 59 · 341 · 49 с). Расходились именно
показания `analyze_freeze.py`, и он исправлен: счёта строками в нём
не осталось. Разбор — `PIT-203`.

## Четвёртый эпизод: 11:00:21 – 11:00:52 (32 с)

Вахта молчала (64 кБ/с при пороге 500). Шлюз 3.01 мс, ноль потерь.
Процесс туннеля жив, соединений к серверу 39–42 — **не осыпались**,
до сервера 82 мс — **норма**. То есть все измеряемые величины в норме,
а игра стояла полминуты.

🔴 **Это самый неудобный из четырёх эпизодов:** предыдущие три хотя бы
показывали, что именно испортилось (путь замедлился или процесс умер).
Здесь не испортилось ничего из того, что мы умеем мерить. Ряд соединений
идёт с шагом 15 с и мог проспать событие целиком — это ограничение
по построению, а не вывод.

## 05.09.2026 11:12 — 🔴 ГЛАВНОЕ ЧИСЛО ЭПИЗОДА НАЙДЕНО: ОДНОСТОРОННОСТЬ

Если во время эпизода **отдача держится, а приём рухнул** — машина слала,
обратно не шло, и рвётся **обратный путь**. Если упало в обе стороны —
перестали слать мы. Одно число разделяет то, что четыре часа выглядело
одинаково.

| эпизод | длит | приём | отдача | что это |
|---|---|---|---|---|
| 09:47:18 | 59 с | **6 %** | **70 %** | 🔴 односторонний разрыв |
| 09:52:11 | 341 с | 1 % | 2 % | 🟡 ложный — игра не в матче |
| 10:35:29 | 49 с | 4 % | 43 % | 🔴 смерть процесса туннеля |
| 10:47:25 | 465 с | 3 % | 12 % | 🟡 ложный — владелец сам перезапускал VPN |
| 11:00:21 | 32 с | **7 %** | **82 %** | 🔴 односторонний разрыв |

**Из пяти «эпизодов» настоящих поломок три.** Два оказались не поломками:
один — конец матча, второй — перезапуск VPN самим владельцем, о чём он
сказал словами. Порог живости защищает **окно** целиком и не защищает
эпизод внутри живого окна; предупреждение про «две минуты в нуле в обе
стороны» встроено в `diagnose_episode.py`.

**Что стало ясно про настоящие три:**

- **шлюз исправен во всех трёх** — 3.0–3.2 мс, ноль потерянных проб;
- **вахта молчала перед всеми тремя**;
- два из трёх — **односторонние**: уплинк работал, ответа не было.
  Это не потеря пакетов и не перегрузка канала, а обрыв обратного пути
  где-то за телефоном;
- третий — смерть процесса туннеля, и там отдача тоже держалась на 43 %.

🔴 **Оговорка о размере.** Три поломки за полтора часа. Односторонних —
две. Вывод «рвётся обратный путь» опирается на **два** случая.

## 05.09.2026 11:33 — версия «эпизоды связаны с возрастом туннеля» ОПРОВЕРГНУТА

Догадка возникла из наблюдения: после того как владелец перезапустил VPN
в 10:47, наступили 32 минуты тишины. Проверка:

| эпизод | возраст туннеля |
|---|---|
| 09:47:18 | 35 мин |
| 09:52:11 (ложный) | 40 мин |
| 10:35:29 | **1 ч 24 мин** |
| 11:00:21 | **10 мин** |

Три настоящих эпизода пришлись на возрасты 35 мин, 1 ч 24 мин и **10 мин** —
разброс во всю ширину. Экспозиция при этом примерно ровная: 690–1770 секунд
наблюдения в каждом десятиминутном ведре, то есть перекоса выборки нет.

**Версия мертва.** Молодой туннель ломается так же, как старый.

🔴 **И «32 минуты тишины после перезапуска» тоже ничего не значат** —
эпизод 11:00:21 случился уже ПОСЛЕ того перезапуска, на десятой минуте
жизни нового процесса. Тишина началась после него, а не после перезапуска.
Признак того, что версия строилась на удобном отрезке ряда: началом
отсчёта взято событие, которое хотелось назвать причиной.

## 05.09.2026 11:42 — кадры сняты с экрана: потолок не изменился

Десять кадров с шагом 3 с во время матча:

```
42 · 43 · 31 · 36 · 33 · 37 · 34 · 38 · 37 · 29
```

Медиана **36**, размах **29–43**. Это тот же потолок, что был найден утром
(35–48), то есть **разрешение рендеринга владелец не понижал** — главный
рычаг не применён.

В строке сети клиента: ↓ 20–27, ↑ 42–72, и в одном кадре из десяти ↓ **13
красным** — клиент сам пометил просадку. Что именно означают эти два числа
(пакеты в секунду? килобиты?), по снимку **не определить**, и подписывать
их догадкой нельзя.

🔴 **Оговорка о влиянии замера на замеряемое.** `screencapture` сам стоит
кадров, и первая проба из трёх дала FPS 22 — ниже всего, что видно
в длинном ряду. Возможно, это цена первого снимка (прогрев), возможно —
совпадение с боем. **Ряд из десяти надёжнее ряда из трёх ровно поэтому**,
и число 22 в выводы не берётся.

## 05.09.2026 11:47 — 🔴 ГЛАВНЫЙ СОВЕТ УТРА БЫЛ НЕВЕРЕН

Совет «понизь разрешение рендеринга с 2560×1600» выдан владельцу **четыре
раза** и стоял главным выводом в отчёте по рывкам. Конфиг Dota
(`userdata/*/570/local/cfg/video.txt`, оба профиля):

```
"setting.defaultres"        "1280"
"setting.defaultresheight"  "800"
"setting.high_dpi"          "0"
```

**Игра уже рендерила в 1280×800.** «2560×1600» взято из разрешения **экрана**,
а не из настроек игры.

Там же видно, что и остальное на дне: `gpu_level 0`, `cpu_level 1`, тени,
SSAO, FXAA, нормал-мапы выключены, вода дешёвая, `mip_bias 2`. **Настроек
Dota, которые можно было бы ещё опустить, не осталось.**

**Что это меняет.** Потолок в 36 кадров — это железо: Iris Plus 655 плюс
8 ГБ памяти со свопом 845 МБ. Остаются только системные рычаги: не держать
игру в фоне (macOS душит фоновое приложение), освободить память, свернуть
терминал (iTerm + WindowServer брали до 80 % ядра).

🔴 **Ошибка самоподтверждалась:** каждый замер кадров показывал 29–43, то
есть «низко», и это читалось как подтверждение. Неверная причина и верное
следствие уживаются без противоречия. Разобрано в `PIT-205`; врезка
с поправкой поставлена в шапку самого отчёта.

## 05.09.2026 11:50 — что в конфиге Dota ЕЩЁ не выкручено вниз

Утверждение «рычагов внутри игры не осталось» проверено построчно
по всем 48 настройкам. Оно оказалось **слишком сильным** — четыре
настройки не на минимуме:

| настройка | сейчас | что значит | во что упирается |
|---|---|---|---|
| `r_dota_fsr_upsample` | **0** | апскейл FSR **выключен** | вот это и есть настоящая версия «понизь разрешение»: рендер во внутреннем разрешении ниже с растяжкой |
| `r_low_latency` | **1** | режим низкой задержки | ограничивает очередь кадров; на слабой видеокарте **снижает** FPS ради отзывчивости |
| `mem_level` · `gpu_mem_level` | **2** | бюджет памяти | 8 ГБ со свопом 845 МБ — здесь занижение может дать больше, чем графика |
| `cpu_level` | **1** | нагрузка на процессор | минимум — 0 |

Остальное уже на дне: тени, SSAO, FXAA, нормал-мапы, ветер на деревьях,
параллакс, блум, портреты, живность, ткань — выключены; вода дешёвая,
`cl_particle_fallback_base 4`, `r_texture_stream_mip_bias 2`.

🔴 **Состояние настроек — ФАКТ (прочитан из файла). Эффект их изменения —
ПРЕДСКАЗАНИЕ, и оно не измерялось.** Ровно на смешении этих двух вещей
и построен `PIT-205`: там причина была выведена, а не прочитана. Здесь
прочитано текущее значение; чего оно даст при смене — проверяется только
сменой и замером кадров до/после.

🔴 **Конфиг игры вахта не правит.** Это файл владельца, изменение видно
в его игре, и цена ошибки платится им, а не журналом.

## 05.09.2026 11:59 — шестой «эпизод» и уточнение порога ложности

11:57:08 – 11:59:10, 123 с: приём **1 %**, отдача **5 %** от фона. Туннель
жив (pid 27793), соединений к серверу 27–62 — держались, до сервера 81 мс,
резолвер 287 мс медианы, 24 пробы, ноль неудач.

**Dota при этом работала** — процесс жив, 1 ч 05 мин, 272 % процессора.
То есть игра была **между матчами**, а не сломана.

**Порог «подозрительно» опущен со 120 с до 60.** Обоснование — счёт
накопленных случаев, а не вкус:

| «обе стороны в нуле» дольше минуты | исход |
|---|---|
| 09:52:11 (341 с) | конец матча |
| 10:47:25 (465 с) | владелец перезапускал VPN |
| 11:57:08 (123 с) | конец матча |

**Три из трёх — не поломки.** Настоящие поломки дня выглядели иначе:
32–59 с и **односторонне**, а смерть туннеля держала отдачу на 43 %.
Порог 120 пропустил третий случай молча — он длился 109 с в момент разбора.

🔴 **Итог по счёту за день: шесть найденных эпизодов, настоящих три.**
Половина «зависаний» — состояния игры, а не сети. Без разделения по
односторонности этот счёт был бы вдвое завышен, и все выводы про сеть
строились бы на вдвое большем числе, чем есть.

## 05.09.2026 12:10 — наблюдение завершено владельцем, сборщики погашены

Все три ряда остановлены (`freeze_watch`, `conn_sampler`, `dns_probe`).
Ряды остаются на диске и пригодны для разбора задним числом:
`freeze-watch.tsv`, `conn-samples.tsv`, `dns-samples.tsv`.

**Итог наблюдения 09:44 – 12:01:** шесть эпизодов найдено, настоящих
поломок **три** — два односторонних разрыва обратного пути и одна смерть
процесса туннеля. Разбор — `../reports/dota-zavisaniya-diagnoz-2026-09-05.md`.

🔴 **Возобновить наблюдение одной командой:**
`sh 12-workstation-kit/bin/collectors.sh start`

## 06.09.2026 09:00 — машина уснула дважды под нагрузкой вахты

🔴 **Два принудительных сна, оба перебивают пользовательские настройки:**

```
03:57:41  'Dark Wake Thermal Emergency'  AC, заряд 100%   ← перегрев
07:19:29  'Low Power Sleep'              AC, заряд  13%   ← разряд в розетке
```

Владелец спросил, почему компьютер уснул при выставленном «никогда не засыпать».
Ответ: настройка про **простой**, а сработали защитные механизмы ядра — они на неё
не смотрят.

**Разряд при воткнутой зарядке — не сбой блока питания:** после пробуждения он зарядил
51 % → 76 % за полчаса. Потребление превышало подвод, разницу машина брала из батареи.

**Причина нагрузки — работа вахты, без чужого участия:** полный `pytest` (2046 тестов,
20 мин, `load` 9+ на четырёх ядрах), матрица PostgreSQL, `vitest --coverage`, линтеры
по всему `src` — подряд, без пауз.

**Правило, выведенное отсюда:** перед долгим прогоном смотреть `pmset -g batt`;
ниже 40 % не стартовать. `caffeinate -i` не спасает — он держит сон по простою,
а не по перегреву и не по разряду.

Разбор — `../reports/sleep-during-heavy-run-2026-09-06.md`.


## 06.09.2026 09:30 — поправка: дело не в нагрузке, а в батарее

Владелец: *«странно оно потому что мак был закрыт выключен вообще»*. Проверка
по его реплике нашла то, чего первый разбор не увидел:

```
Condition:            Service Recommended     ← Apple рекомендует замену
Cycle Count:          384
Full Charge Capacity: 3728 mAh
Adapter:              60 W, подключён
```

🔴 **Показания заряда недостоверны.** «100 %» у деградировавшей батареи не означает
полного бака, и падение идёт обрывом, а не плавно: 05:41 — 100 %, 07:19 — 13 %.
На исправной батарее с 60-ваттным адаптером такая работа даёт просадку, а не обвал.

**Адаптер исправен** — после засыпания зарядил 13 % → 76 % за полтора часа.

**Термоавария 03:57 при этом подтвердилась и случилась БЕЗ вахты:** крышка закрыта
с 01:00, шли обычные `DarkWake` по расписанию. У закрытого ноутбука нет продува.

**Действие владельца:** батарею пора менять. До замены любой порог по заряду
считать ориентировочным.

Разбор — `../reports/nochnoe-otklyuchenie-2026-09-06.md`.


## 06.09.2026 10:00 — измерен баланс мощности: система берёт 91 Вт при адаптере 60

Владелец: «зарядка воткнута да я не знаю почему он разряжается». Замер
`ioreg -rn AppleSmartBattery` дал ответ числами:

```
адаптер            60 Вт
батарея отдаёт     30.6 Вт (InstantAmperage −2995 мА × 10.23 В)
потребление        ≈ 91 Вт
IsCharging = No при заряде 9 %
```

🔴 **Дефицит ~30 Вт машина берёт из батареи** — штатное поведение под нагрузкой выше
мощности блока. Противоречия «воткнут и разряжается» нет.

🔴 **Ёмкость 3550 из 5088 мАч = 69.8 % заводской**, 385 циклов, Service Recommended.
Запас, из которого добирается дефицит, на треть меньше нового.

**Нагрузку в момент замера давали НЕ тесты:** iTerm2 54.6 % + WindowServer 40.5 % —
отрисовка вывода вахты. Тот же механизм, что нашёлся 05.09 при разборе просадки кадров
в Dota, но тогда он выглядел неудобством, а не причиной разряда.

Разбор — `../reports/razryad-pri-vtknutoy-zaryadke-2026-09-06.md`.


## 06.09.2026 10:12 — поправка владельца: часть нагрузки была НЕ системной

Владелец: *«просто я отключил зарядку от телефона мак его заряжал через себя
и флешку для мышки»*.

🔴 **Ошибка моего разбора.** Я назвал источником 91 Вт процессы (`iTerm2`,
`WindowServer`) и не проверил, что висит на портах. **Список процессов — не полный
перечень потребителей:** периферия в нём не видна вовсе, а телефон через USB —
это до 7.5–12 Вт постоянно.

**Три замера подряд** (ряд ведётся в `power-samples.tsv`):

| время | разряд | потребление | что воткнуто |
|---|---|---|---|
| 10:00 | 30.6 Вт | ~91 Вт | телефон + мышь, шли тесты |
| 10:07 | 13.5 Вт | ~73 Вт | телефон + мышь, тестов нет |
| 10:12 | 13.7 Вт | ~74 Вт | **телефон отключён** |

**Отключение телефона само по себе разряд не убрало** — 13.5 → 13.7 Вт, шум. Значит
он объяснял часть первого замера вместе с тестами, но не остаток. Оставшиеся ~14 Вт
дефицита — отрисовка вывода вахты.

**Правило:** перед выводом «столько потребляет система» спрашивать, что воткнуто
в порты. Вопрос владельцу дешевле неверного вывода.

Разбор — `../reports/razryad-pri-vtknutoy-zaryadke-2026-09-06.md`.


## 06.09.2026 10:09 — РЕШЕНО: виноват был телефон, заряжаемый от мака

После отключения телефона И переподключения адаптера:

```
InstantAmperage = +1719 мА   (ЗАРЯД, было −2995)
IsCharging      = Yes        (было No всё утро)
потребление     ≈ 40 Вт      (было 91)
```

🔴 **Телефон съедал не 7–12 Вт, как я оценил по справочнику, а более 50 Вт разницы.**
Оценка была занижена в разы.

🔴 **И мой промежуточный вывод «отключение не помогло» был НЕВЕРЕН.** Замер 10:12
снимался после отключения телефона, но ДО переподключения адаптера: контроллер питания
не пересогласовал профиль и отдавал столько же, сколько при подключённой периферии.

> **Урок метода: изменение проверяется после того, как система его УВИДЕЛА.** Отключить
> потребитель и сразу мерить — то же, что мерить температуру, не вынув градусник.

Сон в 10:08:48 (`Low Power Sleep`, 11 %) — последствие: заряд упал до порога раньше,
чем питание восстановилось. Пробуждение 10:09:16 — `EC.ACAttach`.
