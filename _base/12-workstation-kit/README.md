# 12 — Кит рабочей станции

> **Заведён 02.09.2026** по заказу владельца: *«давай обустроим инфраструктуру
> разработческую на моём маке… старое удалим, скачаем актуальное — стандарт рынка,
> рабочее, стабильное»* и *«скилл, который будет считывать состояние сети, нагрузки,
> температуры… какое состояние железа, где проблема может быть»*.

## Как это называется на рынке

Дисциплины с единым именем нет — та же история, что с `principles-audit.md` H3.
Ближайшие устоявшиеся термины, каждый покрывает часть:

| Термин | Что покрывает | Наш случай |
|---|---|---|
| **Developer Experience (DevEx)** | среда, в которой работает разработчик | ✅ ядро кита |
| **Workstation setup / provisioning** | приведение машины к воспроизводимому состоянию | ✅ §2 |
| **Dotfiles management** | конфиги оболочки под версионированием | ⚠️ частично: бэкапы есть, git нет |
| **Toolchain management** | версии языков и пакетов | ✅ §2, `uv` |
| **Системное администрирование** | серверы, сети, пользователи | ⛔ не про нас: одна машина, один человек |

**Честное самоописание: «управление рабочей станцией одного разработчика».**
Не сисадминство — здесь нет ни парка машин, ни пользователей, ни SLA.

---

## §1. Что в ките

| Файл | Что делает |
|---|---|
| [`bin/machine_probe.py`](bin/machine_probe.py) | снимок Mac: железо, нагрузка, память, диск, батарея, сеть, износ компонентов, бэкап + пороги |
| [`bin/inventory.py`](bin/inventory.py) | индекс приложений и крупных каталогов **с историей** — что появилось, исчезло, сменило версию |
| [`bin/disk_audit.py`](bin/disk_audit.py) | карта всего диска: профили, домашние папки, `/Users/Shared` — размер и когда трогали. 🔴 **Ничего не удаляет и не переносит** |
| [`bin/hidden_load.py`](bin/hidden_load.py) | нагрузка, которой не видно в топе процессов: фоновые службы, доля от аптайма вместо мгновенного `%CPU` |
| [`bin/firmware_probe.py`](bin/firmware_probe.py) | прошивки и версии железа — то, что не меняется само и потому выпадает из регулярных замеров |
| [`bin/rename_readiness.py`](bin/rename_readiness.py) | шесть проверок готовности к смене имени учётной записи, у каждой непройденной — строка «что сделать» |
| [`bin/toolchain_check.py`](bin/toolchain_check.py) | сравнение инструментария с эталоном: чего не хватает, что устарело, что лишнее |
| [`bin/net_sampler.sh`](bin/net_sampler.sh) | 🆕 пассивный ряд трафика по секундам во время игры или прогона: только счётчики ядра, **ноль сетевых проб** |
| [`bin/tunnel_sampler.sh`](bin/tunnel_sampler.sh) | 🆕 счётчики **туннеля и игры** во время матча: повторы, дубли, пакеты не в срок — то, чего `net_sampler.sh` не видит |
| [`bin/fps_probe.sh`](bin/fps_probe.sh) | 🆕 ряд **FPS и сетевого графика Dota** прямо с экрана — единственный способ увидеть то, чего нет снаружи процесса |
| [`bin/interplay_probe.sh`](bin/interplay_probe.sh) | 🆕 кадр Dota **и** состояние машины в один и тот же момент — единственный способ связать просадку с причиной |
| [`bin/analyze_interplay.py`](bin/analyze_interplay.py) | 🆕 разбор ряда: что с чем совпало — и 🔴 **сразу печатает, сколько за связью событий**, чтобы связь на трёх не выглядела как связь на трёхстах |
| [`bin/conn_sampler.sh`](bin/conn_sampler.sh) | 🆕 **тождество туннеля**: PID, возраст, соединения, адрес, шлюз, RTT — единственный ряд, по которому видно, что процесс УМЕР и родился заново |
| [`bin/analyze_conn.py`](bin/analyze_conn.py) | 🆕 разбор смертей туннеля: путь сменился или нет · соединения падали или держались · обе задержки — и 🔴 **все длительности сразу**, чтобы одна не выглядела законом |
| [`bin/freeze_watch.sh`](bin/freeze_watch.sh) | 🆕 **секундный** ряд: событие короче шага наблюдения невидимо по построению — ловит зависания на 2+ с |
| [`bin/analyze_freeze.py`](bin/analyze_freeze.py) | 🆕 разбор зависаний и 🔴 **ОТКАЗ отвечать**, если игра в окне не шла: «ноль провалов» при мёртвом трафике — не результат |
| [`bin/dns_probe.sh`](bin/dns_probe.sh) | 🆕 задержка резолвера — туннель резолвит имя сервера на каждое соединение, и заминка DNS 🔴 **не видна ни в пинге до шлюза, ни до сервера** |
| [`bin/diagnose_episode.py`](bin/diagnose_episode.py) | 🆕 🔴 **какой механизм стоял за зависанием**: смерть процесса туннеля или деградация пути — по секундному ряду они неразличимы, а лечатся по-разному |
| [`bin/collectors.sh`](bin/collectors.sh) | 🆕 живы ли сборщики и 🔴 **сколько им ОСТАЛОСЬ** — они умирают молча, и дыра в ряду обнаруживается только потом (`PIT-201`); `start` поднимает лежащие |
| [`bin/compare_windows.py`](bin/compare_windows.py) | 🆕 сравнение двух окон опыта — и 🔴 **три отказа сравнивать**: игра не шла всё окно · контраст по объёму меньше двух раз · одно сравнение не закон |
| [`bin/fps_compare.py`](bin/fps_compare.py) | 🆕 отличается ли FPS после смены настройки — 🔴 **прибавка меньше разброса это не прибавка**: десять кадров подряд без изменений дали размах в 14 |
| [`bin/stitch_png.py`](bin/stitch_png.py) | 🆕 склейка кадров в одну полосу без Pillow и ImageMagick (их на машине нет) |
| [`STATE.md`](STATE.md) | что стоит на машине сейчас — версии, пути, решения |
| [`reports/`](reports/) | отчёты о проведённых работах |
| [`observations/OPYT-vahta-i-provaly.md`](observations/OPYT-vahta-i-provaly.md) | 🆕 протокол опыта «мешает ли сама вахта игре»: два окна, 🔴 **две проверки годности до всякого сравнения** и прямо названное «чего этот опыт не решит» |
| [`reports/dota-zavisaniya-diagnoz-2026-09-05.md`](reports/dota-zavisaniya-diagnoz-2026-09-05.md) | 🔴 разбор **зависаний** (не рывков — это другая поломка): два односторонних разрыва обратного пути и одна смерть процесса туннеля, Wi-Fi чист четырьмя способами |
| [`reports/dota-rivki-diagnoz-2026-09-05.md`](reports/dota-rivki-diagnoz-2026-09-05.md) | 🔴 разбор рывков в Dota: потолок **35 кадров** упирается в Iris 655 на ретине, а не в интернет — с доказательством через связи (r) между FPS и сетевыми числами |
| `snapshots/` | снимки индекса по датам, для сравнения во времени |

### Три инструмента — три разных вопроса

| Вопрос | Чем отвечать |
|---|---|
| «что с машиной прямо сейчас» | `machine_probe.py` |
| «что вообще установлено и когда появилось» | `inventory.py` |
| «всё ли нужное стоит и не устарело ли» | `toolchain_check.py` |
| «кто занимает канал прямо сейчас» | `net_sampler.sh` + одна проба `nettop` |
| «почему рвётся игра, хотя канал свободен» | `tunnel_sampler.sh` — потери живут в счётчиках туннеля, не в объёме |
| «рывки — это сеть или машина» | `fps_probe.sh` — 🔴 сначала посмотреть на FPS, потом копать сеть |
| «что происходило, когда просело» | `interplay_probe.sh` — 🔴 два ряда, снятых порознь, связать нельзя |
| «с чем совпали потери» | `analyze_interplay.py` — 🔴 обращение по имени столбца, не по номеру |
| «почему рвётся связь раз в полчаса» | `conn_sampler.sh` — 🔴 туннель ПАДАЕТ; ряд по имени процесса этого не покажет (`PIT-199`) |
| «почему туннель умер именно тогда» | `analyze_conn.py` — три различающих признака, 🔴 **причину не называет** |
| «игра зависла на несколько секунд» | `freeze_watch.sh` + `analyze_freeze.py` — 🔴 шаг 15 с такого не увидит |

### 🔴 `net_sampler.sh` — почему он пассивный, а не «просто пинг раз в секунду»

Заведён 05.09.2026 по заказу владельца: *«следи активно за трафиком доты,
я сейчас играю»*. Два запрета в нём — не осторожность, а следствие двух
собственных разборов:

| Запрет | Откуда |
|---|---|
| **ни одной сетевой пробы** | отправка пачки скриншотов забила upstream и кончилась разрывом сессии, ↑88 → 632 (`reports/dota-and-claude-coexistence-2026-09-05.md` §2г). Измеритель, занимающий канал, измеряет себя |
| **ни одного обхода процессов** | `/machine --watch` уронил игру с 60 до 5 FPS: полный `ps` шестьдесят раз в минуту |

Читает только `netstat -ib` (готовые числа ядра) и `sysctl vm.loadavg`.
Кто именно занимает канал — **одна** проба `nettop -l 2`, а не цикл.

**Первый прогон нашёл виновника за десять минут:** `steam_osx` слал вверх
**26 кБ/с** против 11 у `dota2`, накопительно **181 МБ против 15**.
Всплески по каналу — 250–304 кБ/с при потолке раздачи 125–250.

🔴 **Ни один из трёх ничего не меняет.** Они читают и печатают команды,
которые выполняет владелец. Обновление тулчейна и удаление программ —
необратимые действия, и решает их человек (`/auto` §5).

```bash
python3 12-workstation-kit/bin/machine_probe.py          # срез, зелёное молчит
python3 12-workstation-kit/bin/machine_probe.py --json    # то же машиночитаемо
python3 12-workstation-kit/bin/machine_probe.py --net      # + замер скорости (~15 с)
```

🔴 **Скрипт только читает.** Ни одной команды, меняющей состояние машины, в нём нет —
иначе замер начал бы влиять на измеряемое (тот же инвариант, что у `sync_base_local.py`).

### Что он честно НЕ может

- **температура CPU** — на Intel читается только через `sudo powermetrics`.
  Без пароля отдаётся температура **батареи**, и она подписана именно так,
  а не выдаётся за процессорную;
- **износ SSD** — нужен `smartctl` (`brew install smartmontools`). Без него
  `diskutil` говорит лишь Verified/Failing, без счётчиков;
- **скорость сети** — только по флагу `--net`: замер занимает ~15 секунд,
  и молча тратить их на каждый запуск неправильно.

---

## §2. Правила, выведенные из наведения порядка 02.09.2026

### 🔴 Один инструмент на одну задачу

Было **девять** установок Python и **три** алиаса, указывающих в разные стороны.
Каждая появилась обоснованно: установщик python.org, brew, pyenv, Framework.
Проблема не в любой из них, а в том, что **ни одна не была назначена главной**.

> **Правило.** Версиями Python управляет `uv` — один инструмент вместо
> `pyenv` + `venv` + `pip` + алиасов. Ставить Python другим способом можно
> только записав в `STATE.md`, зачем.

### 🔴 Установщик дописывает в конец и не смотрит, что уже есть

`.zshrc` содержал `$HOME/.local/bin` дважды и Antigravity **четырежды**.
`.zprofile` — пять блоков PATH, и **три из пяти вели в удалённые каталоги**.
Это не небрежность владельца: так устроены сами установщики.

> **Правило.** Конфиг оболочки читается глазами раз в квартал, а не когда
> «что-то сломалось». Дописывая строку — проверь, нет ли её выше.

### 🔴 Сборка из исходников на Intel — это часы

`brew install uv` начал тянуть **весь Rust** и поднял load average до **103**
на четырёх ядрах. Официальный установщик Astral поставил готовый бинарник
за секунды.

> **Правило.** Инструмент, у которого есть официальный бинарник под платформу,
> ставится бинарником. `brew` хорош для библиотек, не для тулчейнов на Rust/Go.

### Порог «свободно меньше 10 %» — не эстетика

Диск был занят на **95 %** (11 ГБ из 233). macOS при этом ведёт себя странно
неочевидными способами; своп занимал 3.2 ГБ из 4 при 8 ГБ RAM.
После чистки — 60 ГБ свободно.

---

## §3. Связки

`00-infrastructure/95-recovery-and-backup.md` (что делать при отказе) ·
`00-infrastructure/94-secret-storage.md` (связка ключей) ·
`scripts/system_status.py` (состояние репозиториев — не машины) ·
[`STATE.md`](STATE.md) (текущее состояние машины)
