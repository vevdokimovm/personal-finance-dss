# Перепись файлов с признаками метода: 11 946 файлов по всем репам, из них
# 3 667 со следами (правила, разборы, реестры, ретроспективы). Пересобирается
# целиком при каждом запуске `scan_lessons.py` — это СНИМОК состояния реп,
# а не журнал, поэтому и не входит в отпечаток канона.
#
# 🔴 Найдена необъявленной 04.09.2026, и только потому, что путь стал
# абсолютным. Пока умолчание было `Path("scan.csv")` — относительно текущего
# каталога, — проверка не могла привязать запись ни к какому месту и молчала.
# Тихий адрес прятал не только файл, но и сам факт записи.
scan.csv

# 🔴 Счётчик движения очереди (`PIT-195`). Журнал СОБЫТИЙ: по строке на прогон,
# дописывается, задним числом не правится (`94` §1). Заведён после восьми дней
# простоя очереди, который был невидим — никто не считал, сколько кандидатов
# разобрано за период.
census-progress.tsv
