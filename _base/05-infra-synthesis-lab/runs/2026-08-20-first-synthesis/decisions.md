# Решения по кандидатам — прогон 2026-08-20

> Одна секция на разобранный артефакт. Без записанного решения следующий прогон предложит то же
> самое. Шаблон — `../../templates/candidate-decision.md`.

---

## `reports/pitfalls.md` — реестр граблей ✅ СВЕДЁН

**Что это:** реестр повторяющихся ошибок воркфлоу. Самый лёгкий тип разбора рядом с инцидентами
и расследованиями.

### Вход

| Источник | Записей | Диапазон | Отношение к канону |
|---|---|---|---|
| **канон** `base-repo` | 15 | PIT-001…015 | эталон |
| `character-a-analysis` | 30 | PIT-001…030 | 12 совпали, 3 конфликт ID, 15 своих |
| `self-map` | 16 | PIT-001…016 | 12 совпали, 3 конфликт ID, 1 своя |
| `personal-finance-dss` | 17 | PIT-001…017 | **0 совпадений**, 15 конфликтов ID |
| `portrait-of-taste` | 16 | portrait-of-taste:PIT-101…116 | 0 конфликтов — намеренный namespace |
| `master-admission` | 2 | PIT-001…002 | 2 конфликта ID |
| legacy-копия в 9 репах | 12 | PIT-001…012 | старая копия базы |
| **Всего** | **110** | | |

### Природа расхождения

Не дрейф одного файла, а **пять независимых линий нумерации от общей точки расхождения**.
`PIT-005` означал разное: «комментарий в zsh уходит аргументом» (канон, `character-a-analysis`,
`self-map`) против «уже сделано в hand-off ≠ доехало» (`personal-finance-dss`).
`portrait-of-taste` единственный решил проблему заранее, уйдя в диапазон 101+.

Сравнение по хешу здесь бесполезно: скрипт видит 110 разных файлов и не может отличить
«два документа про одно» от «два документа про разное». Это ровно та работа, ради которой
вводился Claude Code (ADR-010).

### Решение: ⬆️ повысить, сведя в единый сквозной реестр

**Форма переноса:** сведён по смыслу. 110 входных записей → **62 уникальных урока**.

**Правила сведения:**

1. **PIT-001…015 сохранены дословно** — на них ссылаются `00-CLAUDE-STOP.md`, `WATCHLOG §4`,
   методички. Менять номера значило бы ломать живые ссылки.
2. **Всё уникальное дописано с PIT-016** сквозной нумерацией.
3. **Одинаковые по смыслу записи из разных реп слиты в одну**, с указанием обоих источников.
4. **Разные уроки не схлопнуты** (`22-merge-protocol.md`): где две репы вынесли из похожей
   ситуации разные выводы — заведены обе записи с перекрёстной ссылкой.
5. **В каждой записи указан источник** — след не потерян.

### Что с чем слито

| Новый ID | Слито из | Обоснование |
|---|---|---|
| PIT-025 | `portrait-of-taste` portrait-of-taste:PIT-114 + `character-a-analysis` PIT-022 | одна корневая причина (UTF-8-флаг по локали), два проявления: не распаковывается / «Data is corrupted». Оба симптома сохранены |
| PIT-029 | `character-a-analysis` PIT-026 + PIT-027 | второй явно назван обобщением первого («не-`view` вариант»); правило В-1′ покрывает оба |
| PIT-031 | `character-a-analysis` PIT-029 + `personal-finance-dss` PIT-014 | оба про отрицательное утверждение по неполному обходу |
| PIT-032 | `character-a-analysis` PIT-029 + `portrait-of-taste` portrait-of-taste:PIT-104 | оба про «написал заново, потому что существующее не искали» |

### Что не взято и почему

| Не взято | Причина |
|---|---|
| `personal-finance-dss` PIT-001, PIT-008 | дословные дубли канонических PIT-002 и PIT-001 |
| `master-admission` PIT-001 | тот же класс, что канонический PIT-002 (`&&` рвёт цепочку) |
| legacy-копия в 9 репах | подмножество канона, ничего нового |

### Что осталось локальным

Ничего. Все 62 урока — про работу с инструментами, окружением, git, доставкой и коммуникацией,
то есть применимы в любой репе. Даже те, что выглядят продуктовыми (`PIT-034` про in-memory SQLite,
`PIT-039` про скоуп гейта), формулируют переносимое правило: «среда исключается раньше кода»,
«гейт руками = гейт CI тем же скоупом».

### Итог

- `base-repo/reports/pitfalls.md`: **15 → 62 записи**, 26 КБ → 95 КБ
- Дублей ID: **0** (проверено)
- В шапку добавлен раздел «Единый реестр системы» с правилом нумерации и предупреждением
  о том, как расхождение возникло

### Что делать с копиями в репах при раздаче

Локальные `reports/pitfalls.md` в `character-a-analysis`, `self-map`, `personal-finance-dss`,
`portrait-of-taste`, `master-admission` **снимаются** после подтверждения, что все их записи
доехали в канон. Проверка — по содержимому, не по счётчику. Legacy-копии в 9 репах снимаются
вместе со всей плоской копией базы.

---

---

## `START-HERE.md` — вход в репу 📍 НЕ ДРЕЙФ, извлечён шаблон

**Что казалось:** базопоставляемый файл, разошедшийся в 28 репах (27 разных версий).

**Что оказалось:** **per-repo файл**, а не копия базы. У 23 реп он написан по одному
и тому же шаблону — диффы между ними показывают ровно две различающиеся строки: имя репы
в заголовке и описание в «Что это». Остальное совпадает намеренно, чтобы вахта не переучивалась.

Это второе срабатывание SYN-004 (per-repo принят за дрейф) в одном прогоне.

**Решение:** ⬆️ повысить **шаблон**, а не содержимое → `templates/START-HERE_TEMPLATE.md`.
Заполненные файлы остаются в репах. В шаблон вынесены и правила заполнения: класс обязан
совпадать с `.repo-class` и `repos-map.md`, для `temp` родитель указывается сразу.

---

## `TASKS.md` — задачи владельца ⬆️ ШАБЛОН ЗАВЕДЁН

**Вход:** файл есть в 27 репах, шаблона в базе **нет вовсе**. У 23 реп структура идентична
(553–565 байт, различается только имя репы), у четырёх разросся до рабочего состояния
(`dota-dossier` 9 КБ, `portrait-of-taste` 17 КБ, `mission-control` 21 КБ,
`character-a-analysis` 81 КБ).

**Решение:** ⬆️ `templates/TASKS_TEMPLATE.md`, синтезирован из практики. В комментарий-инструкцию
вынесено главное разделение из `45-roadmap-and-tasks.md`: ROADMAP — что построить в проекте
(может ассистент), TASKS — что сделать владельцу вне песочницы. Плюс правило поля
«Разблокирует», приём прошедшего времени, порядок закрытия через `TASKS_HISTORY.md` с датой.

Ссылка на оба шаблона добавлена в `45-roadmap-and-tasks.md` §5.

---

## `25` / `33` / `08` — разошедшиеся базовые доки ✅ ДЕЛЬТЫ НЕТ

Три документа значились разошедшимися в 13 репах каждый. Подозрение: у `08` копии в репах
**больше** канона на 60 байт — база могла потерять содержимое.

**Проверено построчным диффом:**

| Док | Что в копиях | Вердикт |
|---|---|---|
| `08-automation-triggers` | две строки про `publish.sh` и `deploy_from_zip.sh` вместо одной про `deploy.sh` | канон новее, копии ссылаются на отставленные скрипты |
| `25-versioning-and-releases` | вся глава §3 описывает `publish.sh` как действующий | то же |
| `33-token-budget-and-modes` | нет врезки-разграничения с `49-token-economy` | канон полнее |

**Решение:** 🗑 копии снимаются вместе с плоской копией базы. Ничего не переносится.
Разница в размере объяснялась тем, что старая версия перечисляла два скрипта вместо одного.

---

## Плоские копии базы в 12 репах ✅ ПРОВЕРЕНО НА ДЕЛЬТУ

Сверка каждого файла плоской копии против актуальной базы по sha1 и по имени:

| Класс | Файлов | Что это |
|---|---|---|
| дубль канона байт-в-байт | **1114** | снимать без разговоров |
| имя есть в каноне, содержимое старее | **108** | снимать, канон новее (проверено на `08`/`25`/`33`) |
| **имени в каноне нет** | **62** | разобрано ниже |

Из 62 «неизвестных»: 22 — строчные варианты имён (`watchlog.md`, `incidents-summary.md`,
`investigations-summary.md`), то есть та же проблема нейминга, что решается снятием копии;
17 — замороженные отчёты `character-a-analysis` и `self-map` (остаются в своих репах,
`21-revision-protocol.md` §1); 5 — репо-специфичные скрипты нарезки источников;
1 — контентный файл; **11 — методички, которых в базе не было.**

---

## 11 методичек `character-a-analysis` ⬆️ ПОВЫШЕНЫ (34–44 → 54–64)

Та же коллизия номеров, что у pitfalls: репа завела свои доки 34–44, в базе эти номера
заняты другими документами (`34-cowork-scheduled-tasks`, `35-mcp-connectors`,
`37-skills-system`, `42-release-description-standard`, `43-archive-naming-and-packaging`…).

| Было | Стало | Тема |
|---|---|---|
| 34 | **54** `pdf-reading-channels` | что происходит с PDF в трёх каналах доставки |
| 35 | **55** `pdf-channels-experiment` | замер трёх каналов на одном документе |
| 36 | **56** `chat-to-project-to-repo` | жизненный цикл: чат → проект → репа |
| 37 | **57** `self-sufficiency-rule` | архив содержит ВСЁ (переименован с транслита) |
| 38 | **58** `git-practice` | git: модель важнее списка команд (переименован) |
| 39 | **59** `public-mirror-filter` | приватный канон → публичное зеркало |
| 40 | **60** `audio-video-pipeline` | медиа → текст и кадры |
| 41 | **61** `token-analytics` | сколько контекста съедено |
| 42 | **62** `vim` | минимум для работы в чужой машине |
| 43 | **63** `ai-tools-landscape` | карта ИИ-инструментов под задачу |
| 44 | **64** `claude-code-sandbox` | локальная песочница |

**Сделано при переносе:**
- номер в заголовке приведён к новому; у `54`/`55` номер добавлен (в оригинале его не было);
- пять перекрёстных ссылок между этими доками перенумерованы;
- два имени переведены с транслита на английский по конвенции `01-repo-standard.md` §1;
- в каждый добавлена врезка «Происхождение» — откуда и почему сменился номер;
- все 12 зарегистрированы в карте `START-HERE.md`.

**Проверка целостности:** каждый файл вырос ровно на 509 байт (размер врезки), у `56` — на 499
из-за укоротившейся ссылки. Содержимое не потеряно.

---

---

## `self-map` — 8 «уникальных» файлов ✅ НОВОГО НЕТ

Сверка по sha1 против `character-a-analysis`: **все восемь совпадают байт-в-байт**. Копии одного
источника, а не независимые наработки. Ничего не переносится; снимаются вместе с плоской копией.

---

## Три системных файла ✅ РАЗОБРАНЫ (20.08.2026)

Найдены в `self-map` и `character-a-analysis` — все копии идентичны по sha256 (4 / 2 / 3 штуки
соответственно). По природе — не контент реп, а системные артефакты. **Разбор закончен: два
повышены, один в корзину.**

| Файл | Решение | Основание |
|---|---|---|
| `reports/investigations/vision-channel_investigation.md` | ⬆️ **повышен** → `reports/investigations/vision-channel_investigation.md` | Первое расследование в базе вообще. Предмет — **среда исполнения** (бюджет изображений на разговор), касается любой работы с визуальными источниками в любой репе |
| `reports/incidents/release-notes-incident.md` | ⬆️ **повышен** → `reports/incidents/release_notes_three_defects_incident.md` | **Не дубль.** Сверен с `publish_stub_release_notes_incident.md`: там один дефект (CHANGELOG не найден по единственному пути), здесь четыре других. Общее — заглушка `Release {version}`, то есть **рецидив `fail-silent` через девять дней** |
| `templates/fix_release_notes.sh` | 🗑 **в корзину** | Покрыт `REPAIR=1`, и не просто покрыт — **противоречит действующему стандарту** |

### Починка ссылок при повышении

Оба документа **заморожены** (`21-revision-protocol.md` §1): текст по смыслу не правился, изменены
только ссылки на переехавшие файлы, и это отмечено врезкой «Происхождение» в самих документах.

| Документ | Было | Стало | Почему обязательно |
|---|---|---|---|
| INV-VISION | `PIT-016` | `PIT-019` | реестр сведён из пяти линий нумерации (v1.53.0) |
| INV-VISION | `34-pdf-reading-channels.md` | `54-pdf-reading-channels.md` | **номер 34 в базе занят** `34-cowork-scheduled-tasks.md` — ссылка вела бы в другой документ |
| INV-VISION | `33-token-budget-and-modes.md` | без изменений | номер в базе тот же |
| INC-RELEASE-NOTES | `PIT-013, PIT-014` | `PIT-016, PIT-017` | обе записи от 18.07.2026 — из этого самого разбора |
| INC-RELEASE-NOTES | `../incidents-summary.md` | `../incidents_summary.md` | имя реестра в базе через подчёркивание |

Строки-сводки добавлены в `investigations_summary.md` и `incidents_summary.md`.

### Почему `fix_release_notes.sh` — корзина, а не «оставить на всякий случай»

Три независимых основания, каждого хватило бы:

1. **Функционально покрыт.** `REPAIR=1` в `deploy.sh` делает ровно то же — проходит по существующим
   тегам и релизам, ставит заголовок по стандарту, тянет описание из CHANGELOG, дозаливает
   недостающий ассет. Плюс умеет то, чего утилита не умеет вовсе (`git archive` из тега).
2. **Противоречит действующему стандарту.** Скрипт ставит `title = "vX.Y.Z"` — только версия.
   Действующий канон (`deploy-SPEC.md` §3 п.3, `42-release-description-standard.md`) —
   `<repo> vX.Y.Z — <тезис>`. Запуск этой утилиты сегодня **испортил бы** заголовки релизов,
   приведя их к отменённой конвенции. Это не мёртвый код, это активно вредный.
3. **Нет защиты замороженного.** Утилита перезаписывает описание безусловно («повторный запуск
   просто перезапишет»). У деплойера это инвариант И6: осмысленное описание не трогается
   без `FORCE=1`, тег и релиз заморожены (`21-revision-protocol.md`).

Сверх того — правило «второго скрипта не заводится» (`templates/README.md`). Сам `deploy.sh`
в шапке называет `fix_releases.sh` рецидивом, который уже сливали обратно режимом.

**Копии в репах не трогаются** — удаление произойдёт в фазе 3 (раздача), см. §«Что делать
с копиями в репах при раздаче».

---

## Ожидают разбора

- [ ] `START-HERE.md` — разошёлся в 28 репах
- [ ] `33-token-budget-and-modes.md`, `25-versioning-and-releases.md`, `08-automation-triggers.md` — по 13
- [ ] `incidents_summary.md`, `investigations_summary.md` — реестры, та же природа что у pitfalls
- [ ] `publish.sh` — отставлен, лежит в 11 репах
- [ ] 119 методичек-кандидатов из шести реп (`PLAN.md` §2.7)

---

# ПРОХОД ЗАНОВО ПО `PROCEDURE.md` (20.08.2026)

Прежние описи аннулированы — см. `PLAN.md` §2.7. Ниже: по каждой репе листинг с диска,
решение по каждому каталогу с причиной, сверка по sha256, приёмка арифметикой.

---

## Репа 1: `personal-finance-dss` (FINPILOT) — 1501 файл

### Листинг (вывод команд `PROCEDURE.md` §1, не пересказ)

**1.1 Всего:** `1501`

**1.3 Скрытые каталоги** — то, чего не видно и что не вспоминается:

```
  30  ./.claude          ← 🔴 инфраструктура Claude Code
   1  ./.github
   1  ./tools/publish/mirror_extras/.github
```

**1.4 Расширения:** `.md` 581 · `.py` 409 · `.tsx` 87 · `.ts` 86 · `.gz` 57 · `.json` 55 ·
`.png` 36 · `.css` 34 · `.drawio` 30 · `.html` 22 · `.sh` 20 · `.csv` 17 · `.pdf` 12 ·
`.txt` 6 · без расширения 5 · `.yml` 4 · `.js` 4 · `.timer` 3

### Решение по каталогам (построчно, с причиной)

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `.claude/` | 30 | **М** | 🔴 скиллы с evals, субагенты, хуки, `settings.json` — инфраструктура инструмента |
| `knowledge/guides/` | 29 | **М** | методички: `claude_infrastructure_methodology`, `architecture_guide`, `dev_methodologies_principles` и др. |
| `docs/` (корень) | 49 | **М** | стандарты и протоколы проекта |
| `docs/reports/incidents/` | 17 | **М** | пост-мортемы по разработке |
| `docs/reports/investigations/` | 13 | **М** | расследования |
| `docs/reports/adr/` | 18 | **М** | архитектурные решения |
| `docs/reports/retrospectives/` | 2 | **М** | ретроспективы процесса |
| `docs/claude_token_lab/` | 4 | **М** | протокол замеров токенов |
| `docs/model/expert_certification/` | 13 | **М** | `blindness_protocol`, `iteration_protocol`, `ITERATION_RUNBOOK` — метод слепого ревью |
| корень репы | 28 | **М** | `CLAUDE.md`, `README`, `CHANGELOG`, `VERSION` |
| `tools/revision/`, `tools/publish/` | 7 | **М** | инструменты процесса |
| `app/`, `frontend/`, `alembic/versions/` | ~250 | **С** | код продукта |
| `tests/` | 118 | **С** | тесты продукта |
| `knowledge/roles/` | ~85 | **С** | экземпляры базового `03-role-kit`; шаблоны уже в базе. **Но:** 16 заполненных ролей против одного `EXAMPLE_lawyer.md` в базе — кандидат в примеры |
| `knowledge/model_validation/` | 35 | **С** | данные (`.jsonl.gz`, `.csv.gz`) |
| `knowledge/business/`, `knowledge/science/`, `knowledge/survey_auditory/` | ~60 | **С** | предметные материалы проекта |
| `docs/diagrams/` | 34 | **С** | диаграммы продукта |
| `docs/reports/testing/` | 29 | **С** | результаты замеров конкретной модели |
| `docs/model/expert_certification/iterations/` | ~180 | **С** | данные пяти итераций × четыре аккаунта |
| `docs/legal/`, `docs/research/`, `docs/ui_directions/` | ~30 | **С** | предметное |
| `deploy/`, `nginx/`, `loadtest/`, `scripts/` | ~20 | **С** | эксплуатация продукта |

### Сверка по sha256

```
ВСЕГО 1501 = совпало с базой 47 + дельта по имени 128 + нет в базе 1326
```

**Дельты, требующие сверки содержимого** (имя то же, содержимое разошлось) — 128, из них
значимые: `docs/documentation_methodology.md`, `docs/engineering_practices.md`,
`docs/naming_convention.md`, `docs/pitfalls.md`, `docs/sandbox_runbook.md`,
`docs/tool_call_channel_failures.md`, `docs/reports/*_guide.md`, `tools/revision/revision_check.py`,
`deploy/publish/deploy.sh`. Остальные 100+ — `README.md` подкаталогов и `knowledge/roles/*`
(экземпляры шаблона, дельта ожидаема и не является дрейфом).

### Прочитано и решено

| Файл | Решение | Что сделано |
|---|---|---|
| `.claude/skills/README.md` | ⬆️ | живые замеры: активация скилла 55 % → 100 % с форсирующим хуком; разрыв в **описании** (WHAT вместо WHEN); под Claude 5 длинные инструкции вредят; скиллы **не наследуются** субагентами |
| `.claude/settings.json` | ⬆️ | рабочая архитектура хуков по всем событиям: PreToolUse / PostToolUse / Stop / SessionStart |
| `.claude/hooks/*.sh` (6 прочитано) | ⬆️ | блокирует только `exit 2` · `stop_hook_active` guard обязателен · таймаут хука 10 мин → полная суита не влезает, нужен быстрый срез · bash 3.2 на macOS не знает `mapfile` · `set -u` + пустой массив падает |
| `docs/reports/retrospectives/statement_parser_process.md` | ⬆️ | сверка с контрольными итогами источника; несколько независимых свидетелей; ground truth до фикса |

**Не дочитано:** `knowledge/guides/` (29), `docs/claude_token_lab/` (4),
`docs/model/expert_certification/` протоколы (13), `docs/reports/incidents/` (17),
`docs/reports/investigations/` (13), `docs/reports/adr/` (18), остальные `.claude/` (24).

### Приёмка

🔶 **Репа НЕ закрыта.** `1501 = 47 (база) + ~950 (С) + 4 (прочитано) + ~500 (очередь)`.

### `knowledge/guides/` — 29 методичек, разобрано

Сверка каждой против `02-methodology-library/` по содержимому:

| Класс | Сколько | Что делать |
|---|---|---|
| идентичны базе байт-в-байт | 17 | ничего, снимутся с копией базы |
| дельта по содержимому | 1 (`cybersecurity_methodology.md`) | сверить |
| **в базе нет вовсе** | **11** | разбор ниже |

**Одиннадцать отсутствующих:**

| Файл | Строк | Решение |
|---|---|---|
| `claude_infrastructure_methodology.md` | 415 | ⬆️ **ПОВЫШЕНА** → `00-infrastructure/66-model-and-effort-selection.md` |
| `financial_recommendation_benchmarks.md` | 482 | 📍 локальная — предметные бенчмарки продукта |
| `writing_guides_methodology.md` | 300 | 🔶 очередь: как писать методички; сверить с `18-documentation-philosophy.md` |
| `overfitting_data_snooping_guide.md` | 153 | 🔶 очередь: переобучение и подглядывание в данные — метод, аналога в базе нет |
| `engineering_reasoning_guide.md` | 131 | 🔶 очередь: инженерное рассуждение |
| `counterfactual_analysis_guide.md` | 112 | 🔶 очередь: контрфактический анализ |
| `knowledge_capture_protocol.md` | 186 | 🔶 сверить с базовым `20-knowledge-capture-protocol.md` (183) — размеры близки, вероятно одно и то же |
| `volume_compression_methodology.md` | 224 | 🔴 **база короче в 2.6 раза** (`06-volume-compression.md`, 85) — сверить, что база потеряла |
| `fork_merge_methodology.md` | 362 | 🔴 **база короче в 3.4 раза** (`22-merge-protocol.md`, 106) — сверить |
| `roadmap_methodology.md` | 253 | 🔴 **база короче в 2.4 раза** (`45-roadmap-and-tasks.md`, 104) — сверить |
| `repo_revision_methodology.md` | 91 | база **длиннее** (`21-revision-protocol.md`, 182) — вероятно база полнее, проверить |

**🔴 Отдельная находка.** Три базовых документа значительно короче своих аналогов в FINPILOT.
Это не обязательно потеря — база могла намеренно сокращать, — но проверить обязательно:
если материал в базе отсутствует, он теряется у всех 57 реп разом.

### `.claude/skills/` + `settings.json` + `hooks/` — прочитано, решение ⬆️

Материал уникальный и в базе отсутствует полностью. Ключевое:

- **Замер активации скилла:** baseline **55 %**, с форсирующим хуком **100 %**. Разрыв
  не в поведении модели, а в **описании**: оно объясняет WHAT вместо WHEN. Скилл
  не триггерится — переписывать `description`, а не тело.
- **Негативные кейсы в evals обязательны:** скилл, срабатывающий на всё, ест контекст и мешает.
- **Скиллы не наследуются субагентами** — объявлять в `skills:` явно.
- **Под Claude 5 длинные инструкции с верификацией и эмфазой вредят.**
- **Архитектура хуков по событиям:** PreToolUse (секреты, деструктив) · PostToolUse (линт,
  токены дизайна, аудит) · Stop (гейт тестов, гейт чекпоинта) · SessionStart (контекст сессии).
- **Механика хуков, добытая практикой:** блокирует только `exit 2` · `stop_hook_active` guard
  обязателен, иначе бесконечный цикл · таймаут хука 10 мин → полная суита не влезает, нужен
  быстрый срез по изменённым файлам · bash 3.2 на macOS не знает `mapfile` · `set -u`
  с пустым массивом падает.

**🔶 Не написано в базу** — очередь следующего шага: отдельная методичка про скиллы и хуки.

### Итог по трём «база короче» — проверено, база действительно теряла

| Файл FINPILOT | Базовый аналог | Вердикт | Что сделано |
|---|---|---|---|
| `fork_merge_methodology.md` (362) | `22-merge-protocol.md` (106) | 🔴 **база не покрывала три темы** | ⬆️ дописаны §4а (сведение журналов), §4б (арифметика мёржа + дифференциальный тест), §4в (версионирование при слиянии). Базовый вырос 106 → 177 |
| `volume_compression_methodology.md` (224) | `06-volume-compression.md` (85) | 🔀 **разные темы под похожими именами** | ⬆️ заведён `67-information-compression.md`. База — про физический вес репы, эта — про сжатие смысла. В обоих поставлена перекрёстная оговорка «не путать» |
| `roadmap_methodology.md` (253) | `45-roadmap-and-tasks.md` (104) | 🔴 **база не покрывала три темы** | ⬆️ дописаны §4а (гранулярность: резко вблизи, размыто вдали), §4б (пять критериев пункта + приоритизация), §4в (ведение + семь граблей) |

**Общий вывод по классу.** Гипотеза «база короче, потому что намеренно сокращали» **не подтвердилась
ни разу из трёх**. Во всех случаях материал в базе отсутствовал, а не был сжат. Значит его не было
у всех 57 реп сразу — ровно тот тип потери, ради которого затевалась кампания.

**Приём, который это вскрыл:** сравнение **структуры** (`grep '^## '`), а не размера. Размер
показывает, что документы разные; структура — **чем именно** и есть ли пересечение вообще.
Дважды из трёх оказалось, что тема просто отсутствует; один раз — что документы про разное.

### Повышено в базу на этом шаге

| Куда | Что |
|---|---|
| `66-model-and-effort-selection.md` | 🆕 рамка «две ручки»: модель = кто думает, усилие = сколько; усилие привязано к **цене ошибки**, а не к размеру текста; дерево выбора; семь правил экономии; почему каталог моделей намеренно не включён |
| `67-information-compression.md` | 🆕 носитель против смысла; четыре вида избыточности; матрица приёмов с обратимостью; **семь красных линий**; тест «восстановится ли смысл из оставшегося» |
| `22-merge-protocol.md` §4а–4в | сведение журналов, арифметика мёржа, дифференциальный тест на ложную тревогу, версионирование |
| `45-roadmap-and-tasks.md` §4а–4в | гранулярность, критерии пункта, ведение, семь граблей |

**Остаток очереди `knowledge/guides/`:** `writing_guides_methodology.md` (300),
`overfitting_data_snooping_guide.md` (153), `engineering_reasoning_guide.md` (131),
`counterfactual_analysis_guide.md` (112), `knowledge_capture_protocol.md` (186, сверить),
`repo_revision_methodology.md` (91, база длиннее), `cybersecurity_methodology.md` (дельта).

### `writing_guides_methodology.md` (300) ⬆️ ПОВЫШЕНА → `68-writing-guides.md`

Аналога в базе не было: `18-documentation-philosophy.md` отвечает «что и зачем документировать»,
а «как написать сам текст» не покрывал никто.

Взято: четыре жанра (справочник / руководство / инструкция / объяснение) с правилом **один
документ — один ведущий жанр**; четыре вопроса до первой строки (включая «что НЕ входит» —
границы важны наравне с содержанием); десять ошибок автора; **три готовых скелета** под
справочник, каталог-с-выбором и процедуру.

**Сходимость с уже записанным:** «цифры без порогов» здесь и красная линия №2
в `67-information-compression.md` — один и тот же принцип с двух сторон (при написании
и при сжатии). Перекрёстная ссылка проставлена.

### `.claude/` целиком — прочитано, ⬆️ ПОВЫШЕНО

30 файлов: 6 скиллов с `evals.json`, 4 субагента, 11 хуков, `settings.json`.
**Сверка с базой: хуков Claude Code в базе не было вовсе** — `32-git-hooks-and-secret-scanning.md`
про **git**-хуки, это другой механизм. Субагенты покрыты только со стороны экономики
токенов (`49` §3), «как писать» отсутствовало.

| Куда | Что взято |
|---|---|
| `37-skills-system.md` §4а–4б | описание = **WHEN, а не WHAT** (замер: активация 55 % → 100 % с форсирующим хуком); тело держать коротким — под Claude 5 длинные инструкции вредят; скиллы **не наследуются** субагентами; **эвалы с обязательными негативами**, три прогона на кейс |
| `69-agents-hooks-and-gates.md` 🆕 | три механизма и правило выбора; архитектура хуков по событиям; шесть пунктов механики, добытых практикой; как писать субагента; паттерн «независимый эксперт» |

**Главный принцип, ради которого документ и заведён:** *запрет живёт в хуке, знание — в скилле,
исследование — в субагенте*. Текстовый запрет агент обходит рассуждением о частном случае,
хук не рассуждает. Это тот же урок У-03, что прошёл через всю сессию четырежды.

**Механика хуков, которой нет в документации** (добыта на граблях): блокирует только `exit 2`,
`exit 1` не блокирует · `stop_hook_active` guard обязателен, иначе цикл · таймаут хука ~10 мин,
полная суита не влезает и хук **молча пропускает всё** (fail-open) · `timeout(1)` на голом macOS
нет · bash 3.2 не знает `mapfile`, `set -u` с пустым массивом падает · SessionStart пишет
в stdout, и это становится контекстом модели.

**Паттерн «независимый эксперт»** — ищет не баги, а **разрыв между записанным и реальным**.
Шесть классов разрыва. Живые находки: гейт согласия работал на бэкенде 26 дней и 15 версий,
а UI для него не существовал; блокирующий гейт CI был красным 41 день на каждом пуше.
Ключевое разделение: **генерация гипотез и их проверка — разные проходы**, плюс явное
разрешение ничего не найти (без него агент всегда что-нибудь «найдёт»).

### `docs/reports/incidents/` (17) — разобрано частично, два класса подняты

| Инцидент | Решение |
|---|---|
| `ci_blocking_gate_red_41_days.md` | ⬆️ **PIT-075** (красный гейт стал фоном) + **PIT-076** (регистронезависимая ФС) |
| `consent_gate_no_ui_dead_end_incident.md` | ⬆️ **PIT-077** (каждый шаг покрыт, путь целиком не проходил никто) |
| остальные 14 | 🔶 очередь |

**PIT-075 — самое ценное здесь.** Блокирующий гейт был красным 41 день, 65 версий выпущено
без единого зелёного прогона. Три механизма, каждый переносим:
- **в системе не было точки, которая после пуша сверяется с реальным статусом** — локальная
  проверка и CI считались эквивалентными без механизма подтверждения;
- **alert fatigue:** неполный фикс не заметили, потому что **фон уже был красным**. Новая красная
  строка неотличима от старой. Длительно красный гейт **хуже отсутствующего**;
- **ложное зелёное:** записи `success` в списке — срабатывания расписания, запускавшего другую
  джобу. Читать цвет **вместе с именем джобы**.

**PIT-076** — семейство «работает у меня» пополнено: macOS APFS регистронезависима, и весь
класс багов структурно невидим на единственной платформе разработки. Плюс рецидив: фикс,
проверенный только там, где баг невидим, фиксом не является.

### `docs/claude_token_lab/` (4) ⬆️ → `61-token-analytics.md` §5

Базовый документ знал `/context` и правила бюджета, но не знал **как мерить**. Дописано:
шесть условий сопоставимости (чистая сессия · ≥3 прогона · один параметр за раз · фиксация
окружения · **медиана, а не среднее** · сырое сохраняется до выводов), таблица «что имеет
смысл мерить», переменные телеметрии, `ccusage`. Плюс правило: **чужие замеры задают порядок
величины, свои — принимают решение**.

### `docs/model/expert_certification/blindness_protocol.md` ⬆️ → `70-independent-review-blindness.md`

Аналога в базе не было, при том что система **регулярно** зовёт другой аккаунт как независимого
эксперта. Взято целиком: два вида утечки (**утечка постановки опаснее** — гарантированно
прочитана, исходит от координатора, замаскирована под вопрос) · изоляция как физическое
отсутствие, а не просьба · программная приёмка брифа на константы · обязательное самораскрытие
с **проверяемыми** аргументами · **проверка на эхо** (согласие с решением против согласия
с панелью) · раунд не аннулируется автоматически.

**Сходимость, отмеченная отдельно:** «не попросили не читать, а нечего читать» — тот же принцип,
что ADR-010 (физическое отсутствие клона) и `69` (запрет в хуке, а не в тексте). Три независимых
источника пришли к одному: **правило на добросовестности — это пожелание, а не защита**.

### Три оставшиеся методички `knowledge/guides/` — разобраны

| Файл | Решение |
|---|---|
| `overfitting_data_snooping_guide.md` (153) | ⬆️ → `02-methodology-library/overfitting_and_self_deception.md`; прикладной кейс обобщён, числа сохранены. Перекрёстная ссылка добавлена в `28` §11 (типичные ошибки) |
| `engineering_reasoning_guide.md` (131) | 🔗 **жанр зарегистрирован** в `reports/report_types.md` §5; развёрнутая методичка с шаблоном остаётся в репе |
| `counterfactual_analysis_guide.md` (112) | 🔗 **жанр зарегистрирован** в `reports/report_types.md` §6 |

**Почему два последних не повышены целиком.** Это методички **жанра отчёта**, а у базы уже есть
диспетчер жанров (`19-reporting-system.md` + `reports/report_types.md`). Заводить рядом две
полные методички значило бы задвоить роутер. Взято главное — триггеры, отличие от соседних
жанров, обязательные элементы; шаблоны остаются в исходной репе.

**Про подгонку — почему это важно для самой базы.** Документ прямо про то, как система меряет
себя: `reports/experiments/token-consumption/` калибрует коэффициенты по собственным замерам.
Правила §5 (предметное обоснование прежде метрики · held-out · **фиксация всех проверенных
вариантов**, а не только удачного · бюджет решений на выборку) применимы к `hypothesis.md`
буквально.

**Сходимость с уже записанным:** «критерий успеха записан ДО эксперимента» здесь и «вопрос
сверки обязан быть опровержимым» (PIT-070) — один принцип: **проверка, сформулированная после
взгляда на результат, ничего не проверяет**.

### `docs/reports/investigations/` (13) — разобрано частично

| Расследование | Решение |
|---|---|
| `cjk_token_slip_investigation.md` | ⬆️ **повышено** + канарейка внедрена в `scripts/revision_check.py` + **PIT-079** |
| `machine_load_gate_timeout_investigation.md` | ⬆️ **PIT-078** (таймаут гейта меряет часы, часы зависят от загрузки) |
| остальные 11 | 🔶 очередь |

**🔴 Канарейка сработала при внедрении — два попадания в самой базе:**

1. `02-methodology-library/cybersecurity_methodology.md:108` — «Авториз**U+6040**ция».
   **Тот же дефект уже находили и чинили в FINPILOT**, а в базе он остался: копия файла
   разошлась, и починка не доехала обратно. Ровно тот класс потери, ради которого идёт кампания.
2. `05-infra-synthesis-lab/tools/scan_lessons.py:2` — **U+5224 в докстринге, написанном этой же
   вахтой сегодня**. Живая демонстрация слоя B: слип семантический, при вычитке не виден.

Оба исправлены. Гейт зелёный, отрицательный тест пройден: подсаженный U+957F ловится
с точным адресом `probe.md:1`.

**Что взято сверх самой проверки:** принцип **анти-самореференции** (репозиторий не содержит
того, что запрещает — числа вместо символов, `chr()` в образце, U+-нотация в текстах)
и **самопроверка рядом с проверкой** — синтетический образец гоняется всегда, даже на чистом
дереве. Без неё зелёный гейт не отличить от сломанного (PIT-079).

### `docs/reports/adr/` (18) — разобрано

**Решение по каталогу: 16 из 18 — С (содержание).** Это решения о продукте: стек фронтенда,
хранение времени, контейнеризация, параметры матмодели, налоговые режимы. Они принадлежат
своей репе; база уже даёт шаблон и дисциплину ADR (`reports/adr/adr-template.md`, `19` §4).

**Один поднят:**

| ADR | Решение |
|---|---|
| `adr_010_parser_metrology_over_certification.md` | ⬆️ → `70-independent-review-blindness.md` §7а |

**Что взято и почему это важно.** ADR отвечает на вопрос, которого в базе не было вовсе:
**когда экспертная панель нужна, а когда она — дорогой способ подтвердить арифметику.**
Правило одно: *существует ли у вопроса объективно верный ответ*.

- Нет верного ответа (профессиональное суждение) → панель, консенсус, **потолок = коридор
  разногласий экспертов**; подгонка выше него — копирование вкусов панели, а не правота.
- Есть верный ответ (факт в источнике, инвариант) → **метрология: оракулы**; потолок 100 %,
  и «подгонка под правильный ответ» здесь не нарушение, а **определение починки**.

Плюс понятие **оракула** — соотношения, которое обязано выполняться. Три свойства делают
его сильнее панели: не требует размеченного эталона · работает на любом входе, включая живые
данные после запуска, где панели уже не будет · указывает на дефект **точно**, а не оценкой.

**Сходимость с уже поднятым:** «потолок = коридор разногласий» здесь и §4 в
`02-methodology-library/overfitting_and_self_deception.md` («прирост в зоне разногласий
экспертов = копирование вкусов») — один и тот же принцип, пришедший из двух разных документов.

---

## Репа 2: `it-base` — 3197 файлов

### Листинг

**1.1 Всего:** `3197`. **1.3 Скрытые:** `.githooks` (1) · `06-claude-workspace/skills/duckdb-skills-main/.claude-plugin` (2) · `.github` и `.vscode` внутри архива кода.

### Решение по каталогам

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `02-code-archive/` | **2890** | **С** | архив старого кода: SQL-тренажёры, C++ университетский, алгоритмы, Jupyter. Предметное содержание, не метод |
| `06-claude-workspace/` | 44 | **М** | методички по инструменту, сторонние скиллы, конфиги |
| `00-infrastructure/` | 38 | **М** | служебка |
| `05-tech-guides/` | 10 | **?→С** | железо, VPN, сети — предметные справочники, не про работу системы |
| `07-career/` | ~50 | **С** | резюме, вакансии, менторство |
| `01-projects/` | ~50 | **С** | код проектов |
| `04-study-guides/`, `03-machine-learning/` | 15 | **С** | учебное |

### Прочитано и решено

| Файл | Решение |
|---|---|
| `06-claude-workspace/guides/claude-code-guide.md` (418) | 📍 **оставлен локальным** — разбор ниже |
| `06-claude-workspace/skills/*` (duckdb, humanizer, find-skills, pencil) | 📍 сторонние наборы, чужой код; ценны как **образцы** оформления скилла, не как знание системы |
| `06-claude-workspace/instruction-files/*` (14) | ✅ дубли того, что уже в базе (`00-infrastructure/CLAUDE.md`, `Memory.md`, `Skills.md`, `Agents.md` и др.) |

**Почему методичка по Claude Code не повышена целиком.** Датирована апрелем 2026 — четыре месяца,
и инструмент за это время сменился. Проверка синтаксиса показала: **команды в ней уже не те**
(`/claude plan` такой команды нет; `/worktree create` / `/exit` — сомнительны). Копировать
устаревший командный справочник в базу значит раздать его в 57 реп и закрепить ошибки.

**Что взято — только долговечное**, без синтаксиса: `69-agents-hooks-and-gates.md` §4а —
три режима (планирование / worktree / цикл), что каждый **изолирует** и когда брать.
Ценность планирования в том, что **разведка отделена от правок**; worktree даёт **второе**
дерево, а не переключает одно, и потому прямо поддерживает дифференциальный тест из
`22-merge-protocol.md` §4б; цикл — только для внешнего ожидания.

**Урок про сами методички по инструментам:** документ с зафиксированными командами протухает
быстрее, чем его перечитывают. Долговечно — **что режим изолирует и когда его брать**;
недолговечно — **как он вызывается**. Тот же приём, что в `66` §6 (каталог моделей намеренно
не включён).

### `it-base/00-infrastructure` — сверка по sha256

```
133 файла = совпало с базой 91 + дельта по имени 14 + «нет в базе» 4
```

**Все четыре «новых» — не новые:** это строчные варианты имён (`watchlog.md`,
`reports/incidents-summary.md`, `reports/investigations-summary.md`) и `base-repo-readme.md`.
Та же проблема нейминга, что решается снятием плоской копии — она в `PLAN.md` P1.1.

**Вывод: `it-base/00-infrastructure` — старая плоская копия базы, нового знания не содержит.**
Снимается вместе с копией при раздаче.

### `05-tech-guides/` (7 файлов, 3829 строк) — **С**

`ai-tools-guide.md` (509) — каталог AI-сервисов; база уже покрывает тему принципиально
(`63-ai-tools-landscape.md`) и **прямо оговаривает срок годности**: долговечны принципы выбора,
недолговечен каталог. Повышать второй каталог значило бы завести в базе то, что протухнет
через квартал. Остальное (`it-guide`, `pc-build-guide`, `setevye-protokoly`, `tech-guide`) —
предметные справочники по железу и сетям, к работе системы не относятся.

### ✅ Приёмка `it-base`

```
3197 = С 3043 (архив кода 2890 + карьера/проекты/учебное ~140 + tech-guides 13)
     + база 91 + дельта 14 + прочитано и решено 49
```

Репа закрыта. **В базу поднято:** `69` §4а (три режима работы, без синтаксиса).

---

## Репа 3: `portrait-of-taste` — 1572 файла

### Листинг

**1.1 Всего:** `1572`. **1.3 Скрытые:** только `.githooks` (1).
**1.4 Расширения:** `.md` 806 · `.jpg` 448 · `.png` 181 · `.jpeg` 86 · `.sh` 16 · `.py` 13.

### Решение по каталогам

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `standards/` | 26 | **М** | 26 стандартов и протоколов — ядро метода этой репы |
| `reports/situations/` | 18 | **М** | разборы ситуаций |
| `docs/`, `reports/` корень | 32 | **М** | методички и реестры |
| `backlog/`, `templates/`, `prompts/` | 10 | **М** | процесс |
| `profiles/`, `men_profiles/` | 479 | **С** | разборы конкретных людей |
| `photos/`, `photos_original/`, `photo_notes/`, `social_screenshots/` | ~760 | **С** | исходники и описания конкретных фотоархивов |
| `characters/`, `recognition-cards/`, `analysis/`, `author/`, `lab/` | ~20 | **С** | предметное |

### Прочитано и решено

| Файл | Решение |
|---|---|
| `PHOTO_OBJECTIVE_ANALYSIS_STANDARD.md` | ⬆️ ранее → `65-visual-source-pipeline.md` §6 (двухфайловое разделение слоёв) |
| `reports/situations/2026-08-15-...-false-marriage-G.md` | ⬆️ ранее → **PIT-073** (каскад из наблюдения в «факт») |
| `standards/FAIL_LOUD.md` (168) | ⬆️ **→ `71-fail-loud-and-sourcing.md`** |
| `standards/COMMUNICATION_PROTOCOL.md` (99) | ⬆️ → `71` §7а (формулы ответа) |
| `standards/HOW_WE_WORK.md` (213) | ⬆️ → **PIT-081**, **PIT-082**; правила 16–18 → `71` §7а |
| `standards/EXISTING_FILE_PRIORITY_PROTOCOL.md` (37) | ⬆️ → **PIT-080** |
| `standards/CHECKPOINT_ZIP_PROTOCOL.md` (65) | ✅ покрыто: UTF-8-флаг уже в `scripts/pack_release.py` и PIT-025 |
| `standards/PROFILE_STANDARD`, `PHOTO_*`, `METRICS_*`, `CROP_*`, `SOCIAL_*`, `CHARACTER_KIT`, `CODE_ASSIGNMENT`, `INTERVIEW_MODE`, `RECOGNITION_SYSTEM`, `PSYCHOLOGY_*`, `MALE_PROFILE_*` | 📍 предметные — метод разбора **людей**, не системы |

**Главное, что взято — `71-fail-loud-and-sourcing.md`.** В базе принцип fail-loud был рассыпан
по инцидентам и упоминался в пяти документах, но **отдельного документа не существовало**.
Собрано: запрещённые формулировки («Поправка», «На самом деле», «Оказывается», «Однозначно» —
все создают ложное впечатление состоявшейся проверки) · гипотезы выдаются, а не реализуются ·
сомнения списком сразу, а не по одному · **у каждого утверждения есть источник, не можешь
назвать — выдумка** · самопроверка перед записью · формулы ответа на критику.

**🔴 Правило 9 оттуда — отдельно.** *«Владелец устал проверять» — это метрика качества работы,
а не жалоба.* Реакция не «спорить», а сократить необходимость проверять. Записано в `71` §7.

### Сходимость, которую стоит отметить

Правило 1 `HOW_WE_WORK` («скрипты только на метаданных, содержимое — вручную») выведено
в `portrait-of-taste` из разрушения 52 документов скриптом. **Та же граница независимо
получена трижды в системе:** ADR-010 (сборщик не сведёт четыре протокола в методичку и
не научится) · PIT-074 (четыре попытки автоматически разделить метод и содержание) ·
этот случай. Записано как **PIT-081: автоматизируется механика, не суждение.**

### `docs/` и `reports/situations/` в `portrait-of-taste` — разобрано

| Файл | Решение |
|---|---|
| `docs/ANTI_PATTERNS.md` §1–5, §7 | ✅ **уже сведено** — это PIT-055…059 и PIT-073, поднятые ранее |
| `docs/ANTI_PATTERNS.md` §6 | ⬆️ → `71` §7б — **граница против уклонения**, синтеза которого в реестре не было |
| `situations/2026-07-24-photo-archive-integrity.md` | ⬆️ **PIT-083** (основание не сохранено), **PIT-084** (по памяти о стандарте), **PIT-085** (кириллица в пути) |
| `situations/2026-07-refusal-consistency-across-sessions.md` | ✅ уже сведено в PIT-055…059 |
| `docs/MERGE_PROTOCOL.md` §4 | ✅ уже сведено — **PIT-052** |
| `docs/MERGE_PROTOCOL.md`, `docs/REVISION_PROTOCOL.md` | ✅ адаптации базовых `22`/`21`; **база теперь полнее обеих** (177 и 182 строки против 83 и 102) |
| `docs/PHILOSOPHY.md`, `RATIONALE.md`, `METHODOLOGY.md`, `INSTAGRAM_FETCH_GUIDE.md`, `STATISTICS.md` | 📍 предметные |

**Отмечено отдельно: шесть проверок из семи показали «уже сведено».** Это и есть правильный
результат сверки — она предотвратила задвоение реестра. Проверять перед добавлением дешевле,
чем чистить дубли потом (PIT-080).

### ✅ Приёмка `portrait-of-taste`

```
1572 = С 1259 (профили 479 + фотоархивы ~760 + предметное ~20)
     + база/дельта 145 + прочитано и решено 168
```

Репа закрыта. **В базу поднято:** `71-fail-loud-and-sourcing.md` (новый документ) ·
`65` §6 (двухфайловое разделение слоёв, ранее) · PIT-073, PIT-080, PIT-081, PIT-082,
PIT-083, PIT-084, PIT-085.

---

## Репа 4: `character-a-analysis` — 337 файлов

### Листинг

**1.1 Всего:** `337`. **1.3 Скрытые:** `.claude/scheduled_tasks.lock` (пустой файл-замок).
**Сверка:** совпало с базой 91 · дельта 25 · нет в базе 221.

### Решение по каталогам

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `10-life-history/00-protocol/` | 10 | **М** | 🔴 ядро метода: правила, процесс, каналы, механика контекста, самопроверка, промпты вахты |
| `00-infrastructure/` | 50 | **М/✅** | плоская копия базы + 11 методичек, **уже поднятых в v1.53.0** как `54`–`64` |
| `context-lab/` | 12 | **М** | лаборатория замеров контекста |
| `10-life-history/09-history/` | 39 | **С/?** | история работы: черновики, промпты прошлых сессий, инвентаризации |
| `10-life-history/02-notes/`, `01-registry/` | ~25 | **С** | дословные заметки по источникам, реестр фактов о человеке |
| `01-psychological-profile/`, `04-genetics-genotek/`, `03-reports/`, `04-deliverables/` | ~30 | **С** | предметное |

### Прочитано и решено

| Файл | Решение |
|---|---|
| `00-protocol/09-session-end-self-check.md` (140) | ⬆️ **→ `23-session-continuity.md` §5а–5б** |
| `00-protocol/05-context-mechanics.md` (94) | ⬆️ **PIT-086** + `49` §1а (цена кириллицы) |
| `00-protocol/01-rules.md` §1 (264) | ⬆️ → `65` §6.1а–6.1б (паспорт качества, заявление шире работы) |
| `00-protocol/03-channels.md` (199) | ✅ покрыто базовым `54-pdf-reading-channels.md` (240 строк) |
| `00-infrastructure/reports/incidents/*` | ✅ поднято ранее: INC-RELEASE-NOTES, INC-PDF-DESYNC, INV-VISION |
| 11 методичек `34`–`44` | ✅ подняты в v1.53.0 как `54`–`64` |

**Главное — `23` §5а: проба вместо ощущения.** Собственную заполненность контекста измерить
нельзя, «ощущение» ненадёжно — но можно построить **пробу**: дешёвый детерминированный тест,
отказ которого бинарен и наблюдаем. Два сигнала: **авторитетный** (проба) и **ранний
предупредитель** (прокси-балл, краснеет раньше). Плюс различение компакции окна и более
дешёвой очистки результатов инструментов — их путают.

**`23` §5б — три вещи в момент передачи**, и третью забывают: **текст промпта прямо в ответе,
одним блоком для копирования**. «Промпт см. в файле X» не работает — у следующей сессии этого
файла ещё нет.

**PIT-086 — «верь файлам, не саммари».** Журнал сказал «сделано до 49», вахта поверила метке →
батч продублирован. **Тот же механизм, что мой сегодняшний PIT-072** (принял §0 за опись
сделанного). Два независимых источника, один вывод: запись о состоянии — это **гипотеза**
о состоянии.

**`65` §6.1а–6.1б — два понятия, которых в базе не было:**
- **паспорт качества как условие закрытия** (режим · покрытие X/Y · что взято · ограничения
  списком · честная оценка), с порогом приёмки, назначенным **заранее**;
- **заявление о работе шире, чем работа** (в исходной репе — транслитерация overclaim,
  закрепившаяся с опечаткой; в базе формулировка русская). «Прочитано по старым заметкам» = не
  прочитано; прежний разбор — источник гипотез, но не источник правды (в живом корпусе
  прежние наработки содержали ≥9 подтверждённых ошибок).

---

## 2026-08-20, вечер — решения по мосту планировщика (P1.5, до возврата в синтез)

Владелец в отъезде, дал мандат решать самому и фиксировать здесь.

**Решение 1: разрезать `BOARD.md`, а не обходить проблему в скрипте.**
Срезы для `base-repo` и `mission-control` выходили байт-в-байт одинаковыми — один
`source-hash` на две репы. Можно было научить скрипт делить общую секцию по подстроке,
но это лечило бы **симптом в производном**: доска — вид, а не источник (её собственная
шапка это и предписывает). Разрезано вручную по одной секции на репу, каждая задача
отнесена к той репе, где исполняется. Публиковано `63046dd0`.

**Решение 2: 38 реп без секции — НЕ заполнять.**
Соблазн был: довести покрытие с 19 до 57 и закрыть пункт. Отказался. Назначить репе
задачу можно только зная, что в ней делать, — иначе это выдуманная работа, которая
потом поедет мостом в 38 реп и будет выглядеть как решение владельца. Пробел записан
задачей на еженедельный разбор, где ему и место. **Закон 4 («при сомнении повышать»)
здесь не применим: он про полноту переноса существующего, а не про сочинение нового.**

**Решение 3: guard'ы — в скрипт, а не в инструкцию.**
Правило «не доставляй общий срез» можно было записать в `74-planner-bridge.md` и
надеяться. Записано **в код** (`push-inbox.sh` 1.1.0): срез обязан принадлежать одной
репе; приёмник обязан существовать. Оба guard'а поймали реальные случаи в первую же
минуту — `base-repo`/`mission-control` и `it-base`. Это ровно тот выбор, что уже сделан
для секретов и версии деплойера: правило, живущее в документе, не исполняется.

**Решение 4: доставить в `base-repo` и разобрать по контракту прямо сейчас.**
Не ради демонстрации: в доске лежали две задачи базы, которых в её `ROADMAP.md` не было
(баг `finpilot-mirror`, 5 конфликтных служебок). Мост существует ровно для этого разрыва.
Доставлено (sha `1b082813`), разобрано в §P2.

**Находка, ради которой стоило довести до конца — PIT-088.**
Разбирая доставленное, проверил, что делает с этим файлом деплойер. `deploy.sh:1470`
сносит рабочее дерево клона и накрывает архивом из **локального** дерева, где файла
доставки нет. Значит **любая публикация репы молча удаляет доставленное ей**. Порядок
«доставка → деплой → вахта» теряет задачу без единого сигнала.

Замечено только потому, что после доставки задан вопрос «а что с этим файлом делает
второй инструмент». **Если два инструмента пишут в одну репу разными каналами, ответ
„второй ничего не сделает“ надо доказать, а не предположить.**

---

## 2026-08-21 — доска доведена до 57 секций (решение владельца, отменяет моё решение 2)

**Владелец: «заведи пока фейковые задачи временные типо foo bar и интегрируй каждую репу
на доску».** Моё вчерашнее решение 2 было — не заполнять. Оно отменено, и правильно.

**Почему возражение снялось.** Я отказывался сочинять задачи, потому что выдуманная
**правдоподобная** задача читается как решение владельца и уезжает мостом в таком виде.
Явная заглушка `foo`/`bar` с пометкой «задача не назначена» так читаться не может —
она сообщает «секция есть, содержания нет», что и есть правда. Опасна была не пустота
формы, а **ложное содержание**; заглушка формы не подделывает содержание.

Заведено 38 секций, стало 57. Каждая помечена 🔸 и словами «заглушка, наполнить
на еженедельном разборе».

**Найдено при этом — дефект, который с 19 секциями не проявлялся.**
`push-inbox.sh` искал секцию **подстрокой**: `grep -i -m1 -- "$repo"`. Пока реп на доске
было 19, коллизий не возникало. На 57 всплыла пара **`style` ⊂ `visual-style`** —
и репа `style` получила бы срез `visual-style`.

Guard 1 этого **не ловил**: заголовок `## visual-style` называет ровно одну репу,
то есть с его точки зрения всё законно. Guard проверял неоднозначность заголовка,
а дефект был в **сопоставлении имени с заголовком** — уровнем ниже.

Исправлено в 1.2.0: имя сравнивается целиком (до « (» или « —»), подстрока — запасной путь.
Проверено сухим прогоном по всем 57: каждая резолвится в свою секцию, **все 57 хешей
срезов различны**.

**Урок, который стоит отдельно.** Guard написан против известного случая и потому
проверял симптом, а не свойство. Свойство здесь — «репа получает свой и только свой
срез», и проверяется оно не формой заголовка, а **уникальностью 57 хешей**. Проверка,
не ловящая этот класс ошибок, проверкой не является (PIT-016, PIT-070).

**Массовую доставку заглушек в 57 реп НЕ делал.** Это 57 мусорных коммитов `foo`/`bar`
в живые репы, и по PIT-088 часть из них переживёт до следующего деплоя. Проверка
достигнута сухим прогоном — он доказывает то же самое, ничего не записывая.
Реальная доставка — по слову владельца, отдельным действием.

---

## 2026-08-21 — `mission-control`, разметка каталогов и первые протоколы

### Четыре команды листинга (вывод, не пересказ)

```
1.1 всего файлов: 102

1.2 перепись каталогов:
  23 ./scripts          22 ./00-infrastructure   18 .
  12 ./decisions        10 ./reports              4 ./plans
   3 ./reports/data      2 ./profile              2 ./plans/finpilot
   2 ./daily/source      2 ./daily                1 ./scripts/tests
   1 ./daily/habits

1.3 скрытые каталоги: нет ни одного (только «.» — корень)

1.4 расширения:
  72 .md · 13 .sh · 8 .py · 2 .docx · 1 (без расширения)
   1 .txt · 1 .repo-id · 1 .repo-class · 1 .log · 1 .json · 1 .gitignore
```

**Сверка с базой:** совпало байт-в-байт **1**, в очереди **101**.

### Разметка каталогов — построчно, весь список

| Каталог | Отметка | Обоснование |
|---|---|---|
| `00-infrastructure/` (22) | **М** | 21 протокол + README, ядро метода репы |
| `scripts/` (23) | **М** | метод бывает скриптом; здесь же мост, деплой, аудиты |
| `.` корень (18) | **М** | `MISSION`, `PLAN`, `BOARD`, `WATCHLOG`, `START-HERE` — устройство планировщика |
| `decisions/` (12) | **М** | 11 ADR — решения с обоснованием, переносимый жанр |
| `reports/` (10) | **?→М** | открыл три: разборы инцидентов и исследований, не сырьё |
| `reports/data/` (3) | **С** | выгрузки: `coverage-*.json`, инвентаризация `.txt` — результат, не метод |
| `plans/` (4) | **?→С** | открыл два: планы конкретных кампаний владельца, привязаны к его предмету |
| `plans/finpilot/` (2) | **С** | задачи конкретного продукта |
| `profile/` (2) | **?→М** | `SETUP.md` — как разворачивать профиль, метод |
| `daily/` (2) | **М** | `ROTATION.md`, `TEMPLATE.md` — механика дневного листа |
| `daily/habits/` (1) | **С** | заполненный трекер за месяц |
| `daily/source/` (2) | **С** | два `.docx` — исходники дневного листа. **Формат запрещён стандартом репы** (`06-volume-compression.md`): распаковать в `.md` или удалить. Задача заведена |
| `scripts/tests/` (1) | **М** | тест моста |

### Прочитано и решено

**`04-capture-protocol.md` — ⬆️ повысить.**
Воронка захвата: восемь мест записи → один вход. Ключевая мысль, которой в базе нет:
**«выбрать один инструмент» не работает** — инструмент выбирается не рационально,
а по тому, что было под рукой в ту секунду (голосом в метро — Telegram, за столом —
бумага). Отсюда модель «один основной канал + **дренаж** остальных по расписанию»:
**источники не запрещаются, они опустошаются.**

Проверка на дубль по структуре: база имеет `20-knowledge-capture-protocol.md`, но он про
**захват знания по ходу работы** (7 триггеров → `.md`). Здесь — про **входящий поток мыслей
из личных инструментов**. Разные предметы, не дубль. Плюс рабочий AppleScript экспорта
Apple Notes с двумя предупреждениями (пароли в выгрузке, locked-заметки теряются молча).

**`13-watch-discipline.md` — 🔗 свести, две части подняты в базу.**
Тело протокола дублирует `04-watchlog-protocol.md` и `23-session-continuity.md` базы.
Но **две части оказались уникальными**, обе подняты сегодня же:

1. **§3а «Проверка §0 на подлинность»** → базовый `04-watchlog-protocol.md`.
   Версия в §0 правится механически, тело — руками; расходятся незаметно. Проверка —
   взять **три конкретных** утверждения и сверить; устарело хоть одно → §0 **переписывается
   целиком**, а не точечно. Случай: 13.08 тело §0 совпадало с редакцией четырёхдневной
   давности при новом номере — четыре версии подряд журнал врал о трёх вещах сразу.

2. **§3б «Коллизия номеров между параллельными вахтами»** → базовый `72-source-of-truth.md` §4а.
   **Это не то же самое, что уже описанный в §4 отставший журнал.** Там вахта одна и журнал
   устарел — пересобирают архив. Здесь вахт **несколько одновременно**, обе берут честно
   один номер, и работа второй **исчезает с GitHub целиком**. Случай: 09.08 две вахты
   выпустили `0.16.0`, отчёт и маршруты пропали, восстанавливали четыре дня.
   Почему не лечится договорённостью: общего канала между аккаунтами нет по построению,
   единственное общее место — GitHub, там и ведётся очередь номеров.

> Закон 4 сработал ровно так, как задуман: тема выглядела покрытой («ну это же наш
> watchlog-протокол»), а сравнение **структуры** дало два блока, которых в базе не было.

**`07-repo-classes.md` — ⬆️ повысить, заведён `00-infrastructure/76-repo-classes.md`.**

Проверка на дубль дала **пустоту там, где ожидалось покрытие**: у базы `.repo-class`
**есть** (содержит `infra`), она **предписывает** его репам через `START-HERE_TEMPLATE`,
и упоминает в трёх документах — но **нигде не определяет, какие значения законны
и что они значат**. `48-repo-identity.md` описывает `.repo-id` и `VERSION`, класс не трогает.

**Найдено по дороге — сильнее самого пробела.** `templates/START-HERE_TEMPLATE.md:41`
отсылал за определением классов в **`mission-control/00-infrastructure/07-repo-classes.md`**.
Родительская репа делегировала базовое понятие чужой репе, и **каждая репа, заведённая
из шаблона, наследовала указатель на файл, которого у неё нет**. Класс PIT-063: механизм
есть, документация живёт не там, где её прочтут. Указатель переписан на `76`.

Проверил остальные 11 упоминаний `mission-control/` в доках базы — все остальные
это пометки **происхождения** («Извлечено из …»), что методом и предписано. Живая
делегация была ровно одна.

**Что поднято:** семь классов с судьбой и обязательным минимумом · различие
`satellite`/`temp` с обязательным родителем («без родителя все временные становятся
вечными») · три места хранения класса и правило править их вместе · два железных правила
заведения (репа не заведена без строки в карте; репа не заведена без манифеста
«за что отвечает») · приём проведения границ парой через «↔» · семь правил именования.

**Что НЕ поднято, намеренно:** раскладка конкретных реп по классам. Институт переживает
раскладку, раскладка меняется с каждой новой репой — две копии разойдутся
(`72-source-of-truth.md`). Плюс она **уже отстала**: называет 49 реп при фактических 57.

**Добавлено от себя, из практики этого прогона:** восьмое правило именования — проверять,
не является ли новое имя **подстрокой** существующего. Пара `style` ⊂ `visual-style`
вчера стоила дефекта в мосте (PIT-089), и это ровно правило именования, которого
в исходнике не было.

**`14-system-audit.md` — ⬆️ повысить, заведён `00-infrastructure/77-system-wide-audit.md`.**

Проверка на дубль: у базы есть `21-revision-protocol.md` (ревизия **внутри одной репы**)
и `53-infrastructure-sync-standard.md` (**разовая** кампания сведения). Регулярной сплошной
ревизии **всей системы** нет. Это третья сущность, не покрытая двумя первыми:
`53` — операция с началом и концом, `77` — **ритм**. Без ритма перекос, разобранный
кампанией, накапливается заново с той же скоростью.

**Что поднято:** асимметрия раздачи как корень перекоса (база уезжает вниз одной командой,
обратного хода нет — дрейф гарантирован конструкцией, а не разгильдяйством) · месячная
каденция + внеочередной повод «изменился стандарт» · две таблицы аудита · три исхода
по кандидату · 11 обязательных файлов и 4 условия · **показатель — доля реп, прошедших
проверку целиком**, явно не число правок и не объём отчёта («растут сами собой и ни о чём
не говорят»).

**Главное — частотный сигнал, внесён и в метод текущей кампании (`PROCEDURE.md` §3а).**
«Файл, независимо появившийся в пяти репах, почти наверняка описывает общесистемное
правило.» Ценность: это **единственный сигнал в синтезе, не зависящий от читающего**.
Все прочие («похоже на метод», «выглядит переиспользуемым») — суждения, и ошибаются
молча; ровно так четыре инструмента подряд теряли материал (PIT-074).

### Сигнал применён немедленно — и дал не то, что ожидалось

Посчитал частоту служебок по всем 57 репам. Ожидались кандидаты в базу. Получилось третье.

Три имени встречаются в 11–13 репах и **в базе отсутствуют**: `base-repo-readme.md`,
`changelog.md`, `watchlog.md`. Сравнение показало: это **не наработки реп и не пробел
базы, а протечка самой базы** — переименованные копии её `README`/`CHANGELOG`/`WATCHLOG`
от раннего, плоского механизма раздачи, оставшиеся после переезда раздачи в `_base/`.

Замер при базе **1.66.0**: у 12 реп верхняя запись `changelog.md` — **`1.3.0`**
(63 минорные версии назад), `watchlog.md` разошёлся на **три редакции**. При этом
у 42 реп те же документы лежат свежими в `_base/` — **две копии одной правды в одной репе**.

Почему не находилось раньше: имена при копировании **переименовывались**
(`CHANGELOG.md` → `changelog.md`), и ни одна сверка «есть ли файл в базе» их не видела.
Файл невидим ровно потому, что назван иначе.

Заведено: **PIT-090** (реестр 90 → 91) и задача в `ROADMAP.md` §P2.

> **Оценка самого приёма.** Сигнал сработал именно так, как описан: сказал «начни
> сравнение», а не «поднимай». Ловушка «одинаковое имя ≠ одинаковое содержание»,
> записанная в §3а вместе с приёмом, сработала в первом же применении — и вывела
> на дефект, которого никто не искал.

**`09-base-sync.md` — ⬆️ повысить, заведён `00-infrastructure/78-base-embedding.md`.**

Тот же случай, что с `76`: у базы **механизм есть** (`_base/` раскладывается деплойером),
а **принцип, ради которого он существует, не записан нигде**. `53` упоминает `_base/`
в фазе 3 как результат раскатки, но не объясняет ни зачем каталог отдельный, ни почему
из этого следует отсутствие слияний.

**Что поднято:** формулировка задачи (57 копий вручную не сводятся — нужно правило,
снимающее слияние **целиком**, а не «делающее его лучше») · разделение пространств имён
и правило одной строкой · таблица «вниз/вверх — конфликты невозможны» · две детали
раздачи из инцидентов (`.repo-id` удаляется, `VERSION` → `BASE_VERSION`) с общим корнем
(**рекурсивный поиск не различает уровни вложенности**) · сбор вверх ничего не переносит
сам, и почему автоматизация решения замусорит базу · таблица отвергнутых механизмов
(submodule / subtree / template / Actions) с причинами.

### Протокол уточнил корень вчерашнего PIT-090 — до структурного

Вчера записал причину как «сменили механизм, забыли убрать за старым». Прочитав §1,
вижу, что причина глубже и хуже.

**Граница режет в обе стороны.** Контракт: `_base/` — чужое, скрипт управляет;
`00-infrastructure/` репы — своё, **скрипт не трогает никогда**. Именно этот запрет
делает раздачу безопасной. Он же делает **неисправимым всё, что попало на сторону «моё»
по ошибке**: протёкшие копии лежат в пространстве, которого автоматика по контракту
касаться не имеет права.

**Они не забыли обновиться — они не могут обновиться по построению.**

**Плюс обратная петля, которой вчера не увидел.** Сбор вверх относит в кандидаты
на повышение всё, чего нет в `_base/`. Переименованная копия по имени с оригиналом
не совпадает (`changelog.md` ≠ `CHANGELOG.md`) → механизм предложит **поднять в базу
её же собственный устаревший файл**. Карточка PIT-090 дополнена обоими пунктами.

> **Урок про сам синтез:** карточка, заведённая по замеру, уточнилась через час чтением
> исходного протокола. Замер даёт **факт**, документ источника — **механику**, и порядок
> «сначала замер, потом чтение» не заменяет обратного. Читать надо оба.

### Стареющие места, замеченные по ходу (в базу не идут)

`09` говорит про **29 реп** (сейчас 57) и путь `~/repos` (фактически
`~/Documents/система_репозиториев`). `07` называет 49 реп. Это содержание планировщика,
не метод; правится у него, здесь только отмечено.

**`05-weekly-review.md` — 📍 оставить локальным, одно правило ⬆️ поднято.**

Сам ритуал — про `INBOX.md` планировщика, у остальных 56 реп инбокса нет. Это протокол
конкретной репы, не системы.

Но одна формулировка оказалась шире своего повода и поднята в `77-system-wide-audit.md` §6:

> **Ритуал, который не выполняется, чинится изменением ритуала, а не усилием воли.**

Пропущен трижды подряд — факт о **конструкции**, а не о характере. Правило применимо
к любому повторяющемуся механизму базы: ревизии, сплошному аудиту, чек-листам, гейтам.
Вместе с ним поднята таблица признаков (диагностика по признакам, а не по ощущению).

Отдельно отмечу: в `77` §6 у меня **уже была** близкая мысль («не движется несколько
месяцев — проблема в стандарте, а не в дисциплине»), выведенная самостоятельно из метрики.
Формулировка источника резче, потому что называет **что делать** (менять ритуал),
а не только **где проблема**. Заменил на неё, свою оставил рядом как следствие.

**`18-final-command.md` — 🔗 свести, три блока ⬆️ подняты в `73-chat-handoff.md`.**

Тело дублирует базовый `73` (триггер, ранние признаки, четыре действия, эталон промпта,
признаки плохой передачи) — всё это поднималось из `16-chat-handoff.md` в прошлую вахту.
Но `18` описывает **закрытие чата**, а не только передачу, и три вещи оказались новыми:

1. **«Вычерпать чат» — главный шаг, заведён как §2.1а.** База закрывала **состояние**
   (что сделано), но не **вычерпывание** (что прозвучало и не сделано). Источник прямо
   говорит: «это главный шаг, всё остальное механическое, а здесь теряется больше всего».
   Ключевое — искать сказанное **вскользь**: «а ещё надо бы», отложенные идеи и,
   чаще всего теряемое, **найденные и незакрытые дефекты** (в момент находки они кажутся
   незабываемыми).

2. **Метрика вычерпанности** — назвать числом, сколько пунктов ушло в `TASKS`/`ROADMAP`/
   бэклог. «Всё перенёс» непроверяемо, «7 / 4 / 3» проверяемо. Ноль дефектов в бэклоге
   при том, что в чате их находили, — признак пропущенного шага.

3. **«Уточняющих вопросов не задаётся»** с причиной: чат у предела, лишний круг вопросов
   может **не поместиться вместе с ответом**. Спорное идёт в бэклог с пометкой о сомнении.

Плюс мелочь, но полезная: закрытое **зачёркивается с датой и «чем закрыто»**, а не
удаляется — через месяц ценно не отсутствие пункта, а причина исчезновения.

**Прогресс по репе:** прочитано **8 протоколов из 21** (12, 16, 21 — прошлой вахтой;
04, 13, 07, 14, 09, 05, 18 — этой). Осталось 13.

**`17-gh-automation.md` — 🔗 свести, четыре блока ⬆️ подняты, один помечен УСТАРЕВШИМ.**

**1. Условие переноса — два условия, не одно** → `77-system-wide-audit.md` §4.
Сегодня утром я поднял частотное правило («файл в 5 репах — почти наверняка база»)
и оно было **неполным**. Объективное правило состоит из двух условий: частота **И**
совпадение всех копий **по хешу**. Разное содержимое под одним именем — **конфликт,
а не кандидат**: автоперенос выбрал бы одну редакцию и молча затёр чужую. Ровно это
уже случалось с реестром граблей (пять независимых линий нумерации, 110 записей → 62 урока).
Конфликт — сигнал **сильнее** совпадения: репы решали одну задачу по-разному.

**2. Каталоги служебок ищутся по факту, а не по списку** → туда же.
Первая версия сборщика имела зашитый перечень из четырёх каталогов и не увидела ничего
в четырёх репах, где служебки лежали в `reports/`, `docs/standards/`, `protocols/`.
**Независимый случай PIT-074** — тот же дефект, что четырежды губил инвентаризацию этой
кампании, найденный другими людьми в другом инструменте. Лечение: обход на три уровня
+ подсказка в имени, причём подсказка **расширяет** охват, а не сужает: её промах даёт
лишний каталог на проверку, а не потерянный.

**3. Гонка при push** → `58-git-practice.md` §3а. `! [rejected] (fetch first)` — не ошибка,
а **гонка**: между клонированием и записью кто-то запушил в ту же ветку. Случай: прогон
перенёс 60 файлов и упал на push, потому что параллельный скрипт правил карту.
Лечится паттерном `fetch` + `rebase` + повтор до трёх раз. **`--force` в массовом скрипте
никогда** — он затирает чужой законный коммит; если нужен форс, то только
`--force-with-lease`, отказывающийся ровно в опасном случае.

**4. Производное чинится в источнике** → `72-source-of-truth.md` §4б. Цепочка карты
замкнута: `.repo-meta` → деплойер → описание на GitHub → сборщик → карта. «НЕ ОПИСАНА»
лечится правкой `.repo-meta`, а не карты: правка производного **тупик по построению**,
следующая пересборка её перетрёт — и перетрёт молча. Записаны три признака, по которым
производное опознаётся.

### 🔴 §1 протокола УСТАРЕЛ для Claude Code — проверено, не вычитано

Протокол утверждает: у Claude в песочнице **нет токена и нет сетевого выхода**, приватная
репа недостижима **в принципе**, поэтому архив в чат — единственный способ показать
содержимое. Отсюда его вывод: *Claude пишет скрипты, скрипты работают с репами*.

**В Claude Code это не так.** Проверка: приватная `self-map` читается через `gh api`;
три коммита в `mission-control` за 20.08 сделаны **прямо из этой сессии**.

Записано в `64-claude-code-sandbox.md` таблицей «браузерная песочница vs Claude Code».
Три следствия:

- «архив — единственный способ показать содержимое» **неверно**; архив остаётся каноном
  передачи, но по другой причине — он **фиксирует состояние**, а не даёт доступ;
- разделение «Claude пишет скрипты, скрипты работают с репами» здесь **не обязательное,
  а выбранное**;
- **опасность именно в этом**: сессия с привычками браузерной песочницы недооценивает
  свои права — команда, которая там была бы безобидной попыткой, здесь **меняет живую
  репу на GitHub**.

> Класс PIT-063 в чистом виде: документ верен для своей среды и **перестал быть общим**,
> а по тексту этого не видно. Отмечать надо не «протокол неправ», а **границу его среды**.

**Прогресс:** прочитано **9 протоколов из 21**, осталось 12.

**`02-entity-types.md` — ⬆️ повысить, поднято в `45-roadmap-and-tasks.md` §3а–3б.**

База имела **два** адреса (роадмап / задачи) — этот протокол даёт **шесть типов
входящего** с исполнителем и домом у каждого, плюс две вещи, которых не было совсем.

**1. Дифференцированная цена ошибки сортировки** — самое ценное здесь. Не «сортируй
аккуратно», а **чем именно платишь за каждую конкретную ошибку**:

- действие в роадмапе — **зависает навсегда**: ассистент не может, владелец туда не смотрит;
- вопрос в задачах — **ложное ощущение объёма** (весит как месячный проект, закрывается за пять минут);
- идея в задачах — **вызывает вину**: лежит и не делается, потому что делать в ней нечего;
- инсайт в задачах — **теряется**: его никто не выполнит, он не задача.

Ключевое наблюдение, которое я дописал от себя: **ни одна из четырёх ошибок не выглядит
ошибкой изнутри списка** — видно только общее «список не работает». Поэтому чинить
пытаются дисциплину, а чинить надо раскладку.

**2. Четыре выхода записи, и пятого нет.** Формулировка источника точная:
«выхода *переехало в следующий список без изменений* — нет; именно он превращает список
задач в мемориал». База знала слово «мемориал» (в шапке `45`) и имела раздел «Что осознанно
НЕ делаем», но **не имела самой рамки четырёх выходов** — а без неё раздел отказов
остаётся необязательным украшением.

Плюс **правило конфликта копий**: одна задача в нескольких поколениях списков с разными
статусами → верен статус в **самой свежей** копии. Не в той, где отмечено сделанным,
и не в той, что под рукой.

Не поднимал: §3 (разделение по исполнителю) — это цитата базового `45`, вернувшаяся
из планировщика; §4 про время глагола — уже есть в шаблоне `TASKS_TEMPLATE.md`.

**Прогресс:** прочитано **10 протоколов из 21**, осталось 11.

**`19-claude-code.md` — 🔗 свести, три блока ⬆️ подняты, зоны 📍 оставлены локальными.**

**1. «Где решает смысл — модель, где решает совпадение — скрипт»** → `69-agents-hooks-and-gates.md`
§4а. Лучшая формулировка делегирования, что встречалась в системе. Ценно, что правило
**симметрично, но цены разные**: отдать скриптовую работу модели — дороже и менее надёжно;
отдать смысловую скрипту — **невозможно в принципе**. Детерминированный перенос умеет
сказать «файл побайтово одинаков в трёх репах», но не умеет «эти четыре документа про одно
и то же». Ровно поэтому автоклассификация «метод/содержание» отменена в `PROCEDURE.md` §7.

**2. Массовая правка — только в отдельном дереве** → туда же. Причина названа сильнее,
чем «перестраховка»: **модель, получившая право писать в полсотни реп, ошибается тихо
и одинаково во всех** — то есть дефект не выглядит дефектом ни в одной репе по отдельности,
а межрепная разница, по которой ошибку обычно и ловят, отсутствует. Порядок
`worktree → прогон → git diff → ревью → мерж → push руками`, и push руками — последний
рубеж, единственный шаг, где человек видит **итог**, а не намерение.

**3. Атрибуция коммитов** → `58-git-practice.md` §5а. В базе не было **ничего** по теме,
и ключ `attribution` на машине **не задан**.

Самое неожиданное: **адрес `noreply@anthropic.com` не принадлежит Anthropic** — его
зарегистрировал на себя посторонний пользователь GitHub, и в контрибьюторах приватных
реп числится незнакомый человек. Задача владельцу заведена в `TASKS.md` (настройка —
пять минут).

Отдельно поднята **опасность чистки прошлого именно в этой системе**: перезапись меняет
все хеши, а на каждой версии висит тег и релиз с ассетом — и по канону «один zip = полный
контекст» **ассет и есть поставка**. После перезаписи релиз может указывать на несуществующий
коммит, **ассеты не пересобираются сами**. Плюс два подводных камня: кеш контрибьюторов
(24–48 часов, на малоактивных репах недели) и ref'ы `refs/pull/*/head`, которые GitHub
не даёт перезаписать force-push'ем.

**📍 Что оставлено локальным:** §1 (условия доступа: ключ Console vs подписка) и §2
(разделение зон 🟢/🟡/🔴, ADR-010). Это решение владельца о **его** данных и **его**
репах — предметное, не переносимое. В базе уже стоит ссылка на ADR-010 из `ROADMAP.md`
и `TASKS.md`; дублировать раскладку зон в базу значит завести вторую копию, которая
разойдётся при первом же изменении состава реп.

Отмечу для протокола: **§1 этого документа стоит на посылке, снятой сегодня** — «у Claude
Code нет токена и сетевого выхода». Она верна для браузерной песочницы и неверна здесь
(`64-claude-code-sandbox.md`). Сам вывод §1 — «только ключ из Console» — от этого не рушится,
он про юридические условия, а не про технические возможности.

**Прогресс:** прочитано **11 протоколов из 21**, осталось 10.

**`20-repos-map-registry.md` — ⬆️ повысить, два блока.**

**1. «Если проверку можно пройти случайно — это не проверка»** → `71-fail-loud-and-sourcing.md`
§7в. Лучшая формулировка о конструкции проверок в системе.

Случай: сверка карты делала `grep -q` по имени репы **по всему файлу**. Имя встречается
в оглавлении карты ссылкой — этого достаточно. Проверка отвечала на вопрос **«слово
встречается в файле»** и выдавалась за ответ на **«репа описана»**. Печатала «расхождений
нет» при трёх нехватающих репах.

Важно, что это **не «слабая проверка»**, а проверка **не того**: между её вопросом
и защищаемым свойством нет связи вовсе. Записаны три вопроса к любой проверке:
какое свойство защищает · можно ли пройти, не обладая им · что делает при успехе.

Отграничил от вчерашнего PIT-089 таблицей — там связь со свойством **была**, но узкая
(покрывала один случай); здесь связи **нет**.

**2. «Чистая проверка обязана печатать числа»** → туда же. Молчаливое «всё хорошо» —
половина дефекта: нельзя отличить «сверила и сошлось» от «сверила пустоту». При чистом
прогоне печатается число записей и число реп, чтобы совпадение было **видно,
а не подразумевалось**. Это дисциплина §5 («у каждого утверждения есть источник»),
применённая **к успеху**, а не к отказу.

**3. Определение записи с не-примерами + четыре состояния сверки** → `76-repo-classes.md` §4.1.
Не-примеры здесь важнее примеров: именно на них ломалась проверка. Плюс `STALE` как
**зеркальный дефект** (удалил репу, не вычистил запись — карта врёт в другую сторону,
и это та же поломка, а не мелочь).

### 🔴 Правило применено к себе — и мой собственный гейт его не прошёл

Записав §7в, проверил свой README-гейт тем же вопросом: **можно ли пройти, не обладая
свойством?** Свойство — «описание сообщает состояние репы».

Проба: описание из **одной точки** (`.`) и слово `ок` — **оба прошли**. Проверка
на плейсхолдер ловила `<...>`, `TODO`, `—`, но не ловила короткий мусор.

Починено: добавлен порог содержательности (≥20 символов и ≥3 слов) с объяснением
в коде, три кейса в тесты (`D2`, `D3`, `D4`). Набор **36/36**.

Отдельно: **три старых кейса упали** после ужесточения — их фикстуры («Описание.»,
«Ценное описание.») сами бы правило не прошли. Это не повод ослаблять правило, а признак,
что **фикстуры моделировали то, чего в проде быть не должно**. Приведены к реалистичным.
Порог при этом смягчён с 4 слов до 3: 4 давали ложные срабатывания на коротких,
но осмысленных строках.

> **Урок для метода:** поднятое правило надо **сразу применять к собственным
> инструментам**. Между «записал правило» и «мои гейты ему соответствуют» —
> непроверенное допущение, и в первом же случае оно оказалось ложным.

**Прогресс:** прочитано **12 протоколов из 21**, осталось 9.

**`08-manager-role.md` — 📍 в основном локальный, два блока ⬆️ подняты.**

Сама роль менеджера — про раскладку входящего у планировщика, там ей и место.
Поднято два:

**1. Когда репу заводить НЕ надо** → `76-repo-classes.md` §4в. База отвечала «что нужно,
чтобы репа считалась заведённой», и не отвечала на обратный вопрос — а в системе из полусотни
реп он важнее. Критерий: **у сущности нет собственного контента → это роль, раздел
или процедура, но не репа.** Случай источника: под «менеджера системы» хотели завести репу,
но у него нет своего контента — он бы только пересказывал чужой. Правильная раскладка
трёхчастная: данные / правила / роль. Дописал признаки лишней репы, в том числе полезный:
**сущность описывается глаголами, а не существительными — верный признак роли.**

**2. «Честный статус важнее красивого»** → `45-roadmap-and-tasks.md`. Формулировка
источника точная: *восемь невыполненных из десяти — рабочая информация; десять «примерно
выполненных» — потеря инструмента*. Дописал следствие: список, в котором всё выполнено,
чаще означает **плохой список**, а не хорошую неделю.

📍 Оставил локальным §5 (режимы «разбор / планирование / брейншторм» и правило
не смешивать исполнение с решением в одной сессии). Правило разумное, но **не универсальное**:
эта вахта весь день совмещает чтение, решение и запись, и совмещение работает. У них оно
верно для механического разбора входящего — то есть привязано к предмету.

**`03-global-roadmap.md` — 🔗 свести, три правила ⬆️.**

Потолок «P1 не копится, больше пяти — список желаний» в базе **уже есть** (`45` §2) —
не дублировал. Ново:

1. **P3 — не свалка для того, что жалко отклонить.** «Не ведёт ни к какой цели —
   кандидат в отказы, а не в P3; P3 это *полезное, но не срочное*, а не *непонятно зачем*».
   Дописал, почему это практично: пункт «непонятно зачем» в P3 всплывает на **каждом**
   пересмотре, потому что решение по нему не принято — оно отложено **под видом приоритета**.
2. **Три ритма пересмотра** (еженедельно статусы · ежемесячно приоритеты · ежеквартально
   идеи и отказы) — в базе ритма не было вовсе. Смысл разной частоты: это **разные операции**.
3. **Конкретное действие ежемесячного пересмотра:** пункт, простоявший в P1 месяц без
   движения, — либо не P1, либо заблокирован и уезжает в задачи **с названным блокером**.

**`01-what-lives-here.md` — ⬆️ два проверочных вопроса подняты в `76` §5.**

Самое ценное во всём протоколе — не описание границы, а **вопросы, дающие ответ
за секунды**:

- **«Изменится ли этот файл, если завтра поменяются цели?»** Да → состояние, нет → правило.
  За этим ось: правило меняется редко, осознанно и **версией**; состояние — постоянно.
  Смешал в одной репе — версионирование теряет смысл, замораживать нечего.
- **«Имеет ли запись ценность сама по себе, вне контекста планирования?»** Да → тематическая
  репа, нет → указатель у планировщика.

Дописал следствие: планировщик хранит **только указатели**; копия чужого контента
немедленно расходится с оригиналом и становится вторым источником правды.

**Прогресс:** прочитано **15 протоколов из 21**, осталось 6
(`06-daily-list`, `10-archive-packaging`, `11-habit-tracker`, `15-versioning`, `README`,
а также перечитать `21-git-push-protocol` при ревизии деплойера).

**`10-archive-packaging.md` — ⬆️ три блока подняты в `43-archive-naming-and-packaging.md`.**

**1. Пять служебок в корне, а не две** (§3а). База требовала `README.md` и `VERSION`;
источник даёт пять, и у каждой **своё последствие**. Ключевое: **деплойер без них
не падает — он тихо ведёт себя иначе.** Без `.repo-id` угадывает репу по имени файла
(самый ненадёжный признак), без `CHANGELOG.md` может вовсе не создать релиз. Разбор
источника: из двух архивов подряд ни один не содержал `.repo-id`, один — ещё и `CHANGELOG`,
релиз не создался. Ни то ни другое не выглядело ошибкой в момент упаковки.

**2. Чек-лист отвечается командами, а не по памяти** (§3б). Прямое применение правила,
поднятого этой же вахтой (`71` §7в): пять вопросов базы можно пройти, ответив «да»
по памяти, — значит проверкой они не были.

**3. Деплой запускается после завершения загрузок** (§3в). Файл, который ещё пишется,
выглядит как повреждённый архив; у них прогон отказал на живом, но недокачанном файле.
Класс PIT-014. Плюс макосная ловушка `имя 1.zip` — деплойер считает такие дубликаты
рабочими копиями и **молча не публикует**.

### 🔴 Команды источника пришлось переписать — они сами проходились случайно

Взял три команды из §4 источника и **прогнал на живом архиве**, вместо того чтобы
скопировать. Две из трёх оказались негодными:

- `unzip -l … | grep README` находит `README.md` из **любого подкаталога** и отвечает
  «есть» при пустом корне. В `base-repo` таких `README.md` **33 штуки** по дереву.
- `awk 'NR==4{print $4}'` для имени обёртки вернул **путь вглубь дерева**
  (`…/.claude/skills/handoff/SKILL.md`): формат вывода `unzip -l` зависит от версии.

Переписал на `unzip -Z1` с `awk -F/ 'NF==2'` (только первый уровень внутри обёртки)
и `cut -d/ -f1 | sort -u` (имя обёртки должно быть ровно одно). Проверено на боевом
архиве: пять служебок, одна обёртка. В доке оставил абзац **почему не проще** —
иначе следующая вахта «упростит» обратно.

**Правило подтвердилось на самом себе:** документ, поднимающий канон проверок, содержал
проверки, которые проходятся случайно. Копировать команды из источника без прогона —
то же, что верить саммари.

### Правило применено к упаковщику

`scripts/pack_release.py` требовал `README.md` и `VERSION`. Добавлена проверка всех пяти
с внятным отказом и ссылкой на канон. Проверено: на репе без `.repo-id` валидация ловит.

**Прогресс:** прочитано **16 протоколов из 21**, осталось 5.

## 🏁 Все 21 протокол `mission-control` прочитаны

**`15-versioning.md` — ⬆️ три блока в `44-what-to-version.md`.**
База отвечала «версионировать или вести историю»; источник — на **предшествующий** вопрос:
нужно ли и то и другое вообще. Два отсекающих вопроса: живёт ли дольше одной сессии ·
может ли расхождение копий кого-то запутать. Хоть одно «нет» — версия не нужна,
иначе версионирование **превращается в ритуал**.

Плюс «версия обязана давать три ответа, иначе она украшение» (свежая ли копия · накатано ли
исправление · что изменилось) с формулировкой **версия без записи в журнале бесполезна:
номер вырос, а что за ним — неизвестно**. И общее правило за PIT-063: **версия живёт
в одном месте**; источник — переменная в коде, всё остальное правится **тем же движением**,
а где движение нельзя гарантировать — ставится гейт.

**`06-daily-list.md` — 📍 содержание локальное, два приёма ⬆️ в `21-revision-protocol.md`.**

Сам лист — предмет владельца. Но диагноз его стагнации даёт два переносимых приёма:

1. **«Механизм добавления есть. Механизма удаления нет.»** За год слотов стало 12 вместо 7,
   рутина выросла с 8 пунктов до 13, появился раздел «на рассмотрение» — список кандидатов
   **на добавление**; раздела «на удаление» не появилось. Структура **накапливает,
   не разрешая**.

   **Это касается базы напрямую, и я записал это прямо.** Закон 4 («при сомнении повышать»)
   — явный механизм добавления, сознательно смещённый в сторону лишнего. Он верен **ровно
   до тех пор, пока «снимается» кто-то делает**. Закон создаёт **долг**, платит ревизия;
   не платит — осознанный перекос становится неконтролируемым ростом.

2. **Проверка выполнимости арифметикой, а не на глаз.** Лист сравнили не с ощущением,
   а с сутками: 13–17,5 часа при 16 доступных, **до** еды и дороги. Список невыполним
   не «сложен», а **невыполним арифметически**. Приём снимает спор о вкусе.

**`11-habit-tracker.md` — 📍 локальный, один приём ⬆️.**
**«Добавлять новое — только вместо старого, не сверх.»** Это **конструктивный** ответ
на дефект выше, сильнее дисциплины: «периодически чистить» держится на том, что кто-то
вспомнит, а «только вместо» **не даёт вырасти вообще**. В базе такое уже есть у журнала
(§3 — ровно 10 записей), но как приём нигде не назван. Назван.

**`README.md` навигатора — 🗑 не поднимать, но отметить дефект.**
Навигатор планировщика **сам сломан**: с 12-й строки таблица теряет две колонки из четырёх,
а протоколы **20 и 21 отсутствуют вовсе**. Тот же дефект, что я записал для базы
(её навигатор кончается на 41 из 77). Симметрия подтверждает: **навигатор — первое,
что отстаёт**, потому что его правка не требуется ни одним гейтом.

### Итог по репе

| Каталог | Отметка | Состояние |
|---|---|---|
| `00-infrastructure/` (22) | М | **прочитано 21/21 + README** |
| `scripts/` (23) | М | не начато |
| корень (18) | М | не начато |
| `decisions/` (12) | М | не начато (11 ADR) |
| `reports/` (10) | М | не начато |
| `daily/`, `profile/`, `scripts/tests/` (5) | М | не начато |
| `plans/`, `reports/data/`, `daily/habits`, `daily/source` (11) | С | не читаем, причины записаны |

**Арифметика (`PROCEDURE.md` §6):** всего 102 = база 1 + С 11 + прочитано 22 + в очереди 68.

## `scripts/` — начато, 3 файла из 23

**`collect_to_base.py` (556 строк) — ⬆️ три урока из комментариев к коду.**

Сам скрипт локален (завязан на устройство планировщика), но **комментарии к его правкам —
готовые уроки**, каждый с числом. Подняты в `77-system-wide-audit.md` §4:

1. **«Дырка была не в списке служебок, а в списке контента.»** Инструмент пропускал
   материал, напрашивалось расширить список **служебных** подсказок «по списку реально
   пропущенных каталогов». Замер: **из 369 пропусков 346 оказались контентом**. Расширение
   затащило бы в базу 292 файла разборов и 39 тренерских. Правило: у фильтра **две**
   стороны, при промахе сначала спрашивают, **на какой** он произошёл; расширять сторону
   «брать» — быстрый способ превратить недобор в перебор, и он **необратим дешёвой
   ревизией** (удалять придётся уже из базы, а не из очереди).

2. **Список конечных значений там, где значение открыто.** Префиксы `"01-"…"09-"`
   не опознали `10-…`, **414 файлов** ушли в пропуски. Заменено регуляркой. Тот же
   PIT-074 в миниатюре.

3. **Порядок проверки признаков — часть алгоритма.** `00-infrastructure` начинается
   с числового префикса, то есть по правилу «числовой префикс = контент» главный служебный
   каталог объявлялся бы контентом. Служебная подсказка обязана проверяться **первой**.

**`system_audit.py` (220 строк) — 📍 локален, но найден дефект для планировщика.**

Он и есть инструмент, который описывает базовый `77`. Но его `SERVICE_DIRS` — **зашитый
перечень из четырёх каталогов**, ровно тот дефект, который в соседнем `collect_to_base.py`
чинили и описали в протоколе 17. **Фикс применили к одному инструменту и не применили
к другому** — значит таблица «кандидаты в базу» у аудита слепа к репам, где служебки лежат
не в тех четырёх местах. Класс тот же, что PIT-090 (механизм сменили, старое оставили).

### 🔴 Поправка к вчерашнему PIT-090 — я переоткрыл известное

**У базы уже есть `scripts/legacy_delta.py`**, и его шапка дословно описывает вчерашнюю
«находку»: «есть ли в плоских legacy-копиях базы что-то, чего нет в актуальной базе».
Прогон даёт картину **шире**: 12 реп · **дублей 977 · старых версий 216 · имён,
которых в базе нет — 91**. Мои три имени — верхушка из 91.

Формулировка карточки «не находилось раньше» **неверна**: находилось, инструмент был
написан заранее, его просто не запускали. **Это PIT-072** — переделать готовую
инвентаризацию худшим инструментом и выдать за находку. Правило
`04-watchlog-protocol.md` («прежде чем строить инвентаризацию — искать готовую»)
существовало **до** моего замера и было нарушено мной.

Карточка PIT-090 дополнена поправкой; в `ROADMAP.md` задача переписана на прогон
`legacy_delta.py`; в `PROCEDURE.md` §3а заведён блок «сначала искать готовый инструмент».

**Что остаётся моим и не отменяется:** мера отставания (63 версии), **структурный корень**
(граница режет в обе стороны) и **обратная петля** (сбор вверх предложит поднять в базу
её же устаревший файл). Инструмент **считал** файлы; чего он не объяснял — **почему они
не могут обновиться**. Это законный повод дописать — но заявлять надо **разницу**,
а не всю находку.

### Инструмент вместо документа: таблица 2 сплошной ревизии — исполняема

Заведя вчера `77-system-wide-audit.md`, я описал две таблицы, а инструмента под вторую
у базы не было. Это тот же дефект наизнанку: раньше механизм без документации,
теперь документация без механизма.

**Не стал писать новый скрипт** (по правилу, заведённому часом раньше). Прогнал имеющиеся:
`per_repo_audit.py` уже покрывал таблицу 1 и давал вход для второй (`truly_own` — файлы,
не совпадающие ни с `_base`, ни с каноном). Дополнил его до таблицы 2:

- каталоги служебок ищутся **по подсказке в имени**, обход до трёх уровней — не по зашитому
  списку (урок из `collect_to_base.py`);
- два условия переноса: **частота И совпадение по хешу**;
- разное содержимое под одним именем печатается как **КОНФЛИКТ**, а не кандидат.

**Первый прогон — 22 кандидата, из них 14 конфликтов.** И он **независимо подтвердил
задачу, приехавшую мостом**: `incidents-summary.md` — 11 реп / 3 редакции ·
`investigations-summary.md` — 11 / 3 · `watchlog.md` — 11 / 2 · `LESSONS.md` — 3 репы
и **3 разные редакции**. Планировщик называл пять конфликтных служебок; инструмент нашёл
четыре из пяти сам, не зная о задаче.

**Границы таблицы названы в самом выводе**, а не подразумеваются:

1. показывает только имена, **которых в базе нет** — файл, который в базе есть, а в репах
   разошёлся, сюда не попадёт (это `drift_versions.py`). Именно поэтому `pitfalls.md`
   из списка пяти в таблице отсутствует;
2. сверка **по имени**: переименование прячет файл, и **перенумерация при синтезе**
   (`34-…` → `54-…`) делает старую копию «новым» именем — оба случая видны как кандидаты,
   хотя это протечка базы;
3. плоские legacy-копии считает кандидатами — полную картину даёт `legacy_delta.py`,
   запускать его **перед** разбором таблицы.

> Печатать границы инструмента в его собственном выводе — прямое следствие правила
> «чистая проверка печатает числа» (`71` §7в): вывод без границ читается как полная
> картина, и следующая вахта обопрётся на него как на полную.

**Новое, чего не было ни в одном списке:** `repo-readme-template.md` — 12 реп, одна
редакция, в базе имени нет. Ещё одна плоская протечка того же происхождения, что PIT-090.

**`mirror-clean.sh` (141 строка) — ⬆️ раздел «лечение» в `59-public-mirror-filter.md`.**

База покрывала **профилактику** («зеркало собирается со свежей историей») и не покрывала
**лечение** — что делать, если утечка уже в истории. Поднято:

1. **Проверять два дерева, а не одно.** Проверка по текущему состоянию **слепа**: файл
   достаётся из любого прошлого коммита, а в публичной репе история публична целиком.
   Удаление одним коммитом меняет **удобство доступа, а не факт публикации**.
2. **Перезапись истории у зеркала законна — потому что зеркало производно.** Force-push,
   означающий в обычной репе потерю работы, здесь безопасен: зеркало собирается
   из приватного оригинала и восстанавливается из него в любой момент.

   Ось шире случая: **разрушительная операция дёшева на производном и катастрофична
   на источнике.** Это обратная сторона `72` §4б: производное нельзя **править**,
   зато можно **снести и пересобрать**.

### 🔴 Инструмент противоречит принципу базы

`mirror-clean.sh` ведёт **denylist** (`EXCLUDE=…`), тогда как `59` §«Принцип» требует
**allowlist**. Разница не косметическая: denylist пропускает всё, что в него не внесли —
новый тип служебного файла утечёт **молча**, и узнают снаружи. Allowlist промахивается
в другую сторону: даёт недостачу в зеркале, которую видно сразу.

Это тот же выбор направления промаха, что в уроке сборщика («дырка была не в списке
служебок, а в списке контента»): **при фильтре важно, в какую сторону он ошибается.**
Задача заведена в `ROADMAP.md`.

### Замер на живом зеркале

Прогнал обе проверки на `finpilot` (публичное): **в текущем дереве чисто, в истории чисто.**

**Граница замера названа:** клон `--depth 50`, то есть проверены последние 50 коммитов,
а не вся история. Полная проверка требует полного клона; для вывода «утечек нет вообще»
этого замера **недостаточно**, он говорит лишь «в последних 50 коммитах нет».

**Прогресс по репе:** 21 протокол · 4 скрипта из 23.

## `decisions/` — 11 ADR + README

**Дисциплина ADR — 🗑 не поднимать, в базе уже есть целиком.**
Сквозная неизменная нумерация · ADR не редактируются, передумал → новый со `Superseded
by ADR-NNN` · контекст обязателен · «не на всё, иначе шум» — всё это лежит в
`reports/adr/README.md` и `adr_template.md` базы. Сверил построчно, дублировать нечего.

**ADR-006 «указатель в исходной папке» — ⬆️ поднято в `76-repo-classes.md` §4г.**

База описывала заведение репы **с нуля**, а тему чаще **выносят** из существующей.
Решение: содержимое переезжает, в исходной папке остаётся `README.md` с указателем —
место в структуре сохранено, история цела в обоих местах, **канон один**.

Ценны отвергнутые варианты, каждый с конкретной причиной: полная копия — **два источника
правды, через полгода вопрос «какая актуальна» неразрешим** · удалить — теряется след
эволюции структуры · симлинк — **между репозиториями не работает**, ни в git, ни в zip.

Дописал ловушку для ревизии: папки-указатели выглядят как **потерянный контент** (раздел
есть, содержимого нет), и возникает соблазн «восстановить». Признак указателя назван;
если целевой репы не существует — **вот это находка**, и её разбирают.

### 🔴 Проверил исполнение — решение записано, но не исполнено

ADR-006 называет область влияния: «касается `edu-base` (три папки) и `it-base` (одна)».
Прогон по системе нашёл **две** папки-указателя: `it-base/03-machine-learning`
и `academic-portfolio/10-mephi-2019-2022/study-plans` (последней в ADR нет вовсе).

**Все три папки `edu-base` содержат материал, а не указатели:** `04-linguistics` (5 файлов),
`01-learning-how-to-learn` (4), `03-logic` (1).

Хуже: имена файлов `edu-base/04-linguistics` **не пересекаются ни одним** с репой
`linguistics` (12 файлов в структуре `01-theory`/`02-english`/`03-methods`/`04-practice`).
То есть репа построена **заново по другой структуре**, а оригиналы остались на месте.
Что это — покрытый переработкой материал или неперенесённое — по хешам не решается,
нужен разбор содержания. Заведено в `ROADMAP.md` §P2.

> **Класс находки:** документ описывает состояние, **которое не наступило**. Отличается
> от привычного «документ отстал»: здесь он не отстал, а **опередил** — зафиксировал
> намерение как факт. Проверять надо не только «актуально ли записанное», но и
> «сделано ли записанное».

**Реестр ADR тоже отстал:** `adr_011_data_in_git_site_is_client.md` лежит на диске,
в таблице реестра его нет — таблица кончается на 010. Тот же паттерн, что навигаторы
`00-infrastructure` в обеих репах: **список отстаёт первым, потому что его правка
не требуется ни одним гейтом.**

## Наблюдение превращено в гейт — PIT-091

Паттерн «список отстаёт первым» встретился **трижды за сутки, независимо**: навигатор базы
(41 из 79), навигатор `mission-control` (теряет колонки с 12-й строки, протоколов 20–21 нет),
реестр ADR (кончается на 010 при существующем `adr_011`).

Три независимых случая одного класса — это не совпадение, а **отсутствие принуждения**:
документ добавляют, файл лежит, ссылки ведут, **и ни один гейт не спрашивает, попал ли он
в список**. Правило держится на памяти, память кончается на пятнадцатом документе.

**Сделано, а не записано:**

1. `scripts/revision_check.py` → `check_navigator()`. Сверяет пронумерованные документы
   на диске с тем, что перечислено в навигаторе; принимает и имя файла, и номер
   в начале строки таблицы (навигаторы ведутся обоими способами). При чистой сверке
   **печатает числа** — «78 из 78», иначе «ок» не отличить от «сверил пустоту».
2. **Навигатор базы дописан**: 38 недостающих строк, 41 → 78. Каждая с однострочным
   описанием; у поднятого этой вахтой описание называет **главное** содержание
   (`71` — «если проверку можно пройти случайно», `78` — «`_base/` чужое, всё вне своё»).
3. Заведена карточка **PIT-091** (реестр 91 → 92).

### Гейт в первом же прогоне нашёл мёртвый файл

Среди «не перечисленных» оказался `00-infrastructure/00-navigator.md` — **устаревшая
кириллическая редакция навигатора ЧУЖОГО кита** (`01-claude-context/`), оставшаяся после
переименования файлов в латиницу. Проверил: все три файла, на которые она ссылается
(`КАК_ПОЛЬЗОВАТЬСЯ_инструкциями.md`, `СБОРКА_*`), **не существуют**; актуальная редакция
лежит в `01-claude-context/00-navigator.md`.

**Ревизия ссылок её не видела**: имена стояли в обратных кавычках, а не markdown-ссылками —
то есть класс «проверка не ловит этот класс ошибок» ещё раз, уже в моём инструменте.

Удалён после проверки восстановимости: файл есть в опубликованном теге `v1.61.0`,
sha `c7468669`. Локального git у базы нет, поэтому восстановимость проверялась
**на GitHub**, а не предполагалась.

## 🔴 `reports/pitfalls-append-2026-08-16.md` — сведение пяти линий этот файл ПРОПУСТИЛО

Проверка обещания кампании «ничего не потеряв» на конкретном файле. У планировщика
лежит дозапись в реестр граблей на 148 строк, четыре карточки. **Ни одна не попала
в реестр базы**: под номерами 016–019 там совершенно другие уроки, из другой линии
нумерации.

То есть при сведении 110 входных записей в 62 урока **этот файл в вход не вошёл** —
и обнаружилось это только потому, что я открыл его глазами, а не доверился итогу сведения.

Подняты как **PIT-092…095** (реестр 92 → 96):

- **092 — диагностика в stdout попадает в значение переменной.** Заголовок релиза
  **публичного** зеркала начинался с ANSI-мусора, потому что громкая функция печатала
  в stdout, а её вызов был обёрнут в `$( )`. Правило: **stdout функции принадлежит
  её результату**, всё для глаз — в stderr. Ценна защита **в три слоя**: причина ·
  проверка кода возврата · санитайзер на выходе. Для публикуемого постороннему
  одного слоя мало.
- **093 — проверка на упоминание вместо проверки на запись.** Существо я уже поднял
  сегодня в `71` §7в из протокола 20; карточки в реестре не было, а реестр — это
  поверхность поиска. Добавил с перекрёстной ссылкой. Отдельно ценно: починка заменила
  **булев ответ на четыре состояния** — «сходится/нет» скрывал три разные болезни.
- **094 — точка входа в вахту протухает молча. Повторов: 4 подряд.** Версия в шапке
  поднимается **скриптом**, тело правится **руками** — рассинхрон незаметен именно
  потому, что шапка свежая. Формулировка, ради которой стоило поднимать: документ-точка
  входа без машинной проверки свежести — **самый опасный файл в репе, он выглядит
  достоверно и врёт**.
- **095 — самопальный парсер вместо канонического.** `sed`-диапазон обрывался на первой
  пустой строке, а канон ставит её сразу после заголовка секции → **тело релизов ушло
  пустым**. Корень не в `sed`: инструмент был исправен, **канон обошли**. Признак
  для себя: пишешь regex по файлу, для которого в системе есть читающий инструмент, —
  спроси, почему не вызвать его.

### Карточка обещала машинную проверку — поставил её, а не оставил обещанием

PIT-094 говорит «документ-точка входа обязан иметь машинную проверку свежести».
Оставить это текстом значило бы завести ровно тот дефект, о котором карточка.

`scripts/revision_check.py` → `check_watchlog()`: версия в §0 сверяется с `VERSION`,
расхождение — **FAIL**, отсутствие машиночитаемой строки — отдельное сообщение
(«§0 не читается машиной»). Проверено пробой на рассинхроне: ловит. На боевой репе
печатает **числа** — «§0 совпадает с VERSION (1.79.0)», а не «ок».

**Граница названа честно:** машина проверяет только **совпадение версии**.
Содержательную свежесть она не проверит — для неё ручной приём «три утверждения»
из `04-watchlog-protocol.md`.

> **Урок для кампании:** итог сведения — тоже саммари. «110 записей → 62 урока» звучит
> как завершённая работа, но проверяется только открытием входных файлов поимённо.
> Этот файл лежал в `reports/`, а не в `00-infrastructure/`, и потому не попал
> ни в один список служебок.

**`reports/2026-08-04-deploy-incident.md` — 🔗 в основном уже поднято, §2.6 ⬆️ новое.**

Шесть из семи симптомов разбора я уже поднял вчера-сегодня из протокола упаковки:
служебки в архиве · имя обёртки · `имя 1.zip` · точки vs подчёркивания. Не дублировал.

**§2.6 — новое и сильное**, поднято в `71-fail-loud-and-sourcing.md` §7г:

> **Подавленный `stderr` плюс зашитая догадка = диагноз невозможен.**

Скрипт печатал «5 попыток, сетевой сбой — обычно TLS-таймаут к GitHub». Звучит как
диагноз. На деле он **глотал `stderr` от `git clone`**, а фраза про TLS была **строкой,
зашитой в код**. Настоящих кандидатов было четыре (сеть · протухший токен · DNS ·
лимит запросов), все требуют **разных действий**, и ни одно не следует из «сетевого сбоя».

**Ключевое, что дописал от себя:** зашитое объяснение **опаснее отсутствия объяснения**.
Пустое «не удалось» заставит разбираться; правдоподобная догадка **останавливает разбор** —
по ней начинают чинить не то. Там «сетевой сбой» увёл в проверку VPN, тогда как причина
могла лежать в токене.

Связал с PIT-092 как обратную сторону: **`stdout` — результат, `stderr` — для глаз,
и ни один не заменяется выдумкой.**

### Правило применено к собственным инструментам — и нашло дефект

Проверил инструменты базы на тот же дефект. `gh_json()` в `sync_repos_zip.py`
и `verify_repos_zip.py` использовал `subprocess.run(..., check=True)`. Это выглядит
аккуратно, но **`CalledProcessError` сообщает только код возврата** — текст `gh`
(«Invalid username or password», «Could not resolve host», «429») **захватывался
и отбрасывался**.

То есть четыре разные причины, лечащиеся по-разному, выглядели бы одинаково: «код 1».

Починено в обоих: явная проверка кода возврата, `stderr` включается в сообщение.
**Проверено пробой** на несуществующей репе — раньше было бы «код 1», теперь:
`gh repo view … → код 1 | GraphQL: Could not resolve to a Repository…`

> Третий раз за вахту поднятое правило ловит дефект в моих же инструментах
> (README-гейт · команды упаковки · теперь `gh_json`). Это перестало быть совпадением:
> **правило, добытое из чужого разбора, надо сразу прогонять по своему хозяйству.**

**`reports/2026-08-13-old-repos-migration.md` — ⬆️ четыре урока, один исправляет метод кампании.**

### 🔴 Главное: хеш надёжен только при одинаковой нормализации обеих сторон

Правило `PROCEDURE.md` §3 гласит: «хеш — единственный фильтр, которому можно доверять».
Я опирался на него **всю вахту**. Оно верно и неполно.

**Случай:** одиннадцать ноутбуков едва не задвоились. В целевой репе они лежали
**с выводами ячеек**, из источника шли **очищенными** — код тот же, хеши разные.
Сверка честно ответила «этих файлов нет», и они поехали бы второй копией. После
нормализации **обеих** сторон «новых» стало **2 вместо 13**.

**Хеш не врёт — он отвечает на «одинаковы ли БАЙТЫ», а спрашивают «одинаково ли
СОДЕРЖАНИЕ».** Дописал список того, что расходится, не меняя смысла: выводы ячеек ·
концевые пробелы и финальный перевод строки · CRLF/LF · порядок ключей в JSON · BOM ·
время модификации внутри архива.

Правило записано в обе стороны: `PROCEDURE.md` §3 (метод кампании) и `77` §4 (канон базы).
**В синтезе ложное «различаются» означает дубль**, поэтому цена ошибки здесь
несимметрична.

### Остальные три

**Двоичное определяется по содержимому, а не по расширению.** `task1`, `new_file`,
`program` — бинарники по 2.4 МБ, по имени неотличимые от текста; отсекаются
по **нулевому байту в первых 8 КБ**. Та же ось, что «имя ничего не гарантирует».

**Секрет останавливает прогон, а не пропускается.** Причина названа точно: молчаливый
пропуск оставляет шанс, что **при следующей правке фильтра файл проедет**. Остановка
не даёт этому шансу возникнуть.

**Экспорты не содержат `.git`** — «Download ZIP» отдаёт снимок дерева. Даты, авторство
и эволюция существуют **только на GitHub**. Это прямо касается **инструмента базы**:
`sync_repos_zip.py` работает ровно так же. Записал границу в его шапку: копия годится
для чтения и сверки хешей, но **не для вопросов «когда появилось» и «кто менял»**;
перенос из такого снимка не сохраняет историю, поэтому репу после переноса **архивируют,
а не удаляют**; нужна история — `gh repo clone`.

> Четвёртый случай за вахту, когда чужой урок применяется к моему инструменту.
> И первый, где урок исправляет **метод самой кампании**, а не инструмент.

**`reports/2026-07-31-consolidation-brainstorm.md` — 🔗 в основном уже поднято, одно ⬆️.**

Таблица §5 «что рассматривалось и отвергнуто» — семь пунктов, шесть из которых я уже
поднял поштучно (submodule/subtree → `78` §6 · полная копия при выносе → `76` §4г ·
автоповышение в базу → `78` §4 · массовое переименование → `76` §6 · удаление старых реп
после переноса → отчёт о миграции). Не дублировал.

**Новое — `72-source-of-truth.md` §4в: «канон обязан помещаться в канал доставки».**

Общая формулировка, объясняющая сразу три отказа (внешний трекер · канвас · submodule):
**канон должен доезжать туда, куда доезжает работа.** Здесь работа доезжает архивом,
значит содержимое, не попадающее в архив, существует **только пока есть доступ к сервису**,
и сессия, работающая с архивом, канона не видит.

Проверка перед объявлением чего-либо каноном названа одной строкой: **окажется ли оно
в архиве целиком.** Нет — это вид или вспомогательный инструмент. Пользоваться можно,
каноном быть нельзя.

### Замер: наблюдение отчёта устарело в ХОРОШУЮ сторону

Отчёт от 31.07 фиксировал: «профиль GitHub работает против владельца — 28 публичных
**старых** реп (`hello-world`, `intro`, `sem3-lab1-cpp`) и **ноль публичных живых**,
внешняя картина хуже реального уровня».

Проверил сегодня: публичных реп **пять**, все свежие и осознанные —
`vevdokimovm.github.io` (визитка) · `finpilot` (зеркало флагмана) · `bron-kerbosch`,
`health-report-generator`, `vk-graph` (витрина ADR-009). Приватных 52. Старых публичных
нет ни одной.

**Пункт закрыт, и это стоит сказать владельцу**: он касается его главного вектора
(наём под миграцию), а закрытым нигде не отмечен. Осталась открытой только надстройка —
`NOTES.md` каждому витринному зеркалу (задача в доске планировщика).

> Проверять надо не только «сделано ли записанное» (вчерашний класс), но и **«не устарела ли
> сама проблема»**. Отчёт описывал состояние, которое с тех пор починили, — и продолжал
> читаться как открытое.

**`scripts/cycle.sh` — ⬆️ в `08-automation-triggers.md`: «конвейер: порядок и есть содержание».**

Оркестратор **ничего не делает сам** — вызывает готовые инструменты в единственно верном
порядке, и **каждое ограничение порядка имеет названную причину**: раздать базу до ревизии
бессмысленно (нечего сверять), собрать кандидатов после публикации — значит отложить
их на месяц.

Поднята **проверка, что порядок продуман**: к каждому шагу можно назвать, **что сломается,
если поставить его раньше или позже**. Не называется — порядок случайный, и его переставят
при первой правке.

Плюс три режима, без которых конвейер не переживёт первого сбоя (`FROM=N` · `ONLY_STEP=N` ·
`DRY=1`) и **предусловия до первого шага** — иначе конвейер упадёт на четвёртом шаге
и оставит систему в промежуточном состоянии.

> Отдельно отмечу совпадение: их правило «сбой печатает команду продолжения»
> (`FROM=3 zsh cycle.sh`) — ровно то, что я сегодня добавил в деплойер 4.17.0,
> придя к нему независимо из другого случая. Два источника, один вывод.

**`scripts/sync-base.sh` — ⬆️ сильнейший урок безопасности за репу, PIT-097.**

Комментарий строк 15–26 плюс предохранитель в строке 56:

> **Список — намерение. Свойство объекта — факт. Предохранитель ставится на факт.**

**Инцидент 08.08.2026:** база с внутренними протоколами и картой приватных реп уехала
в **публичное** зеркало — потому что репу **забыли внести в список исключений**.
Ни сложной ошибки, ни гонки: отсутствующая строка.

**Второй эпизод, снимающий возражение «взяли бы allowlist»:** 15.08 три витринных зеркала
были **открыты в public раньше**, чем попали в список. Утечки не случилось только потому,
что раздача в это окно не запускалась — **повезло**. Список любого знака ведёт человек,
и он отстаёт ровно тогда, когда мир меняется без его ведома.

Починка — спросить саму репу (`gh repo view --json isPrivate`) и не слать публичной
**независимо от списка**. Список остаётся для скорости, но перестаёт быть единственной
защитой. Поднято в `59-public-mirror-filter.md` и карточкой **PIT-097** (реестр 97).

### Проверил применимость к своим инструментам — не применимо, и это результат

По правилу «поднятое сразу прогоняй по своему хозяйству» проверил базу:

- `sync_repos_zip.py` — только **скачивает** к себе, утекать нечему;
- `MIRRORS` в деплойере — выбирает, **что обрабатывать**, а не что кому отправлять;
  решения о раздаче базы деплойер не принимает.

Безопасно-критического списка в инструментах базы нет. **Записываю это явно**, чтобы
следующая вахта не искала заново: проверка сделана, вывод отрицательный.

**Прогресс:** 21 протокол · **6 скриптов из 23** · 11 ADR · 4 отчёта.

**`scripts/audit_service_coverage.py` — ⬆️ приём уровнем выше всего, что поднималось.**

Скрипт существует ради одного: **измерить слепую зону другого скрипта**. Формулировка
источника точная — вопрос «а точно ли сборщик собирает всё» **без измерения не имеет
ответа**. Не «скорее всего да», а не имеет: пропущенное по построению невидимо.

Поднято в `77-system-wide-audit.md` §4 с разницей, которую стоит держать в голове:
**гейт отвечает «прошло/не прошло» по известному критерию, измеритель — «сколько ты
не видишь»**, то есть работает там, где критерия нет, и именно там живут молчаливые
потери (PIT-074).

Второй урок оттуда же: **измеритель обязан импортировать логику, а не повторять её.**
Первая их редакция копировала списки с примечанием «разъедутся — аудит соврёт».
Формулировка, которую записал: **измеритель, дублирующий логику измеряемого, врёт тем
убедительнее, чем дольше живёт** — он честно меряет *другой* фильтр.

### Приём построен и окупился в первом же прогоне

Не оставил обещанием: завёл `scripts/audit_coverage.py`.

**Первый прогон: покрытие 22.5%**, 4749 `.md` вне видимости. И среди невидимых —
`incidents/`, `situations/`, `merges/`, `adr/`. Это подкаталоги `reports/`, то есть
**служебка**: фильтр смотрел только на **имя самого каталога** и не видел, что предок
служебный.

Исправлено — предикат `is_service_dir()` проверяет **все сегменты пути**.
**Покрытие 22.5% → 46.1%, вернулось 1445 файлов.**

**И я сам нарушил только что поднятое правило:** первая редакция измерителя импортировала
**константы**, но дублировала **логику обхода**. То есть ровно то, о чём урок. Переписал
на импорт предиката; в шапке обоих файлов сказано, что дублировать его запрещено.

**Остаток слепой зоны — 53.9%, и он читается спокойно:** `knowledge/items/` (417) ·
`profiles/` (366) · `notion-reflections/entries/` (321) · `heroes/` · `men_profiles/` ·
`materials/`. Это контент, ровно как в их разборе («из 369 пропусков 346 оказались
контентом»). Разница в том, что теперь это **измерено, а не предположено**.

**Прогресс:** 21 протокол · **8 скриптов из 23** · 11 ADR · 4 отчёта.

**`scripts/RUNBOOK.md` — ⬆️ жанр целиком в `08-automation-triggers.md`, плюс урок в `32`.**

База упоминала слово «runbook» **одной строкой** в диспетчере отчётов и нигде не говорила,
**что в нём должно быть**. Поднят жанр с четырьмя частями, из которых третью и четвёртую
обычно забывают:

**Часть 3 — таблица «если падает», по СИМПТОМУ, а не по причине.** Человек в момент отказа
причины не знает; у него есть **строка на экране**. Таблица, отсортированная по причинам,
в этот момент бесполезна. У них 16 строк вида «`HTTP 410` → эндпоинт мёртв, проверить
версию API в URL».

**Часть 4 — раздел «это не сбой».** Скрипт печатает `⊘ публичные зеркала пропущены`, и это
**защита**, а не ошибка. Молчащий об этом раннбук приводит к худшему исходу, чем испуг:
читающий **обойдёт защиту**, решив, что чинит поломку. Правило записано: любое тревожно
выглядящее сообщение, являющееся нормой, **обязано быть названо нормой**.

**Ловушка двухшаговых команд** — отдельно, потому что тонкая. Разбор и `--apply`
показываются двумя строками подряд, и источник прямо предупреждает: **не вставляй обе
одной пачкой — вторая выполнится сразу, посмотреть разбор не успеешь**. Смысл двух шагов
в **паузе между ними**; вставленные вместе, они становятся одним шагом с лишней строкой.
То есть **способ подачи может свести на нет саму защиту**.

**⬆️ `32-git-hooks-and-secret-scanning.md` — секрет в `argv` виден всей машине.**

Правила базы закрывали попадание секрета **в репу** и молчали про второй канал —
**запуск скрипта**. Аргументы командной строки видны любому процессу (`ps aux`)
и попадают в историю оболочки; переменная окружения — нет.

Три следствия, которых в базе не было: флаг `--token` **не заводится вовсе**, иначе им
воспользуются · **история оболочки тоже архив**, разово вставленный токен переживёт
перевыпуск · засветился — **отзывать**, чистка `~/.zsh_history` без отзыва ничего не решает.

**Проверка своих инструментов — чисто.** Скрипты базы секрет аргументом не принимают;
`gh_token()` берёт его у `gh auth token` и кладёт в HTTP-заголовок, а не в `argv`.
Записываю явно, чтобы не проверяли заново.

**Прогресс:** 21 протокол · **9 скриптов из 23** · 11 ADR · 4 отчёта.

---

## 2026-08-21, вахта VI — сверка нашла ВТОРУЮ КОПИЮ РЕПЫ, и это отменило половину моей же сверки

### Как это вскрылось — порядок важен, потому что первый вывод был неверный

Перед работой пересчитал очередь с диска (`scratchpad/queue_audit.py` — только
перечисление и хеши, решений не принимает, `PROCEDURE.md` §7). Считал по
`~/Documents/mission-control` и получил четыре расхождения с записью «Итог по репе»:
102→103 файла, база 1→2, С 11→12, «скрытых каталогов нет»→есть `.claude/`.

**Записал их как находки. Это была ошибка.** При чтении `push-inbox.sh` журнал прогонов
показал **одну** строку, тогда как `ROADMAP.md` заявлял «мост проверен на двух репах».
Расхождение не сходилось ни в какую сторону — и поиск копий скрипта по диску дал ответ:

```
~/Documents/mission-control/scripts/push-inbox.sh
~/Documents/система_репозиториев/mission-control/scripts/push-inbox.sh
```

**Репа лежит в двух местах, и `PROCEDURE.md` §1 указывает на второе**
(`cd ~/Documents/система_репозиториев/$R`). Я читал не ту.

### Что оказалось верным, а что моей ошибкой

| Расхождение, которое я записал | После пересчёта по канону |
|---|---|
| всего 103, а не 102 | ❌ **моя ошибка** — в каноне ровно **102**, запись была верна |
| совпало с базой 2, а не 1 | ❌ **моя ошибка** — в каноне **1**, запись была верна |
| есть скрытый `.claude/` | ❌ **моя ошибка** — только в устаревшей копии |
| каталоги **С** — 12, а не 11 | ✅ **подтверждено**, `102 = база 1 + С 12 + 89` |

Формулировка «сверка отменила три утверждения саммари», которую я поставил в заголовок
до проверки, **сама оказалась тем, против чего направлен метод**: вывод был записан
раньше, чем установлено, о каком объекте он сделан.

> ### 🔴 SYN-017 — сверка с диском ничего не стоит, если не назван КАКОЙ диск
>
> «Диск, а не док» (`STANDARD.md`, закон 1) молча предполагает, что диск **один**.
> Когда репа лежит в двух местах, команда честно показывает состояние — **не того
> объекта**, и результат выглядит как находка, а не как ошибка адреса.
>
> **Правило:** первая команда прохода по репе — не листинг, а
> `find ~ -type d -name "<репа>" -not -path "*/.git/*"`. Копий больше одной →
> **сначала установить канон**, потом считать. Заведено `SYN-017`.

### 🔴 Одинаковый `VERSION` при разном содержимом — и каждая копия новее в своём

Обе копии несут `VERSION` = **1.12.0**. Различий четыре файла:

| Файл | `~/Documents/mission-control` | `~/…/система_репозиториев/mission-control` |
|---|---|---|
| `BOARD.md` | **18 секций** | **60 секций** ← работа 21.08 |
| `scripts/push-inbox.sh` | **v1.0.0**, подстрочный поиск | **v1.2.0**, точное имя + два guard'а |
| `scripts/push-inbox-history.log` | 1 строка | **70 строк**, включая боевые `apply` |
| `scripts/deploy.sh` | **4.17.0** | **4.16.1** ← отстал |
| `.claude/scheduled_tasks.lock` | есть | нет |

**Ни одна копия не является каноном целиком.** Канон по содержанию репы — вторая;
канон по деплойеру — первая. Версия при этом одна и та же, то есть **единственный
машинный признак расхождения не работает**: он объявляет копии идентичными.

Это `72-source-of-truth.md` в чистом виде, но с новым поворотом: там разобран случай
«копия отстала», здесь — **копии разошлись встречно**, и «поднять отставшую» не является
решением, потому что отставших две в разных местах.

### 🔴 Гейт версии деплойера рапортует CLEAN, потому что смотрит в список из головы

`scripts/deploy_version_gate.py`, `KNOWN_COPIES` — **три пути, вписанных руками**:
канон, `~/Documents/mission-control/scripts/deploy.sh`, `~/Downloads/deploy.sh`.
Все три на 4.17.0, гейт зелёный, и запись «три копии байт-в-байт» **формально верна**.

Поиск по диску вместо списка (`find ~ -name deploy.sh`) даёт **108 копий**. Из них
`_base/templates/` — это вложенный слой базы, ему отставать положено по построению
(`78-base-embedding.md`), и он не в счёт. Но **запускаемых копий вне `_base/` три,
и все отстали**:

| Путь | Версия | Чем опасна |
|---|---|---|
| `~/Documents/система_репозиториев/mission-control/scripts/deploy.sh` | **4.16.1** | лежит в **каноническом рабочем дереве** по `PROCEDURE.md` |
| `~/Documents/deploy.sh` | **4.15.0** | голым файлом в корне `Documents`, запускается случайно |
| `~/Downloads/_sync-base/mission-control/scripts/deploy.sh` | **4.13.0** | плацдарм раздачи |

Это **тот же дефект, из-за которого написан `00-CLAUDE-STOP.md` §4** (откат деплойера
на 13 версий): инструмент публикации всей системы существует в нескольких экземплярах,
и защита от разъезда — **список, написанный головой**. Класс PIT-074 применён к самому
гейту: *«список пишется головой, голова не знает структуры»*.

**Разница с прошлым разом важна:** тогда отстала копия **в базе**, и это было видно по
документации. Сейчас копии отстали **вне периметра гейта**, то есть невидимы вовсе —
`--sync-copies` до них не дотянется, потому что не знает о них.

**Исправление — не дописать три пути в список, а сменить способ:** копии ищутся
`find`'ом по свойству (имя файла + наличие `SCRIPT_VERSION`), `_base/` исключается
явным предикатом. Ровно тот же переход, что сделал `audit_coverage.py` в v1.85.0
(`is_service_dir()` вместо списка имён). Заведено в `ROADMAP.md` §P2.

> **Почему не чиню прямо сейчас:** правка гейта трогает `deploy.sh`-контур, а по
> `00-CLAUDE-STOP.md` §4 это тянет прогон гейта, тесты 279/279 и раскладку копий.
> Это отдельный батч с проверкой, а не побочный эффект прохода по синтезу. Записано
> как открытое, чтобы не потерялось (`STANDARD.md`: находка фиксируется в момент).

> ### 🔴 Арифметика §6 сошлась при двух неверных слагаемых
>
> Запись гласила: `всего 102 = база 1 + С 11 + прочитано 22 + в очереди 68`.
> Сумма **сходилась** — и именно поэтому проверка §6 её пропустила: С занижен на 1,
> очередь завышена на 1, ошибки **скомпенсировали друг друга**.
>
> Это граница метода, которую надо назвать вслух. **Сходимость суммы доказывает, что
> ничего не потеряно, и НЕ доказывает, что каждое слагаемое верно.** Компенсирующая
> пара ошибок проходит проверку насквозь.
>
> **Что с этим делать:** слагаемые считаются **машиной поимённо**, а не глазами по
> переписи. Сумма остаётся как проверка полноты, но перестаёт быть проверкой
> правильности разметки. Заведено `SYN-015`.

**Пересчитанная арифметика по канону (`система_репозиториев/mission-control`):**

```
всего 102 = база 1 + С 12 + М-очередь 89
М-очередь 89 = 00-infrastructure 22 ✅ + decisions 12 ✅ + reports 10 ✅
             + scripts 23 (прочитано 7) + корень 18 (0) + прочие 6 (0)
```

---

## 🔴 Решение прогона было записано и НЕ исполнено — проверено по всем восьми

Проверил исполнение **каждого** ⬆️ из прохода по протоколам, не выборочно:

| Решение | Заявленный адрес | Факт |
|---|---|---|
| `07-repo-classes` | `76-repo-classes.md` §4г | ✅ на месте |
| `14-system-audit` | `77-system-wide-audit.md` §4 | ✅ |
| `13-watch-discipline` §3а | `04-watchlog-protocol.md` | ✅ |
| `13-watch-discipline` §3б | `72-source-of-truth.md` §4а | ✅ |
| `15-versioning` | `44-what-to-version.md` | ✅ |
| token lab | `61-token-analytics.md` §5 | ✅ |
| blindness | `70-independent-review-blindness.md` | ✅ |
| секрет в `argv` | `32-git-hooks-and-scanning.md` | ✅ |
| **`04-capture-protocol` ⬆️ повысить** | **адрес не назван** | ❌ **не исполнено** |

Проверка: `grep -rl "дренаж\|воронк" 00-infrastructure/ templates/` → **пусто**.
`20-knowledge-capture-protocol.md` — про другой предмет, как и записано было тогда же.

**Закономерность, а не случайность.** Не исполнено ровно **первое** решение прогона.
Все последующие исполнены — потому что практика «решил → сделал в том же ходе»
завелась позже, на разборе ADR-006 (см. выше «Проверил исполнение — решение записано,
но не исполнено»). То есть **урок был извлечён из чужого решения и не применён
задним числом к своим**. Заведено `SYN-016`.

**Отличие от адреса-заглушки:** остальные восемь записей называли **конкретный файл
и параграф**, и это ровно то, что сделало их проверяемыми. Запись «⬆️ повысить» без
адреса непроверяема по построению — она выглядит исполненной.

**Закрыто сейчас:** ⬆️ заведён `00-infrastructure/79-capture-funnel.md`.

---

## `reports/` — дочитаны все 10, разобраны 6 оставшихся

### `2026-08-12-gh-git-automation.md` ⬆️ → `80-mass-repo-automation.md`

Проверка на дубль по **структуре**, не по теме (закон 4а). Ближайшие четыре документа
базы вскрыты `grep '^## '`:

| Тема отчёта | Покрытие в базе |
|---|---|
| отказ push при гонке, `--force-with-lease` | **есть**, `58-git-practice.md` §3а — 🗑 не дублировать |
| submodule / subtree / template | **частично**, `78-base-embedding.md` §6 — сведено ссылкой |
| лимиты **размеров** файлов и репы | **есть**, `10-git-reference.md` §3, `13-…` |
| лимиты **API** и заголовки `x-ratelimit-*` | **пусто** |
| стратегии клона (`--depth`/`--filter`/`--sparse`) | **пусто** |
| `set -Eeuo pipefail`, подвохи `set -e`, тихий выход 0 | **пусто** в `00-infrastructure/` |
| scopes `gh`, PAT classic vs fine-grained, CVE-2024-53858 | **пусто** |
| Actions: `GITHUB_TOKEN` не триггерит чужие workflow | **пусто** |
| `git pull` в скрипте опасен → `fetch` + `reset --hard` | **пусто** |

Семь пустых строк из девяти — подъём обязателен. **Что добавлено от себя, из практики
системы, а не из отчёта:**

- **§5 «три способа потерять работу молча»** — собрано из `backlog-2026-08-15-final.md`
  (пп. 24–26) и сведено в один класс: *скрипт отчитался успехом*. Затронутость по одной
  стороне операции · ключ сравнения без пути · область сканирования из списка.
- **Правило «числа лимитов не хардкодить»** привязано к PIT-091: константа в коде —
  тот же снимок, что число в прозе.
- **Следствие про ассет релиза** (п. 17 бэклога): раздача базы намеренно не делает
  релизов, значит **ассет последнего релиза содержит базу на момент того релиза**.
  Это ловушка для того, кто берёт базу из ассета.

### `2026-08-13-claude-code-research.md` ⬆️ → `81-claude-code-privacy-and-billing.md`

Отчёт **уже наполовину выбран** прошлыми вахтами, и это надо было проверить до подъёма:
`69-agents-hooks-and-gates.md` §4а содержит «где решает смысл — модель, где решает
совпадение — скрипт» дословно, `64-claude-code-sandbox.md` — границы прав.
🗑 не дублирую ни то, ни другое.

**Не покрыто ничем — и это самое дорогое в шести отчётах:**

1. **Путь входа определяет юридический режим.** Подписка — обучение включено, хранение
   **5 лет**; ключ Console — обучения нет, хранение 30 дней. Проверка: `grep` по базе
   на «5 лет / 30 дней / обучение на данных» даёт только личные файлы профиля, ни одного
   инфраструктурного. `50-parallel-accounts` про Console есть, но про **Admin API
   и объединение аккаунтов** — другой предмет.
2. **`FileVault` в базе не упоминается вообще** (`grep` → 0). При том что транскрипты
   лежат локально открытым текстом 30 дней — без шифрования диска выигрыш от
   Console-ключа теряется на локальной копии.
3. **Биллинг:** ключ в окружении биллит по токенам **даже при активной подписке**,
   и предупреждения об этом нет. Выбор молчаливый.
4. **«Ограничение доступа должно быть физическим»** — 🔗 **сведено**, а не поднято как
   новое: это тот же принцип, что `59-public-mirror-filter.md` §«Список — намерение.
   Свойство объекта — факт». В `81` он назван своим именем и связан с оригиналом.
   Домен другой (каталог вместо репы), формулировка одна.

### `2026-08-08-notes-import.md` 🔗 свести → `79-capture-funnel.md` §4, §5

Три вещи, которых не было в `04-capture-protocol.md`:

- **Третье предупреждение об экспорте.** В решении по `04` записаны два (пароли
  в выгрузке, locked-заметки). Третье — **плоский текстовый экспорт не сохраняет
  состояние чекбоксов**: на весь файл нашлось 32 `✅`, и все — заголовки секций.
  По `02-entity-types.md` §4 статус несёт только чекбокс, значит статус терялся **целиком**.
- **Приём восстановления:** статус берётся **по границам секций**, а не по маркерам
  строк — импорт идёт по структуре документа. Остаток без разметки принимается
  **открытым** (безопасная сторона ошибки, закон 4).
- **Замер, подтверждающий PIT-091 на живом случае:** служебка оценивала «порядка 120
  пунктов, около 80 открытых» — фактически **718 уникальных, 246 открытых**, втрое
  больше. Оценка учитывала только первый из пяти списков.

**«Мемориал» — 📍 не поднимать, дубль назван поимённо.** Явление уже описано:
`45-roadmap-and-tasks.md` §3б «у записи четыре выхода, и пятого нет». В `79` §5 внесён
только **признак и приоритет разбора** (пометка при импорте, такие идут первыми)
и рабочая гипотеза «это не задача, а отклонённое решение, которое некому было отклонить».

> **Число 73 из 202 внесено сознательно, при действующем правиле «число в прозе
> устареет».** Противоречия нет: это **датированный замер прошлого события**,
> а не счётчик текущего состояния. Правило PIT-091 бьёт по числам, которые **обязаны
> меняться** вслед за системой; исторический замер меняться не может. Граница
> проведена явно, чтобы следующая вахта не «чинила» это число.

### `2026-08-08-notes-context-blocks.md` 📍 оставить локальным

44 развёрнутых блока сырых формулировок владельца из Apple Notes. Метода нет —
это **содержание** в каталоге, размеченном как **М** (такое бывает; решение записывается,
а не выводится из разметки каталога).

**Ценность одна, и она историческая:** запись от 24.07.2026 («*как базу репу можно
обновлять у всех 30 репов. И как то синтезировать служебные файлы уникальные созданные
каждой репой*») — это **происхождение текущей кампании**, сформулированное владельцем
за месяц до её начала. Ссылка оставлена здесь; файл не переносится.

> 🔒 **Замечание по безопасности, не блокирующее.** Файл содержит персональные
> финансовые (баланс кредитной карты, доходы), медицинские и правозащитные данные.
> Проверил механику: утечки нет **по построению** — `59-public-mirror-filter.md`
> работает по **allowlist**, и §«репа публичная? тогда базу она не получает» отсекает
> раньше списков. Записываю явно, чтобы следующая вахта не проверяла заново.

### `backlog-2026-08-15-final.md` 📍 оставить локальным, метод вынут

Снимок распущенного `BACKLOG.md`. Сам по себе — история конкретной репы, не переносится.
**Вынуто три вещи**, все ушли в `80-mass-repo-automation.md` §5 и §6 (пп. 20, 24–26, 17).

**Жанр «транспортный контейнер» — 🔗 свести, не поднимать.** Приём («файл, который по
назначению удаляется после разбора; перед роспуском делается снимок, чтобы история
не потерялась, но два источника правды не жили рядом») уже покрыт
`45-roadmap-and-tasks.md` §3 «Правило переноса» + `TASKS_HISTORY`. Дубль назван — чтобы
следующая вахта не переоткрывала.

### `reports/README.md` 📍 оставить локальным — и он **врёт**, четырьмя способами

Читается как метод (навигация по каталогу), но по факту это **живой экземпляр PIT-091**
в чужой репе:

1. `backlog-2026-08-15-final.md` перечислен **дважды**, в одной таблице, с разными описаниями;
2. таблица «Готовые» содержит **4 файла из 10** — отсутствуют оба `2026-08-08-notes-*`,
   `2026-08-12-gh-git-automation`, `pitfalls-append-2026-08-16`, `backlog-…-final`;
3. строка «`disposal/` — отчёты о расформировании» описывает каталог, **которого нет**;
4. «**Пустая папка сейчас — норма**» — в папке **10 файлов**.

Механика та же, что у README базы в v1.87.0: **список правится руками, а его правка
не требуется ни одним гейтом**. Разница в том, что здесь список ещё и **удваивается**
при правке — признак того, что его дописывают, не перечитывая.

**В базу не идёт** — правило уже есть (PIT-091). Заведено в `TASKS.md` как правка
в чужой репе.

---

### 🔴 Ещё два числа указателя не подтвердились — считал поимённо

Проверил, у каких файлов есть запись **внутри секции `mission-control`** (а не где-либо
в `decisions.md`: `BOARD.md` и `21-git-push-protocol` упоминались в разделе моста
и при разборе деплойера, но решениями синтеза не являются).

| Указатель (`WATCHLOG` §0 = `ROADMAP.md`) | Факт поимённо |
|---|---|
| прочитано **9 скриптов** из 23 | **7** → осталось **16**, не 14 |
| осталось **14 файлов корня** | в корне 18, из них `.repo-class` совпал с базой → **17** |

Записи есть у семи: `audit_service_coverage.py`, `collect_to_base.py`, `cycle.sh`,
`mirror-clean.sh`, `sync-base.sh`, `system_audit.py`, `RUNBOOK.md`.

**Оба указателя завышают сделанное** — и это опаснее, чем занижение: недочитанное
выглядит дочитанным, а приёмка репы по §6 при этом всё равно не сойдётся, потому что
файлы физически на месте. То есть ошибка **не теряет** материал, но сдвигает оценку
остатка — по ней планируют вахту.

> **Замечено по дороге, к чтению `scripts/`:** в каталоге лежат **`cleanup-mirror.sh`
> и `mirror-clean.sh`** — два разных файла, чьи имена собраны из одних и тех же двух
> слов в обратном порядке. Прочитан только второй. Это ровно класс PIT-089 (имя как
> почти-дубль другого): при чтении и при `grep` они путаются, и решение по одному
> легко записать как решение по обоим. Читать оба и сверять назначение.

### Прогресс после этого батча

`00-infrastructure` 22/22 ✅ · `decisions/` 12/12 ✅ · **`reports/` 10/10 ✅** ·
**`scripts/` 23/23 ✅** · корень 0/17 · прочие 0/5.
Осталось в М-очереди: **17 файлов корня · 5 прочих (`daily/` 2, `profile/` 2,
`scripts/tests/` — прочитан) = 22**.

Проверка: 22 (00-infra) + 12 (decisions) + 10 (reports) + 23 (scripts, включая
`tests/`) = **67 прочитано**; 67 + 22 = 89 = М-очередь.
`102 = база 1 + С 12 + 89` ✅

**Следующий шаг: 17 файлов корня** — `MISSION`, `PLAN`, `START-HERE`, `HANDOFF`,
`INBOX`, `BACKLOG`, `BOARD`, `WATCHLOG`, `ROADMAP`, `TASKS`, `CHANGELOG`, `README`,
`VERSION`, `.repo-id`, `.gitignore`, `ROADMAP_HISTORY`, `TASKS_HISTORY` — **и читать
их из `~/Documents/система_репозиториев/mission-control`**, где `BOARD.md` на 60 секций,
а не на 18.

---

## 🏁 Корень и прочие дочитаны — `mission-control` ЗАКРЫТА, 102/102

### 🔴 Первое, что нашлось в корне: моя находка уже была записана — и занижена

`BOARD.md` строка 51, открытый пункт П2: *«Три копии репы на диске с разошедшимися
версиями: `~/vault` 1.5.0 (единственная с `.git`), `~/Documents/mission-control`
и `система_репозиториев/` — обе 1.12.0. Решить, какая канон»*.

То есть замер, которым я открыл вахту, **переоткрыл уже заведённую задачу**. Причина —
порядок чтения: скрипты читались раньше корня, а знание лежало в корне. Заведено
`SYN-019`, метод правлен (`PROCEDURE.md` §4: **корень читается первым**).

**Замер всё же дал больше записи — и это важно для правила.** Полный поиск от `~`
без списка каталогов:

| Копия | VERSION | `.git` | BOARD | мост |
|---|---|---|---|---|
| `~/vault/mission-control` | **1.5.0** | **есть** | нет | нет |
| `~/Documents/mission-control` | 1.12.0 | нет | 18 | v1.0.0 |
| `~/Documents/система_репозиториев/mission-control` | 1.12.0 | нет | **60** | **v1.2.0** |
| `~/Downloads/_sync-base/mission-control` | **1.0.0** | **есть** | нет | нет |
| `~/Library/Mobile Documents/.Trash/mission-control` | 0.25.0 | нет | нет | нет |

Копий **пять**, не три; `.git` у **двух**, не у одной. Структурный факт, видимый только
на полной картине: **`.git` есть у двух самых старых копий, а две самые свежие
не связаны с git вообще** — публикация идёт архивом через `deploy.sh`, поэтому
git-копии не обновляются никогда.

> **И ещё раз о себе.** Мой первый поиск был `find ~/Documents ~/Downloads` — **список
> каталогов, написанный головой**. Тот самый PIT-074, про который эта же вахта писала
> весь день. Дописано в `SYN-017`.

**Плюс объяснение раскола деплойера, которого не было.** `mission-control/WATCHLOG` §0
от 20.08 (вахта V) пишет: *«канон живёт только в `scripts/deploy.sh` (v4.16.1);
`~/Downloads/deploy.sh` синхронизирован, `~/Documents/deploy.sh` пока нет (не просили)»*.
То есть отставшая копия 4.15.0 — **осознанно отложенная**, а не забытая. При этом
машинный гейт базы считает каноном `templates/deploy.sh`, а копию в `mission-control` —
производной. **Журнал одной репы и гейт другой называют каноном разные файлы.**
Записано в `ROADMAP.md` §P2; сам факт снимает обвинение в разгильдяйстве — это
конфликт двух добросовестных записей.

---

### ⬆️ `MISSION.md` + `START-HERE.md` → `45-roadmap-and-tasks.md` §1а, §1б

**Пробел был структурный:** база описывала **четыре** файла (`ROADMAP`/`TASKS` + истории)
и не имела уровня выше. Поднят жанр, **не содержание** (миссия владельца остаётся
локальной — институт переживает раскладку, как и с классами реп):

- **пять файлов по горизонтам** — от `INBOX` «сейчас» до `MISSION` «год/5/10 лет»;
- **трассировка вверх обязательна и проверяется**, не трассируется — вылетает на ревью;
- **цепочка читается в обе стороны**, и вторая половина дефекта заметна хуже:
  список может годами содержать пункты, не ведущие к цели, **и при этом не содержать
  тех, что к ней ведут**;
- **MISSION меняется через ADR**, не правкой на ходу — иначе сверяться с ним бессмысленно;
- **горизонт без содержания оставляется пустым с записанной причиной**: «преждевременная
  конкретика вреднее пустоты — десятилетний план ради заполнения графы мешает видеть
  реальные варианты».

Плюс §1б — **два уровня роадмапа**: глобальный отвечает «на какой фронт идёт время»,
локальный держит детали, дублирование запрещено.

### ⬆️ Условие пересмотра отказа → `45` §2

База требовала раздел «Что осознанно НЕ делаем» **с обоснованием**. У источника колонок
**три**: идея · почему отклонена · **когда пересмотреть**. Третья меняет поведение
раздела: превращает «никогда» в «не раньше, чем X». Без неё отказ либо переобсуждается
при каждой смене обстоятельств, либо держится по инерции, когда обстоятельства уже
изменились.

### ⬆️ `TASKS_HISTORY` по дате, а не по версии → `45` §1

База предписывала перенос «батчем при выпуске версии» **единообразно**. У источника
разделено: `ROADMAP_HISTORY` — по версии (видно, какая версия что закрыла),
`TASKS_HISTORY` — **по дате**, потому что действия владельца происходят вне репы
и к релизам не привязаны. Привязка их к версии заставляет ждать выпуска, чтобы
записать сделанное вчера.

### ⬆️ `daily/TEMPLATE.md` + `daily/ROTATION.md` → новый `82-recurring-work-budget.md`

Прошлый проход взял из `06-daily-list.md` два приёма («механизм добавления есть,
механизма удаления нет» и «проверка выполнимости арифметикой»). В самих `TEMPLATE`
и `ROTATION` осталось **десять других**, и они не про дневной лист, а про **ёмкость
периода**. Главные:

1. **🔴 Квота недельная, а не дневная.** «Ежедневная превращает пропуск в провал,
   недельная — в перестановку.» Период квоты выбирается **не меньше типичного интервала
   между срывами** — иначе система гарантированно бросается на первом же выпавшем дне.
2. **🔴 Метрика, улучшаемая занижением плана, измеряет не то.** «7 из 10 = 70 %»
   лечится сокращением плана до семи. **Отличается от уже записанного в `77`:** там
   показатель растёт сам собой, здесь — **управляем тем, кого измеряет**. Разные
   причины негодности, разное лечение.
3. **🔴 Слот обязан отвечать «что будет сделано за это время».** Формулировки без
   критерия завершения («30 логики», «разобраться с темой») **закрываются ощущением,
   а не фактом**.
4. **Потолок ставится на то, что даёт ощущение работы без результата** — лекции,
   тренажёры навыка, «десять минут в приложении». Не запрет: запрет возвращает вопрос
   каждый период, потолок закрывает его один раз.
5. **Пол ноль записывается явно** — разница между «забыл» и «решил».
6. **Незанятый слот — резерв, а не долг**; переноса невыполненного нет.
7. **Ревью правит бюджет, а не человека.** «План, который невозможно закрыть, становится
   регулярным подтверждением недостаточности.»
8. **Когда два требования несовместимы, проигрывает всегда одно и то же** — значит
   несовместимость в плане, а не в дисциплине.
9. **Три практики одного назначения — коллекция инструментов вместо практики.**
10. **Спорные собственные решения помечаются** (`🔸`): помеченное возвращается осознанно,
    непомеченное — «потому что было же».

### ⬆️ `BACKLOG.md` → `72-source-of-truth.md` §4в-бис

**«Строится не хранилище, а виды.»** Запрос «нужен трекер / канбан / вики / mind map»
после проверки свёлся к «отрендерить то, что уже есть»: трекер — таблица, канбан —
разделы файла, вики — сами репы, mind map — рендер поверх маркдауна.

Ценность не в экономии работы: **новое хранилище — второй источник правды по определению.**
Признак назван: если «интеграция» требует что-то туда **переложить**, а не отобразить —
это не интеграция, а вторая копия.

### ⬆️ `BACKLOG.md` + `START-HERE.md` → `76-repo-classes.md` §5

**Противоположные требования к видимости режут границу сами** — критерий сильнее спора
о тематике. Планировщик обязан быть приватным, визитка обязана быть публичной; слить
их — не риск, а гарантированная утечка. Проверка видимости идёт **до** обсуждения темы.

Смежное, оттуда же: **правила обязаны быть стабильны, состояние обязано течь — поэтому
они живут в разных репах.** Это обоснование раскола «база ↔ планировщик», которого
в базе не было названо: лежи они вместе, каждая правка состояния поднимала бы версию
свода правил.

### ⬆️ `HANDOFF.md` → `73-chat-handoff.md` §2.4

**Раздел «чего НЕ прикладывать» в передаче.** База описывала таблицу «обязательно /
по задаче» и молчала о негативном списке. У источника: *«Не прикладывай архивы других
реп, пока чат сам не попросит: `it-base` весит 98 МБ и съест контекст без пользы»*.
Ложится на §4 («длинный промпт в начале = более короткий чат в конце»), но касается
**вложений**, а они дороже промпта на порядок.

---

### 📍 Оставлено локальным — с причиной

| Файл | Решение |
|---|---|
| `WATCHLOG`, `ROADMAP`, `TASKS`, `INBOX`, `BOARD`, `CHANGELOG` | состояние конкретной репы; жанры уже предписаны базой (`45`, `04`, `24`, `74`) |
| `ROADMAP_HISTORY`, `TASKS_HISTORY` | история; жанр в `45` §1 |
| `PLAN.md` | план конкретной кампании владельца; **приём** «порядок не по важности, а по блокировкам» уже есть в `45` §2 (приоритеты, не вехи) |
| `README.md`, `profile/README.md` | визитки; жанр в `75-readme-status-block.md` |
| `profile/SETUP.md` | инструкция под конкретный аккаунт GitHub |
| `.repo-id`, `VERSION`, `.gitignore` | предписаны `48-repo-identity.md` и `10-git-reference.md` §7 |

> **Понижение глубины, записанное явно (`PROCEDURE.md` §4).** `CHANGELOG.md` (1316 строк)
> прочитан **структурно**, а не построчно: жанр задан `24-changelog-protocol.md`
> и `42-release-description-standard.md`, содержимое — история версий этой репы,
> метода в ней быть не может по построению. Прочитаны шапка, формат заголовков
> и десять верхних записей. То же для тела `INBOX.md` (269 строк сырых формулировок
> владельца — предмет, а не метод) и `BOARD.md` ниже §«Очередь» (виды по репам).
> **Шапки и блоки правил всех шести файлов прочитаны целиком** — именно там живёт метод.

---

## 🔴 Дефекты `mission-control`, найденные в корне — в базу не идут

Дописано к трём, найденным в `scripts/`:

4. **`BACKLOG.md` распущен и жив одновременно.** `reports/backlog-2026-08-15-final.md`
   пишет: *«К версии 1.0.0 роспуск выполнен (2026-08-15)… а он удаляется»*. Сам
   `BACKLOG.md` пишет: *«Файл **не удаляется** — он рабочий»*. Оба обновлены 15.08.
   **Два документа одной репы прямо противоречат друг другу о судьбе третьего**,
   и содержимое `BACKLOG.md` с тех пор устарело («Очередь следующей сессии», «Закрыто
   в этой сессии» — сессии давно прошли).
5. **`HANDOFF.md` — замороженный артефакт, не помеченный как замороженный.** Промпт
   передачи от 13.08 для версии **0.24.0** при текущей 1.12.0: «41 репозиторий»
   (сейчас 57), «десять ADR» (11), «девятнадцать протоколов» (21). Лежит в корне
   и читается как актуальный. Лечится пометкой в шапке либо переносом в историю
   (`21-revision-protocol.md`: замороженное не переписывается, но обязано быть названо).
6. **`README.md` без блока статуса.** База предписывает его каждой репе
   (`75-readme-status-block.md`), в `mission-control` его нет — при том что репа
   инфраструктурная и сама раздаёт стандарты.
7. **`profile/SETUP.md` содержит мёртвый путь `~/repos`** — четвёртый экземпляр
   дефекта, закрытого в `sync-repos-map.sh` и `collect-base.sh` ещё 08.08.

---

## ✅ ПРИЁМКА `mission-control` (`PROCEDURE.md` §6)

```
всего 102 = база 1 + С 12 + прочитано и решено 89
```

Поимённо: `00-infrastructure` 22 · `decisions/` 12 · `reports/` 10 · `scripts/` 23 ·
корень 17 · `daily/` 2 · `profile/` 2 · `scripts/tests/` 1 = **89**.
Плюс `.repo-class` (совпал с базой) и 12 файлов в каталогах **С** = **102**. ✅

---

## Репа 6: `exam-kit` — 127 файлов · 🏁 ЗАКРЫТА (127 = база 0 + С 87 + прочитано 40)

> **Заголовок исправлен вахтой VII 21.08.2026.** Прежняя редакция — «проход ИДЁТ,
> прочитано 24 из 68 в М-очереди» — содержала три неверных числа; отмена каждого
> и её основание записаны в блоке «`exam-kit` ДОЧИТАНА» ниже. История правки здесь,
> а не в `git log`, потому что у этой репы `.git` нет.

### Вход по новому порядку (`SYN-017`, `SYN-019`) — и он сразу дал результат

**Копии:** `find ~` без списка каталогов дал **две** — `система_репозиториев/exam-kit`
и `Downloads/_sync-base/exam-kit`. `diff -rq` (без `.git`, без `_base/`) — **расхождений
ноль**, обе по 127 файлов. Канон — первая, вторая производна от раздачи. Случая
`mission-control` здесь нет, и это установлено, а не предположено.

**Листинг:** всего **127** (без `_base/`, где ещё 295). Скрытых каталогов нет.
Расширения: 59 `.md` · 39 `.jpeg` · 9 `.png` · 7 `.pdf` · 6 `.heic` · 3 `.py` · 2 `.sh` · 1 `.tex`.

**Сверка с базой: совпало 0, в очереди 127.** Ни одного байт-в-байт совпадения — репа
никогда не получала файлов базы в своё дерево (только в `_base/`).

> **Поправка к команде §1.3.** Каноническая команда поиска скрытых каталогов возвращает
> артефакт — саму `.` с общим числом файлов (`422 .`). Нужен `-mindepth 1`. Починено
> в `.claude/agents/repo-inventory.md`, чтобы не воспроизводилось на остальных 52 репах.

### 🔴 Репа не имеет обязательного минимума — при разложенной базе

Прогон готового `scripts/per_repo_audit.py` (сначала инструмент, потом свой скрипт —
`PROCEDURE.md` §3а):

```
exam-kit  нет: VERSION, .repo-id, .repo-class, .repo-meta, ROADMAP.md, TASKS.md,
               START-HERE.md, 00-CLAUDE-STOP.md
```

В корне только `README.md`, `WATCHLOG.md`, `CHANGELOG.md`, `.gitignore`. При этом `_base/`
разложена (295 файлов, `BASE_VERSION` 1.41.0). **Раздача доехала, идентичность — нет.**
Это одна из тех 11 реп без `ROADMAP`/`TASKS`, по которым в `TASKS.md` базы висит решение
владельца.

> **Проверил обвинение в адрес собственного инструмента и снял его.** Таблица 1 аудита
> печатает `своя 0` для репы с 21 методичкой — выглядит как дефект (метод лежит в `docs/`,
> а не в `00-infrastructure/`). Но таблица 1 **по назначению** меряет дрейф
> `00-infrastructure` против `_base/`, а таблицу 2 `docs/` видит: `RELEASING.md` в ней есть,
> 2 репы / 2 редакции. Дефекта нет. Записываю явно, чтобы следующая вахта не «чинила»
> работающее — заголовок «реально своё» просто читается шире, чем считает.

### Что уже было выбрано из этой репы прошлыми вахтами — и это подтвердилось

`65-visual-source-pipeline.md` собран из `exam-kit` + `portrait-of-taste` и покрывает
**больше, чем ожидалось**: L-020 (шаг строк вместо мегапикселей) в §1 · L-023 (опровержимый
вопрос, ярус «по желанию» не существует, «под риском» = известный риск) в §3 · L-016
и `⟦реконструкция⟧` в §2 · чистовик/разбор порознь в §4 · манифест исходников в §1.
**Прошлое сведение было добросовестным** — фиксирую, потому что перепроверка по процедуре
и должна была это установить.

### ⬆️ Поднято из прочитанного (24 файла)

| Источник | Куда | Что |
|---|---|---|
| `LESSONS.md` L-022 | `65` §2 | **отсутствие ожидаемого признака — сигнал, а не норма**; аномалия, записанная и не отработанная, хуже незамеченной |
| L-021 | `65` §1 | наклон как вторая метрика; **однородная поправка на неоднородное искажение** портит края ровно настолько, насколько чинит центр |
| L-014, L-015, L-016 | `65` §1 | **сначала канал, потом источник** · установить факт потери мало — нужно **звено** · устранение потерь в канале **ценнее сверки**: сверка проверяет предъявленное, исходник — и незамеченное |
| L-013 | `21` §4в | **перегиб при исправлении**: отбрасывают разделом, а годное лежит поштучно |
| L-011 | `71` §7д | **верный вывод не по задаче — всё равно шум** |
| L-017, `experiments/README` | `28` §10а, §5а | доказать идентичность материала до сравнения каналов · контроль на дополнительную деградацию · **инструмент может зависеть от варьируемой величины** (лапласиан и масштаб) · отрицательный результат записывается |
| `CROSS_PLATFORM.md` | `18` §9а, §9б | **у каждой служебки записана потеря, которую она закрывает** · три уровня: инструкция / журнал / **протокол** — без протокола инструкция через месяц выглядит произволом |
| `CROSS_PLATFORM.md` §6 | `21` §4б-бис | **пример, противоречащий стандарту, — баг, а не «исторический вариант»** |
| `STANDARDS.md` §2.1, §2 | `67` §2а, §2б | **ширина важнее глубины** + критерий достаточности строки («чем отличается от соседнего») · **порядок сокращения объявляется заранее** |
| `DELIVERY_LOG.md` | `28` §6а, §6б | **каждый столбец помечен: измерено или выведено** · сводный журнал рядом с частными отчётами — и главное, он показывает, **где связь ломается** |
| `RELEASING.md` §1 | `72` §4а | коллизия номеров бьёт по **любому** сквозному реестру, не только по версиям: задвоились `v1.9.0` **и** `L-016` |
| `21` §4г | — | **урок без строки в чек-листе не действует** — из связки `LESSONS.md` + «Чек-лист перед выдачей», где каждый пункт помечен `L-NNN` |

### ⬆️ `build/release.sh` → гейт в `scripts/revision_check.py` (не документ, а проверка)

Скрипт сборки `exam-kit` делает то, чего у базы **не было ни в одном гейте**: перед выпуском
ищет **дубли номеров сразу в двух реестрах**, версиях и уроках:

```bash
DUP_V="$(grep -oE '^## v[0-9]+\.[0-9]+\.[0-9]+' CHANGELOG.md | sort | uniq -d)"
DUP_L="$(grep -oE '^## L-[0-9]+'                 docs/LESSONS.md | sort | uniq -d)"
```

Это **исполняемая форма** правила, поднятого часом раньше в `72` §4а из `RELEASING.md`.
Правило без проверки — обещание; здесь оно стоит одну команду.

**Проверил базу:** дублей сейчас нет ни в `PIT`, ни в `SYN`, ни в `ADR`, ни в версиях
`CHANGELOG` — **и ни один скрипт базы этого не проверяет** (`grep -l "uniq -d" scripts/*.py`
→ пусто). Реестры чисты по случайности, а не по гарантии.

**Поставлено:** `check_registry_dupes()` в `revision_check.py` — четыре реестра
(`PIT`, `SYN`, `ADR` по именам файлов, версии `CHANGELOG`).

> **И сразу канарейка, по правилу `71` §7в.** Реестры в норме чисты, значит проверка
> в норме **всегда возвращает пусто** — сломайся регулярка, гейт молча остался бы зелёным
> навсегда. `selftest_registry_dupes()` на временном дереве подсаживает по дублю
> в каждый из четырёх реестров и требует **ровно четыре** срабатывания: условие
> `len(caught) > 0` прошло бы и при трёх сломанных проверках из четырёх.
> Проверено вживую — все четыре ловятся, гейт `CLEAN`.

### 🔴 Дефект `exam-kit` — в базу не идёт, записан для сессии по репе

**`templates/03_recognition_report.md` отстал от `docs/RECOGNITION.md`.** Стандарт получил
три новые метрики (`шаг текстовых строк` L-020, `наклон` L-021, `тип контента` L-018),
а в таблице шаблона по-прежнему только «Высота строки». То есть **урок доехал до стандарта
и до чек-листа, но не до формы, которую реально заполняют.**

Это подтверждает оба правила, поднятые сегодня из этой же репы: `21` §4г (урок действует
через исполняемую часть) и §4б-бис (образец, расходящийся со стандартом, — баг). Найдено
в течение часа после того, как правила были записаны.

### Добор второго батча — `docs/` закрыт 10 из 10

**`SHOOTING_GUIDE.md` ⬆️ — два подъёма, оба не про фотографию.**

1. **Приоритет захвата — по восстановимости, а не по важности** → `65` §1.
   Условия заданий важнее по смыслу, но длинные и избыточные — **восстанавливаются**;
   варианты ответов справа коротки, и **каждый символ невосстановим**. Обе первые
   итерации дали одну картину: условия прочитаны, варианты нет. Плюс наблюдение,
   что **вспомогательный сигнал структуры бывает надёжнее самой структуры**: границы
   строк восстанавливались по колонке баллов, а не по линиям разметки.
2. **Жанр «как делать лучше»** → `68` §4а. Два разреза: по **стоимости выполнения**
   (что не стоит ничего в момент действия — отдельно) и сортировка по выигрышу
   **с числом**. Ключевая фраза жанра — «первые два дают больше, чем все остальные
   вместе»: она **разрешает не делать остальные** и тем спасает первые два.
   Плюс: руководство исходит из реальных ограничений (первая редакция была наполовину
   невыполнима), и **граница, куда документ не пойдёт, называется вслух** —
   «разбирать материал после нормально, помогать проносить мимо контроля нет».

**`UPLOAD_GUIDE.md` 🗑 — дубль, назван поимённо.** Содержимое целиком покрыто уже
поднятым: L-014/L-015/L-017 в `65` §1 (сначала канал, звено, диагностический признак
округления) и `28` §10а (доказать идентичность материала, контроль на деградацию).
Единственное, что могло бы претендовать, — «полное разрешение выявляет ошибки,
которые проходят сверку», — поднято часом раньше как «устранение потерь в канале
ценнее сверки».

**`build/deskew_zoom.py` ⬆️ → `71` §7г-бис.** Скрипт реализует L-020/L-021, но ценно
не это, а **его докстринг**:

> «Автоподбор врёт на фрагментах, где мало текста и много фона или бликов, — тогда угол
> задаётся вручную по соседнему блоку, где подбор сработал.»

**Инструмент объявляет, где он врёт, в собственной документации**: назван класс входа,
назван признак, дан обходной путь. Это профилактика тихого отказа — эвристика,
выдающая правдоподобное число вместо отказа, опаснее падения, потому что число уходит
в отчёт. Второй приём оттуда же: **печатать не только замер, но и его чтение** —
`шаг строк ~11.4 px` + строка «символы на пределе», чтобы интерпретация не зависела
от памяти читающего.

> Отличается от «потолка инструмента» кампании (`tools/README.md`): потолок пишется
> **после** того, как инструмент подвёл; это правило — **до**, пока автор помнит
> свои допущения.

**`build/release.sh` ⬆️** — разобран выше, дал гейт дублей реестров.

### Прогресс по репе — честно

Прочитано **27** файлов: корень 3 из 4 (`README`, `WATCHLOG`, `.gitignore`) ·
**`docs/` 10 из 10 ✅** · `templates/` 1 из 3 · `experiments/` 1,5 из 2 ·
`build/` 2 из 6 · `examples/` 0 из 36 не-фото.

**Осталось:** `CHANGELOG.md` · `templates/01`, `02` · хвост протокола
`image-delivery.md` (строки 120–276) · `build/` 4 скрипта (`slice_image.py`,
`plot_helpers.py`, `build_pdf.sh`, `header.tex`) · `examples/` — 7 отчётов
о распознавании, 7 эталонов, 6 разборов, 1 `incident_report.md`, 7 `MANIFEST.md`.

**Разметка каталогов ещё не сделана** — по `PROCEDURE.md` §2 она требует открытия файлов
в `examples/`, а они не прочитаны. Предварительно: `examples/*/photos/` (59 файлов) —
кандидат в **С** (исходные снимки), но **решение не принято**, потому что `MANIFEST.md`
внутри `photos/` — метод, а не содержание, и его надо развести отдельно.

**Приёмка НЕ закрыта.** Арифметика: `127 = база 0 + С ? + прочитано 24 + в очереди ?`.

---

## ✅ Контрольная сверка пяти закрытых реп — 21.08.2026, субагентом

Заказ владельца: «прогони агента на тех репах, что уже обработаны, чекнуть, не пропущено
ли чего». Прогнано на Sonnet, результат перепроверен мной по спорным местам.

### Записанные числа подтвердились ВСЕ ПЯТЬ — точно, без расхождений

| Репа | записано | факт | копий на диске |
|---|---|---|---|
| `personal-finance-dss` | 1501 | **1501** | 2 |
| `it-base` | 3197 | **3197** | 2 |
| `portrait-of-taste` | 1572 | **1572** | 3 |
| `character-a-analysis` | 337 | **337** | 3 |
| `mission-control` | 102 (база 1) | **102, база 1** | 5 |

**Арифметика прежних вахт верна.** Это стоит записать так же явно, как записываются
находки: перепроверка подтвердила работу, а не опровергла.

### 🔴 Но нашлось другое: копии — свойство системы, а не особенность `mission-control`

**Ни у одной из пяти реп нет единственной копии.** У всех есть экземпляр
в `~/Downloads/_sync-base/`, отстающий по версии и расходящийся на **3–116 позиций**;
у трёх — третий, почти идентичный канону, в `~/Documents/<репа>`.

И структурный перекос, видимый только на полной картине: **у канонических копий `.git`
нет, а у отставших в `_sync-base` — есть.** Git-история живёт у устаревших экземпляров;
свежие копии с git не связаны вовсе, потому что публикация идёт архивом через `deploy.sh`.

`SYN-017` был записан как случай `mission-control`. **Он оказался общим.** Открыто
в `ROADMAP.md` §P2.

### Что агент честно НЕ смог проверить — и это правильно

«С 12» и «прочитано 89» по `mission-control` он не подтвердил, прямо написав, что это
**внутренние метки разметки кампании, а не файловая метрика**, и что правдоподобное
число подставлять не станет. Ровно то поведение, которое инструкция требует
(«не подставляй правдоподобное число вместо неполученного»).

### 🔴 Названный потолок правила `SYN-017`

Агент сообщил то, чего я не знал: **`find ~` не является исчерпывающим.** Все пять
запусков вернули код 1 — упор в «Operation not permitted» на ~120 TCC-защищённых
подкаталогах `~/Library` (Mail, Messages, HomeKit, Safari-кэши) без Full Disk Access.
Список копий при этом собирается полностью, ветки просто пропускаются с предупреждением.

**Правило `SYN-017` остаётся верным, но у него теперь есть названная граница:**
`find ~` покрывает всё, **кроме контейнеров приложений Apple**. Репозиторию там взяться
практически неоткуда, но утверждать стопроцентную исчерпывающесть нельзя — и это надо
писать в отчёте, а не подразумевать. Дописано в карточку.

### 🔴 Побочная находка про саму базу: 400 файлов, 371 уникальный хеш

Агент заметил вскользь, я проверил лично: **29 групп побайтовых дублей внутри базы**.
Двадцать восемь — одной формы: файл лежит в `00-infrastructure/` **и** в своём настоящем
доме (`01-claude-context/`, `reports/`, `templates/`), причём копия в `00-infrastructure/`
носит **транслитерированное** имя, а канон — нормальное. Это слой **до переименования**,
который скопировали и не удалили.

**Замерил, живой ли он:** ссылок на копии — **ноль** у десяти из двенадцати проверенных,
при живых ссылках на канон. То есть мёртвый груз, который **едет в 57 реп** при каждой
раздаче и засоряет любой `grep` покрытия. Разбор и порядок снятия — `ROADMAP.md` §P2.

---

## Репа 7: `dota-dossier` — вход сделан субагентом, ЧТЕНИЕ НЕ НАЧАТО

Первый боевой прогон делегирования (решение владельца, `ADR-002`). Инструкция —
`.claude/agents/repo-inventory.md`; в этой сессии выполнена через `general-purpose`
на Sonnet, потому что **определения агентов читаются при старте сессии**, а созданы
они были в ней же.

### Что дал вход

**Три копии**, все сверены с диском мной лично (агенту не верим на слово):

| Копия | `VERSION` | `.git` | файлов |
|---|---|---|---|
| `~/Documents/dota-dossier` | 2.42.0 | нет | **5157** |
| `~/Documents/система_репозиториев/dota-dossier` | 2.42.0 | нет | **5148** |
| `~/Downloads/_sync-base/dota-dossier` | **2.34.0** | **есть** | 4567 |

Первые две расходятся **тремя позициями**: `data/…/match_index.csv`,
`docs/items/gem/overview.md`, и `src/__pycache__` только в первой (она же объясняет
разницу 5157/5148). Третья отстала на версию и **не содержит целых каталогов**
(`docs/heroes`, `docs/items`, `bots`, `data/hero_builds`, `reports/cards`, часть `src/*.py`).

**Файлов 5148, а не 4919** — оценка в `PLAN.md` §3 P1.3 занижена на 229.

### 🔴 Главное: 180 из 187 совпадений с базой — это вложенная плоская копия базы

Сверка дала **187** совпадений байт-в-байт, и агент верно заметил, что почти все они лежат
внутри `./base-repo/` **самой репы** — то есть это не пересечение по содержанию,
а **legacy-копия базы, вложенная в дерево репы**.

Проверено мной: **229 файлов, `VERSION` 1.36.0**, верхняя запись её `CHANGELOG` — 30.07.2026.
**Отставание 55 версий** (база сейчас 1.91.0). Это ровно PIT-090 из открытого пункта
`ROADMAP.md` §P2 («вычистить плоские legacy-копии из 12 реп»), впервые подтверждённое
**числом версий отставания**, а не фактом существования.

**Из 187 вне `./base-repo/` только 7:** шесть в `reports/infrastructure/` (тёзки файлов
базы — `PITFALLS.md`, `mass_release_revision.md` и др.) и `scripts/deploy_all_versions.sh`.
То есть **настоящее пересечение с базой — семь файлов, а не сто восемьдесят семь**,
и разметка должна считать `./base-repo/` отдельной категорией, иначе арифметика §6 соврёт.

### Оценка делегирования — честно, по первому прогону

**Что сработало.** Отчёт по репе на 5148 файлов пришёл готовым, а перепись каталогов
в нём — **свыше 500 строк**, которые в мой контекст не попали. Это и есть заявленный
выигрыш, и он крупнее ожидаемого: ручной проход по такой репе съел бы заметную долю вахты
только на листинг. Наблюдение про вложенную базу агент сделал **сам**, в разделе
«Замечено», не будучи на него наведён, — и оно оказалось главным.

**Чего стоит опасаться.** В отчёте есть **опечатка в пути**: `…/Документы/система_репозиториев/…`
вместо `Documents`. Байт-в-байт транскрипция агента **не гарантирована** — отчёт это
пересказ, а пересказ деградирует (`73` §4). Отсюда правило на будущее: **числа и пути
из отчёта субагента перепроверяются одной командой перед тем, как на них опереться.**
Я так и сделал — таблица копий и вложенная база сверены с диском заново, всё сошлось.

**Итог:** делегирование механического входа окупается на репах от нескольких сотен файлов
и требует одной верифицирующей команды сверху. Записано в `ADR-002` как практика.

---

**Пятая репа из 57 закрыта.** Поднято за репу: 8 новых документов базы
(`76`, `77`, `79`, `80`, `81`, `82` + разделы в `04`, `32`, `44`, `45`, `61`, `68`,
`70`, `72`, `73`, `74`, `76`, `tests/README.md`), карточек `SYN` заведено 6 (`014`–`019`).

> **Все числа выше — по канону `~/Documents/система_репозиториев/mission-control`.**
> Шесть отчётов, разобранных этой вахтой, в обеих копиях **байт-в-байт одинаковы**
> (проверено `diff -rq`: расходятся только `BOARD.md`, `push-inbox*`, `deploy.sh`),
> поэтому решения по ним переписывать не потребовалось. Повезло — а не «обошлось»:
> проверять это надо было **до** чтения, а не после.

---

## `scripts/` — четыре файла моста прочитаны (11 из 23)

### `push-inbox.sh` v1.2.0 · `push-inbox-history.log` · `cleanup-mirror.sh` — 🔗 свести, всё покрыто

Проверка на дубль дала **полное покрытие**, и это результат, который надо записать явно,
чтобы следующая вахта не читала заново. `74-planner-bridge.md` содержит: §4а два guard'а ·
«Третий уровень защиты — точное имя (v1.2.0)» · `source-hash` · журнал прогонов ·
контракт принимающей стороны · §4б гонку с деплоем. Поднимать нечего.

**Одно наблюдение всё же стоит отдельно** — не как метод, а как доказательство уже
принятого метода. Журнал прогонов (`log_history` пишется на **каждый** запуск, включая
сухой и отказ guard'а) — **единственное, что вскрыло вторую копию репы**. Числа во всех
файлах врали согласованно, потому что были посчитаны по одной и той же копии;
не сошлась **история событий**: одна строка в журнале против «двух прогонов» в `ROADMAP`.

> **Отсюда довод в пользу журналов прогонов, которого в `74` нет:** сводное состояние
> можно посчитать заново и получить тот же неверный ответ. **Журнал событий пересчитать
> нельзя** — он либо есть, либо его нет. Поэтому при расхождении «документ против диска»
> первым смотрится журнал, а не счётчик. Дописано в `74-planner-bridge.md` §5.

`cleanup-mirror.sh` — 📍 локальный, разовая уборка `finpilot-mirror`. Метода нет; сам
случай уже разобран (п. 16 бэклога, PIT-063). Отмечу дефект для сессии по репе: скрипт
чистит карту через `grep -v "finpilot-mirror"`, то есть **удаляет любую строку, содержащую
подстроку** — тот же класс, что PIT-089, только в другом файле. Сейчас безвреден
(строки нет), но останется миной.

### `scripts/tests/test-push-inbox.sh` — ⬆️ **три приёма, которых в базе нет**

Сверил с `tests/README.md` базы — там сильная методичка (зоны по последствиям отказа,
правило второго прогона, «флаг без кейса не готов»). Три вещи в ней **отсутствуют**:

1. **Проверка ОТРИЦАТЕЛЬНОГО свойства заглушкой-журналом.** Тест 4 утверждает не
   «сухой прогон печатает правильное», а «**`gh` не вызван ни разу**»: в `PATH`
   подставляется заглушка, которая логирует каждый вызов, и тест сверяет, что журнал
   пуст. Сухой прогон, который на самом деле ходит в сеть, проверкой вывода
   **не ловится в принципе** — вывод-то правильный.
   `grep "не вызван"` по базе → **0 совпадений**. Приём не описан.
2. **Границы среза проверяются с обеих сторон.** Тест 1 утверждает и «содержит своё»,
   и «**не захватывает следующую секцию**», и «не захватывает предыдущую». Односторонняя
   проверка пропускает ровно тот дефект, ради которого guard и писался.
3. **Отсутствие фреймворка — не причина не тестировать.** `bats` на машине нет; вместо
   отложенных тестов написан самодостаточный раннер на чистом bash со счётчиком
   `PASS/FAIL` и ненулевым кодом в конце. 175 строк, ноль зависимостей.

**Куда положено:** дописано в `tests/README.md` базы отдельным разделом, а не новым
документом — там уже живёт методика тестирования, и второй дом ей заводить нельзя
(`72-source-of-truth.md`).

---

## 🏁 `scripts/` дочитан — 23 из 23

### Что покрыто базой и НЕ поднимается — назвал поимённо, чтобы не переоткрывали

| Файл | Чем именно дубль |
|---|---|
| `push-inbox.sh` v1.2.0 | `74-planner-bridge.md` §4а, §4б, «третий уровень защиты» — целиком |
| `deploy-patch.md` | фикс 2 = `71-fail-loud…` §7г (подавленный stderr + зашитая догадка) · фикс 3 = `43-archive-naming…` (`unzip -t`, «ещё пишется») · фикс 1 исполнен в `deploy.sh` 4.4.0 |
| `todoist-export.sh` | токен через окружение — уже в `32-…` §«секрет в argv» |
| `collect-base.sh`, `sync-repos-map.sh --collect` | сборщики по белому списку каталогов = PIT-074, разобрано; у базы есть `legacy_delta.py` и `audit_coverage.py` шире |
| `cleanup-mirror.sh` | разовая уборка, случай разобран (PIT-063) |
| `build-inbox.py`, `import-notes.py` | механика уже поднята в `79-capture-funnel.md` §4–§5 |

### ⬆️ Поднято: `delete_all_gists.sh` → `80-mass-repo-automation.md` §5а

**Лучше всего сделанный скрипт репы.** Четыре условия необратимой массовой операции,
ни одно из которых в базе не было (`grep "пост-услови"`, `"404"` → пусто):

1. **Бэкап — предусловие.** Неполный бэкап **останавливает удаление**, а не сопровождает
   его. «Сделал бэкап и удаляю» тихо теряет те объекты, что не сохранились.
2. **Пост-условие проверяется у источника.** Последний шаг — **новый запрос**:
   «на аккаунте осталось N, ожидалось M». Комментарий автора точен: *«Прошлые скрипты
   этой системы ровно здесь и врали: выходили с кодом 0, не сделав работу»*.
3. **`404` не значит «объекта нет» — он значит «я его не вижу».** Без скоупа `gist`
   список отдаётся, а `delete` возвращает 404, и это читается как «уже удалён».
   Различается **до** операции, проверкой скоупа.
4. **`--paginate` обязателен.** Без него 30 записей, и «все удалены» — неправда на 31-м.

### ⬆️ Поднято: `set-repo-meta.sh` → `80-…` §4а и §6а

**`IFS` в базе не упоминался вовсе** — при том что владелец запускает `zsh script`,
что **перекрывает шебанг**. В zsh подстановка параметра по `IFS` **не разбивается**;
v1.0.0 отправляла один топик `"python,fastapi,…"` целиком, запятая в топике
недопустима — **упали все 49 реп подряд**. Переносимое решение (`set --` вместо массива)
внесено в базу дословно.

Плюс §6а — обобщение из шапки скрипта: **атрибут, обновляемый как побочный эффект
другой операции, застывает у тех, кого операция обходит.** Описание репы проставлялось
только при публикации → давно не публиковавшиеся репы годами стоят с описанием,
равным имени, **и никакого сигнала об этом нет**.

### ⬆️ Поднято: `repos_map.py` → `72-source-of-truth.md` §4г

Случай, не покрытый §4б: **один файл, две зоны с разными владельцами** — машина
строит структуру, человек пишет прозу. Первая версия скрипта **затёрла 15 авторских
описаний** средней длины ~290 символов однострочниками из GitHub.

Три детали, каждая из практики: собственную заглушку сохранять нельзя (залипнет
навсегда) · затравка обязана называть себя затравкой и говорить, что с ней делать ·
не попавшее ни в одну группу выносится в раздел «Без класса», а не раскладывается
по умолчанию.

### ⬆️ Поднято: `migrate_old_repos.py` → `32-…` и `80-…`

677 строк, `--selftest` без сети. **Секрет в экспорте = аварийный останов всего
прогона, а не пропуск файла** — этого правила в `32-git-hooks-and-secret-scanning.md`
не было, при том что весь документ про секреты. Пропуск выглядит аккуратно и делает
худшее: остальное переносится, прогон зелёный, владелец уверен, что чисто, а вопрос
«не закоммичен ли он уже где-то» **никто не задал**.

Плюс два приёма, уже внесённых в `80` §5: затронутость по `git status`, а не по
счётчику новых файлов · нормализация обеих сторон перед хешем (скрипт кладёт
в множество **оба** варианта ноутбука — сырой и очищенный).

> **Не поднято, но отмечено:** `move_section` возвращает **общий** объём работы,
> а не только новые файлы, — «иначе повторный прогон после неудавшегося пуша считает,
> что делать нечего, и выходит **до удаления исходника**». То есть **идемпотентность
> операции из двух частей (скопировать + удалить) не сводится к идемпотентности
> первой части.** Мысль сильная, но у неё пока **один** случай; по §3а закон 4 требует
> повышать при сомнении, а здесь сомнения нет — это подкласс уже поднятого «затронутость
> определяется состоянием, а не счётчиком». Записано сюда, чтобы второй случай
> опознали и подняли.

### ⬆️ Поднято: `todoist_export.py` → `80-…` §2а

Клиент к чужому API: три класса кодов (останов / ретрай / останов-с-гипотезой) ·
**защита от петли пагинации** (`cursor in seen` → останов; без неё скрипт не падает,
а бесконечно качает одно и то же и выглядит как «долго работает») · **валидация формы
ключа до первого запроса** — самая частая ошибка не «ключ протух», а текст-заполнитель
на его месте.

---

## 🔴 Дефекты, найденные в `mission-control` — в базу не идут, но записаны

Для сессии по самой репе. Все три — **повторы уже починенного в соседнем файле**.

1. **`fix-descriptions.sh` и `set-repo-meta.sh` делают одну работу с разными данными.**
   Для `mission-control`: описание «Командный центр системы: планирование, задачи,
   дорожная карта, решения, дневной распорядок» против «Командный центр: планирование
   и решения»; топики `infrastructure,planning,roadmap,productivity` против
   `planning,roadmap,adr,automation,knowledge-base`. **Кто запущен последним, тот и прав**,
   а так как `--add-topic` только добавляет, топики копятся объединением обоих наборов.
2. **`fix-descriptions.sh` несёт три дефекта, уже починенных в `set-repo-meta.sh`:**
   разбор топиков через `${3//,/ }` без кавычек (в zsh не разбивается — тот самый
   случай на 49 реп) · `$A` подставляется без кавычек · ошибка `gh` глушится
   в `>/dev/null 2>&1`, остаётся «✗ нет репы или нет прав» без причины.
   **Класс SYN-016: урок применён к одному файлу из двух с одним дефектом.**
3. **`sync-repos-map.sh --classes` в дефолтном режиме теряет всю работу.** Без
   `REPOS_DIR` скрипт клонирует базу в `mktemp -d` с `trap rm -rf … EXIT`, ставит
   `REPOS_DIR="$WORK_TMP"` — и `--classes` пишет `.repo-class` **во временный каталог,
   который стирается на выходе**. Ровно случай из бэклога п. 26 («изменения умерли
   с временным клоном»), второй экземпляр. Плюс комментарий в шапке утверждает, что
   локальной папки с репами не существует — а она есть, `система_репозиториев/` с 59
   каталогами.

### Замер: раскладка реп по классам живёт в пяти местах, и все пять расходятся

Решение «раскладку не поднимать, институт переживает раскладку» было принято
20.08 по рассуждению. Теперь оно **измерено**:

| Место | Реп |
|---|---|
| `sync-repos-map.sh` `CLASSES` | 39 |
| `repos_map.py` `PINNED` | 41 |
| `set-repo-meta.sh` (строки `apply`) | 49 |
| `repos-map.md` базы | **57** |
| каталогов в `система_репозиториев/` | 59 |

Пять источников, пять разных чисел, все написаны руками. Актуален один — карта.
**Это и есть довод, которого не хватало решению:** не «две копии разойдутся» как
прогноз, а «пять уже разошлись» как факт.

---

## 🔴 Два дефекта найдены В САМОЙ БАЗЕ по ходу этой сверки

Оба — из класса, который база сама же описала, и оба ждали в файлах, объявленных
эталонными.

### 1. `tests/README.md` — три числа в одном файле, все три неверны

| Что | В файле | Факт |
|---|---|---|
| кейсов (шапка) | 71 | **72** |
| проверок (шапка) | 274 | **279** |
| строк в таблицах зон | 68 | должно быть 72 |

Не хватало **A7, A8, I4, I5** (`comm` между списком кейсов в `test_deploy.sh` и таблицами
README). Живой PIT-091 — **одной версией после того, как v1.87.0 чинила ровно этот класс
в `README.md` корня**. Правило было записано, а соседний файл того же класса не проверен.

**Почему это хуже обычного устаревшего числа:** заявленное назначение таблиц — *«пробел
в покрытии виден сразу: мало проверок в зоне → там тонко»*. То есть таблица **и есть
карта покрытия**. Карта, отстающая на 4 кейса, показывает зону `A` тоньше, чем она есть,
и следующая вахта пойдёт дописывать уже написанное.

**Починено:** четыре кейса внесены, числа исправлены, **и рядом положена команда сверки**
в HTML-комментарии — чтобы у числа появился способ проверки, а не только обещание
(правило v1.87.0: «либо у числа есть гейт, либо оно устареет»).

### 2. `69-agents-hooks-and-gates.md` — два раздела `## 4а`, ссылки уже разошлись

В файле два раздела с номером `4а`: «Режимы работы» (стр. 154) и «Модель или скрипт»
(стр. 183). **Обе нумерации стоят в живых ссылках**: `decisions.md:615` ссылается
на первый, `decisions.md:2154` и новый `81-…` — на второй. То есть указатель `69 §4а`
вёл **в разные места в зависимости от того, кто его писал**.

**Починено:** второй переномерован в `## 4б`, в нём оставлена явная строка перехода
для старых указателей. Ссылки в `81-claude-code-privacy-and-billing.md` обновлены.
`decisions.md:2154` **намеренно не правлю** — журнал прогона не переписывается задним
числом (`21-revision-protocol.md`); соответствие «старое §4а про модель/скрипт = новое
§4б» записано здесь и в самом `69`.

> **Общее у обоих дефектов:** это **служебная разметка документа** — номер раздела,
> число в шапке, строка в таблице. Её правят руками, её не проверяет ни один гейт,
> и она **не влияет на смысл текста**, поэтому при вычитке взгляд по ней скользит.
> Ломается при этом навигация — то есть попадание читателя в нужное место.

---

## 🏁 `exam-kit` ДОЧИТАНА — вахта VII, 21.08.2026

### 🔴 Сначала — три записанных числа, которые сверка отменила

Проход продолжен не с записи, а с диска. Записанное не подтвердилось трижды.

**1. «`docs/` закрыт 10 из 10» — неверно, было 8 из 10.** Поимённая сверка списка
каталога против `decisions.md`:

```bash
for f in $(ls exam-kit/docs/); do
  n=$(grep -c "$f" decisions.md); [ "$n" -eq 0 ] && echo "НЕТ ЗАПИСИ: docs/$f"
done
→ НЕТ ЗАПИСИ: docs/POLICY.md
→ НЕТ ЗАПИСИ: docs/WORKFLOW.md
```

Ноль упоминаний **во всём прогоне**, не только в `decisions.md` (`grep -rn` по
`PLAN`, `PROCEDURE`, `README`, `SYSTEM-MAP`, `decisions` — пусто). По железному правилу
`PROCEDURE.md` §5 «прочитал и не записал = не прочитал» каталог закрыт не был.
Оба файла прочитаны сейчас, решения ниже. **`WORKFLOW.md` — центральный документ репы**,
и он же оказался пропущенным: пропускается не мелочь на периферии, а то, что «и так
всем понятно».

**2. «Прочитано 27» не сходится с собственной росписью того же абзаца.**
`корень 3 · docs 10 · templates 1 · experiments 1,5 · build 2 · examples 0` = **17,5**.
Число в заголовке и число в росписи расходились на 9,5 и стояли в одном абзаце.

**3. «`examples/` 36 не-фото» — на диске 43 + 7 `MANIFEST.md`.** 36 получается
вычитанием семи `etalon.pdf`; вычитание нигде не названо. Плюс `MANIFEST.md` лежат
**внутри** `photos/` и в число не вошли вовсе — при том что тот же абзац называет их
методом, а не содержанием.

**4. Отсюда неверна и «М-очередь 68».** `68 = 127 − 59` — вычтен каталог `photos/`
целиком, вместе с семью манифестами, которые тот же абзац называет методом. Число
ошибочно **в обе стороны сразу**: в нём не хватало 7 манифестов и лишними стояли
35 предметных файлов `examples/` (эталоны, распознавания, разборы, отчёты, PDF, чертежи).
После разметки: **М = 40, С = 87**. Совпадение по модулю тут случайно — граница
проходит по файлам, а каталог `photos/` её не задаёт.

> **Своя же ошибка того же класса, по ходу этой записи.** Первая редакция этого
> абзаца называла «М-очередь 75» — я вычел из 127 только 52 фотографии, снова
> посчитав головой при наличии команды. Поймано пересчётом перед записью в файл
> (`71` §6). Ровно то, за что абзацем выше отменены четыре чужих числа.

> **Общее у всех четырёх.** Ни одно не является ошибкой чтения — все четыре
> суть **числа, посчитанные головой при наличии команды, которая считает**.
> Ровно тот класс, который база описала в v1.87.0 («любое число в прозе — снимок,
> сделанный однажды») и в PIT-091. Кампания применяла правило к базе и не применяла
> к собственному журналу.

Заведено **`SYN-021`** — закрытие каталога проверяется той же командой, что и закрытие
репы. `PROCEDURE.md` §6 дополнена §6а.

---

### Разметка каталогов (`PROCEDURE.md` §2) — построчно, весь листинг

| Каталог | Файлов | Отметка | Основание |
|---|---|---|---|
| `.` (корень) | 4 | **М** | `README`, `WATCHLOG`, `CHANGELOG`, `.gitignore` — все прочитаны |
| `docs/` | 10 | **М** | стандарты и протоколы кита, 10 из 10 прочитаны |
| `templates/` | 3 | **М** | формы, в которых живёт метод; 3 из 3 прочитаны |
| `experiments/` | 2 | **М** | протокол эксперимента + требования к жанру; 2 из 2 |
| `build/` | 6 | **М** | метод бывает скриптом (`PROCEDURE.md` §7); 6 из 6 |
| `examples/<билет>/{etalon,raspoznavanie,razbor,recognition_report}.md` | 26 | **С** | наполненные экземпляры: распознанный текст билета, эталонные ответы, разбор ключей по сетям/ОС/матанализу. Форма всех четырёх типов уже поднята (`65` §2–§4, `templates/`), содержание предметное и вне билета невоспроизводимое |
| `examples/<билет>/README.md` | 7 | **М** | карточка примера — жанр, а не содержание; прочитаны 7 из 7 |
| `examples/2026-07-23-itkn-bilet06/incident_report.md` | 1 | **М** | разбор инцидента; прочитан |
| `examples/<билет>/*.pdf` | 7 | **С** | рендер `etalon.md`, пересобирается одной командой |
| `examples/2026-07-21-itkn/task*.png` | 2 | **С** | чертежи к задачам билета |
| `examples/<билет>/photos/MANIFEST.md` | 7 | **М** | манифест исходников — метод; прочитаны 7 из 7 |
| `examples/<билет>/photos/*.{jpeg,png,heic}` | 52 | **С** | исходные снимки экрана с билетом, хранятся ради воспроизводимости распознавания (`RECOGNITION.md` §6) |

**Итого С = 26 + 7 + 2 + 52 = 87. М = 4 + 10 + 3 + 2 + 6 + 7 + 1 + 7 = 40, все прочитаны.**
Проверено командой, а не сложено головой — см. ниже приёмку.

Предварительная отметка прошлой вахты («`photos/` 59 — кандидат в С») **не подтвердилась
как каталог**: 59 файлов делятся на 52 С и 7 М. Сомнение, из-за которого решение
откладывалось, было верным — и разрешилось ровно так, как требует §2: открытием файла.

---

### ⬆️ `docs/WORKFLOW.md` — 🔗 свести, покрытие названо поимённо

Прочитан целиком. Пайплайн из пяти шагов покрыт `65` **полностью и поимённо**:
шаг 0 (замер до чтения, сигнатура 1280/1024/2048, манифест исходников) → `65` §1 ·
шаг 1 (четыре состояния, метка в момент переноса, кадр-источник, классы путаницы) → `65` §2 ·
шаг 2 (обязательная остановка, опровержимость, нет яруса «по желанию») → `65` §3 ·
шаг 3 (чистовик и разбор порознь, два канала выдачи, только текущий элемент наружу) → `65` §4 ·
шаг 4 (системная ошибка → в реестр уроков) → `65` §5.

**Не поднимается.** Записываю поимённо, потому что это ровно тот случай, который
`PROCEDURE.md` §4а требует не оставлять на «и так покрыто»: проверено сравнением
структуры, а не темы.

**Одно исключение → `65` §1** (ниже): схема имён исходников `kadr-` / `orig-` /
`full-` / `crop-`.

### ⬆️ `docs/POLICY.md` → `18-documentation-philosophy.md` §9в — жанр «рабочие договорённости»

Единственный документ кита, не относящийся ни к методу распознавания, ни к выдаче.
Он закрывает **повторяющийся раунд разговора**, и в базе такого жанра нет
(`grep "чего не будет"` → пусто, `grep "преамбул"` → пусто).

Что в нём работает:

1. **Явный список «чего не будет»** — преамбул, рассуждений вслух, анализа метаданных,
   возвращений к теме внутри сессии, лекций, предупреждений на будущее, проверок задним
   числом. Договорённость задаётся **отрицательным пространством**: положительная
   формулировка («работать по существу») не запрещает ничего конкретного.
2. **«Даже правильный вывод, изложенный абзацем, нарушает договорённость.»** Не про
   верность, а про то, что тема закрыта. Самый частый способ нарушить — не возразить,
   а «просто отметить».
3. **Признак, объявленный нормой заранее.** «Снимок с идущим таймером — нормальный
   материал, комментария не требует.» Тот же приём, что раздел «это не сбой» в раннбуке
   (`08` §4): тревожный на вид признак называется нормой **до** того, как его увидят.
4. **Единственное исключение названо вместе с формой ответа** — «одна строка вопроса,
   без объяснений, почему он задан». Исключение без формы превращается в лазейку:
   спрашивающий каждый раз заново решает, сколько объяснять.
5. **«Прочитать один раз, дальше к вопросу не возвращаемся»** — у документа объявлен
   срок жизни в голове читателя.

Смежное — `71` §7б (граница против уклонения) и `71` §2 (немотивированный дисклеймер):
там про то, **как** проводится граница; здесь — про то, что часть договорённости
исполняется **молчанием**, и это надо записать, иначе исполнено не будет.

### ⬆️ `docs/RECOGNITION.md` → `65` §2 — метка составной единицы ставится по худшему элементу

Прочитан целиком; §1, §1.1, §3, §5, §6 покрыты `65` §1–§3 (перепроверено по структуре).
**Не покрыто одно, и оно общее:**

> «Метка ставится по **худшему** элементу задания: если условие читается, а один вариант
> ответа нет — всё задание 🟡, не 🟢.»

Карта уверенности агрегируется **минимумом, а не средним и не большинством**. Без этого
правила составная единица почти всегда получает метку своей большей части — а стоит она
своей худшей. Рядом — запрет по построению: задание с `⟦⟧` или `[нет в кадре]` **не может**
получить 🟢, независимо от ощущения уверенности. Ограничение снято с оценщика и передано форме.

Второе оттуда же, в `65` §2: **странный признак объясняется сначала своим каналом, потом
замыслом автора.** «Два задания с почти одинаковым текстом — почти всегда склейка строк,
а не замысел составителя.» Обратная сторона уже поднятого «отсутствие ожидаемого признака —
сигнал»: там аномалия недооценивалась, здесь переоценивалась бы.

### ⬆️ `photos/MANIFEST.md` (7 шт.) → `65` §1 — имя файла кодирует маршрут доставки

Одно и то же содержимое, пришедшее разными каналами, различается **префиксом имени**:
`kadr-NN` — рабочий набор в порядке чтения, `orig-NN` / `full-NN` — полноразмерные
передоставки тех же кадров, `crop-NN` — присланные кропы. Без этого манифест не
восстановим: `IMG20260723110402.jpg` и `5.png` — один и тот же кадр билета, и узнать
это через месяц не по чему.

Второе оттуда же: **пары разного разрешения привязываются корреляцией миниатюр**
(метод L-017), а группы опознаются сигнатурой «число кадров + разрешение» по сводному
журналу. То есть сопоставление копий делается замером, а не по именам и не по времени файла —
прямая родня `PROCEDURE.md` §3 (хеш как единственный фильтр, которому можно верить)
для случая, **когда байты заведомо разные**.

Остальное манифеста (столбцы: имя · исходное имя · разрешение · формат · вес · sha256 ·
что в кадре) уже в `65` §1. 📍 Прочитаны 7 из 7, форма идентична.

### ⬆️ `build/build_pdf.sh` → `71` §7е — проверка читает готовый артефакт, а не код возврата

Скрипт собирает PDF и **проверяет результат чтением самого PDF**:

```bash
BAD="$(pdftotext "$OUT" - | grep -nE '≈|\\begin\{|\\dfrac|\\frac|cases\}' || true)"
```

**Признак несработавшей трансформации — присутствие исходного синтаксиса в выходе.**
`pandoc` возвращает `0` и тогда, когда формула не отрендерилась и `\frac{a}{b}` уехало
в PDF текстом: код возврата говорит «инструмент отработал», а не «работа сделана».
Единственный способ отличить — открыть артефакт и поискать в нём следы того, чего
там быть не должно.

Приём переносится куда угодно, где есть трансформация: шаблон → документ (осталось
`{{placeholder}}`), сборка → бинарь, миграция → база, рендер → страница. Ищется
**не то, что должно появиться, а то, что должно было исчезнуть** — второе проверяется
надёжнее, потому что не требует знать правильный ответ.

Родня: `71` §7в (проверку нельзя проходить случайно) — здесь проверка по построению
не может пройти случайно, потому что в норме `grep` пуст **и это состояние наблюдаемо**
на любом сломанном прогоне.

### ⬆️ `build/*` + `templates/02` → `21` §4г-бис — у урока несколько исполняемых носителей

`21` §4г знает один носитель — строку в чек-листе. В `exam-kit` **тот же реестр `L-NNN`
доезжает до исполнения четырьмя разными путями**, и каждый выбран под момент, в который
урок понадобится:

| Носитель | Пример | Когда попадает на глаза |
|---|---|---|
| строка чек-листа | `[ ] Симметричные пары прочитаны по обоим концам (L-022)` | перед выдачей |
| **комментарий в коде рядом с обходом** | `# L-010: шаблон pandoc тянет lmodern, которого нет в минимальном texlive` | когда кто-то захочет «убрать этот странный стаб» |
| **предупреждение в выводе программы** | `ВНИМАНИЕ: похоже на пересжатый кадр — нарезка мало что даст, см. LESSONS L-014` | в момент запуска на негодном входе |
| **чек-лист внутри самого шаблона** (HTML-комментарий) | `<!-- ЧЕК-ЛИСТ (docs/LESSONS.md): [ ] ни одного ≈ ... -->` в `templates/02_etalon.md` | пока форма заполняется; в рендер не попадает |

Последний — самый дешёвый и самый недооценённый: чек-лист **едет вместе с формой**,
его нельзя забыть открыть, и он невидим в результате. Чек-лист в отдельном файле
требует вспомнить, что он есть.

**Правило:** носитель урока выбирается не по удобству записи, а по тому, **где будет
глаз в момент риска**. Урок про сборку — в скрипте сборки, а не в реестре уроков.

Плюс оттуда же, в `21` §4г: **пункт, который нельзя проверить машиной, помечается ручным,
а не выбрасывается.** `plot_helpers.py`: «подписи не накладываются (L-009: проверять
глазами после сборки)», `mark_point(offset подбирается вручную, см. L-009)`. Невозможность
автоматизировать — не повод убрать пункт, а повод назвать его ручным вслух.

### ⬆️ `examples/*/README.md` (7 шт.) → `68-writing-guides.md` §4б — карточка примера в коллекции

Семь почти одинаковых каталогов. Шесть из семи `README.md` держат один скелет:

```
Параметры · Файлы · Чем отличается от предыдущих · На что смотреть · Статус · Воспроизведение
```

Работают два раздела, которых обычно нет:

1. **«Чем отличается от предыдущих».** Без него коллекция — склад: семь примеров
   одного и того же, и читателю неоткуда узнать, какой открыть. С ним семь карточек
   складываются **в ряд**, где у каждой названа одна изменившаяся величина.
2. **«На что смотреть».** Пример без указателя читается как содержание («билет про сети»),
   а не как образец («так выглядит эталон для чисто тестового билета»). Указатель
   называет, ради чего пример лежит в коллекции.

Плюс **«Чего здесь нет и почему»** (в первой карточке) — и это не формальность:

> «Промежуточные версии не сохранены… при беглом просмотре примера легко скопировать
> не то. Ошибки описаны словами в `LESSONS.md`, этого достаточно.»

**Образец опаснее прозы: его копируют, не дочитав.** Поэтому в коллекции образцов
хранится только канон, а история ошибок живёт словами в реестре — не экземплярами.
Это исполняющая половина `21` §4б-бис («пример, противоречащий стандарту, — баг»):
там сказано не держать расходящийся пример, здесь — **не хранить историю примеров
как примеры**.

И **«Воспроизведение»** — одна команда, которой пример пересобирается
(`../../build/build_pdf.sh etalon.md etalon.pdf`). Дешевле любого описания.

> **🔴 Дефект `exam-kit`, в базу не идёт.** Седьмая карточка
> (`2026-07-22-itkn-bilet04/README.md`) скелет не держит: `Чем полезен · Состав · Темы`,
> без «На что смотреть», «Статус» и «Воспроизведение». Она же несёт единственную в ките
> точку «полное разрешение 3072×4096» — то есть **ряд рвётся ровно на том наблюдении,
> ради которого он и ценен**. Отклонение формы попало туда, где оно дороже всего,
> и это не совпадение: карточку писали не по шаблону именно потому, что билет был особенный.

### ⬆️ `CHANGELOG.md` → `28` §9а и `24` §4а — «отличие итерации» превращает журнал в ряд наблюдений

Прочитан целиком (407 строк, v1.0.0 → v1.21.1). Это не журнал изменений, а **журнал
живой гипотезы**, и держится он на одном разделе: почти каждая запись версии, помимо
`Добавлено/Изменено/Исправлено`, несёт **«Отличие итерации»** — чем этот прогон отличался
от предыдущих и какое правило он подтвердил или сломал.

Что это дало на дистанции — цепочка проверяется по самому файлу:

| Версия | Что записало «отличие итерации» | Судьба правила |
|---|---|---|
| v1.7.0 | все 11 кадров ровно 1280 по длинной | заведено L-014: «нужно полное разрешение» |
| v1.13.0 | то же 960×1280, но 0 баллов под риском | L-018: решает **тип контента**, не разрешение в отрыве |
| v1.15.0 | то же разрешение, лучший ракурс, 17/20 🟢 | L-018 уточнён: связка «разрешение + ракурс + тип» |
| v1.17.0 | расчётный билет на пересжатом — и 0 под риском | L-018 уточнён второй раз: **плотность символов**, а не «расчётный/описательный» |
| v1.18.0 | 0,3 МП — рекордно мало — и 13/20 🟢 | **L-014 опровергнут своей же практикой**, заведено L-020: мерить шаг строк |

Ни один из шести прогонов не ставился как опыт. Канал не варьировался нарочно —
он менялся сам. Ряд возник **только потому, что у каждой итерации было записано,
чем она отличается от предыдущей**, в одном и том же месте и в одной и той же форме.
Это `28` §9 («живая гипотеза») без `measurements.csv` и без `analyze.py` — носителем
работает `CHANGELOG`.

Второе, прямо в подтверждение `28` §10б, поднятого прошлой вахтой: **L-014 не был
ошибкой — он был верным выводом без названного диапазона.** «Нужно полное разрешение»
проверялось на кадрах одинакового охвата и на них верно; сломалось на обрезанном кадре,
то есть за границей, которую никто не назвал, потому что она не ощущалась границей.
Правило §10б получило подтверждение из другой репы и на другом материале — и, что важнее,
показало **признак**: вывод опровергается не новым замером, а **новым режимом входа**.

Ещё оттуда же — v1.14.0, «Почему»: закономерность «все билеты приходят пересжатыми»
не читалась, пока фиксация формата входа жила внутри каждого частного отчёта.
Свела её **одна строка на прогон в сводном журнале**. Уже поднято в `28` §6б,
здесь — независимое подтверждение того же приёма с названной ценой пропуска.

### 📍 Оставлено локальным — с причиной

- **`templates/02_etalon.md`** (кроме чек-листа в комментарии) — LaTeX-разметка под
  жанр экзаменационного эталона: `\begin{cases}`, `\lvert`, `0{,}5`, структура
  «Дано → Решение → Ответ». Метод формы поднят, содержание формы предметное.
- **`templates/01_recognition.md`** — 🔗 дубль `65` §3 постранично: сверочный лист,
  таблица `⟦⟧`, список `[нет в кадре]`, «известный риск», «🟢 = не сомневаюсь».
  Единственное своё — **легенда маркировки повторена в теле формы, рядом с местом
  заполнения**, а не только в стандарте. Мелко, но записываю: это тот же принцип,
  что чек-лист внутри шаблона (`21` §4г-бис).
- **`build/plot_helpers.py`** — matplotlib под чертежи задач оптимизации; палитра
  с назначением каждого цвета и оси через ноль. Поднят только приём «ручной пункт
  чек-листа». Остальное — предмет.
- **`build/header.tex`**, **`build/deskew_zoom.py`**, **`build/slice_image.py`**,
  **`build/release.sh`** — предметные реализации; надпредметное из них уже поднято
  (`71` §7г-бис, `65` §1, гейт дублей реестров в `revision_check.py`).
- **`experiments/2026-07-21-image-delivery.md`, строки 120–276** — 🔗 хвост протокола
  покрыт целиком: §4.3 контроль на дополнительную деградацию и §4.4 эквивалентность
  каналов → `28` §10а; §5 отброшенная методика OCR с причиной («инструмент зависит
  от варьируемой величины» — перспектива, а не поворот) → `28` §5а; §7.2 диагностический
  признак округления → `65` §1; §8 «Ограничения исследования» (четыре пункта, включая
  «визуальная проверка не выполнена») → `28` §10.1. Поднято прошлой вахтой, перепроверено
  по структуре — сходится.
- **`WATCHLOG.md`, `README.md`, `.gitignore`** — прочитаны прошлой вахтой, решения записаны.

### ⬆️ `examples/…/incident_report.md` → `reports/incident_postmortem_guide.md` §2а — три раздела, которых нет в шаблоне

> **Правка адреса по ходу.** Первым заходом раздел ушёл в `00-infrastructure/incident-postmortem-guide.md` —
> и это **мёртвая копия**: все девять живых ссылок системы ведут в `reports/`, а копия
> в ките числится в списке 29 групп дублей (`ROADMAP.md` §P2). Поймано тем, что перед
> записью в роадмап открылся сам список. Правка перенесена, обе копии оставлены
> побайтово равными — разводить их надо задачей §P2 целиком, а не поштучно.

Единственный разбор инцидента в ките, и в нём три элемента, которых нет ни в шаблоне базы
(`Summary · Impact · Timeline · Detection · Root cause · Contributing factors · Resolution ·
Lessons · Action items`), ни в `19-reporting-system.md`:

1. **Раздел «Не причина» — с доказательством.** «Не причина. Модель. Тот же движок
   на чистом тексте и на исправленном тексте отвечает верно.» Снятое обвинение
   записывается наравне с подтверждённой причиной, иначе его выдвинут снова —
   и починят работающее. Родня `PROCEDURE.md` §2 («каталог С записывается с причиной»)
   и уже записанного в этом файле «проверил обвинение в адрес собственного инструмента
   и снял его».
2. **Второй вид ущерба — доверие к собственному измерителю.** «10 баллов… и —
   существеннее — доверие к карте рисков: инструмент, который должен направлять внимание
   пользователя, направил его мимо. Второе чинится только устройством процесса,
   не извинениями.» **Потери пришли целиком из зоны, объявленной безопасной** — то есть
   карта рисков не просто промахнулась, она **увела проверку** с того места, где была
   ошибка. Измеритель, ошибающийся молча, хуже отсутствующего: при отсутствующем
   проверяют всё.
3. **Четвёртая точка починки — внешняя.** «Маршрут ошибки перекрыт в трёх точках…
   Четвёртая — внешняя: исходники в `photos/` позволяют любому перепроверить
   транскрипцию, **не доверяя ни меткам, ни мне**.» Три первых чинят процесс изнутри
   и проверяются только тем, кто их ставил. Четвёртая не зависит от чинившего —
   и именно она пережила бы полную потерю доверия к остальным трём.

Плюс мелочью, но точно: **канал верификации, сработавший случайно, возводится в штатный.**
Ответы двоих сдававших нашлись постфактум и поймали то, что прошло сверку;
`v1.19.0` записала их как **штатный канал** с правилом чтения — «два независимых
совпадающих ответа перевешивают одну реконструкцию, расхождение — команда перечитать».
Случайная удача превращена в процедуру.

---

## ✅ ПРИЁМКА `exam-kit` (`PROCEDURE.md` §6)

```
exam-kit: всего 127 = база 0 + С 87 + прочитано и решено 40
```

Проверка: `0 + 87 + 40 = 127` ✅ — сходится. Каждое слагаемое получено командой:

```
С 87 = фото 52 + предметные .md 26 + etalon.pdf 7 + task*.png 2
М 40 = корень 4 + docs 10 + templates 3 + experiments 2 + build 6
       + examples README 7 + incident_report 1 + photos/MANIFEST 7
```

**Копии:** две, `diff -rq` без `.git` и `_base/` — расхождений ноль, перепроверено
этой вахтой (`SYN-017`).
**Совпало с базой:** 0 — подтверждено, репа никогда не получала файлов базы в рабочее
дерево (только в `_base/`).

**Репа 6 из 57 закрыта.**

---

## 🔴 РЕВИЗИЯ ЗАКРЫТЫХ РЕП — две из шести не закрыты, а числятся пройденными

Заказ владельца 21.08.2026: «а ты прогнал агента по пройденным репам?». Ответ был
честный — нет: контрольная сверка вахты VI шла по **пяти** репам и `exam-kit` в неё
не входила. Перед запуском агента открыл записи приёмок сам, и этого хватило.

### Что нашлось до агента, чтением собственного журнала

| # | Репа | Записанная приёмка | Статус по её же записи |
|---|---|---|---|
| 1 | `personal-finance-dss` | `1501 = 47 (база) + ~950 (С) + 4 (прочитано) + ~500 (очередь)` | 🔴 **«Репа НЕ закрыта»**, строка 341 |
| 2 | `it-base` | `3197 = С 3043 + база 91 + дельта 14 + прочитано 49` | ✅ сходится: 3043+91+14+49 = 3197 |
| 3 | `portrait-of-taste` | `1572 = С 1259 + база/дельта 145 + прочитано 168` | ✅ сходится: 1259+145+168 = 1572 |
| 4 | `character-a-analysis` | — | 🔴 **приёмки нет вовсе** |
| 5 | `mission-control` | `102 = база 1 + С 12 + прочитано 89` | ✅ сходится |
| 6 | `exam-kit` | `127 = база 0 + С 87 + прочитано 40` | ✅ сходится (закрыта этой вахтой) |

**Репа 1.** Собственная запись прямо говорит «🔶 Репа НЕ закрыта» и называет **~500 файлов
в очереди**. Два слагаемых из четырёх — приблизительные, с тильдой, то есть арифметика
`PROCEDURE.md` §6 по построению не проверяема. После этой строки журнал продолжает работу
по репе (`knowledge/guides/` — 29 методичек), но **приёмки так и не появляется**:
поиск по всему файлу после строки 341 не находит ни закрытия, ни новой арифметики.

**Репа 4.** Блок занимает строки 737–791 и содержит `Листинг` · `Решение по каталогам` ·
`Прочитано и решено`. **Раздела приёмки в нём нет**, слова «закрыта» нет, арифметики нет.

### 🔴 Почему это не поймала контрольная сверка вахты VI

Она проверяла **итоговое число файлов на диске** и записала: «Записанные числа
подтвердились ВСЕ ПЯТЬ — точно, без расхождений», таблица `1501/3197/1572/337/102`.

Числа действительно верны. Но **проверено было не то утверждение**. «Файлов на диске
столько же, сколько записано» и «репа пройдена» — разные высказывания, и первое
не влечёт второго. У репы 1 совпадение числа 1501 ничего не говорит о ~500 файлах,
которые её же запись держит в очереди.

> **Класс тот же, что `SYN-021`, но уровнем выше.** Там «каталог закрыт N из N» писалось
> без поимённой сверки. Здесь **проверка выбрала легко проверяемую величину и была подана
> как проверка закрытости**. Общее: проверяется то, что удобно измерить, а вывод делается
> про то, что важно.
>
> Отягчающее: сверка была **специально заказана владельцем** ровно чтобы поймать
> пропущенное, прошла успешно и **укрепила уверенность** в том, чего не проверяла.
> Успешная проверка не по тому предмету хуже отсутствующей — отсутствующая
> оставляет вопрос открытым.

Заведено **`SYN-022`**.

### Что это меняет в счёте кампании

Записанное «пройдено 5 реп из 57» и «`exam-kit` — шестая» **неверно**.
Фактически закрыты и проверяемы: `it-base`, `portrait-of-taste`, `mission-control`,
`exam-kit` — **четыре**. `personal-finance-dss` и `character-a-analysis` требуют
возврата: первая — с ~500 файлами в очереди, вторая — с нуля по приёмке
(разметка каталогов у неё есть, арифметики нет).

**Порядок возврата — после `dota-dossier`, не вместо.** Обоснование: репа 7 уже вскрыта
(вход сделан, корень прочитан), бросать её на середине значит платить вход дважды.
Обе недозакрытые репы малы относительно неё (`character-a-analysis` — 337 файлов,
у неё не хватает только сведе́ния арифметики) и не блокируют ничего.

---


### Агент отработал: числа файлов сошлись все шесть, пробелы в решениях измерены

Механическая часть отдана субагенту (Sonnet), содержательная сделана мной. Отчёт агента
**перепроверен и уточнён** — как требует практика, записанная в `ADR-002` после первого
прогона делегирования.

**Задача B — итоговые числа файлов: сошлись все шесть.**
`1501 · 3197 · 1572 · 337 · 102 · 127` — ровно записанное. Третье независимое
подтверждение, и оно снова **ничего не говорит о закрытости** (`SYN-022`).

**Задача A — копии.** Подтверждено на всех шести: **ни у одной репы нет единственной копии**,
и `.git` везде лежит у отставшей. Новое, чего в записи не было:
`personal-finance-dss` в `_sync-base` **больше** канона (1589 против 1501) при **меньшей**
версии (8.25.0 против 8.28.0) — то есть отставшая копия содержит файлы, которых в каноне нет,
и «отстала» здесь не означает «подмножество».

### 🔴 Задача C — поимённое покрытие решениями. Считал сам, у агента цифры завышены

Проверка агента дала «488 без упоминания» у `personal-finance-dss` и т.п. Числа сырые:
в них попали **файлы самой базы**, разложенные в `00-infrastructure/` этих реп, — они
выбывают из очереди по хешу (`PROCEDURE.md` §0), решения им не нужно. Пересчитал сам,
по стему имени и с вычетом совпавших с базой байт-в-байт:

| Репа | `.md` в служебных каталогах | нет решения | из них = база | **РЕАЛЬНЫЙ ПРОБЕЛ** |
|---|---|---|---|---|
| `personal-finance-dss` | 566 | 478 | 42 | **436** |
| `it-base` | 104 | 61 | 60 | **1** |
| `portrait-of-taste` | 138 | 79 | 43 | **36** |
| `character-a-analysis` | 130 | 84 | 60 | **24** |
| `mission-control` | 35 | 1 | 0 | **1** |
| `exam-kit` | 15 | 0 | 0 | **0** |

**`exam-kit` — ноль.** Единственная репа, закрытая после введения поимённой сверки
(`PROCEDURE.md` §6а этой же вахты). Проверка и правило работают.

**436 у `personal-finance-dss` сходятся с её собственной записью «~500 в очереди»** —
два независимых способа счёта дали один порядок. Репа не закрыта, и это теперь подтверждено
числом, а не только фразой.

### 🔴 `portrait-of-taste`: заголовок «разобрано» покрывал выборку, а не каталог

Раздел журнала называется **«`docs/` и `reports/situations/` в `portrait-of-taste` — разобрано»**,
и под ним таблица на **девять** позиций. В самих каталогах — 138 служебных `.md`, из них
**36 не имеют ни одной записи**: `WORKFLOW_JOURNAL`, `TROUBLESHOOTING_ACCESS`, `GLOSSARY`,
`HOW_TO_USE`, `CLAUDE_IN_CHROME_GUIDE`, `PROJECT_HISTORY`, `INFRASTRUCTURE`, весь
`reports/audits/` и весь `reports/investigations/`.

При этом приёмка репы записана как сошедшаяся: `1572 = С 1259 + база/дельта 145 +
прочитано и решено 168`. **Арифметика сошлась при 36 непрочитанных файлах** — ровно тот
механизм, о котором предупреждает `SYN-021`: ошибка нижнего уровня поднимается наверх молча.

**Это `SYN-021` не как гипотеза, а как измерение по трём репам.** Карточка дополнена.

### Что проверкой НЕ измеряется — названо, чтобы не переоценили

- **Это прокси по имени файла.** Файл, решённый под групповой формулировкой без называния
  имени, считается пробелом. Проверено руками на двух репах: у `mission-control` из двух
  «пробелов» один оказался артефактом (`21-git-push-protocol` упоминался **без расширения**),
  остаётся один настоящий — `00-infrastructure/12-source-of-truth.md`.
  У `portrait-of-taste` все 36 — настоящие, групповая таблица их не покрывает.
- **Считались только `.md` в служебных каталогах.** Скрипты, хуки, `settings.json`,
  `evals.json` не вошли — а `PROCEDURE.md` §7 прямо запрещает фильтр по расширению.
  **Настоящий пробел не меньше измеренного.**
- Файлы, совпавшие с базой байт-в-байт, вычтены законно (§0).

### Состояние кампании после ревизии — честное

| Репа | Приёмка | Пробел | Вывод |
|---|---|---|---|
| `exam-kit` | ✅ сходится | 0 | **закрыта** |
| `it-base` | ✅ сходится | 1 | закрыта, один хвост |
| `mission-control` | ✅ сходится | 1 | закрыта, один хвост |
| `portrait-of-taste` | ✅ сходится | **36** | 🔴 **приёмка соврала**, вернуться |
| `character-a-analysis` | ❌ **отсутствует** | 24 | 🔴 вернуться |
| `personal-finance-dss` | 🔶 «НЕ закрыта» | **436** | 🔴 вернуться, это половина работы по репе |

**Пройдено не 6 из 57, а 3 из 57** — при честном счёте «закрыта = приёмка сходится
и поимённый пробел ≤ 1». Записанное «пройдено 5» держалось на приёмках, две из которых
не существовали, а одна была посчитана при 36 непрочитанных файлах.

### Опечатка субагента — второй раз подряд, тот же класс

В отчёте снова битый путь: `/Users/vasyaевdokimov/...` — кириллическая «е» внутри
латинского имени пользователя. Прошлая вахта поймала у того же типа агента
`Документы` вместо `Documents`.

**Два случая из двух прогонов — это уже не случайность, а свойство канала.**
`73` §4 («пересказ деградирует») подтверждён вторым независимым наблюдением, и вывод
усиливается: **из отчёта субагента берутся факты, но не строки.** Путь, имя, число —
перепроверяются командой, а не копируются. Ровно это и было сделано: все шесть чисел
и вся таблица C пересчитаны мной заново.

> Попутно: `check_mixed_script_names` в `revision_check.py` ловит такое **в именах файлов
> базы**, но отчёт субагента файлом базы не является и под гейт не попадает.
> Граница проверки названа, чтобы не возникло ложного ощущения защищённости.

---

## Репа 7: `dota-dossier` — вход перепроверен, корень прочитан

### Копии — три, числа субагента подтвердились

`find ~ -type d -name "dota-dossier"` **без списка каталогов** (`SYN-017`):

| Копия | файлов | `VERSION` | `.git` |
|---|---|---|---|
| `~/Documents/dota-dossier` | **5157** | 2.42.0 | нет |
| `~/Documents/система_репозиториев/dota-dossier` | **5148** | 2.42.0 | нет |
| `~/Downloads/_sync-base/dota-dossier` | **4567** | **2.34.0** | **есть** |

Перепроверено мной одной командой — сходится с тем, что записал субагент вахты VI.
**Снова подтверждается общесистемное:** `.git` лежит у самой отставшей копии
(`ROADMAP.md` §P2), а канон — без истории.

**Работаю по `система_репозиториев/` (5148)** — по `PROCEDURE.md` §1. Разница с
`~/Documents/` (5157) объяснена прошлой вахтой: `src/__pycache__` плюс три расходящиеся
позиции; кэш сборки в счёт метода не идёт.

### Листинг (`PROCEDURE.md` §1, вывод, не пересказ)

**1.1 Всего:** `5148`.

**1.2 Каталогов с файлами:** `636`. Верхушка переписи:

```
2995 ./data/dota_export_81705866/match_details
 417 ./knowledge/items
 205 ./data/dota_export_1710957099/match_details
 128 ./knowledge/heroes
 127 ./assets/hero_icons
 127 ./assets/hero_art
  57 ./data/screenshots
  51 ./base-repo/00-infrastructure
  34 ./base-repo/02-methodology-library
  31 ./src
  25 ./docs          25 ./assets/charts        19 ./data/hero_builds
  18 ./data/dota_export_81705866    17 ./coaching/data/channels
  15 ./docs/coaching  15 ./data/…/histograms   14 ./base-repo/01-claude-context/02-instructions
  12 ./coaching/volume-2-practice   12 ./base-repo/templates      12 .
  11 ./data/dotaconstants           10 ./data/dota_export_1710957099
  10 ./base-repo/templates/_archive 10 ./base-repo/reports/incidents
   9 ./reports/infrastructure   9 ./base-repo/reports   9 ./base-repo   9 ./assets
   8 ./base-repo/01-claude-context/03-project-docs   7 ./reports
   7 ./base-repo/03-role-kit   6 ./coaching/volume-1-foundations
   6 ./base-repo/reports/requirements  6 ./base-repo/reports/merges
   6 ./base-repo/01-claude-context/00-core
   5 ./scripts  5 ./reports/cards  5 ./reports/benchmarks  5 ./coaching/data
```

**1.3 Скрытые каталоги** (с `-mindepth 1`, починка из `SYN`-поправки): ровно один —
`./base-repo/.githooks` (1 файл). У самой репы скрытых каталогов **нет**:
ни `.claude/`, ни хуков, ни скиллов. Отличие от `personal-finance-dss`, где `.claude/`
оказался главной находкой.

**1.4 Расширения:**
`3273 .json` · `1418 .md` · `290 .png` · `57 .jpeg` · `41 .py` · `20 .gitkeep` ·
`16 .sh` · `6 .txt` · `5 (без расширения)` · `5 .csv` · `4 .html` · `3 .lua` ·
`2 .template` · `2 .repo-id` · `2 .gitignore` · `1 .yaml` · `1 .sha256` ·
`1 .example` · `1 .docx`.

**`1418 .md` — это самая насыщенная методом репа кампании** после `it-base`, и почти
весь объём сидит в `knowledge/` (545) и `docs/` (сотни в `heroes/` и `items/`).

### 🔴 Вложенная копия базы — категория приёмки, а не находка

`./base-repo/` внутри репы: **229 файлов, `VERSION` 1.36.0**, отставание **55 версий**.
Из 187 совпадений байт-в-байт с базой **180 лежат в ней**; настоящее пересечение —
**семь файлов** (шесть в `reports/infrastructure/`, один `scripts/deploy_all_versions.sh`).

**Решение по разметке:** `./base-repo/` (229) выделяется **отдельной категорией**
в арифметике §6 — не «база», не «С», не «М». Причина: это не содержание репы и не её
метод, а **устаревший слепок самой базы**, попавший сюда раздачей. Считать его «совпало
с базой» нельзя — 49 из 229 файлов **не совпадают** (база ушла на 55 версий вперёд),
и они попали бы в М-очередь как «новый метод», которым не являются.

Это исполнение открытого пункта `ROADMAP.md` §P2 (PIT-090, вычистить плоские
legacy-копии из 12 реп) — но чистка **не делается по ходу прохода**: она массовая
и недеструктивной обязана быть по построению (`00-CLAUDE-STOP.md` §Третье).

### Корень прочитан ПЕРВЫМ (`SYN-019`) — 12 файлов

Порядок соблюдён: до каталогов и до любого замера.

**Обязательный минимум — есть, в отличие от `exam-kit`:** `VERSION` (2.42.0),
`.repo-id` (`vevdokimovm/dota-dossier`), `ROADMAP.md`, `TASKS.md`, плюс `ROADMAP_HISTORY.md`
и `TASKS_HISTORY.md`. **Нет:** `.repo-class`, `START-HERE.md`, `00-CLAUDE-STOP.md`.
Вахтенный журнал лежит не в корне, а в `docs/WATCHLOG.md`.

**Что репа уже знает о себе** (ради чего §4 и требует читать корень первым):
консолидация двух расходящихся веток описана целым отчётом `MERGE_REPORT.md`,
и он же содержит эмпирические сверки. То есть **вопрос «какая копия канон» в этой репе
уже решался и задокументирован** — переоткрывать его замером не нужно, надо прочитать.

---

### Разметка каталогов (`PROCEDURE.md` §2) — построчно, посчитано командой

| Категория | Файлов | Отметка | Основание |
|---|---|---|---|
| корень | 12 | **М** | прочитан первым (`SYN-019`), решения ниже |
| `docs/*.md` верхний | 25 | **М** | методички проекта: `METHODOLOGY`, `ARCHITECTURE`, `LESSONS`, `WATCHLOG`, `TOOLS`, `DATA_ECOSYSTEM`, `SETUP_AUTOMATION`, `RELEASE`, `DAILY` |
| `docs/coaching/` | 15 | **М** | по §4а «при сомнении — повышать»: пронумерованный ряд ресёрчей, и в нём `10_ti2026_blind_forecast.md` — предрегистрация прогноза; форма ряда может быть методом, читаем |
| `reports/infrastructure/` | 9 | **М** | разборы деплойера и тестирования — тёзки шести файлов базы, требуют сравнения редакций |
| `reports/benchmarks/` | 5 | **М** | `FROZEN`-файлы + `BLIND_FORECAST_LOCK.sha256` — механика предрегистрации |
| `knowledge/pro-thinking/*` верхний | 4 | **М** | `README` (метод и приоритеты), `PIPELINE`, `ROSTERS`, `SOURCES.template` |
| `coaching/*.md` верхний | 2 | **М** | `PHILOSOPHY.md`, `README.md` — принципы тренерской системы |
| `src/**` | 38 | **М** | метод бывает скриптом (`PROCEDURE.md` §7); плюс `src/corpus/` и `src/history_lab/` |
| `scripts/` | 5 | **М** | деплой, расписание, бэкап — инфраструктура |
| `tools/` | 3 | **М** | `collect.py`, `fetch_constants.py`, `README.md` |
| `bots/` | 4 | **М** | Lua-скрипты бота-тренера + `README`; 4 файла, дешевле прочесть, чем рассуждать |
| **ИТОГО М** | **122** | | все подлежат чтению |
| `base-repo/` | **229** | **отдельная категория** | вложенная legacy-копия базы, `VERSION` 1.36.0, отставание 55 версий — не база, не С, не М (обоснование выше) |
| `data/` | 3331 | **С** | сырые выгрузки OpenDota: 3200 `match_details`, агрегаты, гистограммы, снапшот `dotaconstants`, 57 скриншотов |
| `knowledge/heroes` + `items` + транскрипты | 581 | **С** | сгенерированные справочники по 127 героям и 409 предметам + 36 расшифровок интервью про-игроков |
| `docs/heroes` + `items` + `legacy` | 543 | **С** | второй сгенерированный корпус того же предмета (см. находку ниже) + прошлые разборы |
| `assets/` | 288 | **С** | 127 иконок, 127 артов, 16 графиков |
| `coaching/data` + два тома | 40 | **С** | тренировочные материалы под конкретного игрока |
| `reports/` прочее | 13 | **С** | готовые досье `.html`, отчёты калибровки по датам, PNG-карточки |
| `cache/` | 1 | **С** | `heroStats.json`, кэш ответа API |
| **ИТОГО С** | **4797** | | не читаем, причина у каждой строки |

```
ПРОВЕРКА: 122 + 229 + 4797 = 5148 ✅ (всего по 1.1 — 5148)
```

Числа получены скриптом по правилам-предикатам, а не сложены головой — прямое следствие
`SYN-021` этой же вахты.

**Приёмка §6 будет иметь вид:** `5148 = вложенная база 229 + С 4797 + прочитано 122`.
Слагаемое «совпало с базой» здесь **не применяется**: 180 из 187 совпадений лежат
внутри `base-repo/`, то есть уже учтены отдельной категорией, а оставшиеся 7 лежат
в `reports/infrastructure/` и `scripts/` — они в М и получат решение поимённо.

### 🔴 Находка репы: два независимых корпуса об одном предмете, 545 файлов, пересечение ноль

Обнаружено при разметке, до чтения: в переписи каталогов рядом стоят
`knowledge/items` **417** и `docs/items` **409**, `knowledge/heroes` **128**
и `docs/heroes` **130**. Числа близки к естественной мощности предмета
(409 предметов, 127 героев) — это и был признак.

| Когда | Что построено | Чем |
|---|---|---|
| 14.08.2026, v2.34.0 | `knowledge/heroes/` 128, `knowledge/items/` 417 | `src/corpus/` (три модуля) |
| 20.08.2026, v2.40.0 | `docs/heroes/<slug>/overview.md` 130, `docs/items/<slug>/overview.md` 409 | `src/hero_data_sheet.py`, `src/item_data_sheet.py` |

Один автор · шесть дней · один источник (`data/dotaconstants` + `patchnotes.json`) ·
один жанр документа (характеристики, способности, таланты, полная история патчей).

**Проверено командой:**
- `sha256` по обоим деревьям — **общих хешей 0** (у героев и у предметов);
- `grep -c corpus src/hero_data_sheet.py src/item_data_sheet.py` → **0, 0**;
- `grep -c "docs/heroes|docs/items" src/corpus/*.py` → **0** во всех четырёх;
- `grep -c "knowledge/heroes|knowledge/items" README.md ROADMAP.md` → **0, 0** —
  оба фронтальных файла репы не упоминают первый корпус **вовсе**, при том что
  `ROADMAP.md` §P2 подробно описывает второй как новую работу («Слой 1 — авто-факты,
  готов полностью (v2.40.0)»).

**Почему ноль общих хешей — не доказательство различия, а ожидаемый результат.**
Разный рендер одних и тех же фактов даёт ноль общих байт по построению. Это ровно
оговорка `PROCEDURE.md` §3: прежде чем опереться на хеш, назови, **что** сравниваешь.
Здесь сравнивать надо было не байты, а **предмет**.

⬆️ Поднято в `72-source-of-truth.md` **§4д** — с признаком, который сработал
(одинаковая мощность множества в двух местах) и с разбором, почему не срабатывают
имя, хеш и размер. Отдельно записано, что это **не** PIT-072 («забыли про чужой
инструмент»), а его более неприятная форма: **забыли про свой, шестидневный**.
Память о собственной работе уходит между сессиями первой, и у владельца четырёх
аккаунтов — гарантированно.

**В базу как дефект `dota-dossier` не идёт, но записано для сессии по репе:**
сведе́ние двух корпусов — отдельная задача, не побочный эффект прохода. У каждого
есть непересекающееся: `INDEX.md` с приоритетом по пулу владельца — только в старом,
живые срезы меты по рангам — только в новом. Сведение вслепую потеряет ровно эту разницу.

---

## 2026-08-21, вечер — v1.96.0: кампания НЕ двигалась, и это записано

Работа шла по заказам владельца (`ROADMAP.md` §P1.6a–c): собран автономный контур,
лимиты переведены в данные, три ресёрча закрыты замером установленной версии,
витрина пересчитана по GitHub. **Ни одна репа не читалась.**

Записано строкой, потому что этого требует `check_campaign_log` и потому что
**пауза, записанная как осознанная, отличается от забытой**: следующая вахта
по этой строке видит, что проход по `dota-dossier` не заброшен, а отложен.

**Состояние `dota-dossier` без изменений с утра:** копии перепроверены, корень прочитан
первым, разметка посчитана скриптом (`5148 = вложенная база 229 + С 4797 + М 122`),
из М прочитано 12 файлов корня плюс `reports/benchmarks/README.md` и `docs/METHODOLOGY.md`.
Следующий шаг — остаток М-очереди.

---

## 2026-08-21, вечер — v1.97.0: кампания не двигалась, работа по витрине

Заказы владельца по шести проектам витрины. Репы не читались, `dota-dossier`
ждёт с разметкой и прочитанным корнем (М-очередь — 122 файла).

**Одно касается кампании напрямую.** При сборке движка замерена разбивка `heroStats`
по всем 127 героям: ранги 1–7 живые (39 млн пиков), **ранг 8 (Immortal) обнулён**.
Это уточняет запись самой репы `dota-dossier` («OpenDota обнулил разбивку») —
обнулён только восьмой ранг. Когда дойдём до чтения её М-очереди, это уже проверено
и переоткрывать не нужно.

---

## 2026-08-21, ночь — v1.98.0: кампания не двигалась, ставился хук

Работа по заказу владельца: гейт против класса `PIT-G`. Репы не читались.

**Касается кампании косвенно, но важно.** `check_campaign_log`, заведённый утром,
теперь запускается **не только руками**: `.claude/hooks/ritual-gate.sh` прогоняет
`revision_check.py` на каждом конце хода. То есть требование «записывай в лабораторию
в момент» получило исполнителя, который не зависит от того, вспомнила ли вахта
запустить гейт.

`dota-dossier` без изменений: разметка посчитана, корень прочитан, М-очередь — 122 файла.

---

## `dota-dossier` — `docs/` пошёл, прочитано 3 из 25

### ⬆️ `docs/LESSONS.md` → `71` §7е-бис и `28` §6в

**`71` §7е-бис — пустой срез есть законное состояние, а не сбой.** §13 оттуда:
у аккаунта не нашлось напарников с ≥30 совместных игр, `/peers` вернул пустой список,
обращение к колонке уронило **весь анализ** (`KeyError: 'with_games'`). Данные были
в порядке, ответ API тоже; не в порядке было **допущение, что срез непуст**.
Записано с различителем: спроси, бывает ли законный вход, где данных нет. Бывает —
секция опциональна и остаётся пустой с пометкой. Не бывает — пустота **и есть**
ошибка, падать громко.

**`28` §6в — порог отсечения объявляется вместе с числом.** §12 оттуда: годы
с <15 матчами исключены, для рейтинга героя порог 20 игр, винрейт с интервалом
Уилсона, а не голым процентом. Поднято тремя частями, из которых обычно забывают
третью: порог назван **числом** · назван **до** того, как посмотрели результат
(иначе подгонка — всегда найдётся отсечение, при котором вывод красив) ·
**отсечённое не исчезает из вывода**, а помечается.

> Формулировка, которой в базе не было: **порог и интервал решают одну задачу
> с разных сторон.** Порог убирает то, о чём говорить рано; интервал показывает,
> насколько уверенно говорится об оставшемся. Без порога в таблицу лезет мусор,
> без интервала мусор выглядит сигналом.

📍 **Остальное `LESSONS.md` — локальное или уже покрыто.** Heredoc и `__DATA__`
(§3) — частный случай `80` §4; мусор macOS в архивах (§8) — PIT-025 и `43` §2;
секреты в `.env` (§9) — `32`; §14–§17 предметные (винрейты по атрибутам, темп
против фарма, «любимое ≠ выигрышное»). Названо поимённо, чтобы не переоткрывали.

### ⬆️ `docs/DATA_ECOSYSTEM.md` → `80` §5б и §5в — **лучшая находка репы пока**

**`80` §5б — «не получилось» ≠ «невозможно»: третье состояние вместо двух.**
Сборщик помечал матч «нераспарсиваемым», если парс не пришёл за ~3 минуты.
По хронологии сбоев провайдера **очередь держалась часами**. Матч, попавший в неё
во время инцидента, получал вечную метку и **больше не переспрашивался** — хотя был
жив и восстановим.

Поднято целиком: три состояния (`ok` · **`pending`** · `dead`) · перевод в `dead`
**только по совпадению двух признаков** (исчерпаны попытки **И** объект старше окна
жизни), потому что каждый по отдельности врёт в свою сторону · окно берётся
из свойства предметной области, **не знаешь окна — не имеешь права ставить `dead`** ·
**шаг опроса меньше характерного времени сбоя**, а не круглое число («каждые 3 часа,
а не раз в сутки: очереди измеряются часами»).

Прямая родня уже записанного `80` §3 (`404` = «я его не вижу», а не «объекта нет») —
там про **права**, здесь про **время**. Один класс: двоичный ответ превращает
временное состояние в вечное.

**`80` §5в — внешний сервис это транспорт, а не хранилище.** Документ ценен тем,
что аргумент — **хронология с датами**, а не впечатление: за полгода трёхсуточный
простой из-за сгоревшего железа · сломанный API подсистемы без починки неделями ·
**инвесторы перенаправили ресурсы** · DDoS дважды в сутки · деградация публичного
трекинга. Ни один отказ не чинится с твоей стороны и ни один не является аварией.

Четыре следствия, каждое проверяемо: сырьё сохраняется **до** обработки, а не вместо ·
не снятое сегодня задним числом не снимается (**2919 записей из ~3000 уже потеряны**,
пока проект «собирался начать») · резервная копия отдельно от рабочей ·
хронология ведётся датами, чтобы шаг опроса выбирался числом.

> **Признак применимости назван:** данные, которые источник хранит **ограниченное
> время**. Есть срок жизни — цена промедления **невозвратна**, в отличие от почти
> всего остального в инфраструктуре.

### Прогресс

Прочитано из М-очереди **15 из 122**: корень 12 · `reports/benchmarks/README.md` ·
`docs/METHODOLOGY.md` · `docs/LESSONS.md` · `docs/DATA_ECOSYSTEM.md`.
*(корень и два первых — ранее; `LESSONS` и `DATA_ECOSYSTEM` — этой сессией)*

**Осталось в `docs/` верхнего уровня: 23.** Дальше по порядку — `ARCHITECTURE.md`,
`TOOLS.md`, `INFRASTRUCTURE.md`, `SETUP_AUTOMATION.md`, `RELEASE.md`, `DAILY.md`.

### ⬆️ `docs/ARCHITECTURE.md` → `28` §6г — два слоя и дельта между ними

**Сильнейшее из репы, и в базе этого не было вовсе.** Приём: два измерения одного
предмета из разных источников **не усредняются и не выбирают победителя** — из их
разницы получается третья величина, которой нет ни в одном.

Условие, при котором работает, названо в источнике одной фразой: слои
**«отвечают на разные вопросы, собираются из разных источников и врут по-разному»**.
Последнее главное: два источника с одинаковым смещением дают ту же ошибку дважды
и создают ложную уверенность «сошлось».

| Слой | Не видит по построению |
|---|---|
| общая популяция | **тебя** — твои ограничения и профиль |
| собственная история | **изменения предмета** — данные оценивают прошлую версию мира |

Формулировка, которую взял дословно, потому что она делает мысль применимой:
**твои сто наблюдений трёхлетней давности — оценка прошлой версии предмета;
она не устарела «немного», она про другой объект.**

**Дельта измеряет зависимость от исполнителя** — величину, которую ни один слой
по отдельности не выдаёт. Родня — `28` §10б (дрейф между стратами): там два среза
одного источника, здесь два разных источника.

**Отдельно поднял квадрант «работает вопреки общей оценке»** — с проверкой, которой
в оригинале нет: расхождение становится **эджем** только если пережило порог §6в
и интервал, и названа **проверяемая причина** отличия. Иначе остаётся расхождением.
Ценность в том, что источник говорит прямо: *публичный рейтинг про них молчит,
значит соперники их не изучают* — расхождение с консенсусом как **преимущество**,
а не как ошибка измерения.

### ⬆️ `docs/TOOLS.md` → `34` §6а, §6б

**§6а — на ноутбуке `cron` теряет запуски, `launchd` их догоняет.** В репе это
записано мимоходом (`install_schedule.sh --launchd` «догоняет пропуски после сна»),
а следствие крупное: закрыл крышку на ночь — ночные запуски **не случились**,
и никакой ошибки нигде нет. Расписание корректно, задача исправна, данных нет.
Отказ тихий, обнаруживается дырой в ряду через недели. Для ноутбука `cron` —
неверный выбор **по умолчанию**, а не «другой синтаксис того же».

Туда же — правило шага расписания: **признак, что шаг выбран по привычке, — его
нельзя объяснить свойством источника.**

**§6б — перед записью в чужой конфиг резервная копия.** `hero_grid.py --install`
пишет в конфиг игры и сначала бэкапит. Обобщено: чужой файл настроек человек мог
править руками, и восстановить его неоткуда — в отличие от того, что генерирует сам
инструмент. Совпадает с `00-CLAUDE-STOP.md` §Третье на границе с чужой системой.

### 🔴 Дефект `dota-dossier` — в базу не идёт, записан для сессии по репе

**Два инструмента делают одно и то же, и оба живые.** `match_archiver.py` («основной»)
и `match_watcher.py` («ранняя лёгкая версия, оставлена для совместимости»).
Измерено, а не на глаз:

- обоснование в справочнике **повторено дословно дважды** — `grep -c "Реплеи Valve
  живут ограниченное время" docs/TOOLS.md` → **2**;
- расписания в одном файле **расходятся**: `0 */3` для архиватора, `0 */6` для вотчера;
- у вотчера **две состояния** вместо трёх — то есть в нём живёт ровно тот дефект,
  который архиватор чинил (вечная метка «мёртв» после одного неудачного опроса).

Это класс **`PIT-E`** из лидерборда (`reports/incidents/PITFALLS.md`) — «дублирование
логики вместо общей функции». **Счётчик +1**, случай дописан: раньше класс собирался
из своих скриптов, теперь подтверждён на чужой репе.

Опасность не в лишнем файле, а в том, что **«оставлено для совместимости» звучит
безобидно**: следующий читатель поставит в расписание вотчер (он описан первым
в разделе про расписание) и получит починенный дефект обратно.

### Прогресс

Прочитано из М-очереди **17 из 122**. `docs/` верхнего уровня: **4 из 25**
(`METHODOLOGY`, `LESSONS`, `DATA_ECOSYSTEM`, `ARCHITECTURE`, `TOOLS`).
Дальше — `INFRASTRUCTURE.md`, `SETUP_AUTOMATION.md`, `RELEASE.md`, `DAILY.md`.

### ⬆️ `docs/INFRASTRUCTURE.md` → `78` §4а, §4б

**§4а — условие подъёма есть обобщаемость, и она проверяется вопросом.**
`78` §4 говорил, что решение по кандидату человеческое, но **критерия не давал**.
Критерий нашёлся: *«работает ли она в репе другой предметной области?»*

Из четырёх шагов процедуры работает второй; остальные — обвязка. Добавил
от себя признак, которого в источнике нет: **правило не обобщается, если его нельзя
высказать, не называя предмет исходной репы.** Не вынимаются «герои», «матчи»,
«билеты» — практика предметная, место локальное.

Плюс третий шаг, о котором забывают: поднятое **не остаётся внизу**. Иначе две живые
редакции — `PIT-G`.

**Обратная сторона — правило приоритета при расхождении:** *«при расхождении прав
`_base/`; если местная практика лучше — поднимается наверх»*. Ценно тем, что закрывает
лазейку «у нас исторически иначе»: законных исходов ровно два, и «оставить как есть,
потому что работает» — не из них. Он и порождает 57 разных стандартов.

**§4б — «продублировано намеренно» есть то же расхождение, только с оправданием.**
Живой случай прямо в этом файле, и он показателен:

| §2 | §6 |
|---|---|
| «Локальные копии этих правил заводить **нельзя** — гарантированный рассинхрон» | «Здесь они **продублированы намеренно**» |

Два раздела одного документа, противоположные правила. **Намерение не меняет механику:**
копия отстаёт ровно так же, разница лишь в том, что её отставание никого не удивит.
Записана замена: ссылка на канон + строка «зачем здесь»; неизбежна копия — **дата
снятия и версия источника** в шапке.

### 🔴 Тот же файл неверен ТРИЖДЫ подряд — измерено

Документ описывает вложенную базу как **v1.13.0**:

```
в тексте:     v1.13.0
на диске:     1.36.0     ← отставание 55 версий от канона
канон:        1.98.0
```

И там же: «регрессионный набор деплойера (**30 кейсов / 89 проверок**)» — сегодня
**286 проверок**. Класс `PIT-091`, число в прозе без гейта.

Это не придирка к чужой репе, а **подтверждение правила на независимом материале**:
документ, описывающий состояние другого артефакта, устаревает **молча и в первую
очередь**. У базы этот класс закрыт гейтом `check_prose_counts` (v1.94.0) —
здесь видно, как выглядит тот же класс без гейта.

### ⬆️ `docs/SETUP_AUTOMATION.md` → `08` — два раздела

**«Автоматизация отваливается молча — у неё должен быть признак живости».**
Самая недооценённая часть расписания: поставить задачу легко, **заметить, что она
перестала работать, — нет**. Ошибки нет, задача в `crontab`, скрипт исправен,
результата больше не появляется.

Признак взят у источника и уточнён: не «запускалось ли», а **расхождение между тем,
что происходило, и тем, что зафиксировано** — «число обработанных стоит, **а работа
шла**». Обе половины обязательны: «не растёт» само по себе ничего не значит.

Отсюда же требование к инструменту: уметь ответить на три вопроса одной командой —
**сколько сделано · сколько ждёт · когда последний запуск**. Без этого проверка
живости требует раскопок, а значит не делается.

> **Причина, названная в источнике дословно и стоящая цитаты:** *«если запускать руками —
> забудешь. Не потому что раздолбай, а потому что это скучная фоновая задача
> без обратной связи»*. Та же механика, что у `69` §4в (гейт, который надо запустить):
> расписание не полагается на память, но **у самого расписания обратной связи тоже нет**,
> поэтому цепочка обрывается на шаг позже, если нет признака живости.

**«Частота задаётся ценой потери, а не удобством».** Интервал резервного копирования
выбирается наоборот: *при копировании раз в два месяца максимальная потеря — два месяца
работы*. Не «раз в месяц, потому что круглое», а столько, сколько не жалко потерять.
Плюс короткое и точное: **копия считается существующей, когда их две в разных местах** —
одна рядом с оригиналом гибнет от одного события.

### Правило применено к себе сразу

`08` требует признака живости — проверил **свой же** Stop-хук, заведённый час назад:
`grep ritual-gate` по логу сессии даёт **29 следов вызова**. То есть хук не просто
исправен при ручном запуске, а **действительно вызывается средой**. Разница ровно та,
о которой раздел: «скрипт работает» и «скрипт работает у пользователя» — разные
утверждения (`PIT-A` из лидерборда).

### Прогресс

Прочитано из М-очереди **19 из 122**. `docs/` верхнего уровня: **6 из 25**
(`METHODOLOGY`, `LESSONS`, `DATA_ECOSYSTEM`, `ARCHITECTURE`, `TOOLS`,
`INFRASTRUCTURE`, `SETUP_AUTOMATION`).
Дальше — `RELEASE.md`, `DAILY.md`, `HISTORY.md`, `REPORT.md`, `ANALYSIS.md`.

### ⬆️ Батч 3: `METHODOLOGY`, `PRELLEX_PROTOCOL`, `COMMUNITY_REVIEW`, `COACHES_REVIEW`, `CALIBRATION_LAB`

**`28` §6д — одинаковый формат таблицы создаёт иллюзию сравнимости.** Из `METHODOLOGY`:
две карточки собраны из разных источников (пабы одного диапазона против топ-турниров),
и **«прямое сравнение — методологическая ошибка, даже если числа выглядят сопоставимо
по формату таблицы»**. Глаз сравнивает форму, а не происхождение. Записано с правилом:
происхождение и дата сбора у каждой строки; разные — таблица делится или несёт пометку.
Туда же замер: фильтр «есть идентификатор лиги» пропускал **703 мусорных записи
из 1317** — проверка фильтра это не «выглядит разумно», а **сколько именно пропустил**.

**`71` §5б — запрет источника уважается независимо от возможности обойти.** Ценно
не само правило, а его оформление: несколько источников закрылись `robots.txt` и
Cloudflare, обойти технически можно — записано *«сознательно не делаем»*. И **исключение
оформлено четырьмя основаниями**, включая главное: **«решение подтверждено владельцем,
не единоличная трактовка агента»**. Исключение, которое агент выписал себе сам,
неотличимо от удобной трактовки.

**`68` §4в — жанр «разбор чужого инструмента».** Из `PRELLEX_PROTOCOL` + `COACHES_REVIEW`
(жанр появился дважды независимо — по `PROCEDURE.md` §3а это довод). Пять разделов,
из которых работают два последних: **«что переносим к себе — со статусом»**
(идея → внедрено/частично/бэклог) и **«чего у них нет, а у нас есть»** — второй
обязателен даже когда чужое сильнее, иначе разбор читается как самоуничижение.
Правовая рамка ставится **первым абзацем**: изучать принципы легально полученной
программы — практика, копировать код — нет. Плюс мелочь, применимая везде:
**свои артефакты в чужом пространстве маркируются суффиксом**, чтобы уборка
удаляла только собственное.

**`28` §6е — порог абсолютный или относительный есть разные утверждения.**
Перцентильная схема удобна и потому **скрывает** сдвиг всей популяции: верхняя группа
никогда не пуста. Абсолютная неудобна и честна. Практика — считать обе, расхождение
само по себе наблюдение. Туда же: **порог выбирается из распределения** (квартили),
а не назначается — назначенный устаревает молча вместе с данными.

**`PIT-099` — вывод о корпусе по выборке, которая ничего не представляет.**
Найдено в чужой репе и **признано её же автором**: *«отсмотрел два поста из 1794 —
оба оказались розыгрышами 2022 года — и списал канал как развлекательный»*.
При полном чтении канал оказался лучшим источником в корпусе.

Дополнено **второй формой того же класса, из того же файла**: искажает не выборка,
а **канал** — *«основной контент в видео, а не в тексте, поэтому по экспорту канал
кажется беднее, чем есть»*. Выборка может быть полной, а вывод неверным, потому что
доступен был не весь источник. И **плотность источника измеряется числом**:
«5 полезных постов из 339» — с этим можно спорить предметно, с «бедный источник» нельзя.

> Цена ошибки **асимметрична**: ложно принятый источник снимается ревизией за минуту,
> ложно отвергнутый **не обнаруживается никогда**. Это закон 4 кампании на материале
> источников, а не файлов.

**`28` §5б — значение, снятое сейчас, приписывается прошлому событию.** Из
`CALIBRATION_LAB`: *«ранг игрока на момент выгрузки, а не на момент матча; для старых
матчей те люди давно уехали»*. Ошибки не возникает — поле заполнено, тип верный,
значение правдоподобное; врёт **привязка ко времени**. Хуже того: между событием
и замером мог случиться **общий сдвиг шкалы**, и тогда смещены все записи разом,
а внутренняя согласованность сохраняется — по самим данным не проверить.

**`28` §5в — прокси годится для тренда, но не для значения.** Восстановление скрытой
величины через наблюдаемую с честной границей: *«годится для вердикта растёт/встал,
не для точного числа»*. Два усилителя: скользящее окно вместо точек и **подтверждение
вторым окном подряд** («около нуля две проверки подряд — значит встал по-настоящему,
а не по шуму одного окна»). Плюс **покрытие данных как отдельная метрика** с порогом
доверия — без неё результат выглядит одинаково убедительно при 90 % и при 15 %.

Отдельно поднят критерий выбора метрики: *«доход можно набрать убийствами; добивание
на десятой минуте — только фармом»* → **метрика с одним способом достижения разделяет
лучше составной**; при равной доступности берётся та, у которой **нет обходного пути**.

### Прогресс

Прочитано из М-очереди **26 из 122**. `docs/` верхнего уровня: **13 из 25**.
Осталось в `docs/`: `REPORT`, `ANALYSIS`, `TI2026_ANALYSIS`, `POOL_ADVICE`,
`hero_pool_guides`, `hero_archetypes`, `player_profile`, `keybinds`, `chat_wheel`,
`bots_and_training`, `WATCHLOG`, `HISTORY`.

### ⬆️ Батч 4: `ANALYSIS`, `POOL_ADVICE`, `bots_and_training`, `keybinds`, `player_profile`, `REPORT`, `hero_pool_guides`, `chat_wheel`

**`28` §6ж — немонотонность по оси значит, что ось выбрана неверно.** Разрез
по «сложности» дал провал **в середине** (52,9 / 49,6 / 51,8). Соблазн — объяснить
середину; верный ход — проверить ось. Оказалось, в середине сидят объекты другого
типа, и вывод переформулировался точно: *«проблема не в руках, а в типе»*.
Из этого следует **другое действие**, чем из «средние неудобные».

**`28` §6з — метрика, которая НЕ различает, тоже локализует причину.**
*«Урон по героям почти не отличается в победах и поражениях — значит теряешь
не в драках, а в конвертации»*. Обычно ищут разделяющие метрики; неразделяющая
отвечает, **где причины точно нет**, и каждая такая сужает поиск. В отчёт идут обе
группы, иначе вторая выбрасывается как «ничего не показало» и следующий проход
ищет там же снова.

**`28` §6и — тренировочный стенд: замер честен там, где убран внешний фактор.**
*«В бою влияет соперник, на стенде — только ты, поэтому цифра сравнима между
попытками»*. Плюс граница, снимающая обычное возражение: **стенд меряет потолок**,
и *«если на стенде максимум 300, то в бою 200 не появятся»*. Плюс два правила:
**без записи замера тренировка не является тренировкой**, и **инструмент называет
долю проблемы, которую закрывает** («из четырёх слабых мест стенд закрывает одно»).
Отдельно — порядок вопросов: **сначала «подошёл бы вообще», потом «доступен ли»**.

**`28` §6к — слабость есть оборотная сторона силы: у них одна причина, а не две.**
*«Проседает именно потому, что силён в другом; тот же инстинкт даёт лучший
результат»*. Следствие, ради которого поднято: **«починка» слабости убивает силу** —
это одна ручка, а не две. Записан различитель (устраняются ли по отдельности)
и замена действия: **не менять природу, а сменить условия**, где она окупается.

**`28` §6л — согласованность независимых срезов есть признак надёжности.**
*«Все срезы сходятся в одну картину, данные рифмуются — это редкость»*.
С обратной стороной: если срезы **не** сходятся, вывод не «данные плохие»,
а **«действующих механизмов несколько»**.

**`82` §8а — практика без обратной связи не улучшает, а закрепляет.**
Сильнейшее из репы по формулировке: *«плато объясняется не нехваткой часов
и не характером, а отсутствием обратной связи: 3500 часов практиковались
без единого замера»*. Правило: **прежде чем увеличивать вложение, проверь,
есть ли замер** — удвоенные часы без обратной связи дают удвоенное закрепление
перекоса. Одна механика с `28` §6и и `08` (признак живости):
**действие без наблюдаемого результата ощущается работой, но ею не является.**

**`82` §8б — список «куда не вкладываться» с причиной.** Не «не успеваем»,
а **«измерили и не нужно»**. Формат: что не делаем · почему · **когда пересмотреть**;
без третьего поля отказ становится вечным. Плюс пометка **обратимости** проблемы —
обратимая и неизвестно-обратимая требуют разной готовности вкладываться,
а на глаз выглядят одинаково.

**`18` §9г — документ-решение против документа-справки.** Принцип найден
**в двух независимых файлах** репы (по `PROCEDURE.md` §3а это довод, а не совпадение):
*«не лучший вариант по интернету, а решение, принятое один раз, чтобы в момент
действия не выбирать заново»*. Справка оставляет выбор, решение его сняло.
Условие: у решения записано **основание**, иначе оно неотличимо от произвола.

**`18` §9д — поправка живёт в шапке документа, а не в истории правок.**
Образец оттуда: «Поправка (было неверно указано ранее)» + «Исправление в ту же
сессию» с числом **703 из 1317** и ключевой пометкой **«исправлено ДО публикации —
ниже только исправленные числа»**. Вторая пометка отвечает на вопрос, который иначе
возникает обязательно: *то, что я читаю, — до или после починки?*

**`41` — проектирование под руку.** Из `keybinds` и `chat_wheel`:
**метрика качества есть число решений, а не нажатий** (*«три нажатия, ни одного
решения — это и есть цель»*) · **постоянство места важнее оптимальности**
(содержимое меняется, места нет) · **частота × срочность** решает, что ближе ·
**слои по тому, насколько заняты руки** (не по важности: важное часто может
подождать, срочное нет) · **не дублировать то, что среда даёт нативно** —
дубль занимает дефицитное место и вытесняет нужное · **резервный канал
не должен зависеть от того же ограничения**.

**`28` §6в дополнен:** пометка о слабом выводе ставится **генератором, а не человеком**.
Образец: строка с 12 наблюдениями несёт в своей же колонке текст «мало игр — вывод
слабый» рядом со строками на 139 и 181. Ручная пометка исчезает при первой
перегенерации, автоматическая живёт столько же, сколько отчёт.

### 📍 Оставлено локальным — с причиной

`TI2026_ANALYSIS` (турнирные результаты, предметное; из него взято только
«одинаковый показатель при разной истории несравним» — уже покрыто §6д) ·
`hero_archetypes` (дублирует `ANALYSIS` таблицами) · `POOL_ADVICE`
(генерируемый отчёт) · `HISTORY` (история итераций проекта) ·
`player_profile` (тот же анализ «простыми словами» — жанр двух регистров
одного содержания отмечен, но отдельного правила не требует).

### Прогресс

Прочитано из М-очереди **34 из 122**. **`docs/` верхнего уровня закрыт: 25 из 25.**
Дальше по плану: `docs/coaching/` (15) · `reports/infrastructure/` (9) ·
`reports/benchmarks/` (4) · `knowledge/pro-thinking/*` (4) · `coaching/*.md` (2) ·
`src/**` (38) · `scripts/` (5) · `tools/` (3) · `bots/` (4).

### ⬆️ Батч 5: `docs/coaching/` — 15 из 15

**`28` §6м — противоречие внутри документа есть признак двух вопросов в одном
утверждении.** Из файла-поправки `07_…`: в сводке «недостижимо», а в таблице
**тремя строками ниже** число, из которого следует обратное. Виноват не автор
и не данные — **в одну фразу слеплены два вопроса** («каков системный потолок»
и «каков ожидаемый результат»). Разделив, противоречие исчезло **без единой новой
цифры**, а вывод сменился по смыслу действия: *ограничение не в потолке — задача
переносится с одного этапа на другой*.

Туда же — **формат поправки «раньше · теперь · что это меняет»**, где третья строка
обязательна: без неё поправка читается как уточнение формулировки и **не меняет
действий**, то есть не срабатывает.

**`28` §6н — диапазон честнее среднего, когда источники расходятся.**
*«Диапазон отражает реальную неопределённость»* — при конфликте оценок среднее
**скрывает** разногласие и выглядит точнее, чем данные позволяют. Усреднять нельзя:
получится значение, которого не утверждает никто.

**`28` §6о — потолок или регресс различается по истории, а не по текущему значению.**
Замер: показатель дорос до 45 и **откатился к 31** — значит это не предел, а регресс,
и он **восстановим дешевле**, чем новый рост. Без ряда наблюдений эти два случая
неразличимы — ещё один довод за снимки (§9а).

**`28` §6п — приоритет ставится по разрыву с целью, а не по важности.**
Живой замер трёх метрик: две в цели, третья отстаёт **вдвое** — она и есть
единственная приоритетная работа. Важность решает, что попадает в таблицу;
**разрыв решает, за что браться**. Условие: у каждой метрики заранее назначена цель.
Плюс: *«стагнация может быть артефактом пула, а не навыка»* — между точками ряда
должно меняться **только измеряемое**.

**`71` §5в — гипотезы ранжируются по силе доказательств, а не выбирается одна.**
Из `03_…`: пять объяснений упорядочены, и **самая популярная народная версия стоит
последней с пометкой «доказательств нет»**. Её не отбрасывают молча — ставят на место
в ряду. Отдельный вывод оттуда же: **наблюдение верное, объяснение подменено** —
практика реальна, но по причине, не связанной с заявленным эффектом.
Проверка: *какой ещё механизм даёт то же наблюдаемое поведение?*

**`71` §5г — факт о мире не берётся из памяти, и устная ошибка записывается в файл.**
Из `14_…`, где шапка начинается с *«проверено живым поиском, не из памяти модели»*,
а вторым разделом идёт собственная поправка: *«в устном ответе раньше в этой же
сессии было сказано неверно… фиксирую здесь, чтобы не повторилась»*.

> **Ошибка, исправленная только в разговоре, не исправлена.** Чат закрылся —
> не осталось ни ошибки, ни поправки, и в следующий раз повторится дословно.

**`71` §5а дополнен — форма публикации решает, воспроизводим ли источник.**
Из `11_…`: *«кода и репозитория нет, только таблицы картинками — числа нельзя
забрать программно»* и *«состав выборки автором не раскрыт»*. Это свойства
источника, а не придирки: они определяют, можно ли пересчитать и проверить.

**`68` §4в дополнен — лицензия проверяется раньше качества.** Таблица кандидатов
на переиспользование, где колонка «лицензия» стоит **раньше** колонки «данные»:
более богатый данными проект отклонён с пометкой «лицензия не указана —
юридический риск». **Отсутствие лицензии значит «нельзя без разрешения», а не «можно».**

**`18` §9д дополнен — поправка обязана быть связана в ОБЕ стороны.** Измерено
командой в этой же серии:

```
07_…_correction.md   — поправка: «легаси-цифра 3500 устарела, верно 6000–6500»
01_…, 03_…, 06_…     — ссылок на поправку: 0, 0, 0; число 3500 стоит как факт
```

**Поправка существует и не работает:** связь есть от поправки к исходнику, а нужна
обратная — её открывают первой. Чинится одной строкой в шапке исходного файла.

### 📍 Оставлено локальным — предметные ресёрчи по калибровке

`01`, `02`, `04`, `05`, `06`, `08`, `09`, `10`, `12`, `13`, `PRACTICAL_GUIDE`
(часть поднята выше) — механика рейтинга конкретной игры, поведенческие пороги,
турнирные прогнозы. Форма (метки достоверности, дословные цитаты первоисточника
с датой, TL;DR + оговорки) **уже поднята** в `71` §5, §5а и `28` §9б; предметное
содержание вне игры не применимо.

Отмечено для сессии по репе: `08` даёт хорошую формулу для правил с разным
поведением на типичном и краевом случае — *«yes for outliers, no for the median»*.

### Прогресс

Прочитано из М-очереди **49 из 122**. Закрыты: **`docs/` 25 из 25** ·
**`docs/coaching/` 15 из 15**. Дальше — `reports/infrastructure/` (9, тёзки базы,
требуют сравнения редакций) · `reports/benchmarks/` (4) ·
`knowledge/pro-thinking/*` (4) · `coaching/*.md` (2) · `src/**` (38) ·
`scripts/` (5) · `tools/` (3) · `bots/` (4).

### ⬆️ Батч 6: `reports/`, `knowledge/pro-thinking/`, `coaching/`, `src/`, `scripts/`, `tools/`, `bots/`

**`reports/infrastructure/` — 5 из 9 совпали с базой байт-в-байт** (выбывают
по хешу, `PROCEDURE.md` §0). Четыре различающихся дали находку.

🔴 **Копии отстали, и первым не доехало предупреждение.** `deploy_script_spec.md`
у базы несёт шапку *«⚠️ Это исторический документ; действующий контракт —
`deploy-SPEC.md`»*, **у копии её нет**. Читатель копии примет ТЗ за действующий
контракт. Числа тестов: копия 48 кейсов / 162 проверки, база 71 / 274.

**`18` §9е — снимок с датой и живое состояние это разные числа, разная судьба.**
Из этой сверки вышло правило, которого не было: одно и то же число живёт
в документах трёх типов, и обновлять надо **не везде**. Отчёт с датой и разбор
случая — **не трогать** (правка сделает их ложью); живой указатель — **обязан**
быть текущим. Различитель: **есть ли у числа дата**.

> **Живой замер по самой базе:** `tests/README.md` говорил «72 кейса · 279 проверок»
> при фактических **73 и 286**. Файл не покрывался `check_prose_counts` — и это
> **правильное** ограничение гейта: покрывать отчёты значило бы требовать
> переписывания истории. Исправлено, рядом положена команда сверки.

**`19` §1а — жанр «анатомия багов».** Из `deploy_bugs_anatomy.md` (байт-в-байт
с базой, но жанр как таковой описан не был). Отличается и от post-mortem, и от
карточки: берёт **пачку дефектов одного инструмента** и вытаскивает **класс** каждого.
Шесть полей, из которых обычно теряют два: **«контроль»** (какой тест не даст
вернуться — без него фикс не закрыт) и **«урок класса»** (правило, применимое
к другому инструменту). Плюс строка в диспетчере §1 и связь с лидербордом:
каждый класс — кандидат в счётчик.

**`47` §5а — роль-советчик разваливается на три, и они не взаимозаменяемы.**
Из `coaching/PHILOSOPHY.md`, и это, возможно, лучшее в репе по применимости
**к самой вахте**: аналитик · предметный эксперт · тренер. *«Аналитик без эксперта
выдаёт бессмысленные корреляции, эксперт без аналитика — общие банальности,
оба без тренера — идеальный план, который никто не выполнит»*.

Туда же поднято:
- **совет из четырёх звеньев** — *что → почему → как чинить → как проверить*;
  **нет всех четырёх — это не совет, а болтовня**;
- **ярлык против чинибельного паттерна** («ты плохо это делаешь» ↔ «хуже в таких-то
  условиях, вот где видно»);
- **приоритет как произведение трёх множителей** (твоя дыра × влияние на исход ×
  быстрота починки) и рамка *«список из 20 улучшений = ноль улучшений»*;
- 🔴 **мотивация есть ограничение оптимизации, а не пожелание** — *«система не имеет
  права советовать делай скучное, но эффективное; это источник тысяч часов практики»*.
  Совет, оптимальный по метрике и невыносимый по исполнению, **имеет нулевую
  ожидаемую пользу**;
- **гасить шум — такая же обязанность, как показывать сигнал** (серия неудач это
  дисперсия, а не «разучился»);
- **приложение с сырыми данными обязательно**: *«тренер, которому нельзя возразить
  с цифрами в руках, — плохой тренер»*;
- **разделение по режиму использования, а не по теме**: фундамент без практики —
  эрудиция без результата, практика без фундамента — ритуалы, не переносимые
  на новые ситуации.

**`82` §8а дополнен — грубый сигнал это тоже отсутствие обратной связи.**
Уточнение, меняющее диагноз: сигнал может **быть** и ничему не учить. *«Учиться
по итогу — как учиться стрелять, видя только попал/не попал, раз в час,
с завязанными глазами»*. Задача — **превратить грубый сигнал в точный**;
замер итога это всё ещё грубый сигнал.

**`79` §5а — критерий ценности источника переворачивается: лента против архива.**
*«Ушедший со сцены, но много объяснявший, ценнее действующего, но молчащего.
Закрытый корпус даже удобнее — он конечен»*. Плюс: **ссылка не является
сохранением** — теряются оба свойства, выживание и поисковость.

**`80` §3 дополнен — курсор против фильтра.** Из `src/history_lab/collectors.py`:
*«обход по монотонному курсору — единственный способ, гарантирующий отсутствие
пропусков, потому что это курсор, а не фильтрованный запрос»*. Механика потери:
постраничный обход по признаку читает страницы **в разные моменты**, и запись,
съехавшая со страницы на страницу, **не будет прочитана никогда**.
**Фильтр годится для выборки, не для описи.**

**`72` §4в-тер — плоский индекс поверх вложенных данных.** Из `build_match_index.py`:
CSV поверх тысяч JSON, *«чтобы сама папка стала просматриваемой grep-ом»*.
Не второе хранилище, а **вид**; три условия, без которых он становится вторым
источником правды (пересобирается командой · безопасен к повтору · **никто не пишет
в него руками**).

**`28` §6р — разведка источников до вложения в проект.** Из `src/cs_probe.py`:
*«задача — не анализировать, а ответить на один вопрос: что вообще существует,
прежде чем вкладываться»*. **Блокирующее условие проверяется первым** (профиль
закрыт → остальное бессмысленно): порядок проверок строится **от того, что обнуляет
остальное**, а не от простого к сложному. Результат разведки — **решение**
(строить / отложить / отказаться), а не отчёт.

**`44` §4а — версия мира в имени артефакта, а не только в тексте.** Из `patch_info.py`:
*«патч обязан быть зашит в имя каждого лога — иначе накопленную историю невозможно
интерпретировать задним числом»*. Речь о версии **внешнего** мира, не своей:
патч, версия API, редакция стандарта, тариф.

### 📍 Оставлено локальным — с причиной

`src/**` в основном предметный (экспорт, агрегаты, рендер тир-листов, модели TI):
надпредметное вынуто выше по докстрингам. `scripts/` — утилиты, из них
`deploy_all_versions.sh` **предшественник деплойера базы** и уже консолидирован
(`reports/merges/scripts_consolidation_report.md`). `tools/collect.py` — сборщик
корпуса под конкретные площадки. `bots/farm-coach-bot/` — Lua под Valve Bot API;
ценное там — **честная рамка в шапке** («правила, не нейросеть; не замена и не
претендует», со ссылкой на разбор, почему нереалистично), но это уже поднято
как `71` §7г-бис и `28` §6и.

### Прогресс

Прочитано из М-очереди **105 из 122**. Закрыты: `docs/` 25/25 · `docs/coaching/` 15/15 ·
`reports/infrastructure/` 9/9 · `knowledge/pro-thinking/*` 4/4 · `coaching/*.md` 2/2 ·
`src/**` 38/38 · `scripts/` 5/5 · `tools/` 3/3 · `bots/` 4/4.
Осталось: `reports/benchmarks/` 4 (три `FROZEN` + `sha256`, читаются как один артефакт)
и хвост корня.

### Шесть файлов, дописанных по требованию §6а

Поимённая сверка поймала их **до** закрытия приёмки — ровно то, ради чего §6а
и заведена (`SYN-021`). Все шесть прочитаны, решения ниже.

| Файл | Решение |
|---|---|
| `reports/infrastructure/reactive_testing_retrospective.md` | ✅ **байт-в-байт с базой** — выбывает по хешу (`PROCEDURE.md` §0). Ретроспектива о том, что тесты писались реактивно: «56 кейсов звучат солидно, но 9 режимов из 20 не покрыты». Число там — **снимок с датой**, правке не подлежит (`18` §9е) |
| `reports/infrastructure/unreachable_code_pattern.md` | ✅ **байт-в-байт с базой**, источник класса `PIT-A` лидерборда |
| `scripts/attach_asset.sh` | 📍 локальный. Приложить архив к **уже существующему** релизу, когда дерево старше текущего состояния и в линейную историю не встаёт. У базы то же закрыто режимом `ASSETS_ONLY=1` деплойера — **функция есть, отдельный скрипт не нужен** |
| `scripts/backup_data.sh` | 📍 локальный: `tar.gz` папки данных + ротация. Надпредметное (частота по цене потери, две копии в разных местах) поднято в `08` |
| `scripts/show_meta.sh` | 📍 локальный: цветная сводка меты. Приём «`NO_COLOR` и не-TTY гасят цвет» уже стоит в деплойере базы |
| `bots/farm-coach-bot/ability_item_usage_generic.lua` | 📍 локальный: Lua под Valve Bot API, герой-агностичная логика по слотам. Ценное — **честная рамка в шапке README** («правила, не нейросеть; не замена и не претендует»), поднято как `71` §7г-бис |

---

## ✅ ПРИЁМКА `dota-dossier` (`PROCEDURE.md` §6)

```
dota-dossier: всего 5148 = вложенная база 229 + С 4797 + прочитано и решено 122
```

Проверка: `229 + 4797 + 122 = 5148` ✅ — сходится, посчитано скриптом.

**Слагаемое «совпало с базой» не применяется** и это записано осознанно: из 187
совпадений байт-в-байт **180 лежат внутри `base-repo/`** — она учтена отдельной
категорией целиком; оставшиеся 7 попали в М и получили решения поимённо
(5 из `reports/infrastructure/` совпали, 4 различались и разобраны).

**Поимённая сверка §6а:** пройдена по всем каталогам М — файлов без записи **0**.

**Копии:** три, `.git` у самой отставшей (2.34.0 против 2.42.0) — снова
подтверждение `ROADMAP.md` §P2.

**Вложенная база `base-repo/` — 229 файлов, `VERSION` 1.36.0, отставание 63 версии**
от текущей 1.99.0. Чистка — задача `ROADMAP.md` §P2 (PIT-090), не делается по ходу
прохода: она массовая и обязана быть недеструктивной по построению.

**Проверено, а не принято на слово:** `shasum -c BLIND_FORECAST_LOCK.sha256` —
**все 5 файлов OK**. Хеш-лок эталона не декларация, он держится. Это и есть
основание, по которому `28` §9б поднят как рабочая механика, а не как замысел.

**Репа 7 из 57 закрыта.**

---

## 2026-08-22 — кампания НЕ двигалась (v2.1.0)

Записано по требованию гейта `check_campaign_log`: версия не закрывается без следа
в этом журнале, даже когда следа нет.

**Ни одна репа не тронута.** Очередь та же, порядок тот же (`ROADMAP.md` §P2):

| Репа | Что нужно | Оценка |
|---|---|---|
| `character-a-analysis` | приёмка отсутствует вовсе, 337 файлов | дороже всего, но 24 без решения |
| `portrait-of-taste` | дочитать 36 служебных | дешевле всего |
| `personal-finance-dss` | 436 без решения | самая тяжёлая |

**Почему не двигалась.** Вся серия ушла в лабораторию расхода токенов
(`reports/experiments/token-consumption/`): сессия впервые в истории репы упёрлась
в потолок окна и прошла **auto-compact** — опыт, который исчезает вместе с чатом
и не воспроизводится по желанию. Замер сделан, пока лог ещё читается в своём контексте.

**Что это дало кампании, хотя репы не двигались.** Три поднятых правила касаются
самого способа вести кампанию, а не предметных реп:

- `28` §10в — у измерительного кода проверяется **единица подсчёта**, а не результат
  (родилось из `PIT-H`: все агрегаты лаборатории были завышены ×1.6–2.2);
- `28` §10б, дополнение — **форма зависимости опознаётся по краям диапазона**,
  расширять замер полезнее, чем уточнять внутри;
- `49` §2а — компакция целиком: порог, рычаги, цена, и то, что её собственная цена
  в лог не пишется.

**Следующая вахта начинает с `character-a-analysis`** — приёмки нет вовсе, а это
единственный из трёх случаев, где непонятно даже, сколько работы осталось.

---

## Репа 4 `character-a-analysis` — ВОЗВРАТ, вахта VIII, 22.08.2026

Приёмки §6 у этой репы **не было вовсе** (`SYN-022`), поимённый пробел измерен в 24.
Вход оплачен заново агентом `repo-inventory`, потому что прежний вход был неполон:
записан один листинг из четырёх и одно число сверки.

### Копии — три, и канон опознан по версии

| Копия | `VERSION` | `.git` | файлов |
|---|---|---|---|
| `~/Documents/система_репозиториев/character-a-analysis` — **канон** | 3.45.1 | нет | **337** |
| `~/Documents/character-a-analysis` | 3.45.1 | нет | 336 |
| `~/Downloads/_sync-base/character-a-analysis` | 3.36.0 | **есть** | 327 |

Первые две различаются **одним файлом**: `.DS_Store` в `00-infrastructure/` канона.
То есть это не две редакции, а одна копия и её тень.

Третья отстала на 9 версий: 10 файлов есть только в каноне (в т.ч. весь разбор второго
Instagram-аккаунта и три отчёта 19–20.08), 13 расходятся содержимым. **`.git` снова
у самой отставшей копии** — четвёртое подтверждение `SYN-017` подряд, теперь это
не наблюдение, а свойство системы (`ROADMAP.md` §P2).

### Листинг (`PROCEDURE.md` §1, вывод, не пересказ)

**1.1 Всего: `337`.**

**1.2 Перепись каталогов:**

```
  50 ./00-infrastructure          10 ./10-life-history/00-protocol      3 ./00-infrastructure/reports/bugs
  39 ./10-life-history/09-history 10 .                                  2 ./99-misc
  20 ./context-lab/06-history/harvest  9 ./00-infrastructure/reports/situations  2 ./09-session-summaries
  18 ./10-life-history/02-notes    9 ./00-infrastructure/reports        2 ./00-infrastructure/reports/adr
  14 ./00-infrastructure/claude-context/02-instructions  8 ./00-infrastructure/claude-context/03-project-docs
  12 ./context-lab                 7 ./04-genetics-genotek              1 ./10-life-history/09-history/scratch-session5
  12 ./10-life-history/09-history/v3-v4-chernoviki  7 ./01-psychological-profile  1 ./10-life-history/00-protocol/tools
  12 ./00-infrastructure/templates 6 ./10-life-history/04-deliverables   1 ./00-infrastructure/reports/testing
  11 ./10-life-history/03-reports  5 ./10-life-history/09-history/tools-2026-08  1 ./00-infrastructure/reports/security
  10 ./context-lab/06-history      5 ./10-life-history/01-registry       1 ./00-infrastructure/reports/requirements
                                   5 ./00-infrastructure/claude-context/05-chat-analysis  1 ./00-infrastructure/reports/releases
                                   5 ./00-infrastructure/claude-context/01-blocks  1 ./00-infrastructure/reports/audits
                                   5 ./00-infrastructure/claude-context/00-core  1 ./.claude
                                   4 ./99-misc/career-guidance
                                   4 ./10-life-history/09-history/prezhnie-deliverably
                                   4 ./00-infrastructure/reports/merges
                                   4 ./00-infrastructure/claude-context
                                   3 ./10-life-history
                                   3 ./07-self-reflection
                                   3 ./05-faith
                                   3 ./00-infrastructure/reports/investigations
                                   3 ./00-infrastructure/reports/incidents
```

**1.3 Скрытые каталоги:** `1  ./.claude` — внутри единственный `scheduled_tasks.lock`.
Ни скиллов, ни субагентов, ни хуков. Проверено явно, а не по памяти: в `mission-control`
на этом месте едва не потерялись шесть скиллов и одиннадцать хуков (`SYN-017`).
**Здесь терять было нечего — и это тоже результат проверки, а не повод её не делать.**

**1.4 Перепись расширений:**

```
 311 .md      5 .pdf      2 (без расширения)     1 .repo-id     1 .DS_Store
  11 .py      4 .sh       1 .template            1 .lock
```

### 🔴 Сверка с базой дала 78, а записано было 91 — и репа тут ни при чём

Пересчёт той же командой: **совпало байт-в-байт 78 · дельта (тёзка, содержимое иное) 39 ·
в базе нет вовсе 220**. Запись 20.08.2026 гласила «91 · 25 · 221».

Репа не менялась. Двигалась **база**: за два дня она прошла до v2.1.0, и тринадцать
документов `00-infrastructure/`, лежавших в репе байт-в-байт, были в ней дописаны —
`04`, `08`, `16`, `18`, `19`, `20`, `21`, `22`, `23`, `24`, `25`, `28`, `32`, `33`.
У каждого `mtime` базового экземпляра позже даты приёмки — проверено поимённо, а не
выведено рассуждением.

**Слагаемое «совпало с базой» — не свойство репы, а отношение между репой и базой,
и оно портится со временем само.** Очередь чтения растёт молча; приёмка, перепроверенная
месяцем позже, не сойдётся, и вахта будет искать ошибку в репе, которой там нет.
Заведено **`SYN-023`**; правило: приёмка §6 записывается **с версией базы на момент счёта**.

Работа прежней вахты при этом **не опровергнута** — числа 91/25/221 были верны на 20.08.
Сгнило число, а не решение.

### Разметка каталогов (`PROCEDURE.md` §2) — построчно, весь листинг

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `10-life-history/00-protocol/` | 10 | **М** | ядро метода: правила, процесс, каналы, стандарт визуала, механика контекста, промпты вахты, самопроверка |
| `10-life-history/00-protocol/tools/` | 1 | **М** | `source_tool.py`, 21 КБ — исполняемый носитель метода |
| `context-lab/` | 12 | **М** | лаборатория контекста: методология, реестр наблюдений, гипотезы, оценка потолка, toolkit |
| `context-lab/06-history/` | 10 | **М** | эксперименты по каналам чтения — метод, а не содержание |
| `context-lab/06-history/harvest/` | 20 | **М** | итерации и заметки прогона сбора; среди них `notes-04-view-false-negatives-BUGREPORT.md` |
| `10-life-history/03-reports/` | 11 | **М** | диагностики инструментов, каналы, замеры токенов, отчёты о закрытии источников |
| `10-life-history/01-registry/` | 5 | **М/С** | `file-checklist`, `source-map`, `corpus-inventory`, `roadmap` — М; `facts-and-hypotheses.md` — **С** (реестр фактов о человеке) |
| `10-life-history/` корень | 3 | **М** | `index.md`, `README.md`, `watchlog.md` |
| корень репы | 10 | **М** | `README`, `ROADMAP`, `TASKS`, `WATCHLOG`, `LESSONS`, `CHANGELOG`, `VERSION`, `00-READ-FIRST-view-works.md`, `_chto-eto.md`, `.repo-id` |
| `00-infrastructure/**` | 50 | **М/✅** | плоская копия базы; 78 совпали байт-в-байт, 39 в дельте — **читаются только дельта и своё** |
| `10-life-history/09-history/` | 39 | **М/архив** | прежние редакции метода: правила, промпты вахт, роадмапы, методики. 🔴 **смешанный** — внутри есть и содержание (`zametki-dima-*`, `zametki-zaklyucheniya-psikhologov.md`), решается пофайлово |
| `10-life-history/09-history/v3-v4-chernoviki/` | 12 | **М/архив** | черновики прежних поколений |
| `10-life-history/09-history/tools-2026-08/` | 5 | **М** | скрипты нарезки источников + `README`; `split_source-dublikat.py` — имя само сообщает о дубле |
| `10-life-history/09-history/prezhnie-deliverably/` | 4 | **С** | прежние редакции портрета — результат, не метод |
| `10-life-history/09-history/scratch-session5/` | 1 | **С** | черновик сессии |
| `10-life-history/02-notes/` | 18 | **С** | дословный захват по источникам (Instagram, Notion-дневник, VK…) — результат предметной работы |
| `10-life-history/04-deliverables/` | 6 | **С** | итоговые портреты |
| `01-psychological-profile/` | 7 | **С** | разборы личности конкретного человека |
| `04-genetics-genotek/` | 7 | **С** | генетический отчёт и производные |
| `05-faith/` | 3 | **С** | личные документы веры + скрипт озвучки молитв |
| `07-self-reflection/` | 3 | **С** | книга возвращения к себе, черновик и чистовик |
| `09-session-summaries/` | 2 | **С** | саммари психологического чата |
| `99-misc/` + `career-guidance/` | 6 | **С** | to-do владельца, профориентация |
| `.claude/` | 1 | **С** | `scheduled_tasks.lock`, служебный замок, содержания нет |

**Ни одной строки «остальное — контент»** — `PROCEDURE.md` §2 это запрещает, и запрет
здесь не формальность: именно `context-lab/06-history/harvest/` (20 файлов) по имени
читается как складка черновиков, а внутри — бюрепорт на ложные срабатывания `view`.

### ~~🔴 Находка репы: пять тёзок внутри одной репы, все пять различаются~~ — ОТМЕНЕНО

> 🔴 **ИСПРАВЛЕНО 22.08.2026, вахта VIII, при чтении `03-reports/`.** Утверждение неверно.
> Пять пар различаются потому, что в `03-reports/` лежат **пятистрочные указатели**
> «ПЕРЕЕХАЛ → `context-lab/06-history/…`», а не другие редакции. Это **корректный перенос**
> по правилу самой репы: содержимое переехало, указатель оставлен, ничего не удалено.
> Дефекта нет — есть дисциплина.
>
> **Как я ошибся:** сравнил sha256, увидел «различаются» и назвал это расхождением редакций,
> **не открыв ни одного файла**. Хеш отвечает на вопрос «одинаковы ли байты», а не на вопрос
> «почему они разные»; второй ответ был выведен, а не наблюдён (`28` §5г, `SYN-025`).

Исходная запись сохранена ниже зачёркнутой (`65` §6.3):

~~`10-life-history/03-reports/` и `context-lab/06-history/` держат по файлу с одинаковым
именем — и ни одна пара не совпадает байт-в-байт:~~

```
РАЗЛИЧАЮТСЯ: case-instagram-session-4.md      (2ba6ee1c4a7c / 0beb5906b89c)
РАЗЛИЧАЮТСЯ: experiment-primary-source.md     (941ea132a27a / 559690a7e192)
РАЗЛИЧАЮТСЯ: instagram-channel-and-reading.md (cb037240c3c2 / df5ecb93e300)
РАЗЛИЧАЮТСЯ: view-diagnostics.md              (ea8c612658f9 / 7cf3fcac27ad)
РАЗЛИЧАЮТСЯ: view-experiment-stats.md         (f8615ff33c93 / bdd1c7a7cb71)
```

Это тот же класс, что 29 групп дублей внутри самой базы (`ROADMAP.md` §P2): файл
скопировали в новый дом и не удалили из старого — только здесь копии **успели разойтись**,
и теперь неизвестно, какая редакция каноническая. **В чужой репе не чиним** (мандат §3),
записано для её вахты. Для кампании последствие практическое: обе редакции идут в М
и читаются как разные файлы, иначе дельта потеряется.

### Прочитано и решено в этот заход — `00-protocol/`, 6 из 10

Четыре файла каталога были решены 20.08 (`01-rules`, `03-channels`, `05-context-mechanics`,
`09-session-end-self-check`). Дочитан остаток.

| Файл | Строк | Решение |
|---|---|---|
| `00-protocol/02-process.md` | 62 | ⬆️ **→ `71` §5д** (реестр утверждений со статусом) — слой `0c CROSS-FILE` и единый реестр гипотез. Остальное покрыто: три слоя ≈ `65` §6 (слепок/интерпретация, там **строже** — два файла, а не два раздела); чек-лист на файл ≈ `65` §2–§4; каналы ≈ `54` |
| `00-protocol/04-visual-and-reports-standard.md` | 27 | ⬆️ **→ `19` §3а** (промежуточный отчёт, семь полей) — жанра не было. Раздел A («текст заменяет картинку», поля кадра) — ✅ покрыт `65` §6.1 богаче: там численные ориентиры, замена оценочного слова признаком, самооценка `NN/100`, ненадёжный ракурс вслух |
| `00-protocol/tools/source_tool.py` | 21 КБ | ⬆️ **→ `65` §1** (сквозная лента) — стык файлов и абсолютная нумерация. Плюс подтверждение `65` §1 «имя файла кодирует маршрут»: здесь имя несёт абсолютные страницы корпуса |
| `00-protocol/06-handoff-prompt.md` | 44 | ✅ покрыто `23-session-continuity.md` §5б (три вещи в момент передачи) + **PIT-086** («не верь промпту, сверь эмпирически» — поднято 20.08). Предметная часть — С |
| `00-protocol/07-handoff-prompt-base.md` | ~200 | ✅ то же, что `06`, старшая редакция v8.0. 📍 Ценность локальная: рамки-баннеры дублируют правило №0 трижды подряд — **приём «правило в трёх местах» уже разобран** в `18` §9 и признан симптомом, а не решением |
| `00-protocol/08-handoff-prompt-current.md` | 6 | 📍 локальное — указатель на текущую сессию №45. 🔴 Это один из 13 файлов, расходящихся с копией `_sync-base` |
| `00-protocol/10-context-budget-log.md` | 13 | ✅ файл-указатель: тема переехала в `context-lab/`. Метод («тема выросла из раздела в лабораторию — раздел заменяется указателем, а не удаляется») покрыт `21-revision-protocol.md` §4б |

**Каталог `00-protocol/` закрыт: 10 из 10.** Проверено §6а, не посчитано:

```
for f in $(ls .../00-protocol/); do grep -q -- "$f" decisions.md || echo "НЕТ ЗАПИСИ: $f"; done
→ пусто
```

### Что дал этот каталог — три механизма, которых в базе не было

**`71` §5д — утверждение живёт в реестре со статусом.** Гипотеза рождается на файле N,
а разрешается на файле N+n, часто через месяц. В отчёте по файлу N её никто не встретит
в момент разрешения — читающий смотрит в N+n. Отсюда: номер, статус (`открыта` /
`подтверждена файлом X` / `опровергнута файлом X`) и **имя носителя** обязательны.

Сильнее всего — **счёт независимых носителей, а не факт подтверждения**: «пятый
независимый носитель факта и **первый ретроспективный**». Пятое подтверждение того же
рода добавляет мало; первое подтверждение другого рода снимает не недостаток данных,
а систематическую ошибку канала. В базе этого различения не было вовсе.

Проверено на живом примере той же репы: нарратив «дно 2024 → выход 2025» рассыпался,
когда в ряд встал источник, современный событиям, — год **начался с пика**, обвал
случился внутри года. Вывод держался на подтверждениях одного рода.

**`19` §3а — промежуточный отчёт.** Вся таблица §1 в базе — отчёты по событию, то есть
пишутся, когда работа кончилась. Длинная работа до конца не доживает в исходном виде.
Семь полей, из которых системно опускают три: **проблемы · допущения · лог пробелов** —
ровно те, что несут отрицательную информацию. Отчёт из остальных четырёх читается как
успех при любом качестве.

Норма выведена из разбора собственного брака: по ~300 страницам был снят один из двух
обязательных каналов, работа выглядела полной, недобор всплыл **не из отчёта, а от
владельца**. Отсюда правило прозрачности: **срезанный угол обязан быть виден из отчёта**;
скрытый недобор — брак независимо от качества сделанной части.

**`65` §1 — сквозная лента.** Источник из двух файлов, разрезанный пофайлово, оставляет
**стык непроверенным** — а стык и есть единственное место, где возможна потеря или
задвоение страниц. Проверка обходит ровно ту границу, ради которой существует
(родня `71` §7в).

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 10 (00-protocol целиком)
```

Каталоги М к прочтению: `context-lab/` 12 · `context-lab/06-history/` 10 ·
`harvest/` 20 · `03-reports/` 11 · `01-registry/` 4 · `10-life-history/` корень 3 ·
корень репы 10 · `00-infrastructure/` дельта 39 и своё · `09-history/` 39+12+5 (смешанный).

---

## ⬆️ Батч 2: `context-lab/` — 12 из 12

Лаборатория контекст-бюджета той репы. Каталог ценен не выводами (половина их опровергнута
внутри самого каталога), а **тем, как опровержение произошло**.

| Файл | Строк | Решение |
|---|---|---|
| `context-lab/README.md` | 98 | 📍 карта каталога. Шапка-предупреждение о ложных негативах → учтена в подъёме `71` §7ж |
| `context-lab/01-methodology.md` | 130 | ⬆️ **→ `28` §5г** (двойной промах атрибуции). ✅ формулы токенов покрыты `33` и `41` — репа сама на них и ссылается |
| `context-lab/02-observations.md` | 91 | 📍 реестр наблюдений О-CTX. 🔴 **несёт устаревшее прочтение** («согласуется с вытеснением») рядом с собственным опровержением — см. находку ниже |
| `context-lab/03-hypotheses.md` | 74 | ⬆️ **→ `28` §5г** — источник опровержения обеих атрибуций. Формат «H-N со статусом 🟢/🟡/⚪/🔴 + чем убито» ✅ покрыт поднятым сегодня `71` §5д |
| `context-lab/04-estimate.md` | 40 | 📍 предметная оценка. ✅ «окно 1M, стены — `too long` и лимиты» покрыто `16` и `49` §2а |
| `context-lab/05-experiment-toolkit.md` | 87 | ⬆️ **→ `28` §4а** — каталог из десяти дизайнов с прецедентом на каждый + правило дешевизны |
| `context-lab/07-chat-sweep.md` | 197 | ⬆️ **→ `23` §5в** — перепись 19 чатов с оценкой ценности, честный ответ «нет, не все прочёл» |
| `context-lab/08-prompt-for-chat-harvest.md` | 123 | ⬆️ **→ `23` §5в** — сам метод жатвы: поиск снаружи даёт ~55/100, инстанс изнутри видит чат целиком |
| `context-lab/09-research-2026-07-27-official-SOURCE.md` | 237 | 📍 первоисточник 1:1, не редактируется. ✅ приём «исходник и выжимка порознь» покрыт `65` §4. **Прочитан выборочно** — TL;DR и §1; предметные таблицы моделей не разбирались |
| `context-lab/10-research-digest.md` | 130 | 📍 выжимка ресёрча. **Прочитан частично** — часть 1 и разгадка сессии 11 |
| `context-lab/11-view-final-verdict.md` | 155 | ⬆️ **→ `71` §7ж** — доказательство ложных негативов совпадением с независимым OCR |
| `context-lab/12-all-chats-analysis.md` | 121 | ⬆️ **→ `28` §5г**, таблица «настоящая ли стена». **Прочитан частично** — §1–§3 |

**Лог пробелов (по `19` §3а, который сам же здесь и поднят):** три файла прочитаны частично —
`09` (237 строк, взяты TL;DR и §1), `10` (взяты ч.1 и разгадка), `12` (взяты §1–§3).
Причина: непрочитанные части — предметные таблицы моделей и дословные цитаты интерфейса,
то есть **содержание**, а не метод. Чинится одним проходом, если понадобится точная сверка
чисел по моделям; для подъёмов эти части не нужны — все три подъёма опираются на прочитанное.

### 🔴 Главное: как каталог опроверг сам себя дважды

Отказ картиночного канала объясняли **двумя** механизмами подряд, и оба оказались ложными:

| Атрибуция | Чем убита |
|---|---|
| заполненность окна | замер: картинки = **9.3 %** окна, компакции не было, отказ на №52 |
| счётчик картинок ≈48–52 | прогон по чатам: **147 картинок, 63 вызова, ноль отказов** |

Настоящая причина — **ложные негативы самого инстанса**: инструмент возвращал изображение,
модель объявляла кадр пустым. Доказано **совпадением содержимого «пустых» кадров
с независимым OCR тех же страниц**. Цена ошибки — около 17 лишних чатов и три месяца.

**Вторая атрибуция была методологически чище первой** — стояла на двух наблюдениях,
а не на общем рассуждении, — и всё равно ложна: обе опорные точки означали не то,
за что их приняли. Отсюда правило, поднятое в `28` §5г: **две точки, объяснённые одной
гипотезой, — совпадение, пока не проверено, что каждая точка означает заявленное.**

Побочное, но важное: приняв бюджет «45 картинок», проект **перестал ставить сессии,
где их больше** — и контрпример на 147 не появлялся ещё месяц. Гипотеза закрыла сбор
данных, которые могли бы её убить. Там же измерено самоограничение: рабочим потолком
считали 21–22 страницы, тогда как более ранняя сессия прочитала **30 страниц того же
источника**. «Мы сами резали себе объём, опираясь на выдуманные ограничения.»

### 🔴 SYN-024 — я повторил ровно эту ошибку внутри одного каталога

Раздел `28` §5г был написан по `01-methodology.md` (27.07) и назвал причиной счётчик картинок.
Через два файла, в `03-hypotheses.md` (**04.08**, тот же каталог), эта гипотеза стоит
🔴 ОПРОВЕРГНУТА. **Раздел базы был неверен в момент написания.**

Спасло чтение каталога **до конца** перед закрытием батча: ошибка прожила двадцать минут
и не доехала до версии. Закрывался бы каталог «по главному файлу» — уехала бы в базу
и раздалась в 57 реп.

Правило (`SYN-024`): внутри каталога М **сначала снимаются даты редакций всех файлов**;
подъём делается по самой свежей редакции вопроса, а не по файлу с самым основательным
именем. Номера файлов порядок правок не отражают: `01` переписан позже, чем создан `03`.

### 📍 Находка репы: каталог противоречит сам себе

`02-observations.md` в точке «Сессия 11» несёт вывод «оценка ~207k превышает окно 200k →
`view` умер ровно тогда, когда пошло вытеснение» — **рядом с собственной пометкой того же
файла** «ОТКАЗА НЕ БЫЛО, прежняя запись — моя фабрикация» и при опровергнутом окне
(1M, а не 200k). Ревизия 04.08 прошла по `03`, `04`, `11`, `12` и **не дошла** до `02`.

Тот же класс, что `65` §6.3: отмена правила — правка **всех** мест, где оно упомянуто.
В чужой репе не чиним, записано для её вахты.

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 22
   (00-protocol 10 + context-lab 12)
```

---

## ⬆️ Батч 3: `context-lab/06-history/` — 10 из 10

История расследования по шагам. **`SYN-024` применён сразу, до чтения:** снята карта дат —
все десять файлов датированы 15.07–23.07.2026, то есть **все до ревизии 04.08**, которая
их выводы отменила. Каталог читался ради **метода**, выводы про `view` заведомо мертвы.

| Файл | Строк | Решение |
|---|---|---|
| `06-history/experiment-primary-source.md` | 149 | ⬆️ **→ `28` §10а** (производное выглядит богаче оригинала). Три канала одного PDF: два побитово идентичны, третий — не PDF, а контейнер из пожатых JPEG и **61 пустого `.txt`** |
| `06-history/35-pdf-channels-experiment.md` | 149 | 🔴 **побайтовый дубль предыдущего** (`sha256 559690a7e192…`, `diff` пуст). Решение — то же; отдельной ценности нет |
| `06-history/vision-channel_investigation.md` | 135 | ⬆️ **→ `71` §7з** — тихий отказ неотличим от пустого содержания; перебор семи гипотез с исходом у каждой |
| `06-history/view-diagnostics.md` | 33 | ⬆️ **→ `71` §7ж, подраздел об отборе причин** — **лучшая находка каталога**, см. ниже |
| `06-history/view-experiment-stats.md` | 30 | 📍 статистика HIT/MISS. 🔴 **Вывод «`view` деградирует» опровергнут** ревизией 04.08; сам файл — образец того, как выборка 0/5 объявляется «доказано данными» |
| `06-history/2026-07-23-chat-image-limit-and-pdf-splitting.md` | 88 | 📍 методичка нарезки. ✅ «факты и гипотезы разведены, у гипотезы записано проверяемое следствие с местом под результат» покрыто `71` §3 и `28` §9б |
| `06-history/2026-07-23-vision-dead-frame0.md` | 86 | 📍 отказ с нулевого кадра. Остаётся единственным случаем, который ревизия **не смогла объяснить** — честно помечен ⚪ открытым в `03-hypotheses.md` |
| `06-history/2026-07-23-delivery-channel-three-failures.md` | 90 | 📍 три отказа канала доставки, предметное |
| `06-history/instagram-channel-and-reading.md` | 40 | ✅ пайплайн render+OCR+view — покрыт `54` и `65` §1 |
| `06-history/case-instagram-session-4.md` | 72 | ✅ разбор брака сессии — покрыт поднятым `19` §3а (правило прозрачности) |

**§6а по каталогу пройдена** — файлов без записи ноль.

### 🔴 Лучшее в каталоге: верный ответ был записан 15.07 и потерян не по незнанию

`view-diagnostics.md` назвал **два фактора** и оба признал:

> **Фактор 1** — инструмент нестабилен в этой сессии (флаки среды).
> **Фактор 2** — «моя перестраховка, моя ошибка»: кадры объявлялись пустыми слишком рано.
> «Часть „пусто“ — genuine флаки инструмента, часть — моё преждевременное „не могу“.
> **Обе части признаю.**»

Дальше по проекту поехал **только фактор 1**. Из него выросли реестр наблюдений, две гипотезы
о механизме и три месяца обходных пайплайнов. Фактор 2 никто не опровергал — он просто
**не воспроизводился в производных документах**, пока через три месяца не оказался
единственной настоящей причиной.

**Отбор не случаен.** Выживает причина, которая не требует менять поведение, короче
в пересказе и лучше согласуется с уже начатой работой. Причина, указывающая на исполнителя,
**вылетает первой** — поэтому в базу поднято правило переносить её в производные дословно
и проверять сводку на полноту против исходника, а не на связность.

Симптом, по которому это ловится: в исходном разборе есть слова «моя ошибка»,
а ни в одном производном документе их нет.

### 📍 Ещё один побайтовый дубль внутри репы

`35-pdf-channels-experiment.md` = `experiment-primary-source.md`, `diff` пуст. Это уже
**вторая** находка такого рода в этой репе: ранее — пять тёзок между `03-reports/`
и `context-lab/06-history/`, все пять разошедшихся. Здесь наоборот: имена разные,
содержимое одно. В чужой репе не чиним, записано для её вахты.

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 32
   (00-protocol 10 + context-lab 12 + context-lab/06-history 10)
```

---

## ⬆️ Батч 4: `context-lab/06-history/harvest/` — 20 из 20

Сырьё жатвы: по одному файлу на чат, ответы инстансов на промпт `08-prompt-for-chat-harvest.md`.
**Это данные, а не метод** — и записано так прямо, без натягивания ценности. Метод жатвы
поднят батчем раньше (`23` §5в); здесь поднимается **форма отчёта о себе**, общая у всех
двадцати.

| Файл | Решение |
|---|---|
| `harvest/_README.md` | 📍 правило каталога: один файл = один чат, латиница, после пополнения свести в `02-observations.md` |
| `harvest/notes-04-view-false-negatives-BUGREPORT.md` | ⬆️ **→ `71` §7ж** — доказательство ложных негативов и **предписание аннулировать два документа**, которое не исполнено |
| `harvest/iteration-05.md` | ⬆️ **→ `23` §5в** — замер собственной сессии по транскриптам: три компакции, четыре сегмента, 147 просмотров, `cut_off:false` во всех 77 |
| `harvest/iteration-05b.md` | ⬆️ **→ `23` §5в** — тот же чат, разбор **построчным JSON-парсингом транскрипта, а не грепом** |
| `harvest/notes-11-current.md` | ⬆️ **→ `71` §7ж** — поимённая роспись 55 картинок и признание «что я утверждал вместо этого (фабрикация)» |
| `harvest/pdf-experiment.md`, `harvest/pdf-experiment-b.md` | ⬆️ **→ `23` §5в** — метка `[ВОССТАНОВЛЕНО]` и рамка честности «мой контекст — сжатый огрызок сессии» |
| `harvest/iteration-06.md`, `harvest/project-analysis-docs.md` | ⬆️ **→ `23` §5в** — «среда сброшена между ходами, обмерить заново нельзя → нет данных» |
| `harvest/iteration-01.md`, `harvest/iteration-02.md`, `harvest/iteration-03.md` | 📍 данные жатвы по трём ранним чатам, разметка `[замер]`/`[оценка]`/`[предположение]` |
| `harvest/notes-01.md`, `harvest/notes-02.md`, `harvest/notes-04-tech-report.md` | 📍 данные жатвы; `notes-01` — тот самый чат с официальным ресёрчем, который проигнорировали и переоткрыли |
| `harvest/notes-05.md`, `harvest/notes-08.md`, `harvest/notes-09.md`, `harvest/notes-10.md` | 📍 данные жатвы; `notes-08` — образец честного «неприменимо — нет данных» (канал не вызывался вовсе) |
| `harvest/project-archives.md` | 📍 дебриф чата на два хода, самый короткий |

**§6а по каталогу пройдена** — файлов без записи ноль. Открыты все двадцать.

### ⬆️ Что поднято: форма отчёта агента о собственной работе

`28` §6а требует помечать число как **измеренное или выведенное**. Для отчёта о себе этого
мало — нужна вторая ось: **откуда автор это знает**. После уплотнения контекста автор
перестаёт различать «я это видел» и «я это помню по сводке»; обе вещи ощущаются одинаково.

Разметка, единая у всех двадцати файлов: `[замер]` · `[оценка]` · `[предпол.]` ·
**`[ВОССТАНОВЛЕНО]` — нет в моём контексте, взято с диска**. Четвёртая метка и есть добавка.

Плюс **рамка честности до данных**, называющая три вещи: что автор не видит · что уже нельзя
перепроверить · чем закрывается пробел. Дословно оттуда:

> «Мой видимый контекст — **сжатый огрызок** реальной сессии… первый прогон **вырезан
> из моего контекста**, но уцелел на диске.»

> «Среда **сброшена между ходами**… там, где число нельзя восстановить из видимого,
> пишу **„нет данных“**.»

**«Нет данных» — полноценный результат жатвы**, а не пропуск: пустая строка провоцирует
додумывание, явное «нет данных, потому что…» само является наблюдением.

Оттуда же — проверка вместо впечатления: канал признан рабочим не по ощущению, а **по флагам
в самих результатах** (`cut_off: false` во всех 77); и встречно — слово «пусто» встретилось
в транскрипте трижды, но все три раза **в тексте документации**, а не в ответах инструмента.
Совпадение по слову проверено контекстом, прежде чем стать наблюдением.

### 🔴 Проверяемый факт: предписанная отмена не исполнена

Баг-репорт 29.07.2026 признал ошибку полностью и **прямо предписал**: «аннулировать/исправить»
два документа, названных поимённо, «пометить как основанные на ошибочной оценке».

Проверено командой сегодня: **ни один из двух такой пометки не несёт.** Оба продолжали
читаться как действующие ещё неделю, до ревизии 04.08, и всё это время кормили опровергнутый
вывод. Отмена, записанная внутри отчёта, — обещание: у неё нет исполнителя, отчёт закрывается,
задача остаётся в его теле и не попадает ни в один реестр (`21` §4г).

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 52
   (00-protocol 10 + context-lab 12 + 06-history 10 + harvest 20)
```

**`context-lab/` закрыт целиком: 42 файла из 42.**

---

## ⬆️ Батч 5: `10-life-history/03-reports/` — 11 из 11

Карта дат снята первой (`SYN-024`): разброс 15.07 → 20.08.2026, то есть каталог **живой**,
в отличие от `06-history/`. Пять файлов оказались указателями — см. отмену находки ниже.

| Файл | Решение |
|---|---|
| `03-reports/visual-missed-and-standard.md` | ⬆️ **→ `19` §3а** — «визуал не был **обязательным полем** → выпал незаметно». Механизм «сползания в дешёвый режим» |
| `03-reports/2026-08-19-pixel-stitch-tool-diagnosis.md` | ⬆️ **→ `28` §6з** — четыре метрики подряд провалились по-разному; причина найдена **прямым просмотром**, и она в допущении о данных |
| `03-reports/vk-source-final-report.md` | ⬆️ **→ `65` §1** — корпус снят **двумя прогонами разной глубины**; чтение его как одного ряда дало ложный факт |
| `03-reports/2026-08-19-token-and-session-estimate.md` | 📍 оценка расхода до закрытия источника. ✅ метод покрыт `41` и `49`; ценно честное §4 «ограничения оценки» |
| `03-reports/integration-manifest-v1-0-0.md` | ✅ консолидация двух линий — покрыто `22-merge-protocol.md`. 📍 ценен приём «победитель по каждому **измерению**, а не по репе целиком» |
| `03-reports/2026-08-20-instagram-second-account-probe-and-close.md` | 📍 разведка до выбора метода. ✅ покрыто `28` §6р. Ценно: **правило применено по своей причине, а не по букве** — «§7 писано под скриншотный корпус, здесь весь текст физически в файлах» |
| `03-reports/case-instagram-session-4.md` | 📍 **указатель** (5 строк) → `context-lab/06-history/`, решён там |
| `03-reports/view-diagnostics.md` | 📍 **указатель** → `context-lab/06-history/`, решён там |
| `03-reports/view-experiment-stats.md` | 📍 **указатель** → `context-lab/06-history/`, решён там |
| `03-reports/instagram-channel-and-reading.md` | 📍 **указатель** → `context-lab/06-history/`, решён там |
| `03-reports/experiment-primary-source.md` | 📍 **указатель** → `context-lab/06-history/`, решён там |

**§6а по каталогу пройдена** — файлов без записи ноль.

### 🔴 ОТМЕНА СОБСТВЕННОЙ НАХОДКИ — `SYN-025`

В батче 1 (v2.2.0) записано: «пять файлов-тёзок, **ни одна пара не совпадает** — копии
успели разойтись, каноническая редакция неизвестна». **Неверно.**

Пять файлов в `03-reports/` — **пятистрочные указатели** «ПЕРЕЕХАЛ → `context-lab/06-history/…`».
Содержимое переехало, указатель оставлен, ничего не удалено. Это **правильный перенос
по правилу самой репы**, а не расхождение. Репу оболгали.

**Как ошибка получилась:** сравнил `sha256`, увидел «различаются» и назвал это расхождением
редакций, **не открыв ни одного файла**. Команда ответила честно на свой вопрос —
«одинаковы ли байты»; вывод сделан на другой — «почему они разные». Проверка стоила
одной команды `head -5`.

**Отягчающее:** запись стояла под заголовком «🔴 Находка репы», то есть подана как
результат, а не как гипотеза. В такой рамке её никто не перепроверяет.

**Исправлено во всех трёх местах, где записано** (`decisions.md`, `CHANGELOG` v2.2.0,
`WATCHLOG` §0) — зачёркиванием поверх, а не удалением (`65` §6.3). Именно та работа,
которую этой же вахтой поймали неисполненной в чужой репе: признание ошибки и правка
несущих её документов — **две разные работы**.

### Что дал каталог

**`19` §3а — тихий пропуск ловится обязательным полем, а не дисциплиной.** По ~300
единицам из 309 был снят только дешёвый канал; результат выглядел полным, потому что
форма записи **не спрашивала** про дорогой. «Визуал не был обязательным полем → выпал
незаметно». Дисциплина не масштабируется на триста единиц, форма — масштабируется.

**`28` §6з — четвёртая метрика подряд означает, что неверна модель данных.** Метрики
проваливались **по-разному** — признак, что каждая ловит свой кусок неверного допущения.
Порог окна перекрытия (12–35 % высоты) отсекал **единственный настоящий сигнал** — узкую
текстовую полосу подписи. Правило: после второй неудачной метрики смотрят на данные,
а не пишут третью; порог «слишком малого» проверяется на том, что он отсекает.

**`65` §1 — структура корпуса устанавливается до чтения.** Источник снят двумя прогонами
разной глубины по одной ленте; отрезок, пропущенный в подробном прогоне, закрывался
быстрым. Чтение как одного ряда дало **ложный факт «постов не существует»** и сессию,
закрывшую задачу как невыполнимую. Номер страницы — координата носителя, не координата
времени.

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 63
   (00-protocol 10 + context-lab 42 + 03-reports 11)
```

---

## ⬆️ Батч 6: `01-registry/` + корень `10-life-history/` + корень репы — 17 из 17

| Файл | Решение |
|---|---|
| `LESSONS.md` | ⬆️ **→ `18` §9ж, `71` §7ж, `STOP_CONDITIONS.md`** — сводный разбор ошибок за 19 чатов, уроки `У-N`. Лучший файл репы |
| `10-life-history/index.md` | ⬆️ **→ `18` §9ж** — редакция v9.0 сняла баннер, противоречивший §4 правил, и назвала цену |
| `10-life-history/01-registry/roadmap.md` | ✅ критерий закрытия из четырёх условий (режим · дословность · паспорт · ≥85/100) покрыт `65` §6.1а — **поднято этой же вахтой** из `01-rules.md` |
| `10-life-history/01-registry/corpus-inventory.md` | 📍 устарел, и **помечен об этом в шапке** — образец правильной отмены, см. ниже |
| `10-life-history/01-registry/file-checklist.md` | 📍 то же: «🔴 УСТАРЕЛО 14.08.2026. НЕ ИСПОЛЬЗОВАТЬ КАК ИСТОЧНИК СТАТУСОВ» + что в нём остаётся ценным |
| `10-life-history/01-registry/source-map.md` | 📍 карта источников после интеграции. ✅ покрыто `22-merge-protocol.md` |
| `10-life-history/01-registry/facts-and-hypotheses.md` | **С** — реестр фактов о человеке; механизм реестра уже поднят в `71` §5д |
| `10-life-history/README.md`, `10-life-history/watchlog.md` | 📍 точки входа проектного уровня. ✅ двухуровневый журнал (репа/проект) покрыт `04-watchlog-protocol.md` |
| `README.md`, `_chto-eto.md` | 📍 точки входа репы. Ценно: `_chto-eto.md` описывает **стадийный вывод проекта из репы и план возврата**, включая запрет `rsync --delete` при ре-мерже |
| `00-READ-FIRST-view-works.md` | ⬆️ **→ `18` §9ж** — файл-баннер в корне, адресованный читающему сейчас: «речь о ТЕБЕ, не о прошлом инстансе» |
| `ROADMAP.md`, `TASKS.md`, `WATCHLOG.md`, `CHANGELOG.md`, `VERSION`, `.repo-id` | 📍 служебка репы. ✅ покрыто `30`, `24`, `04`, `25`, `01` |

**§6а по трём каталогам пройдена** — файлов без записи ноль.

### ⬆️ `18` §9ж — исполняется самый заметный носитель правила, а не канон

Урок `У-1п`, сессия №33. `00-protocol/01-rules.md` §4 **прямо запрещал** распознавание текста
как канал чтения. Шапка **того же файла** несла баннер-приказ «два канала: ТЕКСТ (OCR) +
ВИЗУАЛ, оба обязательны».

> Файл противоречил сам себе **через четыре экрана прокрутки**, и исполнялся **баннер** —
> потому что он стоял выше и был оформлен как приказ.

**Цена:** источник на 309 страниц прочитан запрещённым каналом; комментарии не захвачены
ни под одной единицей, визуальный слой — 1 кадр из 309, паспорта нет. При этом в двух других
служебках источник числился **закрытым**.

**Механика:** баннер родился, когда визуальный канал считался нерабочим — тогда это была
вынужденная мера. Канал реабилитировали, а **баннер, выпущенный под старую картину мира,
никто не отозвал**. Он пережил своё основание на месяц и продолжал командовать.

Отсюда правило: **отменяя основание, тем же ходом правь всё, что было под него выпущено** —
баннеры, шапки, стандарты, промпты вахты, таблицы прогресса.

**Посчитано командой:** один и тот же баннер лежит в **десяти** файлах репы, включая
**архивную редакцию**. Дублирование на входах — не дубль, а защита (читатель не выбирает
дверь), но копии поддерживаются руками, и расходится не текст, а поведение. Требование
в базу: баннер входа не пишется руками второй раз — он генерируется из канона или
проверяется гейтом.

### ⬆️ `71` §7ж — настойчивое несогласие владельца есть сигнал перепроверить вывод

Владелец месяцами повторял «инструмент должен работать, дело не в файлах» и каждый раз
получал технически связное объяснение, почему инструмент всё-таки сломан. Прав был он.

Различать по **повторяемости**, а не по аргументам: разовое возражение — обычный обмен
доводами; то же возражение в третий раз, после того как ему уже отвечали, — **данные**.
Действие: не усиливать аргументацию, а поставить проверку, которую владелец поставить
не может.

### ⬆️ `STOP_CONDITIONS.md` — ложный стоп: ограничение объявлено по ощущению

Урок `У-1в`. На двенадцатой странице из тридцати исполнитель объявил, что упёрся в «лимит
вызовов инструментов», и свернулся. **Ни один инструмент ничего подобного не вернул** —
было только внутреннее ощущение «подхожу к границе». После одной реплики владельца сделал
ещё ~10 вызовов и **дочитал все тридцать**.

Прямо в кит: в автономном прогоне ложный стоп стоит не реплики «продолжай», а **всей ночи**.
Правило — **стоп предъявляется, а не ощущается**: приводится вывод, который его показал.
Исключение оговорено: классы 1 и 2 (необратимое вовне, секрет) объявляются по признаку,
там ошибка в обратную сторону необратима.

### 📍 Образец, который стоит отметить в плюс репе

`corpus-inventory.md` и `file-checklist.md` несут в шапке **правильную отмену**:

> «🔴 УСТАРЕЛО 14.08.2026. НЕ ИСПОЛЬЗОВАТЬ КАК ИСТОЧНИК СТАТУСОВ… Ценность файла —
> техническая: каналы доставки, эксперимент 14.07, поимённый реестр. Копия прежней
> редакции: `09-history/`.»

Названо: **что именно неверно** · **где актуальное** · **что в файле остаётся годным** ·
**где прежняя редакция**. Ровно та работа, которую та же репа **не сделала** для двух
документов про `view` (предписание 29.07 не исполнено, батч 4). Один и тот же приём
исполнен и не исполнен в одной репе — разница в том, что здесь отмену делали **вместе
с пересборкой роадмапа**, а там она была отдельной задачей внутри отчёта.

### Прогресс по репе

```
337 = база(v2.1.0) 78 + С <считается> + прочитано и решено 80
   (00-protocol 10 + context-lab 42 + 03-reports 11 + 01-registry 5 + корни 12)
```

---

## ⬆️ Батч 7: `00-infrastructure/` репы — 63 из 63 в очереди

Каталог посчитан скриптом: **всего 141 · совпало с базой 78 · дельта 27 · нет в базе 36.**
Это единственный каталог репы, где «уже есть в базе» покрывает больше половины.

### Дельта — 27 файлов, из них 24 базовых новее

Проверено сравнением размеров и содержания: у `04`, `08`, `16`, `18`–`25`, `28`, `32`,
`README`, `reports/*/README`, `incident-postmortem-guide`, `pitfalls`, `vision-channel_investigation`,
`templates/pre-commit` **база ушла вперёд** (напр. `28`: 16 КБ в репе против 109 КБ в базе).
Читать нечего — это прежние редакции базовых документов, розданные до их роста.

**Три файла помечены `mtime` как «репа новее» — проверено, ни один таковым не является:**

| Файл | Что оказалось |
|---|---|
| `33-token-budget-and-modes.md` | база больше (8555 б против 8265), репа — прежняя редакция |
| `templates/publish.sh` | база 28.7 КБ против 14.6 КБ — репа отстала вдвое |
| `claude-context/00-navigator.md` | 🔴 **дореименовочная редакция**: кириллические имена файлов (`КОНТЕКСТ_Василий_ЕДИНЫЙ.md`) против латинских в базе |

🔴 **`mtime` соврал во всех трёх случаях** — файловая метка переживает копирование, распаковку
и синк, она говорит о **последнем касании**, а не о последней правке. Уточнение дописано
в `SYN-024`: **дата берётся из шапки документа**.

### Нет в базе — 36 файлов

**11 методичек — ✅ подняты ранее в v1.53.0.** Проверено командой, не принято на слово:
все одиннадцать присутствуют в базе. Поимённо (требование §6а — диапазон «34–44» проверку
не проходит, и она это поймала):

| В репе | В базе |
|---|---|
| `34-pdf-reading-channels.md` | `54-pdf-reading-channels.md` |
| `35-pdf-channels-experiment.md` | `55-pdf-channels-experiment.md` |
| `36-chat-to-project-to-repo.md` | `56-chat-to-project-to-repo.md` |
| `37-samodostatochnost.md` | `57-self-sufficiency-rule.md` |
| `38-git-praktika.md` | `58-git-practice.md` |
| `39-public-mirror-filter.md` | `59-public-mirror-filter.md` |
| `40-audio-video-pipeline.md` | `60-audio-video-pipeline.md` |
| `41-token-analytics.md` | `61-token-analytics.md` |
| `42-vim.md` | `62-vim.md` |
| `43-ai-tools-landscape.md` | `63-ai-tools-landscape.md` |
| `44-claude-code-sandbox.md` | `64-claude-code-sandbox.md` |

**Дельта — поимённо** (те же 24, что выше списком диапазонов; названы по одному, чтобы
проверка §6а проходила): `04-watchlog-protocol.md` · `08-automation-triggers.md` ·
`16-limits-empirical-estimate.md` · `18-documentation-philosophy.md` · `19-reporting-system.md` ·
`20-knowledge-capture-protocol.md` · `21-revision-protocol.md` · `22-merge-protocol.md` ·
`23-session-continuity.md` · `24-changelog-protocol.md` · `25-versioning-and-releases.md` ·
`28-empirical-experiment-methodology.md` · `32-git-hooks-and-secret-scanning.md` —
у всех база ушла вперёд.

| Файл | Решение |
|---|---|
| `reports/situations/2026-07-23-fail-loud-gap-d2.md` | ⬆️ **→ `71` §1а** — пять действий при найденной дыре, и забывают **третье** |
| `reports/incidents/2026-08-13-pdf-delivery-desync-incident.md` | ⬆️ **→ `STOP_CONDITIONS.md`** — правило против ложного стопа породило **молчание о внешней проблеме**. Лучшая находка батча |
| `reports/situations/2026-08-14-instagram-narezka-i-chetyre-skripta.md` | ⬆️ **→ `SYN-020` (уточнение)** — область поиска сужена **границей артефакта**, а не памятью: ревизия шла по архиву, скрипты владельца в него не входили → написан четвёртый скрипт при трёх существующих |
| `reports/situations/2026-07-22-knowledge-vs-arhiv-v-chat.md` | ✅ покрыто `56-chat-to-project-to-repo.md` (поднято в v1.53.0). Ценно как обоснование канона «один zip = полный контекст»: механизм доставки провалился **не по лимиту** (тонкий срез влезал), а на первом боевом запуске — многошаговая оркестрация «найди → прочитай 4 файла → потом читай источник» оказалась хрупкой |
| `reports/situations/2026-07-22-spin-off-standalone-i-plan-merge.md` | ✅ парный к предыдущему, покрыт `22-merge-protocol.md` |
| `reports/situations/2026-08-20-repo-standard-gaps-and-repair.md` | ✅ покрыто гейтовой философией базы: три проверки деплоя были **предупреждениями, а не стопами**, и дерево на 334 файла ушло в публикацию несоответствующим. Из **83 заголовков** CHANGELOG маркер разряда несли **8** — долг с genesis репы. Класс `PIT-G`, лечится хуком, а не памятью |
| `reports/situations/2026-07-23-chat-image-limit-and-pdf-splitting.md`, `…-delivery-channel-three-failures.md` | 📍 решены в батче 3 по копиям в `context-lab/06-history/` |
| `reports/investigations/2026-07-23-vision-dead-frame0.md` | 📍 то же, решён в батче 3 |
| `reports/bugs/2026-07-23-zip-cyrillic-name-unarchiver-corrupted.md` | ✅ поднято ранее — **PIT-025** (UTF-8-флаг по локали, два проявления) |
| `reports/bugs/2026-07-23-uploads-manifest-vs-disk.md` | ✅ покрыто законом «диск, а не док»: манифест вложений неполон, файлы на диске есть, а в списке нет → провоцирует ложный вывод «файла нет». Подтверждение закона живым случаем |
| `reports/bugs/2026-07-23-ukrainian-in-thinking-summary.md` | 📍 дефект продукта, чинить нельзя. Ценен жанрово: **наблюдение о среде фиксируется как баг-репорт с пометкой «не наш»**, а не теряется |
| `reports/incidents/release-notes-incident.md` | ✅ поднято ранее (INC-RELEASE-NOTES) |
| `reports/incidents-summary.md`, `reports/investigations-summary.md` | ✅ **парные реестры** «был дефект» / «дефекта не оказалось» — покрыто `19` §1 (диспетчер) |
| `reports/merges/merge-manifest-session9-v1-2-0.md` | ✅ `22-merge-protocol.md`; режим **additive-only** назван явно |
| `watchlog.md`, `base-repo-readme.md` | 📍 унаследованные копии базы прежних поколений — **не редакции репы**, читать нечего |
| `templates/split_all.py`, `split_all_v1_backup.py`, `split_pdf.py`, `probe_source.py` | 📍 те самые четыре скрипта, слитые позже в `source_tool.py` (решён в батче 1). `_v1_backup` — прежняя версия рядом с текущей |
| `templates/fix_release_notes.sh`, `repo-readme-template.md` | ✅ покрыто `templates/` базы и `25-versioning-and-releases.md` |
| `.DS_Store` | 📍 мусор macOS, единственное расхождение между двумя копиями репы |

**§6а по каталогу пройдена** — файлов без записи ноль.

### 🔴 Главное: правило против ложного стопа породило зеркальный дефект

Post-mortem SEV3 от `13.08.2026`. Источник числился во вложении, страницы были видны
в контексте, **а файла на диске не было**. Исполнитель это обнаружил — двумя `ls` и одним
`find` — и **не сказал в первой реплике**. Продолжил обходным каналом. Через одиннадцать
минут сказал, но **вердиктом** («кропы физически невозможны»), а не запросом («перезалей»).
Пользователь заметил сам; после перезалива вопрос закрылся **одним действием**.

> **Мониторинг сработал, оповещение — нет.**

**Корень, названный автором:** правило против ложных ограничений («объявил ограничение —
почти наверняка ошибся, проверь себя и работай дальше») исполнено **буквально**. Оно написано
про **свои** ограничения, а применено к **внешней** проблеме.

Это прямой противовес тому, что поднято в кит батчем раньше (ложный стоп). Без него правило
опасно: **своё ограничение проверяют молча, чужую проблему называют сразу.** Проверочный
вопрос — не «уверен ли я, что не могу», а **«может ли это починить владелец одним действием?»**.

Три отдельные ошибки того случая: задержка на 14 минут · **форма вердикта вместо запроса** ·
молчаливая деградация качества (три батча записаны заведомо хуже, и об этом не сказано).

### Прогресс по репе

```
337 = база 78 + С <считается> + прочитано и решено 143
   (00-protocol 10 + context-lab 42 + 03-reports 11 + 01-registry 5 + корни 12 + 00-infrastructure 63)
```

Осталось в М: `09-history/` 56 файлов (смешанный, решается пофайлово).

---

## ⬆️ Батч 8: `10-life-history/09-history/` — 61 из 61

Архив прежних редакций. **`SYN-024` применён до чтения:** снята карта дат и шапок — весь
каталог датирован 12.07 → 07.08.2026, то есть **старше** и `00-protocol/`, и `context-lab/`.
Читалось ради того, чего в живых редакциях уже нет.

### М — прежние редакции метода (47 файлов)

| Файл | Решение |
|---|---|
| `reviziya-plan.md` | ⬆️ **→ `21` §4д** — ревизия собственных утверждений, заказанная **до** работы, для чистой сессии. Лучшее в каталоге |
| `eksperimenty-k-proverke.md` | ⬆️ **→ `21` §4д** — парный: восемь проб перед стартом, с критерием провала у каждой |
| `rezhimy-analiza.md` | ✅ режимы глубины покрыты `33`. 📍 Ценно: **процедура смены режима** — понизить можно только через аргументацию + явное подтверждение владельца + запись причины; «молчаливое понижение = брак». Родня поднятому `19` §3а |
| `metodika-analiza.md` | ✅ методика v4.0, заменена `00-protocol/02-process.md` (решён в батче 1) |
| `00-glavnye-pravila.md`, `glavnye-pravila-v2-sessiya2.md` | ✅ прежние редакции правил, заменены `00-protocol/01-rules.md` |
| `index-v8-0-2026-07-15.md` | 📍 **та самая редакция с баннером-приказом**, разобранная в `18` §9ж. Сохранена не зря: без неё нельзя было бы доказать, что противоречие существовало |
| `01-kak-nastroit-proekt.md` | ✅ покрыто `56-chat-to-project-to-repo.md` |
| `prompt-novogo-chata.md`, `prompt-v2-sessiya2.md`, `mega-handoff-2.md` | ✅ прежние промпты вахты; форма покрыта `23` §5б |
| `handoff-prompt-session-5.md`, `-9.md`, `-10.md`, `-23.md`, `-34.md`, `-38.md` | 📍 шесть промптов вахты подряд. **Ценно как ряд:** видно, как форма промпта менялась 19 сессий; ранние несут «НЕ верь этому промпту, сверь эмпирически» — поднято как PIT-086 |
| `roadmap-v1-sessiya1.md`, `roadmap-v2-sessiya2.md`, `roadmap-v3-0-2026-07-14.md`, `roadmap-root-v1-2026-08-07.md` | 📍 четыре редакции роадмапа. Каждая явно называет, какую заменяет — образец `65` §6.3 |
| `corpus-inventory-v4-0-2026-07-14.md`, `inventar-korpusa.md` | 📍 прежние инвентари; живой помечен УСТАРЕВШИМ (батч 6) |
| `file-checklist-v1-0-2026-07-15.md` | 📍 прежний чек-лист файлов |
| `gipotezy-i-voprosy.md` | 📍 прежний реестр гипотез, заменён `04-fakty-gipotezy` |
| `vyvod-0-watchlog.md`, `vyvod-1-eksperimenty-rezultaty.md`, `vyvod-2-gipotezy-status.md`, `vyvod-4-metodika-obnovlenie.md` | 📍 выводы сессии 12.07 по служебным осям. `vyvod-1` ценен формулировкой «честный учёт по Правилу №4: что сделано — сделано, что нет — не приписываем» |
| `watchlog-sessiya2.md`, `watchlog-v6-sessiya1.md` | 📍 прежние журналы. `watchlog-v6` несёт канон **«один архив = полный контекст»** в исходной формулировке |
| `prep.py` | 📍 прежний скрипт подготовки страниц, предшественник `source_tool.py` |
| `v3-v4-chernoviki/00-indeks-3-0.md`, `00-indeks-4-0.md` | 📍 индексы поколений 3.0 и 4.0 |
| `v3-v4-chernoviki/01-pravila-3-0.md`, `01-pravila-4-0.md` | 📍 правила двух поколений |
| `v3-v4-chernoviki/02-watchlog-3-0.md`, `02-watchlog-4-0.md` | 📍 журналы двух поколений |
| `v3-v4-chernoviki/03-inventar-3-0.md`, `03-inventar-4-0.md` | 📍 инвентари двух поколений |
| `v3-v4-chernoviki/05-prompt-3-0.md`, `05-prompt-4-0.md` | 📍 промпты двух поколений |
| `tools-2026-08/README.md` | 📍 **образец правильной отмены инструмента:** «⛔ АРХИВ. НЕ ЗАПУСКАТЬ» + таблица «какой скрипт какой подкомандой заменён» + причина хранения |
| `tools-2026-08/split_all.py`, `split_pdf.py`, `probe_source.py`, `split_source-dublikat.py` | 📍 четыре скрипта, слитые в `source_tool.py` (решён в батче 1). Имя `-dublikat` само сообщает о дубле |

### С — содержание (14 файлов, с причиной)

| Файл | Причина |
|---|---|
| `zametki-dima-ch1-str01-27.md`, `zametki-dima-ch1-str28-55.md` | дословный захват источника — результат предметной работы |
| `zametki-zaklyucheniya-psikhologov.md` | то же, заключения по конкретному человеку |
| `11-instagram-tezisnyy-staryy-do-perekata.md` | прежние заметки по источнику |
| `compass-artifact-wf-aada4654-d57d-5090-8f5e-c6c9a60fb118-text-markdown.md` | внешний ресёрч-экспорт по лимитам выгрузок — предметный материал |
| `vyvod-3-testy-dinamika.md`, `vyvod-5-zametki-notion.md` | выводы по содержанию тестов и дневника |
| `v3-v4-chernoviki/04-fakty-gipotezy-3-0.md`, `04-fakty-gipotezy-4-0.md` | реестры фактов о человеке двух поколений |
| `prezhnie-deliverably/portret-final-tekst.md`, `vasily-analysis-2026-tekst.md`, `zametki-kloda-po-vsem-faylam.md`, `zametki-po-faylam-vasiliy.md` | прежние редакции портрета — результат, не метод |
| `scratch-session5/batch6-fragment-zamenen-batchem6-v-12-notion.md` | черновик батча, заменён |

**§6а по каталогу пройдена** — файлов без записи ноль.

### Что дал каталог

**`21` §4д — ревизия собственных утверждений заказывается ДО вложения в работу.** План,
написанный исполнителем **на самого себя**, для исполнения **другой, чистой сессией**:

> «Перепроверить всё, что я утверждаю, **своими глазами**, а не на моё слово. Лучше узнать
> это сейчас, чем **после 20 чатов работы**. Занимает ~10 просмотров и 15 минут.»

Четыре элемента, без которых это просто «перепроверь меня»: **чистая сессия** (ревизор
не наследует байас) · **таблица «что утверждал → каким способом получил»** (воспроизводится
способ, а не пересказывается вывод) · **критерий провала записан заранее** для каждой пробы,
с приписанным действием на каждый исход · **собственные проколы помечены прямо в плане**
(«⚠️ я это утверждал, НЕ ПОСМОТРЕВ страницу», «⚠️ бессмысленная проверка, мой прокол») —
это разметка доверия, чтобы ревизор тратил время на слабые места.

📍 **Отмечено в плюс репе:** `tools-2026-08/README.md` — образец отмены инструмента,
которого не хватило двум документам про `view` (батч 4). Шапка «⛔ АРХИВ. НЕ ЗАПУСКАТЬ»,
таблица соответствия «старый скрипт → новая подкоманда», причина хранения. Разница с
неисполненным случаем та же, что в батче 6: **отмену делали одним ходом с заменой**,
а не отдельной задачей на потом.

---

## 📍 Каталоги С — поимённо (требование §6а)

`PROCEDURE.md` §2 разрешает записывать каталог С **одной строкой с причиной**, и это сделано
в батче 1. Но §6а проверяет **поимённо**, и по всей репе она показала 47 файлов «без записи» —
все из С-каталогов. Формально проверка права: групповая формулировка имени файла не содержит.

Довод прежней ревизии (`SYN-021`, «закрытие каталога проверяется той же командой, что и репа»)
здесь работает буквально. Поэтому имена выписаны — дёшево, и делает приёмку непробиваемой.

**Ни один из этих файлов не читался и читаться не должен** — это результат предметной работы
о человеке, а не метод. Причина у каждого каталога записана в батче 1.

| Каталог | Файлы |
|---|---|
| `10-life-history/02-notes/` — дословный захват источников, 18 | `dashboard-session-2.md` · `handwritten-diary.md` · `hypotheses-synthesis-errors.md` · `instagram.md` · `instagram-v2.md` · `instagram-v2-account2.md` · `mega-handoff.md` · `mentor-part-1.md` · `mentor-part-2.md` · `msu-tests.md` · `notion-diary.md` · `notion-map.md` · `pleasant-words.md` · `psychologist-conclusions.md` · `psychologist-sessions.md` · `quality-passports.md` · `text-sources-recon.md` · `vk.md` |
| `10-life-history/04-deliverables/` — готовые портреты, 6 | `portret-final-tekst.md` · `portret-vasiliy-evdokimov-final.md` · `vasily-analysis-2026-tekst.md` · `vasily-analysis-2026.md` · `zametki-kloda-po-vsem-faylam.md` · `zametki-po-faylam-vasiliy.md` |
| `01-psychological-profile/` — разборы личности, 7 | `kak-otpustit-styd-proshlogo-i-provalov.md` · `korni-zhazhdy-k-samopoznaniyu.md` · `mne-byvaet-bolno.md` · `psikhologicheskiy-portret.md` · `ten.md` · `terapevticheskaya-karta.md` · `ya-i-podlichnosti-ifs.md` |
| `04-genetics-genotek/` — генетический отчёт и производные, 7 | `buklet-vasiliya-evdokimova-polnyy.md` · `dnk-analiz-po-psikhike-fizike-i-vitaminam.md` · `dnk-razbor.md` · `geneticheskaya-kniga-vasiliya-evdokimova.pdf` · `moya-vnutrennyaya-sila-vasiliy-evdokimov.md` · `pitanie-i-nutrienty-vasiliya-evdokimova.md` · `psikhologicheskiy-i-fiziologicheskiy-profil-vasiliya-evdokimova.md` |
| `05-faith/` — личные документы веры, 3 | `dnevnik-ispovedi.pdf` · `dogovor-very-v-iisusa-khrista.pdf` · `generate_prayer_audio.py` |
| `07-self-reflection/` — книга возвращения к себе, 3 | `kniga-vozvrashcheniya-k-sebe.md` · `kniga-vozvrashcheniya-k-sebe-chernovik.md` · `modul-9.md` |
| `09-session-summaries/` — саммари психологического чата, 2 | `sammari-psikhologicheskogo-chata-bez-vlozheniy.md` · `sammari-psikhologicheskogo-chata-s-vlozheniyami.md` |
| `99-misc/` — личный чек-лист владельца, 2 | `to-do-list.md` · `to-do-list-chernovik.md` |
| `99-misc/career-guidance/` — профориентация, 3 | `career-psychologist-summary.md` · `career-psychologist-summary.pdf` · `proforientation-results-2023-03.pdf` |

**Одно уточнение по `02-notes/quality-passports.md`:** это паспорта качества источников,
то есть служебка, а не захват. Оставлен в С осознанно — его **механизм** (паспорт как условие
закрытия) уже поднят в `65` §6.1а из `01-rules.md`, а содержание паспортов предметное.

**Одно уточнение по `05-faith/generate_prayer_audio.py`:** скрипт в С-каталоге. Открыт,
проверено — генерация аудио из личных текстов, метода в нём нет. Оставлен С с причиной,
а не пропущен по расширению (`PROCEDURE.md` §7 запрещает фильтр по расширению).

### Поправка к записи батча 8

Пять промптов вахты были записаны сокращённо (`handoff-prompt-session-5.md`, `-9.md`, `-10.md`,
`-23.md`, `-34.md`, `-38.md`) — §6а их не нашла, потому что сокращение имени файла не содержит.
**Это третий случай за вахту** (батч 4 — усечённое имя, батч 7 — диапазоны, здесь — сокращения).
Полные имена: `handoff-prompt-session-5.md` · `handoff-prompt-session-9.md` ·
`handoff-prompt-session-10.md` · `handoff-prompt-session-23.md` · `handoff-prompt-session-34.md` ·
`handoff-prompt-session-38.md`.

> 🔴 **Три раза за одну вахту проверка ловила один и тот же класс ошибки:** имя файла,
> записанное короче, чем оно есть. Это не небрежность, а свойство письма — при перечислении
> ряда рука сама сокращает общую часть. Вывод для процедуры: **§6а гоняется после каждого
> батча, а не в конце репы**; ловит она дёшево, а её отсутствие стоило прошлым вахтам
> двух ложных приёмок.

---

## ✅ ПРИЁМКА `character-a-analysis` (`PROCEDURE.md` §6)

```
character-a-analysis: всего 337 = база(v2.1.0) 78 + С 68 + прочитано и решено 191
```

Проверка: `78 + 68 + 191 = 337` ✅ — сходится. **Посчитано скриптом, не головой**
(`SYN-021`: числа, посчитанные головой при наличии команды, ошибались четыре раза из четырёх).

**🔴 Версия базы указана в самой формуле — впервые.** Это `SYN-023`, заведённый в начале этой же
вахты: слагаемое «совпало с базой» — не свойство репы, а отношение между репой и базой,
и оно портится со временем само. Пересчёт при другой версии базы даст другое число,
и это будет **норма**, а не пробел.

**Поимённая сверка §6а: пройдена по всей репе — файлов без записи `0`.**
Прогонялась после каждого батча и **три раза поймала реальный пропуск**: усечённое имя файла
(батч 4), диапазоны вместо имён (батч 7), сокращения общей части (батч 8). Ни один из трёх
не был бы виден глазами.

**Приёмка, которой у этой репы не было вовсе.** До этой вахты `character-a-analysis` числилась
пройденной **без раздела приёмки** — ревизия 21.08 показала пробел в 24 файла. Сейчас репа
закрыта обеими проверками.

### Копии

Три. Канон — `~/Documents/система_репозиториев/character-a-analysis` (`VERSION` 3.45.1).
Вторая копия в `~/Documents/` отличается **одним `.DS_Store`**. Третья, в `~/Downloads/_sync-base/`,
отстала на 9 версий и **держит `.git`** — четвёртое подряд подтверждение `SYN-017`
(`ROADMAP.md` §P2: git-история живёт у устаревших экземпляров).

### Что репа дала базе — 17 разделов

| Документ | Разделы |
|---|---|
| `71-fail-loud-and-sourcing.md` | §1а (пять действий при дыре) · §5д (реестр утверждений со статусом) · §7ж (ложный негатив + отбор причин + несогласие владельца) · §7з (пустой ответ не пишется как содержание) |
| `28-empirical-experiment-methodology.md` | §4а (каталог дизайнов) · §5г (механизм приписан заметной величине) · §6з (четвёртая метрика = неверна модель) · §10а (производное богаче оригинала) |
| `19-reporting-system.md` | §3а (промежуточный отчёт, семь полей + обязательное поле против тихого пропуска) |
| `23-session-continuity.md` | §5в (жатва знаний из прошлых сессий + источник знания в отчёте о себе) |
| `65-visual-source-pipeline.md` | §1 (сквозная лента + структура корпуса до чтения) |
| `18-documentation-philosophy.md` | §9ж (исполняется самый заметный носитель правила) |
| `21-revision-protocol.md` | §4д (ревизия своих утверждений до вложения в работу) |
| `06-autonomous-mode-kit/STOP_CONDITIONS.md` | ложный стоп + противовес (молчание о внешней проблеме) |

Плюс три карточки лаборатории: `SYN-023` (усыхание слагаемого приёмки), `SYN-024` (подъём
по отменённому файлу; дата из шапки, не из `mtime`), `SYN-025` (хеши сравнили, файлы не открыли),
и уточнение `SYN-020` (область сужается носителем).

### Дефекты репы — записаны, не чинились (чужая репа)

- 🔴 **Побайтовый дубль внутри каталога:** `35-pdf-channels-experiment.md` = `experiment-primary-source.md`.
- 🔴 **Каталог противоречит сам себе:** `context-lab/02-observations.md` несёт устаревший вывод
  рядом с собственным опровержением — ревизия 04.08 прошла по четырём файлам и не дошла до пятого.
- 🔴 **Предписанная отмена не исполнена:** баг-репорт 29.07 назвал два документа поимённо
  и потребовал пометить их как ошибочные; **ни один пометки не несёт**.
- 🔶 Три проверки деплоя были **предупреждениями, а не стопами** — дерево на 334 файла ушло
  в публикацию несоответствующим собственному стандарту.

### Что в плюс репе — отмечено отдельно

Та же репа **умеет** правильную отмену и показывает это трижды: два реестра с шапкой
«🔴 УСТАРЕЛО, НЕ ИСПОЛЬЗОВАТЬ КАК ИСТОЧНИК СТАТУСОВ + где актуальное + что остаётся годным»,
и `tools-2026-08/README.md` с «⛔ АРХИВ. НЕ ЗАПУСКАТЬ» и таблицей замен. Разница
с неисполненным случаем одна: **отмену делали одним ходом с заменой**, а не отдельной
задачей внутри отчёта.

**Репа 5 из 57 закрыта.** Честный счёт кампании: `it-base`, `mission-control`, `exam-kit`,
`dota-dossier`, `character-a-analysis`.

---

## Репа 3 `portrait-of-taste` — ВОЗВРАТ, вахта VIII, 22.08.2026

Приёмка репы **сошлась** 20.08 (`1572 = С 1259 + база/дельта 145 + прочитано 168`) —
и соврала: ревизия 21.08 показала **36 служебных `.md` без единой записи решения**
(`SYN-021`, `SYN-022`). Возврат.

### Копии — три, картина та же

| Копия | `VERSION` | `.git` | файлов |
|---|---|---|---|
| `~/Documents/система_репозиториев/portrait-of-taste` — **канон** | 3.6.14 | нет | **1572** |
| `~/Documents/portrait-of-taste` | 3.6.14 | нет | 1571 |
| `~/Downloads/_sync-base/portrait-of-taste` | 3.5.7 | **есть** | 1656 |

**Пятое подряд подтверждение `SYN-017`:** `.git` у самой отставшей копии.

### 🔴 Пробел оказался 73, а не 36 — и причина в групповой пометке

Пересчёт по М-каталогам разметки 20.08 (`standards/`, `reports/`, `docs/`, `backlog/`,
`templates/`, `prompts/`, корень) дал **73 файла без поимённой записи**, а не 36.

Разница объясняется целиком: **20 файлов `standards/` были решены одной строкой** —
«📍 предметные — метод разбора **людей**, не системы». Ревизия 21.08 считала только `.md`
в части каталогов и групповую строку зачла.

**Проверка открытием показала, что групповая пометка неверна для семи файлов.** Это
не предметный метод, а **системный**:

| Файл | Что в нём на самом деле |
|---|---|
| `standards/NAMING_STANDARD.md` | единый стандарт имён файлов и папок репы |
| `standards/REPORTS_SPEC.md` | ТЗ на отчёты, введённое после того, как отчёт сломали 5+ раз |
| `standards/RESTORATION_PROTOCOL.md` | восстановление рабочего каталога из последнего архива |
| `standards/WORKSPACE_CONTINUITY_PROTOCOL.md` | эфемерность рабочей ФС, **−30 % времени за неделю** |
| `standards/ASSET_STORAGE_PROTOCOL.md` | критичные активы живут в репе, не в `/tmp` |
| `standards/DEPLOY_COMPATIBILITY.md` | совместимость с общим деплойером, обязательные файлы в корне |
| `standards/SCRIPT_USAGE_ON_PROFILES_PROTOCOL.md` | граница «скрипты на метаданных — да, на содержимом — нет» |

> **`SYN-021` подтверждён третьим случаем подряд.** Групповая пометка — это **гипотеза
> о содержимом**, а не решение. Она законна только там, где проверена открытием
> хотя бы выборочно, и это должно быть записано.

### М — прочитано и решено в этот заход

**`standards/` — 20 файлов.**

| Файл | Решение |
|---|---|
| `standards/WORKSPACE_CONTINUITY_PROTOCOL.md`, `standards/RESTORATION_PROTOCOL.md`, `standards/ASSET_STORAGE_PROTOCOL.md` | ⬆️ **→ `64-claude-code-sandbox.md`** — эфемерность рабочей ФС как планируемое условие |
| `standards/SCRIPT_USAGE_ON_PROFILES_PROTOCOL.md` | ✅ **PIT-081** (автоматизируется механика, не суждение) — поднято 20.08. Здесь граница названа точнее: «metadata можно, content — нет» |
| `standards/NAMING_STANDARD.md` | ✅ покрыто `01-repo-standard.md`; причина введения — расхождение имён между местами — тот же довод |
| `standards/REPORTS_SPEC.md`, `standards/REPORT_STANDARD.md` | ✅ покрыто `19` §1 и поднятым сегодня `19` §3а. 📍 Ценно: ТЗ появилось **после пятой поломки одного артефакта** |
| `standards/DEPLOY_COMPATIBILITY.md` | ✅ покрыто `25-versioning-and-releases.md` и гейтом деплойера базы; здесь — клиентская сторона того же контракта |
| `standards/PROFILE_STANDARD.md`, `standards/MALE_PROFILE_STANDARD.md`, `standards/PHOTO_STANDARD.md`, `standards/PHOTO_ANALYSIS_STANDARD.md`, `standards/CROP_STANDARD.md`, `standards/METRICS_EXTENSION.md`, `standards/RECOGNITION_SYSTEM.md`, `standards/INTERVIEW_MODE.md`, `standards/SOCIAL_ANALYSIS.md`, `standards/CHARACTER_KIT.md`, `standards/CODE_ASSIGNMENT_PROTOCOL.md`, `standards/PSYCHOLOGY_INTERPRETATION_PROTOCOL.md` | 📍 **предметные — подтверждено открытием**, а не групповой пометкой. Метод разбора людей; `CHARACTER_KIT` — аналог `03-role-kit/` базы, `PSYCHOLOGY_INTERPRETATION_PROTOCOL` выведен из preferences владельца |

**`reports/situations/` — 12 файлов.**

| Файл | Решение |
|---|---|
| `reports/situations/2026-08-19-stop-asking-same-person-questions.md` | ⬆️ **→ `71` §4а** — правило действует в границах урока, из которого выведено |
| `reports/situations/2026-07-22-privacy-position-disagreement.md` | ⬆️ **→ `71` §7а** — разногласие, не решаемое доводами, решается вопросом «чьё это решение» |
| `reports/situations/2026-07-30-profile-destruction-post-mortem.md` | ⬆️ **→ `71` §7з** — `default=''` стирает разницу между «не нашёл» и «пусто» |
| `reports/situations/2026-07-31-false-confident-assertions-H-J.md` | ✅ **PIT-073** + `71` §2 (запрещённые формулировки, включая «Поправка») — поднято 20.08 |
| `reports/situations/2026-08-15-photo-recognition-false-marriage-G.md` | ✅ **PIT-073**, поднято 20.08 |
| `reports/situations/2026-07-22-deploy-infrastructure-saga.md`, `reports/situations/SITUATIONS_REPORT.md` | ✅ сведено в реестр базы — **27 упоминаний `portrait-of-taste`** в `reports/pitfalls.md`, проверено командой |
| `reports/situations/2026-07-30-ignored-handles-and-photo-rendering.md` | ✅ покрыто `20-knowledge-capture-protocol.md`. 📍 Ценно: «работал в узком контексте задачи, **не сохранял side-info в структурированной форме**» |
| `reports/situations/2026-07-29-chrome-connection-reset.md` | 📍 предметное. Ценно методически: «QUIC был **только первым слоем** — после отключения обнажилась основная проблема» |
| `reports/situations/2026-07-25-instagram-access.md` | 📍 предметное — варианты доступа к источнику |
| `reports/situations/2026-07-23-version-numbering-fix.md` | ✅ покрыто `25-versioning-and-releases.md` (soft-фикс опубликованной нумерации) |
| `reports/situations/2026-08-14-full-revision.md` | 📍 отчёт полной ревизии репы |

**`reports/audits/`, `investigations/`, `incidents/` — 11 файлов.**

| Файл | Решение |
|---|---|
| `reports/audits/2026-07-24-full-revision-plan.md`, `reports/audits/2026-07-25-full-revision-protocol.md`, `reports/audits/revision-registry.md`, `reports/audits/repo_revision_2026-07-22.md` | ✅ покрыто `21-revision-protocol.md`; **реестр ревизий** как жанр — `21` §8 |
| `reports/audits/2026-07-25-recognition-test-1-failure.md` | 📍 **провал собственной проверки записан** (0 из 2) — покрыто `28` §11 и §6з, но отмечено как образец |
| `reports/audits/2026-07-25-deduplication-log.md`, `reports/audits/2026-07-25-photos-integration-log.md`, `reports/audits/naming-migration-log.md` | 📍 логи операций — след массовых правок, `00-CLAUDE-STOP.md` §3 |
| `reports/investigations/archive_size_investigation.md` | ✅ жанр расследования «дефекта не оказалось» покрыт `19` §1; стоимость названа («~3 сообщения, ~15 минут») |
| `reports/investigations/2026-07-23-pr-agax-reanalysis.md` | 📍 предметный переанализ |
| `reports/incidents/deploy_pipeline_incident.md` | ✅ сведено в portrait-of-taste:PIT-101…108 → реестр базы |

**`docs/` — 9 файлов.**

| Файл | Решение |
|---|---|
| `docs/CLAUDE_IN_CHROME_GUIDE.md` | ⬆️ **→ `40-tools-and-capabilities-map.md` §1а** — браузерного канала в карте возможностей **не было вовсе** |
| `docs/WORKFLOW_JOURNAL.md` | 📍 хронологический журнал проекта, отдельный от вахтенного — покрыто `04-watchlog-protocol.md` |
| `docs/GLOSSARY.md`, `docs/PROJECT_HISTORY.md`, `docs/HOW_TO_USE.md` | 📍 предметные: словарь осей оценки, история проекта, гайд по пакету |
| `docs/TROUBLESHOOTING_ACCESS.md`, `docs/TECHNICAL_ACCOUNT_EXPLAINED.md` | 📍 предметные: обход сетевых проблем и отдельная учётка для парсера |
| `docs/ROADMAP-v3.0.0-deprecated.md` | 📍 прежний роадмап, **помечен deprecated в имени файла** — редкая и хорошая форма |
| `docs/instagram_handles.yaml` | 📍 предметные метаданные |

**Корень, `backlog/`, `templates/`, `prompts/` — 11 файлов.**

| Файл | Решение |
|---|---|
| `_READ_FIRST.md` | ⬆️ **→ `18` §9з** — порядок входа, ставящий объяснение предмета **до** рабочих инструкций |
| `PROJECT_DEFENSE_PROMPTS.md`, `PROJECT_DEFENSE_ADVANCED.md` | ⬆️ **→ `18` §9з** — объяснение легитимности предмета, записанное **один раз**, вместо пересказа каждую сессию |
| `NEXT_STEPS.md` | 📍 приоритеты; явно помечен «Заменяет старый `TASKS.md` (устарел)» |
| `repos-map-CHANGELOG.md` | 📍 унаследованная копия журнала карты реп |
| `backlog/PROTOCOLS_BACKLOG.md` | 📍 бэклог служебок с приоритетами P0/P1/P2 по частоте поломок. **Заказан владельцем дословно:** «сделай служебки… мне надоело наступать на одни и те же грабли» |
| `templates/REPO_README_TEMPLATE.md` | ✅ покрыто `templates/` базы |
| `prompts/MASTER_PROMPT.md`, `prompts/PROMPT_batch1.md`, `prompts/PROMPT_batch2.md`, `prompts/PROMPT_batch3.md` | 📍 промпты обработки; форма покрыта `23` §5б |

**`reports/` — 10 файлов итоговых отчётов** (`PORTRAIT_MODELS.md`, `PORTRAIT_MODELS.docx`,
`PORTRAIT_MODELS.html`, `PORTRAIT_MODELS_dark.html`, `PORTRAIT_MODELS.pdf`,
`VKUS_ANALYSIS.md`, `VKUS_ANALYSIS.docx`, `VKUS_ANALYSIS.html`, `VKUS_ANALYSIS_dark.html`,
`VKUS_ANALYSIS.pdf`) — **С**: главный результат проекта в пяти форматах. Не метод.

### Что репа дала базе в этот заход — 6 разделов

- **`71` §4а** — правило действует в границах урока, из которого выведено. Правило про
  **факт, вписанный без проверки**, было расширено на **идентичность человека в папке** —
  и породило вопросы владельцу о том, что он уже решил структурно. «Хватит спрашивать.»
  Перед вопросом — три проверки: ответ уже записан? · это факт или его решение? ·
  правило выведено из **этого** случая или из похожего?
- **`71` §7а** — разногласие, не решаемое доводами, решается вопросом «чьё это решение».
  Разбор написан участником спора, **не отозвавшим свой аргумент**: «решение принято
  владельцем, потому что последствия у него; довод остаётся».
- **`71` §7з** — `default=''` в извлечении стирает разницу между «не нашёл» и «пусто»,
  а вызывающий код пишет эту пустоту **на место содержимого**. 52 документа, восстановление
  из бэкапов, **повтор через три дня**. Ни одной ошибки не выброшено.
- **`18` §9з** — предмет, который приходится объяснять каждую сессию, объясняется один раз
  в репе. С обязательной второй половиной: **записанное обоснование снимает лишнее колебание,
  а не границы**; если из файла нельзя понять, что запрещено, он написан наполовину.
- **`40` §1а** — браузерный канал: наследует авторизованную сессию владельца, поэтому даёт
  доступ, закрытый логином, и по той же причине самый чувствительный. **В карте возможностей
  его не было вовсе.**
- **`64`** — рабочая ФС эфемерна: проверка живости первой командой · восстановление
  по протоколу · критичный актив не живёт в `/tmp`. Цена измерена репой: **−30 % времени
  за неделю**.

### ✅ ПРИЁМКА `portrait-of-taste` — ПЕРЕСОБРАНА (`PROCEDURE.md` §6)

```
portrait-of-taste: всего 1572 = база(v2.9.0) 140 + дельта базы 25
                              + С 1271 + прочитано и решено 136
```

Проверка: `140 + 25 + 1271 + 136 = 1572` ✅ — посчитано скриптом.
**Версия базы в формуле** (`SYN-023`).

**Что изменилось против записи 20.08** (`1572 = С 1259 + база/дельта 145 + прочитано 168`):

| | 20.08 | 22.08 | почему |
|---|---|---|---|
| база + дельта | 145 | **165** | база выросла, часть тёзок вышла из совпадения (`SYN-023`) |
| С | 1259 | **1271** | 10 итоговых отчётов в пяти форматах и `meta/` отнесены к С явно |
| прочитано и решено | 168 | **136** | 🔴 **прежнее число было завышено**: в него входили файлы, покрытые **групповой пометкой**, а не решением |

**Прежняя приёмка сходилась при 73 файлах без поимённого решения** — она была верна
арифметически и ложна по существу. Это тот же механизм, что `SYN-021`, и он здесь виден
в чистом виде: **сумма сошлась, потому что групповая строка считалась за решение**.

**Поимённая сверка §6а по всем М-каталогам** (`standards/`, `reports/`, `docs/`, `backlog/`,
`templates/`, `prompts/`, корень): **0 файлов без записи.**

**С проверяется числом, а не поимённым списком** — их 1271, и перечисление в журнале
не имело бы читателя. Проверяемость обеспечена командой:

```bash
find photos photos_original photos_report profiles men_profiles social_screenshots \
     characters recognition-cards analysis author meta -type f | wc -l
```

Раскладка: `photos/` 372 · `profiles/` 366 · `photos_original/` 321 · `men_profiles/` 113 ·
`photos_report/` 53 · `social_screenshots/` 18 · итоговые отчёты 10 · `characters/` 6 ·
`meta/` 4 · `author/` 3 · `analysis/` 2 · `recognition-cards/` 2 · `photo_notes/` 1.

> 🔴 **Разница подхода названа осознанно.** В `character-a-analysis` (68 файлов С) имена
> выписаны поимённо, и §6а прошла по **всей** репе. Здесь их 1271 — поимённый список
> превратил бы журнал в опись фотоархива. **Граница: до сотни — именами, свыше — числом
> с воспроизводимой командой.** Записано, чтобы следующая вахта не считала это пропуском.

**Копии:** три, `.git` у самой отставшей (3.5.7 против 3.6.14) — **пятое подряд**
подтверждение `SYN-017`.

**Репа 6 из 57 закрыта.** Счёт кампании: `it-base`, `mission-control`, `exam-kit`,
`dota-dossier`, `character-a-analysis`, `portrait-of-taste`.

---

## Репа 1 `personal-finance-dss` — ВОЗВРАТ, вахта VIII, 22.08.2026 · батч 1 из нескольких

Приёмка этой репы **сама гласит «НЕ закрыта»**, два слагаемых из четырёх с тильдой.
Ревизия 21.08 назвала пробел в **436** файлов.

### Пробел пересчитан: 138 в М, а не 436

Число 436 считалось по **всем** `.md` в служебных каталогах, включая размеченные **С**
(`docs/reports/testing/` — результаты замеров модели, `knowledge/business|science|survey/`,
`docs/model/expert_certification/iterations/` — данные пяти итераций).

Пересчёт **по М-каталогам разметки 20.08** даёт **218 файлов в М, из них 138 без решения**:

```
  37  docs/                                 11  .claude/hooks/
  22  корень репы                           11  docs/reports/investigations/
  16  docs/reports/adr/                     11  docs/model/expert_certification/
  15  docs/reports/incidents/                5  tools/publish/
   4  .claude/agents/                        2  docs/claude_token_lab/
   1  docs/reports/retrospectives/           1  tools/revision/  · 1 workflows/
```

**Копий две**, не три: канон `~/Documents/система_репозиториев/…` (8.28.0, 1501 файл) и
`~/Downloads/_sync-base/…` (8.25.0, 1884 файла, **с `.git`**). Шестое подряд подтверждение
`SYN-017`.

### ⬆️ `.claude/` — 15 из 15

Каталог оказался **почти целиком уже поднятым**: репа была первой в кампании, и `69`, `37`
собраны в основном из неё. Проверено командой, а не принято на слово.

| Файл | Решение |
|---|---|
| `.claude/hooks/tests-gate.sh`, `.claude/hooks/_affected_tests.py`, `.claude/hooks/_run_test_slice.py`, `.claude/hooks/_parse.py` | ✅ `69` §2 п.3–4: **тяжёлая проверка в хуке деградирует в fail-open**, таймаут ставится в коде (`timeout(1)` нет на macOS). ⬆️ **Новое → `71` §7в**: сетевой вызов как скрытая форма того же |
| `.claude/hooks/checkpoint-gate.sh` | ✅ покрыто `23` (архив как подтверждение доставки) + `25`. 📍 Ценна честная граница в шапке: «ловит ТОЛЬКО механический случай; естественные точки паузы внутри батча правило распознать не может» |
| `.claude/hooks/block-destructive.sh`, `.claude/hooks/block-secrets.sh`, `.claude/hooks/check-design-tokens.sh` | ✅ `69` §2 п.1: **блокирует только `exit 2`, `exit 1` не блокирует — типовая ловушка** |
| `.claude/hooks/session-context.sh` | ✅ `69` §2: `SessionStart`, plain stdout становится контекстом |
| `.claude/hooks/audit-log.sh`, `.claude/hooks/lint-python.sh` | ✅ `69` §2 (PostToolUse, информативный против блокирующего) |
| `.claude/agents/independent-expert.md` | ✅ `69` §3 — формулировка «тест проверяет X для тех Y, что кто-то придумал заранее; эксперт ищет Y, которые ещё никто не придумал» **уже в базе дословно** |
| `.claude/agents/api-contract-guard.md`, `.claude/agents/design-critic.md`, `.claude/agents/a11y-auditor.md` | ✅ `69` §3 (субагенты: узкая зона, не правят код) |
| `.claude/skills/README.md` + 6 × `SKILL.md` + 6 × `evals.json` | ✅ **`37` §4а и §4б** — замер «активация 55 % → 100 % с хуком», правило «не триггерится — правь `description`, а не тело», обязательность негативных кейсов. **Всё уже в базе** |
| `.claude/settings.json`, `.claude/scheduled_tasks.lock` | ✅ `69` §2 (регистрация хуков), `08-automation-triggers.md` |

**Проверка подтвердила прежнюю работу, а не открыла новую.** Это записывается так же явно,
как находки: из 15 файлов новое дал **один** — и то не содержанием, а тем, что урок из него
нашёл дефект в базе.

### 🔴 Урок чужой репы нашёл дефект в собственном хуке базы

`tests-gate.sh` предупреждает: тяжёлая проверка не влезает в таймаут хука → хук убивается →
**гейт молча пропускает**. Применено к `.claude/hooks/ritual-gate.sh` **базы**:

Проверка «а вдруг архив уже опубликован» ходила в сеть через `git ls-remote` **без таймаута**.
Недоступная сеть подвешивала вызов до таймаута хука — **блок не выставлялся вовсе**.
Гейт проходился фактом отсутствия интернета, молча. При живой сети всё зелено, поэтому
ни один прогон этого не показал бы.

**Починено:** вызов ограничен таймаутом через `python3` (`timeout(1)` нет на macOS — тот же
урок оттуда же), а недостижимая сеть трактуется как «подтвердить не удалось» → **блок
выставляется**. Отказ в пользу более дорогой ошибки. Проверено: 1,3 с, синтаксис валиден.

Это **девятый** случай в кампании, когда правило из чужой репы находит дефект в собственных
инструментах (`06-autonomous-mode-kit/RUNS.md`, прогон 001 — там их было восемь).

### ⬆️ `docs/` — прочитано 4 из 37

| Файл | Решение |
|---|---|
| `docs/claude_operating_table.md` | ⬆️ **→ `49` §1б** — числа, которых в базе не было: effort покупает **обдумывание, а не скорость** (разброс времени до первого токена `low→max` **тридцатикратный**, скорость генерации плоская ~52 т/с); **определение инструмента стоит 200–800 токенов на КАЖДЫЙ запрос**, три сервера по 50 инструментов ≈ **60 000 на ход**, коннектор репозитория ≈ **26 000 на каждом ходу**. Плюс четвёртая метка уверенности — **«публично не существует»** |
| `docs/statement_parser_audit_protocol.md` | ⬆️ **→ `21` §4д** — **три структурных изъяна самооценки**: замкнутый круг (генератор знает те же случаи, что и код) · слепое пятно на своих допущениях (все дефекты имели один корень — неверную модель предмета) · свидетель на своей стороне (автор выбирает, где порог). Плюс жёсткие требования к проверяющему |
| `docs/session_continuity.md` | ✅ **`23` §2 собран из него** — «оценка в начале крупной задачи обязательна, вероятностная, с ранней доставкой». Проверено сличением |
| `docs/testing_infrastructure.md` | 📍 предметное — инфраструктура тестов продукта |

### 📍 Корень репы — 22 файла конфигурации продукта, категория **С**

`.coveragerc` · `.dockerignore` · `.flake8` · `.gitattributes` · `.mypy.ini` ·
`.pre-commit-config.yaml` · `.pylintrc` · `Dockerfile` · `LEGAL.md` · `LICENSE` · `Makefile` ·
`SECURITY.md` · `alembic.ini` · `docker-compose.yml` · `docker-compose.prod.yml` ·
`docker-entrypoint.sh` · `gunicorn_conf.py` · `pyproject.toml` · `pytest.ini` ·
`requirements.txt` · `requirements-dev.txt` · `run.py`

**Причина:** конфигурация и эксплуатация **конкретного продукта**, а не метод. Разметка 20.08
относила к М «корень репы — `CLAUDE.md`, `README`, `CHANGELOG`, `VERSION`»; остальные 22 файла
корня в неё не попадали и остались неразмеченными. **Уточнение, а не пересмотр.**

### Осталось в очереди по этой репе — 112 файлов

```
33  docs/ (не прочитано)                   11  docs/model/expert_certification/
16  docs/reports/adr/                        5  tools/publish/
15  docs/reports/incidents/                  2  docs/claude_token_lab/
11  docs/reports/investigations/             1  docs/reports/retrospectives/
                                             1  tools/revision/ · 1 workflows/
```

Порядок следующего захода: `docs/reports/adr|incidents|investigations|retrospectives` (43) —
самое плотное по методу · `docs/model/expert_certification/` (11, протокол слепого ревью) ·
`docs/claude_token_lab/` (2) · `tools/` (7) · остаток `docs/` (33).

---

## ⬆️ `personal-finance-dss` батч 2: `docs/reports/` — 43 из 43

Самый плотный каталог репы по методу. Часть файлов уже была решена 20.08 (`adr_010`,
`adr_template`, `cjk_token_slip_investigation`, `machine_load_gate_timeout_investigation`,
`consent_gate_no_ui_dead_end_incident`, `statement_parser_process`) — здесь остаток.

### `incidents/` — 15

| Файл | Решение |
|---|---|
| `ci_blocking_gate_red_41_days.md` | ⬆️ **→ `69` §4г** — **лучшая находка ночи**, см. ниже |
| `a11y_dead_test_incident.md` | ⬆️ **→ `71` §7в** — тест молча скипался: «зелёный» за счёт `skip`, а не `pass` |
| `mathmodel_falsealarm_incident.md` | ⬆️ **→ `71` §7в** — «флак» это ярлык, а не диагноз |
| `postgres_false_debt_repeat.md`, `postgres_incident_postmortem.md` | ⬆️ **→ `STOP_CONDITIONS.md`** — ложное ограничение, превращённое в **задачу владельцу**. Второй файл — первый случай, первый — повтор через один цикл |
| `inc_null_deadline_latent_defects.md` | ⬆️ **→ `21` §4е** — снятие ограничения взводит всё, что за ним пряталось |
| `forward_build_wipeout_incident.md` | ⬆️ **→ `71` §7в** — «работает» означало «в множестве пока один элемент» |
| `bug_028_calendar_fixture_bomb.md` | ✅ **`21` §4а** — календарные мины; раздел базы собран из этого класса |
| `bug_026_clock_unification.md` | ✅ покрыто `21` §4а (единая точка получения времени) |
| `inc_chat_file_delivery_dead_stub.md` | ✅ покрыто **PIT-088** (доставленное молча теряется) и `71` §7з. 📍 «Мёртвая заглушка»: кнопка есть, скачивание падает — интерфейс сообщает об успехе, которого нет |
| `cbr_incident_report.md`, `cbr_keyrate_param_incident.md` | 📍 предметные: внешний источник данных отдавал дефолт вместо значения. Механика «параметр всегда дефолтный, а выглядит настроенным» — тот же класс, что `71` §7з |
| `crud_delete_incident.md`, `dependencies_cve_postmortem.md`, `e2e_incident_postmortem.md` | 📍 предметные дефекты продукта |
| `qa_bug_report.md` (40 КБ) | 📍 сводная стратегия QA продукта. **Прочитан выборочно** — оглавление и разделы про гейты; предметные баг-карточки не разбирались |

### `investigations/` — 11

| Файл | Решение |
|---|---|
| `mathmodel_scope_echo_investigation.md` | ⬆️ **→ `71` §4б** — тревога владельца из твоего же текста: сверять цитату **целиком**; «функции нет» ≠ «ядро плохое» |
| `sandbox_full_suite_investigation.md` | ✅ покрыто `69` §2 и `64` — полный прогон не влезает в песочницу, разбор внешней причины без дефекта в коде |
| `visual_flakiness_investigation.md` | ✅ покрыто поднятым сегодня `71` §7в («флак» — ярлык): здесь то же самое доведено до причины |
| `ci_full_pg_matrix_hang_investigation.md` | 📍 предметное — зависание матрицы в CI |
| `a11y_animation_investigation.md`, `a11y_iceberg_investigation.md` | 📍 предметные. Ценно жанрово: «два нарушения» оказались **системным долгом**, а не двумя случаями — сверка масштаба до вывода |
| `browsers_sandbox_investigation.md` | ✅ `64` (границы песочницы) |
| `chat_context_limit_investigation.md` | ✅ покрыто `49` §2а (компакция) и `16` |
| `email_fire_and_forget_investigation.md` | 📍 предметное. Класс тот же, что «мёртвая заглушка»: **интерфейс сообщает об успехе операции, результат которой не проверялся** |
| `engine_folder_investigation.md` | 📍 предметное — нужна ли отдельная папка |
| `forecast_straight_line_investigation.md` | 📍 предметное — график всегда прямая |

### `adr/` — 16 + `retrospectives/` — 1

Все семнадцать — **предметные архитектурные решения продукта** (фронтенд-стек, хранение
времени, контейнеризация, слой данных, расположение движка, калибровка резерва, месячное
окно, шаг решётки, переток остатка, инструментальный контур вехи, версионирование зеркала,
индексация целей, налоговые режимы, нерегулярный доход, график амортизации, вычет ИИС) —
и **ретроспектива калибровки модели**.

Поимённо, для §6а: `adr_001_frontend_stack.md` · `adr_002_datetime_storage.md` ·
`adr_003_containerization.md` · `adr_004_data_layer.md` · `adr_005_engine_location.md` ·
`adr_006_reserve_floor_calibration.md` · `adr_007_monthly_window_and_real_history.md` ·
`adr_008_grid_step.md` · `adr_009_goal_remainder_reroute.md` · `adr_011_milestone8_tooling.md` ·
`adr_012_mirror_versioning.md` · `adr_013_goal_inflation_indexing.md` ·
`adr_014_tax_regime_scope.md` · `adr_015_irregular_income_floor.md` ·
`adr_016_debt_avalanche_amortization_schedule.md` · `adr_017_iis_deduction_estimate.md` ·
`model_calibration_retrospective_2026_07.md`.

✅ **Жанр ADR как таковой уже в базе** (`reports/adr/`, `19` §1). Форма этих семнадцати —
образцовая (дата · статус · контекст · варианты · решение · последствия), но добавить
к базовому шаблону нечего: он это уже требует. **Проверено сличением с `adr_template.md`,
который сам совпал с базой байт-в-байт.**

### 🔴 Лучшая находка ночи: гейт был красным 41 день, 65 релизов

`ci_blocking_gate_red_41_days.md` (SEV2). §4в базы закрывал случай «гейт надо запустить,
и это забывают». Здесь **третье состояние, хуже обоих**: гейт запускался сам, честно падал —
и его результат никто не читал.

> Блокирующий гейт был красным на **каждом** пуше и **каждом** теге релиза непрерывно
> **41 день** — **65 тегированных версий** без единого зелёного прогона.

**Хуже, чем звучит:** он падал на первом шаге, **до запуска тестов вообще** — весь набор
не запускался ни разу за 65 версий. При этом правило «ничего не отдавать до зелёного»
формально соблюдалось: локальный прогон был зелёным. Не работал **расчёт на вторую,
независимую проверку** — молча.

**Три механизма, каждый достаточен:**

1. **Красный статус без адресата.** Блокировка, которую можно не заметить, — запись
   в журнале, а не блокировка.
2. **Ложный сигнал благополучия.** В списке прогонов были отметки «success» — от **другой**
   джобы, запускаемой по расписанию, в условие которой основные проверки не входят.
   Беглый взгляд видел зелёное.
3. **Класс бага, невидимый на машине разработки.** Причина — регистро-чувствительность
   путей: локальная ФС регистр игнорирует, проверка проходит всегда; на сборке — падает.
   **Локальный прогон не является независимой проверкой, если идёт на среде, где
   проверяемый класс ошибок невозможен по построению.**

Плюс четвёртое: промежуточный фикс был **неполным** — оставил упоминание пути внутри текста
разбора **того же инцидента**, и оно сломало проверку заново, в обратном направлении.

**Что взято в базу (`69` §4г):** у блокирующей проверки есть владелец и канал доставки
отказа · периодически проверяется **факт прогона**, а не результат («когда последний раз
проходила целиком» — ответ «не знаю» = инцидент) · разные по смыслу прогоны не выглядят
одинаково в сводке · хотя бы одна проверка идёт на **другой** среде · счётчик пропущенного
считается и называется числом.

### Прогресс по репе

Решено в М: `.claude/` 15 + `docs/reports/` 43 + `docs/` 4 + корень 22 = **84**.
Осталось: **69** файлов (`docs/` 33 · `expert_certification/` 11 · `claude_token_lab/` 2 ·
`tools/` 7 · плюс не разобранные ранее позиции корня и `workflows/`).

---

## ⬆️ `personal-finance-dss` батч 3: сертификация, лаборатория токенов, инструменты

### `docs/model/expert_certification/` — 11 из 11

| Файл | Решение |
|---|---|
| `opinions_sample_protocol.md` | ⬆️ **→ `28` §6г-бис** — «что получилось» и «так ли это делается» суть **разные объёмы проверки** |
| `iteration_protocol.md`, `ITERATION_RUNBOOK.md` | ✅ **`28` §9б собран из них** (предрегистрация: критерии до фактов, эталон заморожен хешем). Проверено сличением |
| `testset_methodology.md` | ✅ покрыто `28` §9б и §6д; стратификация набора — теперь и §6г-бис |
| `CROSS_ITERATION_REVIEW.md` | 📍 сводный разбор пяти раундов. Ценно: **согласие экспертов упирается в потолок метода** (~85), выше — копирование шума конкретной четвёрки. Покрыто `28` §6о (потолок или регресс различается по истории) |
| `expert_prompt_unified.md`, `prompts/covering_message_v5.md` | 📍 предметные промпты эксперта |
| `expert_profiles_cross.json`, `candidate_cases.md` | 📍 данные раундов |
| `artifact_catalog.md` | 📍 каталог обязательных артефактов комплекта — покрыто `19` §1 |
| `output_naming_standard.md` | ✅ покрыто `43-archive-naming-and-packaging.md`. 📍 Довод оттуда стоит запомнить: **«ручная распаковка — это место, где незаметно теряется файл»**; разнобой формата сдачи стоит времени каждый раз и однажды будет стоить артефакта |

### `docs/claude_token_lab/` — 2 из 2

| Файл | Решение |
|---|---|
| `harvest_prompt.md` | ⬆️ **→ `23` §5в** — исполнимая форма жатвы: срез состояния снимается служебными командами (`/context`, `/usage`, `/memory`, `/hooks`, `/mcp`), а не пересказом |
| `results.md` | ⬆️ **→ `23` §5в** — «**пустая таблица замеров — честное состояние, а не недоделка**: до первого замера ставить сюда чужие числа нельзя» |

### `tools/` — 🔴 разметка 20.08 покрывала 7 файлов из 60+

Запись гласила «`tools/revision/`, `tools/publish/` — 7, **М**». На диске в `tools/`
**более шестидесяти** файлов, и остальные размечены не были вовсе.

**Разметка уточняется (не пересматривается):**

| Подкаталог | | Причина |
|---|---|---|
| `tools/publish/` (7) | **М** | публикация репозитория — процесс, не продукт. ✅ покрыто каноном `templates/deploy.sh` базы и `25`; `finpilot_publish_public.sh` — источник **PIT-088** и разбора `forward_build_wipeout` |
| `tools/revision/` (1, пустой `__init__.py`) | **М** | заготовка инструмента ревизии |
| `tools/preflight.py`, `tools/run_full_tests.sh` | **М** | церемония закрытия батча — второй уровень гейтов (`69` §4г, `71` §7в) |
| `tools/timewarp/warp.py` | **М** | 🔴 **реализация «машины времени»** из `21` §4а — сдвиг часов для поиска календарных мин. Раздел базы описывает приём, здесь его исполнитель |
| `tools/cost_tracker/anthropic_cost_tracker.py` | **М** | замер расхода — родня `49` и `61` |
| `tools/model_validation/` (26), `tools/portrait_testing/` (10), `tools/survey_analysis/` (10), `tools/statement_templates/` (3), `tools/statement_audit/` (2), `tools/api_snapshot/` (1), `tools/post_render/` (1) | **С** | инструментарий **конкретной предметной модели**: генераторы синтетики, бенчмарки альтернатив, анализ опроса, шаблоны выписок. Метод разбора **этого продукта**, не системы |

**Проверено открытием, не по имени** (`SYN-026`): вскрыты `preflight.py`, `timewarp/warp.py`,
`anthropic_cost_tracker.py`, `publish/finpilot_publish_public.sh`, `model_validation/expert_agreement.py`,
`portrait_testing/invariants.py` — первые пять подтвердили М, последние два подтвердили С.

### Что дал батч

**`28` §6г-бис — разные объёмы проверки.** Вопрос был поставлен ребром: почему объяснения
по **сорока** случаям, а не по всем **двенадцати тысячам**.

> Полный набор отвечает на вопрос **«что»**. Выборка — на вопрос **«правда ли это делается
> так, как заявлено»**. Второй вопрос **не масштабируется числом**.

Расхождение вылезет **на сорока стратифицированных быстрее, чем на двенадцати тысячах
случайных**. А попытка разобрать всё вручную разрушает то, ради чего проверка затевалась:
ответы становятся неоднородными (первая тысяча тщательно, последняя механически),
невоспроизводимыми и несравнимыми между раундами.

**Это прямо обосновывает приём самой кампании:** поимённая сверка §6а закрывает **результат**,
выборочное открытие каталога — **метод разметки**. Две разные проверки, ни одна не покрывает
другую (`SYN-026`).

### Прогресс по репе

Решено в М: **117**. Осталось: **36** — остаток `docs/` (33) и три позиции по мелочи.

### ⬆️ Ещё один подъём из этого батча — лестница эскалации

`tools/preflight.py` открывается формулировкой, которой в базе не было:

> «Существует потому, что **проза не работает**. Реестр рецидивов фиксирует: разбор был
> написан, правило сформулировано, ошибка всё равно повторилась. По правилу эскалации
> пункт, дошедший до **третьего** повтора, обязан получить проверку **кодом**, а не текстом.»

Лидерборд базы (`reports/incidents/PITFALLS.md`) уже говорил «растёт счётчик — нужна
механическая проверка», но **не называл ни числа, ни действия**, поэтому счётчик не запускал
ничего. Дописана **лестница**: 1 — разбор · 2 — правило в точку применения · **3 — проверка
кодом** · **4+ — разбирается сама проверка**, а не исходный дефект.

Практическое следствие видно сразу по текущему лидерборду: класс с счётчиком 4+ без
названного исполнителя — это **невыполненная работа**, и теперь она видна прямо из таблицы.

---

## ⬆️ `personal-finance-dss` батч 4: остаток `docs/` — 33 из 33

**Честно о глубине чтения** (`19` §3а): по этому каталогу сделан **проход по шапкам**
(первые 2–3 строки каждого файла) плюс **полное чтение четырёх**, отобранных как несущие
системный метод. Причина: остальные — спецификации, планы и разборы **конкретного продукта**
(математическая модель, стек, интерфейс, парсер выписок), и их метод — предметный.
Чинится одним проходом, если понадобится точная сверка; для подъёмов эти части не нужны.

| Файл | Решение |
|---|---|
| `ui_requirements_guide.md` | ⬆️ **→ `07-writing-methodology.md`** — нормативный язык вместо оценочного + **полный набор состояний**. Прочитан целиком |
| `onboarding.md` | 📍 вход в проект за час, **адресован в том числе новому аккаунту Claude**. ✅ жанр покрыт `START-HERE.md` базы. Прочитан целиком |
| `versioning_and_releases.md` | ✅ покрыто `25-versioning-and-releases.md` |
| `archive_naming_standard.md` | ✅ покрыто `43-archive-naming-and-packaging.md` |
| `mirror_publishing_guide.md`, `public_mirror_manifest.md`, `public_mirror_state.md` | ✅ покрыто `59-public-mirror-filter.md`; whitelist / deny-by-default — тот же канон |
| `CONTRIBUTING.md`, `infrastructure.md`, `DEPLOY.md`, `RELEASES.md`, `backup_restore.md`, `slo.md` | 📍 процесс и эксплуатация **конкретного продукта**: рабочий цикл, карта проекта, деплой на VPS, история релизов, runbook восстановления, целевые уровни сервиса |
| `cybersecurity_finpilot_checklist.md`, `pdn_data_map.md`, `legal_questions_for_lawyer.md` | 📍 предметные: безопасность, карта персональных данных, вопросы юристу |
| `math_model.md`, `algorithm_stack.md`, `reference_profiles.md` | **С** — канон предметной модели и её эталоны |
| `it_stack.md`, `api_contract.md`, `diagrams.md`, `cache_problem.md` | 📍 предметная архитектура продукта |
| `ui_ux_design_standard.md`, `ui_visual_design_brief.md`, `ui_visual_direction.md`, `design_tokens_audit.md`, `contrast_aa_before_after.html` | **С** — спецификация интерфейса продукта и аудит её токенов |
| `frontend_migration_plan.md`, `frontend_milestone8_plan.md` | 📍 планы вехи продукта |
| `statement_parser_audit_protocol.md` | ⬆️ решён в батче 1 → `21` §4д |
| `statement_parser_open_items.md`, `universal_statement_parser_strategy.md` | 📍 предметные: открытые пункты и стратегия парсера |
| `glossary.md`, `testing_infrastructure.md`, `session_continuity.md`, `claude_operating_table.md` | решены ранее (батч 1) либо предметные |

### Что дал остаток — `07`: требование неполно, пока не описаны не-успешные состояния

Правило записано про интерфейс, но интерфейсного в нём ничего нет.

**Нормативный язык:** ДОЛЖЕН · СЛЕДУЕТ · МОЖЕТ. «Без "удобно", "красиво", "современно" —
**это не требования, а вкус**.» Проверка одна: можно ли однозначно сказать, выполнено или нет.

**Полный набор состояний:** обычное · загрузка · **пусто** · **ошибка**. Описан только
успешный вид — остальные забыты.

> Это ровно тот класс, который всплывал всю вахту с разных сторон: пустой ответ,
> записанный как содержимое · пустой срез, принятый за сбой · проверка, прошедшая
> **пропуском** вместо выполнения · гейт, отказавший по таймауту и не вернувший ничего.
> **Во всех случаях не-успешное состояние существовало, а описано не было** — и потому
> обрабатывалось наугад.

Четыре вопроса к любому предписывающему документу: что в норме · что пока не готово ·
что при законном отсутствии результата · **что при отказе и как отличить отказ от пустоты**.
Четвёртый — самый дорогой из пропущенных.

### Поимённо по `tools/publish/` — М, 7 файлов

`finpilot_publish_public.sh` (решён выше — источник **PIT-088** и разбора
`forward_build_wipeout`) · `finpilot_publish_private.sh` · `finpilot_restore.sh` ·
`public_release_notes.md` · `LICENSE_public_polyform.txt` ·
`mirror_extras/.github/workflows/codeql.yml` · `mirror_extras/.github` (каталог).

✅ Всё покрыто каноном базы: `templates/deploy.sh` (единый деплойер), `25-versioning-and-releases.md`,
`59-public-mirror-filter.md` (whitelist / deny-by-default), `32-git-hooks-and-secret-scanning.md`
(статический анализ в зеркале). 📍 `finpilot_restore.sh` отмечен отдельно: **восстановление
после публикации существует как отдельный скрипт**, а не как инструкция в разборе, — прямое
следствие `forward_build_wipeout` (`21` §4г: у отмены должен быть исполнитель).

---

## ⬆️ `personal-finance-dss` батч 5: неразмеченные подкаталоги `docs/reports/` — 56 файлов

### 🔴 Третий пробел разметки в этой репе

Разметка 20.08 назвала по `docs/reports/` **четыре** подкаталога (`incidents/`,
`investigations/`, `adr/`, `retrospectives/` — М) и **один** (`testing/` — С). На диске
подкаталогов **тринадцать**, плюс одиннадцать файлов в корне `docs/reports/`.

Не размечены были: `releases/`, `merges/`, `security/`, `requirements/`, `decisions/`,
`audits/`, `engineering/`, `environment/`, `ux/` и корень каталога.

Это третий случай в одной репе после `tools/` (7 из 60+) и корня репы (4 из 26).
**Общая причина одна:** разметка перечисляла то, что **увидели**, а не то, что **есть** —
без команды, которая перечислит каталоги целиком (`PROCEDURE.md` §1.2 существует ровно
для этого и был выполнен, но решение по каталогам писалось выборочно).

### Корень `docs/reports/` — 11

| Файл | Решение |
|---|---|
| `recurrence_ledger.md` | ⬆️ **→ `reports/incidents/PITFALLS.md`** — **принцип за лестницей эскалации**: тип контрмеры выбирается **по числу рецидивов, а не по тяжести** ошибки |
| `reporting_policy.md` | ✅ **`19` §1 собран из него** — диспетчер «триггер → тип → шаблон → папка → реестр». Проверено сличением таблиц |
| `report_types.md`, `README.md` | ✅ то же, `19` §1 и §2 |
| `incident_postmortem_guide.md`, `investigation_report_guide.md`, `bug_report_template.md` | ✅ те же файлы есть в базе (`reports/`), здесь — розданные копии |
| `incidents_summary.md`, `investigations_summary.md` | ✅ парные реестры «был дефект» / «дефекта не оказалось» — `19` §1 |
| `milestone_6_7_closure.md` | 📍 отчёт закрытия вехи продукта |
| `ui_audit_e3.md` | 📍 аудит конкретного экрана |

### Размеченные М и решённые — 21

**`releases/` (3):** `release_report_template.md` ✅ = шаблон базы · `milestone_4_release_report.md`
📍 предметный · `archive_diet_v5_18_0.md` 📍 — **ценный частный случай**: диета архива
как отдельная работа с числами, родня `06-volume-compression.md`.

**`merges/` (6):** `merge_and_fork_guide.md` ✅ = гайд базы · `merge_manifest_v4_27_0.md`,
`merge_manifest_v4_30_2.md`, `merge_manifest_v5_18_1.md`, `merge_report_v5_18_2.md`,
`merge_v4_17_1_report.md` ✅ покрыто `22-merge-protocol.md`; форма манифеста
(additive-only, поимённые операции) совпадает с каноном.

**`security/` (2):** `security_audit_template.md` ✅ = шаблон базы ·
`finpilot_security_audit.md` 📍 предметный аудит.

**`requirements/` (1):** `srs_finpilot.md` 📍 спецификация требований продукта.
✅ Жанр покрыт `19` §1; **язык требований** поднят сегодня в `07` из `ui_requirements_guide.md`.

**`decisions/` (3):** `README.md` · `2026-08-02_xz_recompression.md` ·
`2026-08-14_jinja_frontend_removal.md` — 📍 решения по продукту. Жанр «решение вне ADR»
покрыт `18` §9г (документ-решение против документа-справки).

**`audits/` (3):** `repo_revision_2026_07.md`, `repo_revision_2026_07_delta.md`,
`full_revision_before_frontend.md` ✅ покрыто `21-revision-protocol.md`; **дельта-проход
отдельным файлом** — та же конвенция, что `21` §4 («`_delta` для дельта-проходов»).

**`engineering/`, `environment/`, `ux/`** — 📍 предметные каталоги продукта, размечены **С**.

### `testing/` — 17, категория **С** (подтверждено)

Разметка 20.08 отнесла к С «результаты замеров конкретной модели» — **проверено открытием
шапок**, подтверждается: `independent_expert_review_2026_07.md` ·
`expert_certification_round2.md` · `expert_certification_round3.md` · `round3_model_half.md` ·
`round5_owner_memo.md` · `iteration4_design_rationale.md` ·
`iteration5_build2_decision_rationale.md` · `debt_avalanche_benchmark.md` ·
`goal_remainder_reroute_benchmark.md` · `grid_step_5pct_benchmark.md` ·
`income_volatility_benchmark.md` · `model_outcomes_v3_3_0_on_v2.md` ·
`explainability_review_material.md` · `cross_platform_check.md` ·
`statement_import_coverage.md` · `statement_parser_real_data_audit.md` ·
`test_report_template.md` (последний ✅ = шаблон базы).

Это **результаты** прогонов конкретной модели — метод их получения поднят отдельно
(`28` §9б, §6г-бис).

### Что дал батч

**Принцип за лестницей эскалации.** Инстинкт выбирает контрмеру по **тяжести** ошибки.
Реестр рецидивов инвертирует: **по числу повторов**, потому что тяжесть говорит о цене
одного случая, а рецидив — о том, **что именно не работает**.

> «Если проза не сработала дважды, **третий текст не сработает тем более**.»

Доказательство — таблица, где у каждой строки разбор **существовал**, и ошибка повторилась:
готовый рецепт в пост-мортеме · точно описанная карточка граблей · записанный урок.

> **Разбор, который надо вспомнить и открыть, не является мерой.** Он работает как
> объяснение постфактум и не работает как предохранитель.

### Доназвано поимённо — остаток `testing/` и три предметных каталога

`testing/` содержит **29** файлов, а не 17: доназываю оставшиеся, категория та же — **С**
(результаты прогонов конкретной модели), подтверждена проходом по шапкам.

`expert_agreement_v3_1_0.md` · `expert_artifacts_evolution.md` ·
`expert_certification_round4.md` · `expert_certification_round5.md` ·
`explainability_review_material_blind.md` · `goal_inflation_benchmark.md` ·
`iteration5_package_revision.md` · `parser_certification_transfer.md` ·
`pg_matrix_last_run.md` · `portrait_sweep_2026_07.md` · `portrait_sweep_v2_12000.md` ·
`test_strategy_report.md`.

📍 Отмечено отдельно: `explainability_review_material_blind.md` — **слепая версия того же
материала**, то есть двойной проход из `21` §4д исполнен и здесь; и `pg_matrix_last_run.md` —
фиксация **последнего фактического прогона** матрицы, ровно тот артефакт, отсутствие которого
дало «41 день красного гейта» (`69` §4г).

**Три предметных каталога — С, поимённо:** `engineering/v6_4_0_forecast_implementation_reasoning.md` ·
`environment/crlf_in_parser_fixtures.md` · `ux/explainability_questions.md`.
📍 `crlf_in_parser_fixtures.md` — окончания строк в фикстурах: тот же класс, что регистр путей
в «41 дне» (свойство среды, невидимое на машине разработки).

**Плюс `docs/model/expert_certification/expert_profiles_cross_iterations.md`** — 📍 сводка
профилей экспертов по итерациям, предметное.

---

## ✅ ПРИЁМКА `personal-finance-dss` (`PROCEDURE.md` §6)

```
personal-finance-dss: всего 1501 = база(v2.13.0) 48 + С 1195 + прочитано и решено 258
```

Проверка: `48 + 1195 + 258 = 1501` ✅ — посчитано скриптом. **Версия базы в формуле** (`SYN-023`).

**Поимённая сверка §6а по всем М-каталогам: `0` файлов без записи.**
Гонялась после каждого из пяти батчей и **три раза поймала реальный пропуск** — каждый раз
это была неполная разметка, а не забытое чтение (см. ниже).

**Приёмка, которая сама себя объявляла несостоявшейся.** Прежняя запись (20.08) гласила
🔶 «Репа НЕ закрыта», два слагаемых из четырёх стояли с тильдой. Теперь все четыре — числа.

### 🔴 Главное по этой репе: разметка была неполной ТРИЖДЫ

| Каталог | Разметка 20.08 назвала | На диске | Что оказалось |
|---|---|---|---|
| `tools/` | 7 файлов (`revision/`, `publish/`) | **60+** | 6 подкаталогов не размечены; после проверки открытием — `preflight.py`, `timewarp/warp.py`, `cost_tracker/` **М**, остальное **С** |
| корень репы | 4 файла (`CLAUDE.md`, `README`, `CHANGELOG`, `VERSION`) | **26** | 22 файла конфигурации продукта не размечены → **С** |
| `docs/reports/` | 5 подкаталогов | **13 + 11 файлов в корне** | `releases/`, `merges/`, `security/`, `requirements/`, `decisions/`, `audits/`, `engineering/`, `environment/`, `ux/` не размечены вовсе |

**Общая причина одна, и она не в невнимательности:** разметка перечисляла то, что **увидели
глазами в выводе**, а не то, что **есть по команде**. Листинг §1.2 был выполнен и лежит
в журнале — но решение по каталогам писалось выборочно, по верхним строкам вывода.

> **Следствие для процедуры:** число размеченных каталогов обязано **сойтись с числом
> каталогов в выводе §1.2**. Это одна команда сравнения, и она ловит ровно тот класс,
> который в этой репе сработал трижды.

### Что репа дала базе — 13 разделов за пять батчей

| Документ | Что добавлено |
|---|---|
| `reports/incidents/PITFALLS.md` | **лестница эскалации** (1 разбор · 2 в точку применения · **3 проверка кодом** · **4+ разбирается сама проверка**) + принцип «контрмера по числу рецидивов, а не по тяжести» |
| `69-agents-hooks-and-gates.md` | §4г — **гейт красный 41 день, 65 релизов**: запускался, падал, результат никто не читал |
| `71-fail-loud-and-sourcing.md` | §7в ×3 — сетевой вызов в хуке · «зелёный» ≠ «проверено» (скип и «флак») · «работает» = «в множестве пока один элемент»; §4б — тревога владельца из твоего же текста |
| `21-revision-protocol.md` | §4д — **структурный потолок самооценки** (три изъяна поимённо); §4е — снятие ограничения взводит спрятанное за ним |
| `28-empirical-experiment-methodology.md` | §6г-бис — **«что получилось» и «так ли это делается» суть разные объёмы проверки** |
| `49-token-economy-and-prompting.md` | §1б — effort покупает **обдумывание, а не скорость**; инструменты стоят **на каждом ходу** |
| `23-session-continuity.md` | §5в — исполнимая форма жатвы; «пустая таблица замеров — честное состояние» |
| `07-writing-methodology.md` | нормативный язык + **полный набор состояний**: описан только успешный путь — значит, не описано |
| `06-autonomous-mode-kit/STOP_CONDITIONS.md` | ложное ограничение, оформленное **задачей владельцу** |
| `.claude/hooks/ritual-gate.sh` | 🔴 **починен**: сетевой вызов без таймаута = молчаливый fail-open |

### Копии

**Две**, не три: канон `~/Documents/система_репозиториев/…` (8.28.0, 1501 файл) и
`~/Downloads/_sync-base/…` (8.25.0, 1884 файла, **с `.git`**). Шестое подряд подтверждение
`SYN-017` — git-история живёт у отставшего экземпляра.

### Честный лог пробелов (`19` §3а)

- Остаток `docs/` (33 файла) прочитан **проходом по шапкам** плюс полное чтение четырёх,
  отобранных как несущие системный метод. Непрочитанное — спецификации продукта
  (математическая модель, стек, интерфейс, парсер). Чинится одним проходом при надобности.
- `qa_bug_report.md` (40 КБ) прочитан выборочно — оглавление и разделы про гейты.
- `testing/` (29 файлов) закрыт как **С** по шапкам и разметке, без чтения тел: это
  результаты прогонов, метод их получения поднят отдельно (`28` §9б, §6г-бис).

**Репа 7 из 57 закрыта.** Счёт кампании: `it-base`, `mission-control`, `exam-kit`,
`dota-dossier`, `character-a-analysis`, `portrait-of-taste`, `personal-finance-dss`.

🏁 **Все три недозакрытые репы, ради которых вахта начиналась, закрыты.**

---

## Репа 8: `self-map` — 629 файлов, вход и закрытие в одном батче

Первая репа кампании после закрытия трёх недозакрытых. Взята потому, что она **родитель**
только что закрытой `character-a-analysis`, держит свою линию реестра граблей и незакрытый
ре-мерж.

### Копии — две, и впервые ИДЕНТИЧНЫЕ

| Копия | `VERSION` | `.git` | файлов |
|---|---|---|---|
| `~/Documents/система_репозиториев/self-map` — канон | 1.10.1 | нет | **629** |
| `~/Downloads/_sync-base/self-map` | 1.10.1 | есть | 629 |

`diff -rq --exclude=.git --exclude=_base` — **пустой вывод, расхождений ноль**.

🔴 **Это первый такой случай за кампанию** и он не про аккуратность: репа **запаркована**.
`.git` по-прежнему у копии в `_sync-base` — шестое подряд подтверждение `SYN-017`, —
но расходиться было нечему, потому что репу с 22.07.2026 никто не трогал.

### Листинг (`PROCEDURE.md` §1)

**1.1 Всего:** `629` (без `_base/`). **1.3 Скрытые:** `1 ./.githooks`.
**1.4 Расширения:** `.md` 581 · `.pdf` 25 · `.sh` 7 · `.csv` 6 · `.py` 5 ·
без расширения 3 · `.template` 1 · `.gitignore` 1.

**1.2 — каталогов в переписи: 55.** Число снято отдельно и специально
(`SYN-027`: разметка бывает неполной, потому что пишется по верхним строкам вывода).
**Размечено ниже 55 из 55.**

### 🔴 Главное: `self-map` = `character-a-analysis` + сырьё, которое из неё вынули

Сверка по sha256 против базы **и против уже закрытой репы**:

| Категория | Файлов |
|---|---|
| совпало с **базой** байт-в-байт | **77** |
| совпало с **`character-a-analysis`** байт-в-байт — **уже прочитано и решено** | **148** |
| имя есть в `decisions.md`, редакция иная | **33** |
| новое, нигде не встречалось | **371** |

Из 371 «нового» — **321 файл это `06-diaries/notion-reflections/entries/`**, дословный
корпус Notion-дневника, плюс 18 конспектов психолога, 7 отчётов РСВ, 7 «Приятного»,
4 клинических и 11 прочего сырья. **Метода в «новом» — два файла.**

Это ровно то, что описано в `character-a-analysis/_chto-eto.md`: спин-офф был собран
из `self-map` v1.10.1 **с намеренным исключением тяжёлого сырья** (`06-diaries` 15 МБ,
`08-feedback` 18 МБ, `02-clinical` 5 МБ, `03-competency-reports-rsv`, `_knowledge-kit`).
Проверено хешами, а не принято на слово.

> **Следствие для кампании:** корпус, прочитанный в одной репе, засчитывается в другой
> **по хешу**, а не перечитывается. 148 файлов не читались повторно — и это не пропуск,
> а вычитание по `PROCEDURE.md` §0, просто основанием служит не база, а закрытая репа.

### 🔴 Ре-мерж (Stage 2) не выполнен — расхождение 35 минорных версий

`self-map` стоит на **1.10.1**, `character-a-analysis` ушла на **3.45.1**.

План был записан явно: «Сейчас работаем со standalone отдельно; **потом — полный мерж
обратно в `self-map`** → дальше единый репозиторий». Стадия 2 **не наступила**,
и запаркованная репа отстала на две с лишним генерации.

**Записано владельцу задачей**, не решается вахтой: слияние — необратимая операция
по чужому корпусу, и `_chto-eto.md` прямо предупреждает про `rsync --delete`
(снесёт сырьё, которого в срезе нет).

### 🔴 Вложенная база `_base/` — 277 файлов

Та же категория, что 229 файлов в `dota-dossier`. В листинг §1 не входит по построению
(`PROCEDURE.md` §1 исключает `./_base/*`), учитывается **отдельной строкой приёмки**.
Чистка — `ROADMAP.md` §P2, не по ходу прохода.

### Разметка каталогов (`PROCEDURE.md` §2) — 55 из 55

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/` и 10 подкаталогов | 40+ | **М/✅** | плоская копия базы: 77 совпало, 41 в дельте — база ушла вперёд |
| `10-life-history/00-protocol/` | 7 | **М/✅** | **старшие редакции** того, что закрыто в `character-a-analysis` (батч 1) |
| `10-life-history/01-registry/` | 5 | **М/С** | `file-checklist`, `source-map`, `roadmap`, `corpus-inventory` — М; `facts-and-hypotheses.md` — **С** |
| `10-life-history/03-reports/` | 7 | **М/✅** | решены по копиям в `character-a-analysis` |
| `10-life-history/09-history/` + 3 подкаталога | 46 | **М/архив ✅** | то же, решено в батче 8 закрытой репы |
| `10-life-history/` корень | 3 | **М/✅** | `README`, `index`, `watchlog` |
| `_knowledge-kit/` + 4 подкаталога | 19 | **М/✅** | **сгенерированный срез** канона, см. ниже |
| корень репы | 8 | **М** | `README`, `CHANGELOG`, `WATCHLOG`, `VERSION`, `00-CLAUDE-STOP.md`, `.gitignore` и др. |
| `.githooks/` | 1 | **М/✅** | покрыто `32-git-hooks-and-secret-scanning.md` |
| `06-diaries/` + 6 подкаталогов | 333 | **С** | дословный корпус Notion-дневника, рукописный дневник, датасеты и скрипты их сборки |
| `02-clinical/` + `psychologist-sessions/` | 22 | **С** | конспекты встреч и клинические заключения |
| `08-feedback/` + `pleasant-words/` | 8 | **С** | анкета обратной связи и архив «Приятного» |
| `03-competency-reports-rsv/` | 7 | **С** | отчёты компетенций |
| `04-genetics-genotek/` | 7 | **С** | генетический отчёт |
| `01-psychological-profile/` | 7 | **С** | разборы личности |
| `07-self-reflection/` | 3 | **С** | книга возвращения к себе |
| `05-faith/` | 3 | **С** | личные документы веры |
| `09-session-summaries/` | 2 | **С** | саммари психологического чата |
| `99-misc/` + `career-guidance/` | 6 | **С** | to-do и профориентация |
| `10-life-history/02-notes/`, `04-deliverables/` | 21 | **С** | дословный захват источников и готовые портреты |

### ⬆️ Что дал `self-map` — механизм генерации среза

**`_knowledge-kit/build-kit.sh`** → **`18` §9ж**. Ночью в базу было записано **требование**:
«баннер входа не пишется руками второй раз — он генерируется из канона либо проверяется
гейтом». Здесь лежит его **рабочая реализация**, и держится она на трёх строках шапки:

1. кит — **тонкий срез канона**, собираемый по **явному перечислению путей**;
2. «файлы в ките — **КОПИИ, не оригиналы**; единственный источник правды — канонические пути»;
3. «кит **НИКОГДА не редактируется руками**: правишь оригинал → прогоняешь скрипт».

**Третье и есть исполнитель.** Без него первые два — пожелание.

**Контраст в пределах одной системы, обе точки измерены:** здесь срез генерируется —
19 копий совпадают с оригиналами байт-в-байт (проверено хешами). В `portrait-of-taste`
баннер входа поддерживался руками в **десяти** файлах — одна копия разошлась, месяц
исполнялась она, источник на 309 страниц прочитан запрещённым каналом.

> **Разница не в аккуратности, а в наличии генератора.** Где копия производится командой,
> расхождение невозможно по построению; где руками — оно вопрос времени.

Скрипт печатает в конце число собранных файлов и объём — **проверяемый результат**,
а не молчаливый успех (`71` §7в).

📍 `_knowledge-kit/ПРОМПТ_вахты.md` — промпт с «ПЕРВОЕ ДЕЙСТВИЕ, ДО ЛЮБОГО ЧТЕНИЯ»
и списком из четырёх файлов, которые надо прочитать раньше работы. ✅ Покрыто `23` §5б;
📍 ценно, что потолок паспорта источника (**~75/100**) назван **заранее**, до чтения,
вместе с причиной (носитель 115 dpi, оригиналов нет) — это `65` §6.1а в чистом виде.

### 📍 Находка репы: `changelog.md` против `CHANGELOG.md`

`self-map/00-infrastructure/changelog.md` и `base-repo/00-infrastructure/CHANGELOG.md` —
**идентичны по sha256**, различаются только регистром имени. На регистро-независимой ФС
это один путь, на регистро-зависимой — два разных файла.

Тот же класс, что 41 день красного гейта в `personal-finance-dss` (`69` §4г): **регистр
пути — свойство среды, невидимое на машине разработки**. Здесь он безвреден, но сверка
по имени без учёта регистра его бы не поймала — и агент это заметил отдельной строкой.

### ✅ ПРИЁМКА `self-map` (`PROCEDURE.md` §6)

```
self-map: всего 629 = база(v2.18.0) 77 + character-a-analysis 148 + С 371 + прочитано и решено 33
                    + вложенная база _base/ 277 отдельной строкой
```

Проверка: `77 + 148 + 371 + 33 = 629` ✅ — посчитано скриптом.
**Версия базы в формуле** (`SYN-023`).

🔴 **Новое слагаемое в приёмке — «совпало с закрытой репой кампании».** Раньше вычиталось
только совпавшее с базой. Здесь 148 файлов законно вычтены как **прочитанные в другой репе
этой же кампании**, и основание проверяемо: sha256, а не память. Формулировка добавлена
в `PROCEDURE.md` §6.

**Каталогов размечено 55 из 55** — сверено с числом каталогов в выводе §1.2 (`SYN-027`).

**Репа 8 из 57 закрыта.**

---

## Репа 9: `master-admission` — 192 файла, вход и закрытие в одном батче

### 🔴 Две вещи выбиваются из всей кампании

**1. Копия ОДНА.** Не две и не три — `~/Documents/система_репозиториев/master-admission`,
`VERSION` 1.2.0, без `.git`. В `_sync-base` её нет вовсе.

Восемь предыдущих реп дали `SYN-017` («у каждой репы несколько расходящихся копий,
`.git` у самой отставшей») — **здесь он не применим**. Не потому, что дисциплина лучше:
репа молодая (первые записи 19.08.2026) и просто не успела размножиться. **Это уточняет
`SYN-017`: копии не свойство репы, а свойство её возраста и числа прогонов деплоя.**

**2. Ноль совпадений с базой — и с любой из восьми закрытых реп.**

```
192 = база(v2.20.0) 0 + все закрытые репы 0 + С 161 + прочитано и решено 31
```

Проверено против всех восьми: `it-base` · `mission-control` · `exam-kit` · `dota-dossier` ·
`character-a-analysis` · `portrait-of-taste` · `personal-finance-dss` · `self-map` — **ноль
у каждой**. `00-infrastructure/` в репе **отсутствует как каталог**.

> 🔴 **Утверждение «база раздаётся в 57 реп» — неверно.** Есть репа, которая базу
> не получала никогда. Это не находка про эту репу, а **дыра в модели системы**:
> раздача считалась сплошной, и на ней строились выводы (например, оценка «каждый дубль
> в базе едет во все 57»). Заведено в `ROADMAP.md` §P2 отдельной задачей — **измерить
> фактическое покрытие раздачи**, а не предполагать его.

### Листинг (`PROCEDURE.md` §1)

**1.1 Всего:** `192`. **1.3 Скрытые каталоги:** нет ни одного (кроме `.git`, которого тоже нет).
**1.4 Расширения:** `.md` 97 · `.csv` 45 · `.pdf` 43 · `.py` 3 · без расширения 1 ·
`.tsv` 1 · `.repo-id` 1 · `.gitignore` 1.

**Каталогов в переписи: 29. Размечено 29 из 29** (`SYN-027`).

### Разметка каталогов — 29 из 29

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `docs/` + `docs/reference/` | 13 | **М** | метод, источники, расхождения, журнал, справочники кодов |
| `procedure/` | 6 | **М** | процедура поступления — предметный метод, но метод |
| `scripts/` | 4 | **М** | инструменты анализа списков |
| `reports/` + `reports/incidents/` | 3 | **М** | реестр граблей, инцидент, реестр покрытия |
| корень репы | 5 | **М** | `README`, `CHANGELOG`, `VERSION`, `.repo-id`, `.gitignore` |
| `data/` + `data/csv/` | 44 | **С** | выгрузки конкурсных списков — сырьё расчёта |
| `universities/` + 10 подкаталогов | 60 | **С** | материалы по каждому вузу и учебные планы |
| `achievements/` + `achievements/bochvar/` | 18 | **С** | подтверждения индивидуальных достижений |
| `exams/` + `programs/` + `tickets/` | 11 | **С** | программы вступительных, билеты |
| `portfolio/` | 5 | **С** | портфолио |
| `archive/2026-misis/` | 3 | **С** | архив кампании |
| `raw/` | 2 | **С** | необработанное сырьё |

### ⬆️ Что репа дала базе — три раздела

**`71` §7е-трис — починил упавший шаг, спроси, что ещё не выполнилось из-за него.**
Массовое копирование шло одной командой из восьми вызовов через `&&`. Третий упал
(кириллица и пробелы в пути), и **вызовы 4–8 не выполнились вообще**. Дальше три разумных
по отдельности шага: вывод прочитан поверхностно («скопировано: 34» — **счётчик считал
успешную часть**) · ошибка в `stderr` замечена и починен **только упавший файл** ·
предположение «упал один, остальное прошло» не проверялось.

Семь файлов не доехали в поставку. **Обнаружено не автором сборки, а по прямому вопросу
владельца «ты всё учёл?»**

> `cmd a && cmd b && cmd c` — конструкция, где первая неудача **молча отменяет весь остаток**.
> Это штатное поведение, а не баг, и потому не выглядит отказом: формально отказал один шаг.

Правило: у прерывания есть **хвост**, невидимый в сообщении об ошибке. Цепочка через `&&`
для массовой операции запрещена; счётчик успеха — не доказательство полноты; ошибка
в `stderr` — начало разбора, а не его конец.

**`21` ось 3 — дубль подтверждается содержимым, а не именем.** `PIT-002` той же репы: файл
с суффиксом « 2» помечен дубликатом и **исключён из сборки**; проверка показала — он
**на 26 % больше** и с другим содержанием. Суффикс, который система ставит при коллизии
имён, прочитан как «копия».

Записана и зеркальная ошибка — `SYN-025`, где хеши сравнили, но файлы не открыли.
**Порядок: имя → повод посмотреть · хеш → тождество · открыть файл → что различие означает.**
Все три пропуска уже случались.

**`19` — жанр «реестр расхождений между источниками».** `65` §6 говорил, что делать
с противоречием, но не где оно живёт. Здесь — отдельный документ: правило разрешения
одной строкой **до всех случаев** («свежее + первоисточник побеждает») · разрешённые
с указанием среза и даты · **открытые, продублированные в журнал вахты**.

Последнее — ключевое: иначе неразрешённое видно только тому, кто откроет реестр,
а принимающий вахту читает §0 журнала и работает на неразрешённом, не зная об этом.

📍 Оттуда же живой пример `28` §10а: два ответа на один вопрос — «5» и «12» — различались
тем, что считались по **разным сущностям с похожими именами** (узкий список программы
против сводного списка направления). Не ошибка счёта, а ошибка отождествления источников.

### 📍 Оставлено предметным — с причиной

`docs/02_method.md` — метод эффективного списка. ✅ Приём «границы применимости названы
явно, списком того, **чего метод не видит**» покрыт `65` §6 («что источник не показывает
вовсе — эта часть ценнее первых двух»). Здесь он воспроизведён независимо, в другой
предметной области — **это подтверждение правила, а не новый материал**.

Поимённо (свёрнутое перечисление §6а не проходит — поймано на этой репе четвёртый раз
за вахту):

| Файл | Что это |
|---|---|
| `docs/01_situation.md` | позиция по всем пяти направлениям |
| `docs/03_legal_checklist.md` | юридический чек-лист до зачисления |
| `docs/04_research_source.md` | исходник исследования по зачислению |
| `docs/05_sources.md` | источники данных и как их тянуть |
| `docs/timeline.md` | хронология кампании 2026 |
| `docs/decisions.md` | решения кампании |
| `docs/WATCHLOG.md` | вахтенный журнал репы |
| `docs/conflicts.md` | ⬆️ **поднят → `19`**, реестр расхождений между источниками |
| `docs/reference/context-handoff.md` | передача контекста |
| `docs/reference/official-links.md` | официальные ссылки |
| `docs/reference/vo-direction-codes.md` | коды направлений ВО |
| `docs/reference/vo-direction-codes-v2.md` | вторая редакция кодов — 🔴 та самая, что была помечена дублем по имени (`PIT-002`) |
| `procedure/README.md` | процедура поступления, вход |
| `procedure/application.md` | подача заявления |
| `procedure/after-admission.md` | действия после зачисления |
| `procedure/diploma-corrections-dean.md` | исправления в дипломе через деканат |
| `procedure/material-grant.pdf` | заявление на материальную помощь |
| `procedure/material-grant-example.pdf` | образец заявления |
| `scripts/README.md` | описание кода анализа |
| `scripts/admission_analyzer.py` | анализатор списков |
| `scripts/effective_lists.py` | расчёт эффективного места |
| `scripts/run_check.py` | прогон проверок |
| `reports/coverage-ledger-v1.2.0.tsv` | реестр покрытия — артефакт, которым и нашли недостачу семи файлов |
| `reports/incidents/2026-08-19-broken-copy-chain.md` | ⬆️ **поднят → `71` §7е-трис** |
| `reports/pitfalls.md` | ⬆️ **поднят → `71` §7е-трис и `21` ось 3** (PIT-001, PIT-002) |

> 🔴 **Четвёртый случай за вахту, один класс.** Батч 4 — усечённое имя · батч 7 — диапазоны ·
> батч 8 — сокращение общей части · здесь — свёртка в `каталог/*`. Рука сама сокращает
> перечисление, и это **свойство письма**, а не небрежность. `SYN-021` подтверждён
> четвёртый раз: §6а гоняется после **каждого** батча, и она каждый раз что-то находит.

### ✅ ПРИЁМКА `master-admission` (`PROCEDURE.md` §6)

```
master-admission: всего 192 = база(v2.20.0) 0 + закрытые репы 0 + С 161 + прочитано и решено 31
```

Проверка: `0 + 0 + 161 + 31 = 192` ✅ — посчитано скриптом.
**Каталогов размечено 29 из 29**, сверено с выводом §1.2.
**§6а** — все 31 файл М названы поимённо в разметке выше.

**Репа 9 из 57 закрыта.**

---

## 2026-08-22, вечер — v2.23.0: кампания НЕ двигалась, батч ушёл в замер расхода

Записано по требованию гейта `check_campaign_log`: версия не закрывается без следа
в этом журнале, даже когда следа нет.

**Ни одна репа не тронута.** Счёт прежний — **9 из 57**. Очередь: 48 непройденных
по `repos-map.md`.

**Почему не двигалась.** Батч ушёл в замер расхода по вызову владельца (`/tokens` →
агент `token-probe`), и замер дал две вещи, ради которых стоило остановиться:

1. 🔴 **Опровергнута собственная запись, сделанная утром той же вахты.**
   «`cache_creation` на реплику растёт от сессии к сессии» (2550 → 3418 → 4255) —
   вечерний срез **той же сессии** дал среднее 3856, а в новом сегменте **2075/реплику**.
   Величина упала **внутри одной сессии**, без смены сессии: значит зависит от **состава
   ходов**, а не от номера прогона. Три точки из трёх сессий легли в монотонный ряд,
   и ряд оказался **артефактом выборки**.

2. 🆕 **Расход субагентов измерим** — из `projects/<session-id>/subagents/`, а не из лога
   родителя. Прежняя формулировка («мерить по этому файлу делегирование нельзя») была
   **верна и неполна полсуток**. Для кампании это прямо существенно: за вахту спавнилось
   четыре субагента (три `repo-inventory`, один `token-probe`), и **их расход не входил
   ни в один подсчёт стоимости прохода по репе**.

**Что это меняет в методе кампании:** цена прохода по репе, которую вахта до сих пор
прикидывала по родительскому логу, **занижена на расход входных агентов**. При следующей
оценке «сколько стоит закрыть репу» слагаемое считается отдельно.

**Следующий шаг кампании не изменился:** следующая репа очереди по `repos-map.md`,
вход через `repo-inventory`, разметка со сверкой числа каталогов (`SYN-027`),
§6 с версией базы и вычетом закрытых реп, §6а после каждого батча.

---

## Репы 10–12: `ai-relay`, `visual-style`, `control-panel` — три малых одним батчем

Взяты вместе по принципу брифа «от дешёвого к дорогому»: 48 файлов на троих, агент здесь
дороже прямого чтения. Все три прочитаны целиком.

### 🔴 Третье подтверждение: базы в этих репах тоже нет

| Репа | Всего | база | закрытые репы | остаток | каталогов |
|---|---|---|---|---|---|
| `ai-relay` | 6 | **0** | 0 | 6 | 1 |
| `visual-style` | 6 | **0** | 0 | 6 | 1 |
| `control-panel` | 36 | **0** | 1 | 35 | 9 |

Ни в одной нет `00-infrastructure/`. Вместе с `master-admission` это **четыре репы подряд
без раздачи базы** — задача §P2 «измерить фактическое покрытие» из гипотезы превращается
в почти уверенность: раздача покрывает **старые** репы, а заведённые в августе 2026 её
не получали.

**Разметка: каталогов 1 + 1 + 9 = 11, размечено 11 из 11** (`SYN-027`).

### `ai-relay` и `visual-style` — скелеты `0.1.0`, класс `satellite`

Обе заведены **20.08.2026**, обе несут ровно один набор из шести файлов:
`.repo-id` · `.repo-class` · `VERSION` · `README.md` · `CHANGELOG.md` · `START-HERE.md`.
**Все шесть — М**, прочитаны, содержания предметного пока нет («пока пусто» их же словами).

| Репа | За что отвечает |
|---|---|
| `ai-relay` | обход слабых мест Claude через сторонние бесплатные сервисы + **протокол возврата результата** в формат, с которым Claude умеет работать (звук, видео) |
| `visual-style` | переносимая между проектами память визуального вкуса: критерии и референсы вместо переписки, которую пересказывают заново |

✅ **Форма скелета соответствует `83-project-maturity-levels.md`** — идея вынесена
из головы, границы названы, есть строка «не склад промптов вообще — только там, где Claude
объективно не может». `ai-relay` прямо очерчивает антипредмет, и это правильная половина
определения.

🔴 **Но критерия выхода из скелета нет ни в одной.** `83` требует: «скелет `0.1.0` —
идея вне головы **+ критерий выхода записан сразу**». Проверено командой: в обоих
`START-HERE.md` нет ни слова о том, **что должно появиться, чтобы репа стала `0.2.0`**.

**Это не нарушение, а разрыв во времени:** репы заведены 20.08, стандарт `83` записан
21.08 — на день позже. Значит **ни одна репа системы, заведённая до 21.08, критерия выхода
не несёт**, и это касается не только этих двух. Заведено в `ROADMAP.md` §P2.

### `control-panel` — 36 файлов, класс `product`

**М — 13:** корень (`README.md`, `ROADMAP.md`, `CHANGELOG.md`, `VERSION`, `.repo-id`,
`.repo-class`, `.gitignore`, `.env.example`, `run.sh`) · `docs/adr_001_markdown_is_the_database.md`,
`docs/adr_002_local_only.md`, `docs/adr_003_vault_root.md` · `tools/preflight.py`.

**С — 23:** `server/` (`__init__.py`, `app.py`, `gitio.py`, `tables.py`, `vault.py`,
`requirements.txt`) · `tests/test_tables.py` · `web/` (`index.html`, `css/app.css`,
`js/api.js`, `js/app.js` и 12 модулей `board`, `calendar`, `canvas`, `database`,
`flashcards`, `graph`, `habits`, `mindmap`, `notes`, `timeline`, `today`, `wiki`) —
код продукта.

| Файл | Решение |
|---|---|
| `docs/adr_001_markdown_is_the_database.md` | ⬆️ **→ `72` §4в-бис** — третий довод, см. ниже. Ядро ADR (**строить вид, а не хранилище**) ✅ уже поднято ранее и **выведено из этого же случая** |
| `docs/adr_002_local_only.md` | ✅ покрыто позицией системы по приватности. 📍 Ценна форма: «соблазн понятен, реализуется тремя способами, **каждый плохой**» — перебор вариантов до решения, а не после |
| `docs/adr_003_vault_root.md` | ✅ покрыто `71` §7е. 📍 **Образец проверки вживую:** решение помечено «реализовано» только после прогона через браузер с числами — `notes:73`, `git.tracked:true`, захват реально дописал строку и выставил `git status` в dirty. Не «должно работать» |
| `tools/preflight.py` | ✅ покрыто `69` §4г (двухуровневые гейты): синтаксис сервера без запуска, `node --check` по всем модулям фронта, `pytest` если есть |
| `README.md`, `ROADMAP.md`, `CHANGELOG.md`, `VERSION`, `.repo-id`, `.repo-class`, `.gitignore`, `.env.example`, `run.sh` | 📍 служебка репы, покрыто `01`, `24`, `25` |

### ⬆️ Что поднято: инструмент смертен, данные — нет

`72` §4в-бис уже говорил два довода против своего хранилища — про **правду** (второй
источник расходится молча) и про **доставку** (канон «один zip = полный контекст»).
ADR-001 называет **третий, про время**:

> «Панель — личный инструмент одного человека. Она может **надоесть, сломаться, остаться
> без времени на поддержку**. Содержимое обязано это пережить.»

**Личный инструмент имеет срок жизни, и он короче, чем у данных.** Заметки живут годами,
самодельная панель — ровно столько, сколько у автора есть время её чинить. Данные внутри
приложения наследуют **его** срок, а не свой.

Проверка, добавленная в базу:

> **Если завтра этот инструмент удалить — что останется?** Останется всё и работа идёт
> как раньше — построен вид. Останется дамп, который надо конвертировать, — построено
> хранилище, и оно уже второй источник правды.

Формулировка приёмки оттуда же и она проверяется одной командой: **«Выключи панель —
система работает как работала»**.

📍 Отдельно записан довод, которого §4в-бис не касался: **плоский текст — не эстетика,
а условие работоспособности передачи вахты**. ADR-001 называет это прямо: база приложения
«ломает чтение системы за один заход», то есть обрывает канон «один zip = полный контекст».

### ✅ ПРИЁМКИ (`PROCEDURE.md` §6)

```
ai-relay:      всего 6  = база(v2.24.0) 0 + закрытые репы 0 + С 0  + прочитано и решено 6
visual-style:  всего 6  = база(v2.24.0) 0 + закрытые репы 0 + С 0  + прочитано и решено 6
control-panel: всего 36 = база(v2.24.0) 0 + закрытые репы 1 + С 23 + прочитано и решено 12
```

Проверка: `0+0+0+6 = 6` ✅ · `0+0+0+6 = 6` ✅ · `0+1+23+12 = 36` ✅ — посчитано скриптом.
Каталогов размечено **11 из 11**. §6а — все файлы М названы поимённо выше.

**Репы 10, 11 и 12 из 57 закрыты.** Счёт кампании: **12 из 57**.

---

## 🔴 ПОПРАВКА к записям 22.08: «база в репу не раздавалась» — вывод был шире измерения

При закрытии `master-admission` и трёх малых реп записано: «ноль совпадений с базой,
каталога `00-infrastructure/` нет — базу не получала». По каждой репе это **верно**.
Но обобщение «раздача покрывает старые репы, а августовские нет» было **выводом,
а не измерением**, и измерение его опровергло.

**Померено скриптом `scripts/base_coverage.py` по всем 57 репам:**

| Статус | Реп |
|---|---|
| полная раздача | **46** |
| частичная | 1 |
| **нет вообще** | **10** |

**Десять непокрытых делятся на две группы, и только одна — дефект:**

- **шесть публичных/витринных** (`finpilot`, `vk-graph`, `health-report-generator`,
  `bron-kerbosch`, `vevdokimovm.github.io`, `vevdokimovm`) — базу **не получают
  осознанно**: она приватна, раздать её туда значит опубликовать
  (`59-public-mirror-filter.md`, deny-by-default). **Правильное поведение.**
- **четыре приватные** (`ai-relay`, `visual-style`, `control-panel`, `master-admission`) —
  **настоящий пробел**, все заведены в августе 2026.

### 🔴 Структурная находка, меняющая чтение листингов кампании

**Раздача живёт в `_base/`, а не в `00-infrastructure/`.** У сорока шести покрытых реп
`_base/` содержит **295 файлов**, а `00-infrastructure/` пуст или почти пуст.

При этом `PROCEDURE.md` §1 **исключает `./_base/*` из листингов по построению** — поэтому
проход по репе раздачу **не видит и правильно её не считает**. Это не дефект процедуры,
а её замысел: раздача не является материалом репы.

> **Отсюда точная формулировка вывода, которой не хватало:** «базу не получала» законно
> только когда **нет ни `_base/`, ни совпадений по хешу**. Отсутствие одного лишь
> `00-infrastructure/` этого не доказывает — и по четырём репам совпало случайно,
> потому что у них не было и `_base/`.

**Что это НЕ отменяет:** записи по четырём репам верны, приёмки не меняются — у них
действительно ноль по обоим признакам. Отменяется **обобщение**, сделанное поверх них.

**Урок в копилку:** четыре совпавших частных случая дали правдоподобное общее правило,
и оно оказалось неверным в шести случаях из десяти. Тот же класс, что `cache_creation`
(три точки → «тренд» → опровергнут в тот же день) и `SYN-025` (хеши сравнил, файлы
не открыл). **Общее правило проверяется командой по всей области, а не по числу
совпавших случаев.**

---

## Репа 13: `research-craft` — 18 файлов, скелет

Взята как «313 файлов» по `du` — на деле **18 собственных**, остальные 295 это `_base/`,
исключаемый листингом §1 по построению. Первый случай, когда разница между «размером
на диске» и «объёмом репы» составила **×17**.

### Копии — две, идентичные

| Копия | `VERSION` | `.git` | файлов |
|---|---|---|---|
| `~/Documents/система_репозиториев/research-craft` — канон | 0.1.0 | нет | **18** |
| `~/Downloads/_sync-base/research-craft` | 0.1.0 | **есть** | 18 |

`diff -rq` пуст. Второй случай подряд идентичных копий (после `self-map`), и по той же
причине: репа заведена **04.08.2026** и не наполнялась — расходиться нечему.
`.git` снова у копии в `_sync-base` — **седьмое** подтверждение `SYN-017`.

### Листинг

**1.1 Всего:** `18`. **1.3 Скрытые:** нет ни одного.
**1.4 Расширения:** `.gitkeep` 7 · `.md` 6 · без расширения 1 · `.repo-meta` 1 ·
`.repo-id` 1 · `.repo-class` 1 · `.gitignore` 1.

**Каталогов в переписи: 8. Размечено 8 из 8** (`SYN-027`).

### Разметка и решения — 18 из 18, все М

Репа — **скелет класса `core`**, заведена 04.08.2026, «наполнение не начато» её же словами.
Семь тематических каталогов (`01-thinking` … `07-independence`) содержат **только
`.gitkeep`** — это каркас, а не содержание.

| Файл | Решение |
|---|---|
| `START-HERE.md` | ⬆️ **→ `PROCEDURE.md` §6** — дословно описывает механизм раздачи, см. ниже |
| `README.md` | 📍 карта репы; таблица разделов **пуста**, что честно отражает состояние |
| `WATCHLOG.md`, `ROADMAP.md`, `TASKS.md`, `CHANGELOG.md` | 📍 служебка, покрыто `04`, `30`, `45`, `24` |
| `VERSION`, `.repo-id`, `.repo-class`, `.repo-meta`, `.gitignore` | 📍 опознавательные знаки репы, покрыто `01` и `48` |
| `01-thinking/.gitkeep` … `07-independence/.gitkeep` (7) | 📍 заглушки каркаса; пустые файлы, содержания нет |

🔴 **Критерия выхода из скелета нет** — как и у `ai-relay` с `visual-style`. Репа заведена
04.08, стандарт `83` записан 21.08. Третий экземпляр той же задачи `ROADMAP.md` §P2.

### ⬆️ Что дала репа: первоисточник механизма раздачи

`START-HERE.md`, дословно:

> «`_base/` — общие правила системы (зеркало `base-repo`), раздаётся скриптом
> `deploy.sh --sync-base`, **не редактировать здесь**. Локальные правила этой репы,
> если появятся, живут в `00-infrastructure/` и в `_base/` не попадают.»

**Это документальное подтверждение поправки, сделанной часом раньше** (`SYN-030`):
отсутствие `00-infrastructure/` означает «нет **локальных** правил», а не «нет базы».
Два каталога разведены **намеренно**, и у них разные владельцы: `_base/` пишет скрипт,
`00-infrastructure/` — вахта репы.

### 🔴 Дефект в правиле, введённом этим же утром

Вход в эту репу поймал, что слагаемое «совпало с закрытой репой кампании» (введено
сегодня на `self-map`) **надувается тривиальными совпадениями**:

| Что совпало | С чем | Почему не перенос |
|---|---|---|
| 7 × `.gitkeep` | произвольные пустые файлы `it-base` | хеш **пустого файла** совпадает со всеми пустыми файлами системы |
| `VERSION` («0.1.0») | `ai-relay` **и** `visual-style` сразу | шаблон новой репы, а не перенос из конкретной |
| `.gitignore` | `mission-control` | типовой игнор-лист |

Без гварда приёмка дала бы «совпало с закрытыми репами: 10 из 18» и **сошлась бы
на пустом месте**.

**Гвард дописан в `PROCEDURE.md` §6:** признак тривиальности — файл совпал с **несколькими**
репами сразу либо его содержимое не несёт информации (пусто, строка версии, стандартный
игнор). Такие идут в «прочитано и решено», а не в вычитание.

> **Хеш доказывает тождество байтов, а не общее происхождение.** Для вычитания нужно
> второе (`SYN-025`).

### ✅ ПРИЁМКА `research-craft` (`PROCEDURE.md` §6)

```
research-craft: всего 18 = база(v2.26.0) 0 + закрытые репы 0 (все совпадения тривиальны)
                          + С 0 + прочитано и решено 18
```

Проверка: `0 + 0 + 0 + 18 = 18` ✅. Каталогов размечено **8 из 8**.
§6а — все 18 файлов названы поимённо выше.

📍 **Отдельной строкой:** `_base/` — **295 файлов**, в приёмку не входит по построению §1.
Это раздача базы, а не материал репы.

**Репа 13 из 57 закрыта.**

---

## 2026-08-22 — v2.28.0: кампания не двигалась, чинился сам авто-режим

`SYN-031`: прогон шёл с КПД **37 %** — 18 минут работы, 30 минут простоя. Вахта
заканчивала ход отчётом после каждого батча, сторож ждал 1800 с. Найдено владельцем.

Починено в скилле: интервал **60 с**, §2б запрещает отчёт между батчами.
Счёт кампании прежний — **13 из 57**. Следующая репа очереди берётся сразу.

---

## Репа 14: `misc-vault` — 123 своих файла, приют для бездомного

### Листинг

**1.1 Всего своих:** `123` (плюс `_base/` 295, вне листинга §1).
**1.3 Скрытые:** `.githooks` 1. **Каталогов в переписи: 27, размечено 27 из 27** (`SYN-027`).

**Сверка:** совпало с базой **77** · остаток **46**, из которых **32** — прежние редакции
базовых документов в плоском `00-infrastructure/` (база ушла вперёд), а **14** —
собственное содержание репы.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` | 109 | **М/✅** | плоская копия базы прежнего поколения: 77 совпало, 32 в дельте |
| `.githooks/` | 1 | **М/✅** | `pre-commit`, покрыт `32` |
| корень | 3 | **М** | `README.md`, `00-CLAUDE-STOP.md` (прежняя редакция), `.gitignore` |
| `00-inbox/`, `01-documents/`, `02-media/`, `03-archives/`, `04-data/` | 6 | **М** | каркас зон, только `.gitkeep` |
| `03-archives/anya-vk-correspondence/` | 5 | **С** | личная переписка: `README.md` + 4 PDF |

**Все 14 собственных файлов прочитаны и решены.** Тридцать два дельта-файла — прежние
редакции базы, читать нечего.

### Назначение репы подтверждено её же README

`misc-vault` — дом для того, у чего дома нет (`01-repo-standard.md` §2.5). Единственное
содержание на сегодня — один архив, и его README прямо это и говорит: «parked in misc-vault
as personal correspondence without a dedicated zone; **move to a better home if one appears**».

📍 **Форма записи правильная и стоит отметки:** файл, положенный «пока сюда», несёт
**причину, по которой он здесь, и условие переезда**. Это то, чего обычно не хватает
в свалках: без такой строки через полгода никто не отличит «положено осознанно»
от «забыто».

### 🔴 Наблюдение для владельца: два артефакта по 488 страниц

`misc-vault/03-archives/anya-vk-correspondence/` содержит **488 страниц VK в четырёх PDF**
(10.1 + 10.2 + 9.8 + 5.9 МБ ≈ 36 МБ), описанных как «full VK chat archive with Anya».

`character-a-analysis/10-life-history/03-reports/vk-source-final-report.md` описывает
источник VK как «**488 страниц**, JPEG 1170×2532, 18 кусков по 28 страниц», прочитанный
целиком, — но это «экспорт **стены и разделов профиля**», не переписка.

**Совпадение числа страниц точное, хеши — нет** (проверено против `character-a-analysis`,
`self-map`, `portrait-of-taste`: ноль совпадений).

**Что это может быть, по убыванию правдоподобия:**

1. один и тот же экспорт, **по-разному названный** в двух репах (здесь «переписка»,
   там «стена профиля») и пережатый — тогда хеши и не должны совпасть;
2. два разных материала, случайно совпавших объёмом.

**Не разрешено вахтой намеренно.** Различить можно только открыв личную переписку,
а мандат этого не требует: для кампании оба варианта дают одну и ту же разметку —
**С**. Записано владельцу строкой в `TASKS.md`.

📍 Отдельно: `character-a-analysis` фиксировала VK-материал в проектном контейнере как
**битые плейсхолдеры по 280–300 байт** («реальные данные не долетели»). Здесь лежат
**настоящие 36 МБ**. Если это тот же источник — он всё это время был в `misc-vault`.

### ✅ ПРИЁМКА `misc-vault` (`PROCEDURE.md` §6)

```
misc-vault: всего 123 = база(v2.28.0) 77 + закрытые репы 0 + С 5 + прочитано и решено 41
```

Проверка: `77 + 0 + 5 + 41 = 123` ✅. Каталогов размечено **27 из 27**.
📍 `_base/` — 295 файлов, в приёмку не входит по построению §1.

**Репа 14 из 57 закрыта.**

---

## Замер расхода 22.08.2026 — компакт наступил, четыре предсказания из семи не сбылись

Кампания в этой записи **не двигалась**: ход ушёл на замер, которого владелец ждал
с 13:48 (предрегистрация компакта). Событие наступило — разбор в
`reports/experiments/token-consumption/hypothesis.md`.

**Что вышло:** П1 (порог 999 944 в коридоре 970K–1010K), П2 (1.86 %), П7 — 🟢.
**Что не вышло:** П4 «~27 сохранённых сообщений» → **7**, промах ×3.9 · П5 «прирост
после компакта выше» → **не вырос** (1 300 против 1 348) · П6 «втрое дешевле» →
**×10.8**, недооценили эффект втрое.

📍 **Предрегистрация окупилась ровно здесь.** П4 и П5 задним числом реконструировались бы
как «ну да, так и думали». Оба опирались на **одно** наблюдение 21.08 — и оба промахнулись:
`preservedMessages` оказался характеристикой той сессии, а не механизма.

### 🔴 Главная находка — простой дороже часа стоит полного контекста

Из 529 ходов пересоздание префикса больше 20 000 токенов случилось **пять раз**; два
из них — простои 3 ч 55 м и 4 ч 33 м — дали **799 104 токена, 40 % всего `creation`
за сессию**. `cache_read` на этих ходах падает до 16 900 (голый системный префикс).

Правило про TTL **уже стояло** в `49` §4.4. Новым оказалось не оно, а **число** и то,
что правило **не доехало до решения**: интервал сторожа выбирали в скилле `/auto`
рассуждением «должен пережить батч», ни разу не заглянув в документ базы про кэш.
Цена дефекта `SYN-031` переопределена: он стоил не только 63 % времени, но и
157 566 токенов пересоздания в ту же ночь.

### 🔴 `SYN-032` — инструкция, запрещающая действие, неотличима от действия

Замер эффекта `/login` начался с поиска подстроки `/login` по логу. Нашлось **25 событий**;
настоящих — единицы. Остальное — **текст самой скиллы `/auto`**, которая содержит строку
«НЕ переключать аккаунт и НЕ делать `/login`». Скилла попадает в лог целиком при каждом
вызове, и грепу её запрет неотличим от нарушения запрета.

Тот же класс, что 11 ложных совпадений слова «компакт» днём раньше.
**Служебное событие ищется по структурному полю, а не по тексту** — маркер, поле, тип
записи. Текстовый поиск по служебным логам находит **упоминания**, а не события,
и чем подробнее инструкции, тем больше ложных срабатываний.

**Развести `/login` и простой в этих данных нельзя** — оба события пересоздания пришлись
на многочасовые паузы. Установлено, что **простоя достаточно**; достаточно ли `/login`
самого по себе — не проверено, правило «не делать `/login` при упоре в лимит» остаётся.

---

## Репы 15–17: `money`, `productivity`, `career` — три скелета одного генератора

Взяты одним батчем: по **17 файлов** каждая, все заведены **04.08.2026**, класс `core`,
`VERSION` 0.1.0 у всех трёх. Прочитаны целиком.

### Разметка — каталогов 6 из 6 в каждой, все файлы М

Своего содержания в репах нет вовсе: шесть зон под тему, в каждой один `.gitkeep`,
и одиннадцать служебных файлов. **С — ноль.**

| Файл | Тождественность между тремя репами |
|---|---|
| `ROADMAP.md`, `TASKS.md`, `WATCHLOG.md`, `CHANGELOG.md`, `.gitignore` | **побайтово тождественны** после снятия имени репы |
| `README.md`, `START-HERE.md`, `.repo-meta` | различаются **только строкой предмета** — так и должно быть |
| `VERSION`, `.repo-class`, 6 × `.gitkeep` | тривиальные, исключены гвардом §6 |

### ⬆️ 🔴 Главное: генератор скелета УСОХ между прогонами

Сравнение поколений выхода — то, чего раньше не делали:

| Заведены | Репы | Файлов | Состав корня |
|---|---|---|---|
| **04.08.2026** | `money`, `productivity`, `career` | **17** | + `ROADMAP` `TASKS` `WATCHLOG` `.repo-meta` `.gitignore` + каркас зон |
| **20.08.2026** | `ai-relay`, `visual-style` | **6** | `README` `START-HERE` `CHANGELOG` `VERSION` `.repo-id` `.repo-class` |

**Пропали ровно те три файла, что делают репу возобновляемой:** `WATCHLOG` (где стоим),
`ROADMAP` (что дальше), `TASKS` (что блокирует владельца).

📍 **Это объясняет находку по `ai-relay`/`visual-style`, объяснённую тогда неверно.**
Записали: «критерия выхода из скелета нет, потому что стандарт `83` написан на день позже».
Правда сложнее: **у ранних реп `ROADMAP` есть**, и критерий выхода туда вписывать было
куда. У поздних `ROADMAP` **отсутствует как файл** — вписывать некуда. Причина не в дате
стандарта, а в регрессии генератора. Прежняя формулировка исправлена в `ROADMAP.md` §P2.

📍 **И `83` §1 зафиксировал на бумаге поведение СТАРОГО генератора** (`ROADMAP` и `TASKS`
в обязательном минимуме), уже утраченное в новом. Совпадение выглядело как исполнение
стандарта. Поднято в **`80` §5.4** четвёртым способом терять работу молча:
**регрессия генератора не видна по одному выходу**, только при сравнении поколений.

### 🔴 Владельцу: три `core`-репы заблокированы одним действием 18 дней

У всех трёх в `TASKS.md` одна и та же строка — «прислать исходные материалы по теме», —
и в `WATCHLOG` §0 «Блокировано владельцем». Дата 04.08.2026, сегодня 22.08: **18 дней**
три репы класса `core` стоят на **одном** действии владельца. Записано в `TASKS.md`.

📍 Наблюдение о форме: сама блокировка оформлена **правильно** — названо, что нужно,
и что это разблокирует. Дефект не в записи, а в том, что **никто не сводит такие строки
по системе**: каждая репа знает про свою блокировку, а «три репы ждут одного действия»
не видит никто. Заведено в `ROADMAP.md` §P2.

### ✅ ПРИЁМКИ (`PROCEDURE.md` §6)

```
money:        всего 17 = база(v2.30.0) 0 + закрытые 0 + тривиальных 9 + С 0 + прочитано 8
productivity: всего 17 = база(v2.30.0) 0 + закрытые 0 + тривиальных 9 + С 0 + прочитано 8
career:       всего 17 = база(v2.30.0) 0 + закрытые 0 + тривиальных 9 + С 0 + прочитано 8
```

Проверка: `0+0+9+0+8 = 17` ✅ ×3 — посчитано скриптом. Каталогов **6 из 6** в каждой.
**§6а** — все восемь файлов М названы поимённо в таблице выше, для каждой репы одинаково.

📍 Пятое поколение подряд без раздачи базы: `база 0` у всех трёх. Согласуется
с `SYN-030` — раздача живёт в `_base/` (295 файлов, вне листинга §1 по построению).

**Репы 15, 16 и 17 из 57 закрыты. Счёт кампании: 17 из 57.**

---

## Репа 18: `truth-seeking` — 151 файл, четыре подъёма из одного каталога

Самая плотная по методу репа кампании: **`00-method/` дал четыре раздела базы**, и каждый
лёг в свой документ, а не в один.

### Листинг (`PROCEDURE.md` §1)

**1.1 Всего своих:** `151` (+ `_base/` 295, вне листинга §1).
**1.4 Расширения:** `.md` 141 · `.pdf` 3 · `.sh` 3 · без расширения 3 · `.template` 1.
**Каталогов в переписи: 32, размечено 32 из 32** (`SYN-027`).

**Сверка:** совпало с базой **77** · с закрытыми репами кампании **34** ·
тривиальных **1** · собственное содержание **39**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` (20 каталогов) | 111 | **М/✅** | раздача базы прежнего поколения + `claude-context`, `reports`, `templates`: 77 совпало с базой, 34 с закрытыми репами |
| `00-method/` | 11 | **М** | **ядро подъёма**, разобрано ниже поимённо |
| `01-how-science-fails/` | 4 | **М** | как ломается институт, производящий знание |
| `07-conspiracy-catalog/` | 12 | **С + 1 М** | реестр предметных карточек; `template-claim-card.md` — **М**, форма |
| `02…06-*/` + `sources/` | 10 | **С** | предметные разборы и материалы к ним |
| корень | 3 | **М** | `README.md`, `research-agenda.md`, `.repo-id` |

**Все 39 собственных файлов решены.** 111 файлов инфраструктуры — прежние редакции,
читать нечего.

### ⬆️ Четыре подъёма, и ни один не про предмет репы

Предмет репы — проверка заявлений. **Поднят только метод**, предметные разборы остались
на месте (**С**). Это важная граница: репа про конспирологию дала базе четыре правила
о **работе с собственными данными**.

**1. `67` §3а — свод наследует качество худшего входа.** Источник: **GIGO**, правило
с вершины иерархии доказательств («мета-анализ из мусорных исследований — мусор,
только с авторитетным видом»). 🔴 **В базе не было ни одного упоминания**, при том что
база сводит постоянно: покрытие, лидерборды, реестры.

> Сведение **не улучшает данные, а прячет их происхождение**. На входе двадцать записей
> разного качества, на выходе одно число, у которого качества не написано.

Три признака перепроверки — итог сильнее любого слагаемого · число слагаемых подменило
их вес · тривиальное слагаемое неотличимо от содержательного. Все три уже случались:
«четыре репы без раздачи базы» оказались **одним** фактом про генератор; хеш пустого
файла надувал приёмку.

**2. `71` §7д-бис — верный вывод по неверному основанию.** Источник: **проблема Гёттиера**
(«можно верить в истину по неправильной причине»). Живой случай — **свой, вчерашний**:
вывод «у скелетов нет критерия выхода» верен, а основание «стандарт написан на день позже»
неверно; настоящая причина — регрессия генератора. Починка пошла бы не туда.

Проверка оттуда же и она механическая: **у объяснения спрашивают не «согласуется ли оно
с фактом», а «что ещё из него следует и наблюдается ли это»**. «Стандарт позже» предсказывал,
что критерия нет у **всех** реп до 21.08 — проверка провалилась бы сразу.

**3. `72` §4е — утверждение, повторённое по системе, становится опорой без замера.**
Источник: **иллюзия истины**. Живой случай базы: «база раздаётся в 57 реп» стояло
в нескольких документах и служило основанием для выводов; замер дал **47**, и шесть
из десяти непокрытых оказались осознанным решением (`SYN-030`).

> Одно утверждение, процитированное в пяти документах, читается как **пять источников**.

Различитель механический: у измерения есть команда и дата, у опоры — только цитаты.
🔴 Маркеры риска — **круглое число** и **число всех объектов системы** («все 57», «каждая
репа»): их обычно не считали, а вывели из определения. Туда же **ошибка выжившего** —
знаменатель берётся из независимого списка, а не из объектов, прошедших фильтр измерения.

**4. `21` §4г-кватер — шаг чек-листа, провал которого сам является ответом.**
Источник: шаблон карточки, шаг «сформулируй проверяемое ядро» → «не получается — значит
`unfalsifiable`, **разбор окончен**». Обычный пункт при провале блокирует; этот —
**завершает работу с ответом**.

Приём в базе уже применялся трижды и **не имел имени**: «назови, что будет считаться
опровержением» · «приведи вывод, вернувший отказ» · «назови область поиска». Правило
при написании чек-листа: у каждого пункта спросить, **что означает невозможность его
выполнить**, и осмысленный вердикт писать прямо в пункт.

📍 Оттуда же требование к ревизии: **чужую позицию излагают в сильнейшей версии**.
Разбор, где отвергнутая альтернатива подана слабой, — не разбор, а оформление уже принятого.

### 📍 Оставлено предметным — с причиной

`00-method/00-philosophy.md`, `02-epistemology.md`, `03-scientific-method-and-reasoning.md`,
`05-critical-thinking-and-fallacies.md`, `06-statistics-for-claim-checking.md`,
`08-logic-and-arguments.md`, `09-thinking-tools-and-mental-models.md`, `04-evidence-hierarchy.md`,
`01-evaluation-framework.md`, `07-finding-primary-sources.md`, `00-method/README.md` —
прочитаны целиком, подняты выборочно. Остальное **проверено на дубль и им является**:
фальсифицируемость, пороги, прокси, корреляция против причинности, абсолютное против
относительного — всё это уже стоит в `28` (§6е, §5в, §6в, §6л) и `71`.

📍 **Отдельно ценно и НЕ поднято сознательно:** позиция репы «сомневаемся во всём одинаково
**на старте**, но не приходим к тому, что всё одинаково верно **в конце**». Для базы это
уже действующая практика («не верь саммари, проверь файлы» + градуированное доверие
к подтверждённому владельцем), и отдельного раздела не требует.

`01-how-science-fails/*` (4), `07-conspiracy-catalog/*` (11 карточек), `02…06-*/README.md`
и `sources/*` — предметные разборы, класс **С**.

### ✅ ПРИЁМКА `truth-seeking` (`PROCEDURE.md` §6)

```
truth-seeking: всего 151 = база(v2.31.0) 77 + закрытые репы 34 + тривиальных 1 + С 21 + прочитано и решено 18
```

Проверка: `77 + 34 + 1 + 21 + 18 = 151` ✅. Каталогов размечено **32 из 32**.

📍 **Сверка двух счётов, потому что они дают разные числа и оба верны.** Своих файлов
вне `00-infrastructure/` — **42** (М 18 + С 24). В формуле приёмки стоит `С 21`, потому что
**три файла С совпали по хешу** с базой или с закрытыми репами и учтены в тех слагаемых.
`21 + 18 = 39` — ровно остаток после вычитания совпадений. Расхождение 42 против 39
не дефект, а разные основания счёта: **разметка считает файлы, приёмка — непокрытые файлы.**

**§6а** — все 18 файлов М названы поимённо, список сверен командой:
`00-method/*` (11) · `01-how-science-fails/*` (4) · `07-conspiracy-catalog/template-claim-card.md` ·
`README.md` · `research-agenda.md`. **Пропусков ноль.**

**Репа 18 из 57 закрыта.**

---

## Репа 19: `christ-walk` — 179 файлов, один подъём и честный отказ от остальных

### Листинг

**1.1 Всего своих:** `179`. **1.4 Расширения:** `.md` 166 · `.pdf` 5 · `.sh` 3 ·
без расширения 3 · `.mp3` 1 · `.template` 1. **Каталогов: 31, размечено 31 из 31.**

**Сверка:** база **77** · закрытые репы кампании **34** · тривиальных **1** · остаток **67**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` | 112 | **М/✅** | раздача базы прежнего поколения |
| корень + 11 `README.md` зон | 12 | **М** | служебный слой: назначение репы и каждой зоны |
| `01`…`10-*` содержание | 55 | **С** | предметное — Писание, молитвы, Таинства, жития, пост, духовная брань |

🔴 **Личный дневник исповедей (`05-sacraments/dnevnik-ispovedey-*.md`) не открывался.**
README репы прямо помечает материал как «deeply personal». Для кампании открытие ничего
не даёт: разметка **С** следует из расположения и из шапки зоны. Тот же случай, что
переписка в `misc-vault`, и то же решение.

### ⬆️ Единственный подъём — `75` §5б: README каталога с двумя слотами

Все одиннадцать зон написаны по одной форме, и она выдержала:
**назначение · «Здесь:» что лежит сейчас · «Растить:» куда раздел растёт.**

> **Второй слот и есть весь смысл.** «Растить» отличает «пусто, потому что не дошли руки»
> от «пусто, потому что раздел молодой, и вот его вектор». Без этой строки оба состояния
> выглядят одинаково, и разбирающий не знает, дописывать сюда или сворачивать каталог.

В базе `75` держал статус **репы**; у **каталога** статуса не было вовсе — заметная дыра
при 84 документах и десятках каталогов. Это **читательская сторона `ROADMAP`**: роадмап
отвечает пришедшему работать, строка «Растить» — открывшему папку.

📍 Почему форма выдержала: в ней **нет поля, которое нечем заполнить**. Назначение есть
всегда, содержимое видно глазами, направление знает заводивший раздел. Форма с пустующим
полем перестаёт заполняться целиком.

### 📍 Почему подъём один, и это правильный результат

Репа предметная почти целиком: 55 из 67 собственных файлов — содержание веры. **Метода,
переносимого в инфраструктуру, в них нет по построению**, и выжимать его оттуда значило бы
подгонять материал под ожидание «репа обязана дать базе раздел».

`PROCEDURE.md` §6 требует, чтобы каждый файл был решён, — **и «прочитано, предметное,
не переносится» это законное решение.** Счёт подъёмов не является метрикой прохода;
метрика — ноль файлов без записи.

📍 Отдельно: `00-infrastructure/README.md` этой репы — **окаменелость**. Он описывает базу
на момент документа `33` (сейчас 84), упоминает `sync-all.sh` и «все 10 реп системы»
(сейчас 57). Не дефект: это прежняя редакция раздачи, ровно как в `misc-vault`. Но как
датирующий артефакт полезен — показывает, что раздача сюда не приходила давно.

### ✅ ПРИЁМКА `christ-walk` (`PROCEDURE.md` §6)

```
christ-walk: всего 179 = база(v2.32.0) 77 + закрытые репы 34 + тривиальных 1 + С 55 + прочитано и решено 12
```

Проверка: `77 + 34 + 1 + 55 + 12 = 179` ✅. Каталогов размечено **31 из 31**.
**§6а** — все 12 файлов М: `README.md` корня + `README.md` зон `01`…`10`
+ `08-spiritual-warfare/demonology/README.md`. **Пропусков ноль.**

**Репа 19 из 57 закрыта.**

---

## Репа 20: `family` — 287 файлов, структура как метод

Генеалогический архив. Самая **структурно** осмысленная репа кампании: подъём взят
не из содержания файлов, а **из того, как они разложены**.

### Листинг

**1.1 Всего своих:** `287`. **1.4 Расширения:** `.jpg` 118 · `.md` 114 · `.csv` 23 ·
`.pdf` 14 · `.png` 5 · `.jpeg` 4 · `.sh` 3 · без расширения 3 · `.html` 1 · `.mp4` 1 ·
`.template` 1. **Каталогов: 43, размечено 43 из 43.**

**Сверка:** база **77** · закрытые репы **34** · тривиальных **6** · остаток **170**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` | 111 | **М/✅** | раздача базы прежнего поколения |
| корень `README.md` · `07-correspondence/README.md` · 2 × `04-photos/*/README.md` | 4 | **М** | служебный слой, читан целиком |
| `01-dossier`, `02-tree-visuals`, `03-primary-sources`, `05-data-tables`, `06-research` | 39 | **С** | генеалогическое содержание: досье, схемы, мемуары, таблицы, разыскания |
| `04-photos/**` | 125 | **С** | фотоархив, группа по `SYN-026` — см. ниже |
| `07-correspondence/**` | 4 | **С** | архивы переписки в PDF |

**Групповая пометка `04-photos` законна по `SYN-026`** — свыше сотни файлов, поэтому
числом и командой, а не поимённо:

```bash
find 04-photos -type f | wc -l          # 127, из них 2 README прочитаны как М
find 04-photos -type f ! -name '*.md' | wc -l   # 125 медиа
```

Выборочное открытие сделано: оба `README.md` фотозон. Они и подтверждают группу —
`childhood-2012` (73) и `graduation-2019` (41) прямо описаны как «camera dumps,
downscaled to max 1600 px; **originals live in the offline photo archive**».

🔴 **Личное не открывалось:** рукописные мемуары бабушки, записи деда, архивы переписки
с тётей и братом. README репы помечает архив как содержащий персональные данные живущих
родственников и ДНК, **намеренно не анонимизированные**. Разметка **С** следует
из расположения и шапки; открытие не дало бы кампании ничего. Третий случай подряд
с тем же решением (`misc-vault`, `christ-walk`).

### ⬆️ `01` §2а — порядок каталогов задаётся читателем, а строятся они наоборот

`01` §1 требует цифровой префикс «для порядка» и **не говорит, чем порядок задан**.
`family` называет обе оси, и они **не совпадают**:

- **порядок построения:** мемуары → таблицы → досье (README: «от первоисточников к синтезу»);
- **порядок нумерации:** `01-dossier` — **синтез первым**, и README подписывает его
  «точка входа, если нужно быстро понять всю картину».

> **Номер обслуживает того, кто открыл репу впервые, а не хронологию накопления.**
> Первым идёт то, что отвечает «что здесь вообще» за один файл; сырьё — глубже.

Проверка одной фразой: *какой каталог ты назовёшь человеку, у которого есть пять минут?*
Не `01-` → нумерация обслуживает историю, а не читателя.

📍 Оттуда же — **«фронт работы» своим номером** (`06-research`: незакрытые вопросы,
гипотеза о старообрядчестве, план запросов в ГАРО). Это не материал и не приоритеты:
роадмап хранит, **что делать**, а этот каталог — **само содержание разысканий**.
В `ROADMAP` он не сворачивается.

### 📍 Две формы, проверенные на дубль и им являющиеся

- **«скан + расшифровка» рядом** (`03-primary-sources`, оба первоисточника в двух видах) —
  уже покрыто `60` и `68`.
- **«originals live in the offline photo archive»** — уменьшенная копия в репе, оригинал
  вне, и README это **говорит** — покрыто `65` и `31`.

Обе отмечены как **независимое подтверждение** правил базы в чужой предметной области,
а не как новый материал.

### ✅ ПРИЁМКА `family` (`PROCEDURE.md` §6)

```
family: всего 287 = база(v2.33.0) 77 + закрытые репы 34 + тривиальных 6 + С 166 + прочитано и решено 4
```

Проверка: `77 + 34 + 6 + 166 + 4 = 287` ✅. Каталогов размечено **43 из 43**.
**§6а** — все 4 файла М названы поимённо: `README.md` корня · `07-correspondence/README.md` ·
`04-photos/childhood-2012/README.md` · `04-photos/graduation-2019/README.md`.

**Репа 20 из 57 закрыта.**

---

## Репа 21: `edu-base` — 242 файла, арсенал инструментов и правило «не выдумывать данные»

### Листинг

**1.1 Всего своих:** `242`. **Каталогов: 46, размечено 46 из 46.**
**Сверка:** база **76** · закрытые репы кампании **48** · тривиальных **1** · остаток **117**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` | 124 | **М/✅** | раздача базы + шаблоны, совпало с базой и закрытыми репами |
| `02-analytics/tools/` | 11 | **М** | переиспользуемый арсенал: HTTP-клиент, скраперы, разборщик PDF, аудит репы, каталогизатор фото |
| `05-books/book-to-audio-scripts/` | 5 | **М** | конвейер книга → аудио |
| `02-analytics/dota2-player-analysis/opendota_complete_parse.py` | 1 | **М** | разбор матчей, живёт рядом со своими данными |
| корень `README.md` · `05-books/books-readme-extra.md` · `06-*/README.md` · `07-*/README.md` | 4 | **М** | служебный слой |
| `01-learning-how-to-learn`, `03-logic`, `04-linguistics`, книги, `06-speech`, `07-guitar` | 96 | **С** | учебный материал: транскрипты, конспекты, книги, упражнения, аккорды |

**§6а — все 21 файл М названы поимённо выше.**

### ⬆️ `71` §7и — правдоподобный неверный вывод инструмента, третий и худший случай

Источник: `02-analytics/tools/README.md`, правило четвёртое.

> Наивный `extract_text()` на PDF-таблице **схлопывает колонки** и выдаёт уверенную чушь.
> Проверено на учебных планах: раздел «Семестр 2» оказался забит значениями ЗЕТ.

Инструмент **не упал, не вернул пусто и не сообщил об ошибке.** Он вернул структурно
правильный текст с неправильными значениями.

| | Что вернул | Как выглядит | Чем кончается |
|---|---|---|---|
| `71` §7ж | отказ, которого не было | сбоем | теряется рабочий канал |
| `71` §7з | пусто | фактом об источнике | в корпус попадает вымысел |
| **`71` §7и** | **правдоподобное и неверное** | **нормальной работой** | **в корпус попадают числа, переживающие проверку глазами** |

**Лечение оттуда же:** развести операцию, которая может тихо соврать, и ту, которая
не может — `pdf_extract.py text` отдельно от `pdf_extract.py tables`, и у второй **право
отказаться**: «не сошлось» вместо правдоподобного. Правило шире PDF: везде, где инструмент
**восстанавливает структуру**, ему нужен контроль сходимости и право на отказ.
🔴 **Инструмент без права отказаться отказываться и не будет — он всегда что-нибудь вернёт.**

Приёмка любого разборщика: подать заведомо негодный вход. Вернулось правдоподобное —
инструмент опасен независимо от качества работы на годном входе.

📍 **Правило рядом, поднято туда же:** «**второй запуск должен быть бесплатным**» — кэш
на диске не оптимизация, а **условие, при котором с инструментом вообще можно работать**.
Без него цена итерации отладки равна цене боевого прогона, и отладку начинают экономить.
Тот же счёт, что у интервала сторожевого будильника (`49` §4.4): **параметр выбирается
по цене повторения.**

### 📍 Отмечено и не поднято

- **Провенанс по файлу прямо в README каталога** (`06-speech-and-diction`): у каждого
  пункта таблицы сказано, чем он приходится источнику — «converted from the original notes» ·
  «original PDF of the same notes (primary source)» · «`notion-*` — rendered pages, остальные —
  photographed sheets». Читаемая форма пометок `71`; уже покрыто им и `65`.
- **Кросс-указатель на то, чего здесь нет намеренно**: «Speech-technique books live
  in `../speech-and-diction/books/`». Частный случай `75` §5б, поднятого этим же прогоном.
- Остальные три правила арсенала — «сеть рвётся всегда» с чекпоинтами после каждой
  страницы и «бот-защита не ошибка кода» — покрыты лестницей ретрая (`/auto` §3) и `80`.

### ✅ ПРИЁМКА `edu-base` (`PROCEDURE.md` §6)

```
edu-base: всего 242 = база(v2.34.0) 76 + закрытые репы 48 + тривиальных 1 + С 96 + прочитано и решено 21
```

Проверка: `76 + 48 + 1 + 96 + 21 = 242` ✅. Каталогов размечено **46 из 46**.

**Репа 21 из 57 закрыта.**

---

## Репа 22: `health-vault` — 390 файлов, README описывает репу, которой больше нет

Самая чувствительная репа кампании: психиатрия, генетика, анализы, медкарты.
**Предметное не открывалось.** Читан служебный слой.

### Листинг

**1.1 Всего своих:** `390`. **1.4 Расширения:** `.csv` 168 · `.md` 140 · `.pdf` 66 ·
`.sh` 3 · без расширения 3 · `.html` 2 · `.ogg` 2 · `.py` 2 · `.jpeg` 1 · `.vcf` 1 ·
`.txt` 1 · `.template` 1. **Каталогов: 47, размечено 47 из 47.**

**Сверка:** база **76** · закрытые репы **35** · тривиальных **3** · остаток **276**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` | 111 | **М/✅** | раздача базы прежнего поколения |
| корень `README.md` · `02-genetics/skripty/*.py` (2) · `04-psychiatry-pnd/psychotherapy-sessions/README.md` | 4 | **М** | служебный слой и конвертеры VCF→CSV |
| `01`…`10-*` содержание | 272 | **С** | медицинские данные |

🔴 **Не открывалось:** амбулаторная карта ПНД, записи 17 психотерапевтических сессий,
заключения генетика, бланки анализов, медкарты. README репы помечает всё как приватные
персональные медицинские данные. Разметка **С** следует из расположения и шапки.
Четвёртый случай подряд с тем же решением.

**Групповая пометка `10-apple-health/trenirovki-detalno` (154 CSV)** — по `SYN-026`,
числом и командой: `find 10-apple-health/trenirovki-detalno -type f | wc -l`.
Схема имён машинная и единообразная: `{тип}-{метрика}-{ГГГГММДД}-{ЧЧММСС}.csv`.

### ⬆️ `75` §5в — переименование чинит ссылки и не чинит прозу

🔴 **Проверено командой, и это находка про чужую репу:** README называет все десять
каталогов в **старой схеме** — `01_Анализы/`, `02_Генетика/`, `04_Психиатрия_ПНД/`, —
а на диске **kebab-case латиницей**: `01-lab-tests`, `02-genetics`, `04-psychiatry-pnd`.
**Все десять путей мертвы.** Заодно устарели числа: «Всего: 255 файлов» при 276 своих.

> **Таблица каталогов в README — это проза, а не ссылки.** Инструмент переименования
> правит пути в коде и относительные ссылки; строка `` `01_Анализы/` `` в клетке таблицы
> для него обычный текст. Он её не трогает и не обязан.

Хуже того, README **выглядит исправным**: таблица на месте, форматирование целое, описания
верные по смыслу. Сломано единственное, что глазами не проверяют, — **соответствие имён
реальности**. Проверка — механическая, `grep` путей + `test -e`, записана в раздел.

📍 Тот же класс уже ловился в этой системе («тело README врало шестью числами», v1.87.0).
Числа и имена стареют по одной причине: **описывают состояние, а живут в документе,
который правят по другому поводу.**

**Дефект чужой репы записан, не чинится** — граница мандата.

### ⬆️ `21` §4б-тер — сборка записывает, что выброшено и почему

Из README, раздел «Что было убрано при сборке»: три формы амбулаторной карты → одна,
три идентичных VCF → один канонический, `.rtf` с тем же VCF внутри (26 МБ) — убран,
монолитный PDF на 89 МБ — убран. **Экономия ~256 МБ.**

> **Удаление без записи неотличимо от пропажи.** Пришедший следом обязан относиться
> к нему как к пропаже — то есть искать.

Запись обязана нести: что убрано (по имени) · почему · что оставлено каноническим ·
сколько сэкономлено. Последнее — единственное, что делает приём воспроизводимым.

📍 Оттуда же: «этот CSV технически дублирует `.vcf`, оставлен как рабочая выгрузка,
можно удалить и **пересобрать скриптом**». **Дубль с названным способом пересборки —
не дубль, а кэш**, и решение по нему принимается за секунду.

### ✅ ПРИЁМКА `health-vault` (`PROCEDURE.md` §6)

```
health-vault: всего 390 = база(v2.35.0) 76 + закрытые репы 35 + тривиальных 3 + С 272 + прочитано и решено 4
```

Проверка: `76 + 35 + 3 + 272 + 4 = 390` ✅. Каталогов размечено **47 из 47**.
**§6а** — все 4 файла М поимённо: `README.md` · `02-genetics/skripty/convert_vcf_to_csv.py` ·
`02-genetics/skripty/vcf_to_full_csv.py` · `04-psychiatry-pnd/psychotherapy-sessions/README.md`.

**Репа 22 из 57 закрыта.**

---

## Репа 23: `legal-knowledge-base` — 589 файлов, пять подъёмов, лучший метод кампании

Крупнейшая репа кампании и самая богатая методом. `05-tools/` — рабочий инструментарий
исследователя, написанный **после реальных проколов**, и каждый прокол разобран автором
о себе.

### Листинг

**1.1 Всего своих:** `589`. **1.4 Расширения:** `.md` 569 · `.pdf` 8 · `.sh` 5 ·
без расширения 4 · `.csv` 1 · `.py` 1 · `.template` 1. **Каталогов: 68, размечено 68 из 68.**

**Сверка:** база **76** · закрытые репы **34** · тривиальных **2** · остаток **477**.

### Разметка

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `00-infrastructure/**` (свои 4 + раздача) | 114 | **М/✅** | раздача базы + собственные отчёты репы |
| `05-tools/` + `proverka-glubiny/` | 9 | **М** | протокол приёмки, протокол ревизии, методичка проверки готовности, трекер, промпт-сборщик |
| корень: `README`, `ROADMAP`, `CHANGELOG`, `publish.sh`, `release.sh`, манифест | 6 | **М** | служебный слой |
| `07-positions-and-tactics` + `08-enforcement-practice` служебка | 6 | **М** | шаблоны карточек, реестр источников, журнал изменений |
| `03-templates/power-of-attorney-*.md` | 1 | **М** | шаблон формы |
| `01-libraries/` (32 организации) | 331 | **С** | собранные библиотеки — предметное |
| `02-guides/` (15) · `03-templates/ryby` (32) · `04-reference` · `06` · `10-personal-strategy` | 120 | **С** | правовое содержание и личная стратегия |

🔴 **`02-guides/` помечены С, а не М сознательно.** Это методички **по праву** — предмет репы,
а не метод инфраструктуры. Помечать их М значило бы обещать чтение, которого не было
(`PROCEDURE.md` §6а: помеченное М обязано быть прочитано поимённо).

### ⬆️ Пять подъёмов

**1. `69` §3а — полнота ответа ограничена полнотой вопроса.** Из разбора реального провала:
спросили «с какого момента наступает статус — присяга или приказ?», получили **точный ответ
про одну точку**, не получили нормы, действующей **между** этапами, — а она и решала дело.

> Вопрос «когда наступает X» систематически хуже вопроса «какие события меняют положение
> и что действует между ними». Первый даёт **точку**, второй — **карту**.

Таблица переписывания задания перенесена целиком. **Прямое следствие для субагентов базы:**
задание переписывается по таблице **перед вызовом**, а не после разочарования в отчёте.
Оттуда же: **факт без основания — половина работы**; именно механизм делает единичный
случай воспроизводимым.

**2. `21` §4в-бис — маятник при исправлении.** Автор признаёт о себе: «на одной норме
ошибся дважды подряд, **в противоположных направлениях**» — исправляя завышение, обнулил
понятие целиком. Второй промах вероятнее первого: свежее опровержение работает как разгон.
Проверка: заменяя A на B, спросить вслух — **точно ли A теперь не значит вообще ничего?**
Признак маятника в тексте правки — исчезло **всякое** упоминание прежнего понятия.

**3. `83` §3а — вторая ось зрелости: происхождение содержимого.** Прокол: библиотека стояла
🟢 «готово», потому что в сводке было «14 файлов, обогащены живой практикой» — строка
описывала **наличие файлов, а не происхождение контента**. Дословного текста источника — ноль.

> «Файлы существуют» и «раздел готов» — **разные утверждения**. Готовность = подтверждённое
> происхождение, а не количество файлов.

Шкала L0–L3, и 🔴 **граница L1/L3 невидима снаружи**: реконструкция и дословная проработка
дают одинаково аккуратные файлы. Четыре признака по убыванию надёжности + правило
**«разошлись скрипт и глаз — верь глазу; скрипт это фильтр, а не судья»**.

**4. `21` §4а-бис — срок годности по типу содержимого, дата проверки построчно.**
Принцип оттуда: **отсутствие даты проверки = считается непроверенным**. Таблица сроков
перенесена. 📍 Сильнейшая строка — **практика меняется чаще правил и незаметнее**: правило
меняют публично, поведение молча. И порядок ревизии как **цепочка зависимостей**: сначала
существуют ли объекты, потом их свойства — «проверять контакты закрывшейся организации
бессмысленно».

**5. `80` §5в дополнен — копия переживает источник, а маршруты внутри неё нет.**
За один прогон обнаружено, что несколько организаций-источников прекратили существование.
Материал сохранился, но **маршруты помощи внутри него ведут к людям, которых больше нет**.

> Копия сохраняет текст, но **не сохраняет его исполнимость**.

Лечение: разделять при сборе слой знания и слой маршрута · маршруты держать в одном месте ·
проверять и источник, и названных им адресатов — вторые умирают раньше.

📍 Оттуда же подтверждение `SYN-017`: часть библиотек считалась несобранной, а лежала
собранной **в другом аккаунте** россыпью. Трекер разошёлся с реальностью не из-за потери,
а из-за рассинхрона. **Перед выводом «этого нет» — сверка по диску, а не по трекеру.**

### ✅ ПРИЁМКА `legal-knowledge-base` (`PROCEDURE.md` §6)

```
legal-knowledge-base: всего 589 = база(v2.36.0) 76 + закрытые репы 34 + тривиальных 2 + С 451 + прочитано и решено 26
```

Проверка: `76 + 34 + 2 + 451 + 26 = 589` ✅. Каталогов размечено **68 из 68**.
**§6а** — 26 файлов М названы поимённо в разметке; список сверен командой.

**Репа 23 из 57 закрыта.**

---

## Репа 24: `academic-portfolio` — 1345 файлов, 447 каталогов, два подъёма из аномалий

Крупнейшая репа системы. Её кампания сжатия уже описана в базе (`12-case-academic-portfolio.md`),
поэтому метод оттуда поднят давно — здесь взяты **две аномалии, найденные командой**.

### Листинг

**1.1 Всего своих:** `1345`. **1.4 Расширения:** `.md` 619 · `.cpp` 158 · `.py` 116 ·
`.pdf` 108 · `.jpg` 91 · `.sql` 50 · `.html` 38 · `.ipynb` 23 · без расширения 22 ·
`.txt` 21 · `.png` 18 · `.dae` 16. **Каталогов: 447, размечено 447 из 447.**

**Сверка:** база **77** · закрытые репы **314** · тривиальных **16** · остаток **938**.

📍 **314 совпадений с закрытыми репами** — рекорд кампании и он объясним: репа делит
служебный слой с `it-base`, `edu-base` и `mission-control`, а `05-semesters` пересекается
по учебным материалам с `edu-base`.

### Разметка — групповая по `SYN-026`, 938 файлов свыше сотни

| Каталог | Файлов | | Причина |
|---|---|---|---|
| `05-semesters/**` | 403 | **С** | конспекты, лабы, курсовые по семестрам |
| `10-mephi-2019-2022/**` | 225 | **С** | архив прежнего вуза |
| `11-school/**` | 113 | **С** | школьный архив |
| `01-bachelor-thesis/**` | 93 | **С** | ВКР и защита |
| `07-certificates`, `12-study-plans`, `06-internships`, прочее | 91 | **С** | документы |
| 11 × `scans-inventory.md` | 11 | **М** | описи снятых сканов — разобраны ниже |
| `README.md`, `repos-map.md` | 2 | **М** | служебный слой |

Воспроизводимая команда группы: `find 05-semesters 10-mephi-2019-2022 11-school -type f | wc -l`.
Выборочное открытие сделано: `scans-inventory.md` (образец), `README.md`, `repos-map.md`.

### ⬆️ `72` §4б-бис — копия, честно указывающая на источник, всё равно читается вместо него

🔴 **Найдено командой.** В корне листовой репы лежит `repos-map.md` — копия карты **всей
системы**. Копия сама признаётся, что она копия, прямо в шапке:

> «📍 Эта карта живёт в **`base-repo`** — родительской репе всей системы.»

И всё равно разошлась: **147 строк** против актуальной карты на 57 реп, состав системы
десятилетней давности по меркам этой системы. Хеши не совпадают
(`1861922d…` против `be19912e…`).

> **Строка «источник вон там» читается как знак качества, а не как указание уйти.**
> Читатель видит, что документ знает своё место, ставит ему доверие — и читает **его**,
> потому что он уже открыт. Небрежная копия хотя бы вызывает подозрение; аккуратная
> не вызывает ничего.

Лечение: ссылка вместо копии; если копия неизбежна — **дата снятия** в ней.
📍 Контрпример из этой же кампании: `_base/` не порождает этой проблемы, потому что
**раздаётся скриптом**. **Копия безопасна, когда у неё есть механизм обновления;
без механизма любая пометка — просто вежливость.**

### ⬆️ `21` §4б-тер дополнен — где лежит след удаления важнее, чем что в нём написано

Одиннадцать `scans-inventory.md`, каждый **в том каталоге, откуда сканы убрали**:

```markdown
# Сканы (не хранятся)
Рукописные материалы сняты при slimming; опись:
| Материал | Стр. | Было, МБ |
```

| Централизованная запись | Локальная метка |
|---|---|
| находит тот, кто **знает**, что запись есть | находит тот, кто **открыл папку и удивился** |
| отвечает «что мы выкинули» | отвечает «а где то, что тут было?» |

> **След удаления кладётся на место удалённого.** Вопрос возникает в каталоге,
> а не в README — там его и надо встретить.

Оба места законны и **не заменяют друг друга**. Массовая чистка оставляет обе.
📍 Форма описи минимальна и достаточна: **что было · сколько · какого объёма** — хватает
решить, искать ли оригинал, и не хватает счесть опись заменой файла.

### 📍 Измерено и оставлено наблюдением: 2.1 файла на каталог

**447 каталогов на 938 своих файлов**, максимальная глубина вложенности — **7**.
Структура почти вырожденная: в среднем два файла на папку.

Не помечено дефектом сознательно: для учебного архива, где иерархия
`семестр → предмет → тип работы` **несёт смысл поиска**, малое наполнение папки — цена
навигации, а не признак беспорядка. Записано как наблюдение для владельца, если он
когда-нибудь захочет уплощать.

### ✅ ПРИЁМКА `academic-portfolio` (`PROCEDURE.md` §6)

```
academic-portfolio: всего 1345 = база(v2.37.0) 77 + закрытые репы 314 + тривиальных 16 + С 925 + прочитано и решено 13
```

Проверка: `77 + 314 + 16 + 925 + 13 = 1345` ✅. Каталогов размечено **447 из 447**.
**§6а** — все 13 файлов М названы: `README.md`, `repos-map.md` и 11 × `scans-inventory.md`
(список сверен командой `find . -name scans-inventory.md`).

**Репа 24 из 57 закрыта.**

---

## Замер 22.08.2026, вечер — `/login` разведён с простоем, и это переписало два вывода

Кампания в этой версии **не двигалась по репам**: ход ушёл на замер по `/tokens` и на
консолидацию копий по прямому запросу владельца.

### 🟢 Главное: `/login` сбрасывает кэш — измерено, впервые

Утром в `hypothesis.md` стояло: «развести `/login` и простой в этих данных **нельзя** —
оба пересоздания префикса пришлись на многочасовые паузы». Это было верно для тех данных.

Вечером владелец сделал `/login` **дважды на коротких паузах** — и поставил естественный
эксперимент, которого вахта поставить не могла:

| Ход | Пересоздано | `cache_read` | Пауза | `/login` до него |
|---|---|---|---|---|
| 11:39:32 | 49 071 | **16 900** | 8 м | **0.3 мин назад** |
| 11:54:49 | 83 264 | **16 900** | 3 м | **0.7 мин назад** |

`cache_read` = 16 900 — голый системный префикс. Паузы **внутри** часового TTL, простой
исключён. Причина одна.

> **Цена `/login` равна всему текущему контексту.** Вышло дёшево (49K и 83K) только потому,
> что оба пришлись на низкий контекст сразу после компакта. На 997K это был бы
> **почти миллион токенов за ход**.

Поднято в `49` §2а и в `/auto` §4 — с числами вместо прежнего «×12 по замеру 21.08».

### 🔴 Опровергнут вывод собственного субагента

`token-probe` записал: «`seven_day` rate-limit прилетел в 11:44:59, а в 11:54:49 работа
пошла — блокировка снялась сама за ~9 минут, причина `[ДН]`».

**Причина не `[ДН]`.** В 11:54:09 владелец выполнил `/login` — сменил аккаунт. Следующий ход
через 40 секунд, и он же несёт полное пересоздание префикса. Лимит никуда не делся:
его обошли, заплатив кэшем.

📍 **Класс — `71` §7д-бис, поднятый этим же прогоном: верный факт по неверному основанию.**
Наблюдение «работа возобновилась» верно, объяснение неверно и **опасно**: по нему следовало
бы ждать девять минут вместо часа. Агент не мог знать про `/login` — он искал события
текстом. Разрешилось структурным поиском.

### 🟢 `SYN-032` подтверждён числом: ошибка в 8 раз, и она прятала сигнал

| Способ | «Событий» `/login` |
|---|---|
| текстовый, подстрока | **25** |
| структурный, по полю команды | **3** |

Двадцать два из двадцати пяти — **текст скиллы `/auto`** со строкой «НЕ делать `/login`».

> **Ложные срабатывания не просто завышают счёт — они прячут сигнал.** Три события
> в ряду из 25 неразличимы; три из трёх — очевидны. Корреляция с кэшем стала видна
> **только** после того, как счёт стал верным.

### 🆕 Субагенты измерены впервые: 8 логов, отказ не бесплатен

Родительский лог: **0** записей `isSidechain` — техническая правда. Собственные логи
субагентов: **8 файлов**, суммарно `read` **2 465 542** · `create` **272 150** ·
`output` **11 831**.

🔴 Упавший от лимита `token-probe` до отказа потратил **read 461 330 · create 54 527**.
**Агент, умерший на лимите, уже израсходовал бюджет** — это не попытка вхолостую.

### 📍 П5 на большей выборке: знак качнулся, вывод устоял

Утром: 1 300 против 1 348 на 28 ходах (−3.6 %). Теперь: ≈2057 против ≈1995 на 162 ходах
(+3.1 %). Обе величины внутри шума.

> **Малая выборка даёт не «менее точный ответ», а уверенный ответ с произвольным знаком.**

### 🗂️ Консолидация копий реп — по запросу владельца

Построена карта расхождений (`reports/audits/repo-copies-divergence-2026-08-22.md`),
**шесть реп сведены**, ничего не удалено — всё перемещено в `~/Documents/_archive-dubli-2026-08-22/`.

- `ai-relay`, `visual-style` — идентичны; 🔴 `.git` был в **убираемой** копии, перенесён
  в остающуюся, `git status` чист.
- `character-a-analysis`, `portrait-of-taste` — идентичны, разница в 1 файл была `.DS_Store`.
- `mission-control` — расходились **в обе стороны**: забран `deploy.sh` 4.16.1 → **4.17.2**
  (совпал с каноном базы байт-в-байт), остальные три файла в системе уже новее.
- `dota-dossier` — «различия» оказались **только переводами строк** (CRLF против LF),
  9 «лишних» файлов — `__pycache__/*.pyc`.
- `база-repo` в системе — **устаревший снимок v1.42.0**; три уникальных файла проверены
  поимённо, у каждого есть живой преемник, уникального не несёт.

Открыто владельцу: 12 файлов в малых копиях `finpilot` (в т.ч. 164 КБ истории
математической модели v3.0–v3.5) и переезд самой рабочей базы.

---

## Репы 25–50: двадцать шесть скелетов одним батчем — и поправка к собственному выводу

Все 26 оставшихся малых реп системы взяты одним заходом: 403 файла на всех, 138 каталогов,
собственного содержания **ноль**. Каждая — выход генератора скелета, ни одна не наполнена.

### 🔴 ГЛАВНОЕ: поправка к `80` §5.4, сделанная по третьей точке

Вчера здесь было записано: «**генератор усох**: 17 файлов (04.08) → 6 (20.08)».
**Направление угадано, механизм назван неверно.** Замер по всем 26 репам дал третью точку,
и она сломала картину:

| Заведены | Реп | Файлов | `START-HERE` | `TASKS` | `.repo-meta` | `00-infra` | `ROADMAP` | `WATCHLOG` |
|---|---:|---:|:---:|:---:|:---:|:---:|:---:|:---:|
| **04–08.08** (α) | 18 | 15–17 | ✅ | ✅ | ✅ | ❌ | ✅ | ✅ |
| **15.08** (β) | 8 | 13–17 | ❌ | ❌ | ❌ | **✅** | ✅ | ✅ |
| **20.08** (γ) | 2 | **6** | ✅ | ❌ | ❌ | ❌ | ❌ | ❌ |

**β не беднее α — она другая.** Добавила `00-infrastructure/`, выбросила `START-HERE`,
`TASKS`, `.repo-meta`. Монотонной деградации нет: **ни один набор не является подмножеством
другого**.

> **Это хуже усыхания.** Усыхание чинится восстановлением утраченного. Здесь чинить нечего:
> **канонического выхода не существовало ни в один момент**, и любая «починка» — выбор
> между тремя равноправными историческими вариантами.

Общее у всех трёх — `README`, `CHANGELOG`, `VERSION`, `.repo-id`, `.repo-class`.
**Пять файлов из семнадцати.** Остальное — предмет случая.

📍 **Как ошибка нашлась, и это урок дороже находки.** Первый вывод строился на **двух**
точках — старой и новой. **Две точки всегда ложатся на прямую и всегда дают «монотонное
изменение»**: форма вывода была предопределена числом наблюдений, а не данными.
Третья точка её сломала. Родня: `71` §7д-бис (верный факт по неверному основанию),
`28` §6н, и сегодняшний же урок про П5 — **малая выборка даёт не менее точный ответ,
а уверенный ответ с произвольным знаком.**

### Разметка

Все 237 непокрытых файлов — **М**, служебка скелетов (`README`, `CHANGELOG`, `ROADMAP`,
`WATCHLOG`, `START-HERE`, `TASKS`, `.repo-id`, `.repo-meta`). **С — ноль во всех 26 репах:**
предметного содержания нет ни в одной, каталоги зон несут только `.gitkeep`.

166 файлов — тривиальные (`.gitkeep`, boilerplate `VERSION`, `.gitignore`, `.repo-class`),
исключены гвардом §6. **База 0 и закрытые репы 0** — пятое подтверждение `SYN-030`:
раздача живёт в `_base/`, вне листинга §1.

### ✅ ПРИЁМКА — 26 реп (`PROCEDURE.md` §6)

```
всего 403 = база(v2.39.0) 0 + закрытые репы 0 + тривиальных 166 + С 0 + прочитано и решено 237
```

Проверка: `0 + 0 + 166 + 0 + 237 = 403` ✅ — посчитано скриптом по каждой репе отдельно,
у всех 26 арифметика сошлась. **Каталогов размечено 138 из 138.**

| Репа | всего | триv | прочитано | кат |
|---|---:|---:|---:|---:|
| `acting` `csgo` `linguistics` | 15 | 7 | 8 | 4 |
| `art` `chess` `cybersecurity` `nutrition` `philosophy` `politics` `rhetoric` `sales` `science` `security-forces` `sport` `spycraft` `style` `war` | 16 | 8 | 8 | 5 |
| `algorithms` | 17 | 9 | 8 | 6 |
| `nationality` `speed-reading` | 13 | 3 | 10 | 5 |
| `history` | 14 | 3 | 11 | 6 |
| `biology` `chemistry` `mathematics` `physics` | 15 | 3 | 12 | 7 |
| `salvation` | 17 | 3 | 14 | 6 |

**§6а** — файлы М перечислены по формам скелета (α/β/γ выше), состав каждой формы сверен
командой; файлов без записи ноль.

📍 **Групповое закрытие законно именно здесь** и не является поблажкой: репы не «похожи»,
а **побайтово одинаковы по служебному слою** после подстановки имени — это проверено,
а не предположено (`21` ось 3: дубль подтверждается содержимым).

**Репы 25–50 из 57 закрыты. Счёт кампании: 50 из 57.**

---

## Репы 51–56: витрина, архив и `ml-base` — 🏁 КАМПАНИЯ ЗАКРЫТА, 56 из 57

Шесть последних реп системы одним батчем: **191 файл, 47 каталогов**.

### ✅ ПРИЁМКИ (`PROCEDURE.md` §6)

```
ml-base:                 97 = база(v2.40.0) 0 + закрытые 5  + триv 8 + С 71  + прочитано 13
health-report-generator: 28 = база 0 + закрытые 20 + триv 3 + С 0   + прочитано 5
vk-graph:                20 = база 0 + закрытые 8  + триv 7 + С 0   + прочитано 5
vevdokimovm.github.io:   11 = база 0 + закрытые 0  + триv 3 + С 4   + прочитано 4
vevdokimovm:              5 = база 0 + закрытые 0  + триv 3 + С 0   + прочитано 2
Old (before Claude):     30 = база 0 + закрытые 0  + триv 0 + С 30  + прочитано 0
```

Все шесть сошлись, суммарно `24 + 0 + 33 + 105 + 29 = 191` ✅.
**Каталогов размечено 47 из 47.** §6а — 29 файлов М названы поимённо командой.

📍 `Old (before Claude)` — **не репа, а архив**: 30 `.zip` с прежними проектами GitHub
(242 МБ). Собственного метода не несёт по построению, вся разметка **С**. Закрыта как
единица переписи, а не как репозиторий.

### ⬆️ `59` — жанр `NOTES.md`: витрина сопровождается разбором собственных слабостей

Найдено у `vk-graph` и `health-report-generator` — обеих публичных. Рядом с `README`
лежит разбор своего же кода:

> «Разбор собственного кода **без скидок**: если проект показывают работодателю,
> то **честный разбор его слабых мест ценнее самого кода** — он показывает,
> что автор вырос с тех пор, как его написал.»

> **Артефакт плюс его критика сильнее артефакта.** Код показывает уровень **на момент
> написания**, разбор — уровень **сегодня**, и разрыв между ними и есть то, что читающий
> хочет знать. Умолчание о слабом месте не прячет его от того, кто умеет читать; оно
> прячет только одно — что автор его видит.

**Форма — два слота:** «Что не так» (дефект конкретно, с механизмом отказа) и
«Как сейчас» (приём по имени). 🔴 **Без второго слота жанр не работает** и превращается
в самоуничижение: «тут плохо» без «а надо так» — признание, что автор и сейчас не знает как.

📍 Тот же приём на уровне страницы — ADR-001 в `vevdokimovm.github.io`: четыре роли
заявлены **неравными**, слабейшая названа слабой прямым текстом. **Заявленная слабость
снимает подозрение в остальном.**

### 📍 Проверено на дубль и им является

ADR-001 той же репы («один источник `profile.json` — четыре вида; девять `.docx` разошлись,
потому что **файл это снимок, а не источник**; PDF не хранится, а печатается в момент
нужды и потому не может устареть») — **независимое подтверждение `72` §4в-бис**,
поднятого из `control-panel`. Другая предметная область, тот же вывод. Записано
подтверждением, не новым материалом.

### 🏁 КАМПАНИЯ СИНТЕЗА ЗАКРЫТА: 56 репозиториев из 57

Пятьдесят седьмая — `finpilot` (33 651 файл, v8.28.0) — продукт, а не knowledge-репа,
и берётся отдельным заходом: у неё свой `.git`, свои релизы и объём больше всей остальной
системы вместе взятой.

**Итог кампании по числам:** пройдено 56 реп, поднято в базу более сорока разделов,
приёмка §6 сошлась у каждой репы, §6а дал ноль пропусков **везде**.

---

## Репа 57: `finpilot` — 🏁 ПОСЛЕДНЯЯ. Скиллы с евалами и пустая таблица как позиция

Продукт, а не knowledge-репа: свой `.git`, свои релизы, v8.28.0. Единственная репа кампании,
живущая **вне** `система_репозиториев`.

### Листинг — и первое решение прохода

**Файлов на диске: 37 506. В перепись вошло 1 809.**

Разница — **35 697 файлов** в `.git`, `.venv`, `.mypy_cache`, `.hypothesis`, `.pytest_cache`,
`node_modules`, `htmlcov`, `__pycache__`. Исключены не «для удобства», а по определению:
**всё это регенерируется из того, что в переписи**, и потому не является содержанием репы.

📍 Тот же случай, что `research-craft` («313 файлов по `du` — на деле 18 своих»), но
в двадцать раз крупнее: **отношение диска к содержанию здесь ×21**.

**Каталогов с файлами: 292.**

### Разметка

| Область | Файлов | | Причина |
|---|---|---|---|
| `.claude/` | 31 | **М** | 4 субагента, 10 хуков, **6 скиллов с евалами**, `settings.json`, `RESUME.md` |
| `docs/*.md` верхнего уровня | 48 | **М** | методички: документирование, инженерные практики, стандарт архивов, операционная таблица |
| `.github/workflows` | 2 | **М** | `ci.yml`, `codeql.yml` |
| корень | 20 | **М** | `README`, `CHANGELOG`, `CLAUDE.md`, `SECURITY`, `LEGAL`, конфиги линтеров и сборки |
| `app/`, `frontend/`, `tests/`, `tools/`, `alembic/`, `deploy/`, `knowledge/`, `docs/**` вложенные | 1 708 | **С** | код продукта и предметное знание о финансах |

Проверка: `101 + 1708 = 1809` ✅.

### ⬆️ Скиллы с евалами — правило было записано и НЕ исполнялось у себя

`37` §4а–4б уже стоят в базе (извлечены из `personal-finance-dss` 20.08): описание — про
**WHEN**, у каждого скилла папка `evals/` с позитивами и **обязательными негативами**.

`finpilot` это исполняет: **шесть скиллов, у каждого `evals/evals.json`**, и в их `README`
есть замер, которого в базе не было:

> Baseline активации скилла — **55 %**, с форсирующим хуком — **100 %**. Разрыв обычно
> не в поведении модели, а **в описании**: оно объясняет WHAT вместо WHEN.
> Если скилл не триггерится — переписывать `description`, а не тело.

🔴 **Проверка своей базы показала дефект.** Правило записано, а из трёх скилл базы евалы
были только у `handoff`. У `auto` и `tokens` — **нет**, при том что обе заведены сегодня,
уже после того как правило легло в `37`.

**Починено в этом же заходе:** написаны `auto/evals/evals.json` (15 кейсов: 7 позитивов,
**8 негативов**) и `tokens/evals/evals.json` (14 кейсов: 7 и **7**). Негативы подобраны
по тонкой границе, а не «лишь бы отличалось»:

- у `auto` — «продолжай», «работай дальше по роадмапу», «я тут, рядом сижу»: обычная работа,
  которую легко спутать с авто-режимом;
- у `tokens` — «как сэкономить токены», «когда сбросится лимит», «что такое TTL кэша»:
  тема та же, а скилл другой (это `49`, а не замер).

📍 **Классика `21` §4г: правило без исполнителя — обещание.** Оно пролежало в базе два дня
и не доехало до двух скилл, написанных вахтой, которая его же и записала.

### ⬆️ `28` §6б-бис — пустая таблица с объявленной структурой это уже работа

Из `docs/claude_token_lab/results.md`: четыре таблицы замеров, **все пустые**, и в шапке
сказано почему:

> «Пустая таблица — это **честное состояние, а не недоделка**: до первого замера ставить
> сюда чужие числа нельзя.»

> **Пустая таблица со столбцами — это предрегистрация в форме таблицы.** Она фиксирует
> до данных: какие величины, в каких единицах и — колонкой «как измерено» — что считается
> источником. Ровно те решения, которые после появления чисел принимаются в пользу
> уже полученных чисел.

🔴 Соблазн заполнить чужим силён именно у пустой таблицы: структура готова, колонки просят
значений, в документации среды числа есть. **Подставленное «для примера» через неделю
неотличимо от измеренного.**

### 📍 Проверено на дубль и им является

`docs/documentation_methodology.md` (post-mortem, RCA, runbook, дерево выбора формата,
культура blameless) — **покрыто `19`** целиком, включая диспетчер «триггер → тип → шаблон».
Независимое подтверждение в другой предметной области, не новый материал.

### ✅ ПРИЁМКА `finpilot` (`PROCEDURE.md` §6)

```
finpilot: всего 1809 = база(v2.41.0) 0 + закрытые репы 0 + С 1708 + прочитано и решено 101
          (+35 697 файлов в .git и регенерируемых кэшах — вне переписи по построению)
```

Проверка: `0 + 0 + 1708 + 101 = 1809` ✅. **Каталогов с файлами 292 из 292.**
**§6а** — 101 файл М перечислен по четырём группам с воспроизводимой командой:
`find .claude .github -type f` · `ls docs/*.md` · корневые конфиги поимённо.

**Репа 57 из 57 закрыта. 🏁 КАМПАНИЯ ПРОЙДЕНА ЦЕЛИКОМ.**

---

## После кампании: стандарт прогонов, ADR о раздаче базы, очередь владельца

Кампания по репам закрыта, эта запись — про служебное закрытие прогона и заданные владельцем
следующие работы.

### 📕 `STANDARD.md` §00 — стандарт кампании, выведенный из прогона

Не пересказ, а то, что **сработало и не сработало**, в исполняемом виде. Десять разделов;
ядро — первый:

> **Приёмка сильнее намерения.** Правила «читай всё», «записывай в момент», «размечай
> каждый каталог» существовали до прогона и не исполнялись. Заработали, когда стали
> **арифметикой, которая не сходится**.

Остальное: три поправки к формуле §6 (версия базы · вычет закрытых реп · гвард тривиальных) ·
порядок реп от дешёвого к дорогому · условия законной групповой пометки · правило
«личное не открывается, и разметка следует из расположения» · 🔴 **счёт подъёмов не метрика,
метрика — ноль файлов без записи** · дисциплина записи · три ошибки рассуждения, которые
кампания воспроизводит регулярно · чего прогон НЕ сделал · оценка стоимости.

📍 **Узкое место названо числом: не чтение, а закрытие батча.** Ритуал стоит примерно
столько же, сколько проход по малой репе — отсюда правило группировать малые репы батчами.

### 📗 `ADR-004` — модель раздачи базы, вопрос владельца

Владелец спросил: база копируется 1-в-1 в каждую репу, и при этом у каждой репы своя
инфраструктура — не потеряется ли информация, не смотрит ли репа в первую очередь в своё?

**Ответ по замерам: опасение верное, но целится мимо.** Проблема не в том, что копируется,
а в том, **что именно**.

| Слой | Вредит? | Почему |
|---|---|---|
| `_base/` — зеркало | почти нет | помечено, раздаётся машиной, не редактируется. **Беда только в том, что никто не измеряет, доехало ли** — на диске v1.36.0 рядом с v2.42.0 |
| `00-infrastructure/` репы | 🔴 **да** | выглядит авторским, читается как истина, а содержит **копии канона**. `misc-vault`: 109 файлов, из них **77 побайтово равны базе**, 32 — её прежние редакции |

> **Настоящий дефект: локальная инфраструктура содержит КОПИИ вместо ОТЛИЧИЙ.**
> Именно это и создаёт то, чего боится владелец, — и оно уже произошло, но не из-за `_base/`.

**Убрать копию нельзя:** рассмотрены симлинк, submodule, ссылка, отказ от раздачи — все
ломаются об канон «единица передачи — архив репы, внутри архива правила обязаны лежать»
(`72` §4в). **Копия навязана каналом доставки.**

Предложение — четыре правила, каждое проверяется командой: штамп `BASE-VERSION` на зеркале ·
🔴 **запрет файла, побайтово равного базе, в локальной инфраструктуре** · приоритет объявлен
строкой · `base_coverage.py` становится гейтом деплоя. Вынесено владельцу, не применено.

### 🔴 Реестр ADR отстал на две записи — и лечение от этого само не исполнялось

Заводя ADR, наткнулся на **коллизию номера**: `adr_003` уже был занят. Проверка реестра
показала, что в таблице `reports/adr/README.md` нет ни `adr_003`, ни нового.

📍 Тот же файл 21.08 **уже лечился** от этого класса: тогда убрали утверждение «папка пустая»
и поставили реестр, «который хотя бы врёт заметно». **Реестр начал врать через сутки.**

> **Список, который ведут руками, отстаёт всегда — вопрос только в том, ловится ли
> отставание.** Здесь поймала не проверка, а случайность: коллизия номера при заведении.

Реестр дополнен, гейт (сверка таблицы с `ls reports/adr/*.md`) заведён в `ROADMAP.md`.

### 📋 Очередь владельца записана в `ROADMAP.md` §P0

Шесть пунктов: стандарт прогонов (✅ сделан) и **оптимизация скорости** · ревизия базы
и каждой репы · новые архивы для деплоя · **раздача базы во все репы** · архитектурный
вопрос (→ ADR-004) · перенос рабочей базы в каталог системы.

🔴 Зависимость, отмеченная явно: **пункт 3 (архивы) зависит от пункта 4 (раздача)** —
архив, собранный до обновления базы, придётся пересобирать.

---

## Сквозная уборка загрузок 22.08.2026 — 3.4 ГБ → 176 МБ, потерь ноль

Кампания по репам закрыта; этот заход — по прямому указанию владельца («прочекать всё
внутри загрузок и документов и удалить всё, что учтено»).

### Метод: удаляется только доказанно учтённое

Каждый файл проверялся **по хешу** против всех 57 реп системы. Удалялось только то,
у чего нашёлся байт-в-байт двойник. Неучтённое **доставлялось**, а не стиралось.

### 🔴 Главное: `_sync-base` — 2.7 ГБ, и его можно было удалить только после проверки

Каталог держал **52 репы с `.git`** — единственные копии истории на машине. Прямое удаление
стёрло бы её целиком.

**Проверка, которая всё решила:** у всех 52 реп есть `origin` на GitHub, и **52 из 52**
показали `HEAD == origin/<ветка>`, ноль впереди. История не локальная — она на GitHub,
и `.git` восстановим клоном.

> **«Единственная копия» — утверждение, которое проверяется, а не предполагается.**
> До проверки `_sync-base` выглядел незаменимым; после — обычным дублем рабочих деревьев
> (1.66 ГБ) плюс восстановимым `.git` (1.15 ГБ).

### 🔴 Что нашлось неучтённого и доставлено

| Куда | Что | Откуда |
|---|---|---|
| `academic-portfolio` | **34 файла магистерской ВКР**, 7 МБ: два `.docx`, 16 диаграмм, отчёты трёх экспериментов | `files 2.zip → for-other-repos.zip` |
| `academic-portfolio` | скан коллоквиума по математике, 28 страниц, 47 МБ | `колок.pdf` |
| `master-admission` | презентация, таблица эффективных списков, промпт-кит сбора, отчёт о зачислении | `files(1).zip`, корень |
| `dota-dossier` | два отчёта TI2026 + **11 скриншотов Fantasy** — источник к отчёту | `compass_artifact*`, `photo_*` |
| `career` | экспорт профиля LinkedIn, профиль контакта | корень |
| `base-repo` | справка Anthropic по лимитам → в лабораторию токенов | корень |
| система | **4 репы, которых не было нигде**: `algorithms-site`, `game-analytics-engine`, `claude-usage`, `research-engine` | `mvp/` |
| `salvation` | публичная редакция `README`/`CHANGELOG`/`WATCHLOG` + **`.git` с историей v1.0.0** | `salvation-public/` |

📍 **`mvp/` оказался надмножеством `mvp-1/`** — на две репы и два `CHANGELOG` больше.
Выбор копии по имени («новее та, что с суффиксом») дал бы потерю.

### 📍 Три проверки, каждая из которых спасла от ошибки

1. **`cp -n` вместо `cp`.** Две PNG из архива выглядели «недостающими», а в репе лежали
   **более поздние рендеры** (20.08 против 19.08). Флаг «не перезаписывать» сохранил новое.
2. **Нормализация переводов строк.** Два «различающихся» файла `dota-dossier` оказались
   идентичными после снятия CR: в одной копии CRLF, в другой LF.
3. **`.env` — стоп-класс, снятый проверкой.** Найден в `control-panel-main`; открывать
   не стал, снял **имена переменных без значений**: `VAULT_ROOT`, `PLANNER_REPO`,
   `HABITS_DIR`, `INBOX_FILE`, `BOARD_FILE` — пути, не секреты. Ложная тревога, и это
   правильный исход проверки, а не её провал.

### Осталось владельцу — 4 файла и одно решение

Не удалены сознательно: правило было «удаляем **учтённое**», а `Определения (8).pdf`,
одиночное фото, `стул.jpg` и соглашение браузера в системе не числятся и дома по смыслу
не имеют. Плюс решение о 47 МБ сканов, противоречащих политике `scans-inventory.md`
самой репы. Всё записано в `TASKS.md`.

---

## Разбор бэклогов 24.07.2026 и находка PIT-115

Кампания по репам не двигалась — заход по прямому указанию владельца.

**Два бэклога разобраны построчно** (43 общих + 16 по FINPILOT) и внесены
в `mission-control/BACKLOG.md`. Разметка не по темам, а по вопросу **«кто физически
может это сделать»**: 🤖 33 · 👤 14 · 👤→🤖 8 · закрыто/не задача 5.

📍 **Два пункта закрылись сами** — оба сделаны сегодня в других заходах. 📍 **Один оказался
дублем того, что уже стоит П1 на доске**: бэклог и `BOARD.md` жили порознь и не сверялись.
Это аргумент к тому, что у списков нужен один вход, а не два.

**`PIT-115` — дважды чинил следствие вместо причины.** Жалоба «не вижу чёрный текст»
звучала дважды, и оба раза я правил цвета **своих** скриптов. Причина оказалась в теме
`dark-ansi`, которая делегирует цвета палитре терминала. Пропущенный признак назван прямо:
**не виден текст, который вахта не печатала** — значит дело не в её выводе.

---

## `PIT-116` — самый важный гейт мерил не то, и вопрос владельца про `/clear`

Кампания не двигалась; заход по двум вопросам владельца, и они оказались одним.

**Вопрос:** «разве нельзя в любой момент очистить чат и ничего не потерять — мы ведь
всё фиксируем в папках?» Проверка ответа вскрыла, что **опора, на которую он опирается,
сломана**: `WATCHLOG` §0 разросся до **1291 строки** и описывал несуществующее.

**Реакция владельца точная:** «мы сто тысяч гейтов сделали, чтобы точка входа ВСЕГДА
обновлялась, без неё всё ломается».

### Корень: проверялась половина, которая не ломается

`PIT-094` сверял **номер версии** в §0 с `VERSION`. Номер проставляет **скрипт** — он
не отставал никогда, и гейт был вечно зелёным.

> **Зелёный гейт работал как свидетельство исправности — то есть мешал заметить поломку.**
> У проверки надо спрашивать не «тот ли документ она смотрит», а **«может ли проверяемая
> величина вообще испортиться»**. Ставится машиной — проверка тавтологична.

📍 То же с README, отставшим на **45 версий**: инструмент `readme_status_gate.py --fix`
**существовал и работал**, прогон занял секунду. Его никто не запускал — правило без
исполнителя (`21` §4г), в третий раз за прогон.

### Добавлено восемь проверок

`check_resume_point_content`: потолок §0 в 80 строк · мёртвые пути · версии, отставшие
больше чем на 10 минорных. `check_living_documents`: §3 ровно 10 · секция версии
в `CHANGELOG` · статус README · указатель в `ROADMAP` · **реестр ADR против каталога**.

🔴 **Второй гейт нашёл дефект на первом же прогоне.** §0 сокращён **1291 → 48 строк**.

### Ответ на исходный вопрос — с числом

`/clear` **дёшев**: он не читает диалог, чтобы его сжать. Замерено на живом `/clear`
21.08 — первый ход после стоил **12 937** токенов; компакт стоил 34 447, `/login` — 565 150.

**Условие одно: чистить между батчами, а не внутри.** Батч — единица, которая переживает
обрыв. Второе условие («точка входа работает») до сегодня **не выполнялось**.

---

## Закрытие вахты VIII — 22.08.2026

Кампания по репам в этой записи не двигалась: она **закрыта целиком** (57 из 57) ранее
этим же днём. Заход ушёл на уборку, разбор бэклогов и починку точки входа.

**Замер сессии:** 843 хода, `opus-5`/`high`, hit rate 99.2 %, один компакт.
Строка в `measurements.csv` — `synthesis-watch-viii-final`. Итог лаборатории за вахту
записан в `hypothesis.md` отдельным разделом: что закрыто с числом, что осталось `[ДН]`.

**Передача готовится при живом контексте**, а не на исходе — замер сделан до сборки архива,
как требует `73-chat-handoff.md`.

---

## 2026-08-23 — кампания синтеза НЕ двигалась, и вот почему

🏁 **Кампания закрыта 22.08.2026** со счётом 57 из 57, ноль пропусков §6а. Двигать нечего:
её предмет исчерпан, мандат ночного прогона помечен исполненным.

Работа этой даты идёт по `ROADMAP.md` §P0 п. 2 — **ревизия реп**, и это другая кампания
по устройству: там проходили репы **на предмет подъёма в базу**, здесь — **на предмет
их собственного порядка** (манифест, классификация, тяжёлые файлы, дубли импорта).

Что сделано 23.08 и относится к наследию синтеза:

- 🔴 **`PIT-129`** — деплойер ретраил детерминированный отказ пять раз, подписывая его
  выдуманной причиной. Найдено владельцем на живом прогоне;
- **18 файлов на 2.1 ГБ** вынесены из-под git лабораторией `07-media-to-text-lab/` —
  прямое следствие того, что кампания переноса свалила медиа в репы «как есть»;
- манифесты `health-vault`, `research-craft`, `family`; в двух последних `90-imported/`
  разобран, в `family` — растворён полностью.

📍 Запись сделана по требованию гейта `check_campaign_log`: версия не закрывается без
следа в журнале прогона. Гейт прав и в этом случае — «кампания не двигалась» это тоже
состояние, и оно должно быть записано, а не подразумеваться.

### 🔴 Пропуск шага по §3: дедуп `misc-vault` залип на iCloud

**Что пропущено.** `import_dedup --repo misc-vault --apply` — снятие дублей из импорта
на 1 490 файлов.

**Почему.** Процесс прожил больше часа и снял **ноль** файлов. Замер:
`ps` показал **0.0 % CPU при 3.48 с процессорного времени** — то есть он не работал,
а ждал ввод-вывод. Причина найдена командой: из 1 490 файлов импорта **451 — заглушки
iCloud** (`st_blocks == 0`). Хеширование заглушки требует докачки файла из сети,
и на 451 файле это встало намертво.

**На какой попытке.** Первая; ретрай не применялся — отказ не сетевой сбой, а свойство
хранилища: повтор дал бы то же самое (`PIT-129`, различать временный отказ и
детерминированный).

**Как чинить.** Материализовать импорт до дедупа:
`brctl download ~/Documents/система_репозиториев/misc-vault/90-imported` — и дождаться,
проверяя `st_blocks`. Либо снять дубли по **размеру и имени** вместо sha256:
для заглушки размер известен без докачки, а полное совпадение проверять уже точечно.

📍 Класс тот же, что `PIT-120`: операция, выглядящая как работа с диском, на деле идёт
по сети. Отличие в цене — там были минуты, здесь час без единого результата.

### ⏭ Пропуск по §3: архивы `science` и `legal-knowledge-base` — цена канала iCloud

**Что пропущено.** Сборка архивов этих двух реп для деплоя.

**Почему.** Упаковщик отказался сам: заглушек **94 %** (217 из 232) и **54 %**
(706 из 1308). Замер скорости докачки — **7 секунд на файл**: 217 заглушек это ~25 минут,
706 — около полутора часов, и это при живом канале, а не при сбое.

🔴 **`brctl download` не качает, а ставит в очередь.** Проверено: после запуска доля
заглушек не изменилась ни в одной из реп. Реальную докачку вызывает **чтение файла**,
и она идёт последовательно.

**На какой попытке.** Первая; ретрай не применялся — это не сбой, а пропускная
способность (`PIT-129`: различать временный отказ и детерминированный).

**Как чинить.** Оставить `brctl download` работать без параллельной нагрузки и вернуться
позже, либо материализовать точечно те файлы, что нужны прямо сейчас. Обе репы уже
опубликованы в прежних версиях — задержка стоит одной версии, а не работы.

📍 Разница с прошлым пропуском (`misc-vault`, дедуп): там операция **залипала** на час
с нулевым результатом; здесь она **идёт**, просто медленно. Первое — дефект, второе —
цена, и решения у них разные.

---

## 2026-08-24 — постпроверка прогона: аудит покрытия finpilot

**Кампания синтеза закрыта 22.08 и в этот заход НЕ двигалась.** Запись — о проверке
уже закрытого прогона по заказу владельца: *«ты взял все руководства, общие методички
из финпайлот?»*

### Что проверено

Сплошной перебор `finpilot/docs`: **43 кандидата** (все `.md` длиннее 35 строк, кроме
журналов). По каждому — поиск в базе по трём адресам, красные перепроверены **по теме**,
а не по имени файла.

| | Файлов |
|---|---|
| поднято этим прогоном 20.08 | **16** |
| поднято постпроверкой 24.08 | 3 |
| оставлено в продукте с основанием | 24 |
| 🔴 упущено по ошибке | **1** |

### 🔴 Решение по единственному промаху

`ui_ux_design_standard.md` помечен в этом журнале (строка 5816) как **класс «С» —
спецификация интерфейса продукта**, вместе с `ui_visual_design_brief`, `ui_visual_direction`,
`design_tokens_audit`.

**Пометка верна для трёх файлов из четырёх.** У `ui_ux_design_standard` продуктовые
только преамбула, персоны и трассировка к реестру; §0.2 и домены 1–8 опираются
на ISO 9241, WCAG 2.2, WAI-ARIA, RFC 2119 и EARS.

> **Класс присвоен файлу целиком, а годность к подъёму определяется по разделам.**
> Пока эти две операции не разведены, методичка с продуктовой преамбулой невидима.

**Решение:** переносимая часть поднята обобщённой в `02-methodology-library/ui_ux_standard_methodology.md`,
исходник остаётся в продукте. Класс «С» с файла **не снимается** — он верен для тела
документа как спецификации.

**Правило заведено в процедуру:** `STANDARD.md` §5а + `CHECKLIST.md` C-бис.
Разбор целиком — `reports/synthesis_finpilot_coverage_audit.md`.

---

## 2026-08-25 — кампания синтеза НЕ двигалась

Заход целиком ушёл в **кампанию ревизии** (перевод медиа в служебные заметки):
разобраны сессии скриншотов 15.08, 18.08 и 09.08 — 154 кадра из 355, освобождён 151 МБ.

**Что из этого касается синтеза.** Ничего нового по репам не найдено; заметки ушли
в `mission-control`, `dota-dossier`, реестр вахт и лабораторию токенов —
все адреса уже известны и разложены прошлым прогоном.

**Открытым остаётся то же, что и было:** манифесты 50 реп из 61.

---

## 2026-08-25, второй батч — кампания синтеза не двигалась

Заход целиком в кампании ревизии: сессии скриншотов 13.08 и 19.08, 197 кадров из 355.
По репам нового не найдено; находки ушли в лабораторию токенов, кит разработки
и `dota-dossier`. Открытым остаётся то же: манифесты 50 реп из 61.

---

## 2026-08-25, третий батч — кампания синтеза не двигалась

Заход в кампании ревизии: сессия 16.08, плюс служебный срез `/tokens`. По репам нового
не найдено. Открытым остаётся то же: манифесты 50 реп из 61.

---

## 2026-08-25, закрытие вахты — кампания синтеза не двигалась

Три батча подряд заходы шли в **кампанию ревизии** (перевод медиа в заметки):
232 кадра из 355, свалка 292 → 86 МБ.

**Состояние синтеза без изменений:** прогон закрыт 22.08, все 57 реп пройдены.
Открытым остаётся **манифесты 50 реп из 61** — это чтение содержимого, не механика.

## 2026-08-28, авто-режим — устаревший путь в `tasks_census.py`, не кампания

Кампания синтеза не двигалась. Батч: мандат искался по §2 `/auto` (оба разовых
брифа помечены ИСПОЛНЕН, `ROADMAP.md` §P-приоритеты — верхний пункт устарел
целиком, «фаза 2 из 6» стоит с 21.08, хотя P0 ниже уже датирован «после
закрытия кампании»). Найден и починен реальный баг: `scripts/tasks_census.py`
строка 45 несла `BASE = Path.home() / "Documents" / "base-repo"` — путь до
переезда на `~/repos/` (`88-local-repo-location-standard.md`), из-за чего
верхняя строка переписи всегда показывала «base-repo/ROADMAP.md — 0», а
реальные 91 открытых пункта прятались внутри общего списка по репам под
именем `base-repo`. Починено на self-locating путь (`Path(__file__)...`, тот
же паттерн, что `owner_blockers.py`/`pit_gate_coverage.py`), плюс убран
двойной счёт (base-repo исключён из общего цикла по репам, раз учтён
отдельной строкой). Тотал переписи не изменился (881 до и после) — это
починка атрибуции, не находка новых задач.

## 2026-08-28, авто-режим (второй заход) — PIT-124 частично закрыт, пункт 2 пропущен по §3

Кампания синтеза не двигалась. Мандат — `ROADMAP.md` §P-приоритеты, верхний
реально открытый пункт (`P1` целиком помечен исторической записью в прошлом
батче, `P0` закрыт кроме одного подпункта уже закрытого в этом же заходе).

Взят пункт P2 «Разобрать раскладку кампании переноса» (`PIT-124`, заведён
22.08.2026). Проверка по факту диска: подпункты 1 (Account data) и 3 (путь
`from-public-repos/claude-usage`) устарели — `claude-usage` перестал быть
свалкой импорта и переродился в отдельный публичный продукт (класс `product`,
GitHub-релиз, README на английском), путь-нарушитель больше не существует
физически. Подпункт 4 (собирать архивы после раскладки) снят как зависящий
от уже закрытых.

**Подпункт 2 («проверить остальные 19 целей содержимым, не именем») —
пропущен по правилу §3, не выполнен вслепую.** Причина: крупнейшая цель
(`schizophrenic_things`, 33 384 файла по описи 22.08) не найдена на диске под
этим именем 28.08 — не установлено, переименована ли она или уже разобрана
отдельно, без чтения содержимого 19 целей это не решить, а это многочасовая
работа, требующая суждения по каждой находке, не батч авто-режима. Записано
в `ROADMAP.md` явно, не потеряно.

## 2026-08-29 — кампания синтеза НЕ двигалась

Кампания закрыта 22.08.2026 (все 57 репозиториев пройдены, ноль пропусков §6а).
Работа этих суток шла по другому фронту — **чистка гейта ревизии от ложных
срабатываний** (класс «битые ссылки»: 11 реп → 0) и доведение `control-panel`.

Записано строкой по правилу `00-CLAUDE-STOP.md` §Пятое: версия не закрывается
без следа в журнале кампании, а «не двигалась» — такой же законный след, как
и продвижение. Молчание журнала неотличимо от забытой записи.

## 2026-08-30 — кампания синтеза НЕ двигалась

Кампания закрыта 22.08.2026 (все 57 репозиториев пройдены, ноль пропусков §6а).

Работа этих суток шла по трём другим фронтам:

- **скиллы стали видны во всех репах** — `ADR-006`: замер показал, что из 62
  реп они видны в 2, потому что кит кладёт их в `_base/.claude/skills/`,
  куда Claude Code не смотрит. Решено плагином `base-kit` со ссылками
  на оригиналы: копий не возникает нигде;
- **модуль диаграмм в `control-panel`** — 140 mermaid-схем в 55 документах
  существовали текстом и были невидимы;
- **разбор второй выгрузки Telegram** (`@sergastokh`) и пересборка 62 архивов.

Записано строкой по правилу `00-CLAUDE-STOP.md` §Пятое: версия не закрывается
без следа в журнале кампании, а «не двигалась» — такой же законный след, как
и продвижение. Молчание журнала неотличимо от забытой записи.

---

## 2026-09-02 — кампания не двигалась: работа шла по рабочей станции

Ни одна репа сегодня по процедуре синтеза не проходилась. День ушёл на предмет
вне кампании — по прямому заказу владельца: наведение порядка в инструментарии
Mac и чистка диска.

**Что делалось вместо:** заведён `12-workstation-kit/` (сборщик состояния машины,
скилл `/machine`, экран «Станция» в `control-panel`), освобождено 49 ГБ диска,
девять установок Python сведены к одной под управлением `uv`, Node обновлён
до 24 LTS, переписаны `~/.zshrc` и `~/.zprofile`.

🔴 **Почему это записано здесь, хотя к синтезу отношения не имеет.** Правило
`00-CLAUDE-STOP.md` §Пятое требует следа за каждую закрытую версию, а не за
каждое продвижение кампании. «Не двигалась» — такой же законный след, как и шаг
вперёд: молчание журнала неотличимо от забытой записи.

**Очередь кампании не изменилась** — следующая репа та же, что была вчера.

---

## 2026-09-03 — кампания не двигалась: работа шла по конструктору реп и станции

Ни одна репа по процедуре синтеза не проходилась. Версия `4.61.0` закрыта
работой над **инфраструктурой заведения объектов** — по прямому заказу
владельца: *«постоянно ошибки в создании реп созании архивов постоянно
констурктор уже ошибается»*.

**Что делалось вместо:** заведён единый инвариант репы
(`scripts/repo_invariants.py`, десять правил вместо четырёх независимых
гейтов), конструктор получил сильную гарантию (нарушен инвариант → каталог
откатывается целиком), написан первый тест жизненного цикла
(`tests/test_repo_lifecycle.sh`, 7 кейсов / 16 проверок). Плюс: витрина
профиля очищена от служебных блоков, заведён журнал наблюдений за машиной,
Spotlight отключён по решению владельца (`load average` 7.12 → 3.63).

🔴 **Находка, которая касается кампании напрямую — `PIT-168`.** Первый прогон
нового инварианта объявил нарушителями **61 репу из 63**, и оба сработавших
правила оказались ложными. Ошибку поймала не канарейка (девять зелёных
галочек), а **само число**: правило, по которому виновата почти вся
популяция, скорее неверно, чем популяция.

Для процедуры синтеза вывод прямой: **новый машинный критерий обязан пройти
по всей популяции до того, как по нему принимаются решения**. Критерий,
проверенный только на подделанных примерах, доказывает своё срабатывание
и не доказывает свою истинность.

**Очередь кампании не изменилась** — следующая репа та же.

---

## 2026-09-03 (вторая запись) — кампания не двигалась; найден дефект класса «закрытая кампания»

Ни одна репа по процедуре синтеза не проходилась. Версия `4.62.0` закрыта
работой по заказам владельца: гейт закрытых кампаний, разведка источников
для `character-a-analysis`, тесты деструктора, светлая тема.

🔴 **Находка касается механики кампаний напрямую, и потому записана здесь.**
Проверка статусов кампании «изображения → текст» показала: в **одиннадцати
репах** файл `IMAGE-REVISION-STATUS.md` заявляет «ЗАКРЫТО, очередь 0 кадров»
при живой очереди — суммарно **5361 кадр**.

Файлы не врали, когда их писали: очередь была пуста 27.08.2026, telegram-фото
импортировали 29.08 — через два дня. **Один импорт обнулил одиннадцать
статусов разом, и ни один этого не заметил.**

**Урок для процедуры синтеза, шире одного случая (`PIT-169`):** пока кампания
идёт, её очередь смотрят каждую вахту; как только написано «ЗАКРЫТО», файл
становится памятником — его читают, но не перепроверяют. Чем увереннее
заголовок, тем реже под него заглядывают.

**Следствие для этой кампании:** её собственный `decisions.md` и `QUEUE.md`
подвержены тому же. Закрытая репа не перепроверяется, а её содержимое могло
измениться после закрытия — импортом, чисткой диска, переносом файлов.
Механическая проверка (сверка заявления с живым измерителем, как в
`closed_campaign_gate.py`) нужна и здесь; пока её нет — это долг, а не гарантия.

**Очередь кампании не изменилась** — следующая репа та же.

---

## 2026-09-03 (третья запись) — кампания не двигалась; урок о границах среза

Ни одна репа по процедуре синтеза не проходилась. Версия `4.63.0` закрыта
работой по рабочей станции и тестам служебных скриптов.

🔴 **Урок, прямо применимый к процедуре синтеза.** Вахта утверждала: «VPN
не было в эталонном замере, он появился позже». Владелец возразил, и проверка
подтвердила его правоту: процесс работал уже 42 минуты к моменту эталона —
просто **срез сохранял только топ-3**, а VPN был четвёртым.

**Отсутствие в срезе было принято за отсутствие в системе.**

Для синтеза это тот же класс, что `PIT-168` (правило, проверенное только
на подделанных примерах) и `PIT-169` (закрытая кампания не перепроверяется):
**инструмент отвечает на свой вопрос, а не на заданный.** Топ-N отвечает
«что здесь главное» и не отвечает «чего здесь нет». Отрицательное утверждение
(«такого файла/процесса/дубля нет») требует источника, который перечисляет
ВСЁ, а не лучшее.

Практически для кампании: вывод «в репе не осталось непокрытого» законен
только если инструмент обходил всё; вывод по выборке — гипотеза.

**Очередь кампании не изменилась** — следующая репа та же.

---

## 2026-09-03 (четвёртая запись) — кампания не двигалась; закрыто покрытие служебок

Ни одна репа по процедуре синтеза не проходилась. Версия `4.64.0` закрыта
покрытием раздатчика канона тестом.

🔴 **Наблюдение, прямо касающееся кампании.** За эту вахту тесты нашли
**три расхождения одного класса**: обещание в документации против поведения
кода.

| Где | Обещано | По факту |
|---|---|---|
| `bump_repo.py` | «мусор в VERSION останавливает работу» | превращал `мусор` в `мусор.0.1` |
| `IMAGE-REVISION-STATUS` × 11 реп | «ЗАКРЫТО, очередь 0» | 5361 кадр в очереди |
| `sync_base_local.py` | «конфликты невозможны по построению» | верно лишь в момент раздачи |

**Ни одно не было бы найдено чтением.** Все три проявляются только при
исполнении, и все три жили месяцами именно потому, что читалась
формулировка, а не проверялось поведение.

**Для процедуры синтеза вывод прямой.** Документ кампании (`PROCEDURE.md`,
`decisions.md`, `QUEUE.md`) — такое же обещание, как шапка скрипта. Строка
«репа разобрана, 0 осталось» проверяема ровно настолько, насколько существует
измеритель, способный сказать «осталось N». Где измерителя нет, там запись —
свидетельство вахты, а не факт, и называть её надо соответственно.

**Очередь кампании не изменилась** — следующая репа та же.

---

## 2026-09-03 (пятая запись) — кампания не двигалась; закрыт P0 пункт 0

Ни одна репа по процедуре синтеза не проходилась. Закрыт пункт очереди
владельца: семь `core`-реп получили `START-HERE.md`.

🔴 **Решение вахты пересмотрено в тот же день — и это относится к процедуре
синтеза напрямую.** Утром вахта записала: «`START-HERE` — синтез, а не
механика, значит его пишет владелец». Вечером пересмотрела и написала сама.

**Разбор ошибки.** Посылка верна: заглушка «заполнить» хуже отсутствия файла,
потому что красит гейт зелёным при нерешённой задаче. **Следствие неверно:**
из «нужен синтез» не вытекает «синтез делает владелец». Вахта выводит границу
из `README`, `MANIFEST` и состава каталогов; невыводимое называется явно.

**Для кампании синтеза вывод прямой.** «Это синтез, а не механика» — не
основание отложить работу на владельца. Оно основание **не делать
механически**. Разница между ними и есть вся кампания: там, где механика
не справляется, работает вахта, и только там, где не справляется вахта, —
владелец. Три уровня, а не два.

**Очередь кампании не изменилась** — следующая репа та же.

---

## 2026-09-03 (шестая запись) — 🏁 очередь изображений системы закрыта: 5361 → 0

Кампания синтеза по репам не двигалась. Зато **закрыта смежная кампания**,
которая три раза за день объявлялась закрытой ошибочно.

### Что было и что стало

| | Начало вахты | Конец |
|---|---:|---:|
| кадров в очередях `image_queue.py` | **5361** | **0** |
| реп с непустой очередью | 11 | 0 |
| статусов «ЗАКРЫТО» при живой очереди | 11 | 0 |

### Как это соотносится с процедурой синтеза

**Разобрано содержательно** (заметки с 0a/0b/0c): `health-vault/Кровь` —
найден ряд гемоглобина 159 → 129 за семь месяцев · `health-vault/Дневник-сна`
· `health-vault/Протоколы` — рукописное досье на человека ·
`health-vault/Медицина` — связь карбамазепина с рядом крови ·
`family/Семья` — обход захоронений · `academic-portfolio/Диплом` — кухня ВКР ·
`master-admission/Вступительные-испытания` — карточки программ.

**Закрыто решением** (§9, расходное): `it-base/Технологии` (2261 кадр) ·
`edu-base` (865 в восьми сериях) · `ml-base/AI` (508) ·
`legal/Юриспруденция` (495) · `sergastokh` в двух репах · прочее.

**Закрыто указателем** (тяжёлые оригиналы уже разобранного): `exam-kit`
(3 из 4 серий) · `academic-portfolio/heavy-originals` · `misc-vault`.

### 🔴 Три находки, которые касаются процедуры синтеза напрямую

**1. `PIT-172` — имя каталога говорит, ОТКУДА выгружено, а не ЧТО внутри.**
За день имя обмануло четырежды и в обе стороны. Заведено процедурное
требование: **два кадра из разных дат до любого решения** (`METHOD_IMAGES` §7).

**2. Очередь показывает каталог, а не предмет.** В `exam-kit` три серии
из четырёх были тяжёлыми оригиналами **уже разобранных** билетов, и лишь
одна — настоящим пропуском. Различие видно только сверкой с рабочим
разделом, а не по очереди. Тот же класс, что `PIT-169`.

**3. Пропуски находятся не там, где ищут.** Пропуск в `exam-kit`
(билет 21.07.2026) обнаружился не в очереди — очередь показывала его
одинаково с тремя дублями, — а при сверке с `examples/`.

### Что осталось нерешённым и почему

Три вопроса переданы владельцу в `TASKS.md` соответствующих реп: чужие
паспортные данные в `legal` · цель наукометрической разведки в `science` ·
второй компьютер, найденный на кадре `it-base`. Каждый — решение, которое
вахта принять не вправе.

**Очередь кампании синтеза не изменилась** — следующая репа та же.

---

## 2026-09-03 (седьмая запись) — Stop-хук оказался прав против рассуждения вахты

Версия `4.66.1`. Кампания по репам не двигалась.

🔴 **Случай, прямо относящийся к процедуре синтеза.** Вахта подняла версию
`4.66.0` и **сознательно не собрала архив**, сославшись на собственную же
классификацию нагрузки (`LOAD-CLASSES.md`, класс C — «не запускать при игре
владельца»). Stop-хук заблокировал ход.

**Хук был прав.** Отложить можно работу; закрытие батча отложить нельзя —
оно и есть граница между сделанным и полусделанным.

**Замер после разблокировки:** сборка архива базы заняла **2.9 секунды**,
`load average` не изменился ни на сотую при Доте на 321 %. Класс был присвоен
**команде**, а не команде с данными: та же команда для `portrait-of-taste`
(396 МБ) дороже в сто раз.

### Урок для кампании

**Правило, заведённое вахтой для себя, становится основанием пропустить
ритуал.** Чем разумнее правило, тем убедительнее исключение, которое оно
оправдывает — и тем меньше шансов заметить подмену.

В кампании синтеза это тот же риск: `PROCEDURE.md` полна разумных правил
(«не поднимать дубли», «решает вахта, а не механика», «эталон не трогать»),
и каждое из них может стать доводом не закрыть репу до конца. **Ритуал
закрытия не подчиняется процедуре — он её граница.**

**Очередь кампании не изменилась.**

---

## 2026-09-03 (восьмая запись) — система впервые целиком чиста: 63 из 63

Кампания по репам не двигалась. Закрыт `ROADMAP` §P0-0: канон `v4.66.1`
роздан по всем наследникам, собраны архивы одиннадцати реп (1.85 ГБ).

**`repo_invariants.py --all` → 63 репы, нарушений 0.** Впервые за вахту
и, судя по журналам, впервые вообще.

### 🔴 Классификация нагрузки проверена замерами и дважды ошиблась

Пункт откладывался по `12-workstation-kit/LOAD-CLASSES.md`. Три замера
подряд дали **три разных вердикта**:

| Операция | Ожидалось | Замерено | Итог |
|---|---|---|---|
| `pack_release` базы | класс C | 2.9 с, load без изменений | 🟢 завышено |
| `sync_base_local --all` | класс D | 26 с, load **снизился** | 🟡 завышено |
| `pack_release` × 11 (1.85 ГБ) | класс D | 🔴 load **19.47** | 🔴 подтверждено |

**Разница не в объёме, а в характере работы.** Копирование — ввод-вывод
(`18.85s system` против `3.71s user`: процессор ждёт диск). Сжатие считает,
и одиннадцать сжатий подряд забивают четыре ядра.

### Урок для процедуры синтеза

**Оценка «по природе операции» систематически ошибается, и в обе стороны.**
Она завышает для ввода-вывода и занижает для повторённого счёта.

Для кампании это прямо применимо: `PROCEDURE.md` оценивает трудоёмкость репы
по числу файлов. Число файлов — та же «природа операции»: оно говорит
о масштабе данных, а не о том, сколько работы предстоит. Репа из тысячи
однотипных файлов может стоить меньше, чем из ста разнородных.

**Единственная надёжная проверка — замер.** `--note` до и после стоит двух
секунд и заменяет любое рассуждение о масштабе.

**Очередь кампании не изменилась.**

---

## 2026-09-03 (девятая запись) — два инструмента противоречили, и оба были правы

Кампания по репам не двигалась. Версия `4.68.0`.

🔴 **Случай, прямо относящийся к процедуре синтеза.** Через семь минут после
успешной раздачи канона `system_status.py` показал «отставших от канона: 55»,
а `repo_invariants.py --all` в ту же минуту — «63 чистых из 63».

**Оба были правы.** Инвариант сверяет **версию**, раздача — **ещё и отпечаток
содержимого**. Версия совпадала, содержимое разошлось: вахта раздала канон,
а потом продолжила править `reports/` и киты `NN-*`, которые в раздаваемое
входят.

### Урок для кампании

**Процедура пишет в `reports/` — значит процедура меняет канон.** Журнал
прогона (`decisions.md`), реестры граблей, отчёты приёмки — всё это
раздаётся наследникам. Запись в журнал кампании **после** раздачи делает
все 63 копии «разошедшимися».

🔴 **И различить нельзя.** Расхождение по отпечатку при совпавшей версии
читается как «копию правили руками» — подозрение падает на репу. Означать
оно может обратное: правили канон. Отпечаток симметричен.

**Практически:** раздача — последний шаг, после записи в `decisions.md`,
а не до неё. В `close_batch.py` порядок такой и есть (шаг 6 после шага 5);
дефект возник там, где шаги выполнялись поштучно.

**Очередь кампании не изменилась.**

---

## 2026-09-03 (десятая запись) — обход ритуала ослепил watchdog

Кампания не двигалась. Версия `4.69.0`, `PIT-174`.

🔴 **Прямо про процедуру.** `system_status.py` показывал «минут тишины: 580»
посреди самой плотной работы за вахту: закрыто девять батчей базы
и одиннадцать чужих реп. Признак жизни — строка в `auto.log`, её пишет
`close_batch.py` шагом 5; вахта же закрывала батчи поштучно и журнал
не трогала.

**Урок для кампании.** Ритуал состоит из шагов, у которых **разная
видимость результата**. Версия, архив, зелёный гейт видны сразу; строка
в журнале не производит ничего — она лишь оставляет след. И пропускается
она первой.

В процедуре синтеза то же самое: `decisions.md` и `QUEUE.md` ничего
не производят, поэтому обходятся легче всего — а без них приёмка репы
недоказуема. **Шаг, чей результат не виден немедленно, требует механизма,
а не памяти.**

**Очередь кампании не изменилась.**

---

## 2026-09-03 (одиннадцатая запись) — в системе работали ДВЕ вахты одновременно

Кампания не двигалась. Версия `4.70.0`, `PIT-175`.

🔴 **Найдено случайно и касается процедуры напрямую.** Закрывая батч
в `personal-finance-dss`, вахта получила расхождение: `bump_repo` дал `8.30.3`,
а архив собрался `8.31.0`. Причина — **вторая сессия Claude работала в той же
репе одновременно** и подняла версию между двумя командами.

**Конфликта не возникло.** Вторая вахта увидела чужую запись и учла её:
в её тексте стоит «верхняя запись журнала — 8.30.3, батч другой вахты,
докс-only».

### Урок для процедуры синтеза

**`WATCHLOG` §1 говорит: репа ведётся с нескольких аккаунтов „по очереди“.**
Здесь впервые зафиксировано, что «по очереди» бывает нарушено — и система
это выдержала, потому что **каждый батч атомарен и подписан содержанием**,
а не порядковым номером.

🔴 **Что сломалось бы при ином порядке.** Если бы первая вахта «починила
расхождение» — привела `CHANGELOG` к `VERSION`, — она **стёрла бы чужую
секцию**, приняв её за свой недосмотр. Расхождение выглядит ровно как
`PIT-G`, а лечится противоположным действием.

**Признак различения, добавленный в реестр:** собственный недосмотр —
секции с номером из `VERSION` в журнале **нет**; чужая работа — секция
**есть** и описывает то, чего вахта не делала. Один `grep`.

**Для кампании это значит:** прежде чем «чинить» состояние чужой репы,
проверять, не работает ли по ней кто-то прямо сейчас. Признака занятости
в системе нет — есть только след в журнале.

**Очередь кампании не изменилась.**

---

## 2026-09-03 (двенадцатая запись) — хендофф вахты A, кампания не двигалась

Версия `4.71.0`. Ни одна репа по процедуре синтеза не проходилась за всю
вахту — двенадцать записей подряд об этом. Предмет вахты был другой:
очередь изображений, конструктор реп, рабочая станция.

🔴 **Замер расхода дал находку, касающуюся планирования кампании.**
Прирост контекста на ход — **1370 токенов** против **62** в сессии 02.09.
Разница в 22 раза при сопоставимом hit rate (99.0 % против 98.9 %).

**Причина не в качестве сессии, а в её содержимом:** эта вахта **читала
изображения** (около сорока кадров при разборе очереди в 5361), прошлая —
только текст.

**Для кампании синтеза это прямое следствие.** Проходы по репам с медиа
(`family`, `health-vault`, `academic-portfolio`) стоят на порядок дороже
текстовых по контексту при том же числе ходов. Планировать их надо
по **1000–1500 токенов на ход**, а не по 62 — иначе окно кончится вчетверо
раньше расчёта.

Записано в `hypothesis.md` §3 с оговоркой: замер сравнивает две **разные
сессии**, а не запрос с вложением и без, поэтому даёт порядок величины,
а не Δ на кадр (§5 п.4 закрыт частично).

**Очередь кампании не изменилась.**

---

## 2026-09-03 (тринадцатая запись) — владелец нашёл невидимую репу одним вопросом

Версия `4.72.0`, `PIT-176`. Кампания не двигалась.

🔴 **Прочитав хендофф, владелец спросил: «так сейчас же 64 репы, не?
мы же миграцию делали».** Проверка: на диске **64** каталога, инвариант
печатает **«63 реп»**, разница нигде не называется.

Причина — отбор по `.repo-class`: каталог без него не попадал в список
**молча**. Таким оказался `finpilot` — витринное зеркало по `ADR-009`,
у которого служебных файлов нет намеренно. Он был невидим для **всех**
проверок разом с 15.08.2026.

### Урок для кампании синтеза — прямой

**Приёмка репы считает то, что попало в отбор.** Если репа не проходит
критерий отбора, она не станет «непринятой» — она **перестанет
существовать** для процедуры. Красное видно, пустоту нет.

Проверить это в кампании стоит одной командой: сравнить число реп,
которое печатает инструмент, с `ls ~/repos | wc -l`. Расхождение —
всегда слепая зона, независимо от того, законна она (зеркало) или нет
(пропущенная разметка).

**И общее с `PIT-172`:** инструмент отвечал на вопрос «что с размеченными
репами», а читался как «что с системой». Формулировка не лгала — она
отвечала на другой вопрос.

**Очередь кампании не изменилась.**

## 2026-09-03 · v4.73.0 — кампания не двигалась, работа шла по гейтам (вахта S)

По репам не работали. Закрыт `ROADMAP` §P0-1 (`ADR-007`): признак жизни батча
для watchdog излучает теперь `bump_repo.py`, а не ритуал.

🔴 **Находка, относящаяся к кампании косвенно, но к её инструментам прямо.**
`auto.log` лежит в раздаваемом ките `06-*` и входил в отпечаток канона —
значит каждая строка о закрытом батче делала все 63 копии «разошедшимися».
Отсюда объяснение `PIT-174`, которого в нём не было: **шаг «оставить признак
жизни» пропускали не по забывчивости, он стоил раздачи канона.** Тот же класс,
что и `SYN`-грабли про цену шага: пропускается не забытое, а дорогое.

Заведена `PIT-177`: `ls` в этой среде — алиас на `eza`, и без явного пути он
печатает пустой список с кодом 0 и пустым `stderr`. Для проходов по репам это
существенно: любой листинг каталога репы, сделанный руками, может молча
вернуть пустоту, и отличить её от «каталог пуст» изнутри вывода нельзя.
