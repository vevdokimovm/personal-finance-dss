# Мандат автономного прогона — ночь 22→23.08.2026

> ✅ **ИСПОЛНЕН — помечено 22.08.2026, вахта IX.** Предмет мандата («кампания синтеза,
> 50 непройденных реп») **исчерпан**: кампания закрыта со счётом **57 из 57**, ноль
> пропусков §6а (`WATCHLOG.md` §0). Мандат не отменён, а **отработан**;
> открытые пункты из его §1 закрыты отдельно — нотация номеров реестров разведена
> (`PIT-117`), лидерборды и дубли остаются в `ROADMAP.md` §P2.
> Пометка ставится по `06-autonomous-mode-kit/…` §2, чтобы следующая вахта не решала
> ту же задачу заново.

> Заполнен по шаблону `06-autonomous-mode-kit/LAUNCH_BRIEF.md`.
> Второй живой прогон контура. Предыдущий мандат (ночь 21→22.08, `dota-dossier`)
> исполнен: репа закрыта, приёмка сошлась, поимённая сверка §6а — ноль.

---

## 1. Предмет и порядок

> 🏁 **ОБНОВЛЕНО 22.08.2026, 07:12.** Три репы, ради которых писался прежний мандат,
> **закрыты в ту же ночь**: `character-a-analysis`, `portrait-of-taste`,
> `personal-finance-dss`. Счёт кампании — **7 из 57**. Прежний текст этого раздела
> устарел целиком и заменён; не выполнять его.

Продолжай **кампанию синтеза** по `ROADMAP.md` §P2 и `repos-map.md`: **50 непройденных реп**.

Порядок входа в каждую (`PROCEDURE.md`):

1. агент `repo-inventory` — все копии, четыре листинга §1, сверка sha256 с базой;
2. разметка каталогов М/С построчно — **и сверка числа размеченных каталогов с числом
   каталогов в выводе §1.2** (`SYN-027`: в одной репе разметка была неполной трижды);
3. чтение М батчами, решение по каждому файлу **в момент**;
4. **§6а после КАЖДОГО батча**, не в конце репы (`SYN-021`; за вахту VIII она трижды
   поймала реальный пропуск);
5. §6 — приёмка **с версией базы в формуле** (`SYN-023`: слагаемое «совпало с базой»
   портится со временем само).

**Границы объёма для С** (`SYN-026`): до сотни файлов — поимённо; свыше — числом
с воспроизводимой командой. Групповая пометка законна только вместе с **выборочным
открытием** и **числом**.

Открытые пункты `ROADMAP.md` §P2, если по репам идти нельзя: свести лидерборды классов
по репам · развести нотацию своих и чужих номеров реестров · 29 групп побайтовых дублей
внутри базы.

## 2. Границы

**Пишешь только в:** `~/Documents/base-repo/` — подъёмы в базу, `decisions.md`, реестры.

**Только чтение:** `~/Documents/система_репозиториев/<репа текущего шага>/`.

**Не трогаешь:** остальные 54 репы · `_base/` и вложенные `base-repo/` в любой репе ·
`~/Downloads/` кроме сборки архива · ничего на GitHub · `~/Downloads/_sync-base/`.

## 3. СТОП

Полный список и обоснование — `06-autonomous-mode-kit/STOP_CONDITIONS.md`.
Останавливаешься на:

1. **Необратимое вовне** — публикация, деплой, тег, релиз, удаление, отправка.
   🔴 **Деплой этой ночью не делается вообще**, даже если версия поднята.
2. **Секрет в материале** — токен, ключ, `.env`. Записываешь где и какого класса,
   **не сам секрет**. Останавливаешься немедленно.
3. **Расхождение, требующее решения владельца** — два источника называют каноном
   разное; правило противоречит правилу.
4. **Потеря опоры** — кончилось место; гейт краснеет непонятно; **три итерации
   подряд один и тот же сбой**.
5. **Выход за мандат** — задачи, которой нет выше, не делаешь. Полезное пишешь
   в `ROADMAP.md` и идёшь дальше.

🔴 **Отдельно: при упоре в лимит НЕ делай `/login` и не меняй аккаунт.** Замерено
21.08.2026 (`49` §2а): смена системного префикса сбрасывает кэш целиком, ход стоит
**×12**. Ждать сброса дешевле — цикл ждёт сам по `resetsAt`.

**Не стоп, решаешь сам:** упор в лимит (ждёшь) · обрыв сети (backoff) · непонятный файл
(открываешь и читаешь) · сомнение «поднимать или дубль» (**повышаешь**, помечаешь
«сверить при ревизии») · дефект в чужой репе (записываешь, **не чинишь**) · опечатка
в базе (чинишь молча, пишешь строкой) · auto-compact (не повод останавливаться,
по расходу выгоден — `49` §2а; после него перечитай `WATCHLOG.md` §0).

## 4. Единица сдачи

**Батч = один каталог репы, доведённый до конца:**

- все файлы каталога прочитаны;
- по каждому — запись в `decisions.md` (`PROCEDURE.md` §5);
- поимённая сверка §6а по этому каталогу прошла: ни одного файла без записи;
- находки подняты в базу **в момент**, а не отложены;
- реестры пополнены по ходу (`PROCEDURE.md` §5а): `reports/pitfalls.md`,
  `reports/incidents/PITFALLS.md`, `05-infra-synthesis-lab/PITFALLS.md`;
- `VERSION` поднята, `CHANGELOG` дописан, `WATCHLOG` §0 отражает точку,
  §3 подрезан до 10 записей;
- `python3 scripts/pack_release.py` — архив в `~/Downloads`, старый удалить.

Гейт `python3 scripts/revision_check.py` обязан быть **CLEAN** на закрытии каждого батча.
Stop-хук `ritual-gate.sh` это проверит сам и не даст закончить ход иначе.

**Закрыл батч — сразу начинай следующий.** Не подводи итоги, не спрашивай разрешения:
владелец спит. Отчёт пишется один раз, когда работать уже нельзя.

## 5. Если застрял

- Не понял файл — прочитал соседние, потом решил. Не понял и после — 📍 локальным
  с причиной «не разобрался», идёшь дальше. **Не зависаешь.**
- Инструмент врёт — записал в `05-infra-synthesis-lab/tools/README.md` с потолком,
  сделал руками.
- Сбой три раза подряд — стоп-класс 4, останавливаешься и пишешь журнал.
- Вопрос владельцу — одной строкой в `TASKS.md`, работу на нём **не останавливаешь**.

---

## Что проверяет сам прогон (приёмка контура, `71` §7в)

Второй прогон — проверяется то, что первый не закрыл:

1. Пережил ли `--resume` **реальный упор в лимит**, а не только перезапуск?
2. Вышел ли цикл по `STOP`-файлу после текущей итерации?
   🔴 Вопрос переформулирован 04.09.2026: раньше спрашивалось «в течение
   минуты», и такой прогон **провалил бы приёмку по верному поведению**.
   Замер `bin/test_stop_latency.sh`: во время работы `claude` стоп-файл
   не проверяется, выход — по её завершении. Проверять надо, что цикл вышел
   **до следующей итерации**, а не мгновенно.
3. Не заснул ли ноутбук (`caffeinate -i` в `night.sh`)?
4. Сколько батчей закрыто за ночь — один или больше? Один = останавливался
   на вежливости, это дефект авто-режима, а не результат.

---

## Запуск

```
~/Documents/base-repo/06-autonomous-mode-kit/bin/night.sh
```

Мягкая остановка:

```
touch ~/claude-autonomous-runs/<папка-прогона>/STOP
```
