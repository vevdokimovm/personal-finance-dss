# Мандат на автономный прогон — 22.08.2026

> ✅ **ИСПОЛНЕН 22.08.2026, вахта VIII.** Все три репы закрыты: `character-a-analysis`
> (`337 = база 78 + С 68 + прочитано 191`), `portrait-of-taste`
> (`1572 = база 140 + дельта 25 + С 1271 + прочитано 136`), `personal-finance-dss`
> (`1501 = база 48 + С 1195 + прочитано 258`). §6а — 0 у всех трёх. Счёт кампании 7 из 57.
> Закрыто 16 версий, v2.2.0 → v2.16.0.
>
> 🔴 **Честно: этот файл вахта не открывала.** Мандат был взят из промпта передачи в чате,
> и содержание совпало построчно — но совпало **случайно**. Разбор — `SYN-028`.
>
> **Актуальный мандат — `~/Downloads/NIGHT-RUN-BRIEF.md`** (обновлён 22.08 в 07:12).
> Этот файл сохранён как история, исполнять его не нужно.

---

АВТОНОМНЫЙ ПРОГОН

1. ПРЕДМЕТ И ПОРЯДОК

   Работай по `ROADMAP.md` §P2 и `05-infra-synthesis-lab/PROCEDURE.md`,
   в порядке приоритетов. Кампания синтеза инфраструктур: закрыто 4 репы из 57.

   Первое:  `character-a-analysis` — 337 файлов, 24 служебных без решения.
            Приёмка §6 ОТСУТСТВУЕТ ВОВСЕ — это единственная из трёх реп,
            где неизвестно даже, сколько работы осталось. Начать с
            агента `repo-inventory`, затем разметка каталогов М/С,
            затем приёмка арифметикой §6 и поимённая сверка §6а.

   Затем:   `portrait-of-taste` — дочитать 36 служебных, приёмка сошлась,
            но при 36 непрочитанных, то есть сошлась неверно. Пересобрать.

   Затем:   `personal-finance-dss` — 436 без решения, приёмка помечена
            «НЕ закрыта» её же записью. Самая тяжёлая, идёт последней.

   Правило порядка: от дешёвого к дорогому, репу не бросать на середине —
   либо закрыта приёмкой, либо честно записана как незакрытая с числами.

2. ГРАНИЦЫ

   Трогать:     `~/Documents/base-repo/` целиком;
                читать репы кампании из `~/Documents/система_репозиториев/`.
   Не трогать:  чужие репы вне кампании · публичные репозитории на GitHub ·
                `~/Downloads/mvp/` (витрина, владелец опубликовал) ·
                ничего не пушить, не деплоить, не удалять пачкой.

   Реестры вести по ходу, не в конце: `reports/pitfalls.md`,
   `reports/incidents/PITFALLS.md`, `05-infra-synthesis-lab/PITFALLS.md`
   (`PROCEDURE.md` §5а).

3. СТОП-УСЛОВИЯ — остановиться и ждать владельца

   Стоп-классы — `06-autonomous-mode-kit/STOP_CONDITIONS.md` и `STANDARD.md` §2:
   речь моим голосом · необратимое вовне · удаление пачкой · безопасность ·
   смена цели.

   Дополнительно к ним, для этого прогона:
   - любая публикация на GitHub, включая профиль и витрину;
   - `gh repo delete`, `git push --force`, любой `rm -rf` вне `/tmp`;
   - решение «эта репа кампании не нужна» — это смена цели, не оптимизация;
   - упор в лимит: НЕ переключать аккаунт и НЕ делать `/login`.
     🔴 Замерено 21.08: смена префикса сбрасывает кэш целиком, ход стоит ×12.
     Ждать `resetsAt` — этим занимается `limits_watch.py`, он уже в цикле.

4. ЕДИНИЦА СДАЧИ

   Батч = VERSION поднята + секция в `CHANGELOG.md` (с блоком `### Campaign`)
   + `WATCHLOG.md` §0 и §3 обновлены + архив `~/Downloads/base-repo-vX.Y.Z.zip`
   (`python3 scripts/pack_release.py`, старый архив удалить).

   Перед каждым закрытием версии: `python3 scripts/revision_check.py` обязан
   дать CLEAN. Гейт `check_campaign_log` не пропустит версию без записи
   сегодняшней даты в `05-infra-synthesis-lab/runs/2026-08-20-first-synthesis/decisions.md` —
   кампания не двигалась, так и написать строкой.

   Решения с альтернативой — в `decisions.md` прогона, по ходу.

5. ЕСЛИ ЗАСТРЯЛ

   Сеть — ретраить, этим занимается цикл. Долго не выходит — отложить
   и делать следующее по списку, записав почему отложено.
   Вопрос к владельцу — одной строкой в конце батча, работу на нём не
   останавливать.

   Не изобретать заново то, что уже исследовано (`PIT-072`): перед новым
   ресёрчем проверить `00-infrastructure/` и `reports/`.
