# Реестр рецидивов: лидерборд повторяющихся ошибок

**Тип:** нормативный реестр · **Введён:** v7.3.0 · **Обновляется:** при каждом
повторе, до записи в CHANGELOG.

---

## 0. Зачем этот файл, если уже есть три реестра

Разборы у нас пишутся хорошо и не работают. Доказательства из одной сессии:

| Что | Разбор существовал | И всё равно повторилось |
|---|---|---|
| «PostgreSQL недоступен в песочнице» | `postgres_incident_postmortem.md` с готовым рецептом | да, на миграции 0030 |
| `cd X && job &` фонит весь пайплайн | **PIT-006**, описано точно | да, дважды за сессию |
| «Сделано» объявлено без проверки | урок v6.20.2 | да, юрблок «закрыт» без прогона |

Вывод неприятный, но однозначный: **разбор, который надо вспомнить и открыть,
не является мерой.** Он работает как объяснение постфактум и не работает как
предохранитель. Значит, к нему нужен второй контур — счётчик и эскалация.

---

## 1. Правило эскалации по числу повторов

Главная новая механика. Тип контрмеры определяется **не тяжестью ошибки, а
числом рецидивов**: если проза не сработала дважды, третий текст не сработает
тем более.

| Повторов | Обязательная контрмера | Почему именно эта |
|---|---|---|
| **1** | разбор прозой (инцидент / расследование / pitfall) | причина ещё не изучена, нужен текст |
| **2** | + правило в стандарт и пункт в чек-лист | текст был и не спас; нужна точка, где на него наткнёшься |
| **3** | **+ автоматическая проверка** (тест, гейт, preflight) | проза доказала неэффективность дважды — дальше только машина |
| **4+** | **менять процесс, а не добавлять правило** | четвёртый повтор означает, что дефект не в исполнителе, а в устройстве работы |

Правило симметричное: **понижать уровень нельзя**. Если пункт дошёл до
автоматической проверки, снять её можно только вместе с исчезновением причины.

---

## 2. Лидерборд (сортировка по числу повторов)

Топ-3 дублируются в шапку `docs/WATCHLOG.md` — единственное место, которое
читается в начале каждой сессии по протоколу вахты.

| # | Класс ошибки | Повторов | Последний | Уровень | Контрмера |
|---|---|---|---|---|---|
| 1 | **Код написан под первый прогон, а живёт в устоявшемся состоянии** | **6** | 2026-07-30, `deploy.sh` | 4 — менять процесс | обязательное утверждение о состоянии **после второго прогона** в каждом новом тесте; `REPORT_unreachable_code_pattern.md` |
| 2 | **`grep` в цепочке `&&` обрывает выполнение** | ≥5 | сессия v7.x | 3 — автопроверка | PIT-001; в preflight — запрет паттерна в скриптах батча |
| 3 | **«Сделано» объявлено без эмпирической проверки** | 3 | 2026-07-30, юрблок | 3 — автопроверка | preflight: гейты обязаны быть в выводе батча, а не в утверждении |
| 4 | **«Невозможно в песочнице» без исчерпания каналов** | 2 | 2026-07-30, PostgreSQL | 3 — автопроверка *(поднят из 2: цена рецидива — скрытый дефект)* | preflight-вопрос при словах «нельзя/недоступно»; рецепты в рунбуке |
| 5 | **«Сделано» подтверждено ВЫБОРОЧНЫМ прогоном вместо полного** | 3 | 2026-07-30 | 3 — автопроверка | батч, меняющий контракт (схема запроса, обязательное поле, новая зависимость роутера), закрывается только полным прогоном; выборочные группы подтверждают то, что менял, и молчат о том, что сломалось |
| 6 | **`cd X && job &` фонит весь пайплайн вместе с `cd`** | 2 | 2026-07-30 | 2 — правило | PIT-006; foreground-часть выносить в отдельную команду |
| 7 | **Результаты прогона читаются `tail`/`grep`, а не JUnit XML** | **3** | 2026-08-14 — `cmd \| tail -N` замаскировал реальный код возврата pytest (эффективный exit — от `tail`, не от pytest), потрачено время на «зависший тест», которого не было | 2 — правило *(3-й повтор формально требует автопроверки, но это интерактивное поведение сессии, не код репозитория — preflight/гейт его не видят; доступная мера — то же правило, PIT-003, но заметнее)* | PIT-003: результат прогона — только через `--junitxml` + разбор XML, никогда через `\| tail`/`\| grep` (маскирует exit code пайплайна) |
| 8 | **Таймаут Stop-гейта принят за зависший тест, хотя причина — сторонняя нагрузка хоста** | **6** | 2026-09-03 (v8.33.0) — ЕДИНСТВЕННЫЙ эпизод с load average НИЖЕ числа ядер (5.39 при 8): срез не уложился из-за конкуренции с СОБСТВЕННЫМ фоновым полным pytest и iTerm2 на 209% CPU. Ломает упрощение «посмотри uptime и всё ясно» — важна не сумма нагрузки, а с кем именно делится машина. Контрмера: сообщение хука теперь просит сначала `ps -Ao %cpu -r`, потом `uptime`; заодно исправлен счётчик в тексте хука, отстававший на три эпизода. Предыдущий: 2026-09-02 (v8.30.2), load average 57.80 → **растёт до 74.30** при 8 ядрах; три источника разом — `iTerm2` 162% (третий эпизод подряд в топе: 1322 / 646 / 55 мин накопленного времени, то есть хронический паттерн, не всплеск), чистка тулчейна владельца (`StorageManagementService`+`Storage.appex`, ~115%, та же кампания сутками раньше пересоздала `.venv` под идущим батчем в v8.30.1) и VPN `Happ` 38.6%; троттлинга НЕТ (`CPU_Scheduler_Limit = 100`) — отличие от эпизода 3 | 2 — правило *(правило сработало сразу и без итераций в эпизодах 3-5; **открыт вопрос к владельцу**: пять эпизодов — один класс «абсолютный порог времени на разделяемом ресурсе», 83 с при бюджете 45 на девятикратно перегруженном хосте это не деградация теста, а бюджет, измеренный на другой машине — стоит ли делать критерий относительным, сверяясь с `uptime`. Это изменение поведения блокирующего гейта, решение не вахты)* | `docs/reports/investigations/machine_load_gate_timeout_investigation.md` (INV-MACHINE-LOAD, 5 эпизодов); `.claude/hooks/_run_test_slice.py` печатает «проверь `uptime` первым делом» прямо в тексте блокирующей ошибки; эпизод 3 добавляет `pmset -g therm`/`sysctl vm.swapusage`; эпизод 5 фиксирует, что бюджет фазы ужесточён 65 → 45 с |
| 10 | **Код правится во время идущего проверочного прогона** | 2 | 2026-09-03 (v8.34.0), дважды за один батч: правка mypy в парсере посреди полного `pytest` — числа стали не сигналом, а совпадением, прогон перезапущен; затем правка `tools/timing_lab/record.py` посреди уже перезапущенного. Второй раз перезапуск не потребовался, но обоснован ФАКТОМ, а не удобством: `grep` показал, что модуль импортируется только своим тестовым файлом, значит на чужие результаты повлиять не мог | 2 — правило *(автопроверки нет и вряд ли будет: «идёт ли сейчас мой полный прогон» — состояние сессии, не репозитория; ближайшее выполнимое — привычка запускать полный прогон ПОСЛЕ последней правки, а не параллельно с ней)* | Полный прогон — измерение, а не фон. Пока он идёт, правки идут в заметки; если правка неотложна, прогон честно считается недействительным и перезапускается. Разбор — CHANGELOG [8.34.0] |
| 9 | **Мягкие переносы `\u00ad` в markdown** | 2 | 2026-07-29 | 3 — автопроверка | preflight: поиск `\u00ad` во всех `.md` |
| 11 | **Команда вахты вызывает диалог разрешения, и владелец читает его как ВОПРОС вахты** | **≥10 за одну сессию** | 2026-09-04 (v8.43.0), авто-режим. Относительный путь-аргумент у читающей утилиты (`grep … docs/pitfalls.md`) харнесс не может сопоставить с `deny`-правилами `Read()` и эскалирует владельцу — при том, что вопросы в авто-режиме запрещены прямым приказом. 🔴 **Первый диагноз был неверен** («`cd` с относительным путём + флаг `-A`»): обход «делать `cd` абсолютным» был соблюдён и не помог — следующий вопрос прилетел на команде с абсолютным `cd`. Значение имеет путь в АРГУМЕНТЕ, а не у `cd`. Правило из неверного корня выглядит соблюдённым и не защищает | 3 — автопроверка, 🟢 **поставлена в тот же день** *(три первые попытки записи упёрлись в классификатор — правка собственной конфигурации агента; владелец выдал право явно)* | PIT-018 · хук `.claude/hooks/block-relative-read-path.py` + гейт `tests/test_block_relative_read_path.py` (11/11, проверен мутацией). 🔴 При исполнении хук трижды поймал ИСПРАВНЫЕ команды — разбор в шапке `docs/reports/engineering/task_pretooluse_guard_relative_paths.md`; цена ошибок несимметрична, поэтому разрешающих случаев в гейте больше, чем блокирующих |

**Как считать повтор.** Повтор — это тот же **класс** ошибки, а не тот же
файл и не та же команда. PostgreSQL на миграции 0018 и на 0030 — один класс
(«объявил невозможным, проверив один канал»), значит два повтора.

---

## 3. Что автоматизировано (уровень 3)

`tools/preflight.py` — запускается перед сдачей батча, часть церемонии версии.
Проверяет механически то, что раньше держалось на памяти:

| Проверка | Ловит пункт |
|---|---|
| `APP_VERSION` == версия верхней записи CHANGELOG == версия в шапке WATCHLOG | «сделано» без ceremony |
| Окно §3 WATCHLOG — ровно 10 записей | ceremony |
| Нет `\u00ad` ни в одном `.md` | № 7 |
| Нет паттерна `&& grep` в скриптах репозитория | № 2 |
| `flake8` по `app/ tools/ tests/` | общий гейт |
| Если в `alembic/versions/` есть миграция новее записи о последнем прогоне PG — предупреждение | № 4 |

Пункты, которые машина проверить не может, печатаются как **обязательный
вопрос**, а не как совет:

> Ты запускал это на втором прогоне подряд, когда входных данных для шага уже
> нет, а результат прошлой работы лежит на диске?

---

## 4. Порядок работы с реестром

1. **Повтор фиксируется до записи в CHANGELOG**, а не после. Счётчик +1, дата,
   уровень пересчитывается по таблице §1.
2. **Достижение уровня 3 — это задача в текущий батч**, а не в бэклог.
   Автопроверку пишем сразу, иначе следующий повтор случится раньше.
3. **Достижение уровня 4** — останавливаемся и разбираем устройство работы.
   Пункт 1 лидерборда уже там: шесть повторов означают, что тестовая фикстура
   систематически воспроизводит не то состояние, в котором живёт пользователь.
4. **Ревизия раз в веху:** пункты без рецидивов за две вехи помечаются
   `остыл` — но контрмера остаётся.

---

## 5. Честная оговорка о самом этом файле

Он тоже документ и тоже может быть не прочитан. Именно поэтому:

- топ-3 живёт **не здесь**, а в шапке WATCHLOG;
- уровень 3 означает **код**, а не строку в таблице;
- уровень 4 запрещает добавлять ещё один документ как контрмеру.

Если через две вехи в лидерборде появится восьмая строка с уровнем 2 и никакой
автопроверкой — значит, не сработал и этот контур, и менять надо его.
