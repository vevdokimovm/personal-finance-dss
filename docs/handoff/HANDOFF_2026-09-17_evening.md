# Передача вахты — 17.09.2026, вечер

Сессия оборвана владельцем по переполнению контекста (`/clear`). Здесь всё состояние,
чтобы следующая вахта продолжила без вопросов.

## Где стоим

**Очередь исследований — `docs/research/queue/GAP_QUEUE.md` (источник правды).**
Порядок: **Г47 проход 5 (перезапустить) → Г48 (1С) → Г49 (бухгалтерия) → Г50 (стек бизнеса) →
Г52 (каталог сценариев) → аудит покрытия № 5 → анализ волны 2 → Г38 → П1–П2 → синтез.**
Г51 (кросс-модельная экспертиза) агентов не держит — идёт параллельно.

Закрыто сегодня: Г37 (инвестконтур), Г42 (переупаковка), Г47 проход 4, Г46 прогон A3.
Заведено сегодня: Г46 (остаток A4), Г47 П5.1–П5.3, Г48, Г49, Г50, Г51, Г52; раздел
`ROADMAP.md §9.D` «После синтеза».

## 🔴 Что прервано и требует перезапуска

**Г47 проход 5** — агент остановлен владельцем на середине. Файл
`docs/research/raw/bank_statement_corpus_pass5_2026-09-17.md` существует (~1,2 КБ, только шапка).
Последнее, что агент успел сказать: **форма 101 ЦБ уже скачана на диск** с прерванной попытки
(искать в `~/raw-originals/finpilot-data/`), он начинал разбирать её своим кодом.
В `GAP_QUEUE.md` проход помечен «В РАБОТЕ» — при перезапуске отметку обновить.
Промпт прохода — в разделе Г47 очереди (П5.1 доля физлиц в 172 непокрытых банках,
П5.2 универсальный разбор PDF без шаблона, П5.3 распределение граждан по банкам и мультибанкинг).

## Кросс-модельная проверка матмодели (Г51) — что уже сделано

- Промпт «модель с нуля» (даёт только смысл продукта, ничего из репозитория):
  `docs/model/expert_certification/prompts/from_scratch_model_prompt.md`.
  🔴 В нём есть блок **2а: нужен ли здесь машинный интеллект вообще** — заведён по прямому
  вопросу владельца 17.09.2026 («мы же спецом без мля, насколько это эффективно»).
- **Прогон 1 — Exa `exa-research-pro`, готов:**
  `docs/model/expert_certification/from_scratch/exa-research-pro/FLO-1.0_response_2026-09-17.md`.
  Модель представилась ChatGPT (семья OpenAI), стоимость $0,025. Ключевое: кризисная ветка
  совпала с нашим I12; ПДН делит на 30 / 50 % против нашего 0,40; подушка 3 / 6 / 9 месяцев;
  «дорогой долг» = безрисковая доходность + 3 п.п.; инвестиции только от 5 лет; метод —
  лексикографические ограничения + перебор шагом 1 % + стресс-сценарии (у нас SAW, шаг 10 %).
- **Локальные модели качаются через `ollama`** (решение владельца: Gemini/ChatGPT/Grok закрыты,
  берём открытых родственников). Очередь в `/tmp/pull_models.sh`, лог `/tmp/pull_models.log`:
  `qwen3:8b` → `gemma3:12b` → `deepseek-r1:8b` → `llama3.1:8b`, ~23 ГБ, свободно было 42 ГБ.
  🔴 **Следующий шаг:** дождаться, прогнать на каждой модели промпт «модель с нуля»
  (`ollama run <модель>` с файлом промпта), ответы сложить в
  `docs/model/expert_certification/from_scratch/<модель>/`, затем сравнить все прогоны
  с `docs/math_model.md` по осям из конца файла промпта.
- Владельцу остаётся ручной прогон в Gemini и ChatGPT (их API нет).

## Решения, ждущие владельца

1. Норма ЦБ «на кредиты не больше 30 % дохода» против канонного ПДН ≤ 0,40 (найдено Г46 A3).
2. Единица подушки: месяцы расходов или месяцы дохода — у ЦБ в разных статьях по-разному.
3. Прогон его реальных чисел через алгоритм (нужны остаток долга, ставка, платёж, доход).
4. Экспорт чатов Gemini про лого; питч-презентация. Всё записано в `ROADMAP.md §9.D`.

## Правила, которые нельзя забыть

- Один агент за раз, перед запуском три гейта с `hook_event_name: PreToolUse`
  (`research-channels-gate`, `exa-gate`, `websearch-budget`), все должны дать `exit=0`.
- Абсолютные пути во всех аргументах Bash (хук PIT-018). `gh api` — полным URL.
- Российский корневой сертификат не ставить; порядок каналов и приём «антибот на домене →
  ищи поддомен справки» — в `CLAUDE.md` правило 11 и в разделе Г47 очереди.
- Версия проекта 9.11.0, батч с начала дня не закрывался: `VERSION`, `CHANGELOG`, `WATCHLOG`
  за сегодня не обновлялись, архив не собирался. Это долг, его закрывает `close_batch.py`.
