# Передача вахты — 22.09.2026

Готовится под `/clear`. Здесь всё состояние, чтобы следующая вахта продолжила без вопросов.
Предыдущая передача — `HANDOFF_2026-09-18.md` (она больше не актуальна по очереди).

---

## 1. Где стоим

**Очередь исследований пройдена до цикла аудитов.** Источник правды —
`docs/research/queue/GAP_QUEUE.md`.

**Порядок (поправлен владельцем 18.09.2026):**

> Г58 ✅ → Г59 ✅ → Г60 ✅ → Г61 ✅ → Г62 ✅ → Г63 ✅ → Г64 ✅ →
> 🔴 **цикл «аудит покрытия № 7 → добор → аудит № 8 → …» до условия остановки** →
> **анализ волны 2 (опрос)** → **Г38** → **П1–П2 (каналы Telegram)** → **синтез** → **роадмапы по сферам**

🔴 **Следующее действие: аудит покрытия № 7.** Это не разовая проверка, а цикл.

### Закрыто 18–22.09.2026 — семь тем

| Тема | Файл | Одной строкой |
|---|---|---|
| Г58 | `raw/bank_statement_corpus_pass6_2026-09-18.md` | непокрыто 193 банка (≈23 % по людям); ошибка агрегата 45.2 формы 101; Почта Банк присоединён к ВТБ 01.05.2026 |
| Г59 | `raw/legal_license_tail_2026-09-18.md` | словари pymorphy — CC BY-SA 3.0 на данные, закрытый продукт можно; строки выписок на сервере = >1 млн идентификаторов = ч. 14 ст. 13.11 КоАП (10–15 млн ₽) |
| Г60 | `raw/revived_sources_2026-09-18.md` | 🔴 Fano & Kurth 2003 сузил новизну по объекту; 13 выводов изменено; canon `novelty_statement.md` §1 поправлен вахтой |
| Г61 | `raw/early_period_rewalk_p1_…md`, `…p2_…md` | из 558 маркеров 89,2 % были историей уже закрытого; 🔴 недоступных источников 0 |
| Г62 | `raw/explanation_without_llm_2026-09-19.md` | спецификация генератора объяснений (6 частей); 🔴 4 новых дефекта текста (Д-09а–г), верность ступени 32,1 % |
| Г63 | `raw/sbp_qr_roles_liability_2026-09-19.md` | таблица «кто отвечает» по 6 ситуациям; оплата ТСП по СБП — расход, не перевод; подписка через СБП дешевле карты |
| Г64 | `raw/open_source_reuse_2026-09-19.md` | 3 формулы подтверждены внешними векторами до копейки; 🔴 2 дефекта матядра + 1 дефект дедупа импорта |

### 🔴 Дефекты продукта, накопленные очередью (в синтез, код НЕ править до него)

1. **`_simulate`: каскад минимумов разовый**, канон §10.5 требует постоянного. Лавина занижена
   на 10–11 месяцев и 16–29 тыс. ₽ (Г64).
2. **Копеечный хвост округлённого аннуитета** даёт лишний месяц в 44 % кредитов (110/252) (Г64).
3. **Дедуп импорта** схлопывает одинаковые операции одного дня; тест закрепляет ошибку;
   `external_id` не используется (Г64).
4. **Текст объяснения врёт про причину** — Д-09 (67,9 %) плюс Д-09а (знак контраста, 29 %),
   Д-09б (константный критерий назван решающим), Д-09в (ничья как «наивысшая оценка»),
   Д-09г («3 месяца» вне канона). Верность ступени 32,1 % (Г62).
5. **`forecasting.py:101-105`** синтезирует историю без пометки (Г45/Г52).
6. **`routes_demo.py:119`** — льготная IT-ипотека 5,5 % в демо-портрете (Г52).
7. Навык `finpilot-money-format` расходится с кодом в трёх местах; по округлению графика прав код (Г64).

---

## 2. 🔴 Правила владельца (18–22.09.2026)

1. **Решения несёт вахта.** Владельцу — только деньги, ключи, платные доступы, логины, юрлицо,
   клик картинки-капчи. Память: `decisions-after-synthesis`.
2. **Синтез обязан кончиться роадмапами по сферам:** правовой контур · матядро · бэкенд ·
   фронтенд · архитектура · тесты · данные и справочники · продукт и сегменты.
3. **ML и LLM в продукте не используются вообще.**
4. **Реальные выписки остаются на устройстве.**
5. **Версии, CHANGELOG, WATCHLOG, архив — отложены** («без версий пока что отставить»).
   Долг с 17.09. 🔴 Плюс долг этой сессии: вахта менялась V → J → A → V → A → J, записи
   в WATCHLOG под прежними буквами перепроверить.
6. Один агент за раз; перед запуском три гейта; абсолютные пути во всех аргументах Bash.
7. **`kad.arbitr.ru` закрыт решением владельца** («да забей на него») — не заводить ни одним аудитом.

---

## 3. Цикл аудитов — что делать следующей вахте

**Аудит № 7 идёт по ДВУМ осям** (решение владельца 18.09.2026):
- **Ось А — добыча:** что не взято инструментами, по трём классам.
- **Ось Б — смысл:** какие вопросы исследование не задало вовсе. Четыре сверки: от решений
  восьми роадмапов · от параметров канона `math_model.md` · от пользователя (Г52, Г57, опрос) ·
  свежий взгляд агента без контекста.

**Пробел по оси Б засчитывается, только если ответ меняет решение хотя бы в одном роадмапе.**

**Условие остановки цикла:** обе оси чисты — по оси А остаток ровно из 🟡 (деньги, ключи,
логины, доступы) и 🔴 (сайт мёртв), по оси Б нет засчитанных пробелов. Полный метод —
`GAP_QUEUE.md`, раздел под Г60. Память: `audit-loop-until-closed`.

---

## 4. Контур добычи — что замерено 18–19.09.2026

Полный разбор — `docs/reports/investigations/research_contour_2026-09-18_investigation.md`.

- **Лестница каналов:** WebFetch → `curl` с браузерным UA → `curl -sk --http1.1` (российский УЦ;
  🔴 сертификат ставить ЗАПРЕЩЕНО) → `curl -s "https://r.jina.ai/<URL>"` БЕЗ UA → Exa → **браузер**.
  PDF: WebFetch кладёт файл на диск → `Read` с `pages`. Exa-поиск по индексу берёт то, что не берёт fetch.
- **Гейт каналов различает три состояния:** жив · деградировал (только 401 и 429 — троттлинг
  нашего выхода, пускает с громким предупреждением) · мёртв (запрет). Тесты — 65, зелёные.
- **Капчи:** Cloudflare Turnstile и Google `/sorry` в браузере автоматизации НЕ проходятся
  (флаг автоматизации; капча привязана к IP, а выход туннеля плавает). Владельцу — только
  картинка с кодом, с заранее вписанным в форму запросом.
- **Браузер общий** с агентами: выбранная страница одна на всех. Свою вкладку закрывать сразу,
  чужие не трогать.
- **Тяжёлые процессы:** машина Intel i5-8259U, 4 ядра, GPU нет. Расшифровка — сначала субтитры
  площадки, иначе `whisper-cli -t 4 -bs 1 -bo 1`. После падения агента вахта смотрит `ps` и
  гасит его сирот; перед словами «процесс не наш» — `ps -o ppid=` до `claude`.
- **Обрывы агентов:** первый — возобновить через SendMessage; второй и дальше с малым приростом
  сырья — запускать СВЕЖЕГО агента с точным остатком (память `fresh-agent-over-bloated-resume`).
- 🔴 Правки канона `~/repos/base-repo/.claude/agents/researcher.md` (§11 вкладки и капчи,
  §12 тяжёлые процессы) видны агентам только в НОВОЙ сессии — после `/clear` уже работают.

---

## 5. Что ждёт владельца (🟡)

Ключ Elsevier Article API (закрывает 2 статьи) · `ANTHROPIC_API_KEY` для эксперимента Г54 ·
партнёрский логин `confluence.korona.net` (спецификация Faktura) · демо-доступ БИФИТ через
`sales@bifit.com` · живые PDF выписок физлиц из десяти банков (остаются на устройстве) ·
панель «Ромир» · RLMS под коммерческое использование · договор с MOEX · запрос в Сбериндекс ·
отчёт NeoAnalytics · правила Direct в туннеле для `ozon.ru` и `sberindex.ru` · ГОСТ-TLS при
запуске с реальными ПДн · при желании логин `releases.1c.ru`.

---

## 6. Первое, что сделать новой вахте

1. Прочитать `GAP_QUEUE.md` (порядок и правило цикла под Г60) и этот файл.
2. Прогнать три гейта (`websearch-budget`, `exa-gate`, `research-channels-gate`) — все `exit=0`.
3. Запустить **аудит покрытия № 7** по двум осям, один агент.
4. Дальше по кругу: добор → аудит, пока обе оси не чисты. Потом опрос → Г38 → П1–П2 → синтез.
5. Не забыть долг: версия, CHANGELOG, WATCHLOG (включая смену вахт), архив — после очереди.
