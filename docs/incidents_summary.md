# Реестр инцидентов FINPILOT

Краткие сводки по каждому инциденту — для быстрой ориентации разработчика/AI в новой сессии.
**Полные разборы (post-mortem)** живут ОТДЕЛЬНЫМИ файлами вне репозитория (у владельца проекта +
в Claude-проекте), оформляются по правилу §12 `WATCHLOG.md`. Здесь — только сводка: что было,
корень, как закрыто. Формат самих разборов — `docs/incident_postmortem.md`.

| ID | Суть | Корень | Статус |
|----|------|--------|--------|
| **INC-PG-SANDBOX** | PostgreSQL-тестирование было ложно объявлено невозможным в песочнице и переложено на владельца как «долг». | Гипотезу не проверили фактически. PG **поднимается** в песочнице: `apt-get install postgresql` → `pg_ctlcluster 16 main start` → роль/БД → `DATABASE_URL=postgresql+psycopg://...`. Полная матрица зелёная на PG 16.14. | Закрыт. Породил правило §11 (не объявлять невозможным без проверки) и процедуру в §5. |
| **INC-MATHMODEL-FALSEALARM** | `test_profiles_produce_different_plans` давал одинаковые планы для разных риск-профилей → подозрение на «баг математического ядра». | **Гонка в самом E2E-тесте, ядро чисто.** `/planning` при загрузке делает авто-расчёт с дефолтным `risk=3`, тест читал его результат вместо ручного. Доказано тремя уровнями: прямой API (risk=5→«на цели»), чтение `app.js`, перехват POST (два запроса). Фикс — `expect_response` на ручной расчёт. | Закрыт. Породил правило §12 (серьёзный баг с долгой диагностикой → авто-incident отдельным `.md`). |
| **BUG-024/025** | Дефекты, разобранные в E2E-постмортеме (см. полный разбор `FINPILOT_E2E_incident_postmortem.md` вне репо). | См. полный разбор. | Закрыт. |
| **ЦБ-403** | `cbr.ru` отдаёт 403 запросам с датацентровых IP → ключевая ставка недоступна напрямую. | DDoS-защита провайдера (не баг кода). В проде — зеркало `cbr-xml-daily.ru`; в коде — graceful fallback `r_bench=0.14`. | Закрыт (внешнее ограничение, обойдено зеркалом). |
| **INC-CRUD-DELETE** | Удаление obligations было сломано несколько версий при зелёных юнит-тестах; restore добавлен позже; E2E на базовые CRUD не было. | Юнит проверяет функцию, но не разрыв между UI и API — кнопка/эндпоинт могли разойтись незаметно. Закрыто 6 CRUD-E2E (create/delete/restore для obligations/goals/transactions, v4.16.28). | Закрыт. Породил правило §13 (базовые CRUD ОБЯЗАНЫ иметь E2E). Полный разбор — `FINPILOT_crud_delete_incident.md` вне репо. |
| **INC-BROWSERS-SANDBOX** | firefox/webkit для E2E не поднимаются в песочнице; стоял вопрос — баг конфигурации или внешнее ограничение. | **Внешнее ограничение, как ЦБ-403.** Сборки браузеров Playwright раздаются только с Azure-CDN (вне allowlist, 403). Перебраны ВСЕ каналы (правило §11): GitHub releases — 404, npm-пакет бинарей не содержит, системный firefox несовместим с juggler, apt даёт только либы. chromium в песочнице работает; firefox/webkit — в CI, где CDN доступен (тесты браузер-агностичны). | Закрыт (внешнее ограничение). Полный разбор — `FINPILOT_browsers_sandbox_incident.md` вне репо. |

> Как добавлять: при закрытии серьёзного инцидента — строка сюда (сводка) + полный разбор
> отдельным файлом владельцу (правило §12). Эти два уровня дополняют друг друга: реестр для
> быстрого контекста в коде, полный разбор — для деталей и метода вне репозитория.
