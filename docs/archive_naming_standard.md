# Стандарт именования выходных архивов

**Тип:** нормативный документ · **Введён:** v7.2.0 · **Заменяет:** прежнюю
схему `finpilot_vX_Y_Z_intl.zip`.

---

## 1. Правило

| Назначение | Имя архива | Корневая папка внутри |
|---|---|---|
| Приватный репозиторий (основной) | `personal-finance-dss-vX.Y.Z.zip` | `personal-finance-dss-vX.Y.Z/` |
| Публичное зеркало (открытый код) | `finpilot-public-mirror-vX.Y.Z.zip` | `finpilot-public-mirror-vX.Y.Z/` |

**Разделители — точки, не подчёркивания.** Имя архива = имя репозитория + `-v` +
версия по SemVer.

**Репозиторий зеркала на GitHub называется просто `finpilot`** (проверено вживую
2026-08-13 — переименование в `finpilot-public-mirror` и обратно в один и тот же день,
владелец явно подтвердил: репа остаётся `finpilot`, длиннее — только имя архива, «так
понятнее и удобнее» на диске). Имя АРХИВА при этом — `finpilot-public-mirror-*` (не
голое `finpilot-*` и не промежуточное `finpilot-mirror-*`, использовавшееся несколько
часов той же сессии) — из-за общего `deploy.sh`: там `REPO_MAP="finpilot=personal-
finance-dss"` (историческая запись про частный случай неверного именования приватных
чекпоинтов) — архив с голым именем `finpilot-*` без `.repo-id` внутри уйдёт по этой
карте в ПРИВАТНЫЙ репозиторий и подменит там дерево обезличенным срезом зеркала.
`REPO_MAP` получил встречную запись `finpilot-public-mirror=finpilot` (и оставленную
для совместимости `finpilot-mirror=finpilot`) — так деплойер сводит длинное имя
архива к короткому имени настоящей репы. Длинное имя в архиве — не косметика, а
единственное, что не даёт деплойеру перепутать репозитории с похожими именами.

**Постфикс `_intl` отменён.** Он появился как временная пометка международной
сборки, международной сборки как отдельной ветки не существует, и с тех пор он
только вводил в заблуждение: деплойер каждый раз печатал «имена не по стандарту»
и переименовывал архив сам.

**Имя `finpilot` зарезервировано за публичным зеркалом.** Внутренние сборки
называются по имени приватного репозитория. Раньше архив назывался `finpilot`, а
уезжал в `personal-finance-dss` — деплойер разруливал это через `REPO_MAP` и
честно предупреждал о расхождении. Теперь расхождения нет.

---

## 2. Что это чинит на стороне деплойера

Из лога прогона `deploy.sh v4.0.0`:

```
имена не по стандарту (обработаю, но переименуй у себя — канон это ТОЧКИ):
  finpilot_v6_25_1_intl.zip → канон: personal-finance-dss-v6.25.1.zip
  finpilot_v6_26_0_intl.zip → канон: personal-finance-dss-v6.26.0.zip
  finpilot_v6_26_1_intl.zip → канон: personal-finance-dss-v6.26.1.zip
  finpilot_v7_0_0_intl.zip  → канон: personal-finance-dss-v7.0.0.zip
```

Четыре предупреждения на четыре архива. Скрипт справлялся, но каждое
переименование — это место, где однажды уедет не в ту репу.

---

## 3. Сопутствующие файлы батча

Именуются по той же версии, без постфикса:

| Файл | Имя |
|---|---|
| Вахтенный журнал | `WATCHLOG-personal-finance-dss-vX.Y.Z.md` |
| Отчёты и документы батча | по смыслу, без версии в имени |

---

## 4. Что НЕ меняется

- Версия внутри `app/config.py`, `CHANGELOG.md` и WATCHLOG — та же SemVer.
- Правило «номер вехи = MAJOR».
- Структура архива: **ровно одна** корневая папка, деплойер разворачивает один
  уровень обёртки.
- Содержимое: полный репозиторий, включая `docs/`, `knowledge/`, `tools/`.

---

## 5. Публичное зеркало

`finpilot-public-mirror-vX.Y.Z.zip` собирается **только** из санитизированного дерева:
whitelist-публикация, без личных имён, без ссылок на приватный репозиторий, без
экспертного сырья сертификации. Сборщик — `tools/publish/finpilot_publish_public.sh`
(`sanitize_tree()` + GUARD 1/3–3/3, режимы `check`/`build`/`push`/`release`) —
существует и работает с v5.18.4, первый пуш прошёл 2026-07-04 (v5.18.3).

**Исправлено 2026-08-13:** строка выше раньше утверждала «санитайзер пока не
реализован» — это была уже один раз пойманная и незакрытая ошибка (см.
`docs/pitfalls.md` PIT-014: то же самое ложно заявлено владельцу в отчёте v8.1.0).
Текущее состояние зеркала (что опубликовано, что ждёт пуша) — не в этом файле, это
не про именование; смотри `docs/public_mirror_state.md` §1/§4.
