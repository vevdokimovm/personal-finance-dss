# Роадмап: бэкенд

---

## P0

### B-01. Белый список ключей в журнале событий
Разделяемый пункт с правовым контуром — см. `02_legal.md` L-01 (ДК-09).
Инженерная часть: заменить `_SENSITIVE_KEYS` (чёрный список) на явный перечень
разрешённых ключей, падать при появлении неразрешённого.

---

## P1

### B-02. `Obligation.type` — свободная строка без enum
🔴 **Источник:** ДК-02, проверено: `app/database/models.py:265` —
`mapped_column(String(32), nullable=False, default="other")`.

Возобновляемый кредит (кредитная карта) не выразим: `grep -rn "credit_card\|revolving"
app/ frontend/src` — **пусто**. Это не косметика: у револьвера другая механика погашения,
и Avalanche-фильтр про него ничего не знает.

### B-03. `payment_day` хранится и выбрасывается
🔴 **Источник:** ДК-04. Поле есть в `models.py:262`, используется в `routes_demo.py`
и в сквозной передаче CRUD, а в `app/core/` не читается ни разу. Дата списания в данных
есть — модель её не видит. Прямо связано с внутримесячной гранулярностью (Г66 Б-03).

### B-04. Валюта не читается ядром (FR-19)
🔴 **Источник:** ДК-03. `grep -rn "currency" app/core/` → **0**. Разделяемый пункт
с матядром (M-06).

### B-05. Вычет ИИС без гейта по дате открытия счёта
🔴 **Источник:** ДК-01, РК-02. `app/core/investment.py:71` —
`if iis_type != "A" or amount <= 0: return None`. Режим ИИС-А закрыт для новых счетов;
код считает вычет всем, кто указал тип «А».

### B-06. Неизвестная категория цели молча получает вес 1.0
🔴 **Источник:** ДК-05. `app/core/goals_priority.py:138,232` —
`CATEGORY_WEIGHTS.get(str(g.get("category","material")), 1.0)`. Fail-silent там, где
канон требует fail-loud: неизвестная категория должна отвергаться, а не получать
средний вес.

### B-07. Истёкшая сессия = молчаливое переключение на общий гостевой пул
🔴 **Источник:** H6 независимого эксперта; частично закрыто `_GUEST`
(`require_account_for_writes`, v8.53.0) — запись перекрыта, **чтение чужого гостевого
пула при протухшем токене осталось**. `get_current_user` возвращает `None` при любом
отказе, `_owner_filter(..., None)` → `model.user_id.is_(None)`, пул глобальный.

---

## P2

### B-08. Контракт `openapi.json` расходится с приложением, и гейт этого не ловит
🔴 **Источник:** H5. `revision_check` пинит только **число путей**
(`EXPECTED_COUNTS["openapi_paths"] = 112`); добавление поля в ответ или ветки 403
число путей не меняет — гейт зелёный по построению. Комментарий
`revision_check.py:38` сам называет этот класс «гейт-с-ложной-уверенностью».

Конкретные расхождения, найденные тогда: `consent_required` — 0 совпадений в
`openapi.json` при десяти роутерах, отвечающих машиночитаемым 403; поле `real_r_bench`
есть в коде и в рукописных типах фронта и отсутствует в снимке.

**Что делать:** сверять снимок с **приложением** (генерировать и диффать), а не
сгенерированный клиент со снимком.

### B-09. Перевод денежного ядра на Decimal
Разделяемый пункт — см. `01_math_core.md` M-09 (РК-17, ВЛ-09).
Инженерное ограничение: схемы ответа остаются `float`, иначе FastAPI отдаёт JSON-строку.

### B-10. Идемпотентность загрузчика выписок
🔴 **Источник:** Г48, анти-урок №11. Стандарт 1С **не идемпотентен по конструкции**:
«синхронизация интервальным способом… при повторной загрузке надо удалить лишние»,
отсюда задокументированная ловушка ««Да» → дубли, «Нет» → потеря платежей».

**У нас:** upsert по естественному ключу (счёт + дата + сумма + номер/RRN), а не по хешу
строки — хеш разваливается от лишнего пробела в назначении платежа. Где номера нет
(в выписках физлиц часто) — явно ослабленный ключ и флаг «дубль возможен».

### B-11. Разнесение операции = соединение со своими данными
🔴 **Источник:** Г48, урок №5, самая дорогая строка темы.

Перед попыткой категоризировать — попытка сопоставить операцию с уже заведённой
сущностью: платёж по долгу (сверка по графику), взнос в цель, известный регулярный
платёж. «18 437,52 ₽ получателю с ИНН банка в дату графика» — это не «категория:
кредиты», а «платёж №14 по долгу id=7». Категория — последний рубеж, а не первый.

### B-12. Отказ от угадывания при неуверенности
🔴 **Источник:** Г48, урок №7. 1С не нашла контрагента — красит строку и оставляет поле
**пустым**, а не подставляет «наиболее вероятного». Прямая противоположность типовому
«отнеси хоть куда-нибудь». Плюс три состояния строки первичны: разнесено · требует
человека · **уже было** (урок №6).

### B-13. Машинная сходимость как инвариант, а не регламент
🔴 **Источник:** Г48, урок №12; Г47 п.5.

Формат 1С несёт остатки и обороты, но сверку итогов все инструкции описывают как действие
бухгалтера глазами. У нас человека в контуре нет: тождество `вх. + приход − расход = исх.`
считается машинно и **блокирует приём**. Для кредитных карт знакозависимо:
`вх. + дебет − кредит = исх.`

### B-14. Жёсткий отказ при несовпадении реквизитов счёта
🔴 **Источник:** Г48, урок №13. БИК/счёт из файла сверяется с заведённым счётом
пользователя **до** разбора содержимого; расхождение → отказ целиком с внятным текстом,
без частичной загрузки. Дешёвая защита от целого класса ошибок.
