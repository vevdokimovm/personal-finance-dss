# Роадмап: данные и справочники

---

## P1

### D-01. Два открытых набора ФССП готовы к использованию
🔴 **Источник:** собственный машинный разбор 23.09.2026 обоих CSV в корпусе.

**Госреестр ПКО** (`a8_fssp_gosreestr_pko_2026-07-31.csv`): 683 записи, **552 действующих**
(131 исключена). По ИНН/ОГРН даёт статус, категорию риска (1 низкая — 488, 3 — 29,
**4 высокая — 21**, 2 — 2), договор страхования ответственности перед должником, сайт,
почту. Применение: ответ на вопрос, который человеку в долговом стрессе нужен раньше
любых советов — **имеет ли эта организация право со мной взаимодействовать**.

**Дела по ст. 14.57 КоАП** (`a8_fssp_adm_14_57_2026-07-08.csv`): 1470 дел за 2026 год,
515 организаций, 77 регионов, штрафов на **87,37 млн ₽**, медиана 50 000 ₽, максимум
500 000 ₽. Применение: история наказаний взыскателя по ИНН.

**Условия использования:** свободно, бесплатно, бессрочно, в том числе в коммерческих
целях, с обязательной ссылкой на источник. Юридического препятствия нет.

🔴 **Качество данных, обязательное к учёту:** в наборе 14.57 колонки `fact` и `total`
пусты во **всех** 1470 строках, `structure of law` — константа. Данные о делах,
**доведённых до постановления**, а не о числе нарушений: отсутствие организации в наборе
не означает отсутствия нарушений. Категория риска в реестре ПКО — метка надзора, а не
ограничение прав (ограничение способов взаимодействия зафиксировано ровно у одной
организации из 683).

🔴 **Содержательная находка:** по числу дел банков **вдвое больше, чем профессиональных
коллекторов** — 482 против 274 (плюс 524 МФО и 190 прочих). Первые три места по сумме
штрафов: Альфа-Банк 7,795 млн ₽ / 85 дел, Сбербанк 7,6675 млн ₽ / 114 дел, ТБанк
4,8725 млн ₽ / 71 дело.

**Поправка к корпусу:** файл `a8_fssp_opendata_2026-09-23.md` даёт разбивку
«банки 482 · МКК/МФО 466 · коллекторские и иные 522» и вывод «банков столько же, сколько
коллекторов». Третья корзина там **остаточная** (1470 − 482 − 466), в неё попало
неопознанное. Прямой счёт по наименованию даёт 274 коллектора. Вывод не ослабевает,
а усиливается.

**Остаток:** из 33 наборов ФССП разобрано два. Прямо назван кандидат «Статистическая
информация ФССП России», срез 06.08.2026 — он закрывает остаток Б8-01 (доля четвёртой
очереди, средний размер удержания).

### D-02. Опознание источника — по совокупности признаков, с резервным ключом
🔴 **Источник:** Г48, урок №3 плюс первоисточник стандарта 1С.

По стандарту `Отправитель=` обязателен **только** в направлении «из 1С в банк»; в файле
выписки его может не быть. Значит опознание банка по этому полю работает **вне стандарта**,
на фактической практике. Порядок: маркер формата → `Отправитель=` → **БИК/счёт
по справочнику ЦБ** (приём Г47 прохода 5). Ни один признак не обязателен поодиночке.

### D-03. Несколько признанных представлений одного факта + приоритет
🔴 **Источник:** Г48, урок №2. 1С решила разнобой банков не борьбой за единственную форму,
а избыточностью с правилом приоритета: «агрегированное поле используется, только если
не переданы атомарные». Наш парсер — так же: список допустимых форм поля и
детерминированный приоритет разбора, а не одна ожидаемая форма.

### D-04. Транспорт и упаковку распознавать по содержимому
🔴 **Источник:** Г48, урок №14, свидетельства практиков: «с 08 апреля сбербанк присылает
ссылку не на ZIP архив, а на текстовой файл», «ТБанк не грузит, тк у них изменился формат
рассылки. Стал архив, а не просто файл». Интеграции вокруг открытого стандарта ломаются
от смены **упаковки**. Смена формата → ошибка класса «формат изменился», не пустой результат.

---

## P2

### D-05. Нормализация назначения платежа — правилами
🔴 **Источник:** Г48, урок №9. Рынок 1С в 2026 ставит ИИ **перед** правилами как
нормализатор входа: дословно «приводит нестандартные назначения платежа к виду,
**понятному алгоритмам**», решение принимают регулярки. Если нормализацию (регистр,
пробелы, переносы, словарь сокращений, транслит) делать правилами — отказ от ML (Г55)
не режет ничего. Если ML когда-нибудь вернётся — его место здесь, а не на месте решения.

### D-06. Точность разнесения = функция качества справочников
🔴 **Источник:** Г48, урок №8. Ни один источник не объясняет низкую точность разнесения
слабостью алгоритма 1С — все объясняют незаполненными справочниками и дублями
контрагентов. Следствие: онбординг, собирающий сущности пользователя (долги с реквизитами
кредитора, цели, регулярные платежи), — **задача точности, а не UX**. Это же объясняет,
почему отказ от ML здесь ничего не стоит: узкое место не в модели.

### D-07. Корпус выписок строим сами — готового нет
🔴 **Источник:** Г47 + Г48. Выделенного открытого парсера PDF-выписок российских банков
не найдено ни на GitHub, ни в выдаче. Корпус — 459 файлов, 265 МБ вне git (Г69);
Сбер сменил раскладку **не менее 22 раз**.

**Лицензии проверены:** среди найденных открытых парсеров выписок GPL/AGPL не встретилось,
доминируют MIT и Apache-2.0. Одно исключение — `ibnu-umer/finance-parser` **без лицензии**:
отсутствие лицензии означает «все права защищены», использовать нельзя.

### D-08. Признак принудительного списания в выписке
🔴 **Источник:** Б8-01. Отдельная задача распознавания. Хорошая новость: назначение
платежа по исполнительному документу **отличимо** (в отличие от P2P-крипты из Г75), то есть
это правило, а не вероятностная модель. Разделяемый пункт с матядром (M-01).

### D-09. Канал курса ЦБ и ISO 4217
🔴 **Источник:** Г73. Нужен, как только валюта перестанет быть невидимой для ядра
(M-06, B-04).

---

## Неопределённость корпуса, которую нельзя забыть

🔴 **291 вердикт корпуса может стоять на кэше прокси** (`g68_jina_cache_probe_2026-09-22`),
перепрогон не сделан. Это не ошибка конкретного числа, а известная граница доверия
ко всему, что добыто через `r.jina.ai`. Учитывать при переносе любых чисел в канон.


### D-10. Возрастные нормы остатка сбережений — чего для объективной шкалы не хватает
**Источник:** P-25 (CBA-MI Observed, пункт 5 — «age-normed percentile of median savings balance»).

Четыре из пяти показателей объективной шкалы вычислимы из разобранной выписки напрямую.
Пятый требует **возрастных перцентилей остатка сбережений по РФ**, и такого справочника
в корпусе нет. Кандидаты: Росстат ОБДХ (распределение по децилям уже есть в
`data/rosstat_obdh_decile_*`), ЦБ по вкладам физлиц. Отдельная задача добора.

---

## Добавлено проходом по Г47-П4 (23.09.2026)

### D-11. 🔴 Faktura — это XML Spreadsheet 2003, а не `.xlsx`; вёрстка известна поимённо
🔴 **Источник:** Г47 проход 4, §П4.1 — разбор живой фикстуры
`faktura_wb_bank_sample.xml` (9 184 Б, 10 операций) и парсера `FakturaParser` из чужого
прода (`mrflxxxme/dds2-sandbox`). Класс материала честный: **вёрстка первичная,
содержимое синтетическое**.

Три прохода корпус искал `.xlsx`/`.xls` и не находил, потому что платформа отдаёт
**XML с namespace `urn:schemas-microsoft-com:office:spreadsheet`** и директивой
`<?mso-application progid="Excel.Sheet"?>`. Один лист, `ss:Name="Выписки"`, шапка ровно
из 11 колонок: `Плат.пор. №` · `Дата` · `Корреспондент` · `ИНН` · `КПП` · `Счёт` · `БИК` ·
`Вх.остаток` · `Оборот Дт` · `Оборот Кт` · `Назначение платежа`.

**Четыре требования к разборщику, каждое проверено по файлу:**
1. **Идентификатора операции НЕТ** — только номер платёжного поручения внутри текста
   колонки 1 (`Плат.пор. № 101 от 05.03.2026`). Дедупликация — хешем
   (дата + сумма + счёт + назначение), как у автора парсера.
2. **Направление — парой колонок** `Оборот Дт` / `Оборот Кт`, в незадействованной **`0`**,
   а не пусто. Отдельного признака дебет/кредит нет.
3. **Суммы — `ss:Type="Number"`, точка как десятичный разделитель.** 🔴 Поправка
   к проходу 3, который предполагал «рубли с запятой»: локали здесь нет вовсе.
4. 🔴 **`ss:Index` — разрежённые строки.** Excel 2003 XML пропускает пустые ячейки
   атрибутом `ss:Index="N"`; наивный `findall` ломает выравнивание колонок. Нужен
   индексно-осведомлённый обход строки (у автора — `_row_cells_indexed`).

**Плюс семантика:** `Корреспондент` — это **вторая сторона**, поля «мы» в файле нет;
владелец счёта берётся из контекста выгрузки, а не из строки.

**+1 банк к охвату:** образец подписан `WB_BANK`, ИНН **9707021650** — ООО «Вайлдберриз
Банк». В списке 30 кандидатов Faktura прохода 3 его не было.

### D-12. 🔴 `iBank2` — словарь тегов выписки и ТРИ текстовых диалекта БИФИТ
🔴 **Источник:** Г47 проход 4, §П4.2.1 — `n-gribov/cyberft_terminal`,
`IBankStatementType.php` + `FormatDetectorIBank.php`. Это производственный код терминала
ЮЗДО против живых банков, не PDF-спецификация вендора.

**Шапка, 8 тегов:** `ACCOUNT`, `BEGIN_DATE`, `END_DATE`, `IN_REST`, `OUT_REST`, `CREDIT`,
🔴 **`DEBET`** (именно так — опечатка вендора, закреплённая в формате), `LAST_OPER_DAY`.

**Операция, 39 тегов.** Две стороны — симметричными семействами `CORR_*` (всегда вторая
сторона) и `CLN_*` (клиент); налоговый блок `CHARGE_*` (КБК, ОКАТО, основание, период).

🔴 **Две поправки к проходу 3, обе повышают качество разбора:**
- **`OPER_ID` в формате ЕСТЬ** — проход 3 его не отметил и записал «направление выводится
  из счетов» как единственный путь. Направление надёжнее берётся сверкой `CLN_ACCOUNT`
  со счётом шапки `ACCOUNT`.
- **В шапке есть контрольные итоги `CREDIT` / `DEBET`** — выписку можно **сверять
  на сходимость**. Такого рычага контроля целостности у нас до сих пор не было ни в одном
  формате; это готовый инвариант для разборщика.

**Опознание и кодировка.** У БИФИТ **три разных текстовых диалекта**, а не один:
`#1|<тип>` (документы v1, регулярка `^#1\|([a-z0-9]+)…`), `Content-Type=doc/<тип>`
(документы v2, составные поля `ИМЯ.<индекс>.КОЛОНКА=значение`), `$OPERS_LIST` (выписка).
Проход 3 знал только третий. 🔴 **Кодировка жёстко `windows-1251`**, UTF-8 не бывает;
пустое значение = `null`, не пустая строка. Разбор строки — `explode('=', …, 2)`:
значение может содержать `=`. Теги сравниваются **без учёта регистра**, неизвестный тег —
пропуск, не ошибка.

### D-13. Перепись платформ ДБО: 301 банк ≠ 301 формат, но одним числом не измеряется
🔴 **Источник:** Г47 проход 4, §П4.5 и итоговая таблица покрытия.

| Платформа | Банков | Единица и источник | Сегмент |
|---|---|---|---|
| **1CClientBankExchange** | де-факто все, кто работает с юрлицами | открытый стандарт «1С» | юрлица |
| **iBank / БИФИТ** | **≥71** поимённо; вендор — «более трети банков» | список вендора + TAdviser | юрлица |
| **BSS** | **39** имён в списке вендора, действующих ≈26; **32,2 % проектов по ДБО** | `bssys.com`, ComNews/TAdviser 12.02.2024 | юрлица |
| **Faktura.ru** | **130** (вторичные); 30 кандидатов + ВБ Банк | `sravni.ru`, `brobank.ru` | **физлица** + юрлица |
| **HandyBank** | пик **147** (2014) → «более 50» (2018) → **после 2018 чисел нет** | TAdviser, РБК 16.02.2018 | физлица |
| R-Style, Diasoft, Compass Plus, «Инверсия» | **чисел нет** | — | — |

🔴 **Почему доли нельзя складывать:** 32,2 % у BSS — доля **проектов по ДБО**, «более
трети» у БИФИТ — доля **банков**; один банк бывает клиентом нескольких вендоров.
Арифметика «32 % + 33 % = 65 %» неверна. Итоговая оценка вилочная: **4 платформы
+ собственные ИБ ≈ 60–70 % банков**.

**Покрытие 301 действующего банка после прохода 4:** живым файлом — 9, платформенным
парсером с образцом или вёрсткой — 94, только по спецификации (BSS) — 26,
**не закрыто — 172**. 🔴 **Три оговорки, без которых числа врут:** (1) поимённый список
66 банков iBank не раскрыт, пересечение с новыми 17 из BSS не проверено — реальная
цифра «не закрыто» лежит между **172 и 189**; (2) список BSS датирован ~2020 (в разметке
шаблон `bss-2020`, внутри ВТБ 24, «Возрождение», Связь-Банк — банков, которых нет);
(3) 🔴 **iBank, BSS и F.Business закрывают ЮРЛИЦ** — см. P-28.

### D-14. ISO 20022 в России уже отдаётся в проде — опцией, а не обязанностью
🔴 **Источник:** Г47 проход 4, §П4.5-добор (официальное руководство БИФИТ
`Corporate_iBank2-Format_Guide.pdf`) и §П4.2.2 (XSD СББОЛ).

Дословный перечень форматов обмена iBank: **1C · CSV** (запятая, точка с запятой или
табуляция) **· DBF · ISO 20022 · MT103 · MT202 · MT950 · iBank2 · УФЭБС**, плюс раздел
**Excel** в настройках экспорта. 🔴 Поправка к проходу 3: добавляются **DBF, ISO 20022,
MT103, MT202, Excel**, а **MT940 вендором не назван** — назван MT950 (выписка
по корреспондентскому счёту). Значит **живой канал `camt` в РФ существует** — опцией
iBank у ~71 банка, до всяких Открытых API.

**Сбербанк для юрлиц — на шлюзе BSS**, доказательство машинное: XSD запроса выписки
СББОЛ объявлена в namespace `http://bssys.com/upg/request`. Атрибут `statementFormat`
ограничен ровно тремя значениями — **`XML` (по умолчанию), `MT940`, `1C`**; есть
`lastRequestTime` для инкрементальной догрузки. Проход 3 Сбербанк в BSS не считал.

---

## Добавлено проходом по Г47-П5 (23.09.2026)

### D-15. 🔴 Форма 101 ЦБ разобрана своим кодом — концентрация вкладов физлиц измерена
🔴 **Источник:** Г47 проход 5, §П5.1. **Свой расчёт по обязательной отчётности банков перед
регулятором** (форма 0409101), воспроизводим: `parse_f101.py` (свой `DbfReader`, `dbfread`
в системе нет), данные `~/raw-originals/.../f101/`, публикации на 01.08.2026 и 01.08.2021.

**Концентрация вкладов физлиц (счёт `42.2`, 01.08.2026, 45 110,5 млрд ₽, 265 банков
из 353):** 50 % держат **2** банка, 80 % — **5**, 90 % — **10**, 95 % — **23**, 99 % — **68**.
HHI = 2 434 (эквивалент 4,1 банка). Топ-10 = **90,75 %**. Средний банк вне топ-50 держит
3,6 млрд ₽ — **0,008 % рынка**.

🔴 **Динамика 2021 → 2026: рынок физлиц сжался вдвое по числу нужных банков.** Порог 90 %
закрывался 29 банками в 2021 и **10** в 2026; 95 % — 50 против 23; 99 % — 114 против 68.
Задача «поддержать выписки всех банков» за пять лет стала объективно **легче**. Прирост
концентрации — не только Сбер (49,58 → 43,99 %), а переток в четвёрку: ВТБ 10,27 → **18,96**
(поглощение «Открытия»), Т-Банк 1,32 → **6,60**, Альфа 1,05 → **6,50**, ГПБ 5,10 → 5,51.

🔴 **Три методические оговорки, без которых числа врут:**
1. **В публикации 2026 года счетов второго порядка НЕТ.** `423` и `40817` схлопнуты
   в агрегаты `42.2` и `408`; расшифровок кодов с точкой (`42.1`, `42.2`, `45.x`…)
   в `NAMES.dbf` **нет вовсе**. Атрибуция `42.2` = вклады физлиц — **наша**, выведена
   по величинам и по дополнению (сходимость с детализацией 2021: `423`+`426` = 19 637,6 млрд
   → `42.2` = 45 110,5 млрд, рост ×2,30 согласуется с депозитным бумом при ставке 16–21 %).
   Регулятором не подтверждена — кандидат на сверку с «Обзором банковского сектора».
2. **Текущие счета физлиц отделить нельзя** — `40817`/`40820` внутри одной строки `408`
   (32 329,3 млрд). По детализации 2021 доля физлиц в `408` = 75,2 %, по банкам отдельно
   не проверяема.
3. **ПСБ, ВБРР и «Пересвет» раскрывают форму 101 только разделами** — из знаменателя
   выпало **11 941,9 млрд** пассива раздела 4. Все доли — доли **среди раскрывающих**;
   истинная доля Сбербанка на 1–2 п. п. ниже (ПСБ — зарплатный банк военнослужащих).

**Технические грабли разбора DBF ЦБ (замерены, стоили двух прогонов):** поля добиты
**нулевыми байтами**, а не пробелами — `strip()` их не убирает, `float("1\x00")` падает,
`REGN` молча читался как `None`; нужен `.replace("\x00","")` **до** `strip()`. Кодировка —
**cp866**, не cp1251. Архивы ЦБ в `.rar`, распакованы `unar` (`unrar`/`7z` нет).

### D-16. 🔴 Банк опознавать по БИК из справочника ЦБ, а не по имени в заголовке
🔴 **Источник:** Г47 проход 5, «Бонус к остатку прохода». Сопоставление своим кодом,
не оценка.

Справочник БИК ЦБ (`cbr.ru/vfs/mcirabis/BIKNew/<ГГГГММДД>ED01OSBR.zip`, ~108 КБ, внутри один
XML `ED807_full`, `WINDOWS-1251`, namespace `urn:cbr-ru:ed:v2.0`) — **1 397 действующих
записей**, 370 кредитных организаций, 302 филиала, 647 уникальных `RegN`. 🔴 **Сопоставление
с формой 101 по регномеру — 353 из 353, ни одного промаха.** Тонкость: `RegN` пишется
с суффиксом (`3553-К` у НКО), сопоставлять по числовому префиксу. У **57** банков более
одного БИК, максимум — **65** у одного банка. Файл выходит **накануне** операционного дня
(за 18.09 — 404 при 200 за 16–17.09).

**Почему это решает задачу опознания источника (смыкается с D-02).** БИК — обязательное поле
**в любом** формате выписки (1С, BSS, Faktura, iBank, ISO 20022, УФЭБС). Значит существует
полный официальный **ежедневно обновляемый** справочник «БИК → банк», покрывающий **все**
действующие банки, включая те ~172, для которых у нас нет ни файла, ни спецификации.
Опознание по БИК надёжнее опознания по имени в заголовке и **не требует образца формата**.

🔴 **И знаменатель хвоста уточнён:** вклады физлиц (`42.2`) держат 265 банков, счёт `408` —
320, хотя бы одно из двух — **321**, а **32 банка с физлицами не работают вовсе**. Считать
непокрытый хвост надо от **321**, а не от 301 и не от 353. Признак розничности берётся
не из справочника БИК, а из формы 101 — наличием остатка по `42.2`.

### D-17. 🔴 Синтетические PDF корпуса для проверки парсера НЕПРИГОДНЫ — геометрии в них нет
🔴 **Источник:** Г47 проход 5, §П5.2 замер 3. Свой замер на 6 PDF.

У `yandexbank/ya_synthetic_card.pdf` и `ya_synthetic_deposit.pdf` распознано **0 операций**,
и причина измерена точно: **все зазоры между словами в строке равны 2,9 pt**
(min = median = p90 = max). Коридоров пустоты нет вовсе — слова выложены подряд с постоянным
шагом; видимая «таблица» в `pdftotext -layout` возникает из расстановки пробелов, а не
из координат. То же у Ozon (медианный зазор 2,9 pt). **Из шести файлов настоящую геометрию
имеет ровно один — реальная выписка Сбера.**

🔴 **Требование к корпусу, теперь с замером, а не по интуиции: нужны ЖИВЫЕ PDF банков,
синтетика здесь не заменитель.** Это тот же вывод, что по Faktura/iBank/BSS в проходах 3–4,
но здесь он доказан числом.

**Две поправки к учёту корпуса, найденные по ходу:**
1. `sberbank/sber_example.pdf` — **не выписка вообще**, внутри статья про PyPDF2/PyMuPDF,
   создана Word 2016. У Сбербанка в корпусе **один** PDF-образец, а не два.
2. `sberbank/sber_document_2022-05-16.pdf` — **рабочий эталон парсера**: единственный файл
   корпуса с настоящей геометрией. Содержит реальные данные владельца счёта, и 🔴 **по решению
   владельца 23.09.2026 это НЕ развилка и не риск** (`CLAUDE.md` §7): опубликованное
   в интернете храним, анализируем, используем; наружу не отдаём, в репозиторий не коммитим.
   Анонимизация и поиск замены не нужны.

## Добавлено проходом по Г58 / Г47-П6 (23.09.2026)

### D-18. 🔴 Агрегаты формы 101 закрыты документом ЦБ — и правило чтения, без которого числа врут
🔴 **Источник:** Г58 §П6.2-бис. Первоисточник добыт: «Описание формата данных» ЦБ
`cbr.ru/Content/Document/File/159279/101-20240201.pdf` (389 926 Б, найден **браузером**;
догадки по шаблону `/vfs/credit/formats/101-*.pdf` дали 22 × 404 — адрес переехал,
это не поломка).

Дословно из п. 3 «Суммы балансовых счетов первого порядка»:
`42.1 = 421 + 425` · `42.2 = 423 + 426` · `43.1 = 438 + 440` · `45.0 = 408 + 452 + 456` ·
`45.1 = 452 + 456` · `45.2 = 455 + 457` · `20.0 = 202 + 203 + 204`.

**Что это даёт и что правит в D-15:**
- 🟢 **`42.2 = 423 + 426` подтверждено дословно** — знаменатель рынка вкладов физлиц
  (45 110,5 млрд) стоит на правильном агрегате, наша атрибуция по величинам была верна.
- 🟡 `43.1 = 438 + **440**`, а не только 438 (440 методом исключения не ловился).
- 🔴 **`45.1` разгадан: это ПАССИВНАЯ сторона 452 + 456** — резервы на возможные потери
  по ссудам юрлицам. Отсюда «массовость» (ненулевой у 257 банков). Гипотеза «кредиты ИП» —
  **снята**.
- 🔴 **`45.0` включает 408**, не только 452.
- 🔴 **`45.2` в прежнем расчёте сложен по ДВУМ сторонам** (38 431,5 млрд): актив — кредиты
  физлицам **36 931,4 млрд**, пассив — резервы по ним 1 500,1 млрд. У Т-Банка актив 2 468,5
  (не 2 643,0). Розничный профиль банков не изменился, числа уточнены.

🔴 **ПРАВИЛО для кода разбора формы 101: агрегат с точкой читать ТОЛЬКО вместе с `A_P`.**
`45.2`, `32.x`, `47.1`, `60.0` существуют **на обеих сторонах баланса**, и сложение без
разделения даёт завышение. Для `42.2` ошибки не было — он только пассивный.

**Две поправки учёта и одна дыра во временном ряду:**
- **Критерий «работает с физлицами» назван явно** (в проходе 5 он назван не был): ненулевой
  хотя бы один из `42.2`, `42.1` или `408`. Таких **323 из 353**, а не 321. Только вклады
  `42.2` — у 265. Разница 58 банков — со счетами, но без вкладов физлиц.
- 🔴 **Почта Банка нет в форме 101, потому что с 01.05.2026 он присоединён к ВТБ** (лицензия
  прекращена, приложение отключено). Из знаменателя убрать; архивные PDF Почта Банка
  у пользователя возможны — класс «исторический формат», приоритет низкий.
- **Публикация формы 101 прерывалась между 01.02.2022 и 01.06.2023** — для временных рядов
  доли рынка это дыра в **16 месяцев**.

### D-19. Список платформ-адаптеров сокращён замером: пять вычеркнуты, один добавлен
🔴 **Источник:** Г58 §П6.5, П6.6, П6.9, П6.11–П6.13.

**Вычеркнуто, с причиной (это экономия работы, а не потеря):**
- **Colvir** — поимённый список клиентов вендора (31 страница) не содержит **ни одного
  российского банка**: 33 клиента в Казахстане, плюс Узбекистан, Туркменистан, Азербайджан.
  Для выписок российских физлиц не нужен вовсе.
- **`reg.ubrr.ru`** — попал в список по ошибке имени поддомена: это **сервис регистрации
  ИП и ООО**, не ДБО.
- **Diasoft, «Инверсия»** — это **АБС (ядра учёта)**, формат выписки задаёт не они, а фронт
  ДБО. Чисел по ним искать больше не нужно: для решения они безразличны.
- **Compass Plus (TranzWare)** — процессинг карт и банкоматов, не интернет-банк; формат
  выписки физлица не задаёт.
- 🔴 **HandyBank** — оператор ПС формально жив (реестр ЦБ на 17.09.2026, запись 0009,
  «национально значимая» с 31.10.2014), но **домен `handybank.ru` не продлён**
  (`paid-till: 2026-09-17`, DNS не резолвится), последнее число участников — «более 50»
  за 2018, страницы банков-участников 2010–2013 годов. В адаптеры **не включать**;
  пересмотреть только если живой файл всплывёт у пользователя.

**Добавлено:** 🔴 **RS-InterBank (R-Style Softlab)** — «более чем 60 российским банкам,
включая банки топ-10», продукт заявлен «как для юридических, так и **физических** лиц».
🔴 **И попутная находка, меняющая приоритет:** R-Style Softlab **входит в группу
Россельхозбанка**, а РСХБ — № 2 непокрытого списка (3,41 % вкладов физлиц). То есть выгрузка
РСХБ, вероятнее всего, на собственной платформе группы. Это **гипотеза для добора**
(ищется по «RS-InterBank экспорт выписки»), не факт.

**Итоговый список адаптеров платформ:** `1CClientBankExchange` · iBank2 · BSS · Faktura ·
RS-InterBank — плюс собственные вёрстки у крупных банков.

🔴 **Сигнатура опознания платформы по URL, замерена:** `bsi.dll?T=RT_…Loader` = **BSS**
(так установлен корпоративный канал МТС Банка — 8-й подтверждённый банк BSS).

🔴 **Урок лестницы каналов, оплаченный ложноположительным замером.** У МКБ, «Точки»,
Уралсиба и Дом.РФ **одновременно** российский УЦ и JS-антибот: `curl -sk` отдаёт **200,
но 1–2 КБ** (заглушка `servicepipe.tech`), а браузер падает на `ERR_CERT_AUTHORITY_INVALID`
(корневой сертификат ставить запрещено). Проходит **Exa** — её краулер снимает обе преграды,
и `r.jina.ai` на поддоменах. **Код «200» без размера тела — не вердикт: в таблицу каналов
писать код И байты.**

## Добавлено проходом по Т35 «эталон калибровки и микроданные» (23.09.2026)

### D-20. 🔴🔴 Микроданные ЦБ (ОДПФ) открыты, скачаны и вскрыты — это замена синтетике
🔴 **Источник:** `calibration_ground_truth_2026-09-10.md` — файлы скачаны и посчитаны
в самой сессии, а не описаны по публикациям.

**Что есть.** Всероссийское обследование домохозяйств по потребительским финансам (ОДПФ),
6 волн 2013–2024, деперсонифицированные микроданные на `cbr.ru` **без регистрации, без заявки,
без соглашения** — пять прямых `curl` дали 200 без кук. 🔴 **Единственное условие
использования — ссылка на источник** (дословно со страницы ЦБ); запрета на производные
и публикацию нет.

**Состав волны 2024:** домохозяйства — **6 079 строк × 2 680 столбцов** (65 МБ CSV),
индивиды — **11 835 × 1 859** (160 МБ). Есть **вес `hhwgt`** (у индивидов **`inwgt`**, не тот же!),
**`psu`** = субъект РФ, сквозной `idind` и повалновые `aid_h…fid_h`. Дизайн —
«стратифицированная, многоступенчатая, вероятностная, территориальная адресная выборка»,
сплит-панель: **без весов выборка смещена по построению**.

🔴 **Шесть технических ловушек, каждая измерена:**
1. Имена внутри архивов — **кириллица в CP866**, `unzip` падает с «Illegal byte sequence»;
   распаковка через `zipfile` в Python с `n.encode('cp437').decode('cp866')`.
2. Объединённые файлы всех волн (`t_ankea.rar` 15,8 МБ, `t_anketa_1.rar` 28,2 МБ) —
   **формат RAR5**; `unrar`/`unar`/`7z` в системе нет, 🟢 **штатный `bsdtar` его читает**.
3. 🔴 **CSV от ЦБ — это ЛЕЙБЛЫ, а не коды** (получены через `haven::as_factor`): фильтровать
   надо по строкам «ЗАТРУДНЯЮСЬ ОТВЕТИТЬ»/«ОТКАЗ ОТ ОТВЕТА»/`NA`, а не по кодам 97/98/997/998 —
   числовые коды живут только в `.RData`.
4. Крупные суммы записаны в R-нотации (`3e+05`) — **нужен float-парсер**, не `int(str)`.
5. Разделитель в файле домохозяйств — `;`, **в индивидуальном — запятая**.
6. Имена переменных **угадывать нельзя**: `p615` выглядит как платёж по карте, а это **остаток
   задолженности** (`p614` — обороты трат), и 🔴 **минимального платежа по карте в анкете нет
   вообще**. Ошибка была поймана только сверкой с публикацией ЦБ — без сверки ушла бы в отчёт.

**Панель:** волна 2024 несёт заполненный `eid_h` (2022) у **5 326 домохозяйств = 87,6 %**.
🔴 Но панель **двухволновая по деньгам**: доход, расходы, месячный поток сбережений и весь
ипотечный блок заполнены **только в 2022 и 2024** (в волнах Минфина 2013–2020 — ровно ноль);
сквозных столбцов во всех шести волнах — **96 из 2 680**. По автономии, целям и желаемому
резерву — полные шесть волн.

🔴 **Смещение неответа названо числом:** 6 079 интервью на **8 940 обойдённых адресов = 68 %**,
и неответ растёт среди новых домохозяйств, то есть среди тех, кто отказывается говорить
о финансах. Веса правят известные маргиналы, но не правят отказ по причине темы. **Любая
популяция, построенная из ОДПФ, наследует это смещение, и метриками SRMSE его не увидеть —
референс сам смещён.** Это надо писать в отчёте прямо.

**Два дополняющих источника.** **ВНДН Росстата** — покредитный блок со ставкой (вопрос 5,
код 3), платежом, остатком и годом договора подтверждён Указаниями к НДН-2025; объём —
ежегодные волны плюс широкомасштабная **160 тыс. домохозяйств** (2022). 🔴 Но: блок **усечён**
(«указываются наиболее крупные из них»), остаток — на конец года, ставка — **самоотчёт**,
а не ПСК; вопросов о досрочном погашении и сумме сбережений **нет** (проверено `grep`
по 523 КБ). Рабочий канал к микроданным — зеркало **tochno.st под CC BY 4.0**;
`rosstat.gov.ru/microdata` — 404, `obdx.gks.ru` за логином. **RLMS-HSE** — публичный слой
1994–2025 без заявки, сильна доходами и занятостью, слаба покредитной детализацией.

🔴 **Чего в ОДПФ НЕТ, и это четыре дыры, каждая критична:** ставки по потребкредиту
(«С4.8» — это **канал выдачи**, а не процент; из 645 назвавших сумму ставку не назвал никто,
потому что вопроса нет) · просрочки по потребкредиту (есть по картам, авто и недвижимости;
базовая частота по публикации ЦБ **0,9–2,5 %**) · факта досрочного погашения (только намерение
по рефинансированной ипотеке) · суммы, идущей на цель (только бинарное «начали копить»).
**Ставку по потребкредиту вменять** из публикуемой ЦБ ПСК по сроку (`p96_*`) и каналу
(`c48_*`), долю вменённых публиковать; распределение вменённых сверять с самоотчётными
ставками ВНДН.

### D-21. Карта альтернативных источников: что закрывает нашу дыру, а что нет
🔴 **Источник:** `calibration_ground_truth_2026-09-11_subagent_B_alternatives.md` — по каждому
источнику проверялись четыре переменные: досрочное погашение, сбережения по целям, ставка
по каждому кредиту, просрочка.

| Источник | Досрочное погашение | Цели сбережений | Ставка по кредиту | Просрочка | Доступ |
|---|---|---|---|---|---|
| **SCF 2022 (США)** | 🟢 **ДА, категорией** по каждому кредиту (`X7534` и серия), суммы нет | цели ДА (до 6, ранжированы), **сумм нет** | 🟢 ДА по каждому | 🟢 ДА (`X3004`, `X3005`) | **публичный, без заявки** (проверено `curl`) |
| **HFCS 2021/2023 (ЕЦБ)** | 🔴 **НЕТ вовсе** (ноль совпадений по анкете и каталогу) | ДА, бинарно, 12 целей | 🟢 ДА (`HC090$x`) | 🟢 ДА + порог 90 дней | **заявка + паспорт + CV + форма** |
| **PSID (Мичиган)** | нет (только «behind») | — | — | ДА | публичный, но **за регистрацией** |
| **Бюро РФ (НБКИ/ОКБ)** | 🟡 по природе данных ДА (факт против графика) | 🔴 нет принципиально | по природе ДА | по природе ДА | публичных микроданных **не найдено** |
| **КОУЖ / ВНДН** | не установлено (вопросники внутри архивов) | не установлено | ДА (ВНДН, D-20) | частично | открыто, **CC BY 4.0** |

🟢 **Главная внешняя опора для нашей постановки задачи:** в HFCS «**f — Paying off debts**» стоит
**отдельной целью сбережения** наравне с «e — Provision for unexpected events» и покупками.
**То есть трёхчастное деление потока (долг / резерв / цели) — не наша выдумка, а структура
гармонизированного европейского обследования.** Формулировки вопросов HFCS и SCF почти дословно
совпадают (HC1250 ≈ X3004) — обследования намеренно сопоставимы.

🔴 **И ключевое ограничение, объясняющее, почему бюро не спасают:** кредитная история по 218-ФЗ
содержит фактические платежи против графика, то есть **досрочное погашение в данных бюро
наблюдается по определению**, плюс ставка и просрочка. Но в бюро **нет доходов, расходов,
сбережений и целей** — то есть **левой части нашего уравнения (свободный поток) там нет вообще**,
а без неё «как поделить поток» не восстанавливается. Бюро закрывают три переменные из пяти
и не закрывают главную.

**Три технические оговорки PSID, применимые к любому источнику самоотчёта:** компоненты
богатства **импутированы hot-deck** (доля ненулевая, сравнивать распределения с учётом этого);
🔴 `credit card debt` **исключает convenience use** — это переносимый остаток, а не оборот
по карте, смешать значит завысить долговую нагрузку; и 🔴 **57 % ответивших «нет» на сложный
вопрос об активах отвечали «да» на простой** — формулировка меняет ответ радикально, что прямо
касается нашего опросника (F-11).

**Практический путь к вопросникам КОУЖ/ВНДН, не качая гигабайты:** архивы на зеркале
**tochno.st под CC BY 4.0** качаются **по отдельным обследованиям**, и внутри каждого лежат
**вопросники и кодбуки** (дословно со страницы каталога). Уточнённые годы: КОУЖ — 2011, 2014,
2016, 2018, 2020, 2022, 2024 (раз в два года); ВНДН — 2012, 2014–2025 ежегодно; ОДПФ — шесть волн.

## Добавлено проходом по Г18 «закрытое насовсем» (23.09.2026)

### D-22. Росстат: структура использования доходов добыта, но ряд обрывается на 2018 — и три приёма добычи, годные повторно
🔴 **Источник:** `closed_forever_retry_2026-09-16.md`, §Г18.15 (Е.2) и «Приёмы, которые
окупились».

🔴 **`urov_14g` не отсутствовал — он лежит под версионным именем.** Прямое имя
`rosstat.gov.ru/storage/mediabank/urov_14g.xlsx` → **404**; листинг раздела `folder/13397`
(HTTP 200, 899 396 байт) содержит 22 ссылки `urov_*`, и реальный файл —
`urov_14g(1)_349665.xls` (**HTTP 200, 32 256 байт**, читается `xlrd`). Заголовок внутри:
«Структура использования денежных доходов (динамика)», отметка «Обновлено 26.06.2019»,
ряд **1970…2018**.

**Хвост ряда, % от всех денежных доходов** (2013 → 2018): покупка товаров и услуг
73,6 → **77,0**; обязательные платежи и взносы 11,7 → **12,2**; **сбережения 9,8 → 5,6**
(в 2015 было 14,3); покупка валюты 4,2 → 3,7; прирост денег на руках 0,7 → 1,5.
Для контекста: 1970 — 86,2 / 10,0 / 4,0; 1998 — 77,7 / 6,1 / 2,5; 2010 — 69,6 / 9,7 / 14,8.
🔴 Для нас значимо соотношение «обязательные платежи ≈ 12 % против сбережений ≈ 5,6 %»:
это агрегатная картина того же, что ОДПФ показал на микроуровне (D-20) — платежи по долгам
стабильно больше, чем то, что откладывается. Как якорь годится только до 2018:
🔴 **новая методология после 2018 в этом файле не добыта** — `urov_2010-2024.xlsx`
(7,7 МБ) оказался **другим показателем** (социальные выплаты и налогооблагаемые доходы
по муниципальным образованиям, помесячно по ОКТМО, 2010–2024). Полезен сам по себе как
муниципальный разрез доходов, но структуру использования не заменяет.

🔴 **Три приёма добычи, которые надо держать в каноне.** (1) **«404 на файле» ≠ «файла нет»:**
сначала листинг раздела, потом ссылка из него, никогда — сконструированный URL.
(2) **Фильтр в URL может обнулить выдачу:** `af=on` («только актуальные») в поиске ЕИС давал
**0 записей** там, где без него — **16**; пустая выдача — повод проверить параметры,
а не делать вывод. (3) **`curl -sk --http1.1` открывает весь российский госсегмент**
(`zakupki.gov.ru`, `cbr.ru`, `nalog.gov.ru`, `rosstat.gov.ru`), при этом российский корневой
сертификат **не устанавливается**. Плюс мелочь, стоящая часа: системный Python блокирует
`pip install` по PEP 668 — для `xlrd`/`openpyxl` заводится `venv` в скрэтчпаде.

## Добавлено проходом по Г46 фаза B «сборка каталога data/» (23.09.2026)

### D-23. 🔴 Каталог `data/` собран, воспроизводим побайтово — и по ходу проверки нашлись четыре дефекта данных, один с неверным выводом в паспорте
🔴 **Источник:** `data_catalog_build_2026-09-17.md` — прогоны B1 и B2 целиком (35 CSV,
10 скриптов `tools/data/`, сверка манифеста и паспорта).

**Что есть.** 35 рядов CSV плюс `MANIFEST.md`, **13 МБ** при лимите 30; десять скриптов
`build_*.py` на stdlib + openpyxl (в venv нет ни `pandas`, ни `scipy`, ни `xlrd` — новых
зависимостей не заводили, `norm_ppf` реализован по Acklam); 🔴 **полный пересбор ≈ 10 секунд
и 35 из 35 файлов совпадают по sha256 побайтово** (проверено дважды — до и после исправлений).
Гейты: `flake8 --max-line-length=88` — 0, `mypy tools/data` — чисто, функции ≤ 50 строк,
docstrings по AST.

🔴 **Четыре дефекта данных, найденных проверками, а не глазами** — все исправлены в скриптах,
руками не правилось ничего:
1. **`cbr_deposit_rate_individuals_month.csv` — 1 191 дублированный ключ.** Лист ЦБ несёт два
   блока колонок с одинаковыми подписями сроков: «Физических лиц» и «Нефинансовых организаций»,
   скрипт брал оба. 🔴 **И это единственный дефект, изменивший вывод:** в паспорт попала ставка
   **14,59 % (бизнес)** вместо **11,51 % (люди)**, то есть спред «кредит минус вклад»
   не 4,3, а **7,4 п. п.** Для нашей модели это прямо влияет на порог «гасить или копить».
2. **`rosstat_population_age_groups.csv` — 450 дублей:** таблица состоит из трёх одинаковых
   блоков (всё население, городское, сельское), разрез блока терялся; теперь `breakdown` несёт
   `total|…`, `urban|…`, `rural|…`.
3. **`rosstat_cpi_kipc_year.csv` — не было 2022 года:** в шапке Росстата год подписан `20221)`
   (год плюс сноска), строгая проверка `\d{4}` молча выбрасывала **весь год**.
4. **`rosstat_cpi_basket_weights_year.csv` — не было 2013–2021:** сводный лист «по колонке
   на год», а скрипт брал только первую колонку.
🔴 **Приём, который это нашёл, стоит держать:** сплошная проверка «ключ `date+unit+region+
breakdown` уникален» по всем файлам и отдельный скан «годы-дыры внутри диапазона файла».
Второй завёлся случайно — понадобился ряд ИПЦ по годам, и 2022-го не оказалось.

**Сверка паспорта `docs/model/rf_empirical_profile.md`: 114 чисел, 107 подтверждены,
7 исправлены** — одно по существу (ставка вкладов выше), шесть — словесные обобщения поверх
верных чисел: «60 000 ₽ ≈ 53-й процентиль» → **54,4**; «годовая норма врёт по кварталам
в 4–7 раз» → **4–5**; «разрыв спокойное/кризисное втрое» → **более чем вчетверо** (11,0 %
против 48,1 %); «обычные 1,5 % за месяц» → **2,5 % в calm и 11 % в crisis**; «разрыв КИПЦ
до 5 п. п.» → **5,5**; окно ИПЦ подписано не тем месяцем.
**Манифест:** все 35 описаны, лишних нет, у каждого ряда прямой ответ «можно ли в продукт»;
🔴 **у 6 из 40 сокращённых sha256 оригиналов был опечатан хвост** — по такому сокращению файл
не опознать, это ошибка реквизита; исправлены все 40 к единому виду «8…8» и добавлен раздел
полных хэшей на 67 файлов. 🔴 **И найдена ошибка лицензии:** `world_benchmarks.csv` был помечен
целиком как «общественное достояние США» — на деле `SP500` запрещён к воспроизведению без
письменного разрешения S&P, `VIXCLS` — copyright CBOE «Citation Required», `MORTGAGE30US` —
Freddie Mac «Reprinted with permission». Внутренние расчёты законны, **показ пользователю
sp500 — нет**. ⬜ Не проверены: условия LBMA на показ котировок золота и «Правила использования
информации» MOEX.
**Содержательные находки самих рядов:** 🔴 **σ логнормали дохода тридцать лет держится
в коридоре 0,65–0,79** (1995 — 0,654; 2010 — 0,782; 2025 — 0,783) — значит для генератора
когорт это **константа, а не параметр подбора**; 🔴 **сезонность сбережения**, которой
не было ни в одном стенде: прирост финансовых активов в I квартале 3–6 % дохода,
в IV — 18–22 % при годовой норме 14,1 % (2025); разметка режимов курса на 2015+ —
calm 116 месяцев, stress 16, crisis 9. Независимая сверка качества: цена вторички по Москве
у Росстата 391 067 ₽/м² против 386 912 у индекса MREDC — **сошлись в пределах 1 %**.
⬜ Задолженности сбора: цены жилья 2022–2024 (выпуски не скачаны), нормализация подписей
сроков ЦБ (`''до востребова-ния''**`), помесячные ряды доходов — только через ЕМИСС.
🔴 **Расхождение ПДН, которое надо помнить при цитировании:** «18 % выдач с ПДН 50+
в I кв. 2026» неверно — 18 % это **IV квартал 2025** по необеспеченным в целом, а в I кв. 2026
это **14 % по кредитам наличными и 20 % по картам**; NPL 30+ на 3 MOB — **0,8 % наличными
и 2,7 % по картам**, а 5,4 % — только сегмент наличных с ПДН выше 80. Доля портфеля 35 %
на 01.04.2026 подтверждена.

### D-24. Ландшафт источников: ЕМИСС отдаёт только мелкие показатели, `data.gov.ru` про деньги населения почти пуст, а ФНС даёт то, чего нет ни у кого
🔴 **Источник:** тот же файл, прогоны «ЕМИСС» (Е1–Е3) и «data.gov.ru» (Д1–Д4).

**ЕМИСС.** 🔴 **Разгадка прежнего `000`: это не отказ в выгрузке и не нехватка поля,
а обрыв долгой генерации на шлюзе примерно через 45 секунд.** Малый показатель (27 КБ)
отдаётся за 3 с, большой (страница 625 КБ) не успевает собраться; увеличение таймаута
на нашей стороне до 900 с не помогает — рвёт сервер. Механизм — обычная HTML-форма
`POST /indicator/<id>/download` с одноразовым токеном Struts со страницы.
🔴 **Путь «Выбранные данные» (который сузил бы выгрузку фильтрами) защищён от автоматизации
в самом коде портала:** `if (navigator.webdriver || !e.originalEvent || !e.originalEvent.isTrusted)
return;` — работает только настоящий клик человека, и по запрету задания он **не эмулируется**.
reCAPTCHA на странице есть, но только на формах поддержки, к выгрузке не относится.
🔴 **Приём, экономящий половину запросов:** слаг набора кодирует номер показателя словами —
`7708234640-sixazeroasevenafiveaeight` = ведомство Росстата + показатель **60758**, значит
`meta.xml` для поиска номера запрашивать не нужно, декодируется локально.
Отобрано **263 профильных набора** из каталога 7 073 (доходы 81, демография 86, зарплаты 25,
кредиты и сбережения 23, расходы 17, цены 14, занятость 9, жильё 8); выгрузка —
последовательно по одному, журнал пишется **после каждого набора**, чтобы обрыв не терял
прогресс. Пробный прогон 5 из 5 успешен, причём «Величина прожиточного минимума» 3,8 МБ
за 17,7 с — то есть порог обрыва не жёстко 45 с на любой файл.

**`data.gov.ru`.** Каналы: системный `curl` ломается на рукопожатии TLS (`sslv3 alert bad
record mac`), Python `urllib` отдаёт оболочку SPA, `r.jina.ai` отрисовывает список,
API портала — **401 даже с cookie**. 🔴 **Рабочий вход — кнопка «Экспорт» из бандла:**
`GET /portal-back/api/v1/registry/csv` (и `/json`) отдаёт реестр целиком одним запросом
против 704 страниц SPA. Но 🔴 **в экспорте из 58 колонок заполнены по сути три** (название,
организация, дата изменения) — **нет идентификатора, категории, формата, лицензии и ссылки
на файл**: это оглавление, а не каталог адресов.
**Вывод по содержанию, честный и отрицательный:** 7 037 наборов, 501 поставщик, и про деньги
населения там почти ничего. ХМАО — 1 075 наборов, Росстат — 787, но нечёткое сравнение
названий с каталогом ЕМИСС дало **совпадение 1 из 787**, а выборка — микроперепись 2015 года
и образование по возрастам. «Доходы» и «расходы» (166 наборов) — это **исполнение бюджетов
и декларации чиновников**; «кредит» — в основном ложные совпадения со словом «аккредитация»;
🔴 **Банка России среди 501 поставщика нет вовсе** — ставки, долги и вклады остаются за cbr.ru.
Свежесть обманчива: 2015–2017 годы дают 2 264 набора, большая часть реестра не обновлялась годами.
🔴 **Единственное реальное приобретение — ФНС, форма 1-ДДК (`data.nalog.ru`), и его надо брать
с сайта ФНС, а не с портала:** число людей и суммы **имущественного вычета (покупка жилья
и проценты по ипотеке), социальных (лечение, обучение) и инвестиционных (ИИС) по субъектам**,
годовой ряд с 2016 года. Ни Росстат, ни ЦБ этого не дают, а для продукта это эмпирическая база
модуля «налоговый вычет как источник денег под цель»: сколько людей в регионе реально возвращают
НДФЛ и какого размера. Плюс мелочь на случай цели «автомобиль» — ежедневная стоимость топлива
по городам ХМАО. **Лицензионный режим** наборов ФНС и Минэкономразвития — общедоступная
информация госоргана по методрекомендациям 4.0, использование со ссылкой; прямого ответа
«можно ли в коммерческий продукт» сам портал не даёт, режим берётся по паспорту ведомства.
