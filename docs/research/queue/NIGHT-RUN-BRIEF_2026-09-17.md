# Мандат автономного прогона FINPILOT — вечер/ночь 17→18.09.2026

> Действующий. Заведён 17.09.2026 по решению владельца («у нас ночной режим есть, давай
> протестим, чтобы ты дальше работал»). Запускается `night.sh` из корня репозитория
> `personal-finance-dss` — иначе не подхватятся `CLAUDE.md` и хуки проекта.

## 1. Предмет и порядок

Ты — вахта FINPILOT. Первым делом прочитай `CLAUDE.md` проекта, затем
`docs/research/queue/GAP_QUEUE.md` — разделы Г53, Г54, Г55, Г47. Это источник правды.

Доводишь **очередь исследований**, строго по одному агенту за раз, в таком порядке:

0. 🔴 **Проверка на второго живого агента.** Прогон мог стартовать, пока в дневной сессии ещё
   работает агент Г54. Команда:
   `find /Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/raw -name "*.md" -mmin -10`.
   Если в выдаче есть `llm_vs_product_2026-09-17.md` или `_g54_sub_*` — чужой агент жив:
   ничего не запускай, подожди 10 минут (`sleep 600` внутри команды) и проверь снова.
   Запускать своего агента можно только когда файлы Г54 не менялись 10 минут подряд.
1. **Г54** «зачем продукт, если есть LLM». Проверь файл
   `docs/research/raw/llm_vs_product_2026-09-17.md`. Если в нём уже есть раздел `ВЕРДИКТ` —
   Г54 закрыта, отметь в очереди и иди дальше. Если файла нет или вердикта нет — агент
   предыдущей сессии оборвался: запусти `base-kit:researcher` с промптом
   `docs/research/queue/prompts/g54_prompt.md`, добавив в начало: «ПЕРЕЗАПУСК: файл результата
   уже частично написан — прочитай его и ДОПИСЫВАЙ недостающие разделы, не переписывая готовые».
2. **Г55** «чем закрыть потери от отказа от ML — алгоритмами». Промпт
   `docs/research/queue/prompts/g55_prompt.md`. 🔴 Рамка владельца: ML не используется вообще.
3. **Г47 проход 5** (П5.1 → П5.3 → П5.2 → остаток). Промпт
   `docs/research/queue/prompts/g47p5_prompt.md`. Форма 101 ЦБ уже на диске.
4. Дальше по очереди из `GAP_QUEUE.md`: **Г48 (1С) → Г49 (бухгалтерия) → Г50 (стек бизнеса) →
   Г52 (сценарии) → аудит покрытия № 5**. Промпты этих тем — в их разделах очереди; оформи
   промпт по образцу файлов в `docs/research/queue/prompts/` (те же жёсткие правила внизу).

**На каждую тему:**
- перед запуском агента — три гейта (`research-channels-gate`, `exa-gate`,
  `websearch-budget`) через `python3 <абсолютный путь>/.claude/hooks/<имя>.py` с JSON
  `{"hook_event_name":"PreToolUse","tool_name":"Agent",...}`; все три — `exit=0`.
  Красный гейт: замерить `uptime` и скорость канала (сегодня гейты краснели из-за забитой
  сети, не мёртвых каналов), повторить до трёх раз с паузой 60 с; не прошло — пропустить
  тему с записью причины в её раздел очереди и перейти к следующей;
- отметить в очереди «🟡 В РАБОТЕ»;
- 🔴 **дождаться завершения агента** — не заканчивать ход, пока агент не вернул отчёт:
  в режиме `claude -p` конец хода может убить фоновый агент;
- агент оборвался — проверить файл результата; если растёт — продолжить того же агента
  (`SendMessage`), если нет — перезапустить с пометкой «ПЕРЕЗАПУСК»;
- по завершении — выжимку 10–20 строк в раздел темы очереди со статусом «✅ ЗАКРЫТА»
  и путём к файлу; следующая тема.

## 2. Границы

**Пишешь только в:** `docs/research/raw/`, `docs/research/queue/`,
`~/raw-originals/finpilot-data/` (первоисточники).

**Не трогаешь:** код приложения (`app/`, `frontend/`, `tests/`), `docs/math_model.md`,
`VERSION`, `CHANGELOG.md`, `docs/WATCHLOG.md`, `app/config.py`, архивы.
🔴 **Версии, CHANGELOG, WATCHLOG и архив — ОТЛОЖЕНЫ решением владельца 17.09.2026**
(«без версий пока что отставить»). Батч не закрывать.

## 3. СТОП

Полный список — `~/repos/base-repo/06-autonomous-mode-kit/STOP_CONDITIONS.md`. Останавливаешься на:

1. **Необратимое вовне** — публикация, деплой, тег, релиз, push, удаление, отправка писем
   и сообщений. Ничего на GitHub, кроме чтения (`gh api` поиск).
2. **Секрет в материале** — записать где и какого класса, не сам секрет.
3. **Речь голосом владельца** — никаких писем, постов, форм от его имени.
4. **Реальные персональные данные владельца** — не использовать никогда (правило 7).
5. 🔴 **Российский корневой сертификат** — не ставить ни при каких условиях.
6. **Больше одного агента одновременно** — запрещено (правило 11, сожжены три лимита).

Стоп — это стоп ветки: записал, пропустил тему, пошёл дальше.

## 4. Правила, которые нельзя забыть

- Абсолютные пути во всех аргументах Bash (хук PIT-018). `gh api` — полным URL.
- Агенту: потолок подагентов 2, сырьё в файл ДО ответа, раздел за разделом.
- Решений за владельца не принимать; развилки записывать в раздел темы как «ждёт владельца».
- В конце прогона — короткий отчёт в
  `docs/research/queue/NIGHT-RUN-REPORT_2026-09-17.md`: какие темы закрыты, что пропущено
  и почему, что ждёт владельца.
