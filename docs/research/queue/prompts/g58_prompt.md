FINPILOT, тема Г58 — «Хвост корпуса выписок и рынка» (проход 6 темы Г47).

ПЕРВЫМ ДЕЙСТВИЕМ прочитай постановку:
`grep -n "^### → Г58" /Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/queue/COVERAGE_AUDIT_5.md`
и `grep -n "^## Г47" /Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/queue/GAP_QUEUE.md`
(раздел Г47, блок «Остаток на проход 5» и итоги проходов 1–5). 8 пунктов.

## Что уже сделано — не повторяй
- `raw/bank_statement_corpus_2026-09-17.md` (проходы 1–3), `..._pass4_...`, `..._pass5_...`:
  301→321 банк, платформы (iBank ~71 банк, Faktura, BSS), 10 нужных парсеров, форма 101 ЦБ
  разобрана своим кодом, справочник БИК ЦБ сопоставлен с формой 101 — **353 из 353, ключ
  `БИК ↔ RegN` построен** (сопоставлять по числовому префиксу `RegN`);
- 🔴 покрытие по ЛЮДЯМ, а не по деньгам: 5 форматов = 81,6 % счетов, но лишь ~62 % пользователей
  целиком; 10 = 78,5 %; 30 = 90,1 %. Цель — 10 банков по охвату людей;
- универсальный разбор PDF: x-проекция **по странице и только по строкам-кандидатам**;
  🔴 для кредитных карт тождество знакозависимо: `вх. + дебет − кредит = исх.`;
- 🔴 пять синтетических PDF корпуса для проверки геометрии НЕПРИГОДНЫ (все зазоры 2,9 pt);
  настоящую проверку выдержал 1 файл из 6. **Нужны живые PDF других банков** — владелец
  17.09.2026 разрешил работать на реальных выписках, они остаются на устройстве.

## Главное к добору
1. Поимённый список 172–189 непокрытых банков — ключ `БИК ↔ RegN` уже есть, задача механическая.
2. Живые файлы Faktura, `iBank2` (`$OPERS_LIST`), BSS-текст, BSS-DBF — ни одного до сих пор.
3. Вендор ДБО МКБ: 🔴 **`mkb.ru` ожил** — `curl -sk --http1.1` даёт 200 (замер 18.09.2026),
   Г47 писал «не берётся принципиально» и был неправ.
4. Платформы `colvir.ru`, `reg.ubrr.ru` — 🔴 тоже ожили от `-k`.
5. Число клиентов-физлиц: МКБ, Яндекс, WB, Почта Банк, Райффайзен, РСХБ, Русский Стандарт,
   Уралсиб, Дом.РФ. Разрезы мультибанкинга по возрасту, доходу, селу. СберИндекс, НБКИ/ОКБ.
6. Точность разбора на незнакомых вёрстках — нужны живые PDF.
7. `confluence.korona.net` (спецификация Faktura, включая выгрузку физлица) — партнёрская
   авторизация, 302/401. Это класс «нужен доступ владельца», не пытайся ломать.

## 🔴 Каналы — норма, оплаченная аудитами № 5 и допроверкой 18.09.2026
Четыре причины отказа, у каждой свой инструмент:
- **антибот, JS-проверка, Cloudflare** → 🟢 **БРАУЗЕР** (`mcp__plugin_chrome-devtools-mcp_chrome-devtools__new_page`
  → `take_snapshot`). До 18.09 агенты браузером не пользовались ВООБЩЕ, потому что его не было
  в лестнице каналов; так потеряли `dl.acm.org` (открывается, статья FREE ACCESS);
- **российский УЦ в цепочке TLS** → 🟢 `curl -sk --http1.1`; 🔴 браузер здесь бессилен,
  сертификат ставить ЗАПРЕЩЕНО;
- **гео-фильтр** → 🟢 `r.jina.ai`: выход туннеля во Франции, `data.gov.ru` из-под него 000,
  через прокси **200 и 21 986 байт**;
- **пейволл, форма входа** → честный отказ.
🔴 Вердикт «не добыто» недействителен без попытки всеми каналами; в отчёт — все коды.
Абсолютные пути везде, включая `-o` (хук PIT-018 блокирует относительные).

## Правила
1. Файл создать ПЕРВЫМ действием, дописывать после КАЖДОГО вызова:
   `/Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/raw/bank_statement_corpus_pass6_2026-09-18.md`
2. Подагентов не больше двух, по одному; делить между ними СПИСКИ (банки, платформы), не темы.
3. Первичка дословно, класс материала помечать. «Не нашли» ≠ «этого нет».
4. Развилки — только деньги, ключи, доступы. Остальное решай замером и пиши вывод.
5. В конце — выжимка 10–20 строк, статус ✅ в очереди переставь сам. Версию не инкрементируй.
