FINPILOT, тема Г64 — «Код открытых похожих проектов: что взять себе».

ПЕРВЫМ ДЕЙСТВИЕМ прочитай постановку:
`grep -n "^## Г64" /Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/queue/GAP_QUEUE.md`
— шесть пунктов, что уже есть, фильтр лицензий.

## Отличие от Г14 — главное
Г14.1 спрашивал «делает ли кто-то то же, что мы» (ответ — никто). Г64 спрашивает другое:
**что конкретно из чужого КОДА взять себе** — алгоритм, формат, тест, приём. Итог по каждому
найденному: репозиторий · файл и строки · лицензия (прочитана ФАЙЛОМ, не по полю API) · что
берём (код / идею / тестовые данные / ничего) · почему.

## Что уже прочитано — не повторять
Actual Budget — детекция регулярных платежей (Г55 §6.2, `raw/ml_gaps_closed_by_algorithms_2026-09-17.md`);
байесовский матчер GnuCash (Г59, `raw/legal_license_tail_2026-09-18.md`); парсеры выписок и
лицензии 396 репозиториев-парсеров (Г47, Г58, Г59); `money-keeper` — ошибка «оплата ТСП по СБП
как перевод» (Г63, `raw/sbp_qr_roles_liability_2026-09-19.md`). Кандидаты из Г14.1
(`raw/desktop_web_catalogs_sweep_2026-09-12.md`): `better-debt`, `FreeD`, `bucketwise`,
`cdinnison/ray-finance` (`calculate_debt_payoff`), `loudsun1997/SenseiFi` (код НЕ читался).

## 🔴 Приоритет — пункт 2: эталонные тестовые векторы
Готовые графики погашения, аннуитеты, досрочки, округления из ТЕСТОВ открытых проектов —
независимая проверка нашей математики. Сверить с `docs/math_model.md` (версия внутри файла)
и существующими тестами `tests/`: где наши числа совпадают, где нет и почему. Если
расхождение — это находка для синтеза, код НЕ править.

## Прочие пункты
1. Погашение долгов: avalanche / snowball / гибриды — минимальные платежи, досрочки, промо-ставки,
   округление копеек.
3. Прогноз потока и бюджет «конвертами»: Actual Budget, Firefly III, `bucketwise`.
4. Импорт и нормализация: OFX / QIF / CSV / camt в hledger, beancount, Firefly III, Actual —
   модель операции, склейка переводов «себе», дедупликация повторного импорта.
5. Категоризация правилами (не ML): движки правил Actual Budget и Firefly III, память правок.
6. Хранение денег: целые копейки против decimal, мультивалюта, округление (сверить с навыком
   `finpilot-money-format` в `.claude/skills/`).

## 🔴 Лицензии — фильтр
Продукт закрытый. GPL / AGPL (GnuCash, Firefly III, monopoly) — только идеи и алгоритмы, НЕ код и
НЕ копии тестовых файлов; числа из тестов (факты) — можно, как проверку. MIT / BSD / Apache — код
с атрибуцией. Без лицензии — только справка. Лицензию читать файлом LICENSE, не по `spdx_id`.

## Каналы
Главный — GitHub: `gh api`, `gh search code` (gh авторизован), `api.github.com`, raw.githubusercontent.com.
Далее Exa, deepwiki. Код можно клонировать в scratchpad (`git clone --depth 1`) и читать там;
в репозиторий FINPILOT чужой код НЕ класть. Абсолютные пути во всех аргументах Bash, включая `-o`.
- Код 200 без проверки тела — не вердикт. Капчи владельцу не отдавать. Свою вкладку закрывать.
- Тяжёлые процессы не запускать; клоны — `--depth 1`, после работы удалить.

## Правила
1. Файл создать ПЕРВЫМ действием, дописывать после КАЖДОГО вызова:
   `/Users/vasyaevdokimov/repos/personal-finance-dss/docs/research/raw/open_source_reuse_2026-09-19.md`
2. Подагентов не больше ОДНОГО; на репозиторий не больше 3–4 попыток.
3. Итог — таблица «репозиторий · файл · лицензия · что берём · зачем» + раздел «какие решения меняет»
   по сферам: матядро, бэкенд, тесты, данные.
4. ML в продукте не используется; реальные выписки остаются на устройстве.
5. Решения принимай сам с обоснованием. Владельцу — только деньги, ключи, доступы.
6. В конце — выжимка 10–20 строк; ✅ по Г64 в `GAP_QUEUE.md` (строка таблицы и раздел).
   Версию, CHANGELOG, WATCHLOG, код и канон не трогать.
