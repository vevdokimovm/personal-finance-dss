# Манифест публичного зеркала `finpilot` (v5.18.4)

> Формализация whitelist'а перед ПЕРВЫМ пушем зеркала. Механика — уже готовый
> `tools/publish/finpilot_publish_public.sh` (deny-by-default: наружу уходит только
> явно разрешённое; guard роняет сборку на секретах/запрещённых файлах/личных
> именах). Этот документ = человекочитаемый контракт «что и почему», скрипт =
> исполнение. Менять состав → менять оба.
>
> **Статус: ОПУБЛИКОВАНО — v5.18.3, 2026-07-04.** Первый пуш прошёл (HTTPS+токен).
> Практика доступов/токена/пуша — `docs/mirror_publishing_guide.md`. Дальнейшие синки:
> `zsh ~/Downloads/finpilot_publish_public.sh` (dry-run) → `build` → глазами staging → `push`.

## 1. ПУБЛИКУЕТСЯ (allow-list) и почему

| Группа | Состав | Обоснование |
|---|---|---|
| Код приложения | `app/` целиком | Витрина инженерии (найм-аргумент №1): FastAPI, SQLAlchemy 2.0, ядро SAW/Avalanche/SES+MC, MFA, JWT-ревокация, шифрование полей. Секретов нет — всё через `.env`. |
| Тесты | `tests/` (~950+, fast/full/deep/e2e) | Сильнейшее доказательство качества; данные в тестах синтетические (проверено сканом). |
| Миграции | `alembic/`, `alembic.ini` | Часть инженерной истории (0001–0028, реверсивность на PG). |
| Фронт (текущий) | `frontend/` | Работающий Jinja+JS слой; заменится React-фронтом (веха 7). |
| Инфра | `Dockerfile`, `docker-compose*.yml`, `docker-entrypoint.sh`, `deploy/`, `nginx/`, `gunicorn_conf.py`, `run.py`, `Makefile`, `loadtest/`, `scripts/`, `.github/` | Показ прод-зрелости: контейнеризация, reverse-proxy, CI трёх уровней, нагрузочное. |
| Конфиги качества | `pyproject.toml`, `pytest.ini`, `.flake8`, `.mypy.ini`, `.pylintrc`, `.pre-commit-config.yaml`, `.coveragerc`, `requirements*.txt`, `.gitignore`, `.dockerignore` | Инженерная культура видна с порога. |
| Env-шаблоны | `.env.example`, `.env.prod.example` | Только плейсхолдеры (проверено); показывают операционную продуманность (fail-loud, ротация ключей). |
| Корневые доки | `README.md`, `LICENSE`, `SECURITY.md` | Лицо репозитория; SECURITY.md — конвенция GitHub (vulnerability disclosure). |
| Тех-доки | `docs/`: **только `DEPLOY.md` + `RELEASES.md`** (RELEASES переписан с нуля — история публичных версий) | Минимум для запуска. Процесс, баг-репорты, ADR, глоссарий, гайды, реестры — конфиденциальная кухня, закрыты (v5.18.4). |

## 2. НЕ публикуется (по определению — вне allow-list)

| Группа | Почему |
|---|---|
| `knowledge/**` целиком | Бизнес-стратегия, питчи, конкурентный анализ, опросы (сырьё респондентов), юр-черновики, бренд, **наука (private-only, решение v5.16.0)**. |
| `docs/WATCHLOG.md`, `docs/ROADMAP.md`, `docs/roadmap_archive/` | Внутренняя кухня 4 аккаунтов, стратегия и приоритеты — конкурентам не показываем. |
| `docs/reports/**` (кроме шаблонов/гайдов из allow) | Инциденты, расследования, аудиты, merge-манифесты — внутренние разборы, упоминают процесс/аккаунты. |
| `CHANGELOG.md` | 300+ КБ внутренней истории с деталями процесса; версию наружу несёт README-бейдж и релизы зеркала. |
| `docs/pitfalls.md`, `sandbox_runbook.md`, `session_continuity.md`, `tool_call_channel_failures.md`, `test_run_optimization.md`, `versioning_and_releases.md`, `infrastructure.md` | Claude-специфичная механика работы и внутренняя инфра. |
| `docs/legal_questions_for_lawyer.md`, `docs/pdn_data_map.md`, `docs/backup_restore.md`, `docs/slo.md`, `LEGAL.md` | Юр/операционная внутрянка до ревью юристом; карту ПДн наружу не надо. |
| `docs/math_model_v3_0_0.md`, `algorithm_stack.md`, `reference_profiles.md`, `diagrams.md`, `ui_ux_design_standard.md`, `ui_visual_design_brief.md`, API-снапшот | **Ядро IP и планы** — см. Q2 ниже. В скрипте лежат закомментированным OPTIONAL-блоком: включаются одной строкой, если решишь. |
| `tools/**` | Публикатор, cost-tracker (org id), портретный генератор и внутренние пайплайны — процессная кухня. |
| `.env*` (кроме example), `*.db`, кэши | Секреты и локальные артефакты; guard + rsync-excludes страхуют. |

## 3. Находки ревизии (АВТОМАТИЗИРОВАНЫ в `sanitize_tree` + GUARD 3/3, v5.18.4)

> Раньше эти правки делались руками — теперь их делает `sanitize_tree()` при каждой сборке,
> а GUARD 3/3 роняет билд, если имя/приватный-репо просочились. Ниже — что именно чистится.

1. `README.md` футер `© 2025 Vasilii Evdokimov` и бейдж CI на приватный репо →
   санитайзер заменяет на `© 2025 FINPILOT` и бейдж зеркала.
2. `LICENSE` (MIT) содержит имя правообладателя → по умолчанию обезличивается
   в `FINPILOT` (см. Q1 — юридически спорно, решение за тобой).
3. `docs/DEPLOY.md` клонировал приватный монорепо → в зеркале URL заменяется
   на `finpilot.git`.
4. Новый **GUARD 3/3**: скан собранного дерева на личные имена (латиница +
   кириллица) — правило безымянности теперь механическое, не на память.

## 4. Решения владельца (ПРИНЯТЫ, v5.18.3)

- **Q1 (правообладатель):** `FINPILOT` (безымянно) до регистрации ИП; ИП-реквизиты — на вехе 8.
- **Q2 (лицензия):** **PolyForm Noncommercial 1.0.0** — source-available. Код виден (портфолио +
  юр-подоснова), коммерческое использование запрещено. Текст — `tools/publish/LICENSE_public_polyform.txt`.
- **Q3 (IP-доки ядра):** закрыты, наружу не идут.

<details><summary>Исходные формулировки вопросов (архив)</summary>


- **Q1. Правообладатель в LICENSE.** MIT юридически требует идентифицируемого
  правообладателя. Варианты: (а) оставить `Vasilii Evdokimov` только в LICENSE
  (стандарт open-source, минимальная публичность — тогда убираем одну замену
  санитайзера и строку имя-гарда); (б) `FINPILOT` до регистрации ИП, после —
  ИП-реквизиты; (в) сменить лицензию (см. Q2). Моя рекомендация: **(б) сейчас,
  пересборка после ИП** — согласуется с твоим правилом безымянности, а
  юр-чистоту наводим на вехе 8.
- **Q2. MIT vs source-available.** `app/core/` в зеркале = вся мат-модель читается
  из кода. Под MIT любой ЗАКОННО форкает и запускает конкурента. Варианты:
  (а) MIT как есть — максимум портфолио-ценности, конкурентный риск принят
  (ноу-хау в данных/интеграции/исполнении, SAW/Avalanche/SES — известные методы);
  (б) PolyForm Noncommercial / BUSL — код виден (портфолио работает), коммерческое
  использование запрещено; (в) MIT, но `app/core/` заменяется заглушкой-интерфейсом.
  Моя рекомендация: **(б)** — для продукта с осенним коммерческим запуском
  отдавать ядро под MIT щедро сверх необходимого; (в) не советую — рваное зеркало
  хуже для найма, чем честный source-available.
- **Q3. Тех-доки ядра (OPTIONAL-блок).** Оставляем закрытыми (моя рекомендация —
  да, до запуска) или что-то открываешь?

</details>

## 5. Процедура пуша (детали — `docs/mirror_publishing_guide.md`)

1. Скрипт в `~/Downloads`, dry-run: смотрит манифест-вывод и guard.
2. `build` → глазами пройтись по staging (`~/dev/finpilot-public`).
3. `push` — коммит `public mirror sync vX.Y.Z` (безымянный, версия из
   `app/config.py`), git-retry под РФ-TLS уже встроен.
4. Отметить в ROADMAP «Ожидается» и WATCHLOG §0: зеркало живо, дата, версия.
