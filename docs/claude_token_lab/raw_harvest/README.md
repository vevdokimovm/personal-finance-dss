# Сырые выгрузки

Дословные ответы на `harvest_prompt.md`, без правок и сокращений.
Имя файла: `YYYY-MM-DD_<что-мерили>.md`.

Правило то же, что и §18 для исследований: сырое кладётся сюда **до** того, как из него
сделан вывод. Вывод переписывается, сырое — нет.
