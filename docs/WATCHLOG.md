# FINPILOT — Вахтенный журнал (единый источник «где мы сейчас»)

> **Что это и почему так названо.** Вахтенный журнал — судовой документ, который вахтенный ведёт
> **непрерывно во время вахты** и передаёт сменщику, чтобы тот мгновенно принял управление. Здесь
> так же: два аккаунта Claude несут «вахту» по очереди (у одного кончаются токены — продолжает
> другой), а этот журнал — то, что передаётся между ними. Прочитав его, принимающий Claude понимает
> точку, в которой стоим, и продолжает **без повторов и без дрейфа от плана** — как будто Василий
> просто написал «продолжай» в том же чате. (Метафора в тему бренда FINPILOT — штурман у штурвала.)

> ## [!] КРИТИЧНО — журнал ведётся НЕПРЕРЫВНО, параллельно работе
>
> Этот журнал обновляется **НЕ по просьбе Василия и НЕ только в конце сессии**, а **сразу по ходу
> работы**: сделал подшаг — обнови §0/§4; принял решение или нашёл грабли — впиши в §5. Причина
> жёсткая: **токены могут кончиться в середине батча.** Если журнал писать только «в конце», этого
> конца может не наступить — Василий получит устаревшее состояние, и смысл журнала обнуляется.
> **В ЛЮБОЙ момент журнал обязан отражать реальную текущую точку — так, будто сессия может
> оборваться на следующем сообщении.** Само название к этому и обязывает: вахтенный журнал по
> определению пишут в момент события, а не «потом».

> **Как пользоваться (Василий).** Первым сообщением новому Claude: этот файл (или его текст) +
> последний архив кода → пишешь «продолжай». Всё.
>
> **Где живёт.** В репозитории, `docs/WATCHLOG.md`. Едет в архиве вместе с кодом, поэтому версия в
> §0 физически не расходится с `app/config.py`. Имя без версии — перезаписывается каждый батч.

> ## ЭТОТ ЖУРНАЛ СООТВЕТСТВУЕТ ВЕРСИИ КОДА: **`v4.16.36`** (линейная, после слияния B+C)
> Если на руках архив другой версии — журнал и архив рассинхронизированы; сверь `app/config.py`.
> Ветки B (docs/QA) и C (фичи) сведены в `v4.16.27`; дальше — линейно. Параллельных линий нет.

---

## §0. RESUME POINT — отсюда продолжаем

- **Версия кода:** `v4.16.36` (линейная, после слияния B+C на 27).
- **Последний завершённый батч:** v4.16.36 — **разбор INV-A11Y-ANIMATION (docs)** (запрос Василия, вахта J).
  Только docs. Оформлено расследование про артефакт замера контраста во время fade-in анимации: axe-скан
  раздувал color-contrast (~200 в light), но ~90% ложные — `axe.run` замерял при `opacity≈0.15`
  (незавершённая fade-in секций), а не в финале. Страшная гипотеза («палитра дефектна, токены не работают»)
  опровергнута. Тот же класс, что INV-VISUAL-FLAKINESS; дополняет INV-A11Y-ICEBERG (достоверность замера =
  охват × стабилизация). Сводка в реестре; полный разбор вне репо. Сам фикс методики — в `_run_axe`
  (v4.16.35); здесь только документирование.
- **Следующее:** доступность стабилизирована, color-contrast=0, три a11y-разбора оформлены (DEADTEST,
  ICEBERG, ANIMATION). Рекомендация — P2.7 (обучение категоризации на правках пользователя).
- **Предыдущий батч:** v4.16.35 — **ревизия палитры WCAG AA, этап 2: color-contrast = 0**
  (запрос Василия, вахта J). Фронт (CSS, шаблоны, app.js). Прогнано в песочнице:
  - **НАХОДКА — анимационные артефакты замера.** Скан без стабилизации показывал ~200 «нарушений» в
    light; ~90% ЛОЖНЫЕ — секции имеют fade-in (opacity 0→1), axe.run замерял при opacity≈0.15. С
    заморозкой анимаций реальный остаток — 20 узлов. Перекликается с INV-VISUAL-FLAKINESS.
  - **Заморозка анимаций в `_run_axe`** (`animation/transition-duration:0s` + settle 300 ms) перед
    `axe.run` — иначе fade-in даёт ложный контраст. **`KNOWN_SERIOUS_RULES` = `set()`** (долг устранён).
  - **Добиты semantic** во всех шаблонах (transactions/profile/base/validation) + 47 JS-переменных в
    app.js (одинарные кавычки `'var(--c-X)'`; SVG `fill="..."` НЕ тронуты). **-text под badge-фоны**:
    green `#136a34`, amber `#945200` (на semantic-bg контраст ниже белого). **Захардкоженные** alt-detail
    цвета → токены (введены `--c-pink-text`/`--c-blue-text`). **opacity-приглушение**: rec-formula labels
    (`opacity:.6` → text3), table-header light непрозрачный `#ffffff` (градиент просвечивал).
  - **РЕЗУЛЬТАТ: color-contrast = 0** на 14 страницах × 2 темы. a11y-ratchet baseline ПУСТ.
  - **Вахту нёс аккаунт J.** Тесты: a11y 28 (пустой baseline) / CRUD-E2E 6 / visual 28 / fast-контраст 18.
- **Следующее:** ревизия палитры ЗАВЕРШЕНА (этапы 1+2, color-contrast=0). Опции: (1) оформить находку
  про анимационные артефакты замера отдельным INV (родственно INV-VISUAL-FLAKINESS — артефакт методики);
  (2) P2.7 обучение категоризации; (3) другой долг доступности (если всплывёт не-contrast). Рекомендация —
  P2.7, доступность стабилизирована.
- **Предыдущий батч:** v4.16.34 — **ревизия палитры под WCAG AA, этап 1** (запрос Василия,
  вахта J). Фронт (CSS, app.js inline) — ядро/схема не тронуты. Прогнано в песочнице:
  - **Semantic `-text` токены** (обе темы): `--c-{green,red,amber,cyan,violet}-text` — читаемые варианты
    для ТЕКСТА (dark светлые 6–10:1, light тёмные 4.9–7:1). 67 замен `color: var(--c-X)` → `-text`
    (21 styles.css + 46 app.js inline). Сами `--c-*` остались для заливок.
  - **`--c-text3`** dark `#788aa3`→`#8696ad`, light `#5d6a7d`→`#4a5566` (запас на opacity).
  - **Ссылки** — `--c-accent-hl` в light `#818cf8`→`#5b54e8` (было 2.66, стало 4.82; только color, 13×).
  - **goal-category badges** — light-переопределения тёмными (income_growth/safety/material/emotional).
  - **Прогресс:** dark почти закрыта (узлов 20→8); light — закрыт основной текст, semantic-as-text,
    ссылки, badges. **ОСТАТОК (след. этапы):** text3/accent с локальным `opacity` в конкретных местах,
    hover pills, часть кнопок с accent-фоном (не `.primary-button`), пара пограничных dark.
    `color-contrast` остаётся в a11y-ratchet baseline до полного закрытия.
  - **Расследование `INV-A11Y-ICEBERG`** (§12): «два контраста» → системный долг (айсберг / чеховское
    ружьё). Сводка в реестре; полный разбор вне репо. Парный — INC-A11Y-DEADTEST.
  - **Вахту нёс аккаунт J.** Тесты: a11y 28 / CRUD-E2E 6 / visual 28 / fast-контраст 18 — зелёные.
- **Следующее:** **ревизия палитры, этап 2** — добить остаток (opacity-места text3/accent, hover pills,
  кнопки с accent-фоном, dark-пограничные), цель — `color-contrast` из baseline в 0. Найти конкретные
  места с локальным `opacity` (приглушают текст ниже AA даже при правильном токене). Альтернатива — P2.7.
- **Предыдущий батч:** v4.16.33 — **батч доступности + разборы (инцидент/расследование)**
  (запрос Василия, вахта J). Фронт (app.js, CSS) — ядро/схема не тронуты. Прогнано в песочнице:
  - **`nested-interactive` УСТРАНЁН** — кнопка удаления вынесена из `<summary>` на /obligations в
    позиционированную обёртку; delete-делегирование (`data-obligation-id`) цело, CRUD-E2E зелёный
    (6 passed). axe подтвердил: dark /obligations теперь 0 нарушений.
  - **Контраст кнопок** — токен `--c-accent-strong` (dark `#6769cf` white 4.67; light `#5053ef` 5.49)
    в `.primary-button`/`.undo-toast-btn`/`.income-advice-btn`. **Контраст футера** — снят `opacity:.85`
    (роняло text3 ниже AA; без него 5.47/4.89).
  - **ВАЖНО — долг по контрасту СИСТЕМНЫЙ, не точечный.** Полный скан (14×2) вскрыл: в light-теме
    массово ниже AA semantic-цвета (green/cyan/red/amber на светлом 1.2–3.6), category-badges, ссылки
    (`--c-accent-hl` 2.5), приглушённый текст (text3 3.1–4.4). Закрыта ЧАСТЬ (кнопки+футер); полная
    ревизия палитры под WCAG AA — **отдельная крупная задача** (десятки токенов × 2 темы + регенерация
    visual baseline). До неё `color-contrast` остаётся в a11y-ratchet baseline.
  - **Разборы (правило §12):** инцидент `INC-A11Y-DEADTEST` (мёртвый a11y-тест из-за CSP) +
    расследование `INV-VISUAL-FLAKINESS` (visual-флак — артефакт методики). Сводки в реестрах
    (`incidents_summary.md`, `investigations_summary.md`); полные разборы — отдельными файлами вне репо.
  - **Вахту нёс аккаунт J.** Тесты: a11y 28, CRUD-E2E 6, visual 28, fast/frontend 108 — зелёные.
- **Следующее (рекомендация):** **ревизия цветовой палитры под WCAG AA** — закрыть системный
  `color-contrast` долг (light-тема: semantic-цвета, badges, ссылки, приглушённый текст), поэтапно по
  семантическим группам, под контролем a11y-ratchet + регенерации visual baseline. Альтернатива — по
  роадмапу P2.7 (обучение категоризации).
- **Предыдущий батч:** v4.16.32 — **наращивание тестов deep/full + нагрузка прод-уровня:**
  (запрос Василия, вахта J). Прогнано в песочнице (chromium предустановлен в `/opt/pw-browsers`):
  - **deep property-stress** (`tests/deep/test_property_stress.py`): с 2 до 15 инвариантов на
    3000-5000 примеров. Добавлены |A|≤66 (полный набор 66 только при наличии и долгов, и целей —
    hypothesis нашёл мою неверную предпосылку), сумма компонент = Rt + неотрицательность, единственность
    дефицита, производные метрики, avalanche conservation + payment-never-grows, partition-total+disjoint,
    рекомендация допустима, формулы Lt/Dt/BLR, нормализация, **детерминизм** (повторный прогон = тот же
    результат — новый инвариант). **15 passed за 116с.**
  - **full a11y-axe** (`tests/full/test_a11y_axe.py`): был МЁРТВЫЙ (CSP `script-src 'self' 'unsafe-inline'`
    режет CDN → скип всегда, и в CI). Починен фундаментально: axe-core 4.12.1 **вендорен**
    (`tests/full/vendor/axe.min.js`) + inline-инъекция (`add_script_tag(content=...)` проходит по
    `'unsafe-inline'`). 14 реальных страниц × 2 темы (ключ `finpilot-theme`), два уровня: critical=0
    жёстко + serious-ratchet. **28 passed за 69с.** Тест поймал реальную регрессию (см. §5).
  - **full visual-regression** (`tests/full/test_visual_regression.py`): 14 страниц × 2 темы (было 4×1).
    Стабилизация рендера: ждём шрифты, замораживаем анимации, скрываем canvas, маскируем динамическую
    auth-зону, **допуск на канал в пиксель-диффе** (отсекает антиалиасинг-шум). **28 passed** (механизм
    создание+сравнение). Эталоны платформо-зависимы → `__screenshots__/` в `.gitignore`, генерятся в CI.
  - **Новый `tests/full/conftest.py`** — живой сервер + демо + chromium-флаги (раньше full не имел своих
    фикстур `base_url`/`live_server` — фактически не прогонялся).
  - **loadtest прод-уровень** (`loadtest/locustfile.py`): расширенная read-heavy смесь всех рабочих
    эндпоинтов, **два класса** (гость + авторизованный с JWT), 429-толерантность (rate-limit ≠ failure),
    env-флаг `RATE_LIMIT_REQUESTS` для чистого замера throughput, SLA-ориентиры, прод-профиль запуска.
    Smoke 60u: **0 failures**, ~39 rps (песочница, одно ядро; прод-числа на VPS).
  - Починены битые пути прежних full-тестов: `/login` (страницы нет by design — вход модалкой),
    `/legal` → `/legal/{privacy,terms,consent}`.
  - **Правило WATCHLOG**: локальная копия теперь с версией в имени (`WATCHLOG finpilot_vX_Y_Z`) — см. §1.
  - **Вахту нёс аккаунт J.** Код приложения (`app/`) НЕ тронут; fast-набор цел (688 собирается).
- **Предыдущий батч:** v4.16.31 — **актуализация README + легенда карты тестов (docs):**
  - **README.md переписан целиком** (235 → 430 строк): бейджи (CI/версия/Python/покрытие/лицензия);
    раздел «Как это работает» с двумя Mermaid-диаграммами (слои + 9-шаговый конвейер ядра) и разбором
    мат-ядра; раздел «История версий» (эпохи v1→v2→v3→v4); трёхкатегорийная система тестов вместо
    «все тесты»; карта документации `docs/`. Цифры актуализированы (версия, покрытие 91.6%, миграции
    0019, масштаб). 15 файловых ссылок проверены.
  - `docs/testing_infrastructure.md` §6 — добавлена **легенда колонки «Тир»**: прочерк `—` = «не
    применимо» (инфраструктура/конфиг, НЕ тест), а не «тест отсутствует». Зафиксирован честный статус:
    тиры `full`/`deep` — рабочий каркас минимальной ширины (точка роста, не дыра).
  - **Вахту нёс аккаунт J** (третий — добавлен в §1; см. ниже). Docs-only: код/схема не тронуты.
- **Предыдущий батч:** v4.16.30 — **разведение инцидентов и расследований (docs/process):**
  - Браузерный кейс переклассифицирован: это **investigation** (дефекта в коде нет), а не incident
    (в v4.16.29 был ошибочно оформлен как incident — исправлено).
  - `docs/investigation_report_guide.md` — методичка расследований (синхронизирована из аккаунта S).
  - `docs/investigations_summary.md` — **новый реестр расследований** (парный к incidents): INV-BROWSERS-SANDBOX,
    INV-MATHMODEL-FALSEALARM, INV-PG-SANDBOX, INV-CBR-403; сводки обогащены полем «решение / что
    опробовано / почему не вышло / что дальше».
  - `docs/incidents_summary.md` — переписан: только реальные дефекты (BUG-024/025, INC-CRUD-DELETE),
    тот же обогащённый формат; расследования выехали в свой реестр.
  - `docs/incident_postmortem.md` — §0 «Шаблоны и реестры» (ссылки на 3 шаблона + 2 реестра); §4 → ссылка
    на полную методичку расследований; §7 дополнен браузерным примером.
  - Финализирован `FINPILOT_browsers_sandbox_investigation.md` (вне репо): хвост §11 закрыт — перебраны
    все 4 канала (GitHub 404, npm без бинарей, системный firefox несовместим с juggler, apt только либы).
    Старый `FINPILOT_browsers_sandbox_incident.md` отозван.
  - Имена аккаунтов: этот = **V**, второй = **S** (§1). Без схемных изменений (миграций нет).
- **Предыдущий батч:** v4.16.29 — **очередь сессии 26.06 (8 пунктов) + тестовая инфраструктура:**
  - `tests/test_core_properties.py` — **property-based слой на ядро (hypothesis), 26 тестов**:
    инварианты мат-модели на тысячах входов строго по `math_model_v3_0_0.md` (`|A|=66`, сумма
    долей=1, `Rt≥0`, ПДН≤0.40, `U(a)∈[0,1]`, argmax, сохранность avalanche, монотонность весов).
    `hypothesis==6.155.7` в dev-зависимостях.
  - **3 категории CI** (`pytest.ini` маркеры `fast`/`full`/`deep` + переписан `.github/workflows/ci.yml`):
    fast (push/PR, гейт 90%, матрица SQLite+PG, авто-маркер в `conftest.py`); full (теги/вручную:
    мультибраузер+визуал+a11y+security+нагрузка); deep (cron/вручную: стресс-property+мутации).
  - Мастхэв-тиры: `tests/full/test_visual_regression.py`, `tests/full/test_a11y_axe.py`,
    `tests/deep/test_property_stress.py`. `Makefile`: `test-fast/full/deep`, `security`, `mutation`.
  - `docs/testing_infrastructure.md` — карта инфраструктуры тестов (уровни, категории, PG, E2E-движки).
  - `docs/incidents_summary.md` +2 строки: INC-CRUD-DELETE, INC-BROWSERS-SANDBOX. Полные разборы —
    `FINPILOT_crud_delete_incident.md` и `FINPILOT_browsers_sandbox_investigation.md` (вне репо, §12;
    браузерный — переклассифицирован в investigation в v4.16.30).
  - **firefox/webkit докопано до конца (§11):** все каналы доставки бинарей закрыты (GitHub 404,
    npm без бинарей, системный firefox несовместим с juggler, apt только либы) → внешнее ограничение
    Azure-CDN (как cbr.ru). chromium в песочнице; мультибраузер — в CI.
  - Аудит покрытия: TOTAL 91.61% (гейт держится); ядро `U(a)` — `ranking`/`filtering` 100%,
    `alternatives` 98.7%, `metrics` 95.7%, `avalanche` 89.7%. 6 CRUD-E2E зелёные на chromium.
  - Проверено: **688 unit/integration (вкл. 26 property) + 6 CRUD-E2E + 2 deep** зелёные на SQLite.
- **Предыдущий батч:** v4.16.28 — **CRUD E2E + правила тестирования + реестр инцидентов:**
  - `tests/e2e/test_crud_e2e.py` (6 тестов) — создание/удаление/**восстановление** obligations,
    goals, transactions через UI. E2E на chromium — **20** (5+5+4+6).
  - `docs/incidents_summary.md` — реестр инцидентов в репо; полные разборы — вне репо.
  - §6 правило 13 (E2E авто-добавляются с unit для фронта/API; CRUD обязаны иметь E2E).
- **Предыдущий батч:** v4.16.27 — **МЕРЖ веток B (docs/QA) и C (фичи)** в линейную версию.
- **Предыдущий батч (линия C):** v4.16.26 — **P2.6: история рассчитанных планов** (снапшоты в БД).
  Модель `PlanSnapshot` (таблица `plan_snapshots`: профиль риска, Rt/Lt/Dt/BLR плоскими колонками,
  лучшая альтернатива, топ-3 JSON, заметка, soft-delete) + **миграция `0019`** (первая после 0018,
  применяется чисто на SQLite и PG). CRUD `create/get/list/soft_delete_plan_snapshot` + эндпоинты:
  `POST /history` (считает план и сохраняет), `GET /history` (список), `GET /history/{id}` (детально),
  `DELETE /history/{id}` (soft-delete). Гард `test_plan_history.py` (11). Проверено: **662
  unit/integration + 10 e2e**, ноль падений; **миграция 0019 + история (JSON-колонка) зелены на
  PostgreSQL 16.14**. Без LLM.
- **Предыдущие:** v4.16.25 — P2.5 экспорт XLSX/PDF; v4.16.24 — PDF Райффайзена (импорт-трек закрыт);
  v4.16.23 — `1CClientBankExchange`; v4.16.22 — PDF ВТБ/Сбер + dual-locale `_num`; v4.16.21 — большие
  выписки + CSV/XLSX; v4.16.20 — P2.4-B (P2.4 закрыт); v4.16.18 — P1.7 soft-delete (миграция 0018); §3.
- **СТАТУС:** импорт-трек закрыт, P2.5 экспорт, P2.6 история. **Очередь сессии 26.06 (8 пунктов:
  firefox/webkit-разбор, property-ядро, 3-категорийный CI, аудит покрытия, testing_infrastructure.md,
  user-guide выписок, 2 инцидент-разбора) — ВЫПОЛНЕНА в v4.16.29.** Дальше — чисто по роадмапу.
- **[СЛЕДУЮЩЕЕ] Роадмап: P2.7 — обучение категоризации.** Категоризатор
  (`app/core/categorization.py`) сейчас детерминированный (правила/MCP/ключевые слова) — без ML
  (жёсткое требование: никаких нейросетей/LLM). «Обучение» — это детерминированная адаптация под
  пользователя: запоминать ручные правки категории (пользователь переназначил категорию транзакции →
  применять её к будущим/прошлым операциям с тем же описанием/merchant). Вероятно: таблица
  user-overrides (description/merchant → category) + учёт в `classify_transaction`. Разведать текущий
  категоризатор и где он зовётся при импорте/правке. Затем P3.5 (A/B-разбивка).
- **Мелкий фронт-follow-up (дизайн-блок):** на `planning.html` две кнопки с `id="export-pdf"` (дубль id;
  биндится только первая — `/api/export/report.pdf` финотчёта; вторая «Распечатать» без обработчика).
  Развести id + добавить кнопку для `/api/planning/export.pdf` (PDF плана) + кнопки сохранения/просмотра
  истории плана (`/api/planning/history`). Требует решения по UI.
- **Опционально, не блокер (из разведки слоя 3):** внутримесячные/дни-недели паттерны трат требуют
  точной даты в `ExpenseRecord` (сейчас только `period 'YYYY-MM'`) — +поле `date` в `services/spending.py`.
- **Бэклог роадмапа:** P2.7 (обучение категоризации) → P3.5 (A/B-разбивка).
  Дизайн-блок: P1.5, P2.1, P2.2, P3.1/P3.2. Вне песочницы: P3.3/P3.6/P3.7.
- **PostgreSQL-матрица: миграции 0018+0019 проверены на PostgreSQL 16.14 в песочнице.** На v4.16.18 —
  полная DB-матрица (558 тестов) зелёная; на v4.16.26 — миграция 0019 + целевой прогон
  (история/планирование/soft-delete/экспорт) зелёные на PG. v4.16.19–25, 4.16.27–29 — тесты ядра/БД-лайт
  без миграций (**property-тесты DB-агностичны — чистая математика ядра, результат на SQLite и PG
  идентичен**). **PG-долга нет.** Как поднять PG — см. §5 (apt install postgresql БЕЗ `apt-get update` —
  он падает на стороннем nodesource-репо 403; Ubuntu-репы рабочие; затем `pg_ctlcluster 16 main start`,
  роль/БД через `su postgres -c psql`, `DATABASE_URL=postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test`).

---

## §1. Протокол вахты (как вести этот журнал)

> **Имена аккаунтов (для разграничения, если Василий уточняет «акк S/V/J»).** Василий работает в
> нескольких аккаунтах Claude, несущих вахту по очереди через этот журнал: **V**, **S** и **J**
> (J добавлен на v4.16.31). Если в задаче или разборе нужно сослаться на конкретный аккаунт —
> использовать эти буквы (напр. «расследование начато на S, финализировано V»; «README обновлён на
> J»). На саму работу не влияет — журнал самодостаточен для любого из них.


**Заступая на вахту (начало сессии), принимающий Claude:**
1. Прочитай этот файл целиком + `FINPILOT_Claude_sandbox_runbook.md` (грабли песочницы — НЕ
   диагностируй заново: dash-shell, вывод в файл, `setsid` для долгого, chromium `--no-sandbox`).
2. Распакуй последний архив; сверь версию в `app/config.py` с §0.
3. Подними venv (`python3 -m venv .venv` + `pip install -r requirements.txt -r requirements-dev.txt`),
   при работе с E2E — `playwright install chromium`.
4. Отчитайся Василию коротко (3-4 строки): версия, что закрыто в P0, следующий шаг. Вопрос — только
   если §0 расходится с архивом.
5. НЕ перепроверяй и НЕ переделывай помеченное `[x]` в §2/§3. НЕ уходи с порядка роадмапа без команды.

**Неся вахту — обновляй журнал ПО ХОДУ, не откладывая на конец (см. callout вверху):**
- Поменялась точка работы / закончил подшаг → сразу обнови §0 (resume point) и §4 (нити в работе).
- Принял значимое решение, нашёл грабли, выяснил факт → сразу впиши в §5.
- Закрыл батч (bump версии) → допиши строку в §3 `vX.Y.Z — что — чем проверено`, обнови статусы §2.
- **Отдай журнал Василию ОТДЕЛЬНЫМ файлом** через `present_files` в конце батча — не только внутри
  архива. Василию так удобнее передавать его в другой аккаунт. Всегда два экземпляра: один в
  архиве (`docs/WATCHLOG.md` — БЕЗ версии в имени, перезаписывается каждый батч), один отдельным
  `.md` в чат. **Локальную копию называй с версией в имени файла** — `WATCHLOG finpilot_vX_Y_Z`
  (напр. `WATCHLOG finpilot_v4_16_32`), чтобы Василий не путал, к какому батчу относится копия.
  Внутри архива имя остаётся `docs/WATCHLOG.md` без версии. Правило введено на v4.16.32.
- Держи журнал **тонким**: не дублируй стабильные доки (§7) — ссылайся. Устаревшее перезаписывай,
  не накапливай.

> Правило вахтенного: запись делается в момент события, а не «потом». Оборвёт на полуслове — сменщик
> поднимает управление по тому, что уже записано в §0/§4.

---

## §2. Состояние и статус роадмапа

- **Версия:** v4.16.26. **Тесты:** база зелёная на SQLite (662); **DB-матрица (558) зелёная на
  PostgreSQL 16.14** (v4.16.18), **миграция 0019 + история планов зелены на PG** (v4.16.26, целевой
  прогон); тесты v4.16.19–25 — ядро/БД-лайт. PG-долга нет. Покрытие — гейт 90% (последний полный
  прогон 90.80%). Последний полный локальный прогон —
  **v4.16.26: 662 unit/integration passed + 10 E2E passed**, ноль падений; миграция 0019 (plan_snapshots).
  (Известный e2e-флак `test_profiles_produce_different_plans` — граничный «всё в резерв», на
  перепрогоне зелёный; не касается импорта/экспорта/истории.)
- **Источник приоритетов — `03_Будущий_роадмап.md`** (P0-P3); полный срез всех пунктов
  продублирован ниже, чтобы журнал был самодостаточен без файла роадмапа. Идём строго по порядку номеров.

**P0 — блокеры запуска:**
- [x] P0.1 E2E на всех браузерах — CSRF/CORS-фикс + chromium-флаги; **14 E2E вживую** (5 dashboard + 5 planning + 4 auth/guest); мультибраузер chromium/firefox/webkit (v4.16.28).
- [x] P0.2 Реальный SMTP — код был готов; добавлены тесты `_send` + прод-инструкция в `DEPLOY.md`
  (боевой провайдер настраивается на VPS).
- [x] P0.3 Реальный fetch ЦБ — парсер проверен на реальном ответе (зеркало), кейс 403 покрыт; живой
  сетевой fetch с РФ-IP — на VPS.
- [x] P0.4 Нагрузочное — охарактеризовано в песочнице (locust-сценарий `loadtest/`, baseline
  ~110 RPS @ 60u, бутылочное горло `calculate`); боевые числа снимаются на VPS тем же сценарием.
- [x] P0.5 Секреты в проде (`.env.prod.example`). [x] P0.6 Внешний cron (systemd timers + скрипты).
  [x] P0.7 Бэкапы + restore (`restore_db.sh`, `backup_verify.sh`). Всё это — артефакты v4.16.2,
  финально применяются на VPS.

**P1 — качество и доверие:**
- [x] P1.1 a11y-аудит — **закрыт целиком.** Механика (v4.16.14): метки, sr-only `<h1>`,
  `inert`-фокус модалок + TDD-харнесс (90 проверок). Контраст (v4.16.15): `--c-text3`/`--c-accent`
  до WCAG AA в **обеих темах** (dark + light) + регрессионный гард `test_a11y_contrast.py`
  (8 проверок = 4 токена × 2 темы). Ручной скринридер
  (NVDA/VoiceOver) — на стороне Василия, не блокер. См. §3/§4.
- [x] P1.2 Кэш прогноза — **закрыт (v4.16.16).** `run_planning` обёрнут `TTLCache` (180с/256)
  по отпечатку реальных входов; кэш на уровне расчёта, не роута (аналитика воронки цела); гард
  `test_planning_cache.py` (4 проверки). См. §3/§4.
- [x] P1.3 Rate limiting — **уже реализован** (`RateLimitMiddleware`), НЕ делать.
- [ ] P1.4 Чистка mypy/flake8 — **заблокирована** правилом «без массового реформата» (~72 E501 =
  массовый перенос строк, ломает merge между аккаунтами). Только осознанно.
- [ ] P1.5 UI по дизайн-стандарту (`06_ТЗ_на_современный_UI.md`).
- [x] P1.6 Мониторинг/алертинг — **закрыт (v4.16.17).** `init_sentry` подключён в `lifespan`
  (`release=APP_VERSION`); existed но не вызывался. DSN — на VPS. Гард `TestSentry` (+2). См. §3.
- [x] P1.7 Undo-модалки на удаление — **бэк закрыт (v4.16.18).** Симметричный soft-delete +
  restore + 3 API-эндпоинта для Obligation/Goal/LiquidAsset (транзакции были готовы с BUG-03).
  Миграция 0018. UI-модалки undo → **дизайн-блок** (вместе с P1.5). См. §3/§4.

**P2 — желательный функционал (бэк часто уже готов — перед реализацией сверяйся с §4):**
- [ ] P2.1 UI реферальной программы — **бэк готов** (`/referral/me`); нужна страница: код,
  ссылка-приглашение, счётчик приглашённых.
- [ ] P2.2 UI продуктовой аналитики (админ-дашборд) — воронка/метрики **есть в API**, нужна
  визуализация для себя.
- [ ] P2.3 Дашборд дайджеста/уведомлений в приложении (колокольчик) — сейчас только письмом, нужен UI.
- [x] P2.4 Слой 3 spending-advice — **закрыт (v4.16.19 temporal + v4.16.20 goal-aware).** Слои 1-2 готовы
  (категории, мерчанты). Трогать можно: `spending_advice.py` не входит в `U(a)` (исключение из §6.2).
- [x] P2.5 Экспорт в Excel / расширенный PDF — **закрыт (v4.16.25).** XLSX-выгрузка операций, графики в PDF-отчёте (`plan_export.py`).
- [x] P2.6 История изменений плана — **закрыт (v4.16.26).** Снапшоты планов в БД (`PlanSnapshot`, миграция 0019, `/history`).
- [ ] P2.7 Категоризация: обучение на правках пользователя — запоминать ручное переназначение категории.

**P3 — рост и масштаб:**
- [ ] P3.1 Полная локализация UI (i18n) — **механизм заложен**; вынести строки шаблонов/JS в каталог
  переводов.
- [ ] P3.2 Мобильное приложение / PWA — **манифест и service worker уже есть**; ~76% респондентов хотят
  мобайл. Крупнейший незакрытый сегмент аудитории.
- [ ] P3.3 Open Banking (РФ) — с 2026 обязательные сторонние API; требуется аккредитация.
- [ ] P3.4 Реферальные награды / геймификация — бонусы за приглашения, достижения по целям.
- [ ] P3.5 A/B-тестирование на event-логе — **инфраструктура событий есть**; добавить разбивку по
  экспериментам.
- [ ] P3.6 Telegram-бот — уведомления и быстрые операции через Telegram (синергия с каналом продвижения).
- [ ] P3.7 Многопользовательские/семейные бюджеты — совместный доступ к финансовому плану.

> **Продуктовые дыры (взгляд «чего не хватает», из роадмапа — обоснование приоритетности, не порядок
> исполнения):** визуализация в UI того, что уже считается на бэке (P2.1-2.3, самый дешёвый прирост
> ценности); петля «план vs факт» за месяц (удержание + данные для алгоритма); более простое
> объяснение *почему* такое распределение (особо для персоны R01, 56 лет); интерактивный онбординг;
> мобильная версия. Действующий порядок работы — строго по номерам (§6.7), пока Василий не скажет иначе.

---

## §3. Журнал сделанного (новое сверху; ветка v4.16.1 → v4.16.36)

- **v4.16.36** — разбор INV-A11Y-ANIMATION (docs, запрос Василия, вахта J). Оформлено расследование: axe-скан
  раздувал color-contrast (~200 в light), ~90% ложные — замер во время fade-in анимации (`opacity≈0.15`).
  Страшная гипотеза опровергнута (токены корректны). Класс INV-VISUAL-FLAKINESS; дополняет INV-A11Y-ICEBERG.
  Сводка в реестре, полный файл вне репо. Фикс методики уже в `_run_axe` (v4.16.35). Только docs.

- **v4.16.35** — ревизия палитры WCAG AA этап 2: color-contrast = 0 (запрос Василия, вахта J). НАХОДКА:
  ~90% «нарушений» в скане были ложными — артефакт fade-in анимаций (axe замерял при opacity≈0.15);
  добавлена заморозка анимаций в `_run_axe`. Добиты semantic во всех шаблонах + 47 JS-переменных app.js
  (SVG fill не тронут); -text затемнены под badge-фоны (green #136a34, amber #945200); alt-detail
  захардкоженные цвета → токены (+pink/blue-text); rec-formula opacity-labels → text3; table-header light
  непрозрачный #ffffff. KNOWN_SERIOUS_RULES = set(). color-contrast = 0 (14×2). a11y 28 (пустой baseline) /
  CRUD-E2E 6 / visual 28 / fast 18 зелёные.

- **v4.16.34** — ревизия палитры под WCAG AA, этап 1 (запрос Василия, вахта J). Semantic `-text` токены
  (обе темы) + 67 замен `color: var(--c-X)` → `-text` (21 CSS + 46 app.js inline); `--c-text3` темнее
  (dark #8696ad / light #4a5566); ссылки `--c-accent-hl` light #5b54e8 (2.66→4.82); goal-category badges
  light-переопределения. dark почти закрыта (узлов 20→8), light — текст/semantic/ссылки/badges. ОСТАТОК:
  opacity-места, hover pills, accent-фон кнопок, dark-пограничные — этап 2; color-contrast в baseline.
  Расследование INV-A11Y-ICEBERG (айсберг/чеховское ружьё). Ядро/схема не тронуты; a11y 28 / CRUD-E2E 6 /
  visual 28 / fast-контраст 18 зелёные.

- **v4.16.33** — батч доступности + разборы (запрос Василия, вахта J). **`nested-interactive` УСТРАНЁН**:
  кнопка удаления вынесена из `<summary>` на /obligations в позиционированную обёртку (`position:absolute`),
  убран `onclick="event.preventDefault()"`; делегирование `data-obligation-id` цело, CRUD-E2E 6 passed,
  axe подтвердил 0 нарушений на dark /obligations. **Контраст**: токен `--c-accent-strong` (dark #6769cf
  white 4.67; light #5053ef 5.49) в primary/undo-toast/income-advice кнопках; снят `opacity:.85` футера
  (text3 → 5.47/4.89). **Открытие**: полный скан (14×2) показал, что контраст-долг СИСТЕМНЫЙ (light-тема:
  semantic-цвета, badges, ссылки accent-hl, text3 массово ниже AA) — закрыта часть, полная ревизия палитры
  вынесена в отдельную крупную задачу; `color-contrast` остаётся в a11y-ratchet. **Разборы (§12)**:
  INC-A11Y-DEADTEST (post-mortem) + INV-VISUAL-FLAKINESS (investigation) — сводки в реестрах, полные файлы
  вне репо. Ядро/схема не тронуты; a11y 28 / CRUD-E2E 6 / visual 28 / fast-frontend 108 зелёные.

- **v4.16.32** — наращивание тестов deep/full + нагрузка прод-уровня (запрос Василия, вахта J).
  Прогнано в песочнице (chromium предустановлен в `/opt/pw-browsers`, playwright 1.56). **deep**
  `test_property_stress.py` 2→15 инвариантов на 3000-5000 примеров (включая новый детерминизм;
  hypothesis поймал неверную предпосылку «rt>0 → 66» — полный набор 66 только при наличии и долгов,
  и целей), **15 passed**. **full a11y** `test_a11y_axe.py` был мёртвый (CSP режет CDN → вечный скип);
  починен вендорингом axe-core 4.12.1 (`tests/full/vendor/`) + inline-инъекцией (CSP `'unsafe-inline'`),
  14 страниц × 2 темы, critical=0 + serious-ratchet, **28 passed**; поймал реальную регрессию
  (nested-interactive, см. §5). **full visual** `test_visual_regression.py` 4×1→14×2, стабилизация
  (шрифты/анимации/canvas/маска auth + допуск на канал), **28 passed**; эталоны платформо-зависимы →
  `.gitignore`, генерятся в CI. Новый `tests/full/conftest.py` (живой сервер+демо+chromium-флаги —
  раньше full не имел `base_url`/`live_server`). **loadtest** прод-уровень: read-heavy смесь, два класса
  (гость+JWT), 429-толерантность, env-флаг `RATE_LIMIT_REQUESTS`, SLA, прод-профиль; smoke 0 failures.
  Починены битые пути (`/login`, `/legal`). Правило WATCHLOG: локальная копия с версией в имени (§1).
  Код приложения НЕ тронут; fast-набор цел (688). Контраст + nested-interactive — в очередь на батч
  доступности (§5).

- **v4.16.31** — актуализация README + легенда карты тестов (docs; запрос Василия, вахта J). README
  переписан целиком (235→430): бейджи, две Mermaid-диаграммы (слои + 9-шаговый конвейер ядра, из
  `docs/diagrams.md`), разбор мат-ядра (SAW/Avalanche+OCR/stock-based Lt/SES+MC/инварианты), раздел
  «История версий» (v1 ядро → v2 single-user-дозревание → v3.0.0 MAJOR международный → v4 production-
  hardening), трёхкатегорийная система тестов вместо «все тесты», карта `docs/`. Цифры приведены к
  факту: версия 4.16.0→4.16.31, покрытие 88%→91.6%, миграции 0017→0019, масштаб (~10.6k строк/14
  модулей ядра/19 миграций); 15 файловых ссылок проверены на существование, code-fences парные,
  2 mermaid-блока. `docs/testing_infrastructure.md` §6 — легенда колонки «Тир»: `—` = «не применимо»
  (конфиг/инфра, не тест), НЕ «тест отсутствует» (снятие недопонимания Василия: все виды тестов в
  проекте есть и зелёные); отдельно — честный статус тонких тиров `full`/`deep` (каркас, точка роста).
  Аккаунт J добавлен в §1. Docs-only: код/ядро/схема не тронуты; тестовый статус наследуется от 4.16.30.

- **v4.16.30** — разведение инцидентов и расследований (docs/process; запрос Василия). Браузерный
  кейс — это **investigation** (дефекта в коде нет), а не incident; в v4.16.29 был ошибочно оформлен
  как incident — исправлено. Добавлены: `docs/investigation_report_guide.md` (методичка расследований,
  синхронизация из аккаунта S) и `docs/investigations_summary.md` (новый реестр расследований, парный
  к incidents). Реестры обогащены — теперь не только корень, но и «решение / что опробовано / почему
  не вышло / что дальше» (раньше этот ключевой кусок опускался). `docs/incidents_summary.md` переписан
  под реальные дефекты (BUG-024/025, INC-CRUD-DELETE); 4 расследования (browsers/math/PG/cbr) выехали
  в свой реестр под ID `INV-*`. В `docs/incident_postmortem.md` добавлен §0 со ссылками на все шаблоны
  и реестры, §4 указывает на полную методичку, §7 дополнен. Финализирован
  `FINPILOT_browsers_sandbox_investigation.md` (вне репо): открытый хвост §11 закрыт перебором 4 каналов
  доставки бинарей. Соглашение об именах аккаунтов V/S (§1). Миграций нет; код не трогался.

- **v4.16.29** — очередь сессии 26.06 (8 пунктов) + тестовая инфраструктура. (1) firefox/webkit
  докопано до конца по §11: перебраны 4 канала доставки бинарей — GitHub releases (404 при
  `PLAYWRIGHT_DOWNLOAD_HOST=github.com`), npm-пакет (866 КБ, 0 бинарей), системный firefox
  (`p.firefox.launch()` привязан к juggler-managed-бинарю, стоковый отвергается), apt (только либы,
  `firefox-esr` без кандидата) → корень: Azure-CDN Playwright вне allowlist (403), внешнее
  ограничение как cbr.ru. (2) `tests/test_core_properties.py` — 26 property-тестов на инварианты
  ядра (hypothesis), строго по `math_model_v3_0_0.md`; `hypothesis==6.155.7`. Нашёл и исправил СВОЮ
  ошибку в тесте монотонности (веса целей по канону неубывающие с плато 0.20, а не строго растущие).
  (3) 3 категории CI: маркеры `fast`/`full`/`deep` в `pytest.ini`, авто-`fast` хук в `conftest.py`,
  переписан `ci.yml` (fast push/PR + матрица SQLite/PG гейт 90%; full на тегах — мультибраузер+визуал+
  a11y+security+нагрузка; deep cron — стресс+мутации). Тиры: `tests/full/{test_visual_regression,
  test_a11y_axe}.py`, `tests/deep/test_property_stress.py`. `Makefile` +5 целей. (4) Аудит покрытия:
  TOTAL 91.61%, ядро `U(a)` 90–100%, 6 CRUD-E2E зелёные на chromium. (5) `docs/testing_infrastructure.md`.
  (6) user-guide выписок (вне репо, user-facing). (7,8) разборы INC-CRUD-DELETE и INC-BROWSERS-SANDBOX
  (вне репо) + 2 строки в `docs/incidents_summary.md`. Прогон: 688 (вкл. property) + 6 CRUD-E2E +
  2 deep зелёные на SQLite. Миграций нет → PG-матрица валидна с 0019.

- **v4.16.28** — CRUD E2E + правила тестирования + реестр инцидентов. `test_crud_e2e.py` (6 тестов:
  create/delete/restore для obligations/goals/transactions через UI; элемент по `data-*-id` из
  ответа создания). E2E на chromium = 20. `docs/incidents_summary.md` — реестр сводок. §6 правила
  13 (авто-E2E с unit) + 12 дополнено. Полные incident-разборы убраны из архива (вне репо). Браузеры:
  chromium в песочнице ОК; firefox/webkit — CDN вне allowlist (для локали/CI). Нюанс restore:
  obligations/goals пересоздаются POST на базовый `/api/<entity>` (новый id), transactions —
  `/api/transactions/<id>/restore` (тот же id); E2E проверяет возврат по имени, не по id.

- **v4.16.27** — **МЕРЖ веток B (docs/QA) и C (фичи)** в одну линейную версию. Из B влиты:
  `docs/diagrams.md` (8 диаграмм по коду), `FINPILOT_postgres_incident_postmortem.md`,
  `FINPILOT_mathmodel_falsealarm_incident.md`, `tests/e2e/test_auth_e2e.py` (4 теста) + фикс гонки
  `test_profiles` (читал авто-расчёт risk=3 вместо ручного), §6 правила 11-12 (анти-паттерн +
  авто-incident), удаление `docs/model_vs_code.md` + чистка ссылок, переименование файла мат-модели
  → `math_model_v3_0_0.md`. Все фичи C (1C/Райф/P2.5/P2.6/миграция 0019) сохранены — ветки
  ортогональны (B: доки+E2E, C: `ingestion/`+`services/`+`alembic/`), конфликтов кода нет.

- **v4.16.26** — P2.6: история рассчитанных планов (роадмап, пункт #3). Модель `PlanSnapshot`
  (`plan_snapshots`: профиль, Rt/Lt/Dt/BLR, лучшая альтернатива, топ-3 JSON, заметка, soft-delete) +
  миграция `0019` (первая после 0018). CRUD `create/get/list/soft_delete_plan_snapshot` + эндпоинты
  `POST/GET /history`, `GET/DELETE /history/{id}`. Гард `test_plan_history.py` (11). Проверено: 662 +
  10 e2e, 0 падений; **миграция 0019 + история зелены на PostgreSQL 16.14**. Без LLM.

- **v4.16.25** — P2.5: экспорт плана в XLSX и PDF (пункт #2). Новый `plan_export.py`: `_extract` +
  `plan_to_xlsx` (openpyxl) + `plan_to_pdf` (reportlab, переиспользует `report_pdf`: Liberation Sans,
  `_money`, `_table`; индикаторы ASCII Rt/Lt/Dt). Роуты `GET /export.xlsx` + `/export.pdf` (зеркало
  CSV). Фронт: кнопка «Скачать XLSX». Гарды `test_plan_export.py` (9) + 2 эндпоинт-теста. Контент тот
  же, что у CSV (Rt/Lt/Dt/BLR + распределение + топ-3). Без миграции. Проверено: 651 + 10 e2e, 0 падений.

- **v4.16.24** — импорт PDF Райффайзенбанка (длинный хвост, пункт #1). Разведка `Archive.zip`:
  Альфа-PDF — справки/пустые выписки (Альфа покрыта CSV/XLSX), Газпром-PDF — справка, Райффайзен —
  реальные операции. `parse_raiffeisen_pdf` (seam `_raif_table_to_transactions`): `extract_tables()`,
  колонки [№, дата+время, документ, Debit, Credit, назначение, карта]; Debit (−) → расход, Credit (+)
  → приход, по позициям (EN/RU-шапка одинаково). `PDF_PARSERS` + `raiffeisen`. Гард +5 (test_pdf_parsers
  22). На реальных: Райф 2 — 14 (6/8), Райф 1 (RU) — 7 (3/4). Без миграции, без LLM.
  Проверено: 640 unit/integration + 10 e2e, ноль падений.

- **v4.16.23** — импорт формата `1CClientBankExchange` (универсальный для бизнес-счетов). Завершает
  ядро импорт-трека. `parse_1c_exchange`: документы между `СекцияДокумент`…`КонецДокумента`, знак —
  по счёту владельца (`РасчСчет=` шапки: `ПлательщикСчет`=владелец → расход, `ПолучательСчет`=владелец
  → приход), сумма `Сумма=` (dual-locale), описание `НазначениеПлатежа=`. `parse_bank_statement`
  детектит формат по первой строке. Гард `test_1c_exchange.py` (10, синтетика по «kl_to_1c»). Для
  физлиц формат почти не выгружается — под B2B. Без миграции. Детерминированно, без LLM.
  Проверено: 635 unit/integration + 10 e2e, ноль падений.

- **v4.16.22** — импорт PDF-выписок ВТБ и Сбербанка + dual-locale суммы. PDF умел только Тинькофф;
  добавлены `parse_vtb_pdf` (через `extract_tables()`, знак из колонки «валюте операции») и
  `parse_sber_pdf` («Выписка по платёжному счёту», текст-группировка, знак `+` → приход) + диспетчер
  `parse_bank_pdf`/`PDF_PARSERS`. `_num` сделан dual-locale (ВТБ EN-формат `3,000.00` ломал приход).
  Эндпоинт зовёт `parse_bank_pdf(raw, bank_id)`. Гард `test_pdf_parsers.py` (17, синтетика). На
  реальных файлах: ВТБ 46 операций (11/35), Сбер 9 (5/4). Без миграции. Детерминированно, без LLM.
  Проверено: 625 unit/integration + 10 e2e, ноль падений.

- **v4.16.21** — надёжный импорт больших выписок + табличный мульти-банк (CSV/XLSX). Редирект
  Василия на импорт-трек (P2.5 на паузе). Детерминированно, без LLM.
  **#1:** корень обрыва большой выписки — `_is_recurring` COUNT-запрос на каждую строку (12 788
  запросов → таймаут). `bulk_create_transactions` (recurring одним проходом через `Counter`,
  `bulk_insert_mappings` пачками) + эндпоинт на дедуп-в-памяти + bulk; фронт `app.js` — `res.ok` +
  безопасный `res.json()` + понятная ошибка. Замер: 12 614 операций за 0.84с (было: таймаут).
  **#2:** universal-парсер переписан — детект строки-заголовка (Альфа-метаданные сверху),
  нормализация `\xa0`/`ё`/регистра + матч по подстроке, split Приход/Расход (ВТБ), `parse_xlsx`
  (openpyxl). На реальных Альфа CSV/XLSX — по 54 операции. `openpyxl==3.1.5` в requirements.
  Гарды `test_statement_parser_multibank.py` (13) + `test_bulk_import.py` (7). Без миграции.
  Проверено: 608 unit/integration + 10 e2e, ноль падений.

- **v4.16.20** — P2.4 слой 3-B: goal-aware советы (**P2.4 закрыт целиком**). Связь освобождённой
  экономии с целями: «освободив ~X ₽/мес на цель «Y», закроете её на ~N мес раньше». База —
  наблюдаемый темп пополнения из `GoalContribution` за окно; «N раньше» = ETA(темп) −
  ETA(темп+экономия); темп 0 → «достигнете за M мес» (без вычитания ∞); линейно, проценты
  `savings_rate` игнорируются (консервативно); дедлайн — канон `goals_priority._months_left`.
  Dataclasses `GoalRecord`/`GoalImpact`, `analyze_goal_impact` + `_format_goal_impact`; сервис:
  `_goal_records` + ключ `goal_impact` (роут не тронут). Информационный слой — не в `U(a)`.
  `test_spending_goal_aware.py` (14) + строка в `test_api_planning`. UI → дизайн-блок.
  Проверено: 588 unit/integration + 10 e2e, ноль падений. Чистое ядро без БД, PG-долга нет.

- **v4.16.19** — P2.4 слой 3-A: temporal-паттерны трат. Тренд категории по завершённым месяцам
  («траты на X растут ~N%/мес»). Наклон робастный **Theil-Sen** (медиана попарных наклонов, в
  тон ядру; на выбросе 50 000 ₽ в середине OLS дал бы ~5 700, Theil-Sen — 1 000). Ось X —
  **порядковый номер месяца** (пропуски не искажают темп, нули не дорисовываются). **Текущий
  период исключён из тренда** (неполный). Порог тренда **5%/мес** (ниже — flat). `TemporalPattern`
  (+`message`), методы `theil_sen`/`_month_ordinal`/`analyze_trends`/`_format_trend`; сервис
  добавил ключ `temporal_patterns` (роут не тронут). Информационный слой — не в `U(a)`.
  `test_spending_temporal.py` (16) + строка в `test_api_planning`. UI → дизайн-блок (как слои 1-2).
  Проверено: 574 unit/integration + 10 e2e, ноль падений. Новые 16 тестов — чистое ядро без БД,
  PG-долга не добавляют.

- **v4.16.18** — P1.7: симметричный soft-delete + undo для Obligation/Goal/LiquidAsset
  (**P1.7-бэк закрыт**). Транзакции имели мягкое удаление (BUG-03), остальные три сущности
  удалялись жёстко с каскадом — несимметрично/опасно. Добавлены `is_deleted`/`deleted_at` в
  3 модели, миграция 0018 (`server_default=sa.false()` для PG, индекс по `is_deleted`),
  `delete_*`→мягкие, `restore_*`, фильтр `is_deleted IS FALSE` в `get_*`. **Hard-каскад истории
  убран** (платежи/взносы остаются и возвращаются при restore). `restore_*` проверяют
  владельца (строже образца transaction). `delete_user` (152-ФЗ physical purge) не затронут
  (прямой SQL). 3 API restore-эндпоинта (`*_restored` события). Гарды `test_soft_delete.py`
  (6) + `test_cascade_delete` переписан под новый контракт. UI-модалки → дизайн-блок.
  Проверено: 558 unit/integration + 10 e2e, ноль падений. **Миграция 0018 + вся матрица
  прогнаны и на PostgreSQL 16.14 в песочнице** (см. §5).

- **v4.16.17** — P1.6: подключение Sentry в `lifespan` (**P1.6 закрыт**). `init_sentry` в
  `observability.py` существовал (с зависимостью `sentry-sdk[fastapi]` и `SENTRY_DSN`), но
  **не вызывался** — мёртвый код, Sentry не стартовал даже при DSN. Добавлен вызов первым
  шагом в `lifespan` (`environment=ENVIRONMENT`, `release=APP_VERSION` → группировка по
  релизу); без DSN — no-op, тесты/локалка не затронуты. Гард `test_observability.py::TestSentry`
  +2 проверки (проброс dsn/env/release в `sentry_sdk.init` + вызов из lifespan), red→green.
  DSN задаётся на VPS. Очередной случай «код опережал роадмап» (§4).

- **v4.16.16** — P1.2: кэш расчёта плана (Monte Carlo для `/calculate`; **P1.2 закрыт**).
  Бутылочное горло нагрузочного теста (P0.4: медиана ~360мс / p99 ~860мс) закрыто TTL-кэшем
  вокруг `run_planning`. Ключевое: кэш на уровне **расчёта, не роута** — `log_recommendation`
  и `log_event` в `calculate_plan` обязаны срабатывать на каждый вызов (продуктовая воронка),
  поэтому обёрнут именно `run_planning` внутри `_compute_plan`. Отпечаток (sha256) строится из
  **эффективных** аргументов `run_planning` (после резолва `prefs`/`r_bench`), а не из сырого
  payload — иначе один набор данных с разным профилем риска / `l_min` / override отдал бы из
  кэша чужой результат. Отпечаток порядконезависим (записи → канонический JSON, сортировка
  списков) и устойчив к float-шуму (рекурсивное округление). На попадание — `deepcopy` (кэш не
  мутируется вызывающим/логированием). TTL 180с / 256, как у рекомендаций; `today`(=`utcnow`)
  в ключ не входит — на границе суток протухнет по TTL (приемлемо). Новый гард
  `tests/test_planning_cache.py` (4 проверки: попадание в кэш + три инвалидации — профиль
  риска, эффективный доход, смена набора данных), спай `wraps=run_planning`, red→green.
  Зависимостей не добавлено (stdlib `hashlib`/`json`/`copy`). `app/core/` не тронут. Проверено:
  **550 unit/integration + 10 e2e, ноль падений.**

- **v4.16.15** — A11Y P1.1 контраст в **обеих темах** (закрывающий под-пункт; **P1.1 закрыт целиком**):
  Тёмная (`:root`): `--c-text3` `#475569`→`#788aa3` (2.10→4.52:1), `--c-accent` `#6366f1`→`#797bf3`
  (3.57→4.51:1). Светлая (`[data-theme="light"]`, боевая): `--c-text3` `#8a97a8`→`#5d6a7d` (2.43→4.50:1)
  + собственный `--c-accent: #5053ef` (наследуемый из `:root` осветлённый акцент давал на светлом
  фоне 2.90:1). Всё минимальным сдвигом светлоты, иерархия текста цела. Цель AA, не AAA. Контраст
  считан против worst-case композита surface каждой темы (`#192233` / `#e3e9f3`). Новый гард
  `tests/test_a11y_contrast.py` (8 проверок = 4 токена × 2 темы, резолвит наследование светлой,
  без новых зависимостей), red→green. Проверено: 546 unit/integration + 10 e2e, ноль падений.

- **v4.16.14** — A11Y P1.1 механика (нулевой визуал): TDD-харнесс `tests/test_a11y_mechanical.py`
  (90 проверок рендером страниц через TestClient + `beautifulsoup4`). Фиксы: `inert` на всех 8
  модалках + синхронизация в `app.js`/`auth.js` (`aria-hidden-focus`); sr-only `<h1>` на 8 страницах
  без заголовка + класс `.sr-only` (`page-has-heading-one`); `aria-label`/`for=` на селектах и
  инпутах без имени (`label`/`select-name`). Проверено: харнесс 90 + 538 unit/integration + 10 e2e,
  ноль падений. `nested-interactive`/`landmark-unique` на роутах уже были чисты. Контраст — отдельно.

- **v4.16.13** — docs enrichment: `algorithm_stack.md` (стек алгоритмов ядра, сверен с кодом
  v3.0.0: 66 альт/10%, stock-based `Lt`, MC 80%, 5 профилей весов), `ui_ux_design_standard.md`
  (втащен из методичек — закрыта дыра: на него ссылался `ui_requirements_guide`, но в коде его не
  было), `security.md` (срез безопасности по реальному коду). README-индекс обновлён. Только
  доки; код/ядро не трогал.

- **v4.16.12** — hardening: `RATE_LIMITED_PREFIXES` вынесена в константу в `main.py`, добавлены
  `/api/planning/calculate` и `/api/planning/forecast` под rate-лимит (закрыта находка P0.4 про
  незащищённый дорогой compute-путь). TDD: red→green, тест в `test_security_hardening.py`.
- **v4.16.11** — P0.4 нагрузочное: `loadtest/locustfile.py` + `loadtest/README.md`. Baseline 60u/30с
  (прогнано вживую uvicorn+locust): ~110 RPS, `calculate` медиана 360мс / p99 860мс — бутылочное
  горло; rate-лимитер (30/60с на IP+путь) режет `recommendation`, но `calculate` НЕ защищён (находка).
  Установлен `docs/WATCHLOG.md` — вахтенный журнал (этот стандарт).
- **v4.16.10** — doc-sync: `model_vs_code.md`/`GLOSSARY.md`/`reference_profiles.md` приведены к
  коду/v3.0.0 (66/10%, stock-based `Lt`, 80% MC); эталоны перегнаны через реальный движок. Доки.
- **v4.16.9** — стандарты документации: `requirements_guide.md`, `engineering_practices.md`. Доки.
- **v4.16.8** — стандарты: `incident_postmortem.md`, `adr_template.md`, `ui_requirements_guide.md`. Доки.
- **v4.16.7** — P0.3: парсер ЦБ проверен на реальном ответе (зеркало `cbr-xml-daily.ru`), кейс 403
  покрыт; фикстура `cbr_daily_sample.xml`. Найден LIM-001 (огрубление round(,8) на ультра-дешёвых
  валютах — не критично).
- **v4.16.6** — P0.2: `test_email_service.py` (8 тестов `_send`, smtplib замокан) + секция SMTP в `DEPLOY.md`.
- **v4.16.5** — P0.1: chromium-флаги `--no-sandbox --disable-dev-shm-usage` в `tests/e2e/conftest.py`
  (BUG-025); 10/10 E2E + регрессия 431 зелёные вживую.
- **v4.16.4** — P0.1: CSRF/CORS-фикс в `tests/e2e/conftest.py` (BUG-024, динамический origin
  live-сервера в CORS) + E2E-job в CI (chromium+webkit).
- **v4.16.3** — CI: `.github/workflows/ci.yml` (test = матрица SQLite+PG, гейт 90%, блокирующий;
  lint = информационный). Найден/закрыт BUG-023 (дефект тестовых данных, виден только на PG).
- **v4.16.2** — Production-контейнеризация + деплой-инфра: `HEALTHCHECK` в Dockerfile,
  `restart: unless-stopped`, `docker-compose.prod.yml` (web/db на loopback, nginx публичный, том
  `finpilot_pgdata` сохраняется — закрытый BUG-010), `nginx/` (TLS Let's Encrypt + security-заголовки),
  `.env.prod.example`, cron-скрипты + systemd timers, restore/backup_verify, `docs/DEPLOY.md`.

> До v4.16.1 — фазы P1/P2/P3 ядра продукта (право, безопасность, приватность, импорт, курсы ЦБ, PDF,
> кэш рекомендаций, аналитика, рефералы, i18n). Детали — в архивных отчётах проекта.

---

## §4. Нити в работе и уже выясненное (чтобы не повторять)

- **P0.4 (в работе)** — см. §0. Артефакт `loadtest/locustfile.py` готов, прогона/замера нет.
- **a11y P1.1 — механика ЗАКРЫТА в v4.16.14.** Аудит был на v4.16.6 (axe-core, 14 страниц); при
  починке список §4 сверен с реальным кодом и местами расходился — чинилось **по факту**, не по
  аудиту. Что реально было и сделано:
  - `select-name`: по факту **2** селекта без имени (dashboard `#spending-period`, planning
    `#forecast-horizon`) — `#demo-case-select` уже имел `<label for>`. Дано `aria-label`. ✅
  - `label`: 9 инпутов (dashboard budget-*, planning slider-* + scenario-*, transactions tx-date-*
    + file-input). `for=` к видимым подписям, где они были; `aria-label` остальным. ✅
  - `aria-hidden-focus`: страдал не только `auth-modal`, а **все 8 модалок**. Фикс — `inert` в
    разметке + синхронизация `openModal`/`closeModal` (`app.js`+`auth.js`). Механизм видимости
    (через `aria-hidden`) НЕ трогали, чтобы не сломать показ. ✅
  - `page-has-heading-one`: по факту **8** страниц без `<h1>` (группа A, наследуют base, заголовок
    шёл как `<h2>` в top-header). Дано sr-only `<h1>` из `page_title`. Группу B (legal/contacts/
    forgot/reset — свой видимый `<h1>`) НЕ трогали. ✅
  - `nested-interactive` и `landmark-unique`: на текущих роутах **чисты** — правка не понадобилась
    (landmark-проблема была на legacy `index.html`, который не смонтирован как роут).
  - Харнесс-регрессия зафиксирована: `tests/test_a11y_mechanical.py` (90 проверок) — не даст
    откатить эти инварианты.
- **a11y P1.1 — контраст ЗАКРЫТ в v4.16.15 (обе темы).** Корень «плоского серого» оказался не в
  сотнях разных проблем, а в общих токенах `--c-text3` и `--c-accent`. **Важный урок: тем ДВЕ** —
  кроме тёмной `:root` есть боевая светлая `[data-theme="light"]` (переключатель в `base.html`,
  состояние в localStorage), и она наследует неперекрытые токены из `:root`. Тёмная: `text3`
  `#475569`→`#788aa3` (2.10→4.52:1), `accent` `#6366f1`→`#797bf3` (3.57→4.51:1). Светлая: `text3`
  `#8a97a8`→`#5d6a7d` (2.43→4.50:1) + собственный `--c-accent: #5053ef` — без него тема наследовала
  осветлённый под тёмную `#797bf3`, который на светлом фоне давал 2.90:1 (т.е. фикс одной темы ломал
  другую через общий токен). `--c-text2` и state-цвета AA уже проходили в обеих темах — не трогались.
  Выбор AA, не AAA (AAA-`text3` `#96a4b8` совпал бы с `text2`; WCAG не рекомендует AAA со всего UI).
  Зафиксировано гардом `tests/test_a11y_contrast.py` (8 проверок, обе темы, против worst-case
  композита surface каждой). **Остаётся вне песочницы (не блокер):** ручной скринридер
  (NVDA/VoiceOver) на Mac Василия — автотест его не заменяет; и глазами сверить новые оттенки
  светлой темы (`#5d6a7d` / `#5053ef`) — контраст верный, но это визуал.
- **P1.2 — кэш расчёта ЗАКРЫТ в v4.16.16.** Что важно не переоткрывать:
  - Кэшируется **`run_planning`, не роут `calculate_plan`.** Причина: после расчёта роут пишет
    `log_recommendation` + `log_event` (аналитика воронки); кэш на уровне роута убил бы эти
    события на попаданиях. Обёрнут тяжёлый блок внутри `_compute_plan`.
  - Отпечаток — по **эффективным** входам `run_planning` (income/expense/bliq/r_bench/risk/l_min
    + obligations + goals ПОСЛЕ резолва `prefs`, конвертации валют и конвертов), не по сырому
    payload. Хелперы `_round_floats`/`_stable_items`/`_plan_fingerprint` в `routes_planning.py`.
  - На попадание возвращается `deepcopy` — иначе вызывающий код / логирование мутировали бы
    объект в кэше.
  - `conftest.py`: autouse-фикстура очистки кэшей теперь чистит и `_planning_cache` — модульный
    кэш протекал бы между тестами и давал бы ложные «0 пересчётов» в спай-проверках.
  - Что НЕ делалось намеренно: инвалидация по `today` (за TTL 180с протухнет сам); кэш
    forecast-роута (бутылочное горло — `calculate`, не `forecast`).
- **P1.7 — soft-delete ЗАКРЫТ в v4.16.18 (Obligation/Goal/LiquidAsset).** Не переоткрывать:
  - **`delete_user` (152-ФЗ) НЕ трогали** — он делает physical purge прямым SQL
    (`db.execute(delete(...))`), не через `delete_*`-функции, поэтому soft-delete их не задел.
    Аккаунт по-прежнему удаляется со всеми ПДн (тест `test_delete_user_purges_all_personal_data`).
  - **Hard-каскад истории убран** из `delete_obligation`/`delete_goal`: дети
    (payments/contributions) остаются привязанными и возвращаются при restore. Поэтому
    `test_cascade_delete` тесты 1-2 переписаны (раньше проверяли физическое удаление детей).
  - `restore_*` проверяют `user_id` (изоляция) — асимметрия с `restore_transaction`, который
    владельца НЕ проверяет (можно ужесточить отдельно, не в скоупе P1.7).
  - **Edge-case (известный, не блокер):** при soft-delete привязанного `LiquidAsset`
    (`goal.linked_asset_id`) цель ссылается на скрытый актив; конверт (`apply_envelopes`) не
    найдёт его в `get_liquid_assets`. Для undo приемлемо (restore возвращает). Если всплывёт —
    отвязывать цель при удалении актива или предупреждать.
  - **UI-модалки undo** для всех сущностей — в дизайн-блок (бэк готов, фронт нет).
- **Код часто опережает роадмап** — перед «реализовать с нуля» проверяй код: уже оказались готовы
  rate limiting, CSRF, security-заголовки, SMTP, контейнеризация.

---

## §5. Решения и находки этой ветки (контекст, чтобы не переоткрывать)

- **Долг доступности (обновлено v4.16.33: nested ПОЧИНЕН, контраст оказался СИСТЕМНЫМ).**
  - `nested-interactive` — **УСТРАНЁН в v4.16.33**: кнопка удаления вынесена из `<summary>` на
    /obligations в позиционированную обёртку (`app.js`), делегирование `data-obligation-id` цело,
    CRUD-E2E зелёный, axe подтвердил 0 нарушений на dark /obligations. В a11y-ratchet его больше нет —
    повторное появление = регрессия.
  - `color-contrast` — в v4.16.33 закрыта ЧАСТЬ: токен `--c-accent-strong` (dark `#6769cf` white 4.67;
    light `#5053ef` 5.49) для кнопок с белым текстом (`.primary-button`/`.undo-toast-btn`/`.income-advice-btn`)
    + снят `opacity:.85` футера (`--c-text3` без него 5.47/4.89). НО полный axe-скан (14 страниц × 2 темы)
    вскрыл: остаток — **СИСТЕМНЫЙ долг палитры**, не точечный. Особенно light-тема, массово ниже AA 4.5:
    semantic на светлом фоне (`--c-green` ~1.5–1.9, `--c-cyan` ~1.3, `--c-red`/danger ~2.6–3.1,
    `--c-amber` ~1.4), category-badges, ссылки (`--c-accent-hl #818cf8` ~2.5), приглушённый текст
    (`--c-text3` смешения 3.1–4.4). Dark — несколько пограничных (text3, red, accent-pill 2.8–4.4).
  - **Отдельная крупная задача — РЕВИЗИЯ ПАЛИТРЫ под WCAG AA.** Поэтапно по семантическим группам
    (текст-токены → semantic-цвета → badges → ссылки/accent-hl), начиная с light-темы. Каждый шаг — под
    a11y-ratchet (сужать baseline) + регенерация visual baseline. Риск как v4.16.15 (токены обеих тем),
    но теперь под защитой готовых a11y + visual тестов. Десятки токенов × 2 темы — вероятно не один батч.
  - **Почему light хуже:** light-палитра приглушённых/semantic цветов проектировалась «на глаз» под
    тёмную тему; при инверсии на светлый фон массово не дотягивает до AA. Начинать ревизию с light.
  - **ЭТАП 1 РЕВИЗИИ СДЕЛАН (v4.16.34):** введены semantic `-text` токены (обе темы) + переведены все
    `color: var(--c-X)` на них (67 замен); `--c-text3` затемнён (light) / осветлён (dark); ссылки
    `--c-accent-hl` в light тёмные; goal-category badges light-переопределены. Dark почти закрыта.
  - **ЭТАП 2 РЕВИЗИИ СДЕЛАН (v4.16.35): color-contrast = 0** на 14 страницах × 2 темы. **Ключевая находка:**
    ~90% «нарушений» в скане были ЛОЖНЫМИ — артефакт fade-in анимаций секций (axe.run замерял при
    opacity≈0.15). Добавлена заморозка анимаций в `_run_axe` перед axe.run (родственно visual-стабилизации).
    Реальный остаток (20 узлов) добит: semantic во всех шаблонах + 47 JS-переменных app.js (SVG fill не
    тронут); -text затемнены под semantic-badge-фоны (green #136a34, amber #945200 — на badge контраст
    ниже белого); захардкоженные alt-detail цвета → токены (+`--c-pink-text`/`--c-blue-text`); rec-formula
    `opacity:.6` labels → text3; table-header light непрозрачный `#ffffff` (декоративный градиент
    просвечивал через полупрозрачный фон). **`KNOWN_SERIOUS_RULES` = `set()`** — долг доступности закрыт
    полностью, любое serious-нарушение теперь регрессия.
  - **УРОК (для будущих a11y/visual замеров):** замораживать анимации ПЕРЕД инструментальным замером
    (axe, скриншоты) — иначе fade-in/transition даёт ложные срабатывания. Тот же паттерн в visual-регрессии
    (CHANNEL_TOLERANCE) и теперь в a11y (`animation-duration:0s` + settle). Можно оформить отдельным INV.
- **Браузерные тесты в этой песочнице ПОДНИМАЮТСЯ (chromium предустановлен).** `/opt/pw-browsers/chromium-1194`,
  `PLAYWRIGHT_BROWSERS_PATH=/opt/pw-browsers`, playwright 1.56. Запуск с `--no-sandbox --disable-dev-shm-usage
  --disable-gpu`. firefox/webkit по-прежнему недоступны (INV-BROWSERS-SANDBOX, Azure-CDN вне allowlist).
  Песочница: `/tmp` и фоновые процессы НЕ переживают границу bash-вызова — сервер+нагрузку/браузер гонять
  в ОДНОМ вызове или через скрипт; CSV/логи писать в `/home/claude` (персистентно). `locust` запускать
  `python -m locust` (бинарь `.venv/bin/locust` иногда не находится). `column` в песочнице нет — парсить awk.
- **CSP приложения: `default-src 'self'; script-src 'self' 'unsafe-inline'`.** Внешний CDN (cdnjs) браузер
  отвергает → загрузка скриптов с CDN в тестах НЕ работает (был мёртвый a11y-axe). Inline-инъекция
  (`add_script_tag(content=...)`) проходит по `'unsafe-inline'` — так вендорный axe и инжектится.
- **Visual-регрессия требует стабилизации рендера**, иначе flaky даже между двумя прогонами в одной среде:
  ждать `document.fonts.ready`, заморозить анимации/переходы CSS-инъекцией, скрыть `canvas` (графики
  Chart.js недетерминированы), маскировать динамическую шапку `#auth-widget` (auth.js меняет её асинхронно),
  и — главное — **допуск на канал** в пиксель-диффе (значим пиксель только если канал отличается > 40),
  иначе субпиксельный антиалиасинг текста на длинных страницах превышает порог. Эталоны платформо-зависимы.
- **Математическая модель — канон `docs/math_model_v3_0_0.md`, сверено с кодом:**
  **66 альтернатив, шаг 10%, stock-based `Lt = B_liq/E` (месяцы), L_min=0 (фильтр выключен,
  параметризован), MC-интервал 80%.** Если в контексте всплывает спека с **21 альтернативой / шаг 20%
  / flow-based `Lt = Rt/(E+ΣP)` / 95%** — это **УСТАРЕВШАЯ** редакция (v2.0.2), НЕ источник истины,
  не переписывать под неё. Источник истины — код. (Закрыто в v4.16.10.)
- **Фокус — только продакшн.** Бакалаврская ВКР сдана; академический слой (ГОСТы, защита) больше не
  релевантен. Цель — рабочий публичный продукт уровня Т-Банк/Альфа/Сбер к концу лета 2026.
- **Главный рычаг к «рабочему продукту» — деплой на VPS** (руки Василия по `docs/DEPLOY.md`).
  Остаток P0 (живой SMTP/ЦБ, прод-секреты) разблокируется сервером. В песочнице деплой не поднять.
- **Два аккаунта мёржатся** → массовый реформат (`black .`) запрещён, иначе конфликты слияния.
- **Кэш дорогих расчётов — на уровне чистой функции расчёта, а не роута**, если в роуте есть
  сайд-эффекты, которые обязаны выполняться всегда (логирование, аналитика). Ключ строится по
  **эффективным входам функции** (после резолва дефолтов/prefs), иначе кэш «слипает» логически
  разные запросы и отдаёт чужой результат. (P1.2, v4.16.16.)
- **PostgreSQL ПОДНИМАЕТСЯ В ПЕСОЧНИЦЕ — не спихивать PG-проверку на разработчика.**
  Драйвер `psycopg[binary]` в requirements, `apt` доступен (archive.ubuntu.com в allowlist),
  пользователь — root. Процедура (один раз за сессию):
  1. `apt-get update -qq && DEBIAN_FRONTEND=noninteractive apt-get install -y -qq postgresql postgresql-contrib`
  2. `pg_ctlcluster 16 main start` (кластер `main` создаётся пакетом; порт 5432).
  3. Роль/БД: `su postgres -c "psql -c \"CREATE USER finpilot WITH PASSWORD 'finpilot' SUPERUSER;\""`
     + `... CREATE DATABASE finpilot_test OWNER finpilot;`
  4. Прогон: `DATABASE_URL="postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test"
     SECRET_KEY=x ./.venv/bin/python -m pytest ...` (формат psycopg3 — `postgresql+psycopg://`).
     `conftest.py` уважает внешний `DATABASE_URL` и гоняет тот же набор на PG.
  Это и есть обязательная PG-половина матрицы (§6.3) — критична, т.к. SQLite молча игнорирует FK.
  На v4.16.18 вся матрица (558) прошла на PG 16.14 в песочнице.

---

## §6. Жёсткие правила (чек-лист «не дрейфовать»)

1. **TDD** — тест → красный → реализация → зелёный. Реально прогонять, не теоретизировать.
2. **`app/core/` не трогать** без явного согласования (исключение — `spending_advice.py`, не входит в `U(a)`).
3. **Схемные изменения проверять на PostgreSQL** (SQLite молчит про FK — корень BUG-019/023).
4. **SemVer строго:** фича=minor, фикс=patch. Каждый батч — bump `APP_VERSION` + запись в `CHANGELOG.md`.
5. **Один архив на батч**, inner-папка `finpilot_vX_Y_Z_intl`. Файлы — через `present_files`.
6. **Без массового реформата** (см. §5).
7. **Роадмап (P0-P3) — единственный источник приоритетов, строго по порядку.** Не переключаться и не
   изобретать параллельную нумерацию без команды Василия.
8. **Без эмодзи** нигде. Русская проза + английский код. По-братски, прямо, без канцелярита и лишних извинений.
9. **Не предлагать обход** (новый чат и т.п.) до нахождения корня проблемы. `returncode -1` с пустым
   выводом — почти всегда способ вызова (runbook §2), НЕ смерть песочницы.
10. **Перед «реализовать с нуля» — проверь, не сделано ли уже** (§4).
11. **НЕ объявляй задачу невозможной и НЕ спихивай на Василия, не исчерпав фактическую
    проверку.** «Нельзя в песочнице» — гипотеза, а не факт, пока не проверено командой. Реально
    поднимается здесь (НЕ «долг Василия»): **PostgreSQL** (§5 — apt + pg_ctlcluster), **браузер
    для E2E** (`playwright install chromium`), **apt-пакеты**. Сначала попробуй, потом делай вывод.
    Перекладывание работы как «долг» без попытки — запрещено (разбор: INC-PG-SANDBOX). Граница:
    локальный инструмент → подними и проверь; реально внешнее (`cbr.ru` 403, деплой на VPS, живой
    SMTP) → честно отметь как внешнее ограничение.
12. **Серьёзный баг с долгой проверкой гипотез → авто-incident report.** Если на диагностику
    ушло много времени/итераций (несколько гипотез, многоуровневая проверка) — по итогам
    автоматически оформи разбор в отдельном `.md` (`FINPILOT_<тема>_incident.md`, формат как
    `INC-*`: сводка, хронология, корень 5×«почему», resolution, профилактика, уроки) и отдай
    **ОТДЕЛЬНЫМ файлом** через `present_files`, **НЕ** в архиве. Цель — зафиксировать корень и
    метод, чтобы не переоткрывать. Эталоны: INC-PG-SANDBOX, INC-MATHMODEL-FALSEALARM.
    Дополнительно: короткую сводку строкой в `docs/incidents_summary.md` (реестр живёт В архиве —
    для быстрого контекста), а полный разбор — отдельным файлом владельцу (НЕ в архиве).
13. **E2E добавляются автоматически вместе с unit/integration**, а не только юнит. Для любого
    изменения, затрагивающего фронт или API-эндпоинт, — сразу E2E-тест через браузер: юнит
    проверяет функцию, E2E проверяет, что пользователь реально может выполнить действие через UI.
    Базовые CRUD-флоу (создать/удалить/восстановить obligations, goals, transactions) ОБЯЗАНЫ
    иметь E2E. Урок: удаление обязательства было сломано несколько версий при наличии юнит-тестов —
    юнит не ловит разрыв между UI и API. Тесты браузер-агностичны (`--browser chromium/firefox/webkit`);
    в песочнице доступен chromium (firefox/webkit — где доступен playwright CDN, т.е. локально/CI).

---

## §7. Стабильная библиотека (читать из архива `/docs` или загрузок; НЕ дублировать сюда)

| Файл | Зачем |
|---|---|
| `FINPILOT_Claude_sandbox_runbook.md` | Грабли песочницы + готовые команды (bootstrap, тесты, E2E). |
| `docs/math_model_v3_0_0.md` | **Каноническая** мат-модель (источник истины по параметрам). |
| `docs/algorithm_stack.md` | Как мат-модель работает по шагам в коде (9-шаговый конвейер ядра). |
| `docs/diagrams.md` | Фундаментальные Mermaid-диаграммы (8): архитектура слоёв, ER, конвейер ядра (9 шагов), поток `/calculate`, развёртывание/компоненты, жизненный цикл запроса (middleware), аутентификация, карта фронта. |
| `03_Будущий_роадмап.md` | Первоисточник приоритетов P0-P3 (полные описания и обоснования). Полный срез задач продублирован в §2. |
| `docs/engineering_practices.md` | Инженерные решения с обоснованием (ship-fast, fail-loud, гейты). |
| `docs/ui_ux_design_standard.md` | Полная спецификация UI/UX (токены, состояния, a11y, чек-лист DoD). |
| `docs/security.md` | Срез безопасности по реальному коду (что реализовано, что осталось). |
| `docs/CONTRIBUTING.md` | Процесс: TDD-цикл, PG для схемы, Definition of Done. |
| `docs/GLOSSARY.md` | Термины и обозначения (Rt, Lt, Dt, BLR, Sn…). |
| `docs/QA.md`, `docs/reference_profiles.md`, `docs/bug_report_template.md` | QA-уровни, эталоны (движок v4.16.9), формат багов. |
| `docs/incidents_summary.md` | **Реестр инцидентов В репо** (краткие сводки: INC-PG-SANDBOX, INC-MATHMODEL-FALSEALARM, BUG-024/025, ЦБ-403). |
| `FINPILOT_E2E_incident_postmortem.md`, `FINPILOT_CBR_incident_report.md`, `FINPILOT_postgres_incident_postmortem.md`, `FINPILOT_mathmodel_falsealarm_incident.md` | Полные разборы инцидентов — ВНЕ репозитория (у владельца + в Claude-проекте, правило §12); читать из загрузок, не из архива. |
| `Методичка_документирование_инцидентов_и_процессов.md` | Как оформлять post-mortem / ADR / runbook. |
| `docs/DEPLOY.md` | Пошаговый деплой на VPS (для Василия). |
