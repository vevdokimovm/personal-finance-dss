# FINPILOT — Вахтенный журнал (быстрый снимок «где мы сейчас»)

> # [!!!] ПРОТОКОЛ НЕПРЕРЫВНОСТИ — ЧИТАЙ ПЕРВЫМ, ЭТО ОБЯЗАТЕЛЬНО ДЛЯ КАЖДОГО КЛОДА
> **Не копи работу до конца батча. Собирай рабочий архив РАНО и ЧАСТО и отдавай через `present_files`.**
> Токены/контекст кончаются внезапно — если архив не скинут, работу нельзя ни закончить, ни передать
> на вахту, и всё встаёт на часы. Поэтому:
> 1. В начале крупной задачи — оценка объёма + **вероятность уложиться** (высокая/средняя/низкая).
> 2. Готовое отдавай ДО того, как браться за тяжёлый/творческий хвост.
> 3. По ходу переоценивай «успею/нет»; при росте риска — **немедленный чекпоинт-архив**.
> 4. WATCHLOG держи актуальным ПО ХОДУ (не в конце) — это точка передачи на другой аккаунт.
>
> **Полный протокол — `docs/session_continuity.md`. Нарушение = потеря работы и простой. Не пропускай.**

> **Что это и зачем.** Снимок-контекст для мгновенного входа в работу. Василий ведёт продукт на
> **4 аккаунтах Claude (V, J, M, S)** и в нескольких чатах внутри проекта; токены кончаются —
> вахту принимает другой аккаунт/чат. Этот журнал даёт принимающему быструю сводку: **где стоим,
> что сделано, что дальше, на какие грабли не наступать** — чтобы поднять картину с ОДНОГО архива,
> не читая 20k строк кода. (Метафора в тему бренда — штурман у штурвала.)
>
> **Принцип тонкости.** Журнал НЕ дублирует историю. Полная история версий — в `CHANGELOG.md`
> (каждый патч) и `docs/RELEASES.md` (эпохи). Направление и задачи — в `docs/ROADMAP.md`. Здесь
> только выжимка + ссылки.
>
> ## [!] ЖЁСТКОЕ ПРАВИЛО: §3 хранит РОВНО 10 последних версий, не больше
> §3 — это окно из **10 самых свежих версий, и только**. Добавляя новую версию сверху, СРАЗУ удаляй
> самую старую снизу, чтобы их всегда оставалось 10. Без этого журнал быстро разрастается и теряет
> смысл «быстрого снимка». Удалённое не пропадает — вся история живёт в `CHANGELOG.md` и
> `docs/RELEASES.md`. Это правило касается ИМЕННО этого файла: здесь старое именно УДАЛЯЕТСЯ.

> ## [!] Журнал ведётся НЕПРЕРЫВНО, по ходу работы
> Обновляется СРАЗУ по ходу (сделал подшаг → §0; нашёл грабли/факт → §4), а не «в конце сессии» —
> токены могут кончиться в середине батча. В любой момент журнал обязан отражать реальную текущую
> точку. **См. протокол непрерывности — `docs/session_continuity.md` (и баннер вверху файла).**

> **Как пользоваться (Василий).** Новому чату/аккаунту: этот файл (или его текст) + последний архив
> → «продолжай». Копия в чат — с версией в имени: `WATCHLOG-personal-finance-dss-vX.Y.Z.md`
> (канон — дефисы, точки в версии, см. `[[feedback_finpilot_archive_naming]]`). Архивная копия —
> `docs/WATCHLOG.md` без версии.

> ## ВЕРСИЯ КОДА: `v8.25.3` · **КАНОН v3.9.0 · КАРТА КАЧЕСТВА 87.23/100 — CRUD-ПАРИТЕТ
БАТЧ 1 (ОБЯЗАТЕЛЬСТВО + АКТИВ) ЗАКРЫТ, СЛЕДУЮЩЕЕ — БАТЧ 2 (ОПЕРАЦИЯ + ЦЕЛЬ)
v8.25.3 — оценка стоимости починки джобы `Линтеры` (информационная, не блокирует деплой):
flake8 — 5 минут, ноль риска, починено (`tools/model_validation/model_snapshot.py`, 3 строки
E501); mypy — уже отдельная задача ROADMAP §8.2, не трогать отдельно; pylint — ~150 находок,
часы на весь `app/`, старый фон, не регрессия — не стоит того сейчас. Остаток — новый чекбокс
ROADMAP §9.0, после вехи 8. Полная деталь — CHANGELOG [8.25.3].
v8.25.2 — расследование по прямому вопросу владельца: блокирующий гейт CI был красным
**41 день, 65 релизов** (v5.25.0…v8.25.1) — post-mortem с 5 Whys,
`docs/reports/incidents/ci_blocking_gate_red_41_days.md`. Попутно найден и разобран отдельный
инцидент — первый реальный прогон PostgreSQL-матрицы завис на 60+ минут на `apt-get`-флюке
GitHub-раннера (не код), плюс реальный, но некритичный баг health-check Postgres-сервиса
(`pg_isready` без `-d`). Починено: `timeout-minutes` на всех джобах `ci.yml` (не было ни у
одной), health-check, декоративный `|| true` на locust-smoke. Полная деталь —
CHANGELOG [8.25.2].
v8.25.1 — `python -m tools.preflight` падал на Linux/CI (битая ссылка на заглавный
`GLOSSARY.md` в WATCHLOG), был зелёным на macOS: `LinkChecker` сверял ссылки через
`Path.exists()`, а APFS
регистронезависима — та же природа бага, что чинилась в v8.4.1 для физических дублей пути,
теперь для ссылки без второго файла на диске. Починено: `_exists_with_matching_case()` через
`os.listdir()` вместо `Path.exists()`, регрессионный тест (red→green), само упоминание в
§3 переписано без битого пути. Полная деталь — CHANGELOG [8.25.1].
v8.25.0 — React-экраны «Обязательства»/«Ликвидные активы» были read-only, теперь полный
CRUD: `shared/ui/Modal.tsx` (первое применение `@radix-ui/react-dialog`), `ObligationForm`/
`AssetForm` с валидацией обязательных полей, мягкое удаление с `toast.undo` (самозакрывается
по клику — не даёт дёрнуть `/restore` дважды), `Button variant="danger"` для удаления.
До сдачи design-critic (12 блокеров) и a11y-auditor (3 нарушения AA) нашли и починили:
хардкод-цвет оверлея и слой модалки = слою полей внутри (лестница возвышения ломалась);
`0.078 * 100 === 7.800000000000001` — мусор в поле ставки при правке; `entityForm.css`
дословно дублировал `AuthLayout.css` (CMP-01) — объединены в `shared/ui/field.css`; `.fp-button`
без состояния `:disabled`; фокус после удаления строки проваливался в `<body>` (WCAG 2.4.3);
восстановление не объявлялось скринридеру (WCAG 4.1.3); остаток долга не показывался в строке
списка, хотя форма делает его обязательным первым полем. Vitest 187/187 (было 175 до PATCH
v8.24.1). Проверено живьём в браузере (Chrome DevTools MCP), обе темы. Детали — CHANGELOG
[8.25.0]. v8.24.1 — три вещи перед CRUD-батчами §8.2: гейт `require_financial_consent` на
`budgets_router` (был единственным из пяти финансовых роутеров без него — лимиты по
категориям тоже финансовые данные); мягкое удаление + `restore` для бюджета (миграция `0034`,
симметрия с обязательствами/целями/активами) — `create_budget` (upsert по категории, FR-22)
теперь оживляет ранее удалённую строку вместо UNIQUE-конфликта на `category`; `extractErrorMessage.ts`
разбирает объектный 403 гейта согласия (`{code, consent_type, document, message}`) — текст +
ссылка на документ плоской строкой, минимум до экрана согласия L3, чтобы CRUD-батчи ниже не
переделывать. Матрица SQLite+PostgreSQL прогнана вручную (Homebrew `postgresql@15` локально,
не Docker — `pg_matrix_last_run.md`), 43/43 на PG. Полный pytest 1593/1593, Vitest 178/178.
Далее — CRUD-паритет §8.2 батчами: 1 (Обязательство+Актив, 3 дня) → 2 (Операция+Цель, 3,5 дня,
решения по прогрессу цели и полям операции приняты) → 3 (Бюджет, только фронт, 1 день) → PDF-отчёт
§8.4. v8.24.0 — `ForecastControls` на `/planning`: select горизонта (3/6/12/24 мес.) + ползунок
ставки капитализации r_bench (0–30%, дебаунс 500 мс) с кнопкой «Сбросить к реальной». Бэкенд
`ForecastRequest.r_bench` — тот же паттерн override, что уже был у `PlanningRequest.r_bench`;
ответ дополнен `real_r_bench` (считается ВСЕГДА, не только без override — иначе кнопке сброса
неоткуда взять настоящую ставку, баг найден и исправлен при живой проверке в браузере).
До сдачи `design-critic` и `a11y-auditor` (оба нашли реальные проблемы) починили: сырой JS-`%`
вместо `formatPercent` (точка вместо запятой в деньгах, CMP-04); `isFetching` доходил только до
скринридера, зрячий не видел пересчёта вообще; `flex:1` без `flex-basis` не переносил поле на
узких экранах; фокус после исчезновения кнопки «Сбросить» улетал в `<body>`; сценарий не был
помечен в самом графике (единственный признак — кнопка в другом месте экрана). Параметры движка
(α/β Holt, σ Monte-Carlo, ширина CI 80%) сознательно НЕ отданы пользователю — канон-заморожены.
Побочно: `tools/revision/revision_check.py` — CJK-канарейка ловила себя на минифицированном
KaTeX в `frontend/dist/` (сборочный артефакт) — `dist` добавлен в `SKIP_DIRS`.
Vitest 175/175 (было 172), tsc/eslint/prettier/build чисты. v8.23.0 — частичный снос
Jinja-фронта перед Э6: снесены только `profile`/`forgot-password`/`reset-password`+мёртвый
`index.html` (React-паритет подтверждён). Первый проход по ошибке снёс ещё и `dashboard`/
`planning`/`transactions`/`obligations`/`goals`/`banks` — их React-версии read-only, снос убрал
бы формы создания; найдено и исправлено в том же батче. Побочно подтверждена уже
задокументированная (с v8.6.0) находка: nginx с v8.9.0 прячет Jinja-CRUD этих шести путей от
обычного доменного трафика — продукт не запущен публично. Разбор —
`docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`. v8.22.0 — экраны
входа/регистрации с нуля (`LoginPage`/`RegisterPage`/`ForgotPasswordPage`/`ResetPasswordPage`,
`shared/ui/Toast.tsx`, `AuthTopbarLink`), пойман и починен баг stale-if-error в топбаре после
логаута. v8.21.0 — последний
пункт плана Э5: `ZoneScale` (шкала с зонами) в MetricsGrid + дисклеймер 601-ФЗ при ПДН > 50%
— все шесть пунктов плана Э5 теперь технически закрыты. v8.20.1 — переменные модели везде
через KaTeX (прямая поправка владельца). v8.20.0 — формулы KaTeX по клику (CMP-05).**
**v8.11.0 (ползунки «что если», MINOR):** составной столбец, легенда и диаграмма Санкея в
`AllocationPanel` теперь реагируют на два ползунка (долг/цели, резерв — остаток) — один
интерактивный артефакт, как в плане, не рекомендация сверху и песочница снизу отдельно.
Результат гипотезы ищется в уже посчитанном `ranked[]` (`findMatchingAlternative`), недоступные
модели комбинации помечаются явно (fail-loud). До сдачи `design-critic` (2 запуска — первый упал
на сетевой ошибке API) и `a11y-auditor` нашли и починили три блокера: столбец был заморожен на
рекомендации, пока ползунки показывали другие числа (переписано на общее управляемое состояние);
`accent-color` обоих ползунков совпадал с `--c-green` («Цели») — ползунок долга был случайно
зелёным (теперь цвет = цвет категории); блок результата был неотличим от фона панели в светлой
теме (`--c-surface-up` == `--c-surface` там, тот же класс бага, что раньше чинили для
`--c-bg2`/`--c-surface`) — добавлена рамка. Плюс порог ПДН приведён к словарю бейджей
`MetricsGrid` (был голым текстом), `aria-valuetext`/`label` дублирование убрано, живая область
задержана на 400 мс (не объявляет каждую из 11 отметок при автоповторе стрелки), добавлен
`color-scheme` в tokens.css (первые в проекте range-инпуты вскрыли его отсутствие). Vitest 99/99
(было 83/83). Детали — CHANGELOG [8.11.0]. **v8.10.0 (Санкей,
MINOR):** режим «подробно» под легендой `AllocationPanel` — recharts `<Sankey>`, свёрнуто по
умолчанию, тогл-кнопка не показывается при единственной активной категории/дефиците. Дефолтные
рендереры recharts заменены целиком (магические `#0088fe`/`#333`). До сдачи `design-critic` и
`a11y-auditor` нашли и починили: ложную `aria-expanded`/`aria-controls` на decorative
aria-hidden блоке (→ `aria-pressed`), перепутанные якоря подписей узлов, магические числа в
SVG (сведены к шкале `--sp-*`), `utility.toFixed(2)` без запятой (→ `formatNumber`),
сплющивание на узких экранах (→ `overflow-x` + `--chart-sankey-min-width`). Осознанно отложено:
копия «U = {u}»/«СППР» (решается штатным KaTeX-пунктом Э5) и незагружаемый Manrope в SPA
(найдено попутно, отдельная задача на весь SPA). Vitest 83/83, build/eslint/prettier чисты.
Детали — CHANGELOG [8.10.0]. **v8.9.4 (стабильное имя канона — docs/math_model.md,
PATCH):** структурная причина серии v8.9.2–v8.9.3 — имя файла канона версионировалось
(`math_model_v3_X_Y.md`) и требовало ручного обновления ссылок при каждой рекалибровке.
Владелец предложил устранить класс бага: `docs/math_model_v3_5_0.md` → `docs/math_model.md`
(стабильно, версия только внутри файла), `v3_0_0.md`/`v3_1_0.md` → `docs/model/history/`.
`session-context.sh` и `finpilot-math-model-guard` больше не зависят от номера в имени файла.
~45 живых ссылок по репозиторию переведены на новый путь; три замороженных отчёта, случайно
задетых первым проходом bulk-замены, восстановлены как есть и занесены в `LINK_ALLOWLIST`
(отчёты не переписываются задним числом). Канон v3.5.0 попутно подтверждён рантаймом:
`effective_floor_months`/`is_toxic_debt` (G8) реально вызывается из `planning.py:139-141`,
`build_crisis_plan` (G2) реально вызывается, `avalanche.py` подтверждён как одномоментное
распределение — по всему `app/` нет функций со `schedule`/`amortiz`. `revision_check` CLEAN
(было 5 битых ссылок сразу после переноса файлов), `preflight` чисто, 31/31 тестов G8/G2/
avalanche, 9/9 Vitest на тронутом `MetricsGrid`. Детали — CHANGELOG [8.9.4].
**v8.9.3 (доследствие ревизии v8.9.2, PATCH):**
владелец перепроверил v8.9.2 и нашёл три хвоста: (1) `docs/ROADMAP.md` строка 101 всё ещё
называла канон v3.0.0 — ревизия искала имя файла, не голое упоминание версии в прозе; это и
был первоисточник ошибки; (2) канон подтверждён теперь ПО КОДУ — `app/core/ranking.py`:
`TOXIC_FLOOR_MONTHS`/`is_toxic_debt()` с комментарием в коде «третья сертификация, v3.5.0»,
`app/core/crisis.py` реализует G2 (§12); `app/core/avalanche.py` подтверждён кодом как
одномоментное распределение — вывод про отложенную лавину долгов остался верным при
правильной версии канона; (3) снос старых Jinja-шаблонов/роутов для семи перенесённых на
React экранов выпал из плана при перенумерации этапов — восстановлен явным шагом Э8
(`docs/frontend_milestone8_plan.md`) + чекбокс ROADMAP §8.2, `templates/index.html`
подтверждён мёртвым (не подключён ни к одному роуту). Детали — CHANGELOG [8.9.3].
**v8.9.2 (ревизия канона матмодели по всему репо + README,
PATCH):** владелец поймал, что ассистент дважды подряд назвал каноном матмодели v3.0.0 —
реальный канон v3.5.0 (пять раундов сертификации). Найдено: это старый, уже раз пойманный и
брошенный баг (`docs/ui_visual_direction.md` уже флагал ровно это раньше). 28 файлов
(`CLAUDE.md`, README, доки, knowledge/, код-комментарии, докстринги тестов) заявляли
`math_model_v3_0_0.md` текущим каноном — приведены к `v3_5_0.md`; исторические файлы
(`v3_0_0.md`, `v3_1_0.md`, оба с баннером `SUPERSEDED`) НЕ тронуты — по прямому требованию
владельца история версий модели остаётся. `.claude/hooks/session-context.sh` — вместо
перезаписи числа сделан самообновляющимся (`grep -L SUPERSEDED docs/math_model_v*.md`), баг
не повторится на v3.6.0. `.claude/skills/finpilot-math-model-guard/SKILL.md` — добавлены G1–G8
(гейт вообще не знал про кризисный режим/токсичный долг/floor). Попутно и отдельно —
**README.md** оказался устаревшим сильнее матмодели: версия в шапке отставала на целую веху
(7.7.1 вместо 8.9.1), таблица технологий не упоминала React/Vite/TanStack вообще (описывала
фронт как «Jinja2 без SPA»), миграции 19 вместо 31, добавлена отсутствовавшая эпоха v8.x.x,
инструкция запуска дополнена фронтом (`npm run dev`, Vite-прокси `/api`) — `docker compose up`
(dev) SPA не поднимает, это не было нигде явно сказано. Подробности — CHANGELOG [8.9.2].
**v8.9.1 (Python 3.12→3.13, PATCH):** закрыт разрыв
dev↔prod/CI — `.venv` был на 3.13 с v8.7.1, `Dockerfile`/CI оставались на 3.12. Обновлены
`Dockerfile` (обе стадии), `.github/workflows/ci.yml` (все job'ы), `.mypy.ini`, `pyproject.toml`,
живые доки и `knowledge/roles/*`. Подтверждено: `docker build` на `python:3.13-slim` зелёный
(cryptography/bcrypt/psycopg штатными cp313-колёсами), смоук в контейнере (импорты + 31
миграция Alembic) чист, полный прогон на реальном PostgreSQL 16 под 3.13 — зелёный (exit 0),
`preflight` чист. **v8.9.0 (прод-сборка фронта и деплой на чистом
стенде, MINOR):** задача владельца вне канона плана вехи 8 (Э5→Э6 юр→Э7→Э8) и вне порядка
ROADMAP (деплой формально веха 9). `nginx/Dockerfile` (multi-stage node+nginx) — SPA
собирается прямо внутри `docker compose up --build`; nginx разводит маршруты: `/`,
`/planning`, `/transactions`, `/obligations`, `/goals`, `/banks`, `/profile` — SPA
(`index.html` + fallback), всё остальное (`/api`, `/v1`, `/health`, `/docs`, `/static`,
`/contacts`, `/legal/*`, `/reset-password`, `/forgot-password`, `/validation`) — FastAPI по
умолчанию через fallback `location /`. Корень `/` — SPA-дашборд, не отдельный лендинг
(уточнено у владельца; `templates/index.html` был мёртв ещё до React). `deploy/env.prod.example`
воссоздан (был `.env.prod.example`, утрачен при восстановлении `.git` в v8.7.1; переименован —
хук `block-secrets.sh` блокирует Write/Edit/Read на `*.env.*` без разбора примера от секрета).
Локальная проверка на изолированном docker-стенде (свой том, свой порт — реальный
`finpilot_pgdata` не тронут) поймала три реальных бага: стоковый `default.conf` образа nginx
перехватывал `Host: localhost` healthcheck раньше нашего конфига (контейнер вечно `unhealthy`
при рабочем внешнем трафике) — убран; IPv6 не слушался — `listen [::]:80/443` добавлен;
`docs/DEPLOY.md` учил генерировать `TOKEN_ENCRYPTION_KEY` через `openssl rand -hex 32` —
уронило бы прод на старте (`TokenCipher` требует строгий формат Fernet-ключа), заменено на
`Fernet.generate_key()`. Попутно найден и починен баг ревизионного гейта
(`tools/revision/revision_check.py`): `REF_PATTERN` обрезал «расширения» длиннее 6 символов
(`\.\w{1,6}` → `\.\w+`) — `deploy/env.prod.example`/`finpilot.conf.template` ложно считались
битыми ссылками. По ходу на машине кончилось место на диске (33 ГБ в
`~/Library/Containers/com.docker.docker`) — снесено с согласия владельца, там только
синтетические dev-тома, ничего боевого. Полный прогон: **1512/1512, 0 errors, 0 failures**
(1 skipped); Vitest 75/75, Playwright 25/25; `flake8` чист; `mypy` — 3 известных долга (см.
v8.5.0). **v8.8.0 (Э5 — браузер 66
альтернатив на /planning, MINOR):** первый пункт Э5. `AlternativesBrowser` раскрывает полный
`ranked[]` под `AllocationPanel` с сортировкой по критериям — новых бэкенд-полей не нужно, `ranked`
уже отдавался, фронт его не читал. `PlanAlternative`/`CalculatePlanResult` дотипизированы (`id`,
`Rt_new`, `Lt_new`, `Dt_new`, `is_recommended?`). `api-contract-guard` чисто (поймал неточность в
собственном комментарии types.ts — поправлено). `a11y-auditor`: 2 P1 починены (контраст бейджа
«рекомендовано» в светлой теме ~4.30:1 → сплошная пара `--c-accent-strong`/`--c-accent-ink`;
пересортировка списка не анонсировалась диктору — добавлен `role="status"`), плюс по мелочи
(`aria-controls`, скрытая стрелка, `aria-label` списка, `--c-text3`→`--c-text2` на грани контраста).
Попутно, первым же полным прогоном на закоммиченном дереве (см. v8.7.1), нашлись и почищены два
пре-существующих гейт-долга, не связанных с фичей: битая ссылка в историческом
`docs/reports/ui_audit_e3.md` на `ForecastPanel.tsx` по старому пути (точечное исключение в
`LINK_ALLOWLIST`, отчёт не переписан) и 2×E501 в `.claude/hooks/_run_test_slice.py`. **Лавина
долгов (второй пункт Э5) сознательно отложена** — требует новой математики помесячной амортизации,
которой нет в каноне v3.0.0 (только одномоментное распределение); решение расширять канон — за
владельцем. **v8.7.1 (git-репозиторий
восстановлен, PATCH):** `.git` был случайно инициализирован в `$HOME` прошлой сессией (0 коммитов,
без remote, весь `finpilot/` физически не был под git) — `git init` перевыполнен внутри `finpilot/`,
первый коммит зафиксировал дерево на `v8.7.0` (root-commit, 1384 файла), `node_modules/` добавлен
в корневой `.gitignore`. Пустой `.git` из `$HOME` отодвинут в бэкап, не удалён — решение за
владельцем. Кода продукта не тронуто. Бэк логикой не тронут (только версия в `app/config.py`) —
но именно на v8.7.1 впервые поднят `.venv` (Python 3.13) с зависимостями бэкенда: Stop-гейт до
этого молчал не потому что зелёный, а потому что не мог импортировать `app.main`
(`ModuleNotFoundError: No module named 'jwt'` на системном python3). Первый настоящий полный
прогон на закоммиченном дереве — **1512/1512, 0 errors, 0 failures** (1 skipped, v8.8.0), `flake8 .`
чист, `mypy app` — 3 известных долга (см. v8.5.0), preflight чисто.
Фронт (v8.8.0): Vitest 75/75 (было 72, +3), Playwright 25/25 (было 24, +1), `tsc`/`eslint`/
`prettier`/`check-design-tokens.sh` чисто. **v8.7.0 (Э4 партия 2 — планирование/активы/профиль, MINOR):**
все пять экранов пятёрки ROADMAP §8.2а на React, чекбокс закрыт `[x]`. `/planning` переиспользует
`usePlan`/`useForecast` (те же, что dashboard, не новая сущность) + виджеты `MetricsGrid`/
`AllocationPanel`/`ForecastPanel`, вынесенные из `pages/dashboard/ui` в `@widgets/*` при этой
партии (долг Э3 — `widgets/README.md` называл их примерами виджетов с самого начала, но Э3 положил
их в pages/dashboard). `AllocationPanel` теперь сам принимает `best: PlanAlternative | null` и
рендерит fail-loud «Плана распределения нет» — раньше эта ветка была захардкожена только в
`DashboardPage.tsx`. Сверх пятёрки тем же конвейером: `/banks` (ликвидные активы, путь как в
старой Jinja) и `/profile` (только чтение). Реальный баг пойман до отправки: `interest_rate`
ликвидных активов чуть не поделили на 100 по инерции от формата старой Jinja-формы (0-100 в
input) — сверка с бэкенд-схемой (`Numeric(6,4)`, доля, та же точность что у обязательств) поймала
до коммита. `a11y-auditor`: 1 P1 починен (`/profile` — 401 и сетевой сбой давали идентичное
сообщение с тупиковым циклом «Повторить» без объяснения причины, особенно для screen reader —
`useProfile.ts` теперь различает статус через `throwOnError: false`, отдельная ветка `role="status"`
без кнопки повтора для 401), 3 P2 сделаны (sr-only подпись риск-бейджа, `aria-label` на
`MetricsGrid`, `role="presentation"` на alloc-bar), 2 отложены. `api-contract-guard`: чисто, один
пробел найден на уровне контракта — `openapi.json` не описывает `security`/401 для `/api/auth/me`,
хотя бэкенд их требует (`require_user`) — расхождение схемы, не фронтенда, правка не в этом
батче. **v8.5.2 (правки
UI-аудита Э3, PATCH):** до Э4, разбор `docs/reports/ui_audit_e3.md`. P0-1: чёрный прямоугольник
вместо графика прогноза в тёмной теме — маска `fill=var(--c-bg)` без `stackId` убрана целиком (не
патч через один `stackId`), заменена на два стекированных `Area` (невидимая база p10 + видимая
дельта p90-p10) — корректно и на `anna` (позитив), и на `pavel` (дефицит, где старый баг случайно
не проявлялся). P0-2: тумблера темы не было вообще — `useThemeStore` (Zustand+persist,
light/dark/system, `data-theme` на `<html>`), нативный `<select>` в новом `.fp-app-topbar`. P1-3:
ПДН>0.40 не подсвечивался — добавлен `danger`-вариант (канон v3.0.0: жёсткий инвариант Dt<=0.40),
приближение (Dt>0.35) — warn; ликвидность/подушка — только warn НИКОГДА danger (канон: отсев по
ликвидности мягкий, L_min=0 по умолчанию выключен, красный означал бы несуществующий жёсткий
порог). P1-4: skeleton теперь резервирует место под AllocationPanel/ForecastPanel — не прыгает при
loading→loaded. `a11y-auditor` подтвердил закрытие 2 находок Э3 (акцент ПДН, иерархия заголовков),
нашёл и почитан один P1 (рамка ThemeToggle ~2.9:1 в тёмной теме, ниже WCAG 1.4.11 — заменена на
`--c-text3`, ~5.3:1). P2-5/P2-6 (devtools поверх кнопки на 375px, favicon 404) — отложены осознанно
(dev-only / нужен брендовый актив). Побочно: `docs/onboarding.md` — явное предупреждение про
`.venv` (найдено на практике в v8.5.1: системный python3 на машине резолвился в 3.10, часть
зависимостей ставилась глобально молча). **v8.5.1 (Stop-гейт тестов починен, PATCH):** `.claude/hooks/tests-gate.sh` был fail-open — гонял полную суиту на каждом `Stop` при таймауте хука 10 мин, молча пропускал `Stop` без блокировки при таймауте (найдено `/doctor`: 20/21 таймаутов в соседнем чек-ауте v8.3.1). Заменён на срез по затронутым файлам сессии + маркер `property` (инварианты матмодели, пол 20-51 с); таймаут хука 600→120с; таймаут по фазам — `subprocess.run`, не shell `timeout(1)` (нет на голом macOS); попутно два грабля bash 3.2 на macOS (`mapfile` не существует, `"${arr[@]}"` на пустом массиве под `set -u` падает). **Живая обкатка тут же поймала фан-аут:** правка `app/config.py` дала 12 затронутых файлов (3 уже ~39 с, 12 не уложились и в 2 мин) — тот же fail-open заново, другой триггер. Добавлен потолок `MAX_AFFECTED_FILES=3`: сверх — фаза по файлам честно пропускается, не гонится через силу; бюджеты по фазам пересчитаны по замерам (45+65=110 с, зазор 10 с до внешнего таймаута). `lint-python.sh` уже был корректно ограничен одним файлом — добавлен только явный `--config=.flake8`. **ГЛАВНОЕ ЗА v8.4.1:** починен дефект агентского контура, который вскрыл заход 7 исследования — в v8.2.0 `.claude/settings.json` не имел **ни одного блокирующего хука**, весь заслон 152-ФЗ на секреты держался на тексте в `CLAUDE.md`, а `$CLAUDE_FILE_PATHS` шёл без кавычек (инъекция через имя файла). **ЗАСЛОН ДЕЛАЕТСЯ ХУКОМ, А НЕ ТЕКСТОМ:** `PreToolUse` exit 2 блокирует инструмент даже в `bypassPermissions`; текстовый запрет модель обходит рассуждением (кейс Replit: агент снёс боевую БД вопреки объявленному code freeze). Поставлено семь хуков: блок секретов, блок деструктива (`rm -rf`, `alembic downgrade`, `black .`), линтеры, аудит-лог в `logs/agent-audit.jsonl`, `Stop`-гейт на pytest со `stop_hook_active`-гардом, контекст на `SessionStart`. **ЗАВИСИМОСТЬ ОТ `jq` УБРАНА:** без него блокирующие хуки молча пропускали всё — разбор конверта на `python3`, неразборчивый вход = exit 2 (fail-closed). **ШАГ 3.5 ЗАКРЫТ:** шесть скиллов FINPILOT с евалами (позитив+негатив+edge). **ВОССТАНОВЛЕНО ПОСЛЕ ОБРЫВА ЧАТА №15:** `docs/claude_operating_table.md`, `docs/claude_token_lab/`, `docs/reports/decisions/` — кода потеряно не было. **ПРАВИЛО §19:** решение за владельцем оформляется структурным разбором (≥2 варианта включая «ничего не делать», явная рекомендация, что её изменило бы), вахта не решает молча. **XZ — РЕКОМЕНДАЦИЯ ОТКЛОНИТЬ:** замерено 37 539 861 → 27 245 412 Б (−27,4%), sha256 совпал на 27 из 27, но git-история растёт на 26 МБ — задача была уменьшить репозиторий, а предложение его увеличивает. **PIT-015:** трое суток без чекпоинта.
**ЗА ВЛАДЕЛЬЦЕМ:** решение по xz; `claude update` (нужна >= 2.1.181 для `/design-sync`, качается через VPN); включить Code Execution; `/doctor`; запушить зеркало `finpilot-mirror-v8.0.0.zip`. **ПРАВИЛА:** перед сдачей `python -m tools.preflight` · матрица SQLite+PostgreSQL на схему · **Opus 5 / high по умолчанию, `max` — только эскалация** (TTFT ~92 с против ~3 с на low) · §18 исходники в репозиторий всегда и первыми · §19 решения владельца — документом. ВЕХА 9: L8, Redis rate-limit, CSP без `unsafe-inline`, TLS/HSTS, прод-секреты; **O1 уведомление РКН подаётся ДО начала обработки.**
> Если архив другой версии — сверь `app/config.py`.

---

## §0. RESUME POINT — отсюда продолжаем

- **[!] 2026-08-19 — v8.25.3: FLAKE8 ЧИСТ, ДОЛГ ЛИНТЕРОВ В ROADMAP (PATCH).** По прямому
  вопросу владельца — оценка стоимости починки джобы `Линтеры` (информационная, не блокирует
  деплой) до того, как чинить. flake8 (3× E501 в `tools/model_validation/model_snapshot.py`) —
  5 минут, ноль риска, починено. mypy (3 ошибки в парсере выписок) — уже отдельная задача
  ROADMAP §8.2, решено не дублировать. pylint (~150 находок по всему `app/`, оценка 9.62/10) —
  старый стилевой фон, не регрессия последних версий, часы работы — решено НЕ чинить сейчас.
  Остаток задокументирован новым чекбоксом `docs/ROADMAP.md` §9.0 — после закрытия вехи 8
  (фронт), перед реальным деплоем. Деталь — CHANGELOG [8.25.3].
- **[!] 2026-08-19 — v8.25.2: CI КРАСНЫЙ 41 ДЕНЬ / 65 РЕЛИЗОВ — POST-MORTEM (PATCH).** По
  прямому вопросу владельца («как давно это было?») — полный разбор истории 274 прогонов CI
  через `gh api`: блокирующий гейт (`preflight`+`fast`) был красным на КАЖДОМ пуше/теге
  непрерывно с `v5.25.0` (2026-07-09) по `v8.25.1` (2026-08-19) — **41 день, 65 версий**, ни
  разу не дошедших до реального `pytest` в CI (гейт падал на `revision_check` ДО старта
  тестов). Причина — тот же класс бага регистра путей `docs/glossary.md`, дважды подряд, в
  двух направлениях (v5.25.0: ссылки строчные vs файл заглавный; после неполного фикса v8.4.1
  — наоборот). Процессный корень (5 Whys): ритуал сдачи батча сверяется с локальным
  `preflight`, но никогда — с реальным статусом GitHub Actions; `deploy.sh` пушит версии
  независимо от статуса CI. **Полный post-mortem** —
  `docs/reports/incidents/ci_blocking_gate_red_41_days.md` (SEV2, blameless, action items —
  сверка статуса CI НЕ встроена в ритуал, вынесена в action items, не закрыта в этом батче).
  Попутно — отдельное расследование зависшего первого прогона PostgreSQL-матрицы (60+ минут на
  `apt-get`-флюке GitHub-раннера, не код; плюс реальный некритичный баг health-check Postgres
  `pg_isready` без `-d`) —
  `docs/reports/investigations/ci_full_pg_matrix_hang_investigation.md` (INV-CI-PG-HANG).
  Починено: `timeout-minutes` на всех шести джобах `ci.yml` (не было ни у одной — зависание
  ждало бы дефолтные 6 часов GitHub), health-check, декоративный `|| true` на locust-smoke.
  Деталь — CHANGELOG [8.25.2].
- **[!] 2026-08-19 — v8.25.1: `LinkChecker` СТАЛ РЕГИСТРОЧУВСТВИТЕЛЬНЫМ (PATCH).** Владелец
  поймал: `python -m tools.preflight` падает на Linux/CI (`exit 1`, битая ссылка на заглавный
  `GLOSSARY.md` в `docs/WATCHLOG.md:1012`), на macOS проходит чисто. Подтверждено вручную
  (заглавный путь резолвится через `os.path.exists()` в `True` на APFS, а `os.listdir('docs')`
  содержит только строчный `glossary.md`) — тот же класс бага, что чинился в v8.4.1
  (`CaseCollisionChecker`, физический дубль пути), только здесь коллизии на диске нет, только
  ссылка не в том регистре, и старый `LinkChecker` резолвил её через `Path.exists()`, слепой
  к регистру на macOS/Windows. **Тест сначала (red):** реальные файлы в `tmp_path` (строчный
  `glossary.md` на диске, ссылка на заглавный `GLOSSARY.md`) — `LinkChecker(tmp_path).run().ok`
  был `True` при живом баге. **Фикс:** `_exists_with_matching_case()` в
  `tools/revision/revision_check.py` — сверяет каждый сегмент пути с `os.listdir()`
  родительского каталога вместо `Path.exists()`, тест стал green. Само упоминание в §3 (запись
  v8.4.1 ниже) переписано без битого пути одним токеном, смысл сохранён. Полный
  `tests/test_repo_revision.py` — 10/10. Деталь — CHANGELOG [8.25.1].
- **[!] 2026-08-15 — v8.25.0: CRUD-ПАРИТЕТ БАТЧ 1 ЗАКРЫТ (ОБЯЗАТЕЛЬСТВО + АКТИВ).** До этого
  React-экраны `/obligations` и `/banks` были **read-only** (только кнопка retry на ошибке) —
  единственный способ создать/изменить/удалить запись оставался в Jinja. Первый из трёх
  батчей CRUD-паритета (ROADMAP §8.2, план и порядок согласованы с владельцем 2026-08-14
  в чате).
  **Бэкенд:** `crud.update_obligation`/`crud.update_liquid_asset` — partial update через
  `exclude_unset`, PUT-роуты `/obligations/{id}` и `/liquid-assets/{id}`. Схема
  `LiquidAssetUpdate` уже существовала (orphan — ни crud-функции, ни роута), `ObligationUpdate`
  добавлена новой.
  **Фронтенд:** `shared/ui/Modal.tsx` — первое применение `@radix-ui/react-dialog` (пакет стоял
  в зависимостях, не использовался — тот же случай, что `@radix-ui/react-toast` до `Toast.tsx`
  в v8.22.0). `ObligationForm`/`AssetForm` — создание+правка с валидацией обязательных полей.
  Мягкое удаление с `toast.undo` (новое действие в `toastStore.ts` — самозакрывается по клику,
  повторный клик не может дёрнуть `/restore` дважды). `Button variant="danger"` для удаления.
  **До сдачи `design-critic` (12 блокеров) и `a11y-auditor` (3 нарушения AA) нашли и починили:**
  оверлей и поверхность модалки — хардкод-цвет / тот же слой (`--c-surface-up`), что у полей
  формы внутри (лестница возвышения ломалась) → токены `--c-overlay`/`--z-overlay`/`--z-modal`/
  `--z-toast`, `--c-surface`; `0.078 * 100 === 7.800000000000001` — плавающая точка мусорила
  поле «Ставка» при правке полутора десятками знаков; `entityForm.css` дословно дублировал
  `AuthLayout.css` (CMP-01) — объединены в `shared/ui/field.css`, один источник для auth- и
  entity-форм (auth-экраны не тронуты функционально, 17/17 их тестов зелёные); `.fp-button` не
  имел состояния `:disabled` — клик «Удалить» не показывал признака принятия до тоста
  (FB-01/FB-02); крестик закрытия и кнопка «Вернуть» — тач-таргет заметно меньше 44 (A11Y-09);
  фокус после исчезновения удалённой строки проваливался в `<body>` (WCAG 2.4.3) — перенесён на
  кнопку «Добавить»; успешное восстановление не объявлялось скринридеру (WCAG 4.1.3) — добавлен
  тост; ghost-кнопка «Удалить» красилась в акцентный зелёный на hover, неотличима от «Изменить»
  (CMP-02); остаток долга не показывался в строке списка, хотя форма делает его первым
  обязательным полем — единственная проверка была открыть форму заново.
  Vitest 187/187 (было 175 до PATCH v8.24.1). tsc/eslint/prettier/build чисты. Проверено живьём
  в браузере (Chrome DevTools MCP): полный цикл создание→правка→удаление→отмена на обеих
  сущностях, обе темы. README обновлён (версия/счётчики тестов/статус вехи 8 не менялись с
  v8.9.1 — устарел содержательно). Детали — CHANGELOG [8.25.0].
- **[!] 2026-08-14 — v8.24.1: подготовительный патч перед CRUD-паритетом (PATCH).** Гейт
  согласия на `budgets_router` (был единственным без него), мягкое удаление + `restore` для
  бюджета (миграция `0034`), `extractErrorMessage.ts` разбирает объектный 403 гейта согласия.
  Матрица SQLite+PostgreSQL — 43/43 на PG. Полный pytest 1593/1593. Детали — CHANGELOG [8.24.1].
- **[!] 2026-08-14 — v8.24.0: §8.4 «УЛУЧШИТЬ UI ПРОГНОЗИРОВАНИЯ» ЗАКРЫТО (ГОРИЗОНТ + СТАВКА
  «ЧТО ЕСЛИ»).** Объём согласован владельцем через `AskUserQuestion` заранее: горизонт прогноза
  + сценарная ставка капитализации `r_bench` («что если моя доходность X%»). Явно НЕ отдано
  пользователю — параметры самого движка (α/β Holt-сглаживания, σ Monte-Carlo, ширина
  доверительного интервала 80%) — канон-заморожены, прошли раунды сертификации, обычный
  пользователь не понимает их смысл, и это уже был бы риск для калибровки, а не UI-полировка.
  **Бэкенд:** `ForecastRequest.r_bench` (`app/api/routes_planning.py`) — тот же паттерн
  override, что уже был у `PlanningRequest.r_bench` в `/plan`: явный из запроса → реальная OCR.
  Ответ дополнен `r_bench`/`r_bench_source`/`real_r_bench` — последнее считается ВСЕГДА (не
  только без override) — иначе после override фронту неоткуда взять настоящую ставку для
  кнопки «сбросить» (реальный баг, пойман при живой проверке в браузере уже ПОСЛЕ первого
  прохода гейтов — `r_bench` в ответе эхо'ит именно применённую ставку, одного поля мало).
  **Фронтенд:** `frontend/src/widgets/forecast-panel/ForecastControls.tsx` — select горизонта
  (3/6/12/24 мес.) + ползунок ставки (0–30%, шаг 0.5%, дебаунс 500 мс перед реальным
  пересчётом на бэкенде, не на каждый пиксель протяжки) с кнопкой «Сбросить к реальной
  ставке». Управляемый компонент — состояние в `PlanningPage` (тот же владелец, что
  `useForecast`). `useForecast(horizon, rBench)` — `placeholderData: keepPreviousData`, график
  не схлопывается в пустоту на каждый пересчёт. Проверено живьём в браузере (Chrome DevTools
  MCP): загрузка демо-кейса anna, смена горизонта 12→24 мес пересчитала таблицу/график
  корректно, ползунок ставки 14%→25% поднял медиану к 24 мес с 813 519 ₽ до 1 510 534 ₽ и
  показал кнопку сброса с ПРАВИЛЬНОЙ реальной ставкой (14,0%, не 25%), сброс вернул исходное
  значение, обе темы визуально проверены.
  **До сдачи `design-critic` (4 блокера) и `a11y-auditor` (1 serious + 1 moderate) нашли и
  починили:** сырой JS-`{pct}%` вместо `formatPercent` — точка вместо канонической запятой в
  деньгах (CMP-04, правило 6 CLAUDE.md), тест не ловил (проверял только целые проценты, добавлен
  regression-тест на дробный шаг); `isFetching` доходил только до скринридера — зрячий
  пользователь 500 мс дебаунса плюс раунд-трип Monte-Carlo не видел вообще ничего (FB-01/ST-02);
  `flex:1` без `flex-basis` не давал полю ползунка перенестись на свою строку на узких экранах
  (CMP-07) — новый токен `--forecast-slider-min-width`; сценарий «что если» не был помечен в
  самом графике, единственный признак — кнопка «Сбросить» в другом месте экрана (правило 7 —
  условная информация не держится на одном слабом признаке); accent-цвет ползунка совпадал с
  акцентом медианной линии графика (правило 4 брифа) — заменён на `--c-blue`; фокус после
  исчезновения кнопки «Сбросить» улетал в `<body>` (WCAG 2.4.3 по духу) — перенесён на сам
  ползунок; `<input type="range">` не имел `:focus-visible` в отличие от соседнего `<select>`;
  живая область объявляла только факт «идёт пересчёт», не готовый результат — добавлен
  `ForecastResultAnnouncer` (дочерний компонент с хуками — `ForecastPanel` сам остаётся чистой
  функцией без хуков, `ForecastPanel.test.tsx` вызывает её напрямую как функцию, хуки внутри
  сломали бы этот приём).
  **Побочно найдено и починено:** `docs/api/openapi.json` был protuhшим — снимок не
  регенерировался с момента частичного сноса Jinja (v8.23.0), пин `openapi_paths` в
  `revision_check.py` держал 114 вместо фактических 111 (тот же класс гейта-с-ложной-
  уверенностью, что уже ловился раньше на этом же счётчике) — пересобран, пин обновлён.
  `tools/revision/revision_check.py` — CJK-канарейка ловила саму себя на минифицированном
  KaTeX в `frontend/dist/` (сборочный артефакт, не исходник, `SKIP_DIRS` не знал про `dist`).
  Vitest 175/175 (было 172), tsc/eslint/prettier/build чисты, полный pytest 1587/1588
  (0 failures, 0 errors, 1 skipped, XML), `preflight`/`revision_check` — CLEAN.
  Детали — CHANGELOG [8.24.0].
- **[!] 2026-08-14 — v8.23.0: ЧАСТИЧНЫЙ СНОС JINJA-ФРОНТА ПЕРЕД Э6 (ПОСЛЕ САМОКОРРЕКЦИИ).**
  Владелец: «снеси Jinja-фронт... оставь только API» перед Э6. Разведка нашла, что буквальное
  исполнение снесло бы `legal/*`/`contacts` — единственные на сайте страницы политики/оферты/
  согласия/реквизитов (React-замены нет), риск для 152-ФЗ-поверхности. Владелец выбрал Вариант Б
  (частичный снос) через `AskUserQuestion`, зафиксирован в
  `docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`.
  **Ошибка первого прохода (поймана в этом же батче, без ухода в новую сессию):** снесены не
  только `profile`/`forgot-password`/`reset-password`, но и `dashboard`/`planning`/
  `transactions`/`obligations`/`goals`/`banks` — критерий «в React есть страница» не проверял
  «React-версия умеет то же самое». `grep useMutation` по `frontend/src/entities|pages` для
  `obligations`/`goals`/`transactions` дал ноль совпадений — эти React-экраны **read-only**
  (список, кнопка `refetch()` в error-состоянии, ни одной формы). Jinja-шаблоны несли реальные
  формы: `obligation-form`, `goal-form`, три формы в `transactions.html`, `asset-form` в
  `banks.html`, `budget-form` в `dashboard.html`. Снос убрал бы с продукта единственный способ
  добавить операцию/обязательство/цель/актив/бюджет. Это уже был задокументированный факт
  (CHANGELOG [8.6.0]: «Формы создания/редактирования — отдельный охват, не в этой партии») —
  разведка перед сносом не связала его с последствиями удаления Jinja.
  **Исправлено:** все шесть восстановлены (`git checkout HEAD --` на файлы, роуты вручную
  возвращены в `app/main.py`), вместе с `tests/test_api_pages.py`, `tests/test_a11y_mechanical.py`,
  `tests/full/test_a11y_axe.py`, `tests/full/test_visual_regression.py` (`PAGES`-списки — назад
  к исходному виду минус только реально снесённые 3 пути). `tests/test_frontend_static.py`
  тоже восстановлен целиком (был удалён по ошибке первого прохода).
  **Фактически снесено:** `profile.html`/`forgot_password.html`/`reset_password.html` + 3 роута
  в `app/main.py` (функциональный паритет с React подтверждён — `profile` read-only и там, и там,
  auth-цикл проверен живьём в v8.22.0) + мёртвый `templates/index.html`.
  **Побочная находка (владелец явно попросил проверить «всё ли ок»):** nginx
  `finpilot.conf.template` уже с v8.9.0 (первый прод-деплой) перехватывает
  `planning|transactions|obligations|goals|banks|profile` SPA-блоком раньше FastAPI — Jinja-формы
  создания для этих шести путей и так недостижимы обычным доменным трафиком в проде, только через
  `127.0.0.1:8000` в обход nginx. Проверено: это не новый баг, а уже задокументированное с
  v8.6.0 сознательное решение («read-only, не CRUD, отдельный охват»), продукт ещё не запущен
  публично (осень 2026, `CLAUDE.md`), реальных пользователей сейчас не задевает. ROADMAP §8.2
  («ГЛАВНОЕ — вынести на фронт всё, что висит только в API») дополнен явной припиской про
  CRUD-паритет для этих шести экранов — решение по срокам и приоритету за владельцем, не сделано
  молча в рамках этого батча.
  Полный разбор (обе группы остатка, условия снятия каждой) —
  `docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`. Детали — CHANGELOG [8.23.0].
- **[!] 2026-08-14 — v8.22.0: ЭКРАНЫ ВХОДА/РЕГИСТРАЦИИ С НУЛЯ (ROADMAP §8.4).** Владелец
  выбрал первым quick-win «Красивые системные сообщения — регистрация, ошибки, подтверждения».
  Разведка: в React SPA не было НИ ОДНОГО экрана входа/регистрации (только чистый JSON API,
  `app/api/routes_auth.py` — полностью реализован и покрыт тестами) и **ни одной формы вообще**
  (`<form>`/`useForm`/`onSubmit` — ноль совпадений во всём `frontend/src`).
  **Как выпало из плана (разбор для владельца, задан вопросом «может ещё что-то упущено?»):**
  План Э4 (`frontend_milestone8_plan.md`) сознательно отложил всё, кроме дашборда/плана/
  онбординга, на Jinja2 «до вехи 9» — резерв на случай отставания. Ошибочное допущение — что
  вход был отдельной Jinja-страницей, как остальные. По факту это была JS-модалка в общем
  `base.html`: когда семь экранов перенесли на React, `base.html` остался только у нетронутых
  легаси-страниц, которые nginx больше не показывает публично — модалка физически в коде, но
  недостижима с сайта. `forgot-password`/`reset-password` пережили перенос как настоящие
  отдельные Jinja-страницы и реально работали (`curl` подтвердил 200) — этот батч сознательно
  заменяет и их React-версиями для консистентности (решение владельца, спрошено явно).
  **Реализовано:** `frontend/src/pages/auth/` (`LoginPage`/`RegisterPage`/
  `ForgotPasswordPage`/`ResetPasswordPage` + `AuthLayout`) — соответствие доменам FRM/FB
  (`docs/ui_ux_design_standard.md`): видимые постоянные `label`, инлайн-ошибка текстом,
  `disabled`+`aria-busy` на сабмите, поля не сбрасываются при ошибке (кроме пароля —
  стандартная практика), корректные `type`/`autoComplete`. `entities/auth` — мутации поверх
  сгенерированного SDK; «кто я» переиспользует уже существующий `entities/profile::useProfile`
  (найдена и убрана почти-дублировавшаяся параллельная реализация до коммита).
  `shared/ui/Toast.tsx` — первое реальное применение `@radix-ui/react-toast` (стоял в
  package.json, нигде не использовался). `AuthTopbarLink` — единственная входная точка в новые
  экраны из интерфейса. nginx (`finpilot.conf.template`) — 4 auth-пути добавлены в SPA-блок.
  **Реальный баг, пойман вручную в браузере (не в юнит-тестах):** после `logout()` топбар
  продолжал показывать email/«Выйти» — TanStack Query держит последние успешные `data`, даже
  когда рефетч `/auth/me` упал на 401 (stale-if-error, `data`/`error` не взаимоисключающие).
  Компонент проверял только `if (data)`. Испытан полный цикл живьём: регистрация → редирект на
  `/` → **логаут не переключал топбар** → пойман → исправлено на `if (data && !error)` → вход
  → «забыли пароль» (dev без SMTP отдаёт `reset_url` прямо в ответе API) → сброс по реальному
  токену → вход новым паролем — весь цикл подтверждён после фикса. Регресс-тест добавлен.
  Vitest 166/166 (было 131), tsc/eslint/prettier/build чисты. Визуально проверено в обеих
  темах. Детали — CHANGELOG [8.22.0].
- **[!] 2026-08-13 — v8.21.0: ШКАЛА С ЗОНАМИ + ДИСКЛЕЙМЕР 601-ФЗ — ВСЕ 6 ПУНКТОВ ПЛАНА Э5
  ТЕХНИЧЕСКИ ЗАКРЫТЫ.** Последний буквальный пункт плана Э5 (`frontend_milestone8_plan.md`):
  «Месяцы автономии и ПДН — шкала с зонами. При ПДН выше 50% на экране обязателен спокойный
  дисклеймер о рисках (601-ФЗ)».
  **Реализовано:** `frontend/src/widgets/metrics-grid/ZoneScale.tsx` — переиспользуемая
  горизонтальная шкала с цветными зонами (`muted`/`warn`/`danger`) и маркером текущего
  значения; чисто декоративная (`aria-hidden`) — число уже доступно текстом рядом (крупное
  значение + бейдж в `MetricCard`), не дублируется в дереве доступности другими словами.
  Значение вне диапазона `[0, max]` не ломает вёрстку — маркер прижимается к ближнему краю.
  Карточка «Ликвидность» — шкала 0–6 мес., зоны `warn` (<1 мес.)/`muted` (≥1 мес.) — только
  эти два варианта, канон помечает Lt мягким критерием (никогда `danger`). Карточка «ПДН» —
  шкала 0–60%, зоны на тех же порогах, что уже красят бейдж/число
  (`DTI_WARN_THRESHOLD`=0.35/`DTI_THRESHOLD`=0.40).
  **Дисклеймер 601-ФЗ:** новая константа `DTI_LEGAL_DISCLAIMER_THRESHOLD = 0.5`
  (`entities/plan-summary/model/dtiThresholds.ts`) — юридический порог (Федеральный закон от
  29.12.2022 № 601-ФЗ, кредитор обязан письменно уведомлять о рисках при ПДН > 50%), НЕ
  модельный (отдельно от `DTI_THRESHOLD`=0.40, жёсткого инварианта канона). При
  `indicators.Dt > 0.5` в карточке ПДН — спокойный текстовый блок, не алармистский, не
  дублирует бейдж «превышен порог 40%».
  **Попутно (визуальная проверка в браузере):** зона `muted` красилась `--c-bg2`, которая в
  светлой теме почти сливается с белым фоном карточки (`--c-surface`) — зона переставала
  быть видимой зоной; заменено на `--c-border`.
  Vitest 131/131 (было 122), tsc/eslint/prettier/build чисты. Визуально проверено в браузере
  (демо-кейс «pavel», Dt=82,2% — оба порога сработали разом, обе темы).
  **Все шесть пунктов плана Э5 теперь технически закрыты** (составной столбец+ползунки, 66
  альтернатив/top3, лавина долгов, Монте-Карло веерная область, шкала автономии/ПДН, формулы
  KaTeX) — но формально Э5 не закрыт: в ROADMAP §8.4 остаются 3 пункта полировки вне
  буквального плана Э5 («Красивые виджеты/системные сообщения», «Улучшить UI
  прогнозирования», «PDF-отчёт»). Решение владельца нужно перед Э6 (юр-контур — другой
  домен, требует внешней проверки живым юристом). Детали — CHANGELOG [8.21.0].
- **[!] 2026-08-13 — v8.20.1: ПЕРЕМЕННЫЕ МОДЕЛИ ВЕЗДЕ ЧЕРЕЗ KATEX, ПРЯМАЯ ПОПРАВКА
  ВЛАДЕЛЬЦА.** После v8.20.0 владелец показал скриншот «Месяцев жизни на свободном резерве
  (stock-based, B_liq/E)» — сырая переменная в `MetricsGrid` выглядит дёшево. Первый проход
  ошибочно решил задачу СТИРАНИЕМ нотации (`B_liq` → просто «Свободный резерв», «Rt»/«Lt» в
  `AlternativesBrowser` → без букв, легенда критериев в `UtilityFormulaBody` → «Порядок
  слагаемых — …» без символов). Владелец резко поправил: «нахера ты их просто тупо убираешь?
  я тебя просил сделать их КРАСИВЫМИ» — CMP-05 запрещает НЕОБЪЯСНЁННУЮ формульную нотацию, не
  любое упоминание переменной модели; правильный путь уже был показан в v8.20.0
  (`UtilityFormulaBody` — символьная формула через KaTeX) — оставалось применить тот же
  подход везде, а не удалять.
  **Исправлено:** новый переиспользуемый `frontend/src/shared/ui/Formula.tsx` +
  `FormulaBody.tsx` — единая точка входа для формул модели где угодно в приложении; `katex`
  (~90 КБ gzip) грузится ОДИН общий ленивый чанк (`React.lazy`/`Suspense`) при первом
  использовании, не раздувает бандл страниц без клика-раскрытия (`MetricsGrid`/
  `ForecastPanel` рендерятся сразу, без тоггла — статический импорт вернул бы регрессию
  размера чанка, найденную и починенную для `AllocationPanel` в v8.20.0).
  `MetricsGrid` — карточки получили формулы под подписью (`Lₜ = Bliq/Σe`, `Dₜ = ΣP/I`,
  `BLR = (Bₜ+Bₜliq)/Σe`, канон `docs/math_model.md` §3/§3.5); `MetricCard.caption` расширен
  до `ReactNode`. `PlanningPage` — «Свободный резерв (Bliq)». `AlternativesBrowser` —
  метаданные всех 66 строк («Rₜ 119 880 ₽», «Lₜ 2,0 мес.») через KaTeX; подписи сортировки —
  юникодный подстрочный индекс (Rₜ/Lₜ) — единственное настоящее исключение: нативный
  `<select><option>` не рендерит HTML/React вообще, только текстовый узел, там символ через
  Unicode U+209C — максимум типографики, доступный в этом контексте, не лень.
  `UtilityFormulaBody` — легенда переписана на KaTeX-символ (Rₜ/Lₜ/Dₜ/Sᵢ) + человеческое
  слово, той же типографики, что формула над ней.
  **Попутно (независимая находка, не из этого запроса):** тултип `ForecastPanel` рисовал
  пустые строки для служебных `Rt_band`/`Rt_p10` («Rt_band :», «Rt_p10 :» голыми dataKey) —
  прежний `formatter` умел спрятать только значение, не саму строку; `ForecastTooltip`
  переписан на кастомный `content`, показывает ровно одну строку-медиану с `Rₜ` через
  `Formula`.
  Vitest 122/122 (было 113), tsc/eslint/prettier/build чисты, размер чанков стабилен
  (`katex` общий 257.89 КБ/77.4 КБ gzip, `forecast-panel` не раздут). Визуально проверено в
  браузере. Сохранено в память ([[feedback_finpilot_formula_variables_katex]]) — устойчивое
  правило на будущее, не разовая правка. Детали — CHANGELOG [8.20.1].
- **[!] 2026-08-13 — v8.20.0: ФОРМУЛЫ KATEX ПО КЛИКУ — ЗАКРЫТА ПОСЛЕДНЯЯ НАХОДКА CMP-05.**
  План вехи 8, Э5: «Формулы модели — KaTeX, раскрываются по клику». Закрывает находку
  design-critic v8.10.0 (CMP-05 — «никакой формульной нотации в UI»): лede `AllocationPanel`
  показывал сырую «Рекомендация СППР (…), полезность U = 0,73.», тот же паттерн жил в
  `AlternativesBrowser` («U = {u}» на каждой из 66 строк «Показать все»).
  **Реализовано:** `UtilityFormula.tsx` — раскрываемая по клику формула в блоке объяснения
  `AllocationPanel` (после контрфакта, только у рекомендации): символьная SAW-свёртка из
  канона (`docs/math_model.md` §8, `U(a) = w_R R̂n(a) + w_L L̂n(a) + w_D(1-D̂n(a)) + w_S Ŝn(a)`)
  + та же формула с реальными числами альтернативы (`weighted_scores` — считается для КАЖДОЙ
  альтернативы безусловно в `rank_alternatives`, не только top3) + человеческая расшифровка
  Rt/Lt/Dt/Si. Скрыта по умолчанию — «почему» несёт insight/gains/costs рядом (человеческий
  язык), формула — техническая деталь по запросу. `AlternativesBrowser` — «U = {u}» → «Оценка
  {u}» (без полной формулы на 66 строках — непропорционально тяжело ради второстепенного
  экрана).
  **Побочная находка и починка (перформанс, не в исходном плане задачи):** первая версия
  статически импортировала `katex` (~90 КБ gzip) сверху `UtilityFormula.tsx` — библиотека
  утекла в общий чанк `AllocationPanel`, используемый и на `/dashboard`, и на `/planning`,
  раздув его на +260 КБ (391.99 КБ → 652.12 КБ) для КАЖДОГО визита обеих страниц ради
  опциональной детали по клику меньшинства; `vite build` поймал предупреждение о чанке
  > 500 КБ. Починено вынесением тела формулы в `UtilityFormulaBody.tsx`, подгружаемый через
  `React.lazy` только по первому клику «Показать формулу оценки» — `forecast-panel` вернулся
  к прежним ~393 КБ, `katex` (259 КБ/78 КБ gzip) в собственном ленивом чанке. Тесты
  переписаны под асинхронную загрузку (`findBy`/`waitFor`).
  Vitest 113/113 (было 104), `tsc --noEmit`/eslint/prettier/`npm run build` чисты (без
  предупреждения о чанке). Визуально проверено в браузере (демо-кейс «ekaterina», обе темы):
  формула раскрывается корректно, «Оценка 0,55» на строках `AlternativesBrowser`. Детали —
  CHANGELOG [8.20.0].
- **[!] 2026-08-13 — v8.19.5: DESIGN-CRITIC/A11Y-AUDITOR — БЛОК ОБЪЯСНЕНИЯ ПЛАНА ЗАКРЫТ.**
  Единственный кусок Э5, сданный скелетом без обычного цикла проверки (v8.14.0), наконец
  прошёл design-critic + a11y-auditor. Оба агента отработали параллельно по блоку
  `fp-alloc-explanation` в `AllocationPanel.tsx`/`.css` (167–193/36–75 в старой нумерации).
  **Найдено и починено:**
  (1) **Деньги в прозе не по канону** — `app/core/recommendation.py` собирал суммы как
  `f"{x:,.0f} ₽"` (обычная запятая-разделитель, обычный пробел перед знаком рубля) во всех
  22 местах `explain_alternative`/`build_recommendation_text`, а легенда той же панели
  печатает те же числа через `formatMoney` (NBSP-канон) — пользователь видел `36,000 ₽` и
  `36 000,00 ₽` рядом для одного числа. Добавлены `_ru_thousands`/`_ru_money`, все вхождения
  переведены; TDD — 4 новых теста в `tests/test_recommendation.py`.
  (2) **«На досрочку кредитов»** — разговорное сокращение (CMP-03 явно запрещает) →
  «на досрочное погашение кредитов».
  (3) **Иерархия цвета внутри блока перевёрнута** — инсайт (главный вывод) был `--c-text2`
  (приглушённый), списки gains/costs (детали) — первичный унаследованный цвет. Поменяно
  местами; инсайт заодно получил `--fs-base` (TOK-03, тело ≥16px).
  (4) **Списки gains/costs различались только цветом и CSS `::before`-глифом** (`+`/`−`) —
  ненадёжно для скринридера (WCAG 1.4.1/1.3.1), неочевидно и зрячему. Добавлены видимые
  текстовые подписи «Что улучшается»/«Чем приходится жертвовать».
  (5) **Зелёный маркер `+` конфликтовал с `--c-green`≡«Цели»** в той же панели (столбец,
  легенда, accent-color ползунка) — переведён на нейтральный `--c-text3`, как у costs.
  (6) **UA-маргины браузера не сброшены** — `<p>`/`<ul>` несли собственные отступы, `<ul>`
  не имел `padding: 0` (список уезжал на ~40px вправо) — заявленный `gap`-ритм был фикцией.
  Добавлены явные сбросы.
  (7) **Не было `max-width`** — самый длинный текст экрана растягивался на 150+ знаков в
  строке (бриф §1.3 — норма 60–75); добавлен `max-width: 68ch`, как у `.fp-lede` рядом.
  (8) **Ритм разделителя не совпадал с соседним `.fp-whatif`** (`--sp-4` vs `--sp-5`) —
  приведено к общему `--sp-5`.
  (9) **Блок полностью размонтировался при отходе от рекомендации ползунком** — под ним
  стоят ползунки WhatIfSliders, схлопывание 150–250px верстки прыгало прямо под курсором/
  пальцем при перетаскивании; для AT пропажа контента не объявлялась (WCAG 4.1.3). Блок
  больше не исчезает — контент переключается на короткую заглушку, смена состояния
  объявляется debounced `role="status"` живой областью (паттерн `WhatIfSliders.tsx`,
  400мс — задержка только в озвучке).
  (10) Добавлен `<h3>` («Почему выбран такой план») и `role="list"` на `<ul>` (Safari/
  VoiceOver могут снимать семантику списка при `list-style: none`).
  **Визуально проверено в браузере впервые для этого блока** — демо-кейс «ekaterina»
  (`POST /api/demo/load`), обе темы (`data-theme`), плюс живая проверка переключения
  ползунка «что если» (заглушка + живая область подтверждены в DOM). По ходу перезапущен
  локальный uvicorn (старый процесс с субботы держал код до правок — пришлось убить и
  поднять заново, иначе curl бил в устаревший код). `pytest`/Vitest 104/104/`tsc`/eslint/
  prettier/build — все чисты. Детали — CHANGELOG [8.19.5].
- **[!] 2026-08-13 — v8.19.4: РЕАЛЬНЫЙ PUSH ЗЕРКАЛА — РЕЛИЗ БЕЗ ОПИСАНИЯ, ПОЧИНЕНО.**
  Владелец реально запустил `ONLY=finpilot zsh ~/Downloads/deploy.sh` на архиве из
  v8.19.3 (тогда ещё `finpilot-mirror-v8.19.2.zip`). Дерево и тег `v8.19.2` ушли на
  GitHub чисто (общий деплойер уже верно развёл архив по `REPO_MAP` в репозиторий
  `finpilot`, не в приватный) — но **релиз не создался**: `deploy.sh`'s
  `find_changelog()` ищет `CHANGELOG.md` в корне дерева, а зеркало его намеренно не
  публикует (300+ КБ внутренней кухни приватного репо). Найдено и починено:
  (1) `sanitize_tree()` в `finpilot_publish_public.sh` теперь САМ пишет `CHANGELOG.md`
  в собранное дерево — из той же секции `tools/publish/public_release_notes.md`
  (privacy-reviewed `guard_notes`), что уже готовила `extract_public_notes()` для тела
  релиза, один источник текста, не два. Закрывает класс «релиз без описания» на
  будущее, не только для этой версии.
  (2) Владелец явно поправил: репозиторий на GitHub остаётся называться просто
  `finpilot` (переименование в `finpilot-public-mirror` и обратно проверено вживую в
  этой же сессии — `gh repo rename`, дважды), а имя АРХИВА — `finpilot-public-mirror-
  vX.Y.Z.zip` (понятнее на диске, чем промежуточное `finpilot-mirror-`, прожившее
  несколько часов той же сессии). `docs/archive_naming_standard.md` обновлён, заодно
  поправлен `WATCHLOG_personal-finance-dss-*` → `WATCHLOG-personal-finance-dss-*`
  (дефис — тот же канон).
  (3) `docs/public_mirror_state.md` §7 переписан: раньше предписывал публиковать
  зеркало прямым `push`/`release` самого санитайзера — реальная практика (решение
  владельца) — через общий `deploy.sh` с `ONLY=finpilot`, задокументирован живой
  порядок команд.
  (4) `~/Downloads/deploy.sh` (вне репозитория): `REPO_MAP` — запись
  `finpilot-public-mirror=finpilot` (старая `finpilot-mirror=finpilot` оставлена для
  совместимости с уже лежащим в Downloads архивом старого имени); `find_changelog()`
  получил общий (не только для finpilot) фоллбэк на `RELEASES.md`, когда
  `CHANGELOG.md` нет вовсе — защита на будущее независимо от починки (1).
  Пересобран `finpilot-public-mirror-v8.19.3.zip` (535 файлов, 5.9 МБ, guard пройден
  начисто) — ждёт повторного `ONLY=finpilot zsh ~/Downloads/deploy.sh` от владельца.
  `pytest -q` полный набор зелёный, preflight чист. Детали — CHANGELOG [8.19.4].
- **[!] 2026-08-13 — v8.19.3: ПЕРВАЯ РЕАЛЬНАЯ СБОРКА ПУБЛИЧНОГО ЗЕРКАЛА.** Владелец
  попросил заодно с батчем Decimal собрать архив для публичного зеркала в `~/Downloads`.
  Дошли до `tools/publish/finpilot_publish_public.sh check` — первый реальный прогон с
  тех пор, как в allow-list вехи 8 попал текущий React-фронтенд (`frontend/`). Нашлось
  четыре реальные проблемы, все починены до чистого guard (разбор —
  `docs/public_mirror_state.md` §5а):
  (1) **`frontend/node_modules/` (268 МБ) уехал бы в зеркало целиком** — `RSYNC_EXCLUDES`
  санитайзера исключал только Python-кэши, ни одного JS-паттерна не было вовсе. Добавлены
  исключения 1:1 с `frontend/.gitignore` (node_modules, dist, playwright-report,
  test-results, .tanstack и т.д.).
  (2) **GUARD 2/3 падал с `argument list too long`** вместо диагностики — `hit="$(grep …)"`
  + `echo "$hit"` не пережили объём совпадений на node_modules (ложное срабатывание
  секрет-паттерна AKIA внутри исходника `prettier`, не секрет). Переписан на прямой pipe.
  (3) **`run_guard()` на `build`/`push` ловил собственный `.git/`** — reflog git-клона
  `$STAGING_DIR` несёт имя git-committer'а машины владельца, guard падал бы на КАЖДОЙ
  реальной сборке. Все три guard'а получили `--exclude-dir=.git`.
  (4) **Реальное имя владельца в синтетических тестовых фикстурах** (не в самом
  зеркале — в приватных тестах, которые копируются в `tests/` allow-list как есть):
  `tests/test_crisis.py::TestVasiliiCreditCardCase` → `TestHighInterestCreditCardCase`,
  плюс 5 мест в `test_statement_parser_real_formats.py`/`test_pdf_parsers.py`/
  `test_consents.py` — синтетические строки банковских выписок использовали настоящее
  имя владельца как заполнитель для реалистичности. Найдено РУЧНЫМ сквозным грепом по
  всему allow-list дереву (не только тем, что guard показал за один прогон) — владелец
  явно потребовал дойти до конца, не ограничиваться автоматическим гейтом.
  Попутно починены две нестыковки в доках: канон имени архива зеркала —
  `finpilot-mirror-vX.Y.Z.zip` (не голое `finpilot-`, которое без `.repo-id` в дереве
  зеркала увело бы архив в ПРИВАТНЫЙ репозиторий через `REPO_MAP` общего `deploy.sh`);
  и в `~/Downloads/deploy.sh` (вне этого репозитория) — добавлена запись `REPO_MAP
  finpilot-mirror=finpilot`, без которой архив завёл бы пустую репу-сироту. Итог:
  `finpilot-mirror-v8.19.2.zip` собран, guard пройден начисто (534 файла, 5.9 МБ),
  лежит в `~/Downloads` — публикация (`push`) НЕ выполнена, решение о сроках за
  владельцем. `pytest -q` полный набор зелёный, preflight чист. Детали — CHANGELOG
  [8.19.3].
- **[!] 2026-08-13 — v8.19.2: DECIMAL В ДЕНЕЖНОМ ЯДРЕ, УЗКИЙ ОБЪЁМ.** Владелец на прямой
  вопрос про Decimal (после разбора рисков перехода) ответил «не знаю, сделай как считаешь
  правильным для продукта такого уровня» — решение по срокам, которое `model_completion_
  plan.md` §2.5 три сессии подряд оставлял за владельцем, принято. Оценка «2 батча плюс
  регресс всех golden» предполагала перевод ВСЕГО `app/core` на Decimal без дисциплины
  границ. Три параллельных Explore-агента (`docs/research/raw/08–10_decimal_*_2026-08-13.md`)
  проверили предположение по факту кода: (1) БД уже на Decimal — `Numeric(14,2)` →
  SQLAlchemy отдаёт `Decimal` и на SQLite, и на PostgreSQL, `crud.py` уже нормализует через
  `to_money()`; (2) `app/ingestion/engine.py` уже реализует нужный паттерн границы
  (Decimal→float-ядро→Decimal); (3) API обязан остаться `float` — живой тест FastAPI
  подтвердил, что `response_model` сериализует `Decimal` в JSON-СТРОКУ, не число, что сломало
  бы `api-contract-guard` и фронт; (4) внутри `app/core` денежная арифметика везде одношаговая
  (уже округляется на выходе через `money()`), КРОМЕ `amortization.py::_simulate` —
  единственного накопительного цикла (до 360 месяцев подряд) и единственного места, где
  пользователь сверяет цифру построчно с реальной банковской выпиской (график погашения
  долга, ADR-016). **Периметр батча — только этот цикл**, 1 из изначально оценённых 2 батчей.
  Попутно нашёлся и починён реальный баг: три `round(x, 2)` в `build_debt_amortization_
  schedule` использовали банковское округление (ROUND_HALF_EVEN) stdlib вместо канона проекта
  (ROUND_HALF_UP, `app/core/money.py`) — воспроизведён конкретный случай (`amount=100,
  interest_rate=0.015` → проценты `0.125₽` ровно → было `0.12₽`, стало `0.13₽`). Владелец
  усомнился, объективно ли ROUND_HALF_UP правильно — веб-ресёрч подтвердил: не произвол, п. 6
  ст. 52 НК РФ использует ту же логику для рублей, консьюмер-факинг построчные суммы (эта
  ситуация) конвенционально round-half-up, round-half-even оправдан для статистических
  агрегатов на больших объёмах (IEEE754/IRS), не для сверяемой пользователем строки —
  `docs/research/raw/11_rounding_convention_verification_2026-08-13.md`. Остальной денежный
  контур (`alternatives/crisis/surplus/metrics/...`, `avalanche.py`, `forecast.py`,
  `spending_advice.py`, компаунд-проекции) осознанно остался на float навсегда, не «пока» —
  причины в CHANGELOG [8.19.2]. TDD: red-тест на точное копеечное соответствие → green.
  1602 теста (было 1601), 0 упавших, `test_engine_golden.py` прошёл БЕЗ изменений в файле
  теста — решающий контур (a*/top3/utility) не задет по конструкции. flake8/mypy/preflight
  чисты. `model_completion_plan.md` §2.5/§7 пересчитаны (пункт закрыт, как 2.6). Детали —
  CHANGELOG [8.19.2].
- **[!] 2026-08-13 — v8.19.1: ФИНАЛЬНАЯ РЕВИЗИЯ МАТМОДЕЛИ — 4 НОВЫЕ НАХОДКИ, БЕЗ КОДА.**
  Владелец попросил систематически перепроверить, не упущено ли ещё что-то без экспертов/
  данных — субагент прочитал ПОЛНОСТЬЮ (не грепом) `model_completion_plan.md`,
  `model_quality_scorecard.md`, WATCHLOG §4, весь ROADMAP, `pitfalls.md`,
  «Когда пересмотреть» у ADR-013/014/015/016, `model_testing_next_iterations.md`. Нашёл:
  (1) **Волна 0 (§4 `model_completion_plan.md`)** — таблица 7 пунктов не помечена
  выполненной, хотя закрыта целиком к v8.17.0; §1/§7 пересчитаны под факт 87.18 (было 85.7
  в исходной прикидке до волны 0). (2) **Пункт 2.5 (Decimal в денежном ядре)** сидел в
  «Волне 2 — живые данные», хотя это чисто инженерная задача без блокера эксперт/данные
  (карта качества §4 сама это подтверждает) — попал туда по хронологии батчей, не по типу
  ресурса. **НЕ реализовано в этом батче** — крупный рефакторинг (регресс ВСЕХ golden-
  снапшотов), решение по срокам оставлено владельцу, не взято с ходу без подтверждения.
  (3) **`model_quality_scorecard.md`** не знала про ADR-017 — новый §17, ось «полнота
  охвата» 80→81 (реальный расчёт вычета типа А глубже общей ноты, но тип Б/ИИС-3 всё ещё
  без числа — заниженный, честный шаг, не «почти полное закрытие»), интеграл 87.18→87.23.
  (4) **`model_testing_next_iterations.md`** не редактировался с момента восстановления
  git-истории (до v8.7.0) — описывал как «предстоящее» три уже закрытых пункта (метаморфика,
  инфляция, телеметрия) и не знал про ADR-016/017; расставлены статусы, добавлена
  шапка-предупреждение со ссылкой на актуальные источники. Preflight чист, изменения — только
  документация, версия PATCH. Детали — CHANGELOG [8.19.1].
- **[!] 2026-08-13 — v8.19.0: КОМПАУНД-ИЛЛЮСТРАЦИЯ + ПОЧИНЕНА РЕГРЕССИЯ v8.18.0.** По итогам
  обсуждения идей из опроса ЦА (Госуслуги/ФНС/акции) — только компаунд-иллюстрация не требует
  эксперта/партнёрства/данных, две другие ушли в новую **ROADMAP §6.7** (Барьер другого типа —
  доступ к внешнему API/партнёрство, прецедент в коде — Plaid, дормантный до реальных
  `PLAID_CLIENT_ID`). `app/core/investment.py::project_compound_growth` —
  $FV=amount \cdot (1+r)^t$, 5%/8%/12% × 5/10/20 лет, не привязана к профилю/инструменту.
  **Попутно найдена и починена реальная регрессия v8.18.0:** `iis_deduction_estimate`
  считался в `annotate_investment_tranche`, но НЕ был объявлен в Pydantic-схеме
  `InvestmentTranche` — `response_model=PlanningCalculateResponse` тихо резал бы поле из
  настоящего API-ответа. Юнит-тесты на `run_planning()` этого не ловили (минуют
  сериализацию response_model). Добавлена схема `IISDeductionEstimate` +
  `GrowthIllustrationPoint`, новый API-тест через `TestClient`
  (`test_investment_tranche_iis_and_growth_survive_response_model`) закрывает класс
  регрессии «посчитано в коде, но не объявлено в схеме ответа» на будущее — стоит иметь в
  виду при следующих диагностических полях. Заодно написаны: бриф эксперту по
  инструментам/страхованию (`docs/model/expert_brief_investment_instruments.md`), гид по
  сбору данных после деплоя по каждому пункту H6-1/H6-2/H6-3/Волна 1/2
  (`docs/model/post_launch_data_focus.md`), починена стаза `model_completion_plan.md` (п.
  2.6 давно закрыт). Канон не бампнут — информационная правка §13, тот же класс, что нота
  ИИС в v8.14.2.
- **[!] 2026-08-13 — v8.18.0: ADR-017, РЕАЛЬНЫЙ РАСЧЁТ ВЫЧЕТА ИИС ТИПА А.** Владелец после
  ревизии матмодели спросил: «налоги ведь ещё не доделаны — что можем сдвинуть сейчас?»
  Прочитан ADR-014: отклонение варианта C смешивало два разных риска — (1) новый вход
  пользователя (тип ИИС, лимит года) и (2) пересчёт эффективной доходности, ВХОДЯЩИЙ В ВЫБОР
  альтернативы (риск ADR-008 — лимит года как пороговый критерий SAW при шаге решётки 10%).
  Только (2) был реально рискован. Решение: сделать (1) плюс численный расчёт для типа А как
  ДИАГНОСТИКУ уже выбранной альтернативы — тот же приём красной линии, что ADR-016 (лавина
  долгов): вычисляется ПОСЛЕ ранжирования, не входит в utility. `app/core/investment.py::
  estimate_iis_deduction` — `eligible = min(amount, max(0, 400000 - contributed_this_year))`,
  `deduction = 0.13 * eligible`; тип Б/ИИС-3 численного расчёта НЕ получают (переходные
  положения закона, риск неверной цифры — вариант D ADR-017, отклонён явно, не молча). Новый
  вход `user_prefs.iis_type`/`iis_contributed_this_year` (миграция 0033, SQLite+PostgreSQL
  upgrade/downgrade/upgrade чисты). Кэш-ключ плана учитывает оба поля (иначе смена статуса ИИС
  отдавала бы старую заметку). Красная линия подтверждена тестом (не только архитектурой):
  `test_iis_status_does_not_affect_decision_adr_017` — три статуса ИИС на одном портрете дают
  идентичный Rt/Dt/победителя/utility, различается только текст транша. 15 новых тестов
  (`test_investment.py`), flake8/mypy чисты (3 известных долга не в этих файлах). Канон
  v3.8.0 → **v3.9.0**. Попутно найдено и починено: README.md не обновил интеграл карты
  качества в прошлом батче (85.7 → 87.18 пересчитан в v8.16.1, забыт в «Текущем состоянии»).
  Разбор — `docs/reports/adr/adr_017_iis_deduction_estimate.md`. Осталось (сознательно не
  сейчас): тип Б/ИИС-3, полный пересчёт `r_bench` с учётом налогов — ждёт реальной телеметрии
  (когда включится, п. 0.6) или отдельного решения владельца.
- **[!] 2026-08-13 — v8.17.0: ВОЛНА 0 ЗАКРЫТА ЦЕЛИКОМ — ТЕЛЕМЕТРИЯ ПРИНЯТИЯ СОВЕТА (0.6),
  КОД ДОРМАНТЕН.** Последний из семи пунктов Волны 0. Миграция `0032_plan_advice_events`
  (таблица `plan_advice_events`, проверена upgrade/downgrade/upgrade на SQLite и изолированном
  PostgreSQL), `app/api/routes_telemetry.py` (`POST /api/telemetry/advice-events`,
  `PATCH .../{plan_id}/decision`), за гейтом `require_financial_consent` (тот же, что
  transactions/obligations/goals/liquid-assets). **Сознательно дормантен:** событие содержит
  `user_id` + хеш финансового портрета — `settings.TELEMETRY_COLLECTION_ENABLED=False` по
  умолчанию, оба эндпоинта отвечают 404 (`_ensure_telemetry_enabled`, по образцу
  `_ensure_plaid_enabled` у Plaid), пока правовой контур обезличивания (152-ФЗ, ROADMAP §8.2а,
  задача юриста) не закрыт — переключение флага в True тогда станет решением владельца в одну
  строку конфига, не батчем. 9 новых тестов (`test_api_telemetry.py`) — 404 по умолчанию,
  создание/решение события, 401/403/422 на нарушения, 404 на чужой/несуществующий `plan_id`.
  Зелёные на SQLite и PostgreSQL. Детали — CHANGELOG [8.17.0].
- **[!] 2026-08-13 — v8.16.1: ВОЛНА 0 ДОБИТА (0.5+0.7), КАРТА КАЧЕСТВА ПЕРЕСЧИТАНА.** Владелец
  спросил «точно всё сделали без экспертов/данных?» — ревизия нашла: WATCHLOG сам писал «Волна 0
  закрыта целиком (0.1+0.2+0.3/0.4)» — но у Волны 0 (`model_completion_plan.md` §4) СЕМЬ
  подпунктов, три (0.5/0.6/0.7) были пропущены при переклассификации на барьеры §6.6.
  **0.5 — кросс-платформенная сверка:** `tools/model_validation/cross_platform_check.py` —
  полный `run_planning()` (не только тесты) на 12 000 портретов, Intel Mac (x86_64) vs Linux
  (`python:3.13-slim`, образ CI) — **агрегатный sha256 идентичен побайтово**. Единственный ранее
  названный повод не ставить 100 оси «воспроизводимость» в карте качества закрыт. **0.7 —
  снимок модели:** `tools/model_validation/model_snapshot.py` — канон/конфиг/версия кода в
  `knowledge/model_validation/`, константы читаются прямо из кода (не переписаны вручную).
  **Карта качества** (`model_quality_scorecard.md` §16): полнота охвата 62→**80** (3/7 пунктов
  закрыты полностью — ADR-013/015/016, 1 частично — ADR-014, 1 снят с доски как объявленная
  граница — семейный бюджет, 2 открыты — Барьер 1), адекватность 82→**86** (инфляция закрыта),
  воспроизводимость 96→**98**. Интеграл **85.74 → 87.18**. Объяснимость (74) не тронута — ждёт
  слепой оценки. **0.6 (телеметрия) сознательно НЕ в этом батче:** событие содержит `user_id` +
  хеш финансового портрета — требует правового контура обезличивания ДО реализации (152-ФЗ,
  ROADMAP §8.2а), не докс-правка; отдельный батч со связкой миграция+эндпоинт+152fz-guard.
- **[!] 2026-08-13 — v8.16.0: ADR-016 ЗАКРЫТ — ПОЛНЫЙ ПОМЕСЯЧНЫЙ ГРАФИК ПОГАШЕНИЯ ДОЛГА.**
  Заморозка от 2026-07-02 снятая в v8.15.1 — реализована. `app/core/amortization.py::
  build_debt_amortization_schedule` — два параллельных прогона (baseline: минимальные платежи;
  накопительная лавина: `x_obl_effective` каждый месяц + каскад освободившихся минимальных
  платежей закрытых долгов) до обнуления балансов или капа `MAX_HORIZON_MONTHS=360`
  (гарантирует терминацию при `monthly_payment <= проценты`). Срок кредита (`term`/
  `start_date`) не читается — момент погашения является РЕЗУЛЬТАТОМ симуляции, не входом
  (обходит массовую незаполненность этих полей — синтетические портреты идут по этому пути на
  100%). Красная линия (не влияет на `Rt`/`Dt`/`crisis_plan`) — гарантия по конструкции кода
  (`amortization.py` не импортируется `ranking.py`/`crisis.py`), подтверждена явным тестом
  `tests/test_crisis.py::test_crisis_plan_unaffected_by_debt_schedule_adr_016`. Диагностическое
  поле `indicators.debt_schedule` (по образцу `income_cv`, ADR-015) — вычисляется один раз для
  победившей альтернативы, не на все 66 в сетке. **Стенд на 12 000 портретах:** 0
  исключений/зависаний (терминация подтверждена, включая 9 портретов с
  `negative_amortization`); график посчитан для 23.29%; `months_saved` медиана 17/p95 59;
  `interest_saved` медиана 125 056 ₽/p95 973 472 ₽; `horizon_capped`/`negative_amortization` —
  по 0.32%. 9 новых тестов `test_amortization.py` + красная линия + property-инвариант
  (`accelerated_total_interest <= baseline_total_interest`), все зелёные без изменений смежных
  (`test_avalanche.py`/property-тесты прошли без правок после рефакторинга
  `select_avalanche_targets`). Канон **v3.7.0 → v3.8.0**. Разбор —
  `docs/reports/adr/adr_016_debt_avalanche_amortization_schedule.md`, замер —
  `docs/reports/testing/debt_avalanche_benchmark.md`. **Оба пункта ROADMAP §6.6 (барьеры 2 и 3)
  теперь закрыты целиком.** Следующая задача — design-critic/a11y-auditor проход по блоку
  объяснения плана в `AllocationPanel` (v8.14.0 — только скелет, ROADMAP §8.4).
- **[!] 2026-08-13 — v8.15.1: РЕШЕНИЯ ПО ДВУМ ПУНКТАМ БАРЬЕРА 3/2 (ROADMAP §6.6).** Владелец
  разобрал оба оставшихся пункта. **Семейный бюджет — решено, без правки канона:** продукт про
  личные финансы, семейный профиль — надстройка, не замена; каждый член household считает свой
  личный план независимо тем же SAW-ядром (`ranking.py`/`planning.py` не трогаются), «семейное»
  — агрегированная витрина поверх готовых личных планов (реализация дашборда — фронтенд-задача
  отдельным батчем, не матмодель). Пункт закрыт документально (ROADMAP §6.6), кода в этом
  батче нет. **Лавина долгов/рефинансирование — заморозка от 2026-07-02 СНЯТА для этого пункта.**
  Следующий батч — ADR-016 по установленному процессу (ADR-013/014/015): полный помесячный
  график погашения (baseline «без досрочки» vs накопительная лавина), красная линия — новый
  amortization-модуль не трогает `ranking.py`/`crisis.py`, бенчмарк на 12 000 портретах перед
  сдачей. Канон **v3.7.0 → v3.8.0** (MINOR) ожидается по итогам, не в этом батче.
- **[!] 2026-08-12 — ЧАСТИЧНОЕ СНЯТИЕ ЗАМОРОЗКИ МАТМОДЕЛИ (решение владельца).** Приказ
  2026-07-02 «математика замораживается и проверяется до фронта» (порядок вех бэк → орг →
  тест → юр → фронт → деплой) **остаётся в силе как правило**, но владелец сделал явное
  исключение для трёх пунктов «Волны 0» из `docs/model/model_completion_plan.md` §4
  (дёшево поднимающие карту качества без правки решающего ядра): 0.2 (починка
  метаморфического набора тестов), 0.3/0.4 (вклад критериев SAW + контрфактное объяснение),
  0.1 (инфляционная индексация целей с горизонтом > ~3 лет, единственный из трёх — реальное
  расширение канона: меняет $\Delta_s$/$S_n$ в §11.3, потенциально влияет на argmax
  ранжирования). Разница с прецедентом отказа по `avalanche.py` (v8.8.0): там просили
  математику, которой в каноне нет вообще (месячный график амортизации) — здесь наоборот,
  расширение существующей, уже пятикратно сертифицированной формулы, с планом ADR + бенчмарк
  на портретах перед сдачей (как для floor 2.0 в ADR-006). Лавина долгов (второй пункт Э5)
  **заморозку не снимает** — остаётся отложенной по прежнему решению. По итогам — версия
  канона `docs/math_model.md` v3.5.0 → v3.6.0 (MINOR, не MAJOR: прецедент §19 — MAJOR
  зарезервирован за изменением архитектуры, как 2.x→3.0.0; добавление входной величины в
  существующую формулу — тот же класс, что G4/G5/G7/G8).
- **[!] v8.11.0 — ВЕХА 8, Э5: ПОЛЗУНКИ «ЧТО ЕСЛИ», ПЕРВЫЙ ПУНКТ Э5 ЗАКРЫТ ЦЕЛИКОМ.** Вторая
  (последняя) часть первого пункта Э5 — «составной столбец плюс ползунки «что если»» (первая
  часть, диаграмма Санкея, закрыта в v8.10.0). Ползунки «Досрочное погашение»/«Цели» (шаг 10%,
  резерв — вычисляемый остаток без своего ползунка) живут в `AllocationPanel`
  (`frontend/src/widgets/allocation-panel/WhatIfSliders.tsx`). **Ключевое архитектурное
  решение:** составной столбец, легенда и диаграмма Санкея реагируют на ТЕ ЖЕ ползунки — это
  один интерактивный артефакт (буквально то, что просит план), не статичная рекомендация сверху
  и отдельная песочница с числами снизу. Кнопка «Вернуть рекомендацию» появляется при отклонении
  от рекомендации СППР. Результат гипотезы (свободный поток/ликвидность/ПДН) ищется в уже
  посчитанном бэкендом `ranked[]` (`findMatchingAlternative.ts`, точное совпадение по сетке 10%,
  без плавающей точки) — фронт новых чисел не считает, канон математики заморожен до решения
  владельца; комбинации, не прошедшие отсев модели (например, ПДН > 40%), явно помечены
  недоступными (fail-loud), не молчат и не падают.
  **`design-critic` (2 запуска — первый упал на `UNKNOWN_CERTIFICATE_VERIFICATION_ERROR`) и
  `a11y-auditor` (тоже 2 запуска — первый упал на разрыв соединения с API) до сдачи нашли и
  починили три блокера:** (1) первая версия компонента держала столбец/легенду замороженными на
  `best`, пока ползунки показывали другие числа рядом — переписано на общее управляемое
  состояние (`debtNotch`/`goalsNotch` в `AllocationPanel`, `WhatIfSliders` стал управляемым
  презентационным компонентом); (2) `accent-color` обоих ползунков был захардкожен в
  `--c-accent`, который побайтово равен `--c-green` («Цели») — ползунок «Досрочное погашение»
  оказывался случайно зелёным, ложный сигнал рядом с легендой, где красный/жёлтый/зелёный уже
  что-то значат — теперь каждый ползунок красится цветом своей категории; (3) блок результата
  (`--c-surface-up`) был физически неотличим от фона панели в светлой теме — тот же токен
  побайтово равен `--c-surface` там (тот же класс бага, что уже чинили для
  `--c-bg2`/`--c-surface`, `tokens.css:13`) — добавлена рамка `--c-border`. Плюс: порог ПДН 0.35
  приведён к словарю бейджей `MetricsGrid` (`DTI_THRESHOLD`/`DTI_WARN_THRESHOLD` вынесены в
  `@entities/plan-summary`, общий источник для обоих мест) вместо голого текста; `aria-valuetext`
  дублировался с текстом `<label>` (скринридер зачитывал бы процент дважды на каждый шаг) —
  видимый процент вынесен в `aria-hidden` span, доступное имя — только категория; живая область
  (`role=status`) объявляла каждую из 11 отметок ползунка при автоповторе стрелки — задержана на
  400 мс отдельным `sr-only`-параграфом (тот же паттерн, что у пересортировки в
  `AlternativesBrowser`), видимый результат обновляется без задержки; добавлен `color-scheme` в
  `tokens.css` (первые в проекте нативные `<input type="range">` вскрыли, что без него браузер
  красит нативный UI по теме ОС, а не по теме сайта); тач-таргет перенесён с обёртки на сам
  `<input>`; `id` ползунков — через `useId()`. Vitest 99/99 (было 83/83 — новые
  `WhatIfSliders.test.tsx`, `findMatchingAlternative.test.ts`, `AllocationPanel.test.tsx`
  переписан под реактивную схему). `npm run build`/`eslint`/`prettier --check` чисты. Backend:
  `preflight`/`revision_check` чисты, `flake8`/`mypy` на изменённых файлах чисты; полный `pytest`
  повторно не гонялся — бэкенд-логика не менялась (только версия в `app/config.py` и запись
  allowlist в `tools/revision/revision_check.py`), последний полный прогон в этой же сессии —
  1512/1512, 0 errors (см. запись v8.10.0 ниже). Ручная проверка в браузере (chrome-devtools-mcp,
  временный dev-харнесс вне поставки): обе темы, десктоп/380px, движение ползунков в обе стороны,
  кнопка сброса, недоступная комбинация, диаграмма Санкея на текущей (не только рекомендованной)
  позиции ползунков. Детали — CHANGELOG [8.11.0].
- **[!] v8.10.0 — ВЕХА 8, Э5: ДИАГРАММА САНКЕЯ В AllocationPanel.** Первая часть первого пункта
  Э5 (`docs/frontend_milestone8_plan.md`: «Санкей — только как режим «подробно», не по
  умолчанию»); ползунки «что если» из того же буллета НЕ начаты — отдельная задача, дальше по
  Э5. Режим «подробно» — тогл-кнопка под легендой `AllocationPanel`, раскрывает recharts
  `<Sankey>` (узел «Свободные деньги» → узлы категорий с ненулевой суммой, та же логика фильтра,
  что у сегментов `fp-alloc-bar`). Не показывается при единственной активной категории (Санкей
  из одной связи не добавляет ничего к столбцу) и при дефиците (`best=null`). Дефолтные
  рендереры recharts заменены целиком — штатный `#0088fe`/`#333` не токенизируем, узлы вообще
  не подписаны. **`design-critic` + `a11y-auditor` до сдачи (обязательный проход для куска Э5,
  как и на v8.8.0) нашли реальные баги, все починены до отправки:** (1) кнопка использовала
  `aria-expanded`/`aria-controls`, указывая на строго декоративный `aria-hidden`-блок —
  раскрытие для скринридера никогда не происходило (WCAG 4.1.2, ложная disclosure-семантика),
  плюс `aria-controls` в свёрнутом состоянии ссылался на несмонтированный узел — заменено на
  `aria-pressed`; (2) якоря подписей узлов были перепутаны местами (источник подписывался
  внутрь диаграммы вправо, цели — внутрь влево), обе подписи ложились на цветные ленты, а
  зарезервированные поля margin пустовали — направление исправлено на «наружу»; (3) девять
  чисел в SVG-рендерерах не были привязаны к шкале `--sp-*` (включая `margin.left/right=104` —
  подгон «на глаз», не входит в шкалу, к тому же оказался тем самым «пустым полем» из пункта 2)
  — сведены к `--sp-*`-шкале там, где это возможно (JS-числовые пропы recharts не принимают
  `var()`, задокументировано инлайн-комментарием), радиус скругления узлов (четвёртый в
  системе помимо `--r-xs/sm/md/lg/xl/full`) убран вовсе; (4) `best.utility.toFixed(2)` — точка
  вместо запятой в числе, видимом пользователю (правило 6 CLAUDE.md), рядом же `AlternativesBrowser`
  уже делает это правильно через `formatNumber` — приведено к одному стандарту; (5) на узких
  экранах (~380px) диаграмма без ограничения ширины сплющивала подписи категорий друг на друга
  — добавлены `overflow-x: auto` на контейнер и `min-width` на внутренний блок (новый токен
  `--chart-sankey-min-width`, та же логика, что у `--chart-height-sm`: ограничение
  контента/библиотеки, не design-выбор). Подтверждено вручную в браузере (chrome-devtools-mcp,
  временный dev-харнесс вне поставки): обе темы, десктоп и 380px (диаграмма прокручивается
  горизонтально, не ломается). **Отложено осознанно, не блокирует версию:** копия
  «Рекомендация СППР… полезность U = {u}» — design-critic пометил как сырую формульную запись
  (CMP-05) и непереведённую аббревиатуру, но тот же паттерн живёт и в `AlternativesBrowser`
  («U = {u}») — точечная правка одного файла создала бы расхождение; решается штатно следующим
  пунктом Э5 («Формулы модели — KaTeX, раскрываются по клику»), не отдельным патчем копии.
  Manrope (`--font-body` в токенах) нигде не грузится в SPA ни `@font-face`, ни `<link>` —
  найдено design-critic попутно при ручной проверке, затрагивает весь SPA целиком, а не один
  компонент — заведено в ROADMAP как отдельная задача, не решается этим батчем. Vitest 83/83
  (было 75/75 — добавлены `AllocationPanel.test.tsx`, `buildAllocationSankeyData.test.ts`),
  `npm run build`/`eslint`/`prettier --check` чисты. Детали — CHANGELOG [8.10.0].
- **[!] v8.9.4 — СТАБИЛЬНОЕ ИМЯ КАНОНА (docs/math_model.md), Э5 НЕ ПРОДВИНУТ.** Структурная
  причина серии v8.9.2–v8.9.3: имя файла версионировалось (`math_model_v3_X_Y.md`), требовало
  ручного обновления ссылок при каждой рекалибровке — владелец предложил устранить класс бага,
  не чинить очередной симптом. `math_model_v3_5_0.md` → `docs/math_model.md` (стабильно,
  версия только внутри файла); `v3_0_0.md`/`v3_1_0.md` → `docs/model/history/`. Хук
  `session-context.sh` и скилл `finpilot-math-model-guard` больше не зависят от номера версии
  в имени файла. ~45 живых ссылок переведены; 3 замороженных отчёта, случайно задетых первым
  проходом bulk-замены, восстановлены как есть (не переписываются задним числом) и занесены в
  `LINK_ALLOWLIST`. Канон v3.5.0 подтверждён рантаймом сквозной трассировкой: HTTP-роут →
  `run_planning` → `effective_floor_months`/`is_toxic_debt` (G8) реально вызывается и
  передаётся в `rank_alternatives`; `build_crisis_plan` (G2) реально вызывается;
  `avalanche.py` подтверждён одномоментным без графика. `revision_check` CLEAN, `preflight`
  чисто, 31/31 + 9/9 Vitest. Детали — CHANGELOG [8.9.4].
- **[!] v8.9.3 — ДОСЛЕДСТВИЕ РЕВИЗИИ v8.9.2, Э5 НЕ ПРОДВИНУТ.** Владелец перепроверил v8.9.2 и
  нашёл три хвоста: (1) `docs/ROADMAP.md` строка 101 всё ещё называла канон v3.0.0 (ревизия
  искала имя файла, не голое упоминание версии в прозе — первоисточник исходной ошибки, чинено);
  (2) канон подтверждён ПО КОДУ, не только доками — `app/core/ranking.py::is_toxic_debt`/
  `TOXIC_FLOOR_MONTHS` с комментарием в коде «третья сертификация, v3.5.0», `app/core/crisis.py`
  реализует G2 (§12), `app/core/avalanche.py` подтверждён как одномоментное распределение —
  вывод про отложенную лавину долгов остался верным при правильной версии канона; (3) снос
  старых Jinja-шаблонов/роутов для семи перенесённых экранов выпал из плана при перенумерации
  этапов — восстановлен явным шагом Э8 + чекбокс ROADMAP §8.2, `templates/index.html`
  подтверждён мёртвым. Детали — CHANGELOG [8.9.3].
- **[!] v8.9.1 — Python 3.12→3.13, Э5 НЕ ПРОДВИНУТ.** Прямая задача владельца, вне канона
  плана вехи 8. `.venv` был на 3.13 с v8.7.1, `Dockerfile`/CI оставались на 3.12 — разрыв
  dev↔prod/CI закрыт. Обновлены `Dockerfile` (builder+runtime), `.github/workflows/ci.yml`
  (все job'ы), `.mypy.ini`, `pyproject.toml`, живые доки, `knowledge/roles/*` (14 файлов).
  Подтверждено: `docker build` на `python:3.13-slim` зелёный, смоук в контейнере (импорты
  крипто-/БД-зависимостей + 31 миграция Alembic) чист, полный прогон тестов на реальном
  PostgreSQL 16 под 3.13 — зелёный (exit 0), `preflight` чист. Детали — CHANGELOG [8.9.1].
- **[!] v8.9.2 — РЕВИЗИЯ КАНОНА МАТМОДЕЛИ ПО ВСЕМУ РЕПО + README, Э5 НЕ ПРОДВИНУТ.** Владелец
  поймал: ассистент дважды подряд назвал каноном v3.0.0 вместо реального v3.5.0 — источник
  ошибки был в `CLAUDE.md` и хуке `session-context.sh`. Найдено 28 файлов с той же ошибкой
  (список — CHANGELOG [8.9.2]); исторические файлы канона (`v3_0_0.md`, `v3_1_0.md`) НЕ
  тронуты — сохраняются намеренно, правились только неверные указатели на них. Хук
  `session-context.sh` сделан самообновляющимся (`grep -L SUPERSEDED docs/math_model_v*.md`)
  — не хардкод, баг не повторится на следующей рекалибровке. Гейт
  `finpilot-math-model-guard` дополнен калибровками G1–G8 (раньше вообще не упоминал
  кризисный режим/токсичный долг/floor). Отдельно — README.md пересмотрен комплексно:
  версия отставала на веху (7.7.1 вместо 8.9.1), таблица технологий не упоминала React SPA
  вообще, миграции 19 вместо 31, добавлена эпоха v8.x.x, инструкция запуска дополнена
  фронтом (`docker compose up` dev SPA не поднимает, нужен отдельный `npm run dev` на :5173).
- **[!] v8.9.0 — ВНЕОЧЕРЕДНОЙ ИНФРА-БАТЧ (прод-сборка фронта + деплой), Э5 НЕ ПРОДВИНУТ.**
  Прямая задача владельца вне канона плана вехи 8 (`docs/frontend_milestone8_plan.md`:
  Э5→Э6 юр→Э7→Э8) и вне порядка `docs/ROADMAP.md` («деплой» формально веха 9). Пять шагов:
  (1) `npm run build` — бандл 780 КБ, `TanStackRouterDevtools`/`ReactQueryDevtools` в проде
  нет (dead-code elimination через `import.meta.env.DEV &&` подтверждён grep по бандлу);
  (2) `nginx/Dockerfile` (multi-stage node+nginx) + `nginx/templates/finpilot.conf.template`
  разводит SPA (`/`, `/planning`, `/transactions`, `/obligations`, `/goals`, `/banks`,
  `/profile`, `/assets/*`) от FastAPI/Jinja по умолчанию (`/api`, `/v1`, `/health`, `/docs`,
  `/static`, `/contacts`, `/legal/*`, `/reset-password`, `/forgot-password`, `/validation`) —
  решение по корню `/` (SPA, не отдельный лендинг) уточнено у владельца; (3) `docker-compose.prod.yml`
  собирает nginx из своего Dockerfile вместо готового образа; (4) локальная проверка на
  изолированном docker-стенде (свой том/порт, реальный `finpilot_pgdata` не тронут) —
  все три контейнера healthy, миграции 0001→0031 прошли, все маршруты проверены curl;
  (5) `docs/DEPLOY.md` починен: `deploy/env.prod.example` воссоздан (был `.env.prod.example`,
  утрачен при восстановлении `.git` v8.7.1; переименован — `block-secrets.sh` блокирует
  `*.env.*` без разбора примера от секрета), инструкция `TOKEN_ENCRYPTION_KEY` через
  `openssl rand -hex 32` заменена на `Fernet.generate_key()` (старая уронила бы прод на
  первом старте). Проверка живьём поймала три реальных бага (стоковый `default.conf`
  nginx-образа ломал healthcheck, отсутствовал IPv6-listen, неверная генерация ключа) —
  все починены, подробности CHANGELOG [8.9.0]. Попутно найден и починен баг ревизионного
  гейта (`REF_PATTERN` в `tools/revision/revision_check.py` обрезал расширения длиннее
  6 символов — `\.\w{1,6}` → `\.\w+`). По ходу на машине кончился диск (33 ГБ Docker Desktop,
  снесено с согласия владельца — только синтетические dev-тома). Полный прогон: 1512/1512,
  0 errors, 0 failures (1 skipped); Vitest 75/75, Playwright 25/25; flake8 чист; mypy — 3
  известных долга. **СЛЕДУЮЩИЙ ШАГ на момент v8.9.0 — Санкей-диаграмма, шкала ПДН,
  KaTeX-формулы по клику; Санкей закрыт в v8.10.0 (см. запись выше) — актуальный указатель
  «СЛЕДУЮЩАЯ ЗАДАЧА» смотри в ROADMAP.md и в начале §0.**
- **v8.8.0 — ВЕХА 8, Э5 НАЧАТ: БРАУЗЕР 66 АЛЬТЕРНАТИВ.**
  Развилка из v8.7.1 разрешена владельцем в чате: Э5 (доменные представления), не остаток §8.2а.
  Первый пункт Э5 — `AlternativesBrowser` на `/planning` (`frontend/src/pages/planning/ui/`):
  свёрнутый по умолчанию браузер полного `ranked[]` (до 66 альтернатив) под `AllocationPanel`,
  сортировка по критериям (рекомендация/Rt/Lt/ПДН). Бэкенд не менялся — `ranked` уже отдавался
  `run_planning`, фронт его просто не читал; `PlanAlternative`/`CalculatePlanResult` дотипизированы
  по реальному коду бэка (`id`, `Rt_new`, `Lt_new`, `Dt_new`, `is_recommended?`).
  **Второй пункт Э5 (лавина долгов) сознательно отложен** — при попытке взяться выяснилось, что
  канон v3.0.0 (`app/core/avalanche.py`, §10 матмодели) считает только одномоментное
  распределение, без помесячного графика/сценария «без плана»/экономии на процентах, которые
  требует план. Это новая, несертифицированная математика в замороженной модели (приказ
  владельца 2026-07-02: «математика замораживается и проверяется до фронта») — решение
  расширять канон за владельцем, не за фронтендом. Взят следующий пункт без блокера.
  `api-contract-guard` чисто. `a11y-auditor`: 2 P1 (контраст бейджа «рекомендовано» в светлой
  теме, анонс пересортировки диктору) починены, мелочи по мелочи — см. CHANGELOG [8.8.0].
  Попутно первым полным прогоном на закоммиченном дереве найдены и почищены два
  пре-существующих гейт-долга (битая ссылка в `ui_audit_e3.md`, 2×E501 в хуке) — оба не связаны
  с фичей. **Отодвинутый `.git` из `$HOME` (v8.7.1) по указанию владельца перемещён в `~/.Trash`**
  (не `rm -rf` — заблокирован `block-destructive.sh`, штатно; обычный `mv`, обратимо). Полный
  прогон бэка — **1512/1512, 0 errors, 0 failures** (1 skipped), Vitest 75/75, Playwright 25/25.
  **Отсюда продолжаем: Э5 — следующие кандидаты без блокера: Санкей-диаграмма распределения
  потока (сейчас на `/planning`/dashboard простой bar в `AllocationPanel`, план просит режим
  «подробно» через Санкей), ПДН/автономия шкала с зонами (сейчас текст+бейдж в `MetricsGrid`,
  не визуальная шкала), формулы KaTeX по клику (не начато). Монте-Карло веерная область
  (p10-p90, 80%) уже фактически есть в `ForecastPanel` с Э3 — на Recharts, не visx как в плане,
  но по существу пункт закрыт; при желании довести до буквы плана (visx, формулировка «в 8 из
  10») — отдельная, не блокирующая задача.**
- **[!] v8.7.1 — GIT-РЕПОЗИТОРИЙ ВОССТАНОВЛЕН НА `finpilot/`, PATCH.**
  `.git` оказался инициализирован в домашнем каталоге (`/Users/vasyaevdokimov/.git`, создан 22 июля
  прошлой сессией) вместо `finpilot/` — 0 коммитов, 0 ref, без remote, только Claude-специфичные
  паттерны в `.git/info/exclude` (похоже на случайный `git init` не в той рабочей директории).
  Весь код проекта физически никогда не был под git, несмотря на дисциплину SemVer/CHANGELOG
  в `CLAUDE.md`; `git status` в сессиях показывал не диф проекта, а мусор всего `$HOME`.
  Проверено командой (`git rev-list --all --count` → 0) до любого действия. Пустой `.git`
  отодвинут (не удалён — попытка `rm -rf` заблокирована `block-destructive.sh`, штатно) в
  `~/Documents/.git_home_stray_backup_20260808232757`; решение по удалению — за владельцем.
  `git init` выполнен внутри `finpilot/`, `node_modules/` добавлен в корневой `.gitignore`
  (был только во вложенном `frontend/.gitignore`), индекс перед коммитом проверен вручную на
  секреты/credentials/`.env` — чисто. Первый коммит — root-commit, 1384 файла, дерево как есть
  на `v8.7.0`. **Отсюда продолжаем: веха 8 после Э4 — развилка открыта владельцем (`ROADMAP.md`
  «СЛЕДУЮЩАЯ ЗАДАЧА»): либо остаток §8.2а (API-only фичи фронта), либо Э5 (доменные представления
  — SAW/Монте-Карло/лавина/ПДН); ответ ожидается в чате.**
- **[!] v8.5.0 — ВЕХА 8, Э3 ЗАКРЫТ: ПИЛОТНЫЙ ЭКРАН ЧЕРЕЗ ВЕСЬ КОНВЕЙЕР.**
  Дашборд выбран пилотным экраном Э3 (обоснование: единственный экран сразу с двумя типизированными
  эндпоинтами, обеими визуализациями стека, всеми четырьмя состояниями экрана включая доменный
  «дефицит») и проведён через весь конвейер плана: вёрстка на токенах «Спокойный»
  (`frontend/src/pages/dashboard/`) → данные через сгенерированный клиент (новая сущность
  `frontend/src/entities/plan-summary/`, хуки `usePlan`/`useForecast`) → состояния
  loading/empty/error/success, включая fail-loud «Плана распределения нет» при `top3: []` (дефицит,
  `Rt < 0`) как легитимный домейн-исход, а не ошибку → 5 тестов Vitest
  (`DashboardPage.test.tsx`) → 3 теста Playwright E2E (`e2e/dashboard.spec.ts`, моки `page.route()`
  реалистичными фикстурами — живой бэк в CI-джобе не поднят, сознательная граница) → субагенты.
  **api-contract-guard** нашёл реальное расхождение: `risk_profile` в `CalculatePlanResult` был
  типизирован `number?`, бэк (`run_planning` → `RISK_PROFILES`) всегда отдаёт строку — исправлено
  на обязательный `string`, оба тестовых фикстура обновлены. **a11y-auditor**: 2 блокирующие
  находки починены — все четыре состояния экрана получили `<main>`+sr-only `<h1>` (их не было);
  график прогноза (Recharts) не имел текстовой альтернативы (WCAG 1.1.1) — помечен `aria-hidden`,
  добавлена полная `<table className="sr-only">` по всем точкам прогноза, не только последней;
  плюс 3 точечных фикса (мёртвая CTA дашборда, `role="status"` вместо `aria-live`, разведённый
  токен `--c-bg2`/`--c-surface` тёмной темы); 3 находки отложены осознанно (иерархия заголовков
  Hero/MetricCard, отсутствие акцента при ПДН>40% в отличие от бейджа Ликвидности, устаревший
  `clip: rect()` в `.sr-only` — все требуют ревизии дизайн-системы, не точечной правки этого
  экрана). По ходу найдены и починены два интеграционных бага, не связанных с a11y: двойной
  `/api/api/...` URL-префикс (генерированный SDK уже отдаёт абсолютные пути, `baseUrl` клиента был
  `"/api"` — исправлено на `""`) и CORS 403 на живом обращении фронта к бэку (`CORS_ORIGINS` не
  включал origin Vite dev-сервера — добавлен). Полный прогон бэка после всех правок (включая
  `CORS_ORIGINS`) — **1512/1512, 0 errors, 0 failures** (1 skipped), `flake8`/`mypy` чисто.
  **По пути — ложная тревога, не регрессия:** первый прогон после правок упал десятками `E`/`F`
  на 66% с усечёнными трейсбеками — тот же класс, что чинили в v8.3.2 (свободное место на диске
  прыгало 750 МиБ → 3,6 ГиБ → 1,7 ГиБ за минуты без видимой причины на этой машине). Освобождено
  ~2,9 ГБ переиспользуемых кешей (`pip cache purge`, `brew cleanup -s`, `npm cache clean`) —
  повторный прогон подтвердил зелень, код продукта не тронут. Отдельно найден и починен реальный
  (не связанный с диском) баг гейта: `.flake8` не исключал `frontend/node_modules` — вендоренные
  пакеты (katex, flatted) несут служебные `.py`-скрипты не нашего авторства, `flake8 .` падал
  на них с v8.4.0 и раньше не был перепрогнан целиком после появления `frontend/`. Исключение
  добавлено. **mypy: 3 ошибки в `statement_parser.py`/`statement_reconcile.py` — существовавший
  долг, уже в очереди ROADMAP §8.2 («починить 3 ошибки mypy в парсере»), файлы не тронуты этим
  батчем, вне периметра Э3.** **Отсюда продолжаем: Э4 плана вехи 8 — перенос остальных экранов
  (операции, обязательства, цели, планирование) по тому же конвейеру.**
- **[!] v8.4.1 — ДУБЛЬ ГЛОССАРИЯ ПО РЕГИСТРУ ПОЧИНЕН, ГЕЙТ УСИЛЕН.**
  Заглавный `GLOSSARY.md` (в `docs/`) и строчный `docs/glossary.md` — на macOS (регистронезависимая
  ФС) один и тот же файл (`stat -f "%i"` дал совпадающий inode на обоих путях, диф между
  «версиями» физически невозможен — сравнивать было нечего), в Docker/CI (регистрочувствительная
  Linux-ФС) это два разных файла. Пострадавшие ссылки на заглавный `GLOSSARY.md` — `README.md`,
  `docs/README.md` (три места), `docs/WATCHLOG.md` — приведены к строчному `glossary.md` (уже
  было каноническим case-preserved именем на диске, переименовывать файл не пришлось).
  **Умышленно не тронуто:** `docs/model/expert_certification/GLOSSARY.md` — отдельный, другой
  глоссарий (термины сертификации: «ключ координатора» и т. п.), без строчного двойника; ссылки
  на него в `docs/WATCHLOG.md:235` и `knowledge/business/posts_pipeline.md` — про этот файл, не
  про продуктовый. `CHANGELOG.md` — историческая запись, не редактировалась. Добавлен
  `CaseCollisionChecker` в `tools/revision/revision_check.py` (четвёртая проверка гейта) —
  ловит пары путей, различающиеся только регистром, как класс, через `git ls-files` (обход
  диска коллизию физически не увидит там, где ФС уже её схлопнула). На этом чек-ауте
  `git ls-files` пуст (репозиторий не закоммичен), проверка честно репортует «пропущена».
  Полный прогон бэка: **1512/1512, 0 errors** (1509 + 3 новых теста CaseCollisionChecker).
  **Отсюда продолжаем: Э3 плана вехи 8 — пилотный экран (см. следующий пункт, если уже начат
  в этой же сессии).**
- **[!] v8.4.0 — ВЕХА 8, Э2 ЗАКРЫТ: КАРКАС ФРОНТА.**
  Пять шагов Э2 (`docs/frontend_milestone8_plan.md`): (1) снимок OpenAPI пересобран, 112 путей,
  без дрейфа; (2) типизированный клиент сгенерирован `@hey-api/openapi-ts` (SDK + типы + хуки
  TanStack Query, `frontend/src/shared/api/generated/`, руками не написан — реально подключён
  через `client.setConfig` в `frontend/src/shared/api/client.ts`, `baseUrl: "/api"`,
  `credentials: "include"`); (3) каркас `frontend/` — Vite + React 19 + TS, TanStack Router
  (файловая маршрутизация) + Query, Zustand, Radix UI (Button/Tooltip — без Tailwind, токены
  напрямую), Recharts + KaTeX установлены, Feature-Sliced Design (`pages/widgets/features/
  entities` пусты с README — раскладка отложена до Э3), Vitest + Playwright настроены и зелёные
  (build → preview → реальный браузер, chromium); (4) токены направления «Спокойный» перенесены
  в `frontend/src/app/styles/tokens.css`, `.claude/hooks/check-design-tokens.sh` теперь
  **блокирует** (exit 2) хардкод-hex/px мимо токенов — раньше только предупреждал; (5) CI —
  блокирующий job `frontend` (typecheck/lint/format/vitest/build) в fast-тир,
  фронт-E2E (chromium+firefox+webkit) в full-тир.
  **Побочная находка при переносе токенов (не баг этого батча, но починен здесь):** все три
  макета `docs/ui_directions/{calm,precise,confident}/` держали один хардкод-цвет текста кнопки
  без переопределения на светлую тему — контраст 2.65–3.17:1 при пороге ≥4.5:1 (WCAG AA).
  Добавлен токен `--c-accent-ink`, свой на тему, проверено расчётом во всех трёх.
  Полный прогон бэка — **1509/1509, 0 errors** (первая попытка прервалась процессом на середине,
  не тестом; повтор подтвердил зелень — не путать одно с другим при разборе логов).
  **Открыто, не блокирует:** CI `contract-diff` не собран (Гейт Э2 плана его называет явно —
  перегенерировать openapi.json из живого приложения и сравнить с зафиксированным, падать при
  расхождении; требует бэк в CI-джобе, отдельная задача); `docs/frontend_migration_plan.md` §1.2
  всё ещё называет `openapi-typescript`+`openapi-fetch` (решили — hey-api, генерирует хуки
  TanStack Query целиком) и «68 CSS-переменных» (то же число, что чинили в
  `docs/frontend_milestone8_plan.md` в v8.3.2 — верно 40 уникальных); TypeScript зафиксирован на
  5.9.3, не 7.0.2 (latest) — `typescript-eslint` ни в одной версии пока не поддерживает TS 7.
  **Отсюда продолжаем: Э3 плана — пилотный экран через весь конвейер (компонент → тест →
  визуальная проверка → критика дизайна → аудит доступности). `/design-sync` (Шаг 2.6) —
  осмысленно после первого компонента из Шага 3.1, не раньше: до тех пор синк даст только
  токены/примитивы.**
- **[!] v8.3.2 — ТЕСТОВОЕ ОКРУЖЕНИЕ ПОД MACOS-ХОСТ, STOP-ГЕЙТ ЧИНЁН.**
  Три фикса, не связанных с кодом продукта, вскрытых `/doctor` и повторными блоками
  `Stop`-гейта: (1) `tools/statement_templates/synth.py` и `build_templates.py` жёстко
  ссылались на `/usr/share/fonts/truetype/dejavu/DejaVuSans.ttf` (Linux-путь, работает только
  в Docker/CI) — на macOS без контейнера все 11 property-тестов PDF-рендера
  (`tests/test_statement_property.py`) падали `TTFError`; добавлен фолбэк на
  `/Library/Fonts/DejaVuSans.ttf`. (2) `docs/frontend_milestone8_plan.md` ссылается вперёд на
  `docs/ui_visual_direction.md` (появится на шаге 1.3 этапа Э1) — гейт ревизии считал это
  битой ссылкой; внесена запись в `LINK_ALLOWLIST` (`tools/revision/revision_check.py`) по
  образцу уже существующих записей для непройденных артефактов вехи 8. (3)
  `.claude/hooks/tests-gate.sh` вызывал системный `python3` (3.10, без зависимостей проекта) —
  теперь предпочитает `.venv/bin/python3`. На хосте не было `.venv`; заведён на Python 3.13
  (3.12 из `pyproject.toml` недоступен на машине). Полный прогон после фикса: **1509/1509
  SQLite** (1 skipped), 0 errors. Кода продукта не тронуто. **Отсюда продолжаем: без изменений
  от v8.3.1 — веха 8, этап Э1 (`/design-sync` и три визуальных направления); решение владельца
  по xz и `claude update` остаются открытыми.**
- **[!] v8.3.1 — СНИМОК OPENAPI БЫЛ МЁРТВЫЙ, ПЕРЕСОБРАН.**
  `docs/api/openapi.json` держал версию 5.14.0 при коде 8.3.0. Недоставало шести путей,
  все шесть — юридический контур вехи 7 (`/api/consents`, `/api/legal/documents`,
  `/legal/cookies`, `/legal/marketing-consent` и др.). Сгенерированный из него
  TypeScript-клиент не умел бы запросить согласие — провал 152-ФЗ обнаружился бы
  на приёмке фронта. **Регенерация снимка — первый шаг этапа Э2, до генерации клиента.**
- **[!] v8.3.0 (последний батч) — КОНТУР ПОЧИНЕН, ПОТЕРЯННОЕ ВОССТАНОВЛЕНО.**
  Заход 7 (агентский контур) лёг в `raw/` дословно, выжимка написана. Закрыт дефект,
  который этот же отчёт и вскрыл: в v8.2.0 `.claude/settings.json` не имел ни одного
  блокирующего хука — заслон на секреты держался на тексте. Теперь `PreToolUse` exit 2
  на `.env` и на деструктивные команды, `Stop`-гейт на тесты, аудит-лог действий агента.
  Зависимость от `jq` убрана: без него хуки молча пропускали всё. Восстановлено то, что
  сгорело вместе с чатом №15: операционная таблица, лаборатория токенов, каталог решений
  владельца (правило §19), шесть скиллов с евалами. Разбор xz дал отрицательный итог —
  рекомендация не пережимать, растёт git-история. PIT-015: трое суток без чекпоинта.
  **Отсюда продолжаем: владелец решает по xz и делает `claude update` + `/doctor`;
  вахта идёт в этап Э1 плана вехи 8 — `/design-sync` и три визуальных направления.**
- **[!] v8.2.0 (последний батч) — ШЕСТЬ ТЕМ ИССЛЕДОВАНИЯ ЗАКРЫТЫ.**
  Отчёты 5 и 6 в `docs/research/raw/`: поверхности Claude (Code, Cowork, Design, плагины,
  скиллы) и управление контекстом (запрос, чат, Проект). Разделены две стены, найден
  переключатель Code Execution, объяснено «remove some tools», порог RAG — по числу файлов.
  План вехи дополнен шагами 0.4–0.6 и 3.5. **Отсюда продолжаем: владелец делает `claude update`
  и гигиену контура, пушит зеркало; дальше этап Э1 — `/design-sync` и три направления.**
- **[!] v8.1.0 (последний батч) — ЗЕРКАЛО ГОТОВО К СИНКУ.**
  Собран `finpilot-mirror-v8.0.0.zip`, заведены реестр состояния и ADR-012, закрыты четыре
  утечки (главная — деплойер с картой всех репозиториев). PIT-014: прошлый отчёт соврал о
  состоянии кода по памяти. **Отсюда продолжаем: владелец пушит зеркало своим публикатором
  (`check` → `build` → глазами staging → `push` → `release`), затем этап Э1 плана вехи 8.**
- **[!] v8.0.0 (последний батч) — ВЕХА 8 ОТКРЫТА; ИСХОДНИКИ ИССЛЕДОВАНИЙ В РЕПОЗИТОРИИ.**
  MAJOR подняли до номера вехи. Исправлена ошибка PIT-013: четыре исходника исследований легли
  в `docs/research/raw/` дословно, заведено правило §18 (первичный материал — в репозиторий
  всегда и первым). Скрипт установки плагинов поправлен по логу владельца.
  **Отсюда продолжаем: этап Э1 плана — `/design-sync` и три визуальных направления в Claude
  Design.** Перед этим владелец обновляет Claude Code до ≥2.1.181.
- **[!] v7.8.0 (последний батч) — ИНСТРУМЕНТАЛЬНЫЙ КОНТУР ВЕХИ 8 СОБРАН.**
  Веха 8 — первая, которую делаем не в чате с архивом, а в агентском контуре. Контур нигде
  не был зафиксирован: четыре аккаунта собрали бы четыре разных набора инструментов, и это
  всплыло бы на первом мёрже. Заведены `CLAUDE.md` и `.claude/` (их не было вовсе), ADR-011,
  пошаговый план вехи с промптами, три субагента с урезанными правами, скрипт установки
  плагинов, три методички исследований и реестр источников. Кода не тронуто.
  **Отсюда продолжаем: этап Э1 плана — `/design-sync` и три визуальных направления в
  Claude Design.** Перед этим владелец ставит плагины скриптом.
- **[!] v7.7.1 (последний батч) — ТРЕБОВАНИЯ «ДО ЗАПУСКА» ИЗ ПРОЗЫ В ЧЕКБОКСЫ.**
  Телеметрия принятия совета и снимок модели жили в прозе указателя «СЛЕДУЮЩАЯ ЗАДАЧА»,
  который переписывается каждый батч, — одна вахта не перенесла бы, и требование исчезло.
  Заведён раздел ROADMAP §8.2а с тремя чекбоксами (плюс правовой контур обезличивания —
  его нельзя достроить задним числом) и те же пункты внесены в гейт запуска
  `docs/legal/compliance_register.md` §3.
- **[!] v7.7.0 (последний батч) — ДОКУМЕНТАЦИЯ ПРИВЕДЕНА К СОСТОЯНИЮ, ROADMAP ПОД ФРОНТ.**
  README висел на бейдже 6.12.2 при коде 7.6.0 — на 15 версий и две вехи назад; добавлен блок
  «Текущее состояние», дописаны эпохи v5–v7, секция документации пополнена. `RELEASES.md`
  дополнен вехами 5–8. ROADMAP: §8.2 получил блок готовности бэка и отдельный пункт с
  юр-требованиями фронта, указатель «СЛЕДУЮЩАЯ ЗАДАЧА» переписан под старт вехи 8.
  Вехи 1–7 закрыты, работа продолжается в новом чате с этого журнала.
- **[!] v7.6.0 (последний батч) — ПОЛНАЯ РЕВИЗИЯ ПЕРЕД ФРОНТОМ.**
  Полный прогон (1473 теста) нашёл 38 падений, которых не показывали выборочные группы:
  23 — регистрация без обязательного `consent`, 12 — гейт финданных, 3 — ревизионный гейт.
  Все починены; дельта подтверждена на PostgreSQL. Закрыты два «чеховских ружья»
  (`REQUIRED_AT_REGISTRATION` не исполнялась, `DISCLAIMER_39FZ` не доезжал до рекомендации),
  восстановлен §12 (инцидент занесён в реестр), убран дубль в `docs/legal/`.
  Отчёт ревизии с оценкой готовности к фронту 86/100 —
  `docs/reports/audits/full_revision_before_frontend.md`.
- **[!] v7.5.0 (последний батч) — ГЕЙТ СОГЛАСИЯ НА ФИНДАННЫЕ + ЖИВЫЕ ЮР-ССЫЛКИ.**
  Закрыт разрыв между документом и кодом: согласие `financial_data` записывалось, но не
  ограничивало ничего — финансовый портрет обрабатывался по основанию для ПДн.
  Гейт на четырёх роутерах, 403 с машиночитаемым телом, аноним проходит, миграция 0031
  переносит согласие действующим пользователям. Отдельно найдено, что ВСЕ шесть url
  юр-документов вели в никуда, включая тот, на который ссылается сам отказ гейта, —
  исправлено и закрыто тестом на класс. Матрица 108/108 на обеих СУБД.
- **[!] v7.4.0 (последний батч) — PREFLIGHT В CI + СТАНДАРТ 48 + ИСПРАВЛЕНИЕ ЛОЖНОГО ВЫВОДА.**
  Preflight повешен на CI отдельным блокирующим job'ом: инструмент, зависящий от того,
  вспомнит ли его вахта, — это уровень 2, переодетый в уровень 3. Теперь без него нет гейта.
  Внедрён стандарт 48: `.repo-id` и `VERSION` в корне, preflight сверяет версию в четырёх
  местах и значение `.repo-id`. **Исправлена ложная запись прошлого батча:** CRLF в семи
  эталонах НА МЕСТЕ, восстанавливать ничего не нужно; проверка врала из-за удвоенного
  экранирования в heredoc — PIT-012.
- **[!] v7.3.0 (последний батч) — РЕЕСТР РЕЦИДИВОВ И PREFLIGHT.**
  Ответ на вопрос владельца «поможет ли ещё один репорт, если прошлый не заметили». Не поможет:
  доказано трижды за сессию. Введены счётчик повторов и эскалация типа контрмеры по нему —
  третий повтор обязан получить КОД, а не текст. `tools/preflight.py` (10 тестов, flake8=0)
  проверяет ceremony-дрейф и известные грабли механически; топ-3 рецидивов вынесен в шапку
  этого журнала, потому что файл в `docs/` разделил бы судьбу предыдущих реестров.
  Первое место — чужой класс с шестью повторами: код под первый прогон вместо устоявшегося
  состояния (уровень 4 — менять устройство тестов, а не добавлять правило).
- **[!] v7.2.0 (последний батч) — МАТРИЦА PostgreSQL + НЕЙМИНГ АРХИВОВ.**
  Долг из v7.1.0 закрыт здесь же: PG 16.14 поднят в песочнице (apt), миграция 0030 проверена
  на структуру, внешний ключ и обратимость, матрица зелёная на обеих СУБД (69/69 и 69/69).
  Прогон нашёл PG-only взаимную блокировку в фикстурах — исправлена. Повторный инцидент
  «PG невозможен в песочнице» разобран письменно. Нейминг архивов переведён на
  `personal-finance-dss-vX.Y.Z.zip`, `finpilot` оставлен публичному зеркалу.
  Внесены `.gitattributes`, скрипт публикации и два репорта окружения от другой вахты.
- **[!] v7.1.0 (последний батч) — БЭКЕНД ЮРБЛОКА ЗАКРЫТ.**
  Модель согласий доведена до рабочего состояния и проверена: L1, L2, L3, L4, L9, L10 —
  код + тесты. Карта `docs/legal/README.md` обновлена реальными статусами со ссылками на
  тесты. По ходу найдена и починена регрессия: обязательный `consent` (L2) уронил девять
  старых тестов `test_auth.py`, регистрировавшихся без него, — введён хелпер `_register`,
  через него пущен и `_auth_header`. Гейты: 69 зелёных, flake8 = 0.
  **ДОЛГ: матрица PostgreSQL по миграции 0030 — в песочнице PG нет, прогнать у владельца.**
- **[!] v7.0.0 (последний батч) — ВЕХИ 6 И 7 ЗАКРЫТЫ, ПЕРЕХОД К ВЕХЕ 8.**
  Отчёт закрытия — `docs/reports/milestone_6_7_closure.md`, с обязательным блоком
  «Предложения Claude» (7 пунктов). Веха 6 закрыта по исчерпанию информативности метода;
  канон модели остаётся v3.5.0. Веха 7 закрыта в объёме «документы + требования + реестр +
  гейт»; новый `docs/legal/compliance_register.md` — обязательства владельца O1–O7,
  гейт запуска, порядок при инциденте с ПДн. ADR-010 зафиксировал решение по парсеру.
  ROADMAP: обе вехи помечены завершёнными, указатель переведён на фронтенд.
- **[!] v6.26.1 (последний батч) — РАЗБОР: ПЕРЕНОСИТСЯ ЛИ МЕТОД СЕРТИФИКАЦИИ НА ПАРСЕР.**
  `docs/reports/testing/parser_certification_transfer.md`. Переносится на ~70%, большая часть уже
  перенесена и просто не была так названа. Различие в корне: у матмодели правильного ответа НЕТ
  (отсюда панель, коридор, предрегистрация, слепота), у парсера он ЕСТЬ — написан в документе.
  Значит нужна метрология: оракулы и инварианты, а не консенсус. Самое выгодное — балансовый
  оракул: работает на любой выписке без разметки и после запуска приносит баг-репорты бесплатно.
  Эксперт полезен ровно в одном месте — спорная семантика (кешбэк, кредитный лимит, внутренние
  переводы), и это ОДИН разбор с финансовым аналитиком, а не раунд на четверых.
- **[!] v6.26.0 (последний батч) — ПРОЦЕДУРЫ СЕРТИФИКАЦИИ ДОЗАКРЫТЫ.**
  Закрыты три вопроса владельца по модели. (1) Выборка мнений: `opinions_sample_protocol.md` —
  40 портретов это не «сокращённый разбор», а проверка ДРУГОГО утверждения; 12 000 проверяют
  ответы, 40 проверяют, что движок исполняет записанную методологию. Восемь страт,
  детерминированный отбор, раннер, независимая перепроверка каскада, итог числом.
  (2) Сдача: `output_naming_standard.md` — один архив на эксперта, различие в одной букве,
  плоская структура, пять файлов ядра, нарушение → возврат.
  (3) `GLOSSARY.md` — что такое ключ координатора и почему он раздаётся только после сдачи,
  плюс остальные термины, которые автору задания кажутся очевидными.
  Роль исследователя добавлена (14 ролей). Шесть новых идей в бэклог постов.
- **[!] v6.25.1 (последний батч) — ПАЙПЛАЙН ПОСТОВ + ТРИАДА РОЛЕЙ ВОКРУГ МАТЯДРА.**
  `knowledge/business/posts_pipeline.md`: недельный ритм с обоснованием (дневной съедает годовой
  запас идей за месяц и превращает сериальные посты в поток), раскладка по рубрикам на квартал,
  бэклог 32 идеи с источником материала и признаком готовности, правило накопления.
  Три роли: математик, инженер-исследователь, финансовый аналитик — с разделением в каталоге,
  какой вопрос задаёт каждая и чего она НЕ делает. Всего в библиотеке 13 ролей.
- **[!] v6.25.0 (последний батч) — ЮРБЛОК + РОЛИ КОМАНДЫ + ФОКУС-ЛИСТ СБОРА.**
  `docs/legal/` — юридический пакет целиком (6 документов + UI-тексты, md и docx).
  `docs/legal/README.md` — перевод юрблока в инженерные требования L1–L10 со статусами;
  L1 (три раздельных согласия против одного `consent_at`) — блокер для вехи 8, с решением.
  `knowledge/roles/` — role-kit + 10 ролей команды по стандарту (4 файла на роль).
  §4а плана добивки — что копить параллельно фронту; три журнала заведены ПУСТЫМИ,
  потому что механизм должен существовать до содержимого. ROADMAP: юр-чекбоксы закрыты.
- **[!] v6.24.2 (последний батч) — ПЛАН ДОБИВКИ МОДЕЛИ ДО ПОТОЛКА.**
  `docs/model/model_completion_plan.md`: от 85.7 к ≈91.3 тремя волнами с ценой каждой.
  Полных раундов в текущем формате — НОЛЬ; нужны два мини-раунда (1 сессия вместо 4),
  один объяснительный раунд и два раунда на живых данных после запуска.
  Потолок 91, а не 100: три оси упираются в физику задачи. Главный вывод плана —
  вкладываться надо не туда, где вес, а туда, где разрыв: тяжёлая ось достоверности
  (вес 20%) даёт +0.8, дешёвые объяснимость и полнота дают +1.6 вместе.
  ROADMAP: закрыт протухший чекбокс раунда 4, указатель переведён на веху 8 (фронт).
- **[!] v6.24.1 (последний батч) — ПРОТОКОЛ СЛЕПОТЫ + СТАТУС КАНОНА.**
  Введён `docs/model/expert_certification/blindness_protocol.md`: два вида утечки
  (окружения и постановки), обязательная изоляция эксперт-сессий, запрет на раскрытие
  констант модели в брифе, проверка на эхо при заявленной утечке. Прошит в стандарт
  (красные линии 1а/1б), в рунбук (шаги 13а/13б/15а) и в чек-листы обеих фаз.
  Пять уроков раунда 5 внесены в `CROSS_ITERATION_REVIEW.md` (№ 11–15).
  **КАНОН МОДЕЛИ НЕ ТРОНУТ: v3.5.0 остаётся действующим.** В `math_model.md`
  добавлен блок «Статус сертификации» с обоснованием, почему версия не бампится.
  Следующий кандидат на v3.6.0 — инфляционная индексация целей с горизонтом > ~3 лет.
- **[!] v6.24.0 (последний батч) — РАУНД 5 ЗАКРЫТ ПОЛНОСТЬЮ.**
  Собран `joined_v5.csv.gz` (12 000 × 71) из четырёх экспертных пакетов; приёмка fail-loud
  прошла чисто у всех четверых — впервые за серию ни один пакет не возвращён.
  Инструменты: `build_joined_v5.py` (контракт р.4 без изменений — условие сравнимости),
  `round5_agreement.py` (согласие, два коридора, H5, слой D, профили),
  `expert_profiles_cross.py` (профили панели по всем пяти раундам).
  ЧИСЛА: action-vector 78.63% при попарном коридоре 58.18–79.99 (у верхней границы);
  строгий трио-коридор 78.75–96.44 — модель на 0.12 п.п. ниже нижней границы, держать в уме.
  По слоям A 83.32 · B 73.57 · C 76.89 · E 78.06; худшие семейства `flow_edges` 66.67 и
  `floor_edge` 69.70. ВЕРДИКТЫ: H5-1 не подтверждена, H5-3 без консенсуса, В1 раскол 2/2,
  инверсия уверенности опровергнута. Калибровки нет — внедрять нечего.
  Оценочная карта пересчитана по §13: 84.2 → 85.7 (достоверность 84, робастность 92,
  адекватность 82). Отчёт — `docs/reports/testing/expert_certification_round5.md`.
- **[!] v6.23.0 (последний батч) — ЗАКРЫТИЕ СЕРИИ: ОЦЕНОЧНАЯ КАРТА МОДЕЛИ И ПЛАНЫ.**
  Написана `docs/model/model_quality_scorecard.md` — оценка матмодели v3.5.0 по десяти
  шкалам со 100-балльными рубриками, каждая оценка привязана к замеру из отчётов
  (интеграл **84.2**). Зафиксирован потолок метода: ось достоверности не может расти
  выше коридора согласия самих экспертов. Названы три оси, тянущие интеграл вниз, и
  цена подъёма — до ≈87.5 без правок ядра. Главный НОВЫЙ минус: **инфляционная
  индексация длинных целей** — датасет v5 впервые дал горизонты 10–30 лет, и цель на
  25 лет в номинале это систематическая ошибка, а не спорная позиция.
  Написаны: `iterations/5/round5_closeout_plan.md` (приёмка пакетов 8 проверок,
  решающие деревья H5-1/H5-3/В1, четыре признака исчерпания метода, чего делать нельзя),
  `model_testing_next_iterations.md` (три уровня продолжения, пять триггеров, смена
  природы раунда на живых данных, шесть правил дисциплины на паузе),
  `reports/testing/round5_owner_memo.md` (служебка владельцу + блок «Предложения Claude»),
  акт 7 в `model_history.md`. Гейты: flake8 = 0, целевые группы тестов зелёные.
- **[!] v6.22.0 (последний батч) — ФАЗА D РАУНДА 5: МОДЕЛЬНАЯ ПОЛОВИНА ЗАФИКСИРОВАНА.**
  Прогон модели v3.5.0 по датасету v5 повторён и подтверждён ТРЕМЯ сверками (§8, эмпирика
  вместо журнала): розданный `5.zip` ≡ канонический пакет в репо (sha256 4/4), регенерация
  из seed 20260723 ≡ розданное содержимое (4/4), свежий прогон ≡ снимок v6.21.0 бит-в-бит.
  Написан `tools/model_validation/round5_model_half_analysis.py` (13 тестов TDD, flake8=0):
  метод раунда 3 + разбор ПРОПУСКОВ слоя D по `expected_error` + базлайн семейств.
  По ходу закрыт дефект метрики: пары брались по `pair_id`, а его носят и группы дублей
  id — 259 строк засоряли метаморфику «неполными парами»; отбор по `pair_relation` M*.
  РЕЗУЛЬТАТ: ok 8290 / deficit 3110 / invalid 600; слой D 600/625 при 0 ложных, ВСЕ 25
  пропусков — единственный объявленный класс `near_rate_above_cap` (В1, не чинить);
  метаморфика 309 пар, 0 нарушений статуса; границы (поток ровно 0, ПДН 0.40, масштаб
  10¹⁰) держатся, 0 падений. **Базлайны предрегистрации зафиксированы ДО экспертов:**
  H5-1 +2.1 п.п. по доле `reserve`, H5-3 сплит ≈51/48 без дефицитов.
  Отчёты — `iterations/5/round5_model_half.md` и `.json`.
- **[!] v6.21.0 (последний батч) — СБОРКА 2 ДАТАСЕТА v5, регенерация финальной итерации.**
  Владелец отменил решение «объявить недобор»: итерация последняя, пакет сборки 1 роздан
  не был — регенерация ничем не рисковала. Сделано TDD (46 тестов зелёные): фиксированные
  квоты ячеек H5 (413/419/432 ≥ 385 НА ЯЧЕЙКЕ), слой C 0.33 за счёт A, веса liquidity
  362→410 по замеру (просадка 374), `_seed_goal` без следа 384 000 ₽ (повтор 18→2), точный
  ПДН 0.40 ×29 (продукт без карты, доход кратен 5, восстановление платежа после капов),
  ремонт разрыва групп дублей при обрезке пула (KeyError, регрессия n=60/200/500).
  Пакет+ключ+карточки+прогон v3.5.0 перевыполнены (600/625, ложных 0). Приёмка брифа —
  инструмент `brief_check_v5.py`; найден и закрыт пятый дефект брифа (фантомный
  `artifact_catalog.md`). Сборка 1 аннулирована, sha256 недействительны. Раздаточный
  `5.zip` собран из сборки 2. Решения с альтернативами —
  `iteration5_build2_decision_rationale.md`. Гейты: fast-прогон шестью синхронными
  группами (грабля песочницы) — 1365 тестов, 0 падений, 0 ошибок, 1 skip;
  flake8 по всему репо = 0; revision-gate в группе 6 зелёный.
- **[!] v6.20.2 (последний батч, вахта J) — СКВОЗНАЯ РЕВИЗИЯ ПЯТИ ИТЕРАЦИЙ + СТАНДАРТ.**
  (1) **Третий дефект пакета v5, самый существенный: ТЗ п.1 раунда 4 (высший приоритет,
  6 консенсусных претензий из 6) был ОБЪЯВЛЕН выполненным и НЕ выполнен** — `meta` несла
  пять базовых ключей, ни одной декларации, а бриф утверждал обратное. Реализовано:
  `blind_declarations()` в генераторе v5 → поле `declarations` в `meta` слепого пакета +
  те же числа таблицей в брифе. Пакет перегенерирован, ДАННЫЕ НЕ ИЗМЕНИЛИСЬ. (2) Требование
  чистой сессии вынесено ПЕРВЫМ ЭКРАНОМ брифа. (3) **Сквозная проверка прогоном по данным:**
  ТЗ р.4 — 10/11 выполнено (11-й доделан), уроки р.1–3 сохранены все, семейства под H5-1/H5-2
  на месте (floor_edge 592), каталог A1–A18 отражён в брифе полностью. (4) **Новый канон
  `docs/model/model_testing_standard.md`** — пять уровней тестирования, метрика action-vector,
  правило предрегистрации, дисциплина датасета, ШЕСТЬ КРАСНЫХ ЛИНИЙ, чек-лист вахты.
  ГЛАВНЫЙ УРОК: утверждение брифа «сделано» — это гипотеза; каждый пункт ТЗ проверяется
  прогоном по данным, а не галочкой.
- **[!] v6.20.1 (последний батч, вахта J) — РЕВИЗИЯ ПАКЕТА v5 + ФИКСАЦИЯ ПРОЦЕССА.**
  (1) Пакет итерации 5 проверен независимо по РОЗДАННЫМ файлам: слепота чистая (0 меток,
  только префикс `SP5-`), 12 000 строк, 125 групп дублей (2 и 3), пакет тождественен канону
  по sha256, карточки сходятся с jsonl, обещания брифа сходятся с данными, семейства выше
  385. (2) **Исправлен блокирующий дефект брифа:** §2 называл несуществующий
  `portraits_v5.csv`, не упоминал `__meta__` и карточки → переписан под фактические семь
  файлов; доля составных уточнена до 12.8%. Датасет не перегенерировался. (3) **Закрыт второй дефект брифа: пропали правила чистоты сессии** (были в v3/v4) —
  аккаунты экспертов загружены контекстом продукта, без запрета раунд измерял бы эхо
  собственной модели. Добавлен запрет на память/инструкции проекта + сопроводительное
  сообщение `prompts/covering_message_v5.md`. (4) Скопление
  384 000 ₽ подтверждено как объявленное известное — НЕ чинить перед раздачей.
  (5) Написан `iteration4_design_rationale.md` — журнал инженерных решений итерации 4
  (почему именно так, что отвергнуто, что запрещено пересматривать без данных).
  (6) Runbook: приёмка БРИФА введена отдельным шагом фазы C + диф разделов с прошлым раундом — в р.5 все десять проверок
  были про данные, ни одна про бриф.
- **[!] v6.20.3 (последний батч) — УРОКИ Р.4 СВЕРХ ТЗ ПРОВЕРЕНЫ.** Ячейки H5-1 недобирают
  порог 385 (231/241 при семействе 592; H5-2 — 120). НЕ перегенерировали: точность ±6.4%
  вместо ±5%, но сравнение ячеек ловит разницу от 13 п.п., а эффект р.4 был 52.6%.
  **ОБЯЗАТЕЛЬСТВО НА ФАЗУ РАЗБОРА:** публиковать фактическую точность; «различий нет»
  формулировать как «различие меньше 13 п.п. не обнаружено». Объявлено в приёмке §3.
- **[!] ПЕРЕД РАБОТОЙ:** прочитай баннер протокола непрерывности вверху файла и `docs/session_continuity.md` — рано и часто скидывай архив, иначе работа встанет.
- **[!!!] УПАКОВКА АРХИВА (критично — INC-FWD-BUILD-WIPEOUT):** архив ВСЕГДА с корневой обёрткой
  `finpilot_vX_Y_Z_intl/` (имя обёртки = имя zip без `.zip`). Паковать так:
  `mv <дерево> finpilot_vX_Y_Z_intl && zip -r finpilot_vX_Y_Z_intl.zip finpilot_vX_Y_Z_intl`.
  **НИКОГДА** `zip archive.zip .` (содержимое без обёртки) — публикатор ищет обёртку, при её
  отсутствии старый fallback `find | head -1` выхватывал случайную папку (`deploy/`), а
  `rsync --delete` делал репо равным ей → v5.20.0–v5.23.0 стали огрызками (6 файлов вместо ~623).
  Публикатор с v5.24.0 укреплён (fail-loud по маркерам `app/`+`CHANGELOG.md`), но формат обёртки
  соблюдать всё равно. Разбор: `docs/reports/incidents/forward_build_wipeout_incident.md`.
- **[!] ПАРСЕР ВЫПИСОК — НА ПАУЗЕ. Первым делом читать `docs/statement_parser_open_items.md`.**
  Работа упёрлась в данные: без новых реальных выписок дальнейшая активность здесь — самоуспокоение.
  От владельца нужны: holdout 15–20 свежих файлов (≥5 банков, ≥2 вне топ-6, PDF/CSV/XLSX/1C, кредитка,
  месяц с 200+ операциями, справка и скан), PDF Альфы и Газпромбанка **с операциями**, одна маленькая
  выписка с ручным пересчётом (единственный способ проверить описания и даты), решение по OCR.
  На исполнителе остаётся только добивание итогов Сбера (разбор по координатам).
- **[!] СЛЕДУЮЩИЙ ШАГ (= указатель «СЛЕДУЮЩАЯ ЗАДАЧА» в ROADMAP) — раунд 4: экспертный прогон.**
  Пакет готов и отдан владельцу (v6.16.0): `expert_portraits_v4_part1..4.jsonl.gz` + бриф
  `docs/model/expert_brief_v4.md` + карточки + методичка `expert_certification/testset_methodology.md`.
  **BLOCKED на владельце:** 4 чистые сессии V/M/J/S, с каждого — комплект из 13 артефактов
  (`artifact_catalog.md`). Не заблокировано исполнителю после сдачи: build_joined_v4
  (позиционный контракт), **метрика action-vector** (lump-назначение + месячный сплит —
  месячная доминанта на stock-данных частично невалидна, р.3 §5), сверка D-конфьюжна с
  манифестом, раскрытие ключа координатора экспертам. Гипотеза р.4 ПРЕДЗАРЕГИСТРИРОВАНА
  (не внедрять до данных): токсичный долг >= max(30%, бенч+15пп) пробивает floor до ~1xburn.
  Канон модели — **v3.4.0** (рекалибровки в р.3 не было). Параллельно: PNG-превью на Mac.
  Прогон модели по датасету — команда `dataset_export run --model-version vX_Y_Z --dataset-version N` (стандарт `docs/model/model_run_output_standard.md`): всегда 2 файла,
  имена кодируют версию модели И итерацию датасета.
- **[!] СЛЕДУЮЩИЙ ШАГ (= указатель «СЛЕДУЮЩАЯ ЗАДАЧА» в ROADMAP) — генератор v5 + раунд 5.**
  ТЗ готово и лежит в `docs/model/expert_certification/iterations/4/expert_feedback_aggregate.md` §5:
  11 пунктов, приоритет — усечённая `meta` слепого пакета (шесть проблем из шести
  консенсусных — про слепоту метаданных, а не про содержание данных), снятие капа 10⁸
  на целях, джиттер объёмов, длинные горизонты 10–30 лет, кромка ПСК 292–300%, тройники
  дублей, легальные лимиты продуктов в DOE, расширение слоя D на остальные поля схемы.
  Сверх ТЗ экспертов обязательны два семейства под предзарегистрированные гипотезы:
  «кромка floor» (L_t ∈ [1; 2), разрез по наличию цели с дедлайном < 6 мес) и «кит с
  тонкой подушкой» — без них H5-1 и H5-2 проверить нечем. Гипотезы раунда 5
  ПРЕДЗАРЕГИСТРИРОВАНЫ в `expert_certification_round4.md` §10, внедрять до данных нельзя.
- **v6.20.0 (этот батч) — сквозная ревизия пяти итераций перед финальной раздачей.**
  `CROSS_ITERATION_REVIEW.md`: арка четырёх раундов, устойчивые роли экспертов
  (V нашёл токсичный долг на два раунда раньше остальных; почти всё ТЗ v5 — находки M;
  J независимо предложил action-vector; S единственный сдал машиночитаемые отчёты),
  полная история 35 претензий р.2–р.4 — **ни одна не осталась без ответа**, список
  «что нельзя сломать», открытые вопросы с провенансом, 10 уроков процесса.
  **Найден шестой дефект v5, молчаливый и худший:** семейство `whale_thin_cushion`
  под гипотезу H5-2 китов НЕ СОДЕРЖАЛО (медиана дохода 75 669 ₽) — конструктор кита
  звался с доходом базового портрета. Тесты на наличие семейства проходили, измеряло
  оно не то. Починено + два теста на СОДЕРЖАНИЕ семейства.
  **Бриф восстановлен до полного комплекта 16 артефактов** (в v6.18.0 просил 8 —
  молча вывел `summary.csv`, `expert_opinions_sample`, весь код воспроизводимости).
  **Спонтанные артефакты р.4 промотированы** (раздел обязателен по каталогу §3, не был
  написан ни в р.3, ни в р.4): A17 машиночитаемые json рядом с прозой (S), A18 раннеры
  отчётов (S и J независимо). **Пост-раундовое раскрытие ключа внесено в бриф** —
  просьба 3/4 экспертов из р.3, три раунда лежала невыполненной. Бриф дополнен В6
  (инверсия уверенности) и разделом «закрыто отрицательным результатом» про
  burn-знаменатель, чтобы финальный раунд не переоткрывал тупик.
- **v6.19.1 (этот батч) — runbook процедуры итерации.**
  `ITERATION_RUNBOOK.md` — исполняемый чек-лист раунда (фазы A–H) из прямого провала р.5:
  собрал итерацию 5 без карточек, без методички, без самоаудита датасета, потому что
  процесс раундов 3–4 нигде не был записан ПРОЦЕДУРОЙ (был только статический план
  архитектуры). Смысл истории — не повторять ошибок; файл делает уроки исполняемыми.
  Каждый стоп-контроль подписан раундом, где кусал (утечка `SP4-`, кап 10⁸, k за спекой,
  доля горизонтов на выборке против боевого объёма, PIT-011, split-half и
  предзарегистрированная константа, «не чинить пропуск валидатора до ответов»).
  `iteration_protocol.md` актуализирован (был застрял на итерации 3 → канон v3.4.0),
  приёмочные тесты 6→10. Runbook прошит в указатели WATCHLOG и ROADMAP.
- **v6.19.0 (этот батч) — приёмка датасета v5 и полный раздаточный пакет.**
  Разбор итерации 4 показал, что пакет экспертам отдаётся ЧЕТЫРЬМЯ частями, а в v6.18.0
  были только слепые jsonl и бриф. Добавлены карточки (12 000 шт, битые значения в
  `!..!`) и методичка тест-сетов — без неё эксперт не сделает формальную приёмку.
  **`dataset_stats_v5.py`** — координаторский самоаудит по десяти проверкам методички §3.
  **Нашёл 5 дефектов собственного датасета, все закрыты до раздачи:** (1) утечка префикса
  `SP4-` в 17 карточек — `grep SP4-` выдавал слой дефектов, слепота ломалась одной
  командой; (2) 225 целей ровно по 5 000 ₽ — обрезанный хвост снизу, зеркало капа 10⁸;
  (3) длинных горизонтов 3.96% против заявленных 5% — порог мерился на выборке 3 000,
  а не на боевых 12 000; (4) k до 5.0077 при спеке [0.2; 5.0]; (5) таксономия дефектов
  не собиралась в поля, мощность падала ниже порога 30. Приёмка —
  `iterations/5/dataset_acceptance_v5.md`, там же честный раздел про четыре известные
  особенности, которые НЕ чинились и объявлены явно.
  **Урок в протокол:** приёмочный порог мерить на боевом объёме, а не на тестовой
  выборке — иначе документация расходится с данными (случай 3 был именно таким).
- **[!] СЛЕДУЮЩИЙ ШАГ — раздать пакет v5 и собрать ответы четырёх экспертов.**
  Всё для раздачи готово: слепой пакет `knowledge/model_validation/expert_pack_v5/`
  (4 части jsonl.gz, 12 000 портретов, префикс `SP5`), бриф `docs/model/expert_brief_v5.md`,
  ключ координатора `coordinator_key_v5.csv.gz`, прогон модели `model_outcomes_v3_5_0_on_v5.csv.gz`
  (ok 8276 / deficit 3161 / invalid 563). После сбора ответов: `build_joined_v5` копируется
  с `build_joined_v4` (контракт тот же — позиционное сопоставление, `confidence`,
  разбор `lump_sum_plan` по направлениям, метрика action-vector), затем разбор.
  **Три гипотезы предзарегистрированы** в `docs/reports/testing/expert_certification_round4.md` §10 —
  внедрять их до получения ответов НЕЛЬЗЯ, весь смысл предрегистрации в этом.
- **v6.18.0 (этот батч) — генератор v5 + бриф финальной итерации.**
  Наследование от `PortraitGeneratorV4` (не переписывание: LHS, копула, аннуитеты и
  метаморфика переиспользованы — диф читаемый для мёржа). Закрыты все 11 пунктов ТЗ
  экспертов: слепая `meta` с декларациями (шесть консенсусных проблем из шести были про
  слепоту метаданных, а не про данные), снят кап 10⁸ на целях, джиттер объёмов
  (доля дефектов 4.2–5.8% вместо ровных 5.00%), длинные горизонты 10–30 лет (в v4 такая
  цель была ровно ОДНА на 12 000), кромка ПСК 2.90–3.00, три формы дублей `id` (пара,
  тройник, invalid+invalid), легальные лимиты продуктов, имя-специфичные капы целей,
  кламп k в спеку, слой D с 4 полей до 12 и составные 4% → 15%, минимальный платёж карты.
  Сверх ТЗ — семейства `floor_edge` (592) и `whale_thin_cushion` под H5-1/H5-2.
  Приёмка: 32 теста, все семь семейств выше порога мощности 385, **0 ложных срабатываний
  валидатора на 11 413 валидных**.
  **Находка:** валидатор пропускает 24 дефекта — ставки 2.93–3.00 выше законного потолка
  ПСК 292%, но ниже его порога 300%. Зона юридически невозможна, модель её принимает.
  НЕ чиним до ответов экспертов (вопрос В1 брифа, с явным раскрытием) — иначе правило
  было бы подогнано под собственное мнение до сбора данных.
- **v6.17.0 (этот батч) — третья сертификация закрыта: action-vector + G8.**
  (1) **Экспертная половина раунда 4**: `build_joined_v4.py` (+17 тестов) — позиционный
  контракт 4/4 побайтово, 0 нарушений бюджета на 48 000 строк, 0 нарушений формы invalid,
  0 расхождений `lump` с планом. Впервые сборка не потребовала поправок на дисциплину.
  (2) **Статусы и слой D — 100% у всех четверых** (600/600 при 0 ложных; ok 7334 /
  deficit 4066 / invalid 600 строка в строку). Диагностическая часть модели больше не
  предмет спора; всё оставшееся — про распределение.
  (3) **Action-vector** (`monthly × 12 + lump`, словари ходов нормализуются картой с
  fail-loud): согласие **71.6% → 79.4%** без единой правки модели; слой B 63.5% → 83.5%.
  Эксперт J независимо предложил ту же метрику в прогнозе расхождений.
  (4) **G8 внедрён**: токсичный долг (ставка ≥ max(30%, бенч+15 п.п.)) опускает floor
  2.0 → 1.0. Стенд: 78.19% → 79.42%, **split-half +1.22/+1.24**, 329 из 332 переходов —
  `reserve → debt`. Сетка 0.5/1.0/1.5 = 77.87/79.42/79.70 — взято предзарегистрированное
  1.0, не максимум. 0.5 хуже выключенного правила (немонотонность = не подгонка).
  Канон → **`math_model.md`** (§8/§19/§20). Golden 5 и инварианты 17 — без правок.
  (5) **Открытая зона названа**: L_t ∈ [1; 2) — 52.6% расхождений (против 8.7% при
  L_t < 1). Худшие kind: `magnitude_whale` 41.9%, `duplicate_id_valid_pair` 30.6%.
  (6) **Гипотеза confidence опровергнута с инверсией**: единогласие экспертов падает с
  уверенностью (58→38%), а согласие модели РАСТЁТ (78.4→88.6%) — модель спорит с
  уверенными позициями, а не с кромками. Константами это не чинится.
  (7) **Метаморфика**: модель 0 флипов на всех семи отношениях, у эксперта m — 11
  (пять на чистом масштабировании ×10). (8) **Архив итерации 4**: 70 артефактов,
  comparison, агрегат с ТЗ v5 и сверкой прогнозов расхождений (5 из 6 сбылись).
- **v6.16.1 (этот батч) — прогон модели по v4 + стандарт выходных файлов.**
  Портреты v4 через v3.4.0: ok 7334 / deficit 4066 / invalid 600; D 600/600 без ложных;
  метаморфика 0 нарушений из 300 пар; lump 38%. На жёстком датасете модель не сломалась.
  **Стандарт `model_run_output_standard.md`** (требование владельца): всегда 2 файла
  (сводка .md + csv), нейминг `model_outcomes_<модель>_on_v<датасет>` — команда
  `dataset_export run` гарантирует имена кодом (версия МОДЕЛИ != APP_VERSION). Фикс
  deep-теста argmax под лексикографику floor 2.0 (отставал с v6.13.0, спал в deep-слое).
  INV-SANDBOX-FULL-SUITE: маркер `slow` на тяжёлые v4-классы.
- **v6.16.0 (этот батч) — генератор v4 + систематизация находок экспертов.**
  (1) **Каталог артефактов с провенансом** (`artifact_catalog.md`): ценнейшие вещи трёх
  раундов делал СЛУЧАЙНО один эксперт из четырёх (проза V/и.1, ручной аудит 40 портретов
  S/и.1, кросс-чек карточек M/и.1, самовалидация V/и.2, приёмка датасета J/и.2,
  двухпроходный аудит J/и.3, summary с диагностикой M/J) — сравнить четвёрку по этим осям
  было нельзя. Теперь: находка одного → обязательна для всех; комплект итерации 4 — 13
  артефактов. (2) **Методичка тест-сетов занесена в репо** (редакция 2, жила у владельца):
  + продуктовая типизация, точная арифметика границ, семейства >=385, слой D v2,
  неравномерные возмущения, action-vector, позиционный контракт, уроки трёх раундов.
  (3) **История промптов** (`prompts/README.md`): что менялось и что терялось (jsonl->CSV
  в р.2 убил прозу и богатую разметку). (4) **Генератор v4** — независимый модуль (v3
  заморожен), 35 тестов: продукты РФ с ПСК-капом, k-спека=факт, имя<->сумма, нулевой поток
  ровно 0.0 (R3-F1 by design), киты 5–10 млн vs стресс 2e9, семейства C по 400, D v2
  (18 видов: составные + near-miss + рандомизация), валидные дубли id в C, E из 7
  отношений. Экспортёр обобщён (`LAYERED_GENERATORS`), регресс v3 зелёный.
  (5) **Бриф v4**: критерий invalid уточнён по расхождениям р.3, правило валидных дублей,
  ревью в два прохода с разделом «что хорошо». (6) **Новое, чего не делал никто**: колонка
  `confidence` 1–5 в CSV (взвешенный консенсус), `assumptions_log` (журнал допущений),
  `sensitivity` (±20% по своим константам — отделить позицию от кромки порога),
  `disagreement_forecast` (прогноз зон спора). Отчёт об эволюции артефактов —
  `docs/reports/testing/expert_artifacts_evolution.md`. (7) **Архив добит**: 5 файлов M/и.3
  (богатый jsonl + summary), полнота проверена по sha256 — 0 пропусков на 4x3.
- **v6.15.0 — третья сертификация закрыта + FLOW_EPS + архив итераций 1–3.**
  (1) Joined р.3: `build_joined_v3.py` (+8 тестов, позиционный контракт из-за дублей id,
  enum с invalid) — бюджет 0/48 000, форма invalid 0. (2) Числа: согласие 67.5% при коридоре
  64.8–88.0 (две философии: v/s долговая, m/j резервная; модель между) — триггер floor НЕ
  сработал; слой D: s+модель 600/600, v/m мимо absurd_rate_12, j мимо missing-deadline-key,
  ложных 0. (3) **Lump-стенд реализован** (`model_lump` в outcomes): модель 37.2%/295 тыс.;
  529/703 debt→goals+ — то же действие → **action-adjusted 76.3%**; месячная доминанта
  частично невалидна — р.4 переходит на action-vector. (4) **Burn-гипотеза отвергнута**
  (контрфакт 67.5→64.1). (5) **R3-F1 починен**: FLOW_EPS=0.005, полкопейки не включают
  кризис (нашёл j; v/m/s теперь 99.75 по статусам — симметрия границы). (6) **Гипотеза р.4
  предзарегистрирована**: токсичный долг ≥ max(30%, бенч+15пп) пробивает floor (511 строк,
  медиана 32.4%). (7) Метаморфика: модель консистентнее экспертов (s: 11 флипов на ×10).
  (8) **Архив**: итерация 1 занесена (37 файлов, 18 МБ gzip, jsonl-эра с kind — слепоты не
  было; full/-наборы V и S; вложенные zip распакованы), итерация 2 добита (J/summary),
  итерация 3 разложена (27 файлов + comparison + агрегат с ТЗ v4 + dataset_meta).
- **v6.14.0 — модельная половина раунда 3 закрыта.** Outcomes v3 реализованы
  (ветка invalid: новый `portrait_validation.py`, зеркало брифа; колонка `invalid_reason`;
  срез дат параметризован `model_outcome(today=...)` — v3 на своём 2026-07-16). Прогон
  12 000 через v3.4.0 без падений → `model_outcomes_v3_4_0_on_v3.csv.gz`: ok 7 161 /
  deficit 4 239 / invalid 600. **D-вердикт 600/600 при 0 ложных на 11 400 валидных.**
  Метаморфика (`round3_model_half_analysis.py`): 0 нарушений статусных инвариантов из
  300 пар; **M5 — однородность степени 1** (сплиты ×10 точно); M2/M3 нечувствительность
  объяснима равномерностью возмущений (неравномерные — в E-каталог v4); флипы M1/M4 —
  кромки floor/подушки. Отчёт `docs/reports/testing/round3_model_half.md`. Контракт для
  build_joined_v3: сопоставление ответов экспертов ПО ПОРЯДКУ строк (дубли id).
- **v6.13.2 — разбор INV-CJK в репо + md-карточки v3.** Полный разбор
  положен в `docs/reports/investigations/` (приказ владельца; репо-копия в U+-нотации,
  сырая — владельцу), канон доставки уточнён в `report_types.md` (investigation — в репо,
  post-mortem — владельцу по §12). `export_markdown --version 3`: слепые сухие карточки
  из expert_row, битые значения слоя D — как есть в `!..!`, дубли id доживают; рендер
  унифицирован v1/v2/v3 с защитными форматтерами, +1 тест. 12 000 карточек отданы
  владельцу отдельным .md (регенерируемый артефакт, в архиве не хранится).
- **v6.13.1 — CJK-канарейка в гейте.** Расследование «китайских символов»:
  UI-саммари (косметика платформы) vs токен-слип в генерации, который течёт в файлы —
  3 исторических слипа в knowledge/ найдены и починены (в т.ч. семантический: U+957F
  «долгий» вместо «долгого»). Новая ось `CjkCanaryChecker` (+2 теста): любой CJK = DRIFT;
  анти-самореференция — сырые иероглифы в репо запрещены везде (нотация U+, `chr()` в
  тестах; канарейка дважды поймала мои же цитаты при внедрении). Сводка —
  `docs/reports/investigations_summary.md` (INV-CJK-TOKEN-SLIP); полный разбор — у владельца.
- **v6.13.0 — вторая сертификация закрыта: floor 2.0 (канон v3.4.0) + генератор v3.**
  (1) **Joined собран:** 4 свежих движка (чистые сессии, 4×12 000), новый `build_joined.py` (+5 тестов),
  бюджет 0 нарушений, self-check 12 000/12 000. Согласие v3.3.0 as certified — **78.9%**: калибровка
  G1–G7 р.1 удержалась out-of-sample; 85% расхождений — «эксперты за резерв при Lt ∈ [1, 3)».
  (2) **Floor 1.0 → 2.0:** сетка 1.5/2.0/2.5/3.0 → 85.7/**88.0**/88.2/87.6%; split-half +10.1/+8.1 п.п.
  при шуме 0.5; починено 1 025 / сломано 205. Взят предзарегистрированный 2.0 (не максимум: +0.2 < шума).
  `RESERVE_FLOOR_MONTHS = 2.0`, канон переименован `math_model_v3_3_0.md` → **`math_model_v3_4_0.md`**
  (§8/§16/§19 + запись истории), golden переснят ОСОЗНАННО (2 сценария, оба в сторону консенсуса),
  переснят `model_outcomes_v3_4_0_on_v2.csv.gz`, joined пересобран, стенд подтвердил 88.0% боем.
  Побочный сдвиг кризисных тяжестей (recoverable 335→238) объясним floor-логикой. ADR-006 update:
  4 аргумента сняты/переоценён, следующий триггер — коридор р.3.
  (3) **Артефакты итерации 2:** `iterations/2/{V,M,J,S}/` (CSV/ревью/методологии/движки),
  `dataset_meta.json` (+ пробел: run_metadata не собран), `comparison.md` (кластер m–j–s 86.8–89.8,
  v — выпадающий: токсичная зона пробивает подушку + агрессивный lump), `expert_feedback_aggregate.md`
  (5 слепых зон, 12 дефектов датасета D1–D12, консолидированное ТЗ v3 из 8 пунктов).
  (4) **Генератор v3 с нуля** (`generator_v3.py`, чистый stdlib random — кроссверсионный детерминизм):
  слои A 4 800 (копула, Спирмен цели↔доход 0.36 / расходы↔платежи 0.32 в полосах ТЗ, хвост дохода без
  клампа, 100% аннуитет) / B 3 600 (LHS 10 осей, n_goals без дыр) / C 2 400 (27 kind, все дыры р.2
  закрыты) / D 600 (12 видов битья + дубли id, манифест expected_error) / E 600 (300 пар M1–M5,
  pair_id). r_bench непрерывный truncnorm [0.08; 0.24]; дедлайны T+Δ от frozen_today. Приёмка
  `tests/test_generator_v3.py` — **25 тестов** (TDD red→green; тюнинг демпферов копулы −0.40/−0.50;
  сид-робастность полос проверена). Экспорт: канонический размеченный (meta первой строкой), слепой
  пакет (дубли/мусор доживают, утечек меток 0), `coordinator-key`; outcomes/markdown v3 — fail-loud
  (стенд р.3). numpy==2.4.4 → requirements-dev.
  (5) **История модели** — `docs/model/model_history.md` (v2.x → v3.4.0, пять актов + открытые
  гипотезы + «неизвестное»); зарегистрирована в хабе. Живые указатели обновлены (model/README,
  инструкции V/S, README датасетов, iteration_protocol); blanket-урон истории WATCHLOG/ROADMAP
  починен прицельно (9+4 правок).
  (6) **Чистка §0:** детальные блоки прошлых батчей (v6.12.3…v6.5.0, 17 шт.) удалены из resume —
  их однострочники в §3, полнота в CHANGELOG; §0 снова «быстрый снимок» по собственной философии файла.

---

## §1. Протокол вахты + аккаунты

**Аккаунты:** Василий работает на 4 аккаунтах Claude — **V, J, M, S** — несущих вахту по очереди
через этот журнал (плюс несколько чатов внутри одного проекта). Текущую вахту описывает **M**.
Имена нужны только если Василий уточняет «акк V/J/M/S» в разборе; на работу не влияют — журнал
самодостаточен для любого.

**Заступая на вахту:**
1. Прочитай этот файл + `docs/ROADMAP.md` (направление) + `docs/sandbox_runbook.md` (грабли
   песочницы — НЕ диагностируй заново).
2. Распакуй архив, сверь версию в `app/config.py` с §0.
3. Подними venv (`python3 -m venv .venv` + `pip install -r requirements.txt -r requirements-dev.txt`);
   для E2E — `playwright install chromium`.
4. Отчитайся Василию коротко (3-4 строки): версия, текущая веха, следующий шаг.
5. НЕ переделывай закрытое, НЕ уходи с порядка ROADMAP без команды.

**Неся вахту:** обновляй §0/§4 по ходу; разборы оформляй САМ по триггеру (инцидент/расследование/
pitfall — маппинг в §5); в конце батча WATCHLOG живёт ВНУТРИ архива (`docs/WATCHLOG.md`) — наружу
отдаётся только сам архив. Отдельным `.md` через `present_files` журнал НЕ дублируем (политика
«вся документация внутри продукта», v5.23.0; подробно — `docs/session_continuity.md` §4). Исключение —
явный разовый запрос владельца дать файл.

---

## §2. Направление (выжимка ROADMAP)

Полностью — `docs/ROADMAP.md`. **Вехи проекта нумеруются как мажоры (v4-v9), это СОЗНАТЕЛЬНО не
классический SemVer** (мажор = крупный этап, не слом контракта). Внутри вехи: фича=MINOR, фикс=PATCH.

| Веха | Версия | Тема | Статус |
|---|---|---|---|
| 4 | `v4.x.x` | Завершение бэкенда + техдолг | В работе (остатки §4.4 deferred → 8/9) |
| 5 | `v5.x.x` | Организационное (репозитории, доки, бизнес, исследования, методички, банки, наука) | **Завершена** |
| **6** | `v6.x.x` | **Тестирование** (мат-модель + независимая экспертиза + импорт) | **← в работе** (v6.0.0: экспертиза 12000×4, модель → v3.1.0, согласие 87.4%) |
| 7 | `v7.0.0` | Юридическое (документы + соответствие законам) | Запланировано |
| 8 | `v8.0.0` | Фронтенд (React+TS, редизайн, вынос всех возможностей бэка) | Запланировано |
| 9 | `v9.0.0` | Деплой на сервер | Запланировано |

Порядок строгий: бэк → организация → **тестирование → юр → фронт** → деплой. _(Пересмотр 2026-07-02,
приказ владельца: математика замораживается и проверяется до фронта; юр-тексты — контент экранов,
фронт вшивает их сразу; юр-ответы внешние и медленные — запускаем раньше. Прежний порядок — `docs/roadmap_archive/`.)_

---

## §3. Последние 10 версий (новое сверху; РОВНО 10 — добавил новую, удали старую снизу)

> Полная история — `CHANGELOG.md` / `docs/RELEASES.md`. Здесь только 10 свежих (правило в шапке).
- **v8.25.3** — flake8 чист, долг линтеров в ROADMAP (PATCH). По вопросу владельца — оценка
  стоимости починки джобы `Линтеры` (информационная, не блокирует деплой): flake8 (3× E501 в
  `tools/model_validation/model_snapshot.py`) — 5 минут, ноль риска, починено; mypy — уже
  отдельная задача ROADMAP §8.2; pylint (~150 находок, весь `app/`, старый фон) — часы,
  решено не чинить сейчас. Остаток — чекбокс ROADMAP §9.0, после вехи 8. Детали —
  CHANGELOG [8.25.3].
- **v8.25.2** — CI красный 41 день / 65 релизов — post-mortem (PATCH). По вопросу владельца
  («как давно это было?») — разбор 274 прогонов CI: блокирующий гейт красный непрерывно
  `v5.25.0`→`v8.25.1` (2026-07-09 — 2026-08-19), ни разу не дошедших до `pytest` в CI (гейт
  падал на `revision_check` до старта тестов). Причина — тот же класс бага регистра путей
  `docs/glossary.md`, дважды, в двух направлениях. Процессный корень (5 Whys): ритуал сдачи
  батча не сверяется с реальным статусом GitHub Actions. Post-mortem SEV2 —
  `docs/reports/incidents/ci_blocking_gate_red_41_days.md`. Попутно — отдельное расследование
  зависшего первого прогона PostgreSQL-матрицы (60+ мин на `apt-get`-флюке раннера, не код) —
  `docs/reports/investigations/ci_full_pg_matrix_hang_investigation.md`. Починено:
  `timeout-minutes` на всех джобах `ci.yml`, health-check Postgres, декоративный `|| true` на
  locust-smoke. Детали — CHANGELOG [8.25.2].
- **v8.25.1** — `LinkChecker` стал регистрочувствительным (PATCH). `python -m tools.preflight`
  падал на Linux/CI (`exit 1`, битая ссылка на заглавный `GLOSSARY.md` в `docs/WATCHLOG.md`), был
  зелёным на macOS — `Path.exists()` резолвил ссылку через регистронезависимую APFS, тот же
  класс бага, что чинился в v8.4.1 (`CaseCollisionChecker`), но без второго файла на диске.
  Тест сначала (red: `LinkChecker(tmp_path).run().ok` был `True` при живом баге на реальных
  файлах в `tmp_path`), затем `_exists_with_matching_case()` через `os.listdir()` вместо
  `Path.exists()` (green). Само упоминание в §3 записи v8.4.1 переписано без битого пути.
  Полный `tests/test_repo_revision.py` — 10/10. Детали — CHANGELOG [8.25.1].
- **v8.25.0** — CRUD-паритет, батч 1: Обязательство + Актив (MINOR). React-экраны
  `/obligations`/`/banks` были read-only, теперь полный цикл: `shared/ui/Modal.tsx` (первое
  применение `@radix-ui/react-dialog`), формы с валидацией, мягкое удаление с `toast.undo`
  (самозакрывается по клику), `Button variant="danger"`. До сдачи design-critic (12 блокеров)
  и a11y-auditor (3 нарушения AA) нашли и починили: хардкод-цвет оверлея/слой модалки = слою
  полей внутри, `0.078*100===7.800000000000001` в поле ставки при правке, дублирование
  `entityForm.css`/`AuthLayout.css` (CMP-01, объединены в `field.css`), отсутствие `:disabled`
  у кнопок, фокус после удаления строки проваливался в `<body>`, восстановление не
  объявлялось скринридеру, остаток долга не показывался в строке списка. Vitest 187/187
  (было 175). Проверено живьём в браузере, обе темы. README обновлён (не менялся с v8.9.1).
  Детали — CHANGELOG [8.25.0].
- **v8.24.1** — Подготовительный патч перед CRUD-паритетом (PATCH). Три вещи до батчей §8.2:
  гейт `require_financial_consent` на `budgets_router` (был единственным без него); мягкое
  удаление + `restore` для бюджета (миграция `0034`, симметрия с остальными четырьмя
  сущностями) — `create_budget` при оживлении ранее удалённой категории теперь снимает
  `is_deleted` вместо UNIQUE-конфликта; `extractErrorMessage.ts` разбирает объектный 403
  гейта согласия (текст + ссылка на документ плоской строкой, минимум до экрана согласия L3).
  Матрица SQLite+PostgreSQL прогнана (43/43 на PG, `pg_matrix_last_run.md` обновлён). Полный
  pytest 1593/1593 (0 failures/errors, 1 skip), Vitest 178/178. Детали — CHANGELOG [8.24.1].
- **v8.24.0** — §8.4: горизонт + ставка «что если» в прогнозе (MINOR). Объём согласован
  владельцем: select горизонта (3/6/12/24 мес.) + ползунок сценарной ставки `r_bench`
  (дебаунс 500 мс). Параметры движка (α/β Holt, σ Monte-Carlo, CI 80%) сознательно не
  отданы — канон-заморожены. До сдачи design-critic/a11y-auditor нашли и починили: точку
  вместо запятой в деньгах, невидимый для зрячих `isFetching`, поломку вёрстки на узких
  экранах, непомеченный сценарий, потерю фокуса после «Сбросить». Побочно починен
  протухший `docs/api/openapi.json` (пин 114→111) и CJK-канарейка на `frontend/dist/`.
  Проверено живьём в браузере. Vitest 175/175. Детали — CHANGELOG [8.24.0].
- **v8.23.0** — Частичный снос Jinja-фронта перед Э6 (PATCH). Снесено фактически:
  `profile`/`forgot-password`/`reset-password` + мёртвый `templates/index.html` (функциональный
  паритет с React подтверждён). Первый проход по ошибке снёс ещё и `dashboard`/`planning`/
  `transactions`/`obligations`/`goals`/`banks` — их React-версии read-only (`grep useMutation`:
  ноль совпадений), Jinja нёс реальные формы создания. Найдено и исправлено в том же батче
  (`git checkout` вернул файлы/роуты/4 тестовых `PAGES`-списка). Побочная находка: nginx с
  v8.9.0 уже прячет эти формы от обычного доменного трафика (SPA-regex раньше FastAPI) — не
  новый баг, задокументированное с v8.6.0 read-only-решение, продукт не запущен публично.
  Разбор — `docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`. Детали —
  CHANGELOG [8.23.0].
- **v8.22.0** — Экраны входа/регистрации с нуля (ROADMAP §8.4, MINOR). В SPA не было ни
  одного auth-экрана и ни одной формы вообще — построены `LoginPage`/`RegisterPage`/
  `ForgotPasswordPage`/`ResetPasswordPage`, `shared/ui/Toast.tsx` (первое применение
  `@radix-ui/react-toast`), `AuthTopbarLink`. Разбор, как это выпало из плана вехи 8 — в
  CHANGELOG/§0. Пойман и починен реальный баг топбара (stale-if-error в TanStack Query после
  logout). Полный цикл регистрация→логаут→вход→сброс пароля→вход проверен живьём в браузере.
  Vitest 166/166. Детали — CHANGELOG [8.22.0].
- **v8.21.0** — Шкала с зонами (ПДН/автономия) + дисклеймер 601-ФЗ (MINOR). Последний пункт
  плана Э5 — `ZoneScale` (декоративная, aria-hidden) в карточках «Ликвидность»/«ПДН»
  `MetricsGrid`; спокойный дисклеймер о рисках при `Dt > 0.5` (Федеральный закон № 601-ФЗ,
  `DTI_LEGAL_DISCLAIMER_THRESHOLD`). Все шесть пунктов плана Э5 теперь технически закрыты —
  решение владельца нужно перед Э6. Vitest 131/131. Детали — CHANGELOG [8.21.0].
- **v8.20.1** — Переменные модели везде через KaTeX, не удалены (PATCH). Прямая поправка
  владельца по v8.20.0: первый проход ошибочно стёр сырые переменные (`B_liq`, «Rt»/«Lt»)
  вместо типографики — новый `shared/ui/Formula.tsx` (общий ленивый чанк `katex`, не раздувает
  бандл) применён в `MetricsGrid`/`AlternativesBrowser`/легенде `UtilityFormulaBody`; юникодный
  подстрочный индекс — только в нативном `<select><option>` (HTML там не рендерится). Попутно
  починен независимый баг тултипа `ForecastPanel` (пустые строки `Rt_band`/`Rt_p10`). Vitest
  122/122. Сохранено в память — устойчивое правило на будущее. Детали — CHANGELOG [8.20.1].
## §4. Заметки и находки (не переоткрывать)

- **Месячные показатели — за текущий месяц, НЕ за всю историю (v6.4.0):** `calculate_income_total`/
  `calculate_expense_total` суммируют все транзакции; границу месяца ставит `prepare_data(period_days=30)`
  (единственная точка — у неё ровно 3 вызова, всем нужен месяц). У кого нужна вся история — явно
  `period_days=None`. Латентный баг «доход = сумма всех месяцев» закрыт.
- **Прогноз — на РЕАЛЬНЫХ транзакциях, костыль убран (v6.4.0):** синтетического канала истории больше нет;
  `build_monthly_history` бинует полный список по скользящим 30-дневным окнам (устойчиво к границам месяцев),
  последний бин = месячный тотал. Демо-кейсы получили реальную многомесячную историю (`_history_transactions`,
  `_case_data`) → «Валидация» и `/planning` читают один источник. Признак костыля был: одна сущность считается
  по-разному в двух местах.
- **`run_planning` требует у каждой цели ключ `id` (v6.4.0):** в проде его проставляет сериализатор;
  при прямом вызове (демо/валидация) — проставлять синтетически (`g{i}`), иначе `KeyError: 'id'`.
- **`goals.deadline` — NOT NULL (v6.4.0):** бессрочные цели на уровне БД пока не поддержаны; в демо —
  дальний горизонт (10 лет). Nullable-дедлайн = отдельная миграция (пункт роадмапа).
- **`build_monthly_history` теперь по СКОЛЬЗЯЩИМ окнам, не календарным (v6.4.0):** тест, кодировавший
  календарное поведение, обновлён (`test_forecast_shape`). Красный тест после осознанной смены поведения —
  сверять «кто прав» (тест или код), а не рефлекторно чинить код.
- **Голый `TestClient(app)` НЕ создаёт таблицы (v6.4.0):** для разовых end-to-end прогонов —
  `Base.metadata.create_all(bind=engine)` на временной SQLite; в тестах это делает фикстура.

- **Эталон экспертизы регенерируется ТОЛЬКО генератором v1 (v6.0.0):** `knowledge/model_validation/joined.csv.gz`
  восстановим строго `PortraitGenerator(seed=20260702, version=1)` — v1 в `tools/portrait_testing/generator.py`
  заморожен бит-в-бит ради этого (self-check стенда: статус 12000/12000). Любые новые прогоны — генератор v2
  (default). Раннер: `--gen-version`. Не «чинить» v1 и не менять его RNG-поток.
- **Инвест-транш — разметка, не четвёртое направление (v6.0.0):** решётка 66 альтернатив и инвариант сумм I4
  не тронуты; транш вычисляется поверх `x_reserve` сверх Lt*·Σe (`app/core/investment.py`). В метрике согласия
  с экспертами транш считается в бакет goals+ (семантика заключения: цели и инвестиции объединены).
- **Floor резерва — лексикографический уровень, не вес (v6.0.0):** сортировка ranked — по (floor_level, utility);
  `best.utility` может быть НЕ максимумом по списку — это норма (инвариант I5 обновлён, I13 добавлен).
  Golden-снапшоты при изменениях модели пересматриваются глазами (floor-эффект задокументирован в сценариях).

- **GitHub-тулы — разбор витрины Actions (2026-07-05, вахта M):** трёхуровневый CI (`ci.yml`: fast/full/deep,
  в full уже `bandit`+`pip-audit`+locust, в deep — `mutmut`+стресс-property) закрывает потребности продукта.
  Из marketplace-предложений **почти всё мимо**: Fortify (платный SAST — `bandit` покрывает Python),
  Jscrambler (JS-обфускация не наш кейс), SLSA (оверкилл до запуска), деплой-шаблоны облаков (RU-деплой на
  VPS+docker, санкционные облака нерелевантны), Django/Pylint/Python-app шаблоны (наш CI сильнее),
  Greetings/Labeler/AI-summary (соло-разработчик). **Что реально стоит включить (бесплатно, БЕЗ CI-минут):**
  1) **Dependabot alerts** — тумблер в Settings→Security обеих реп (и приватной). Именно **alerts**, НЕ
     update-PR: авто-PR конфликтуют с нашим forward-build (rsync `--delete` из архивов перетёр бы мердж).
     Закрывает окно между тегами, когда `pip-audit` (только в full-тире, на тегах) молчит.
  2) **Secret scanning + push protection** — Settings публичного зеркала (бесплатно на паблике).
  3) **CodeQL** — бесплатен ТОЛЬКО на публичных репо → внедрён этим релизом как `mirror_extras/…/codeql.yml`
     (в приватный не кладём: без GHAS воркфлоу падал бы). Портфолио-бейдж + межпроцедурный SAST сверх bandit.
  Вывод: писать новые воркфлоу не нужно — три тумблера в Settings + CodeQL уже в зеркале.

- **PIT-009 (2026-07-02, корень подтверждён — Tor Browser; инцидент ЗАКРЫТ):** качать артефакты в обычном браузере + VPN. Три фейла за день: карточки
  не появились (v5.12.0), «мёртвая заглушка» — кнопка есть, скачивание падает (v5.14.0), пропажа
  файлов владельца «вверх». Протокол: доставка не подтверждена до скачивания; outputs не трогать;
  перевыдача → копия под НОВЫМ именем (выданный путь неприкасаем!) → критический минимум;
  sha256 каждой выдачи в чат; владельцу на Tor — новая цепочка, поштучно, сразу. Разбор:
  `docs/reports/incidents/inc_chat_file_delivery_dead_stub.md`.

**PIT-008 (v5.13.0):** `find && echo ЕСТЬ` печатает «ЕСТЬ» и без находок (exit 0 всегда) — так чуть
не потеряли методичку роадмапа между линиями. Существование проверять `test -f` / непустым выводом.
Детали — `docs/pitfalls.md`.

**Рассинхрон flake8-гейта (app/ vs весь проект) — вскрыт и закрыт в v4.30.2.** Гейт flake8 определён в
ТРЁХ местах, и все гоняют `flake8 .` (весь проект): CI `ci.yml:80`, `Makefile` цель `lint`, pre-commit
hook. А руками каждый батч гоняли `flake8 app/` (узко) — поэтому `app/` держали в нуле, а `tests/`/
`loadtest/` незаметно копили E501/F841 (101 нарушение к v4.30.1). Официального GitHub-репа ещё нет →
полный гейт вживую ни разу не запускался, первый CI-run был бы красным. **Урок: гейт руками гонять ТЕМ
ЖЕ скоупом, что CI** — для flake8 это `flake8 .`, не `flake8 app/`. Выровнено вариантом A (реальное
приведение к 0, не игнор-конфиг): autopep8 --select по коду, точечный `# noqa: E501` по data-литералам
(enforcement нового кода жив), XML-фикстура → конкатенация. При merge-боли откатный путь — per-file-
ignore E501 для tests/ в `.flake8` (вариант C). Заодно убран `black` из pre-commit — прямое нарушение
§2 (латентная merge-мина: `make precommit` переформатировал бы весь проект).
> **Рецидив в v5.20.0 (тот же класс):** вахта J в v5.19.0 внесла `tools/portrait_testing/render_portrait_cards.py`
> с 27 нарушениями (26×E501 + 1×E741) — снова проверяли узким `flake8 app/`, а не `flake8 .` (CI-скоуп,
> `continue-on-error`, поэтому красный лог не заметили). Лечение вариантом C (§4 fallback): `per-file-ignores`
> в `.flake8` — строки >100 это выходные литералы досье-карточек, перенос менял бы текст эксперту, реформат
> запрещён (§2). Инвариант «flake8 репо = 0» восстановлен; файл J не тронут; ядро papки под гейтом (инъекция
> подтвердила). **Урок повторно: перед выдачей ВСЕГДА `flake8 .`, не `flake8 app/` — даже для docs/tooling-батча.**

**Текущая фаза планирования (что согласовано с Василием):**
- Карта версий 4-9 подтверждена; правило «вехи ≠ SemVer» — осознанное решение Василия.
- Фронт-стек React+TS зафиксирован с обоснованием — `docs/reports/adr/adr_001_frontend_stack.md`.
- Техдолг (mypy/flake8, utcnow, ExpenseRecord) РАЗБЛОКИРОВАН: работаем с конечной версией
  последовательно, параллельных правок нет → конфликтов мёржа не будет.
- Принцип заглушек вехи 4: MVP/каркас для того, что в роадмапе и точно нужно фронту; спекулятивное
  не выдумываем (YAGNI).

**Мат-модель — канон `docs/model/history/math_model_v3_0_0.md`** (источник истины — код): 66 альтернатив, шаг 10%,
stock-based `Lt = B_liq/E` (месяцы), L_min=0 (фильтр выключен), MC-интервал 80%, инварианты Rt≥0 и
ПДН≤0.40. Если всплывёт спека с 21 альтернативой / шаг 20% / flow-based Lt / 95% — это УСТАРЕВШАЯ
v2.0.2, не источник истины.

**Время — единый контракт (после BUG-026, v4.25.3):** всё «сейчас» в коде идёт через
`app/utils/time.py::utcnow()` (naive UTC). Прямой `datetime.now()` (локаль сервера) в `app/` —
запрещён, он давал 3ч-сдвиг на MSK-VPS. Исключение — намеренные aware-вызовы `datetime.now(timezone.utc)`
в `cbr_rate`/`cbr_fx`/`security` (там нужен aware UTC). Колонки моделей — naive UTC (вариант A, не aware).
Костыль `obligation.replace(tzinfo=None)` оставлен намеренно — гасит aware-вход из Pydantic, это НЕ про
«сейчас». Полная tz-корректность (зона юзера → локальный календарь для дни-недели/внутримесячных
паттернов) — отдельный заход в слое паттернов, там же заведётся поле `tz` у User (сейчас его нет).
Вариант B (миграция timestamp-колонок на `timezone=True`) НЕ нужен для корректности приложения —
только для интеропа БД у вехи деплоя; шов готов (убрать `.replace(tzinfo=None)` в helper). Полное
обоснование развилки A/B (почему B — не требование корректности) — `docs/reports/adr/adr_002_datetime_storage.md`.

**JWT-ревокация (v4.26.0, раздел 4.4) — устройство и контракт.** Логика отзыва централизована в
`app/database/revocation.py`: точечный рубеж — блок-лист `is_token_revoked`/`revoke_token`/`purge_expired`;
массовый — предикат `tokens_invalidated_for(user, iat)` + `bump_tokens_valid_since`; конвертер
`epoch_to_naive_utc` нормализует JWT-таймстампы (int/float/aware-datetime) в naive-UTC. `dependencies`
и `routes_auth` таймстампы НЕ трогают — только зовут модуль (`revoke_token` принимает сырой `exp`).
Рубеж `tokens_valid_since` режется до секунды (iat — секундная гранулярность), сравнение строгий `<` —
немедленный re-login после logout-all проходит. Легаси-токены без `jti`/`iat` остаются валидными
(обратная совместимость, покрыто тестом).

**Инвалидация сессий при смене/сбросе пароля (v4.27.0, раздел 4.4) — устройство и дизайн-выбор.**
Подключена к тому же массовому рубежу `tokens_valid_since`. `change_password`: `bump_tokens_valid_since`
(валит другие устройства) + `_revoke_request_token` (точечно гасит текущий токен по `jti` — закрывает
sub-секундное окно округления рубежа: токен той же секунды иначе пережил бы строгий `<`) + чистка
auth-cookie + событие `password_changed`. `reset_password`: только `bump` (юзер не залогинен; текущего
токена/cookie нет; вход новым паролем проходит сразу — округление рубежа не задевает свежий токен).
Изменения ТОЛЬКО в `routes_auth.py`, миграций нет (механизм из 0027 переиспользован). **Дизайн-выбор
Option A — намеренный, НЕ переоткрывать:** для финпродукта (банковская планка) смена креденшелов валит
ВСЕ сессии, включая текущую — сильнее и ожидаемее «оставить текущую» (Google/GitHub). Option B
(сохранить текущую сессию) сознательно отклонён: требует ре-выпуска токена + нового cookie + чтобы
фронт подхватил новый токен, а фронт — отдельная веха (пока Jinja+vanilla JS). Инвалидация привязана к
УСПЕХУ смены (verify + update прошли); неверный текущий пароль → 400, рубеж не сдвинут. Карта линии для
слияния с другими вахтами — `docs/reports/merges/merge_manifest_v4_27_0.md`.

**Ротация ключа шифрования (v4.28.0, раздел 4.4) — устройство и эксплуатация.** `TokenCipher` под
капотом — `MultiFernet`. Ключи берутся из `settings.token_encryption_keys_list`: `TOKEN_ENCRYPTION_KEYS`
(через запятую, PRIMARY первым) → один `TOKEN_ENCRYPTION_KEY` → derive из `JWT_SECRET`. `encrypt` всегда
шифрует PRIMARY (первый ключ), `decrypt` перебирает весь набор, `rotate` перешифровывает токен primary.
Перешифровка существующих данных — `app/database/reencrypt.py::reencrypt_all`: колонки находятся
**интроспекцией** маппинга (`isinstance(col.type, EncryptedString)`), поэтому новое шифрованное поле
попадёт в перешифровку без правки модуля — не вести ручной список. Переписывает и старо-зашифрованные
(читаются прежним ключом набора), и legacy-незашифрованные (читаются как есть → шифруются primary).
**Эксплуатация ротации без простоя:** (1) сгенерировать ключ `Fernet.generate_key()`, поставить ПЕРВЫМ в
`TOKEN_ENCRYPTION_KEYS`, старый оставить вторым; (2) `python scripts/reencrypt_keys.py`; (3) убрать
старый ключ из набора. Миграций схемы нет — формат хранения тот же, меняется только ключ. `EncryptedString`
сохраняет обратную совместимость (decrypt вернул None → отдаёт значение как есть), так что включение
шифрования на ранее незашифрованных данных безопасно.

**Security-регрессии и старт-гард ключа (v4.29.0, раздел 4.4) — защитная сетка, не ослаблять.**
`tests/test_security_regressions.py` фиксирует инварианты харденинга: (1) security-заголовки на ЛЮБОМ
типе ответа — работает потому, что `SecurityHeadersMiddleware` зарегистрирован ВНЕШНЕ `CSRFMiddleware`
(порядок `add_middleware` в `main.py`: Rate→CSRF→SecurityHeaders→Logging; внешний обрабатывает response
последним), поэтому даже 403 от CSRF проходит через установку заголовков. Не менять порядок middleware
вслепую — заголовки на ошибках/отказах слетят. (2) CSRF-блок на всех изменяющих методах, с исключениями
Bearer/api-key/без-Origin (срабатывает по методу ДО роутинга — тестируется на любом пути). (3) **Старт-гард
ужесточён:** в production обязателен явный `TOKEN_ENCRYPTION_KEY` или `TOKEN_ENCRYPTION_KEYS` — без него
ключ шифрования деривится из `JWT_SECRET`, что лишает смысла ротацию (v4.28.0) и разделение ключей. Это
**намеренное требование деплоя** (`.env.example` обновлён) — не откатывать ради удобства локального прода.

**MFA/TOTP (v4.30.0, раздел 4.4) — устройство и инварианты.** Второй фактор на `pyotp`. Состояние на
User: `mfa_secret` (зашифрован `EncryptedString`) + `mfa_enabled`; пока `mfa_secret` задан, а
`mfa_enabled=False` — это «pending» (enroll сделан, confirm нет). Крипто — `services/mfa.py`, доступ к
данным — `database/mfa_store.py` (разделение как у revocation). **Ключевой инвариант — двухступенчатый
логин:** при `mfa_enabled` `/auth/login` после верного пароля НЕ ставит cookie и НЕ выдаёт сессионный
токен — возвращает `mfa_pending`-токен (purpose-claim, TTL 5 мин); полную сессию выдаёт только
`/auth/mfa/verify` после TOTP/recovery-кода. **Не выдавать сессию в login при включённом MFA** — это
вся суть фичи. Recovery-коды: 10 шт, bcrypt-хеш, одноразовы (`used_at`), показываются один раз при
confirm; и verify, и disable принимают TOTP ИЛИ recovery-код. **Связанный инвариант (purpose-guard):**
`get_current_user` отвергает ЛЮБОЙ токен с claim `purpose` — полная сессия его не имеет. Это держит
mfa_pending (и заодно email_verify/password_reset/telegram_link) вне сессионного доступа; не ослаблять
эту проверку, иначе промежуточный/одноразовый токен станет полноценным входом.

**Консолидация трёх параллельных линий v4.26.0 (правило §5 #15).** JWT-ревокацию делали параллельно
три вахты (M, J, S). Эта версия — их слияние по лучшим практикам, не переписывание: ядро `revocation.py`
(инкапсуляция + предикат + конвертер) и тест-набор — из линии M (самый полный, 17 кейсов, форж токена с
прошлым iat для кросс-сессионного logout-all); DRY-хелпер `_revoke_request_token` в routes — из линии J;
обвязка релиза (CHANGELOG/ROADMAP/WATCHLOG) — из S. PIT-004 (распознать сбой канала) восстановлен по
линии M, PIT-005 (на возобновлении верь архиву) — урок линии J. Модели/`security.py`/миграция 0027 у
всех трёх совпали — мёрджить нечего.

**Консолидация S+J (v4.27.0→v4.28.0).** Линия S наработала только docs-добавки поверх J-базы:
`docs/pitfalls.md` (PIT-006 — `cd X && job &` фонит весь пайплайн вместе с `cd`, приоритет `&` ниже
`&&`) и `SECURITY.md` (строка «Отзыв сессий» в таблице §2 + раздел §3a «Харденинг сверх минимума»).
Сверено `diff` против J-версий: расхождение только в этих аддитивных вставках, иного дрейфа нет —
файлы внесены к J без конфликтов. Код S и J не расходился (S работал по докам). Внесено в архив v4.28.0.

**Известные баги к разбору в вехе 4** (давние заметки Василия): (1) ставка ЦБ всегда ставит дефолт —
реального fetch нет (вероятно cbr.ru 403 → зеркало `cbr-xml-daily.ru`); (2) письма «отправлены», но
на почте пусто (SMTP no-op в dev маскирует). Диагностика до корня, не угадывать.

**Открытые вопросы для вехи 6** (тестирование): график прогноза — у Василия ощущение что баг (всегда
прямая линия от константы-зарплаты); нужен тест-кейс где НЕ прямая. Совет при `Rt<0` — исследовать
(реальный кейс Василия с кредиткой). Шаг 5% вместо 10% — замерить скорость.

**PostgreSQL поднимается в песочнице** (не спихивать на разработчика): `apt install postgresql` БЕЗ
`apt-get update` ПРИ ОТКЛЮЧЁННОМ nodesource (он даёт 403 и травит весь update; Ubuntu-репы рабочие —
`mv /etc/apt/sources.list.d/nodesource.sources{,.disabled}`, затем `apt-get update` уже проходит) →
`pg_ctlcluster 16 main start` → роль/БД через `su postgres -c psql` →
`DATABASE_URL=postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test`.
**PG-хвост ЗАКРЫТ (v4.30.1):** вся цепочка 0001↔0028 прогнана вживую на PG 16 — `upgrade head` ✓,
`downgrade base` ✓, обратно ✓; миграции 0023–0028 аддитивны и реверсивны. Регрессия зафиксирована
`tests/test_migration_reversibility.py` (под PG; скип на SQLite). Больше PG-миграций в очереди нет.

**BUG-027 (v4.30.1) — reverse-каст 0009 на PostgreSQL.** `downgrade()` миграции 0009 кастует
`user_id` varchar(36)→integer для `events`/`recommendations` без `USING` → на PG `DatatypeMismatch`
(SQLite молчит — динамическая типизация). Фикс: `postgresql_using="user_id::integer"` на обе колонки
(на пустой таблице каст — no-op, проходит). Трогает ТОЛЬКО downgrade-ветку 0009, upgrade не изменён →
накатанные базы не задеты. Это была единственная нереверсивная точка цепочки; найдена код-ревью +
живым PG-прогоном, не на проде.

**Сбой канала тул-коллов — методичка `docs/tool_call_channel_failures.md` (v4.30.1).** Развёрнутый
PIT-004: вызов инструмента иногда выходит в ответ ВИДИМЫМ текстом (`court` + сырой `<invoke>`) и ход
обрывается, но песочница жива. Кейсы: завершение хода вызовом, сложная команда (heredoc/кавычки),
**заражение паттерном из истории** (сломанные хвосты в контексте воспроизводятся как текст), короткий
ход между обрывами. Протокол: НЕ завершать ход вызовом (кончать текстом + `present_files`),
игнорировать сломанные образцы из истории, весь батч одним ходом, при обрыве — реран с проверки
состояния (не полный hand-off — это лечится рераном). Не объяснять глюк по пять раз.

**Грабли песочницы** (детали — `docs/sandbox_runbook.md`): полный одновременный прогон всех тестов
рубится лимитом инструмента (~30с → returncode -1) → гонять пофайловыми группами; итоговая строка
pytest не флашится в файл → использовать `--junitxml` + парсить XML + exit code; фоновые процессы не
флашат лог надёжно; многострочные bash с `\` ломаются; браузеры firefox/webkit недоступны (Azure CDN),
chromium есть.

---

## §5. Жёсткие правила (чек-лист «не дрейфовать»)

1. **TDD** — тест → красный → реализация → зелёный, реально прогонять.
2. **`app/core/` не трогать** без согласования (исключение — `spending_advice.py`).
3. **Схемные изменения — на PostgreSQL** (SQLite молчит про FK).
4. **SemVer внутри вехи:** фича=MINOR, фикс=PATCH. Bump `APP_VERSION` + `CHANGELOG.md` каждый батч.
   Вехи (v4-v9) — крупные этапы, не классический SemVer (см. §2).
5. **Без массового реформата** кроме координированных окон (сейчас окно открыто — техдолг закрываем).
6. **ROADMAP — источник приоритетов, строго по порядку.** Не изобретать параллельную нумерацию.
7. **Без эмодзи.** Русская проза + английский код. По-братски, прямо, без канцелярита.
8. **НЕ объявляй невозможным, не проверив.** PostgreSQL/chromium/apt поднимаются здесь. Реально
   внешнее (cbr.ru 403, живой SMTP, деплой) — честно отметить.
9. **Перед «с нуля» — проверь, не сделано ли уже** (бэк часто опережает план).
10. **Вывод тестов — через `--junitxml`** (не `tail` по логу): итоговая строка не флашится надёжно.
    Парсить XML + exit code. (§4, `docs/pitfalls.md`.) **Что гонять и когда (узкий цикл vs полный) —
    `docs/test_run_optimization.md`; механика полного fast — `docs/sandbox_runbook.md` §7.**
11. **НИКОГДА не писать имена в гите / релизах / публичных артефактах** — официально безымянно.
12. **Тестирование мат-модели — СТРОГО вне проекта** (Opus 4.8, без памяти/инструкций → не biased).
13. **E2E добавляются с unit** для изменений фронта/API; CRUD-флоу обязаны иметь E2E.
14. **Разборы — АВТОМАТИЧЕСКИ по триггеру:** дефект в коде → инцидент (`docs/reports/incidents_summary.md`);
    искали, но дефекта нет → расследование (`docs/reports/investigations_summary.md`); повторная ошибка
    воркфлоу → pitfall (`docs/pitfalls.md`); найденный баг → карточка по `docs/reports/bug_report_template.md`.
15. **Слияние разошедшихся линий → консолидация, не переписывание истории.** Полная процедура,
    гейты и антипаттерны — **`docs/reports/merges/merge_and_fork_guide.md`**. Профилактика: синкать аккаунты чаще,
    координировать номера версий (версия закреплена за задачей, не за аккаунтом).
16. **Чекпоинт-дисциплина (непрерывность).** Не копить работу до конца батча: как только есть
    рабочее состояние — собрать архив + WATCHLOG и отдать через `present_files`. В начале крупной
    задачи — оценка объёма и **вероятность уложиться**; по ходу — переоценка, при росте риска
    немедленный чекпоинт. Готовое отдавать прежде тяжёлого хвоста. Протокол — `docs/session_continuity.md`.
18. **Исходник первичного материала — в репозиторий ВСЕГДА и ПЕРВЫМ.** Отчёты глубоких
    исследований, ответы независимых экспертов, чужие выгрузки замеров, длинные логи — всё, что
    получено извне и не воспроизводится повторно бесплатно, сохраняется **дословно** в
    `docs/research/raw/` (или профильный `raw/`) с датой в имени, ДО написания выжимки. Реестр URL
    исходник не заменяет: ссылка отвечает «где это лежит», исходник — «что там было написано на
    момент чтения»; первое протухает, второе нет. Пересказ без сохранённого исходника —
    нарушение, даже если пересказ хороший. Почему правило появилось — `docs/pitfalls.md` PIT-013.
17. **Отчёты — по единому диспетчеру** `docs/reports/reporting_policy.md` (триггер → тип → шаблон →
    папка → реестр). Конвенция имён всего — `docs/naming_convention.md`.
19. **Решение за владельцем → структурный разбор, а не вопрос в чате.** Стратегия, архитектура
    или необратимая правка данных, где последнее слово за владельцем, оформляется документом в
    `docs/reports/decisions/`: что предлагается · фактические изменения с ЗАМЕРЕННЫМИ числами ·
    полная стоимость, включая неочевидную · что необратимо · не меньше двух вариантов, включая
    «ничего не делать» · явная рекомендация вахты с обоснованием · что изменило бы рекомендацию.
    Вахта не решает молча, даже если ответ кажется очевидным. Шаблон и статусы —
    `docs/reports/decisions/README.md`.

---

## §6. Стабильная библиотека (читать из архива `/docs`; НЕ дублировать сюда)

| Файл | Зачем |
|---|---|
| `docs/ROADMAP.md` | **Направление: вехи 4-9, все задачи** (первоисточник приоритетов). |
| `docs/sandbox_runbook.md` | Грабли песочницы + готовые команды (bootstrap, тесты, E2E, PG). |
| `docs/RELEASES.md` | История развития по эпохам (v1→v4). |
| `docs/reports/adr/adr_001_frontend_stack.md` | Обоснование React+TS (веха 8). |
| `docs/reports/adr/adr_002_datetime_storage.md` | Обоснование naive-UTC (вариант A) и отказа от aware-миграции (B). |
| `docs/reports/adr/adr_template.md` | Шаблон для фиксации архитектурных решений. |
| `docs/model/history/math_model_v3_0_0.md` | Историческая мат-модель (источник истины по параметрам на тот момент). |
| `docs/algorithm_stack.md` | Как мат-модель работает по шагам в коде (9-шаговый конвейер). |
| `docs/diagrams.md` | Mermaid-диаграммы (архитектура, ER, конвейер ядра, потоки). |
| `docs/ui_visual_design_brief.md` | Эстетическое ТЗ фронта (веха 8). |
| `docs/engineering_practices.md`, `docs/CONTRIBUTING.md` | Инженерные решения + процесс (TDD, гейты, DoD). |
| `docs/reports/merges/merge_and_fork_guide.md` | **Как сливать/форкать разошедшиеся линии одной версии между аккаунтами** (процедура, гейты, антипаттерны; раскрывает §5 #15). |
| `docs/reports/merges/merge_manifest_v4_27_0.md` | **Прикладная карта линии v4.27.0 для слияния** (surface, зоны конфликта, инварианты I1–I6, чек-лист) — инстанс гайда под этот батч. |
| `docs/test_run_optimization.md` | **Что гонять и когда:** узкий цикл vs полный fast — как не ждать ~9 мин на каждой правке. Механика полного — runbook §7. |
| `docs/ui_ux_design_standard.md` | Полная спецификация UI/UX (токены, состояния, a11y). |
| `SECURITY.md` (корень) | Единый security-док: меры (ссылки на код), что осталось, домены ревью, план реагирования, тезисы. |
| `docs/cybersecurity_finpilot_checklist.md` | Прикладной чек-лист ИБ (что закладывать, приоритеты). |
| `docs/glossary.md` | Термины (Rt, Lt, Dt, BLR, Sn…). |
| `docs/QA.md`, `docs/reference_profiles.md`, `docs/reports/bug_report_template.md` | QA-уровни, эталоны, формат багов. |
| `docs/reports/incidents_summary.md`, `docs/reports/investigations_summary.md`, `docs/pitfalls.md` | Реестры (инциденты / расследования / частые ошибки). |
| `docs/reports/report_types.md`, `docs/reports/incident_postmortem_guide.md`, `docs/reports/investigation_report_guide.md` | Роутер типов разбора + шаблоны. |
| `docs/documentation_methodology.md` | Общая теория (blameless, RCA, runbook, ADR/RFC, SLO). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
| `docs/legal_questions_for_lawyer.md` | Вопросы юристу (веха 8). |
