# FINPILOT — Вахтенный журнал (быстрый снимок «где мы сейчас»)

> # [!!!] ПРОТОКОЛ НЕПРЕРЫВНОСТИ — ЧИТАЙ ПЕРВЫМ, ЭТО ОБЯЗАТЕЛЬНО ДЛЯ КАЖДОГО КЛОДА
> **Не копи работу до конца батча. Собирай рабочий архив РАНО и ЧАСТО и отдавай через `present_files`.**
> Токены/контекст кончаются внезапно — если архив не скинут, работу нельзя ни закончить, ни передать
> на вахту, и всё встаёт на часы. Поэтому:
> 1. В начале крупной задачи — оценка объёма + **вероятность уложиться** (высокая/средняя/низкая).
> 2. Готовое отдавай ДО того, как браться за тяжёлый/творческий хвост.
> 3. По ходу переоценивай «успею/нет»; при росте риска — **немедленный чекпоинт-архив**.
> 4. WATCHLOG держи актуальным ПО ХОДУ (не в конце) — это точка передачи на другой аккаунт.
>
> **Полный протокол — `docs/session_continuity.md`. Нарушение = потеря работы и простой. Не пропускай.**

> **Что это и зачем.** Снимок-контекст для мгновенного входа в работу. Василий ведёт продукт на
> **4 аккаунтах Claude (V, J, M, S)** и в нескольких чатах внутри проекта; токены кончаются —
> вахту принимает другой аккаунт/чат. Этот журнал даёт принимающему быструю сводку: **где стоим,
> что сделано, что дальше, на какие грабли не наступать** — чтобы поднять картину с ОДНОГО архива,
> не читая 20k строк кода. (Метафора в тему бренда — штурман у штурвала.)
>
> **Принцип тонкости.** Журнал НЕ дублирует историю. Полная история версий — в `CHANGELOG.md`
> (каждый патч) и `docs/RELEASES.md` (эпохи). Направление и задачи — в `docs/ROADMAP.md`. Здесь
> только выжимка + ссылки.
>
> ## [!] ЖЁСТКОЕ ПРАВИЛО: §3 хранит РОВНО 10 последних версий, не больше
> §3 — это окно из **10 самых свежих версий, и только**. Добавляя новую версию сверху, СРАЗУ удаляй
> самую старую снизу, чтобы их всегда оставалось 10. Без этого журнал быстро разрастается и теряет
> смысл «быстрого снимка». Удалённое не пропадает — вся история живёт в `CHANGELOG.md` и
> `docs/RELEASES.md`. Это правило касается ИМЕННО этого файла: здесь старое именно УДАЛЯЕТСЯ.

> ## [!] Журнал ведётся НЕПРЕРЫВНО, по ходу работы
> Обновляется СРАЗУ по ходу (сделал подшаг → §0; нашёл грабли/факт → §4), а не «в конце сессии» —
> токены могут кончиться в середине батча. В любой момент журнал обязан отражать реальную текущую
> точку. **См. протокол непрерывности — `docs/session_continuity.md` (и баннер вверху файла).**

> **Как пользоваться (Василий).** Новому чату/аккаунту: этот файл (или его текст) + последний архив
> → «продолжай». Копия в чат — с версией в имени: `WATCHLOG-personal-finance-dss-vX.Y.Z.md`
> (канон — дефисы, точки в версии, см. `[[feedback_finpilot_archive_naming]]`). Архивная копия —
> `docs/WATCHLOG.md` без версии.

> ## ВЕРСИЯ КОДА: `v9.7.0` · **КАНОН v3.9.0 · §8.2: CRUD 5/5, КАРКАС, E2E ЗЕЛЁНЫЙ,
УВЕДОМЛЕНИЯ, ИСТОРИЯ, ВЫГРУЗКА, СЕМЬЯ, РЕФЕРАЛКА, ШРИФТ, ЮРБЛОК, ПАРАМЕТРЫ РАСЧЁТА,
ИМПОРТ ВЫПИСКИ + H3 ЗАКРЫТА, ЮРТЕКСТЫ В REACT, SPA РЕАЛЬНО ОТДАЁТСЯ,
ПЕСОЧНИЦА ВЕРНУЛАСЬ,
JINJA СНЕСЕНА, МЕТРИКИ ВЛАДЕЛЬЦА, A/B-РЕЗУЛЬТАТЫ (v8.49.0).
🔴 §8.2 НЕ ЗАКРЫТ: SPENDING-ADVICE ТОЛЬКО В API (найдено аудитом 05.09.2026)**
>
v8.42.0 — параметры расчёта плана появились в React. До этого батча риск-профиль,
минимальную ликвидность и ставку изменить было НЕЛЬЗЯ ВООБЩЕ: контролы жили только
в Jinja, и человек видел рекомендацию, посчитанную по чужому умолчанию. Остаток H3
разложен по риску: `plaid_router` под гейт закрыт сразу (у него нет фронта вовсе, значит
оговорка v8.27.0 к нему не относится), `banks_router` привязан к переносу импорта выписки —
его зовёт `app.js`, который не умеет показать 403 согласия. Снос Jinja-остатка отложен:
футер L7 ссылается ровно на Jinja-роуты `/legal/*`. Агенты опровергли обоснование
собственного решения вахты и нашли восемь дефектов, главный — успех объявлялся раньше,
чем план пересчитан. Backend 1741/1741, Vitest 401/401, E2E 60/60.
Полная деталь — CHANGELOG [8.42.0].

v8.41.0 — юрблок фронта закрыт целиком. Экран согласий знал ОДИН тип из трёх: отозвать
согласие на рассылку было невозможно, хотя 152-ФЗ даёт это право каждому. Теперь список
строится из ответа `/consents` со всеми типами, включая невыданные; названия и адреса
документов — из реестра, новый тип появится на экране сам. Схема `ConsentsResponse`
заведена на бэкенде до фронта. Два новых гейта проверены мутацией: сверка перечисления
типов фронт↔бэкенд и запрет предотмеченных чекбоксов (L2 соблюдалось, но держалось
на внимательности, а не на механизме).
Backend 1727/1727, Vitest 382/382, E2E 58/58. Полная деталь — CHANGELOG [8.41.0].

v8.40.0 — юридический контур фронта. Закрыты три требования, без которых нельзя
запускаться в РФ: дисклеймер 39-ФЗ на экране рекомендаций (приходит полем ответа, а не
перепечатан во фронте), cookie-баннер с раздельным выбором и привязкой к редакции
политики, футер с документами на каждой странице включая гостевые. Попутно закрыт долг
«генерировать plan-summary из контракта» — он вскрыл семь мест, где код звал
необязательные поля как обязательные. Агенты нашли восемь блокеров, главный — согласие
оформлено `primary`, отказ `ghost`, и фокус ставился на согласие: тёмный паттерн ровно
там, где требование его запрещает. Браузерный тест поймал два расхождения, невидимых
юнит-тестам. Backend 1713/1713, Vitest 380/380, E2E 57/57.
Полная деталь — CHANGELOG [8.40.0].

v8.39.0 — шрифт продукта наконец существует. `--font-body: "Manrope"` был объявлен
токеном и не загружался ниоткуда: ни `@font-face`, ни `<link>`, ни пакета — весь SPA
двадцать восемь версий рисовался системным шрифтом. Подключён самохостом, а не с CDN:
рынок РФ (доступность чужого домена не гарантирована), CSP вехи 9, один запрос вместо
двух. Гейт под это сначала оказался слепым — искал подстроки и находил их в комментариях,
объясняющих самохост; мутация со снятием пакета прошла незамеченной. Переписан на разбор
кода. Backend 1701/1701, Vitest 361/361, E2E 54/54. Полная деталь — CHANGELOG [8.39.0].

v8.38.0 — удаление снимка плана стало обратимым. Продукт до этого говорил на двух языках:
у целей, активов, обязательств, операций и бюджетов есть «Вернуть», а у снимков плана
удаление помечалось мягким и не возвращалось никак — данные оставались в базе
недостижимыми. Оценка задачи из предыдущего указателя («изменение схемы, матрица
SQLite+PostgreSQL») опровергнута сверкой с диском: колонки уже были, хватило запроса
и эндпоинта. Скоуп владения взят тот же, что у удаления, и подтверждён мутацией.
Backend 1699/1699, Vitest 361/361, E2E 53/53. Полная деталь — CHANGELOG [8.38.0].

v8.37.0 — рефералка вынесена на фронт секцией профиля. Главная находка: ссылка
`/register?ref=CODE` открывалась, регистрация проходила — и не засчитывалась никому,
потому что `RegisterPage` не читал параметр `ref`. Программа не работала целиком, и
заметить это можно было только по нулевому счётчику приглашений у всех. Гейт
достижимости расширен второй проверкой: экран обязан не только существовать, но и читать
параметр из ссылки, которую собрал сервер. Схема `ReferralMe` заведена на бэкенде до
фронта. Агенты нашли восемь блокеров, включая второй primary-CTA на странице и побайтовый
дубль блока «ссылка + Скопировать», вынесенный теперь в общий компонент.
Backend 1694/1694, Vitest 360/360, E2E 53/53. Полная деталь — CHANGELOG [8.37.0].

v8.36.0 — семейный доступ (households) вынесен на фронт: двенадцать эндпоинтов бэкенда
существовали, фронта не было ни строки. Главная находка батча — приглашение вело
в никуда: `routes_households.py::_invite_url` строит `/join?token=...` и кладёт этот
адрес в приглашение, а роута `/join` в SPA не существовало вовсе. Дефект невидим при
работе руками: ссылка уходит копированием во внешнюю переписку, и упирался в пустоту
не тот, кто приглашал. Второй случай класса H12 — гейт достижимости расширен на ЛЮБУЮ
собранную на сервере ссылку. Вторая половина той же дыры: токен не переживал вход,
`LoginPage` всегда уходил на `/`; заведён `?redirect=` с проверкой на внутренний путь.
Агенты нашли: удаление участника в один клик рядом с роспуском, который спрашивает;
затирание одноразовой ссылки приглашения; недостижимую роль «наблюдатель»; немой клик;
потерю фокуса после каждого удаления; контраст границы 2.90:1 при пороге 3:1 — во всём
продукте, не только здесь. Backend 1686/1686, Vitest 344/344, E2E 51/51.
Полная деталь — CHANGELOG [8.36.0].

v8.35.0 — выгрузка плана (CSV/XLSX/PDF) вынесена на фронт. Скачивание через fetch+blob,
а не ссылкой: экспорт за гейтом согласия, ссылка скачала бы 403-JSON файлом с
расширением `.csv`. Агенты нашли три ФУНКЦИОНАЛЬНЫХ дефекта, из-за которых фича могла
молча не работать: blob освобождался синхронно сразу после клика (браузер не успевал
начать скачивание — ни файла, ни ошибки), якорь кликался вне DOM (Firefox такое
игнорирует), не было ни таймаута, ни отмены (подвисший сервер запирал все три кнопки
навсегда). Плюс три нарушения AA: успех не подтверждался ничем, фокус проваливался
в body при 403, две соседние кнопки выглядели рабочими и молчали. Всё починено.
Не сделано и записано: три равновесные кнопки вместо иерархии, отсутствие primary на
всём экране, экспорт не знает про согласие, которое экран уже выяснил.
Backend 1684/1685, Vitest 318/318, E2E 46/46. Полная деталь — CHANGELOG [8.35.0].

v8.34.0 — история планов (P2.6) вынесена на фронт. Главное решение батча: схемы ответа
заведены на БЭКЕНДЕ и ДО фронта (`PlanSnapshotSummary` и остальные вместо `dict[str, Any]`),
потому что рукописный тип фронта уже стоил дашборда в v8.31.1. 9 контрактных тестов, из
них четыре — на то, что типизация НЕ изменила поведение; они были зелёными с начала, и это
и есть доказательство. Агенты дали восемь блокеров на один экран: класса `.fp-field` не
существовало в проекте (в продукт уехал бы голый браузерный input с целью нажатия 21px
вместо 44), секция не была карточкой в отличие от всех четырёх соседей, единственный
залитый акцент экрана достался архивированию, ПДН не показывался вовсе при обещании
сравнивать долговую нагрузку, модалка не показывала КАКОЙ снимок удаляют (текст уходил
в sr-only проп), кнопка удаления не блокировалась и второй клик давал ложную ошибку,
список рендерил все 50 снимков. Дата считалась неверно и была третьей копией форматтера —
бэкенд отдаёт наивный UTC без `Z`, общий `formatDate` даёт двойной сдвиг; его дефект
записан в ROADMAP, он касается и других экранов. E2E поймал, что подтверждение не
возвращало фокус: `Modal` управляется пропом, Radix не знает триггера — заведён
`returnFocusTo`, возврат делается в `onCloseAutoFocus`, потому что Radix переносит фокус
ПОСЛЕ `onOpenChange` и внешний вызов молча перетирается. Backend 1681/1682,
Vitest 294/294, E2E 43/43. Полная деталь — CHANGELOG [8.34.0].

v8.33.0 — по прямому требованию владельца: замер времени прогонов и поддерживаемый
справочник «что сколько стоит». Заведено `tools/timing_lab/` + `docs/timing_reference.md`,
таблица в документе обновляется КОМАНДОЙ (`report --update-doc`), а не руками — ручное
ведение уже однажды породило комментарий, год утверждавший неправду (v8.31.1).
Ключевое свойство лаборатории: каждая запись несёт load average, потому что длительность
без него — бессмысленное число. Один и тот же срез `test_api_demo` шёл 40.8 с на спокойной
машине и 83.4 с при load average 74 на 8 ядрах, без единой правки кода. Отсюда: медиана
вместо среднего, спокойные прогоны отделены от перегруженных (но не выброшены — они
показывают худший случай), порог перегрузки ОТНОСИТЕЛЬНЫЙ (выше числа ядер), а не
абсолютный — абсолютный был бы тем же дефектом, что 45-секундный бюджет Stop-гейта.
Засеяно 29 замерами, снятыми по факту в этой сессии. 13 тестов TDD.
🔴 `/code-review` инструмента НЕ проведено — субагент упал на лимите сессии (HTTP 429),
долг занесён в ROADMAP §8.5. Полная деталь — CHANGELOG [8.33.0].

v8.32.0 — первая из API-only фич. Бэкенд ленты уведомлений жил с P2.3 и на фронте не был
представлен ни строкой. Гейт согласия поставлен ПОЭНДПОИНТНО, а не на роутер: `/run` это
cron-рассылка от админа для всех пользователей, роутерный `_FIN` потребовал бы согласия
админа на чужие данные и молча остановил бы рассылку — на `/run` стоит регрессионный тест,
что гейта там нет. Колокольчик на Radix Popover (Esc, клик вне, возврат фокуса, обход края
экрана — своими силами это писать не нужно, проект уже ходил этим путём для Modal).
При отозванном согласии виджет исчезает целиком, при аварии бэкенда — остаётся с честной
ошибкой: 403 имеет путь решения, 500 не имеет. Агенты нашли и починили до сдачи девять
блокеров, включая эмодзи вместо иконки (запрещены брифом), панель, уезжавшую за край
экрана, немой клик по уведомлению (`link` игнорировался) и потерю фокуса на «Прочитать
все». Отдельно: деньги в теле уведомления шли мимо канона («доход 125000») — заведён
`format_money` в `app/core/money.py`, 12 тестов; формат на бэкенде, потому что то же тело уходит
в email и Telegram, где фронта нет. Backend 1643/1644, Vitest 271/271, E2E 32/32.
Полная деталь — CHANGELOG [8.32.0].

v8.31.1 — батч начинался как «починить шесть красных E2E» и по ходу сменил предмет.
`.fp-hero-value` не находился вовсе; в консоли браузера лежала настоящая причина —
`TypeError: reading 'some'` по undefined. Сверка с диском: в схеме
`PlanningCalculateResponse` `ranked` и `top3` НЕ входят в `required`, сгенерированный тип
это отражает (`ranked?:`), а рукописный `entities/plan-summary/model/types.ts` объявлял их
обязательными — TypeScript молчал, и на ответе, который контракт прямо разрешает, весь
дашборд уходил в error boundary. Корень: `usePlan.ts:19` делает
`data as unknown as CalculatePlanResult` — каст снимает сверку с контрактом целиком.
Правка типа по контракту СРАЗУ вскрыла второе место падения (`AlternativesBrowser`),
которое иначе не нашлось бы: TypeScript назвал обе строки сам. Тем же классом оказались
`Explanation.gains/costs` (в схеме `Explanation` required только `delta`) — найдено
`api-contract-guard`. Оставшиеся два падения — действительно неоднозначные локаторы,
сужены, а не заглушены. Комментарий в шапке `types.ts` утверждал неправду про `/calculate`
и потому поддерживал ручное ведение файла — переписан. Backend 1636/1637, Vitest 256/256,
E2E 32/32. Полная деталь — CHANGELOG [8.31.1].

v8.31.0 — навигационного каркаса в SPA не было вообще: `<nav>` и `role="navigation"` не
встречались в `frontend/src` ни разу, все восемь `<Link>` продукта вели на экраны входа,
а `/obligations`, `/goals`, `/banks`, `/profile` открывались только набором адреса руками.
В `/profile` живёт отзыв согласия — право по 152-ФЗ было реализовано и недоступно, тот же
класс, что SEV1 `CONSENT-GATE-NO-UI`. Закрыто: `widgets/app-nav` в `__root.tsx`, подписи
дословно равны `<h1>` экранов, `aria-current` на активном, гостю каркас не показывается
(семь ссылок в гейт-403 были бы семью тупиками). H13 закрыт заодно — три сырых `<a href>`
стали `<Link>`. Гейты поставлены на КЛАСС дефекта: `test_spa_navigation_reachability.py`
роняет сборку, если новый экран не попал в меню (проверено мутацией, не перечитыванием),
`e2e/navigation.spec.ts` проверяет в браузере, что переход не перезагружает документ.
Агенты нашли и починили до сдачи: skip-link (WCAG 2.4.1), фокус-кольцо светлой темы 2.27:1
при норме 3:1 при комментарии, обещавшем обратное, hover 1.03:1 (физически отсутствовал),
прыгающий перенос от смены веса, схлопывание радиуса при фокусе. Backend 1636/1637,
Vitest 252/252, E2E навигации 7/7, preflight чист. Полная деталь — CHANGELOG [8.31.0].

v8.30.3 — истекающие сессии, решение занесено владельцу (вахта не моя, докс-only). Батч не
поднял `VERSION` — на входе в следующую сессию журнал стоял на 8.30.3, а `VERSION` на 8.30.2;
preflight сравнивает все пять источников, значит гейт был красным или не запускался. Класс
`PIT-G`. Снято подъёмом до 8.31.0.

v8.30.2 — adversarial-verify гипотез independent-expert, первый проход. H1 (в SPA нет
навигационного каркаса) и H13 (межстраничные переходы — сырые `<a href>`) подтверждены
чтением кода за минуты: `routes/__root.tsx:18-21` — топбар это `AuthTopbarLink` + `ThemeToggle`
и всё; `<nav>`/`role="navigation"` не встречается в `frontend/src` ни разу; все восемь `<Link>`
продукта ведут на `/login`/`/register`/`/forgot-password`; переходы между финансовыми экранами
— три сырых `<a href>` (`Hero.tsx:32`, `DashboardEmpty.tsx:17`, `PlanningPage.tsx:115`), то есть
полная перезагрузка документа со сбросом кэша TanStack Query. Итог: `/obligations`, `/goals`,
`/banks`, `/profile` недостижимы кликом ниоткуда, а в `/profile` живёт отзыв согласия
(`ProfilePage.tsx:7`, `useWithdrawConsent`) — право по 152-ФЗ реализовано и недоступно.
H3 подтверждён по `app/api/router.py:34-66` (`banks`/`demo`/`notifications`/`plaid` вне `_FIN`).
**Починен в этом батче только H3b** — `POST /demo/clear` для авторизованного был жёстким SQL
`delete()` по всему его портрету мимо soft-delete/undo; гард поставлен дословно по близнецу
`/demo/load`, гостевая песочница не затронута, 3 теста TDD red→green. Остальное H3 требует
фронта одновременно с гейтом (решение владельца v8.27.0) — вынесено в ROADMAP §9.0, не сделано.
Backend pytest 1632/1633 (1 ожидаемый skip), preflight чист. Полная деталь — CHANGELOG [8.30.2].
v8.30.1 — `_base/` (инжектируется системой «база» на 63 репо) добавлен в `SKIP_DIRS`
`tools/revision/revision_check.py` — гейт ревизии/preflight/`tests/test_repo_revision.py`
падал на ~90 ложных «битых ссылках», потому что внутренние ссылки `_base/**/*.md` рассчитаны
на СВОЙ корень базы, не на `REPO_ROOT` этого репозитория (`_base/DO-NOT-EDIT.md` подтверждает:
дерево — read-only зеркало, целиком перезаписывается `sync_base_local.py`, который трогает
строго `<репа>/_base/`, никогда `tools/` — фикс переживёт будущие раздачи). Backend pytest
1628/1629 (1 ожидаемый skip), preflight чист. Заодно обнаружено и не тронуто: во время
диагностики этой сессии `.venv` был пересоздан параллельным процессом (плановая чистка
тулчейна владельца — `_base/12-workstation-kit/reports/cleanup-and-toolchain-2026-09-02.md`,
перевод трёх проектов на `uv`/Python 3.13.15) — venv переустановлен через `uv pip install`,
исходный код батча не затронут ни разу (проверено построчно).
v8.30.0 — батч 3 CRUD-паритета (ROADMAP §8.2), последний из пяти чекбоксов: создание/правка
бюджета в React (было только чтение через `/status`, вообще без формы). Бэк не менялся — готов
заранее (v8.24.1). У бэка нет `PUT` — `POST /api/budgets` upsert по `category` (FR-22):
существующая у владельца категория обновляет `limit_amount`, новая создаёт строку — категория
фактически ключ записи, `BudgetForm` блокирует её (`disabled`) в режиме правки, иначе отправка
с другим значением молча создала бы вторую строку вместо переименования. `BudgetsSection` на
дашборде — самостоятельный запрос (`GET /api/budgets/status`, план-факт), не часть usePlan/
useForecast, свои загрузка/ошибка/согласие/пусто. `shared/ui/StatePanel` получил `headingLevel`
(2|3) — первый случай вложенной панели внутри своей секции с h2 (иначе два h2 подряд без
текста между ними, контур документа); `ConsentRequiredPanel` пробрасывает параметр, поведение
шести существующих вызовов не меняется (default 2). `field.css` — первый `:disabled` в семье
entity-form. Frontend Vitest 238/238 (1 известный флейк KaTeX/Suspense, см. v8.27.0,
перепроверен изолированно — зелёный), typecheck/eslint/build чисты. Бэкенд не трогался,
openapi.json пересборки не требует. Полная деталь — CHANGELOG [8.30.0].
v8.28.0 — батч 2 CRUD-паритета (ROADMAP §8.2/§9.0 «B»): создание/правка/удаление операции и
цели в React (было read-only). У операции — `description` в форме, `mcc`/`currency`
сознательно не включены. У цели — `GoalUpdate` не принимает `current_amount` вообще, прогресс
вносится отдельным роутом `POST /goals/{id}/contributions` (переиспользует существовавший, но
не подключённый `record_goal_contribution`), 409 для целей с `linked_asset_id` (прогресс из
баланса актива). Бэк заложен в объём явно (10 новых тестов TDD red→green), оба роутера уже
были под `_FIN` — новые эндпоинты унаследовали гейт согласия автоматически. `openapi.json`
пересобран (112→113, единственный новый путь — `/goals/{id}/contributions`).
До сдачи design-critic (13 блокеров) и a11y-auditor (1 нарушение AA) нашли и починили: главный —
`GoalContributionForm` размонтировалась целиком вместо перехода `open:true→false` (Radix никогда
не видел закрытие, путь возврата фокуса не гарантирован, WCAG 2.4.3) — компонент теперь
смонтирован всегда, как остальные формы батча; радиокнопки «Тип» красились как текстовый инпут
(потомковый CSS-селектор не исключал `type="radio"`); постоянный красный на каждой строке
расхода нарушал бриф §6 (расход — нейтральный цвет, легенда с инлайн-стилями удалена); прогресс-
бар цели скопирован из ObligationsPage с подменённым токеном (`--c-green` вместо `--c-accent`,
семантически неверно); накопленная сумма подана мелкой подписью вместо якоря строки; три
равновесные кнопки на строке цели — «Внести прогресс» выделена `variant="primary"`; селект
привязанного актива молчал про загрузку/ошибку/пустой список; просроченная цель ничем не
отличалась от обычной. Backend pytest 1629/1629, frontend Vitest 225/225 (после починок),
typecheck/eslint/build чисты. Полная деталь — CHANGELOG [8.28.0].

**Independent-expert (роль реализована и прогнана впервые, ROADMAP §9.0 «D»)** — 16 гипотез
разрыва «докс/канон vs код», сохранены дословно в `docs/research/raw/
12_independent_expert_milestone8_gap_2026-08-19.md`. НЕ проверены эмпирически (следующий шаг —
adversarial-verify, не начат). Верхние по правдоподобности: **H1** — в React-SPA нет
навигационного каркаса вообще (топбар — только email+выход, ни одной ссылки на `/obligations`/
`/goals`/`/banks`/`/profile`; профиль, где живёт отзыв согласия, недостижим кликом — тот же
класс, что CONSENT-GATE-NO-UI, но шире и не про согласие); **H3** — периметр `_FIN` (после
v8.27.0, «10 роутеров») пропускает `banks_router` (импорт выписки пачкой — самая чувствительная
запись в продукте), `POST /demo/clear` (жёсткий SQL delete, без гарда на авторизованного,
асимметрия с `/demo/load`), `notifications_router` (суммы в уведомлениях/email/Telegram),
`plaid_router`; **H2** — `RegisterPage` обещает перенос гостевых данных текстом на экране,
бэкенд явным комментарием этого не делает (прямое противоречие, не гипотеза).

v8.27.0 — разбор §6 `compliance_register.md` нашёл незадокументированный пробел: analysis/
planning/recommendation/export читали финансовый портрет без `require_financial_consent`.
Владелец (роль архитектора) выбрал — гейтить все четыре ОБЯЗАТЕЛЬНО вместе с фронтом, иначе
Dashboard (`/`, главный экран) стал бы новым SEV1-тупиком вместо вчера починенного. Backend:
`_FIN` теперь на 10 роутерах (`app/api/router.py`), 8 новых тестов + 2 почищенных
(`test_household_plan_scenario.py` звал planning без согласия). Frontend: `DashboardPage`/
`PlanningPage` получили ветку `ConsentRequiredPanel` на 403 (тот же вчерашний компонент).
До сдачи design-critic и a11y-auditor нашли и починили: панель согласия на dashboard была
осиротевшей карточкой без якоря раздела (`sr-only` h1 корректен для остальных состояний —
они сами себя называют, эта — нет) — заголовок сделан видимым в этой ветке; гонка `refetch`
могла уронить фокус в `<body>`, если после согласия dashboard разрешался в Empty/Error без
`ref` — почищено (`Promise.all`, `mainRef` в Empty/ErrorState). Backend pytest 1619/1619,
frontend 216/217 (1 известный флейк KaTeX/Suspense под нагрузкой хоста, не регрессия).
Полная деталь — CHANGELOG [8.27.0].
v8.26.1 — батч дрейфа (ROADMAP §9.0 «A», три находки владельца одним заходом, TDD
red→green на каждой): `/dashboard` уходил на легаси Jinja вместо React SPA (`base.html`
ссылался на `/dashboard`, канонический адрес — `/`; nginx теперь 301 на `/`, плюс новый
регрессионный тест — каждый роут `frontend/src/routes/*.tsx` обязан быть покрыт SPA-блоком);
README.md второй раз подряд отстал от реальной версии (в v7.7.0 — на 15 версий) — теперь в
проверке версий `tools/preflight.py`, поймал живое расхождение сразу; инвариант ПДН≤0.40 был
подписан тремя РАЗНЫМИ версиями канона одновременно (v3.5.0/v3.0.0/v3.5.0, ни одна не
историческая — сверка с `model_history.md` показала, что v3.5.0 реально ввёл G8, не этот
инвариант) — номер убран, ссылка на `docs/math_model.md §3`, `LegacyModelChecker` расширен с
markdown на `.ts`/`.tsx`/`.py`. Полная деталь — CHANGELOG [8.26.1].
v8.26.0 — SEV1 near miss найден и починен: `require_financial_consent` (v7.5.0) работал
корректно на бэкенде 26 дней/15+ версий, но UI для прохождения гейта НЕ СУЩЕСТВОВАЛ —
эмпирически подтверждено (Playwright, реальная регистрация → `GET /api/obligations` → 403,
`/profile` без слова про согласие), каждый новый пользователь упирался в тупик без обходного
пути. Полный разбор — `docs/reports/incidents/consent_gate_no_ui_dead_end_incident.md`
(CONSENT-GATE-NO-UI). Рабочий минимум: `entities/consents`, `ConsentRequiredPanel` на всех
четырёх списках, блок «Согласия» в `ProfilePage` (undo-тост на отзыв, тот же паттерн, что
удаление записей). До сдачи design-critic (5 блокеров) и a11y-auditor (3 нарушения AA) нашли и
починили: дублирующийся акцент ссылка+кнопка, «Не дано» без визуального признака при бейдже у
мелочи, отзыв без danger/undo, молчаливые мутации без toast.error, текст-противоречие кнопке,
`disabled`+`aria-busy` роняли фокус в `<body>` (заменено на `aria-disabled`), успех менял
контент без переноса фокуса. Владелец поручил новый обязательный класс тестов — «полный
эмпирический путь реального пользователя» (Playwright, не моки) — ROADMAP §9.0 пункт E.
Полная деталь — CHANGELOG [8.26.0].
v8.25.3 — оценка стоимости починки джобы `Линтеры` (информационная, не блокирует деплой):
flake8 — 5 минут, ноль риска, починено (`tools/model_validation/model_snapshot.py`, 3 строки
E501); mypy — уже отдельная задача ROADMAP §8.2, не трогать отдельно; pylint — ~150 находок,
часы на весь `app/`, старый фон, не регрессия — не стоит того сейчас. Остаток — новый чекбокс
ROADMAP §9.0, после вехи 8. Полная деталь — CHANGELOG [8.25.3].
v8.25.2 — расследование по прямому вопросу владельца: блокирующий гейт CI был красным
**41 день, 65 релизов** (v5.25.0…v8.25.1) — post-mortem с 5 Whys,
`docs/reports/incidents/ci_blocking_gate_red_41_days.md`. Попутно найден и разобран отдельный
инцидент — первый реальный прогон PostgreSQL-матрицы завис на 60+ минут на `apt-get`-флюке
GitHub-раннера (не код), плюс реальный, но некритичный баг health-check Postgres-сервиса
(`pg_isready` без `-d`). Починено: `timeout-minutes` на всех джобах `ci.yml` (не было ни у
одной), health-check, декоративный `|| true` на locust-smoke. Полная деталь —
CHANGELOG [8.25.2].
v8.25.1 — `python -m tools.preflight` падал на Linux/CI (битая ссылка на заглавный
`GLOSSARY.md` в WATCHLOG), был зелёным на macOS: `LinkChecker` сверял ссылки через
`Path.exists()`, а APFS
регистронезависима — та же природа бага, что чинилась в v8.4.1 для физических дублей пути,
теперь для ссылки без второго файла на диске. Починено: `_exists_with_matching_case()` через
`os.listdir()` вместо `Path.exists()`, регрессионный тест (red→green), само упоминание в
§3 переписано без битого пути. Полная деталь — CHANGELOG [8.25.1].
v8.25.0 — React-экраны «Обязательства»/«Ликвидные активы» были read-only, теперь полный
CRUD: `shared/ui/Modal.tsx` (первое применение `@radix-ui/react-dialog`), `ObligationForm`/
`AssetForm` с валидацией обязательных полей, мягкое удаление с `toast.undo` (самозакрывается
по клику — не даёт дёрнуть `/restore` дважды), `Button variant="danger"` для удаления.
До сдачи design-critic (12 блокеров) и a11y-auditor (3 нарушения AA) нашли и починили:
хардкод-цвет оверлея и слой модалки = слою полей внутри (лестница возвышения ломалась);
`0.078 * 100 === 7.800000000000001` — мусор в поле ставки при правке; `entityForm.css`
дословно дублировал `AuthLayout.css` (CMP-01) — объединены в `shared/ui/field.css`; `.fp-button`
без состояния `:disabled`; фокус после удаления строки проваливался в `<body>` (WCAG 2.4.3);
восстановление не объявлялось скринридеру (WCAG 4.1.3); остаток долга не показывался в строке
списка, хотя форма делает его обязательным первым полем. Vitest 187/187 (было 175 до PATCH
v8.24.1). Проверено живьём в браузере (Chrome DevTools MCP), обе темы. Детали — CHANGELOG
[8.25.0]. v8.24.1 — три вещи перед CRUD-батчами §8.2: гейт `require_financial_consent` на
`budgets_router` (был единственным из пяти финансовых роутеров без него — лимиты по
категориям тоже финансовые данные); мягкое удаление + `restore` для бюджета (миграция `0034`,
симметрия с обязательствами/целями/активами) — `create_budget` (upsert по категории, FR-22)
теперь оживляет ранее удалённую строку вместо UNIQUE-конфликта на `category`; `extractErrorMessage.ts`
разбирает объектный 403 гейта согласия (`{code, consent_type, document, message}`) — текст +
ссылка на документ плоской строкой, минимум до экрана согласия L3, чтобы CRUD-батчи ниже не
переделывать. Матрица SQLite+PostgreSQL прогнана вручную (Homebrew `postgresql@15` локально,
не Docker — `pg_matrix_last_run.md`), 43/43 на PG. Полный pytest 1593/1593, Vitest 178/178.
Далее — CRUD-паритет §8.2 батчами: 1 (Обязательство+Актив, 3 дня) → 2 (Операция+Цель, 3,5 дня,
решения по прогрессу цели и полям операции приняты) → 3 (Бюджет, только фронт, 1 день) → PDF-отчёт
§8.4. v8.24.0 — `ForecastControls` на `/planning`: select горизонта (3/6/12/24 мес.) + ползунок
ставки капитализации r_bench (0–30%, дебаунс 500 мс) с кнопкой «Сбросить к реальной». Бэкенд
`ForecastRequest.r_bench` — тот же паттерн override, что уже был у `PlanningRequest.r_bench`;
ответ дополнен `real_r_bench` (считается ВСЕГДА, не только без override — иначе кнопке сброса
неоткуда взять настоящую ставку, баг найден и исправлен при живой проверке в браузере).
До сдачи `design-critic` и `a11y-auditor` (оба нашли реальные проблемы) починили: сырой JS-`%`
вместо `formatPercent` (точка вместо запятой в деньгах, CMP-04); `isFetching` доходил только до
скринридера, зрячий не видел пересчёта вообще; `flex:1` без `flex-basis` не переносил поле на
узких экранах; фокус после исчезновения кнопки «Сбросить» улетал в `<body>`; сценарий не был
помечен в самом графике (единственный признак — кнопка в другом месте экрана). Параметры движка
(α/β Holt, σ Monte-Carlo, ширина CI 80%) сознательно НЕ отданы пользователю — канон-заморожены.
Побочно: `tools/revision/revision_check.py` — CJK-канарейка ловила себя на минифицированном
KaTeX в `frontend/dist/` (сборочный артефакт) — `dist` добавлен в `SKIP_DIRS`.
Vitest 175/175 (было 172), tsc/eslint/prettier/build чисты. v8.23.0 — частичный снос
Jinja-фронта перед Э6: снесены только `profile`/`forgot-password`/`reset-password`+мёртвый
`index.html` (React-паритет подтверждён). Первый проход по ошибке снёс ещё и `dashboard`/
`planning`/`transactions`/`obligations`/`goals`/`banks` — их React-версии read-only, снос убрал
бы формы создания; найдено и исправлено в том же батче. Побочно подтверждена уже
задокументированная (с v8.6.0) находка: nginx с v8.9.0 прячет Jinja-CRUD этих шести путей от
обычного доменного трафика — продукт не запущен публично. Разбор —
`docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`. v8.22.0 — экраны
входа/регистрации с нуля (`LoginPage`/`RegisterPage`/`ForgotPasswordPage`/`ResetPasswordPage`,
`shared/ui/Toast.tsx`, `AuthTopbarLink`), пойман и починен баг stale-if-error в топбаре после
логаута. v8.21.0 — последний
пункт плана Э5: `ZoneScale` (шкала с зонами) в MetricsGrid + дисклеймер 601-ФЗ при ПДН > 50%
— все шесть пунктов плана Э5 теперь технически закрыты. v8.20.1 — переменные модели везде
через KaTeX (прямая поправка владельца). v8.20.0 — формулы KaTeX по клику (CMP-05).**
**v8.11.0 (ползунки «что если», MINOR):** составной столбец, легенда и диаграмма Санкея в
`AllocationPanel` теперь реагируют на два ползунка (долг/цели, резерв — остаток) — один
интерактивный артефакт, как в плане, не рекомендация сверху и песочница снизу отдельно.
Результат гипотезы ищется в уже посчитанном `ranked[]` (`findMatchingAlternative`), недоступные
модели комбинации помечаются явно (fail-loud). До сдачи `design-critic` (2 запуска — первый упал
на сетевой ошибке API) и `a11y-auditor` нашли и починили три блокера: столбец был заморожен на
рекомендации, пока ползунки показывали другие числа (переписано на общее управляемое состояние);
`accent-color` обоих ползунков совпадал с `--c-green` («Цели») — ползунок долга был случайно
зелёным (теперь цвет = цвет категории); блок результата был неотличим от фона панели в светлой
теме (`--c-surface-up` == `--c-surface` там, тот же класс бага, что раньше чинили для
`--c-bg2`/`--c-surface`) — добавлена рамка. Плюс порог ПДН приведён к словарю бейджей
`MetricsGrid` (был голым текстом), `aria-valuetext`/`label` дублирование убрано, живая область
задержана на 400 мс (не объявляет каждую из 11 отметок при автоповторе стрелки), добавлен
`color-scheme` в tokens.css (первые в проекте range-инпуты вскрыли его отсутствие). Vitest 99/99
(было 83/83). Детали — CHANGELOG [8.11.0]. **v8.10.0 (Санкей,
MINOR):** режим «подробно» под легендой `AllocationPanel` — recharts `<Sankey>`, свёрнуто по
умолчанию, тогл-кнопка не показывается при единственной активной категории/дефиците. Дефолтные
рендереры recharts заменены целиком (магические `#0088fe`/`#333`). До сдачи `design-critic` и
`a11y-auditor` нашли и починили: ложную `aria-expanded`/`aria-controls` на decorative
aria-hidden блоке (→ `aria-pressed`), перепутанные якоря подписей узлов, магические числа в
SVG (сведены к шкале `--sp-*`), `utility.toFixed(2)` без запятой (→ `formatNumber`),
сплющивание на узких экранах (→ `overflow-x` + `--chart-sankey-min-width`). Осознанно отложено:
копия «U = {u}»/«СППР» (решается штатным KaTeX-пунктом Э5) и незагружаемый Manrope в SPA
(найдено попутно, отдельная задача на весь SPA). Vitest 83/83, build/eslint/prettier чисты.
Детали — CHANGELOG [8.10.0]. **v8.9.4 (стабильное имя канона — docs/math_model.md,
PATCH):** структурная причина серии v8.9.2–v8.9.3 — имя файла канона версионировалось
(`math_model_v3_X_Y.md`) и требовало ручного обновления ссылок при каждой рекалибровке.
Владелец предложил устранить класс бага: `docs/math_model_v3_5_0.md` → `docs/math_model.md`
(стабильно, версия только внутри файла), `v3_0_0.md`/`v3_1_0.md` → `docs/model/history/`.
`session-context.sh` и `finpilot-math-model-guard` больше не зависят от номера в имени файла.
~45 живых ссылок по репозиторию переведены на новый путь; три замороженных отчёта, случайно
задетых первым проходом bulk-замены, восстановлены как есть и занесены в `LINK_ALLOWLIST`
(отчёты не переписываются задним числом). Канон v3.5.0 попутно подтверждён рантаймом:
`effective_floor_months`/`is_toxic_debt` (G8) реально вызывается из `planning.py:139-141`,
`build_crisis_plan` (G2) реально вызывается, `avalanche.py` подтверждён как одномоментное
распределение — по всему `app/` нет функций со `schedule`/`amortiz`. `revision_check` CLEAN
(было 5 битых ссылок сразу после переноса файлов), `preflight` чисто, 31/31 тестов G8/G2/
avalanche, 9/9 Vitest на тронутом `MetricsGrid`. Детали — CHANGELOG [8.9.4].
**v8.9.3 (доследствие ревизии v8.9.2, PATCH):**
владелец перепроверил v8.9.2 и нашёл три хвоста: (1) `docs/ROADMAP.md` строка 101 всё ещё
называла канон v3.0.0 — ревизия искала имя файла, не голое упоминание версии в прозе; это и
был первоисточник ошибки; (2) канон подтверждён теперь ПО КОДУ — `app/core/ranking.py`:
`TOXIC_FLOOR_MONTHS`/`is_toxic_debt()` с комментарием в коде «третья сертификация, v3.5.0»,
`app/core/crisis.py` реализует G2 (§12); `app/core/avalanche.py` подтверждён кодом как
одномоментное распределение — вывод про отложенную лавину долгов остался верным при
правильной версии канона; (3) снос старых Jinja-шаблонов/роутов для семи перенесённых на
React экранов выпал из плана при перенумерации этапов — восстановлен явным шагом Э8
(`docs/frontend_milestone8_plan.md`) + чекбокс ROADMAP §8.2, `templates/index.html`
подтверждён мёртвым (не подключён ни к одному роуту). Детали — CHANGELOG [8.9.3].
**v8.9.2 (ревизия канона матмодели по всему репо + README,
PATCH):** владелец поймал, что ассистент дважды подряд назвал каноном матмодели v3.0.0 —
реальный канон v3.5.0 (пять раундов сертификации). Найдено: это старый, уже раз пойманный и
брошенный баг (`docs/ui_visual_direction.md` уже флагал ровно это раньше). 28 файлов
(`CLAUDE.md`, README, доки, knowledge/, код-комментарии, докстринги тестов) заявляли
`math_model_v3_0_0.md` текущим каноном — приведены к `v3_5_0.md`; исторические файлы
(`v3_0_0.md`, `v3_1_0.md`, оба с баннером `SUPERSEDED`) НЕ тронуты — по прямому требованию
владельца история версий модели остаётся. `.claude/hooks/session-context.sh` — вместо
перезаписи числа сделан самообновляющимся (`grep -L SUPERSEDED docs/math_model_v*.md`), баг
не повторится на v3.6.0. `.claude/skills/finpilot-math-model-guard/SKILL.md` — добавлены G1–G8
(гейт вообще не знал про кризисный режим/токсичный долг/floor). Попутно и отдельно —
**README.md** оказался устаревшим сильнее матмодели: версия в шапке отставала на целую веху
(7.7.1 вместо 8.9.1), таблица технологий не упоминала React/Vite/TanStack вообще (описывала
фронт как «Jinja2 без SPA»), миграции 19 вместо 31, добавлена отсутствовавшая эпоха v8.x.x,
инструкция запуска дополнена фронтом (`npm run dev`, Vite-прокси `/api`) — `docker compose up`
(dev) SPA не поднимает, это не было нигде явно сказано. Подробности — CHANGELOG [8.9.2].
**v8.9.1 (Python 3.12→3.13, PATCH):** закрыт разрыв
dev↔prod/CI — `.venv` был на 3.13 с v8.7.1, `Dockerfile`/CI оставались на 3.12. Обновлены
`Dockerfile` (обе стадии), `.github/workflows/ci.yml` (все job'ы), `.mypy.ini`, `pyproject.toml`,
живые доки и `knowledge/roles/*`. Подтверждено: `docker build` на `python:3.13-slim` зелёный
(cryptography/bcrypt/psycopg штатными cp313-колёсами), смоук в контейнере (импорты + 31
миграция Alembic) чист, полный прогон на реальном PostgreSQL 16 под 3.13 — зелёный (exit 0),
`preflight` чист. **v8.9.0 (прод-сборка фронта и деплой на чистом
стенде, MINOR):** задача владельца вне канона плана вехи 8 (Э5→Э6 юр→Э7→Э8) и вне порядка
ROADMAP (деплой формально веха 9). `nginx/Dockerfile` (multi-stage node+nginx) — SPA
собирается прямо внутри `docker compose up --build`; nginx разводит маршруты: `/`,
`/planning`, `/transactions`, `/obligations`, `/goals`, `/banks`, `/profile` — SPA
(`index.html` + fallback), всё остальное (`/api`, `/v1`, `/health`, `/docs`, `/static`,
`/contacts`, `/legal/*`, `/reset-password`, `/forgot-password`, `/validation`) — FastAPI по
умолчанию через fallback `location /`. Корень `/` — SPA-дашборд, не отдельный лендинг
(уточнено у владельца; `templates/index.html` был мёртв ещё до React). `deploy/env.prod.example`
воссоздан (был `.env.prod.example`, утрачен при восстановлении `.git` в v8.7.1; переименован —
хук `block-secrets.sh` блокирует Write/Edit/Read на `*.env.*` без разбора примера от секрета).
Локальная проверка на изолированном docker-стенде (свой том, свой порт — реальный
`finpilot_pgdata` не тронут) поймала три реальных бага: стоковый `default.conf` образа nginx
перехватывал `Host: localhost` healthcheck раньше нашего конфига (контейнер вечно `unhealthy`
при рабочем внешнем трафике) — убран; IPv6 не слушался — `listen [::]:80/443` добавлен;
`docs/DEPLOY.md` учил генерировать `TOKEN_ENCRYPTION_KEY` через `openssl rand -hex 32` —
уронило бы прод на старте (`TokenCipher` требует строгий формат Fernet-ключа), заменено на
`Fernet.generate_key()`. Попутно найден и починен баг ревизионного гейта
(`tools/revision/revision_check.py`): `REF_PATTERN` обрезал «расширения» длиннее 6 символов
(`\.\w{1,6}` → `\.\w+`) — `deploy/env.prod.example`/`finpilot.conf.template` ложно считались
битыми ссылками. По ходу на машине кончилось место на диске (33 ГБ в
`~/Library/Containers/com.docker.docker`) — снесено с согласия владельца, там только
синтетические dev-тома, ничего боевого. Полный прогон: **1512/1512, 0 errors, 0 failures**
(1 skipped); Vitest 75/75, Playwright 25/25; `flake8` чист; `mypy` — 3 известных долга (см.
v8.5.0). **v8.8.0 (Э5 — браузер 66
альтернатив на /planning, MINOR):** первый пункт Э5. `AlternativesBrowser` раскрывает полный
`ranked[]` под `AllocationPanel` с сортировкой по критериям — новых бэкенд-полей не нужно, `ranked`
уже отдавался, фронт его не читал. `PlanAlternative`/`CalculatePlanResult` дотипизированы (`id`,
`Rt_new`, `Lt_new`, `Dt_new`, `is_recommended?`). `api-contract-guard` чисто (поймал неточность в
собственном комментарии types.ts — поправлено). `a11y-auditor`: 2 P1 починены (контраст бейджа
«рекомендовано» в светлой теме ~4.30:1 → сплошная пара `--c-accent-strong`/`--c-accent-ink`;
пересортировка списка не анонсировалась диктору — добавлен `role="status"`), плюс по мелочи
(`aria-controls`, скрытая стрелка, `aria-label` списка, `--c-text3`→`--c-text2` на грани контраста).
Попутно, первым же полным прогоном на закоммиченном дереве (см. v8.7.1), нашлись и почищены два
пре-существующих гейт-долга, не связанных с фичей: битая ссылка в историческом
`docs/reports/ui_audit_e3.md` на `ForecastPanel.tsx` по старому пути (точечное исключение в
`LINK_ALLOWLIST`, отчёт не переписан) и 2×E501 в `.claude/hooks/_run_test_slice.py`. **Лавина
долгов (второй пункт Э5) сознательно отложена** — требует новой математики помесячной амортизации,
которой нет в каноне v3.0.0 (только одномоментное распределение); решение расширять канон — за
владельцем. **v8.7.1 (git-репозиторий
восстановлен, PATCH):** `.git` был случайно инициализирован в `$HOME` прошлой сессией (0 коммитов,
без remote, весь `finpilot/` физически не был под git) — `git init` перевыполнен внутри `finpilot/`,
первый коммит зафиксировал дерево на `v8.7.0` (root-commit, 1384 файла), `node_modules/` добавлен
в корневой `.gitignore`. Пустой `.git` из `$HOME` отодвинут в бэкап, не удалён — решение за
владельцем. Кода продукта не тронуто. Бэк логикой не тронут (только версия в `app/config.py`) —
но именно на v8.7.1 впервые поднят `.venv` (Python 3.13) с зависимостями бэкенда: Stop-гейт до
этого молчал не потому что зелёный, а потому что не мог импортировать `app.main`
(`ModuleNotFoundError: No module named 'jwt'` на системном python3). Первый настоящий полный
прогон на закоммиченном дереве — **1512/1512, 0 errors, 0 failures** (1 skipped, v8.8.0), `flake8 .`
чист, `mypy app` — 3 известных долга (см. v8.5.0), preflight чисто.
Фронт (v8.8.0): Vitest 75/75 (было 72, +3), Playwright 25/25 (было 24, +1), `tsc`/`eslint`/
`prettier`/`check-design-tokens.sh` чисто. **v8.7.0 (Э4 партия 2 — планирование/активы/профиль, MINOR):**
все пять экранов пятёрки ROADMAP §8.2а на React, чекбокс закрыт `[x]`. `/planning` переиспользует
`usePlan`/`useForecast` (те же, что dashboard, не новая сущность) + виджеты `MetricsGrid`/
`AllocationPanel`/`ForecastPanel`, вынесенные из `pages/dashboard/ui` в `@widgets/*` при этой
партии (долг Э3 — `widgets/README.md` называл их примерами виджетов с самого начала, но Э3 положил
их в pages/dashboard). `AllocationPanel` теперь сам принимает `best: PlanAlternative | null` и
рендерит fail-loud «Плана распределения нет» — раньше эта ветка была захардкожена только в
`DashboardPage.tsx`. Сверх пятёрки тем же конвейером: `/banks` (ликвидные активы, путь как в
старой Jinja) и `/profile` (только чтение). Реальный баг пойман до отправки: `interest_rate`
ликвидных активов чуть не поделили на 100 по инерции от формата старой Jinja-формы (0-100 в
input) — сверка с бэкенд-схемой (`Numeric(6,4)`, доля, та же точность что у обязательств) поймала
до коммита. `a11y-auditor`: 1 P1 починен (`/profile` — 401 и сетевой сбой давали идентичное
сообщение с тупиковым циклом «Повторить» без объяснения причины, особенно для screen reader —
`useProfile.ts` теперь различает статус через `throwOnError: false`, отдельная ветка `role="status"`
без кнопки повтора для 401), 3 P2 сделаны (sr-only подпись риск-бейджа, `aria-label` на
`MetricsGrid`, `role="presentation"` на alloc-bar), 2 отложены. `api-contract-guard`: чисто, один
пробел найден на уровне контракта — `openapi.json` не описывает `security`/401 для `/api/auth/me`,
хотя бэкенд их требует (`require_user`) — расхождение схемы, не фронтенда, правка не в этом
батче. **v8.5.2 (правки
UI-аудита Э3, PATCH):** до Э4, разбор `docs/reports/ui_audit_e3.md`. P0-1: чёрный прямоугольник
вместо графика прогноза в тёмной теме — маска `fill=var(--c-bg)` без `stackId` убрана целиком (не
патч через один `stackId`), заменена на два стекированных `Area` (невидимая база p10 + видимая
дельта p90-p10) — корректно и на `anna` (позитив), и на `pavel` (дефицит, где старый баг случайно
не проявлялся). P0-2: тумблера темы не было вообще — `useThemeStore` (Zustand+persist,
light/dark/system, `data-theme` на `<html>`), нативный `<select>` в новом `.fp-app-topbar`. P1-3:
ПДН>0.40 не подсвечивался — добавлен `danger`-вариант (канон v3.0.0: жёсткий инвариант Dt<=0.40),
приближение (Dt>0.35) — warn; ликвидность/подушка — только warn НИКОГДА danger (канон: отсев по
ликвидности мягкий, L_min=0 по умолчанию выключен, красный означал бы несуществующий жёсткий
порог). P1-4: skeleton теперь резервирует место под AllocationPanel/ForecastPanel — не прыгает при
loading→loaded. `a11y-auditor` подтвердил закрытие 2 находок Э3 (акцент ПДН, иерархия заголовков),
нашёл и почитан один P1 (рамка ThemeToggle ~2.9:1 в тёмной теме, ниже WCAG 1.4.11 — заменена на
`--c-text3`, ~5.3:1). P2-5/P2-6 (devtools поверх кнопки на 375px, favicon 404) — отложены осознанно
(dev-only / нужен брендовый актив). Побочно: `docs/onboarding.md` — явное предупреждение про
`.venv` (найдено на практике в v8.5.1: системный python3 на машине резолвился в 3.10, часть
зависимостей ставилась глобально молча). **v8.5.1 (Stop-гейт тестов починен, PATCH):** `.claude/hooks/tests-gate.sh` был fail-open — гонял полную суиту на каждом `Stop` при таймауте хука 10 мин, молча пропускал `Stop` без блокировки при таймауте (найдено `/doctor`: 20/21 таймаутов в соседнем чек-ауте v8.3.1). Заменён на срез по затронутым файлам сессии + маркер `property` (инварианты матмодели, пол 20-51 с); таймаут хука 600→120с; таймаут по фазам — `subprocess.run`, не shell `timeout(1)` (нет на голом macOS); попутно два грабля bash 3.2 на macOS (`mapfile` не существует, `"${arr[@]}"` на пустом массиве под `set -u` падает). **Живая обкатка тут же поймала фан-аут:** правка `app/config.py` дала 12 затронутых файлов (3 уже ~39 с, 12 не уложились и в 2 мин) — тот же fail-open заново, другой триггер. Добавлен потолок `MAX_AFFECTED_FILES=3`: сверх — фаза по файлам честно пропускается, не гонится через силу; бюджеты по фазам пересчитаны по замерам (45+65=110 с, зазор 10 с до внешнего таймаута). `lint-python.sh` уже был корректно ограничен одним файлом — добавлен только явный `--config=.flake8`. **ГЛАВНОЕ ЗА v8.4.1:** починен дефект агентского контура, который вскрыл заход 7 исследования — в v8.2.0 `.claude/settings.json` не имел **ни одного блокирующего хука**, весь заслон 152-ФЗ на секреты держался на тексте в `CLAUDE.md`, а `$CLAUDE_FILE_PATHS` шёл без кавычек (инъекция через имя файла). **ЗАСЛОН ДЕЛАЕТСЯ ХУКОМ, А НЕ ТЕКСТОМ:** `PreToolUse` exit 2 блокирует инструмент даже в `bypassPermissions`; текстовый запрет модель обходит рассуждением (кейс Replit: агент снёс боевую БД вопреки объявленному code freeze). Поставлено семь хуков: блок секретов, блок деструктива (`rm -rf`, `alembic downgrade`, `black .`), линтеры, аудит-лог в `logs/agent-audit.jsonl`, `Stop`-гейт на pytest со `stop_hook_active`-гардом, контекст на `SessionStart`. **ЗАВИСИМОСТЬ ОТ `jq` УБРАНА:** без него блокирующие хуки молча пропускали всё — разбор конверта на `python3`, неразборчивый вход = exit 2 (fail-closed). **ШАГ 3.5 ЗАКРЫТ:** шесть скиллов FINPILOT с евалами (позитив+негатив+edge). **ВОССТАНОВЛЕНО ПОСЛЕ ОБРЫВА ЧАТА №15:** `docs/claude_operating_table.md`, `docs/claude_token_lab/`, `docs/reports/decisions/` — кода потеряно не было. **ПРАВИЛО §19:** решение за владельцем оформляется структурным разбором (≥2 варианта включая «ничего не делать», явная рекомендация, что её изменило бы), вахта не решает молча. **XZ — РЕКОМЕНДАЦИЯ ОТКЛОНИТЬ:** замерено 37 539 861 → 27 245 412 Б (−27,4%), sha256 совпал на 27 из 27, но git-история растёт на 26 МБ — задача была уменьшить репозиторий, а предложение его увеличивает. **PIT-015:** трое суток без чекпоинта.
**ЗА ВЛАДЕЛЬЦЕМ:** решение по xz; `claude update` (нужна >= 2.1.181 для `/design-sync`, качается через VPN); включить Code Execution; `/doctor`; запушить зеркало `finpilot-mirror-v8.0.0.zip`. **ПРАВИЛА:** перед сдачей `python -m tools.preflight` · матрица SQLite+PostgreSQL на схему · **Opus 5 / high по умолчанию, `max` — только эскалация** (TTFT ~92 с против ~3 с на low) · §18 исходники в репозиторий всегда и первыми · §19 решения владельца — документом. ВЕХА 9: L8, Redis rate-limit, CSP без `unsafe-inline`, TLS/HSTS, прод-секреты; **O1 уведомление РКН подаётся ДО начала обработки.**
> Если архив другой версии — сверь `app/config.py`.

---

## §0. RESUME POINT — отсюда продолжаем

**Версия:** 9.7.0 · **Дата:** 2026-09-08 · **Вахта:** M

**Текущая точка: v9.7.0 — ТРЕТИЙ АУДИТ, ГЛАВНАЯ НАХОДКА ПРО ВЧЕРАШНИЙ ФИКС**

- 🔴 **СОГЛАСИТЬСЯ НА НОВУЮ РЕДАКЦИЮ БЫЛО НЕЛЬЗЯ.** v9.6.0 научил ВИДЕТЬ расхождение
  редакций (`is_current`) и не дал способа его снять: `grant_consent` возвращал старую
  запись, не сверяя `doc_version`. Для `personal_data` тупик без обхода — тип
  неотзываемый, 409. Поднятие версии политики ПДн заперло бы **100 % пользователей**
  без выхода, кроме удаления аккаунта. Починено: новая редакция — новая запись,
  прежняя сохраняется как доказательство основания обработки.
- 🔴 **ШЕСТОЙ случай `CONSENT-GATE-NO-UI`, и создал его гейт против пятого.**
  `is_current` считался, лежал в контракте и во фронте встречался **только
  в сгенерированном клиенте**. Полутора десятков версий не понадобилось — **хватило
  суток**: периметр гейта взяли из «кластера совета», схема `ConsentState` под описание
  не подпала. Экран теперь показывает действующую редакцию и даёт её подтвердить.
- 🔴 **ТРИ СОБСТВЕННЫХ ГЕЙТА БЫЛИ ЗЕЛЁНЫМИ ПО СЛУЧАЙНОСТИ.** Сверка «поле доехало
  до экрана» искала ПОДСТРОКУ (`Si` находилось в `Size`) и читала тесты — строгая
  проверка даёт **19** полей без потребителя вместо 15. В списке долгов лежала строка
  с выдуманной ссылкой на канон (`x_remain`, «§9» — такой строки в каноне нет вовсе).
  Гейт дрейфа клиента искал поле по всему файлу, а не в своей схеме, и был слеп
  к любому неуникальному имени.
- **Временна́я лаборатория: долг ревью закрыт аудитом** (пользовательское код-ревью
  падало трижды). Нагрузка снималась ОДИН раз до старта — набор, перегружающий машину,
  писался как спокойный; упавший прогон участвовал в медиане; справочник отстал
  на десять замеров и занижал худший случай **в 2.1 раза**.
- **Конвейер анализа опроса не запускался вовсе** — импортировал несуществующий пакет
  `finpilot_survey` (15 ссылок). Умер при переносе в `tools/`. Проверено запуском:
  322 валидных из 385, отчёт и 14 графиков.
- 🔴 **ЧЕТВЁРТЫЙ ПРОХОД — снова находки в правках этого же батча, шесть штук.**
  Отзыв согласия молча не срабатывал после смены редакции (гасилась одна запись
  из двух — ложное подтверждение права по 152-ФЗ); кнопка отзыва **исчезала**, как
  только компания поднимала версию документа (ветка подтверждения поглощала ветку
  отзыва — тот же дефект, что чинили в v8.41.0, вернулся условно); выбор действующей
  записи зависел от совпадения отметок времени; конвейер опроса «починен» на пути
  `/mnt/user-data/uploads/`, которого нет ни на машине, ни в CI, **а гейт против этого
  не выполнялся ни разу** — прятался за отсутствующим `pandas`; свой `--note` вытеснял
  отметку провала и возвращал упавший прогон в медиану.
  🔴 **Четыре ошибки в правках этого раунда нашлись ЗАПУСКОМ, а не чтением:** сверка тел
  типов копировала весь файл до каждого совпадения (прогон перестал завершаться),
  склеивала два doc-комментария (телу `Alternative` доставалось чужое имя), давала имя
  типу без комментария от подписи чужого свойства, и скобка в строковом литерале уводила
  конец тела за настоящую `};`.
- 🔴 **ПЯТЫЙ ПРОХОД: гость на проде писал СНИМКИ ПЛАНА в общий пул.** Митигация v8.53.0
  была неполной — `planning_router` стоял только с `_FIN`, а гейт согласия гостя
  пропускает намеренно. Снимок несёт портрет целиком, в отличие от отдельной транзакции.
  🔴 **Мой собственный гейт периметра, заведённый часом раньше, эту дыру ПРОПУСТИЛ:**
  я внёс `require_financial_consent` в список проверок прав, а он правами не является.
  Проверка против класса воспроизвела класс. Список сужен — гейт сразу вскрыл семь ручек.
- 🔴 **Экран и гейты давали РАЗНЫЕ ответы, какая запись согласия действует.** `_active`
  починили, `active_consents` (источник экрана) — нет. Свойство доказывали на приватном
  хелпере, публичный путь его не наследовал. Сведено к одному источнику.

🔴 **СЛЕДУЮЩИЙ ШАГ.** Цикл «фикс → независимый аудит → фикс» продолжается по поручению
владельца 08.09.2026 — гонять эксперта по всему коду, **пока не вернёт пусто**. Третий
проход запускать после сдачи v9.6.0, со списком уже разобранного (иначе приносит прежнее).
Из открытого в вехе 9 самое дорогое — **пятый случай `CONSENT-GATE-NO-UI`**: 15 полей
кластера совета без экрана, включая расчёт вычета ИИС (v3.9.0, самая свежая фича канона).

- **2026-09-06 — v9.4.0, вторая половина: E2E ВОСКРЕШЁН, ЯДРО СТАЛО ТРЕТЬИМ КОНТУРОМ.**
  🔴 **Весь E2E-контур проверял СНЕСЁННЫЙ интерфейс: 38 селекторов из 38 мертвы.**
  Найдено при заходе на пункт E роадмапа. Тесты писались под Jinja, снесённую в вехе 8
  (`#auth-modal`, `auth.js`, `#rt-value`); каталога `app/templates` не существует вовсе.
  Проверено ПРОГОНОМ: 10 из 10 падали по таймауту локально и на живом CI.
  🔴 **Поправка к собственной записи:** я написал «CI при этом зелёный» — неверно
  и не проверено. Контур не молчал, он кричал давно, и красный принимали за фоновый шум.
  Это хуже молчания: сигнал есть, а привычка игнорировать выработана.
  Переписаны под React четыре файла (было 11 failed → **16 passed, 1 xfailed**),
  заведён статический гейт `test_e2e_selectors_exist.py` — расхождение видно
  из исходников, без сервера и браузера.
  🔴 **`test_category_learning_e2e.py` — `xfail(strict)`, переписать НЕЛЬЗЯ:** правил
  категорий во фронте нет вовсе, `GET/DELETE /api/category-rules` зовёт только
  сгенерированный клиент. Экран потеряли при переносе на React, и единственный,
  кто это ловил, — тот самый падавший тест. Повтор `CONSENT-GATE-NO-UI`.
  🔴 **Мутация нашла слепой тест сразу после написания:** `_wait_logged_in` искал
  `text={email}` и был зелёным при не выводящем email топбаре — адрес остаётся
  в поле формы. Исправлено на `.fp-auth-topbar__email`.
  🔴 **Ядро — третья сущность** (решение владельца: «как будто это три сущности разные»).
  Отличие по ЦЕНЕ ОШИБКИ: бэкенд ломается в 500, фронт в пустой экран, ядро —
  в неверный совет о деньгах, которому человек поверит. Порог **95 %** против общих 90
  (`.coveragerc.core`), своя джоба CI, маркер `core` по факту импорта `app.core`.
  Замер: **97.01 %**. Три порога стережёт `TestCoreIsItsOwnContour`, проверен мутациями.

- **2026-09-06 — v9.4.0: ГЕЙТ ПОКРЫТИЯ ФРОНТА ЗЕЛЁНЫЙ (MINOR).** Порог, выставленный
  в v9.3.0 решением владельца, был честно красным при 80.8 %. Теперь проходит:
  строки **95.6 %**, statements **94.5 %**, ветки **90.2 %**, функции **90.5 %**,
  тестов 567 → **844**.
  🔴 **Ни одного исключения ради процента.** Единственное добавленное —
  `src/routes/**`, по свойству: `createFileRoute` это макрос, плагин переписывает
  файлы при сборке, и счётчик видит ЕГО ветки (файл в 6 строк даёт 5 ветвлений
  в колонке 57 первой строки). `validateSearch` покрыт отдельным тестом.
  🔴 **Тесты нашли дефект:** кнопка «Отмена» в форме семьи не сбрасывала ошибку —
  звала `setFormOpen(false)` мимо `onOpenChange`, где стоял сброс. Сообщение
  переживало закрытие и встречало человека при следующем открытии. Закрытие сведено
  в одну точку.
  **Закрыты:** пять строк списка (ни одна не имела теста при одинаковом паттерне
  обратимого удаления), формы взноса и бюджета, ветки отказа везде, краевые случаи
  данных. Правило, закреплённое тестами: **«не посчитано» и «ноль» — разные вещи**.

**Предыдущая точка: v9.3.0 — ГЕЙТ ПОКРЫТИЯ ФРОНТА 90%, СИММЕТРИЯ С БЭКЕНДОМ**

**Текущая точка: v9.3.0 — ФРОНТ ПОД ТЕМ ЖЕ ГЕЙТОМ ПОКРЫТИЯ, ЧТО БЭКЕНД**

- **2026-09-06 — v9.3.0: ГЕЙТ ПОКРЫТИЯ ФРОНТА 90%, СИММЕТРИЯ С БЭКЕНДОМ (MINOR).**
  🔴 **Решение владельца:** «тогда для бэкенда точно такие же требования как и для фронта
  покрытие должно быть больше или равно 90%», «сделай симметрично».
  **Что было:** бэкенд с `fail_under = 90`, фронт — `npm test` БЕЗ измерения покрытия
  вовсе. Половина продукта держала планку, вторая не измерялась, и разницу не было видно
  ниоткуда: обе джобы зелёные, обе называются «тесты».
  **Покрытие фронта: 68.6% → 88.3%**, тестов 567 → 734. Слой `entities` 7.2% → 80.3% —
  из худшего стал четвёртым. Порог поставлен СРАЗУ на 90 и сейчас честно красный:
  ступенчатый порог всегда зелёный, то есть не сообщает ничего.
  🔴 **Каждый файл проверен мутацией, и несколько тестов оказались слепыми** —
  исправлены на месте. Главная находка формы: человек вводит ставку в ПРОЦЕНТАХ (39),
  модель хранит ДОЛЮ (0.39); это единственное место встречи двух единиц, и ошибка там
  молча уезжает в Avalanche-фильтр (`PIT-031` — про то же с другой стороны).
  **Дальше:** до планки 35 строк — остаток в `HouseholdsPage`, `AllocationPanel`, формах.

**Предыдущая точка: v9.2.0 — ПЕРСОНАЛЬНЫЕ ДАННЫЕ ПОД ГЕЙТОМ**

**Текущая точка: v9.2.0 — ПЕРСОНАЛЬНЫЕ ДАННЫЕ ПОД ГЕЙТОМ, ВЕХА 9 (предподготовка)**

- **2026-09-06 — v9.2.0: CI ЗЕЛЁНЫЙ ПО-НАСТОЯЩЕМУ, ГЕЙТ ПО ПДн, ВТОРОЙ ФАКТОР ДОЖАТ (MINOR).**
  🔴 **Прогон v9.1.0 на живом CI всё равно упал** — владелец проверил push и спросил.
  Две причины, и обе поучительны. (1) **Битую ссылку вернул РАЗБОР инцидента:** текст,
  объясняющий дефект, содержит тот же путь `frontend/dist/index.html`, а проверка
  не отличает упоминание в прозе от ссылки — дефект размножился на три документа.
  Лечение свойством пути (`GENERATED_PREFIXES`), а не тремя записями в allowlist.
  Локально было зелено потому, что `dist/` собран: проверка смотрела на другой
  репозиторий, чем CI. (2) **`tsc` падал тремя ошибками:** маршрут `/login` не объявлял
  свои search-параметры, хотя `?redirect=` и `?ref=` ходили с v8.x — роутер принимал
  любой набор, и они работали молча.
  🔴 **`mfa_pending`-токен теперь сгорает** после пяти неверных кодов (миграция 0037).
  Счёт по ТОКЕНУ, не по пользователю: счётчик на пользователе сбрасывается повторным
  входом. В базе, не в памяти: за четырьмя воркерами память дала бы четыре счётчика.
  🔴 **Три находки `/code-review ultra`,** первый полноценный прогон: upsert бюджета
  терял `household_id` (попытка поделиться давала 201 и не делилась), мёртвый `_key`
  в rate-limit, «срок не задан» у цели со сроком.
  **Про сам ultra:** лимит **500 файлов / 8000 строк** — он для диффа одного батча,
  не для всей базы. Рабочая схема записана в роадмапе.
  🔴 **Покрытия фронта не существовало вовсе** — провайдер не поставлен, секции
  `coverage` в конфиге нет. Год обсуждали «мало тестов на фронт», ни разу не сняв
  величину. Замер: строки **69.6 %**, и разрыв НЕ размазан — `shared` 97.4 %,
  `features` 93.4 %, `widgets` 92.0 %, `pages` 79.2 %, **`entities` 7.2 %**.
  Четыре верхних слоя уже вровень с бэкендом. Закрыты три хука (22 теста), каждый
  проверен мутацией. Отчёт — `docs/reports/testing/frontend_coverage.md`.
  **Прогоны:** бэкенд 2046/2046, фронт 571/571 плюс 22 новых, mypy/flake8/eslint/
  prettier/tsc чисто, матрица SQLite + PostgreSQL пройдена.

- **2026-09-06 — v9.2.0 (часть первая): СКВОЗНОЙ ГЕЙТ ПО ПЕРСОНАЛЬНЫМ ДАННЫМ.** Закрыта таска
  владельца П1. Прежний гейт по секретам смотрел на восемь каталогов кода; «личные данные
  не утекают» этим не покрывалось. Первый же сквозной проход нашёл **27 живых почтовых
  адресов** респондентов опроса в `knowledge/` — каталоге, куда гейт не заглядывал
  **по построению**. 🔴 Отсутствие красного означало отсутствие проверки, а не отсутствие
  данных. Адреса ОСТАВЛЕНЫ решением владельца 06.09.2026: это список первой аудитории
  для запуска. Занесены названным исключением с причиной в коде; список ограничен тремя
  и проверяет сам себя.
  🔴 **Мутационная проверка нашла второе:** `logs/agent-audit.jsonl` копит персональные
  данные сам — хук пишет каждую команду дословно, и примеры из самого гейта осели
  в журнале через минуту после написания.
  Плюс: конфиг-гейт стал герметичным (читал `DATABASE_URL` из окружения и под PG-матрицей
  проверял машину, а не код), блок STATUS в README догнал версию, результат матрицы
  SQLite + PostgreSQL записан в `docs/reports/testing/pg_matrix_last_run.md`.
  **Дальше:** переобработать опрос целиком — выборка выросла, нынешние 385 ответов
  не финальные (поручение владельца 06.09.2026, занесено в роадмап §9.A).

- **2026-09-05 — v9.1.0: CI ПОЧИНЕН, КРИЗИСНЫЙ ПЛАН НА ЭКРАНЕ, 16 НАХОДОК РЕВЬЮ (MINOR).**
  🔴 **CI падал месяцами — три причины, все невидимые локально.** Логи прочитаны через
  `gh run view --repo` (локального `.git` нет, без явной репы `gh` падал — это и мешало
  смотреть логи раньше). (1) роадмап ссылался на `frontend/dist/index.html`, которого
  нет на чистом клоне; (2) снимок контракта отставал от схем — `tsc` падал
  на `household_id`; (3) `full`-тир запускал E2E ДО сборки фронта, сервер отдавал 404,
  и тир падал БЕЗ единой строки о причине. Плюс 54 файла отформатированы Prettier.
  🔴 **Кризисный план считался с v6.0.0 и не показывался НИКОМУ.** `grep crisis`
  по `frontend/src` давал ноль. Владелец, поручивший фичу, считал, что её нет.
  Схемы заведены до фронта, секция стоит ПЕРЕД панелью распределения.
  🔴 **Два прохода `/code-review` — 16 находок, четыре HIGH.** Rate-limit обходился
  одной строкой (nginx ДОПОЛНЯЕТ `X-Forwarded-For`, а не затирает); категория бюджета
  была уникальна ГЛОБАЛЬНО — второй пользователь с «Продуктами» получал 500; запись
  шла через читающий скоуп, и `viewer` правил чужое; у ошибок сгенерированного клиента
  нет `status`, из-за чего `isSessionExpired` не работал нигде, кроме профиля.
  Миграция 0036 (матрица PG — за CI). Backend 2005/2005, Vitest 567/567.

- **2026-09-05 — v9.0.0: ВЕХА 9 ОТКРЫТА, ТРИ ДЕФЕКТА ПЕРЕД ДЕПЛОЕМ (MAJOR).**
  Решение владельца: веха 8 закрыта, его тасок пока нет — занимаемся предподготовкой
  (ревизия, долги, стандарты). Нумерация версий с этого момента `9.x`.
  🔴 **Rate-limit за прокси считал клиентом nginx** — один адрес на весь интернет.
  Лимит тратился всеми вместе, и, исчерпав его, продукт отвечал 429 ВСЕМ сразу:
  защита от перебора работала как способ положить вход сервису. Локально не видно
  вовсе — прокси нет. `TRUST_PROXY_HEADERS` выключен по умолчанию: заголовок ставит
  клиент, и с доверием «всегда» лимит обходится одной подделанной строкой.
  🔴 **Второй фактор запирал аккаунт НАВСЕГДА** (гипотеза 9, стояла в «средних»).
  `/mfa/enroll` открыт любому, а войти с включённым MFA было нельзя — поля кода
  на экране входа не было. `/mfa/disable` требует уже аутентифицированной сессии:
  выключить фактор мог только тот, кто вошёл. Восстановление — правка базы руками.
  🔴 **Дашборд считал план по нулевому доходу** (гипотеза 10). Пустым считался только
  профиль без единой операции, а внёсший ТОЛЬКО расходы получал рекомендацию при
  income=0 — типичный первый заход после импорта выписки.
  **Вывод об аудите:** категория правдоподобности предсказывает уверенность агента,
  а не цену дефекта — самая дорогая находка стояла в «средних».
  Ревизия корня: четыре файла четырьмя способами появления, одна настройка мимо
  `.env.example`. Заведён гейт гигиены. Backend 1949/1949, Vitest 529/529.

- **2026-09-05 — v8.56.0: CSP, CSRF И СТАРТ-ГАРД ПРОД-КОНФИГА (MINOR).** Два пункта
  раздела 4.4, отложенные до закрытия вехи 8, плюс две находки при их разборе.
  **CSP:** `'unsafe-inline'` снят со скриптов — Jinja снесена, собранный SPA inline-скриптов
  не содержит. У стилей остаётся: Radix и `react-remove-scroll` ставят их в рантайме.
  🔴 Вскрылось, что middleware был БЕДНЕЕ nginx: `base-uri`, `form-action`, `connect-src`,
  `font-src` стояли только в прокси — ответ приложения защищался слабее того же ответа
  через nginx. Наборы выровнены, гейт сверяет копии директива за директивой.
  **CSRF:** докстрока утверждала «отсутствие Origin не несёт риска — атака требует
  амбиентных cookies». Рассуждение верное, вывод нет: наличие cookie проверяется
  по cookie, а не по Origin. Запрос **с auth-cookie и без Origin** проходил молча.
  Double-submit токен рассмотрен и отвергнут: даёт браузерным клиентам ту же гарантию.
  🔴 **Старт-гард дополнен тем, что ломает прод НЕ взломом.** `CORS_ORIGINS` с дефолтным
  localhost → 403 на каждое действие: сайт открывается, читается и ничего не сохраняет.
  `DATABASE_URL` со SQLite → файл в контейнере, исчезающий при пересоздании вместе
  с финансовыми данными. Оба дефолта не давали ошибки при старте.
  🔴 **Модалка снимков плана обещала невозможность того, что работает** (PIT-029):
  restore приехал в v8.38.0, а текст «восстановить будет нельзя» прожил 18 версий,
  и тесты его закрепляли. Снята — удаление как у остальных пяти сущностей.
  Backend 1929/1929, Vitest 523/523.

- **2026-09-05 — v8.55.0: «СЕМЕЙНЫЙ ДОСТУП» ДЕЛАЕТ ЗАПИСИ ОБЩИМИ (MINOR).** Последний,
  пятый пункт аудита `independent-expert`. `household_id` был заведён в схемах и колонках
  ПЯТИ сущностей, роли работали, приглашения рассылались, `_owner_filter` умел читать
  общий котёл с P3.7 — но проверка права стояла только в целях, четыре других эндпоинта
  поле из тела **не читали вовсе**, а формы про него не знали. Человек приглашал жену,
  отправлял операцию с `household_id`, получал 201 и `null`: продукт **принимал данные
  и молча их терял**. Ветка чтения общего котла была мёртвой — в него ничего не попадало.
  Сделано: одна проверка `_guards.ensure_can_share` на все сущности (довод `_FIN`),
  поле доезжает во всех пяти путях, `household_id` в пяти Response-схемах, общий контрол
  `HouseholdScopeField` в пяти формах и признак `SharedBadge` в пяти списках.
  🔴 **design-critic — пять блокеров.** Главный: общий контрол оказался в ДВУХ разных
  местах (в форме актива вторым полем, в четырёх — последним) — ровно тот разнобой,
  ради борьбы с которым он и заводился. Плюс подсказка занижала цену выбора («увидят»,
  тогда как `member` правит и удаляет, и запись входит в его расчёты) и не называла
  необратимость: `household_id` не принимает ни один PUT.
  Сквозной тест проверяет то, ради чего функция есть: второй участник видит общую
  операцию и не видит личную. Backend 1904/1904, Vitest 523/523.
  🔴 **ВЕХА 8 ЗАКРЫТА.** Все пять пунктов аудита сняты; блок «Предложения Claude» —
  в `docs/ROADMAP.md`. Дальше веха 9 (деплой) — СТОП-КЛАСС для авто-режима.

- **2026-09-05 — v8.54.0: СОВЕТЫ ПО РАСХОДАМ ВЫШЛИ ИЗ API НА ЭКРАН (MINOR).** Пункт №6
  аудита — тот, что реально закрывает §8.2 «ГЛАВНОЕ». `GET /planning/spending-advice`
  живёт с мат-модели v3.0.0: норма категории (медиана), аномалии (robust z-score на MAD),
  советы, мерчанты, тренды Theil-Sen, пересчёт экономии в месяцы до цели — **всё это
  считалось и не показывалось никому**, посмотреть можно было через `curl`.
  🔴 **Схема заведена ДО фронта** (`app/schemas/spending.py`, шесть моделей). Гейт
  проверяет не только `$ref` на ответе, но и `$ref` в `items` КАЖДОЙ коллекции:
  `list[dict]` внутри типизированной обёртки — та же дыра на уровень глубже, и она
  выглядит закрытой в отчёте (записано как PIT-027).
  🔴 **design-critic — восемь блокеров, все исправлены.** Экран открывался `<section>`
  вместо `<main>` (скип-линк вёл в никуда); 403 «согласие отозвано» показывался как
  техническая поломка, хотя хук специально его не ретраит; акцентный зелёный стоял на
  сумме каждого совета — при пяти советах итог переставал быть ответом; «на 1 месяца
  раньше» в главной формулировке выигрыша при живом `pluralize` с v8.37.0; ворота
  `enoughData` стояли только на итоге. **a11y-auditor:** WCAG 4.1.3 — главный случай
  экрана не объявлялся программе чтения ничем.
  🔴 **Полный прогон поймал восьмое: `/spending` не был в SPA-блоке nginx** — прямой
  заход и F5 давали бы 404 в проде.
  17 тестов экрана, 5 контрактных, десять мутаций — каждая роняет ровно свой тест.
  Backend 1866/1866, Vitest 510/510.

- **2026-09-05 — v8.53.0: ГОСТЬ БОЛЬШЕ НЕ ПИШЕТ В ОБЩИЙ ПУЛ (MINOR).** Пункт №2
  аудита, блокер публичного запуска. `_owner_filter(query, model, None)` фильтрует
  по `user_id IS NULL` — гостевой пул был **один на весь сервер**, без привязки
  к сессии. Посетитель Б видел операции посетителя А; `/demo/load` зовёт
  `_clear_all(user_id=None)` и стирал данные всех гостей разом. При этом продукт
  ВЁЛ гостя туда primary-кнопкой «Внести операции», а `upload_statement` брал тот же
  `get_current_user_id` — то есть в общий пул уезжала **реальная банковская выписка**.
  🔴 **Решение и почему именно оно.** На проде гостевая ЗАПИСЬ финансовых данных
  запрещена, чтение и демо остаются. Правильная починка — свой `session_id` каждому
  гостю с колонкой во всех финансовых таблицах: изменение схемы и правка каждого
  запроса, утечка столько не ждёт. Цена ошибки несимметрична — запрет обратим одной
  строкой конфига, утечка персональных данных нет. В development ничего не менялось.
  Гейт `require_account_for_writes` висит на роутерах целиком (довод `_FIN`: пропуск
  одной ручки — дыра, которую нечем заметить) и сам пропускает безопасные методы.
  Одно исключение, проверенное тестом: `POST /demo/analyze` не берёт `db` вовсе —
  без него гейт чинил бы утечку, закрывая воронку.
  Мутации: снять гейт → 7 красных; не смотреть метод → 3; распространить на dev → 1;
  убрать исключение → 1. Backend 1861/1861.

- **2026-09-05 — v8.52.0: ПРАВО ПО 152-ФЗ СТАЛО ДОСТИЖИМЫМ (MINOR).** Пункт №1 аудита,
  самый дорогой из одиннадцати. `POST /auth/change-password` и `DELETE /auth/me` жили
  на бэкенде **без единой кнопки**, при этом экран согласий отправлял человека именно
  туда («нельзя отозвать без удаления аккаунта»), а юрреестр помечал L9 выполненным —
  по факту существования `crud.delete_user`, то есть ФУНКЦИИ, а не пути пользователя.
  Повтор SEV1 `CONSENT-GATE-NO-UI`.
  🔴 **Правило записано в `compliance_register.md`:** отметка «закрыто» по юртребованию
  ставится за ПУТЬ пользователя. Функция без входа не исполняет права — она создаёт
  видимость исполнения в реестре.
  Появилось: секция «Аккаунт и безопасность» (после согласий, куда они и отправляют),
  смена пароля с предупреждением о выходе из всех сессий, удаление через подтверждение
  с перечнем стираемого, повторная отправка письма подтверждения (бейдж «не подтверждён»
  стоял без действия — на деплое без SMTP это было бы навсегда).
  🔴 **a11y-auditor: паттерн проекта документирован и не унаследован.** Нативный
  `disabled` вместо `aria-disabled` на трёх кнопках, включая подтверждение удаления
  ВНУТРИ focus-trap: фокус проваливается в `<body>` в момент клика. `Button.css`
  объясняет это прямым текстом, `ConsentsSection` применяет — новая секция нет.
  Плюс `--c-border` у danger-кнопки: rgba(…,0.08), границы фактически не было (1.4.11).

- **2026-09-05 — v8.51.0: АУДИТ ПЕРЕД ЗАКРЫТИЕМ ВЕХИ 8 — ВОРОНКА ВРАЛА, ЧУЖОЕ
  ВОССТАНАВЛИВАЛОСЬ, ДЕФИЦИТ МОЛЧАЛ (MINOR).** Вместо блока «Предложения Claude»
  запущен `independent-expert`: **одиннадцать разрывов, шесть высокой правдоподобности**.
  🔴 **Первым исправлено собственное ложное утверждение вахты** — «§8.2 закрыт целиком»
  (v8.49.0) не соответствовало факту: spending-advice живёт только в API, а чекбокс
  пункта всё это время стоял `- [ ]`. **Веха 8 НЕ закрыта.**
  🔴 **H7 подтверждена:** события писались без `user_id`, воронка фильтрует по нему —
  все шаги, кроме первого, были пусты ВСЕГДА. С v8.48.0 это врало владельцу с экрана.
  Поправлено 12 вызовов; попутно вскрылось, что фронт держал в словаре событие
  `register_success`, которого бэкенд никогда не писал.
  🔴 **Побочная находка того же следа — изоляция данных.** Правка не скомпилировалась:
  у `restore_transaction_endpoint` не было `user_id`. Оказалось, `restore_transaction` —
  единственная из пяти `restore_*` без проверки владельца: чужую удалённую операцию мог
  вернуть кто угодно, зная id, а она меняет баланс владельца и рекомендацию плана.
  **`deficit_alert`** больше не молчит: месяц, когда денег не хватит, показан ДО графика.
  **`RegisterPage`** перестал обещать перенос гостевых данных, которого код не делает (H2).

- **2026-09-05 — v8.50.0: СВЕРКА С КОНТРАКТОМ БОЛЬШЕ НЕ СНИМАЕТСЯ КАСТОМ (MINOR).**
  Долг вехи 8 из указателя. Пункт говорил про ОДИН каст в `usePlan.ts` — гейт нашёл
  десять совпадений: четыре оказались текстом правила в комментариях (гейт ловил
  собственную документацию, родня PIT-021), пять — мёртвым грузом (типы перевели на
  генерацию в v8.40.0/v8.48.0, касты остались и уже ни от чего не защищали; сняты,
  `tsc` прошёл без правок), один настоящим.
  🔴 **Настоящий вскрыл прод-баг.** `/budgets/status` отдавал `list[dict]`; схема
  `BudgetStatus` существовала, но не была подключена и не знала про `id`. При заведении
  контракта выяснилось: `crud.get_budget_status` делил `float` на `Decimal` —
  **500 на экране бюджетов у каждого, кто завёл бюджет и ещё ничего не потратил.**
  Путь «бюджет есть, трат нет» не проходил ни один тест: все заводили бюджет вместе
  с операциями. Арифметика переведена на `Decimal`, три регрессионных теста.

- **2026-09-05 — v8.49.0: РЕЗУЛЬТАТЫ A/B НА ФРОНТЕ, §8.2 «ГЛАВНОЕ» ЗАКРЫТ (MINOR).**
  Последняя API-only фича вехи 8. Доступ — признак `is_owner`, как у метрик.
  🔴 **Экран считает значимость, а не показывает два числа рядом.** 12 % против 10 %
  на полусотне наблюдений неотличимы от шума, и человек читает большее как победу.
  Заведён `app/services/experiment_stats.py` — двухпропорциональный z-тест против
  контроля, считает СЕРВЕР (иначе каждый клиент считал бы по-своему). Пустая выборка
  даёт `null`, а не ноль: вариант, который ни разу не показали, не провалился.
  Схемы `ExperimentResults`/`VariantResult` заведены ДО фронта.
  🔴 **Полный прогон нашёл: гейт подписей навигации был красным С v8.48.0** — там
  добавили `/insights` и не пополнили закрытый словарь теста. Увидели через сутки,
  потому что в том батче полный прогон не довели до конца. Долг материализовался.
  🔴 **Агенты: восемь блокеров, пять из них — выдуманные токены.** Экран рисовался
  не так, как написан: `--radius-lg`, `--c-text1`, `--c-accent-soft`, `--radius-sm`,
  `--c-surface2` не существуют (правильные — `--r-lg`, `--c-text`, `--c-accent-bg`,
  `--r-sm`, `--c-bg2`). Невалидное значение custom property откатывается к начальному:
  фон победившей строки не рисовался вовсе, а комментарий рядом обещал WCAG 1.4.1.
  **Подготовка вехи 9:** Docker на машине отсутствует (проверены docker/colima/podman/
  nerdctl/lima/brew), стенд поднять нечем — вместо него `tests/test_deploy_config.py`:
  пути существуют, секреты не литералами, `:?` fail-loud, БД и web только на loopback,
  наружу один nginx, том БД именованный.

- **2026-09-04 — v8.48.0: ПОСЛЕДНИЙ РУКОПИСНЫЙ ТИП ФРОНТА УБРАН, У ВЛАДЕЛЬЦА ПОЯВИЛСЯ
  ЭКРАН МЕТРИК (MINOR).** Четыре темы батча.
  🔴 **`ForecastResult`/`ForecastPoint` знали 5 полей из 12** — рукописный тип пережил
  переход на генерацию клиента, и фронт не видел `deficit_alert`, `trend`,
  `stable_baseline`, `method`: сигнал о грядущем дефиците приходил с сервера и не
  доезжал до человека. Схемы `ForecastResponse`, `AnalyticsOverview`, `FunnelStep`,
  `FunnelResponse`, `DemoCase`, `DemoPreview` заведены на бэкенде ДО фронта.
  **Аналитика перестала быть API-only.** `/analytics/*` и `/admin/experiments` были
  закрыты `ADMIN_API_KEY` — посмотреть их можно было только через `curl`. Развилку решал
  владелец: признак `is_owner` в аккаунте, а не ключ в браузере (ключ пришлось бы держать
  в `localStorage` — секрет за пределами `.env`). Миграция 0035, `server_default=sa.false()`.
  🔴 **Заголовок врал про период:** сводка считалась за 30 дней, воронка — за всю историю,
  а подпись над обеими стояла одна. Владелец сравнивал несравнимое.
  🔴 **PostgreSQL был доступен всё это время.** Канал считался исчерпанным по `pg_isready`,
  а СУБД просто не запущена: стоит через Homebrew. Первая попытка старта упала не от
  отсутствия СУБД, а от длины пути сокета (>103 байт) — снаружи выглядит одинаково.
  Матрица закрыта впервые за долгое время: 37/37 на PG, цепочка 0007 → 0035.
  Рецепт для macOS — `docs/sandbox_runbook.md` §8. Класс 4 в лидерборде рецидивов.

- **2026-09-04 — v8.47.0: JINJA СНЕСЕНА ЦЕЛИКОМ (MINOR).** Веха 8 закрыта по существу.
  🔴 **Срочным снос сделала находка:** оставшиеся `/validation` и `/contacts` наследовали
  `base.html` с ПОЛНОЙ старой оболочкой — навигация по семи экранам, селектор демо-кейсов,
  `app.js` на 2658 строк. Человек по ссылке на контакты попадал в ДРУГОЙ продукт.
  **Раздел валидации перенесён, а не удалён** — и перенос закрыл дефект: `/demo/preview`
  считает портрет без записи в БД и раскрывается в карточке песочницы, то есть человек
  больше не выбирает вслепую действие, которое необратимо стирает его данные.
  **Вскрылось при сносе:** у SPA не было фавиконки и описания ВООБЩЕ (иконки подключал
  только `base.html`, сорок версий вкладка стояла безымянной); тест контраста читал
  палитру Jinja, а не React — сорок версий сторожил не тот интерфейс; `/static/` в nginx
  проксировался в никуда, а иконки из корня сборки уходили на бэкенд. PIT-022.
  **Тесты, потерявшие предмет:** `test_a11y_mechanical` (разбирал HTML Jinja — покрытие
  переехало в axe, 24/24, надмножество тех правил), `test_api_pages` (стал бы ЛОЖНО
  зелёным: catch-all отдаёт `index.html` на любой путь).
  Снос — переносом в `docs/legacy_jinja/`, не удалением: удаление необратимо и остаётся
  решением владельца.

- **2026-09-04 — v8.46.0: ГОСТЕВАЯ ПЕСОЧНИЦА ВЕРНУЛАСЬ В REACT (MINOR).** Выбор одного
  из десяти демо-портретов был в Jinja и потерялся при переносе; сервер всё умел, не было
  только пути. Живёт в пустом состоянии дашборда — там, где человек упирается. Схема
  (`DemoCase`/`DemoCases`) заведена ДО фронта.
  🔴 **Главное: загрузка портрета оказалась ДЕСТРУКТИВНОЙ.** `/demo/load` вызывает
  `_clear_all` и жёстко удаляет всё, что гость внёс, мимо мягкого удаления и отмены —
  а подпись под кнопкой обещала «ваши данные не заменят и никуда не сохранятся», оба
  утверждения ложные. Гость терял свои операции одним кликом. Закрыто подтверждением
  и честной подписью.
  **Текст портретов переписан целиком:** девять полей из двадцати имели склейку слов
  («несколько целей.Свободный поток»), в прозе стояли «Avalanche», «Fail-loud», «BLR»,
  «Bliq», «ПДН», «runway» — нотация модели, запрещённая [CMP-05]; числа шли с точкой
  («27.9%») и рвущимися разрядами. `accent` слал `cyan`/`violet`/`slate`, которых фронт
  не знает: четыре портрета из десяти молча теряли цвет ярлыка.

- **2026-09-04 — v8.45.0: ФРОНТ ВЕХИ 8 ВПЕРВЫЕ ОТДАЁТСЯ СЕРВЕРОМ (MINOR).**
  🔴 Сорок с лишним версий фронта не существовали нигде, кроме машины разработчика:
  `GET /transactions` отдавал Jinja-шаблон, React отвечал только через Vite на 5173.
  Три независимых звена, каждого хватало: Dockerfile фронт не собирал, `dist` в
  `.gitignore` (значит `COPY . .` его не приносил), FastAPI сборку не монтировал,
  а Jinja-роуты выигрывали как более точные. Проверки молчали ПО ПОСТРОЕНИЮ: Playwright
  поднимает тот же dev-сервер, CI собирает фронт и выбрасывает артефакт. PIT-020.
  **Грозило вехой 9:** в контейнер уехал бы продукт со старым интерфейсом; снос Jinja
  «следующим шагом» оставил бы продукт вообще без интерфейса.
  **Заодно вскрылось, что тир `full` не исполнялся** — нет своей версии Chromium. После
  установки axe нашёл два НАСТОЯЩИХ нарушения: `aria-hidden-focus` на графике прогноза
  (Recharts вставляет фокусируемые узлы внутрь `aria-hidden` — добавлен `inert`) и
  `color-contrast` 4.49:1 при норме 4.5 в светлой теме (промах на сотую, глазом не виден).
  Axe теперь 24/24.
  **Три ложных красных разобраны:** ключ темы в axe-тесте был от Jinja; гейт
  `.dockerignore` неверно воспроизводил семантику Docker (`*` не пересекает `/`) и
  вызвал правку несуществующего дефекта — PIT-021; тест обхода каталога проверял путь,
  который нормализуется до отправки.
  **Найдено попутно:** гостевая песочница (демо-портреты + раздел валидации) потеряна
  при переносе на React — сервер всё умеет, входа нет. Заведено в ROADMAP.

- **2026-09-04 — v8.44.1: ХУК PIT-018 СТОЯЛ МЁРТВЫМ (PATCH).** Бит `+x` слетел после
  полной перезаписи файла (`Write` прав не хранит), и `PreToolUse` его не запускал.
  🔴 Отказ бесшумный: код на месте, регистрация на месте, ошибки нет — «зелено» означало
  «не проверялось». Нашла соседняя вахта своим обходом системы, а не гейт этой репы:
  собственный гейт хука проверял разбор команд и регистрацию, но не то, исполняется ли
  файл. Восстановлено, проверено ЗАПУСКОМ в обе стороны, заведён `test_hook_is_executable`
  (проверен мутацией).

- **2026-09-04 — v8.44.0: ТЕКСТЫ ЮРДОКУМЕНТОВ В REACT + ОДИН ИСТОЧНИК ПРАВДЫ (MINOR).**
  Снято последнее препятствие к сносу Jinja: политику, оферту, cookie-политику и тексты
  согласий показывает React. Заведён `GET /legal/documents/{slug}` со схемой
  `LegalDocumentContent` — эндпоинта с СОДЕРЖИМЫМ не было вовсе, только реестр метаданных.
  Текст отдаётся markdown, потому что пакет ведётся в markdown и из него печатаются
  `docx`: конверсия на бэкенде завела бы ТРЕТЬЮ редакцию. Путь берётся из реестра по
  ключу и не склеивается из ввода (иначе `../../.env` читался бы публичным эндпоинтом).
  🔴 **Главное: продукт публиковал ДВЕ противоречащие редакции одних документов** — Jinja
  из `config.py` (ООО «ФИНПАЙЛОТ», хранение 6 мес., `support@finpilot.app`) против пакета
  `docs/legal/` (сервис FINPILOT, 1 год, `finpilot.help@proton.me`). Расхождение прожило
  от v6.25.0 при том, что оба файла лежали рядом. По 152-ФЗ доказывать пришлось бы тот
  текст, который человек видел, а какой — зависело от отрендеренного шаблона. Авторитет
  у пакета, `config.py` выровнен, гейт `test_legal_single_source.py` не даст разойтись.
  **Агенты вскрыли:** согласие при регистрации давалось вслепую (ссылки на политику
  у чекбокса не было); уход на документ стирал заполненную форму (`<a href>` вместо
  `Link`); фронт выбрасывал 503-диагноз сервера и слал чинить исправный Wi-Fi; таблица
  теряла роль таблицы и не бралась с клавиатуры; заголовок вкладки не менялся между
  документами; ритм текста задавал браузер, а не токены.
  🔴 **Отдельная находка: E2E прогонялся в ОДНОМ браузере из трёх.** `playwright.config.ts`
  объявляет chromium, firefox, webkit; два движка не были установлены, прогоны шли
  с `--project=chromium`, и «E2E 60/60 зелёный» за десяток версий означало треть матрицы.
  Для этого продукта расхождения WebKit не теория (`display: block` на таблице, 204
  с `application/json`, Safari у аудитории с iPhone). После установки — **189/189 во всех
  трёх**. PIT-019.
  **Решение владельцу:** баннер «документ в стадии оформления» не перенесён —
  `docs/reports/decisions/2026-09-04_legal_texts_single_source.md`.

- **2026-09-04 — v8.43.0: ИМПОРТ ВЫПИСКИ В REACT + `banks_router` ПОД `_FIN`, H3 ЗАКРЫТА
  (MINOR).** Последняя записывающая функция, жившая только в Jinja, переехала в React.
  **Дыра в периметре 152-ФЗ:** импорт выписки обрабатывал финданные ПАЧКОЙ и без согласия,
  тогда как ручное добавление ОДНОЙ операции согласия требовало. Чем больше данных
  загружалось за раз, тем меньше проверок стояло на пути. Гейт нельзя было поставить раньше
  (решение v8.27.0: только вместе с фронтом) — единственный потребитель `app.js` не умеет
  показать 403, `grep "403|consent"` даёт ноль на 2658 строк. Теперь условие снято.
  **Схема заведена ДО фронта:** `StatementUploadResult` и `BankOption` вместо `dict`; поля
  успеха необязательны — ошибка разбора приходит со статусом 200 и `status="error"`.
  **Агенты вскрыли главное в первой редакции:** у сверки итогов ТРИ исхода, а не два —
  `unavailable` («сверять было нечем») считался расхождением, и на исправном импорте человек
  получал жёлтое «итоги не сошлись» и шёл вручную перепроверять сотню строк. Плюс: сошедшаяся
  сверка не показывалась вовсе, прошлый результат не гасился до отправки, ответ и отказ жили
  в разных местах, подсказка формата не зависела от банка.
  🔴 **PIT-018 — инцидент рабочего процесса:** команда вахты вызывала диалог разрешения
  харнесса, неотличимый от вопроса вахты; владелец в авто-режиме читал его как нарушение
  прямого приказа. Больше десяти срабатываний за сессию. Корень — относительный путь-АРГУМЕНТ
  у читающей утилиты; первый диагноз («`cd` с относительным путём») был неверен, обход
  соблюдался и не помогал. Хук написан как ТЗ, ставит владелец: запись в `.claude/` блокирует
  классификатор.

- **2026-09-04 — v8.42.0: ПАРАМЕТРЫ РАСЧЁТА В REACT + H3 РАЗЛОЖЕН (MINOR).**
  Батч по поручению владельца «сделай ресерч и прими взвешенное решение». Разбор по диску
  (`docs/reports/decisions/2026-09-04_h3_and_jinja_removal_order.md`) показал, что оба
  вопроса стояли на неверных допущениях.
  **Главная находка:** риск-профиль в React изменить было НЕЛЬЗЯ ВООБЩЕ — контролы плана
  жили только в Jinja. Профиль задаёт веса SAW; человек видел рекомендацию по чужому
  умолчанию и повлиять не мог. Панель «Параметры расчёта» построена.
  **H3 — это две задачи разного риска:** `plaid_router` закрыт немедленно (ноль
  потребителей, 404 в РФ-сборке — оговорка v8.27.0 защищает от отказа без объяснения,
  а фронта у него нет вовсе); `banks_router` привязан к переносу ИМПОРТА ВЫПИСКИ, потому
  что его зовёт `app.js`, где `grep 403|consent` даёт ноль на 2658 строк.
  **Снос Jinja НЕ может быть следующим батчем:** футер L7 ссылается на Jinja-роуты
  `/legal/*` — снос превратил бы требование в мёртвые ссылки (класс H12).
  **Агенты нашли, что ОБОСНОВАНИЕ решения ложное:** «пересчёт тяжёлый» опровергается
  соседом на том же экране (Монте-Карло живьём с дебаунсом). Настоящая причина — PATCH
  пишет постоянные настройки. Плюс: успех объявлялся раньше пересчёта, отката не было,
  live-регион тараторил при драге, подсказка врала при `l_min = 0`, заливки выбранного
  в светлой теме не существовало.
  Backend 1741/1741, Vitest 401/401, E2E 60/60, preflight чист.

- **2026-09-04 — v8.41.0: ЭКРАН СОГЛАСИЙ ЦЕЛИКОМ (MINOR).** Закрыты L3 и L2 —
  остаток юрблока фронта.
  **Главное:** экран согласий знал ОДИН тип из трёх (`financial_data`, вшитый в разметку).
  Отозвать согласие на рассылку было невозможно, согласие на обработку ПДн не показывалось
  даже справочно — а по 152-ФЗ право отозвать есть у каждого, и отсутствие механизма это
  нарушение, а не пробел интерфейса. Теперь список строится из ответа `/consents` (он несёт
  ВСЕ типы, включая невыданные), названия и адреса — из реестра документов, новый тип
  появится сам. `withdrawable` решает, показывать ли кнопку отзыва: у согласия-основания
  вместо неё объяснение, потому что кнопка дала бы 409 — тупик.
  **Схема заведена до фронта:** `ConsentsResponse`/`ConsentState` вместо `-> dict`.
  **Два гейта, оба проверены мутацией:** сверка фронтового перечисления типов
  с `CONSENT_TYPES` бэкенда и запрет предотмеченных чекбоксов (L2 — соблюдалось, но
  держалось на внимательности).
  Backend 1727/1727, Vitest 382/382, E2E 58/58, preflight чист.

- **2026-09-04 — v8.40.0: ЮРИДИЧЕСКИЙ КОНТУР ФРОНТА (MINOR).** Закрыты L5, L6, L7 —
  три требования, без которых нельзя запускаться в РФ. Дисклеймер 39-ФЗ теперь приходит
  ПОЛЕМ ответа `/planning/calculate` (раньше не отдавался вовсе — фронт был бы вынужден
  перепечатать юридический текст у себя); cookie-баннер с раздельным выбором, привязанным
  к редакции политики; футер со ссылками из реестра на каждой странице, включая гостевые.
  **Долг «генерировать plan-summary из контракта» закрыт** — и вскрыл СЕМЬ мест, где код
  звал необязательные поля как обязательные (сортировка по показателям давала NaN
  в компараторе, то есть произвольный порядок). Фикстуры тестов тоже были неполными.
  **Агенты нашли восемь блокеров:** согласие `primary` против отказа `ghost` плюс автофокус
  на согласии (тёмный паттерн там, где требование его запрещает); `z-index: 250` поверх
  модалки, где баннер не кликается; баннер накрывал футер — L6 гасил L7; fail-open в обе
  стороны при сетевой ошибке; «изменить решение» обещано комментарием и не существовало.
  **Браузерный тест поймал два расхождения:** проверка дисклеймера была зелёной по ложной
  причине (находила текст в футере, а не на экране плана), и `<aside>` давал роль
  `complementary` вместо заявленной `region`.
  Backend 1713/1713, Vitest 380/380, E2E 57/57, preflight чист.

- **2026-09-04 — v8.39.0: ШРИФТ ПРОДУКТА (MINOR).** `--font-body: "Manrope"` был
  объявлен токеном и НЕ загружался ниоткуда — весь SPA двадцать восемь версий рисовался
  системным шрифтом. Найдено design-critic в v8.10.0, дожило до v8.39.0: класс тот же,
  что у мёртвого `/join` и непрочитанного `?ref=` — объявление есть, механизма нет,
  глазами не видно (на машине разработчика Manrope мог стоять системно).
  Подключён самохостом (`@fontsource-variable/manrope`), не CDN: рынок РФ, CSP вехи 9,
  один запрос вместо двух.
  🔴 **Гейт под это сначала оказался слепым:** искал подстроки по исходникам и находил
  их в КОММЕНТАРИЯХ, объясняющих самохост. Мутация (снятие пакета) прошла незамеченной.
  Переписан на разбор кода: комментарии вырезаются, `@font-face` ищется блоком, пакет —
  именем зависимости. Теперь мутация краснеет.
  Backend 1701/1701, Vitest 361/361, E2E 54/54, preflight чист.

- **2026-09-03 — v8.38.0: ОТМЕНА УДАЛЕНИЯ СНИМКА ПЛАНА (MINOR).** Продукт говорил
  на двух языках удаления: у пяти сущностей удаление обратимо, у снимков плана —
  помечалось мягким и не возвращалось никак. Данные лежали в базе недостижимыми:
  «сказали, что удалили, а на деле спрятали».
  **Оценка задачи была исправлена сверкой с диском:** указатель прошлого батча называл
  это изменением схемы с матрицей SQLite+PostgreSQL — колонки уже существовали, хватило
  запроса в CRUD и эндпоинта по образцу `restore_goal`.
  Скоуп владения взят тот же, что у удаления (`get_plan_snapshot`), иначе удалить было
  бы можно, а вернуть нет; проверка подтверждена мутацией. Живой снимок — 404, а не
  молчаливый успех. На фронте — «Вернуть» в тосте, как у остальных сущностей;
  браузерный тест проверяет, что кнопка доходит до пользователя и запрос уходит.
  Backend 1699/1699, Vitest 361/361, E2E 53/53, preflight чист.

- **2026-09-03 — v8.37.0: РЕФЕРАЛКА (MINOR).** Пятая API-only фича.
  **Главная находка:** `/register?ref=CODE` открывался, регистрация проходила — и не
  засчитывалась НИКОМУ: `RegisterPage` не читал параметр `ref` и не клал `referral_code`
  в запрос, хотя бэкенд его принимает. Вся программа не работала, и увидеть это можно
  было только по нулевому счётчику у всех. Злее мёртвой ссылки v8.36.0: там пустота
  видна, здесь всё выглядит рабочим.
  **Гейт достижимости теперь из двух проверок:** экран существует И экран читает
  параметр. Вторая подтверждена мутацией.
  **Схема заведена до фронта:** `ReferralMe` и соседние вместо `-> dict`.
  **Агенты нашли:** второй primary-CTA на странице, побайтовый дубль блока «ссылка +
  Скопировать» (вынесен в `shared/ui/CopyLinkField`), сломанные русские числительные
  («1 приглашений»), непроектированное состояние нуля, витрину достижений без единой
  реальной награды, h2 за h2, невидимость самой починки на форме регистрации.
  Backend 1694/1694, Vitest 360/360, E2E 53/53, preflight чист.

- **2026-09-03 — v8.36.0: СЕМЕЙНЫЙ ДОСТУП (MINOR).** Четвёртая API-only фича и самая
  крупная: двенадцать эндпоинтов households, фронта не было ни строки (H16).
  **Главная находка:** `routes_households.py::_invite_url` строит `/join?token=...` и
  кладёт этот адрес в приглашение, а роута `/join` в SPA НЕ БЫЛО ВОВСЕ — приглашение
  вело в пустоту, и увидеть это глазами нельзя: ссылка уходит копированием во внешнюю
  переписку. Второй случай класса H12; гейт достижимости расширен на ЛЮБУЮ собранную на
  сервере ссылку (из пяти мест мёртвым был один адрес).
  **Вторая половина той же дыры:** токен не переживал вход — `LoginPage` всегда уходил
  на `/`. Заведён `?redirect=` + `safeRedirect` (только внутренние пути: открытый
  редирект — схема фишинга).
  **Агенты нашли:** удаление участника и отзыв приглашения шли в один клик, хотя рядом
  роспуск спрашивает подтверждение; вторая ссылка приглашения затирала первую (сервер
  отдаёт её один раз); наблюдателя нельзя было пригласить вовсе; немой клик на пустом
  названии; фокус проваливался в `<body>` после каждого удаления; `--c-border-hl` в
  тёмной теме 2.90:1 при пороге 3:1 — это граница ghost-кнопок ВО ВСЁМ продукте.
  Всё починено до сдачи. Backend 1686/1686, Vitest 344/344, E2E 51/51, preflight чист.

- **[!] СНЯТО КАК НЕВЕРНОЕ.** Находка прошлого батча «две панели согласия в 300 px на
  `/planning`» опровергнута сверкой с диском: `PlanningPage.tsx:42-57` делает ранний
  return. Находки агентов проверяются по диску так же, как саммари.

- **2026-09-03 — v8.35.0: ВЫГРУЗКА ПЛАНА CSV/XLSX/PDF (MINOR).** Третья из API-only фич.
  **Ключевое решение:** скачивание через `fetch`+blob, а не `<a href download>`.
  Авторизация в cookie, ссылка формально сработала бы — но у неё нет обработки отказа,
  а экспорт за гейтом согласия: 403 приходит JSON-ом, и ссылка скачала бы его файлом
  `.csv`. Пользователь получил бы «отчёт» с текстом ошибки внутри.
  **Агенты нашли ТРИ функциональных дефекта** — фича могла молча не работать:
  blob освобождался синхронно сразу после клика (браузер не успевал начать скачивание:
  ни файла, ни ошибки, ни обратной связи); якорь кликался вне DOM (Firefox такое
  игнорирует); не было ни таймаута, ни отмены — подвисший сервер запирал все три кнопки
  навсегда. Плюс ТРИ нарушения AA: успех не подтверждался ничем (плашка загрузок
  браузера не в счёт — чужой UI, в Safari мигает, при «скачивать без спроса» её нет),
  фокус проваливался в `<body>` при 403, две соседние кнопки выглядели рабочими и
  молчали. Всё починено до сдачи.
  Backend 1684/1685, Vitest 318/318, E2E 46/46, preflight чист.

- **[!] НЕ СДЕЛАНО СОЗНАТЕЛЬНО — продуктовые решения, ROADMAP §8.2.**
  Три равновесные ghost-кнопки это не иерархия, а её отсутствие: действие одно
  («выгрузить план»), формат — его параметр. На ВСЁМ `/planning` нет ни одного `primary`
  (проверено грепом), и выгрузка — естественный кандидат. Экспорт не знает про согласие,
  которое экран уже выяснил через историю планов: рисует три кнопки, каждая из которых
  гарантированно даст 403, и показывает вторую такую же панель рядом с первой.

- **[!] ДОЛГ: `/code-review` временно́й лаборатории** — три попытки, все упали
  (`HTTP 429` дважды, затем `HTTP 403 Please run /login`). Инструмент прогнан тестами
  19/19, четыре дефекта найдены собственным перечитыванием, но взгляд со стороны не
  получен. §2в требует ревью каждого нового инструмента.

- 🔴 **СЛЕДУЮЩАЯ ЗАДАЧА — ВЕХУ 8 ЗАКРЫВАТЬ РАНО. Аудит independent-expert 05.09.2026
  нашёл одиннадцать разрывов, шесть из них высокой правдоподобности.** Отчёт целиком —
  `docs/research/raw/independent_expert_milestone8_closing_2026-09-05.md`.
  🔴 **Моя собственная ошибка, исправлена здесь же:** записи «§8.2 ГЛАВНОЕ закрыт целиком»
  в v8.49.0 не соответствуют факту — в перечне пункта остаётся **spending-advice слои 1-3**,
  живой на `GET /planning/spending-advice` и не имеющий фронта вовсе. Формулировку я взял
  из собственного предыдущего абзаца, не сверив со списком; чекбокс пункта при этом всё
  время стоял `- [ ]`. Тот же класс, что PIT-014 — утверждение о состоянии по памяти.
  **Порядок работ (по цене ошибки для пользователя):**
  1. ~~Удаления аккаунта и смены пароля нет в UI~~ — **закрыто v8.52.0**: секция
     «Аккаунт и безопасность», плюс правило в юрреестре «отметка ставится за путь,
     а не за функцию».
  2. ~~Гость пишет в ОБЩИЙ на всех анонимный пул~~ — **закрыто v8.53.0**: на проде
     гостевая ЗАПИСЬ финансовых данных требует аккаунта, чтение и демо открыты.
     Сессионный пул с `session_id` — отдельная задача: изменение схемы всех финансовых
     таблиц, а утечка её не ждала.
  3. **`RegisterPage` обещает перенос гостевых данных**, а `register` явным комментарием
     отказывается их переносить (старая гипотеза H2, подтверждена).
  4. ~~`deficit_alert` не показывается~~ — **закрыто v8.51.0**, предупреждение стоит
     до графика.
  5. **`household_id` игнорируется в API транзакций**, форм для него нет ни в одной
     сущности: «Семейный доступ» не делает общим ничего.
  6. ~~spending-advice на фронт~~ — **закрыто v8.54.0**: экран «Советы по расходам»,
     схема `SpendingAdviceResponse` заведена до фронта. Вместе с ним закрыт и чекбокс
     §8.2 «ГЛАВНОЕ» — по факту, а не по формулировке.

  **Закрыто в v8.51.0:** п.3 (`RegisterPage` перестал обещать перенос), п.4
  (`deficit_alert`), плюс не входившие в список H7 (воронка без `user_id`) и изоляция
  восстановления транзакций. **v8.52.0:** п.1. **v8.53.0:** п.2. **v8.54.0:** п.6.
  **Осталось:** п.5 — household ничего не делает общим.

> 🔴 **Здесь только текущая точка.** Раньше §0 держал 71 запись и 1424 строки —
> то есть был архивом, а не точкой входа (`PIT-116`). Предыдущие точки вынесены
> в `WATCHLOG_ARCHIVE.md` без потерь; полная история версий — в `CHANGELOG.md`.


## §1. Протокол вахты + аккаунты

**Аккаунты:** Василий работает на 4 аккаунтах Claude — **V, J, M, S** — несущих вахту по очереди
через этот журнал (плюс несколько чатов внутри одного проекта). Текущую вахту описывает **M**.
Имена нужны только если Василий уточняет «акк V/J/M/S» в разборе; на работу не влияют — журнал
самодостаточен для любого.

**Заступая на вахту:**
1. Прочитай этот файл + `docs/ROADMAP.md` (направление) + `docs/sandbox_runbook.md` (грабли
   песочницы — НЕ диагностируй заново).
2. Распакуй архив, сверь версию в `app/config.py` с §0.
3. Подними venv (`python3 -m venv .venv` + `pip install -r requirements.txt -r requirements-dev.txt`);
   для E2E — `playwright install chromium`.
4. Отчитайся Василию коротко (3-4 строки): версия, текущая веха, следующий шаг.
5. НЕ переделывай закрытое, НЕ уходи с порядка ROADMAP без команды.

**Неся вахту:** обновляй §0/§4 по ходу; разборы оформляй САМ по триггеру (инцидент/расследование/
pitfall — маппинг в §5); в конце батча WATCHLOG живёт ВНУТРИ архива (`docs/WATCHLOG.md`) — наружу
отдаётся только сам архив. Отдельным `.md` через `present_files` журнал НЕ дублируем (политика
«вся документация внутри продукта», v5.23.0; подробно — `docs/session_continuity.md` §4). Исключение —
явный разовый запрос владельца дать файл.

---

## §2. Направление (выжимка ROADMAP)

Полностью — `docs/ROADMAP.md`. **Вехи проекта нумеруются как мажоры (v4-v9), это СОЗНАТЕЛЬНО не
классический SemVer** (мажор = крупный этап, не слом контракта). Внутри вехи: фича=MINOR, фикс=PATCH.

| Веха | Версия | Тема | Статус |
|---|---|---|---|
| 4 | `v4.x.x` | Завершение бэкенда + техдолг | В работе (остатки §4.4 deferred → 8/9) |
| 5 | `v5.x.x` | Организационное (репозитории, доки, бизнес, исследования, методички, банки, наука) | **Завершена** |
| **6** | `v6.x.x` | **Тестирование** (мат-модель + независимая экспертиза + импорт) | **← в работе** (v6.0.0: экспертиза 12000×4, модель → v3.1.0, согласие 87.4%) |
| 7 | `v7.0.0` | Юридическое (документы + соответствие законам) | Запланировано |
| 8 | `v8.0.0` | Фронтенд (React+TS, редизайн, вынос всех возможностей бэка) | Запланировано |
| 9 | `v9.0.0` | Деплой на сервер | Запланировано |

Порядок строгий: бэк → организация → **тестирование → юр → фронт** → деплой. _(Пересмотр 2026-07-02,
приказ владельца: математика замораживается и проверяется до фронта; юр-тексты — контент экранов,
фронт вшивает их сразу; юр-ответы внешние и медленные — запускаем раньше. Прежний порядок — `docs/roadmap_archive/`.)_

---

## §3. Последние 10 версий (новое сверху; РОВНО 10 — добавил новую, удали старую снизу)

> Полная история — `CHANGELOG.md` / `docs/RELEASES.md`. Здесь только 10 свежих (правило в шапке).
>
> **Окно восстановлено в v8.30.2.** До этого оно держало 10 записей, но обрывалось на v8.28.0 —
> версии 8.28.1–8.30.1 в него не попали, то есть «десять свежих» было неверно уже пять версий.
> Собрано заново из `CHANGELOG.md`. Вытесненные 8.24.0–8.28.0 (CRUD-паритет батчи 1-2, SEV1
> `CONSENT-GATE-NO-UI`, расширение гейта согласия) не потеряны — они в `CHANGELOG.md`, а их
> сюжет целиком стоит в шапке этого файла выше.

- **v9.7.0** — третий аудит: вчерашний фикс создал шестой случай того же класса (MINOR).
  Согласиться на новую редакцию документа было нельзя, а для обязательного согласия —
  без обхода вовсе. Три собственных гейта оказались зелёными по случайности. Долг ревью
  временно́й лаборатории закрыт: три дефекта, включая занижение худшего случая вдвое.
  Конвейер анализа опроса не запускался вовсе. Четвёртый проход нашёл ещё шесть —
  и снова в правках этого же батча. Детали — CHANGELOG [9.7.0].
- **v9.6.0** — второй аудит экспертом: захват аккаунта и запись курсов без прав (MINOR).
  `forgot-password` отдавал ссылку сброса в теле ответа на проде без SMTP; `PUT /fx/rates`
  стоял без единой проверки прав. Плюс 401-петля ещё в восьми местах (два — права
  по 152-ФЗ), молчащий экран согласий, ложная причина пустого плана, floor в каноне
  против кода, чистка публичного зеркала. Детали — CHANGELOG [9.6.0].
- **v9.5.0** — пункт E закрыт: 401 на отправке формы был тупиком (MINOR).
  9 сквозных сценариев от чистой регистрации через форму. Находка: v9.1.0 починил 401
  на загрузке экрана и оставил форму, хотя сценарий был назван дословно в шапке его
  собственного теста. 🔴 Поправка внутри батча: сопутствующий «общий анонимный пул»
  я ошибочно объявил новым SEV1 — он закрыт ещё в v8.53.0, а замер шёл в dev, где
  запрет снят намеренно. Детали — CHANGELOG [9.5.0].
- **v9.4.0** — планка 90% взята, E2E воскрешён, ядро стало третьим контуром (MINOR).
  Строки 95.6%, ветки 90.2%, функции 90.5%, тестов 844. Исключений ради процента нет:
  единственное — `src/routes/**` по свойству макроса, с отдельным тестом на логику.
  Тесты нашли дефект: «Отмена» в форме семьи не сбрасывала ошибку.
  Детали — CHANGELOG [9.4.0].
- **v9.3.0** — гейт покрытия фронта 90%, симметрия с бэкендом, `entities` закрыт (MINOR).
  Фронт не измерял покрытие вовсе; теперь тот же порог, что у бэкенда, и гейт следит,
  чтобы они не разъехались. 68.6% → 88.3%, тестов 567 → 734, `entities` 7.2% → 80.3%.
  Детали — CHANGELOG [9.3.0].
- **v9.2.0** — CI зелёный по-настоящему, гейт по ПДн, второй фактор дожат (MINOR).
  Две причины падения CI закрыты гейтами (битую ссылку вернул разбор инцидента;
  `/login` не объявлял search-параметры). Таска владельца П1 закрыта: 27 живых адресов
  найдены в `knowledge/` и оставлены названным исключением по решению владельца.
  `mfa_pending`-токен сгорает после пяти кодов (0037). Три находки `ultra`.
  Детали — CHANGELOG [9.2.0].
- **v9.1.0** — CI зелёный впервые за месяцы, кризисный план на экране, 16 находок
  двух проходов code-review (MINOR). Три причины падений CI — все невидимы локально;
  четыре HIGH, включая обход rate-limit и 500 у каждого второго пользователя бюджетов.
  Детали — CHANGELOG [9.1.0].

- **v9.0.0** — веха 9 открыта: три дефекта перед деплоем (MAJOR). Rate-limit за прокси
  считал клиентом nginx (429 всем сразу); второй фактор запирал аккаунт навсегда
  (гипотеза 9 из «средних» оказалась блокером); дашборд считал план по нулевому доходу.
  Плюс ревизия корня и гейт гигиены. Детали — CHANGELOG [9.0.0].

- **v8.56.0** — CSP без inline-скриптов, дыра CSRF, старт-гард прод-конфига (MINOR).
  Два отложенных пункта 4.4 плюс находки: middleware был беднее nginx по директивам;
  запрос с cookie и без Origin проходил молча; `CORS_ORIGINS` и `DATABASE_URL`
  с дефолтами ломали прод без единой ошибки. Плюс PIT-029 — модалка снимков плана
  обещала необратимость при работающем restore. Детали — CHANGELOG [8.56.0].

- **v8.55.0** — «Семейный доступ» делает записи общими (MINOR). Поле было в схемах пяти
  сущностей, проверка права — в одной, четыре эндпоинта поле не читали, формы про него
  не знали: продукт принимал `household_id` и молча терял. Одна проверка на все,
  контрол в пяти формах, признак в пяти списках. design-critic поймал тот самый разнобой,
  против которого общий контрол и заводился. **Веха 8 закрыта.**
  Детали — CHANGELOG [8.55.0].



## §4. Заметки и находки (не переоткрывать)

- **Месячные показатели — за текущий месяц, НЕ за всю историю (v6.4.0):** `calculate_income_total`/
  `calculate_expense_total` суммируют все транзакции; границу месяца ставит `prepare_data(period_days=30)`
  (единственная точка — у неё ровно 3 вызова, всем нужен месяц). У кого нужна вся история — явно
  `period_days=None`. Латентный баг «доход = сумма всех месяцев» закрыт.
- **Прогноз — на РЕАЛЬНЫХ транзакциях, костыль убран (v6.4.0):** синтетического канала истории больше нет;
  `build_monthly_history` бинует полный список по скользящим 30-дневным окнам (устойчиво к границам месяцев),
  последний бин = месячный тотал. Демо-кейсы получили реальную многомесячную историю (`_history_transactions`,
  `_case_data`) → «Валидация» и `/planning` читают один источник. Признак костыля был: одна сущность считается
  по-разному в двух местах.
- **`run_planning` требует у каждой цели ключ `id` (v6.4.0):** в проде его проставляет сериализатор;
  при прямом вызове (демо/валидация) — проставлять синтетически (`g{i}`), иначе `KeyError: 'id'`.
- **`goals.deadline` — NOT NULL (v6.4.0):** бессрочные цели на уровне БД пока не поддержаны; в демо —
  дальний горизонт (10 лет). Nullable-дедлайн = отдельная миграция (пункт роадмапа).
- **`build_monthly_history` теперь по СКОЛЬЗЯЩИМ окнам, не календарным (v6.4.0):** тест, кодировавший
  календарное поведение, обновлён (`test_forecast_shape`). Красный тест после осознанной смены поведения —
  сверять «кто прав» (тест или код), а не рефлекторно чинить код.
- **Голый `TestClient(app)` НЕ создаёт таблицы (v6.4.0):** для разовых end-to-end прогонов —
  `Base.metadata.create_all(bind=engine)` на временной SQLite; в тестах это делает фикстура.

- **Эталон экспертизы регенерируется ТОЛЬКО генератором v1 (v6.0.0):** `knowledge/model_validation/joined.csv.gz`
  восстановим строго `PortraitGenerator(seed=20260702, version=1)` — v1 в `tools/portrait_testing/generator.py`
  заморожен бит-в-бит ради этого (self-check стенда: статус 12000/12000). Любые новые прогоны — генератор v2
  (default). Раннер: `--gen-version`. Не «чинить» v1 и не менять его RNG-поток.
- **Инвест-транш — разметка, не четвёртое направление (v6.0.0):** решётка 66 альтернатив и инвариант сумм I4
  не тронуты; транш вычисляется поверх `x_reserve` сверх Lt*·Σe (`app/core/investment.py`). В метрике согласия
  с экспертами транш считается в бакет goals+ (семантика заключения: цели и инвестиции объединены).
- **Floor резерва — лексикографический уровень, не вес (v6.0.0):** сортировка ranked — по (floor_level, utility);
  `best.utility` может быть НЕ максимумом по списку — это норма (инвариант I5 обновлён, I13 добавлен).
  Golden-снапшоты при изменениях модели пересматриваются глазами (floor-эффект задокументирован в сценариях).

- **GitHub-тулы — разбор витрины Actions (2026-07-05, вахта M):** трёхуровневый CI (`ci.yml`: fast/full/deep,
  в full уже `bandit`+`pip-audit`+locust, в deep — `mutmut`+стресс-property) закрывает потребности продукта.
  Из marketplace-предложений **почти всё мимо**: Fortify (платный SAST — `bandit` покрывает Python),
  Jscrambler (JS-обфускация не наш кейс), SLSA (оверкилл до запуска), деплой-шаблоны облаков (RU-деплой на
  VPS+docker, санкционные облака нерелевантны), Django/Pylint/Python-app шаблоны (наш CI сильнее),
  Greetings/Labeler/AI-summary (соло-разработчик). **Что реально стоит включить (бесплатно, БЕЗ CI-минут):**
  1) **Dependabot alerts** — тумблер в Settings→Security обеих реп (и приватной). Именно **alerts**, НЕ
     update-PR: авто-PR конфликтуют с нашим forward-build (rsync `--delete` из архивов перетёр бы мердж).
     Закрывает окно между тегами, когда `pip-audit` (только в full-тире, на тегах) молчит.
  2) **Secret scanning + push protection** — Settings публичного зеркала (бесплатно на паблике).
  3) **CodeQL** — бесплатен ТОЛЬКО на публичных репо → внедрён этим релизом как `mirror_extras/…/codeql.yml`
     (в приватный не кладём: без GHAS воркфлоу падал бы). Портфолио-бейдж + межпроцедурный SAST сверх bandit.
  Вывод: писать новые воркфлоу не нужно — три тумблера в Settings + CodeQL уже в зеркале.

- **PIT-009 (2026-07-02, корень подтверждён — Tor Browser; инцидент ЗАКРЫТ):** качать артефакты в обычном браузере + VPN. Три фейла за день: карточки
  не появились (v5.12.0), «мёртвая заглушка» — кнопка есть, скачивание падает (v5.14.0), пропажа
  файлов владельца «вверх». Протокол: доставка не подтверждена до скачивания; outputs не трогать;
  перевыдача → копия под НОВЫМ именем (выданный путь неприкасаем!) → критический минимум;
  sha256 каждой выдачи в чат; владельцу на Tor — новая цепочка, поштучно, сразу. Разбор:
  `docs/reports/incidents/inc_chat_file_delivery_dead_stub.md`.

**PIT-008 (v5.13.0):** `find && echo ЕСТЬ` печатает «ЕСТЬ» и без находок (exit 0 всегда) — так чуть
не потеряли методичку роадмапа между линиями. Существование проверять `test -f` / непустым выводом.
Детали — `docs/pitfalls.md`.

**Рассинхрон flake8-гейта (app/ vs весь проект) — вскрыт и закрыт в v4.30.2.** Гейт flake8 определён в
ТРЁХ местах, и все гоняют `flake8 .` (весь проект): CI `ci.yml:80`, `Makefile` цель `lint`, pre-commit
hook. А руками каждый батч гоняли `flake8 app/` (узко) — поэтому `app/` держали в нуле, а `tests/`/
`loadtest/` незаметно копили E501/F841 (101 нарушение к v4.30.1). Официального GitHub-репа ещё нет →
полный гейт вживую ни разу не запускался, первый CI-run был бы красным. **Урок: гейт руками гонять ТЕМ
ЖЕ скоупом, что CI** — для flake8 это `flake8 .`, не `flake8 app/`. Выровнено вариантом A (реальное
приведение к 0, не игнор-конфиг): autopep8 --select по коду, точечный `# noqa: E501` по data-литералам
(enforcement нового кода жив), XML-фикстура → конкатенация. При merge-боли откатный путь — per-file-
ignore E501 для tests/ в `.flake8` (вариант C). Заодно убран `black` из pre-commit — прямое нарушение
§2 (латентная merge-мина: `make precommit` переформатировал бы весь проект).
> **Рецидив в v5.20.0 (тот же класс):** вахта J в v5.19.0 внесла `tools/portrait_testing/render_portrait_cards.py`
> с 27 нарушениями (26×E501 + 1×E741) — снова проверяли узким `flake8 app/`, а не `flake8 .` (CI-скоуп,
> `continue-on-error`, поэтому красный лог не заметили). Лечение вариантом C (§4 fallback): `per-file-ignores`
> в `.flake8` — строки >100 это выходные литералы досье-карточек, перенос менял бы текст эксперту, реформат
> запрещён (§2). Инвариант «flake8 репо = 0» восстановлен; файл J не тронут; ядро papки под гейтом (инъекция
> подтвердила). **Урок повторно: перед выдачей ВСЕГДА `flake8 .`, не `flake8 app/` — даже для docs/tooling-батча.**

**Текущая фаза планирования (что согласовано с Василием):**
- Карта версий 4-9 подтверждена; правило «вехи ≠ SemVer» — осознанное решение Василия.
- Фронт-стек React+TS зафиксирован с обоснованием — `docs/reports/adr/adr_001_frontend_stack.md`.
- Техдолг (mypy/flake8, utcnow, ExpenseRecord) РАЗБЛОКИРОВАН: работаем с конечной версией
  последовательно, параллельных правок нет → конфликтов мёржа не будет.
- Принцип заглушек вехи 4: MVP/каркас для того, что в роадмапе и точно нужно фронту; спекулятивное
  не выдумываем (YAGNI).

**Мат-модель — канон `docs/model/history/math_model_v3_0_0.md`** (источник истины — код): 66 альтернатив, шаг 10%,
stock-based `Lt = B_liq/E` (месяцы), L_min=0 (фильтр выключен), MC-интервал 80%, инварианты Rt≥0 и
ПДН≤0.40. Если всплывёт спека с 21 альтернативой / шаг 20% / flow-based Lt / 95% — это УСТАРЕВШАЯ
v2.0.2, не источник истины.

**Время — единый контракт (после BUG-026, v4.25.3):** всё «сейчас» в коде идёт через
`app/utils/time.py::utcnow()` (naive UTC). Прямой `datetime.now()` (локаль сервера) в `app/` —
запрещён, он давал 3ч-сдвиг на MSK-VPS. Исключение — намеренные aware-вызовы `datetime.now(timezone.utc)`
в `cbr_rate`/`cbr_fx`/`security` (там нужен aware UTC). Колонки моделей — naive UTC (вариант A, не aware).
Костыль `obligation.replace(tzinfo=None)` оставлен намеренно — гасит aware-вход из Pydantic, это НЕ про
«сейчас». Полная tz-корректность (зона юзера → локальный календарь для дни-недели/внутримесячных
паттернов) — отдельный заход в слое паттернов, там же заведётся поле `tz` у User (сейчас его нет).
Вариант B (миграция timestamp-колонок на `timezone=True`) НЕ нужен для корректности приложения —
только для интеропа БД у вехи деплоя; шов готов (убрать `.replace(tzinfo=None)` в helper). Полное
обоснование развилки A/B (почему B — не требование корректности) — `docs/reports/adr/adr_002_datetime_storage.md`.

**JWT-ревокация (v4.26.0, раздел 4.4) — устройство и контракт.** Логика отзыва централизована в
`app/database/revocation.py`: точечный рубеж — блок-лист `is_token_revoked`/`revoke_token`/`purge_expired`;
массовый — предикат `tokens_invalidated_for(user, iat)` + `bump_tokens_valid_since`; конвертер
`epoch_to_naive_utc` нормализует JWT-таймстампы (int/float/aware-datetime) в naive-UTC. `dependencies`
и `routes_auth` таймстампы НЕ трогают — только зовут модуль (`revoke_token` принимает сырой `exp`).
Рубеж `tokens_valid_since` режется до секунды (iat — секундная гранулярность), сравнение строгий `<` —
немедленный re-login после logout-all проходит. Легаси-токены без `jti`/`iat` остаются валидными
(обратная совместимость, покрыто тестом).

**Инвалидация сессий при смене/сбросе пароля (v4.27.0, раздел 4.4) — устройство и дизайн-выбор.**
Подключена к тому же массовому рубежу `tokens_valid_since`. `change_password`: `bump_tokens_valid_since`
(валит другие устройства) + `_revoke_request_token` (точечно гасит текущий токен по `jti` — закрывает
sub-секундное окно округления рубежа: токен той же секунды иначе пережил бы строгий `<`) + чистка
auth-cookie + событие `password_changed`. `reset_password`: только `bump` (юзер не залогинен; текущего
токена/cookie нет; вход новым паролем проходит сразу — округление рубежа не задевает свежий токен).
Изменения ТОЛЬКО в `routes_auth.py`, миграций нет (механизм из 0027 переиспользован). **Дизайн-выбор
Option A — намеренный, НЕ переоткрывать:** для финпродукта (банковская планка) смена креденшелов валит
ВСЕ сессии, включая текущую — сильнее и ожидаемее «оставить текущую» (Google/GitHub). Option B
(сохранить текущую сессию) сознательно отклонён: требует ре-выпуска токена + нового cookie + чтобы
фронт подхватил новый токен, а фронт — отдельная веха (пока Jinja+vanilla JS). Инвалидация привязана к
УСПЕХУ смены (verify + update прошли); неверный текущий пароль → 400, рубеж не сдвинут. Карта линии для
слияния с другими вахтами — `docs/reports/merges/merge_manifest_v4_27_0.md`.

**Ротация ключа шифрования (v4.28.0, раздел 4.4) — устройство и эксплуатация.** `TokenCipher` под
капотом — `MultiFernet`. Ключи берутся из `settings.token_encryption_keys_list`: `TOKEN_ENCRYPTION_KEYS`
(через запятую, PRIMARY первым) → один `TOKEN_ENCRYPTION_KEY` → derive из `JWT_SECRET`. `encrypt` всегда
шифрует PRIMARY (первый ключ), `decrypt` перебирает весь набор, `rotate` перешифровывает токен primary.
Перешифровка существующих данных — `app/database/reencrypt.py::reencrypt_all`: колонки находятся
**интроспекцией** маппинга (`isinstance(col.type, EncryptedString)`), поэтому новое шифрованное поле
попадёт в перешифровку без правки модуля — не вести ручной список. Переписывает и старо-зашифрованные
(читаются прежним ключом набора), и legacy-незашифрованные (читаются как есть → шифруются primary).
**Эксплуатация ротации без простоя:** (1) сгенерировать ключ `Fernet.generate_key()`, поставить ПЕРВЫМ в
`TOKEN_ENCRYPTION_KEYS`, старый оставить вторым; (2) `python scripts/reencrypt_keys.py`; (3) убрать
старый ключ из набора. Миграций схемы нет — формат хранения тот же, меняется только ключ. `EncryptedString`
сохраняет обратную совместимость (decrypt вернул None → отдаёт значение как есть), так что включение
шифрования на ранее незашифрованных данных безопасно.

**Security-регрессии и старт-гард ключа (v4.29.0, раздел 4.4) — защитная сетка, не ослаблять.**
`tests/test_security_regressions.py` фиксирует инварианты харденинга: (1) security-заголовки на ЛЮБОМ
типе ответа — работает потому, что `SecurityHeadersMiddleware` зарегистрирован ВНЕШНЕ `CSRFMiddleware`
(порядок `add_middleware` в `main.py`: Rate→CSRF→SecurityHeaders→Logging; внешний обрабатывает response
последним), поэтому даже 403 от CSRF проходит через установку заголовков. Не менять порядок middleware
вслепую — заголовки на ошибках/отказах слетят. (2) CSRF-блок на всех изменяющих методах, с исключениями
Bearer/api-key/без-Origin (срабатывает по методу ДО роутинга — тестируется на любом пути). (3) **Старт-гард
ужесточён:** в production обязателен явный `TOKEN_ENCRYPTION_KEY` или `TOKEN_ENCRYPTION_KEYS` — без него
ключ шифрования деривится из `JWT_SECRET`, что лишает смысла ротацию (v4.28.0) и разделение ключей. Это
**намеренное требование деплоя** (`.env.example` обновлён) — не откатывать ради удобства локального прода.

**MFA/TOTP (v4.30.0, раздел 4.4) — устройство и инварианты.** Второй фактор на `pyotp`. Состояние на
User: `mfa_secret` (зашифрован `EncryptedString`) + `mfa_enabled`; пока `mfa_secret` задан, а
`mfa_enabled=False` — это «pending» (enroll сделан, confirm нет). Крипто — `services/mfa.py`, доступ к
данным — `database/mfa_store.py` (разделение как у revocation). **Ключевой инвариант — двухступенчатый
логин:** при `mfa_enabled` `/auth/login` после верного пароля НЕ ставит cookie и НЕ выдаёт сессионный
токен — возвращает `mfa_pending`-токен (purpose-claim, TTL 5 мин); полную сессию выдаёт только
`/auth/mfa/verify` после TOTP/recovery-кода. **Не выдавать сессию в login при включённом MFA** — это
вся суть фичи. Recovery-коды: 10 шт, bcrypt-хеш, одноразовы (`used_at`), показываются один раз при
confirm; и verify, и disable принимают TOTP ИЛИ recovery-код. **Связанный инвариант (purpose-guard):**
`get_current_user` отвергает ЛЮБОЙ токен с claim `purpose` — полная сессия его не имеет. Это держит
mfa_pending (и заодно email_verify/password_reset/telegram_link) вне сессионного доступа; не ослаблять
эту проверку, иначе промежуточный/одноразовый токен станет полноценным входом.

**Консолидация трёх параллельных линий v4.26.0 (правило §5 #15).** JWT-ревокацию делали параллельно
три вахты (M, J, S). Эта версия — их слияние по лучшим практикам, не переписывание: ядро `revocation.py`
(инкапсуляция + предикат + конвертер) и тест-набор — из линии M (самый полный, 17 кейсов, форж токена с
прошлым iat для кросс-сессионного logout-all); DRY-хелпер `_revoke_request_token` в routes — из линии J;
обвязка релиза (CHANGELOG/ROADMAP/WATCHLOG) — из S. PIT-004 (распознать сбой канала) восстановлен по
линии M, PIT-005 (на возобновлении верь архиву) — урок линии J. Модели/`security.py`/миграция 0027 у
всех трёх совпали — мёрджить нечего.

**Консолидация S+J (v4.27.0→v4.28.0).** Линия S наработала только docs-добавки поверх J-базы:
`docs/pitfalls.md` (PIT-006 — `cd X && job &` фонит весь пайплайн вместе с `cd`, приоритет `&` ниже
`&&`) и `SECURITY.md` (строка «Отзыв сессий» в таблице §2 + раздел §3a «Харденинг сверх минимума»).
Сверено `diff` против J-версий: расхождение только в этих аддитивных вставках, иного дрейфа нет —
файлы внесены к J без конфликтов. Код S и J не расходился (S работал по докам). Внесено в архив v4.28.0.

**Известные баги к разбору в вехе 4** (давние заметки Василия): (1) ставка ЦБ всегда ставит дефолт —
реального fetch нет (вероятно cbr.ru 403 → зеркало `cbr-xml-daily.ru`); (2) письма «отправлены», но
на почте пусто (SMTP no-op в dev маскирует). Диагностика до корня, не угадывать.

**Открытые вопросы для вехи 6** (тестирование): график прогноза — у Василия ощущение что баг (всегда
прямая линия от константы-зарплаты); нужен тест-кейс где НЕ прямая. Совет при `Rt<0` — исследовать
(реальный кейс Василия с кредиткой). Шаг 5% вместо 10% — замерить скорость.

**PostgreSQL поднимается в песочнице** (не спихивать на разработчика): `apt install postgresql` БЕЗ
`apt-get update` ПРИ ОТКЛЮЧЁННОМ nodesource (он даёт 403 и травит весь update; Ubuntu-репы рабочие —
`mv /etc/apt/sources.list.d/nodesource.sources{,.disabled}`, затем `apt-get update` уже проходит) →
`pg_ctlcluster 16 main start` → роль/БД через `su postgres -c psql` →
`DATABASE_URL=postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test`.
**PG-хвост ЗАКРЫТ (v4.30.1):** вся цепочка 0001↔0028 прогнана вживую на PG 16 — `upgrade head` ✓,
`downgrade base` ✓, обратно ✓; миграции 0023–0028 аддитивны и реверсивны. Регрессия зафиксирована
`tests/test_migration_reversibility.py` (под PG; скип на SQLite). Больше PG-миграций в очереди нет.

**BUG-027 (v4.30.1) — reverse-каст 0009 на PostgreSQL.** `downgrade()` миграции 0009 кастует
`user_id` varchar(36)→integer для `events`/`recommendations` без `USING` → на PG `DatatypeMismatch`
(SQLite молчит — динамическая типизация). Фикс: `postgresql_using="user_id::integer"` на обе колонки
(на пустой таблице каст — no-op, проходит). Трогает ТОЛЬКО downgrade-ветку 0009, upgrade не изменён →
накатанные базы не задеты. Это была единственная нереверсивная точка цепочки; найдена код-ревью +
живым PG-прогоном, не на проде.

**Сбой канала тул-коллов — методичка `docs/tool_call_channel_failures.md` (v4.30.1).** Развёрнутый
PIT-004: вызов инструмента иногда выходит в ответ ВИДИМЫМ текстом (`court` + сырой `<invoke>`) и ход
обрывается, но песочница жива. Кейсы: завершение хода вызовом, сложная команда (heredoc/кавычки),
**заражение паттерном из истории** (сломанные хвосты в контексте воспроизводятся как текст), короткий
ход между обрывами. Протокол: НЕ завершать ход вызовом (кончать текстом + `present_files`),
игнорировать сломанные образцы из истории, весь батч одним ходом, при обрыве — реран с проверки
состояния (не полный hand-off — это лечится рераном). Не объяснять глюк по пять раз.

**Грабли песочницы** (детали — `docs/sandbox_runbook.md`): полный одновременный прогон всех тестов
рубится лимитом инструмента (~30с → returncode -1) → гонять пофайловыми группами; итоговая строка
pytest не флашится в файл → использовать `--junitxml` + парсить XML + exit code; фоновые процессы не
флашат лог надёжно; многострочные bash с `\` ломаются; браузеры firefox/webkit недоступны (Azure CDN),
chromium есть.

---

## §5. Жёсткие правила (чек-лист «не дрейфовать»)

1. **TDD** — тест → красный → реализация → зелёный, реально прогонять.
2. **`app/core/` не трогать** без согласования (исключение — `spending_advice.py`).
3. **Схемные изменения — на PostgreSQL** (SQLite молчит про FK).
4. **SemVer внутри вехи:** фича=MINOR, фикс=PATCH. Bump `APP_VERSION` + `CHANGELOG.md` каждый батч.
   Вехи (v4-v9) — крупные этапы, не классический SemVer (см. §2).
5. **Без массового реформата** кроме координированных окон (сейчас окно открыто — техдолг закрываем).
6. **ROADMAP — источник приоритетов, строго по порядку.** Не изобретать параллельную нумерацию.
7. **Без эмодзи.** Русская проза + английский код. По-братски, прямо, без канцелярита.
8. **НЕ объявляй невозможным, не проверив.** PostgreSQL/chromium/apt поднимаются здесь. Реально
   внешнее (cbr.ru 403, живой SMTP, деплой) — честно отметить.
9. **Перед «с нуля» — проверь, не сделано ли уже** (бэк часто опережает план).
10. **Вывод тестов — через `--junitxml`** (не `tail` по логу): итоговая строка не флашится надёжно.
    Парсить XML + exit code. (§4, `docs/pitfalls.md`.) **Что гонять и когда (узкий цикл vs полный) —
    `docs/test_run_optimization.md`; механика полного fast — `docs/sandbox_runbook.md` §7.**
11. **НИКОГДА не писать имена в гите / релизах / публичных артефактах** — официально безымянно.
12. **Тестирование мат-модели — СТРОГО вне проекта** (Opus 4.8, без памяти/инструкций → не biased).
13. **E2E добавляются с unit** для изменений фронта/API; CRUD-флоу обязаны иметь E2E.
14. **Разборы — АВТОМАТИЧЕСКИ по триггеру:** дефект в коде → инцидент (`docs/reports/incidents_summary.md`);
    искали, но дефекта нет → расследование (`docs/reports/investigations_summary.md`); повторная ошибка
    воркфлоу → pitfall (`docs/pitfalls.md`); найденный баг → карточка по `docs/reports/bug_report_template.md`.
15. **Слияние разошедшихся линий → консолидация, не переписывание истории.** Полная процедура,
    гейты и антипаттерны — **`docs/reports/merges/merge_and_fork_guide.md`**. Профилактика: синкать аккаунты чаще,
    координировать номера версий (версия закреплена за задачей, не за аккаунтом).
16. **Чекпоинт-дисциплина (непрерывность).** Не копить работу до конца батча: как только есть
    рабочее состояние — собрать архив + WATCHLOG и отдать через `present_files`. В начале крупной
    задачи — оценка объёма и **вероятность уложиться**; по ходу — переоценка, при росте риска
    немедленный чекпоинт. Готовое отдавать прежде тяжёлого хвоста. Протокол — `docs/session_continuity.md`.
18. **Исходник первичного материала — в репозиторий ВСЕГДА и ПЕРВЫМ.** Отчёты глубоких
    исследований, ответы независимых экспертов, чужие выгрузки замеров, длинные логи — всё, что
    получено извне и не воспроизводится повторно бесплатно, сохраняется **дословно** в
    `docs/research/raw/` (или профильный `raw/`) с датой в имени, ДО написания выжимки. Реестр URL
    исходник не заменяет: ссылка отвечает «где это лежит», исходник — «что там было написано на
    момент чтения»; первое протухает, второе нет. Пересказ без сохранённого исходника —
    нарушение, даже если пересказ хороший. Почему правило появилось — `docs/pitfalls.md` PIT-013.
17. **Отчёты — по единому диспетчеру** `docs/reports/reporting_policy.md` (триггер → тип → шаблон →
    папка → реестр). Конвенция имён всего — `docs/naming_convention.md`.
19. **Решение за владельцем → структурный разбор, а не вопрос в чате.** Стратегия, архитектура
    или необратимая правка данных, где последнее слово за владельцем, оформляется документом в
    `docs/reports/decisions/`: что предлагается · фактические изменения с ЗАМЕРЕННЫМИ числами ·
    полная стоимость, включая неочевидную · что необратимо · не меньше двух вариантов, включая
    «ничего не делать» · явная рекомендация вахты с обоснованием · что изменило бы рекомендацию.
    Вахта не решает молча, даже если ответ кажется очевидным. Шаблон и статусы —
    `docs/reports/decisions/README.md`.

---

## §6. Стабильная библиотека (читать из архива `/docs`; НЕ дублировать сюда)

| Файл | Зачем |
|---|---|
| `docs/ROADMAP.md` | **Направление: вехи 4-9, все задачи** (первоисточник приоритетов). |
| `docs/sandbox_runbook.md` | Грабли песочницы + готовые команды (bootstrap, тесты, E2E, PG). |
| `docs/RELEASES.md` | История развития по эпохам (v1→v4). |
| `docs/reports/adr/adr_001_frontend_stack.md` | Обоснование React+TS (веха 8). |
| `docs/reports/adr/adr_002_datetime_storage.md` | Обоснование naive-UTC (вариант A) и отказа от aware-миграции (B). |
| `docs/reports/adr/adr_template.md` | Шаблон для фиксации архитектурных решений. |
| `docs/model/history/math_model_v3_0_0.md` | Историческая мат-модель (источник истины по параметрам на тот момент). |
| `docs/algorithm_stack.md` | Как мат-модель работает по шагам в коде (9-шаговый конвейер). |
| `docs/diagrams.md` | Mermaid-диаграммы (архитектура, ER, конвейер ядра, потоки). |
| `docs/ui_visual_design_brief.md` | Эстетическое ТЗ фронта (веха 8). |
| `docs/engineering_practices.md`, `docs/CONTRIBUTING.md` | Инженерные решения + процесс (TDD, гейты, DoD). |
| `docs/reports/merges/merge_and_fork_guide.md` | **Как сливать/форкать разошедшиеся линии одной версии между аккаунтами** (процедура, гейты, антипаттерны; раскрывает §5 #15). |
| `docs/reports/merges/merge_manifest_v4_27_0.md` | **Прикладная карта линии v4.27.0 для слияния** (surface, зоны конфликта, инварианты I1–I6, чек-лист) — инстанс гайда под этот батч. |
| `docs/test_run_optimization.md` | **Что гонять и когда:** узкий цикл vs полный fast — как не ждать ~9 мин на каждой правке. Механика полного — runbook §7. |
| `docs/ui_ux_design_standard.md` | Полная спецификация UI/UX (токены, состояния, a11y). |
| `SECURITY.md` (корень) | Единый security-док: меры (ссылки на код), что осталось, домены ревью, план реагирования, тезисы. |
| `docs/cybersecurity_finpilot_checklist.md` | Прикладной чек-лист ИБ (что закладывать, приоритеты). |
| `docs/glossary.md` | Термины (Rt, Lt, Dt, BLR, Sn…). |
| `docs/QA.md`, `docs/reference_profiles.md`, `docs/reports/bug_report_template.md` | QA-уровни, эталоны, формат багов. |
| `docs/reports/incidents_summary.md`, `docs/reports/investigations_summary.md`, `docs/pitfalls.md` | Реестры (инциденты / расследования / частые ошибки). |
| `docs/reports/report_types.md`, `docs/reports/incident_postmortem_guide.md`, `docs/reports/investigation_report_guide.md` | Роутер типов разбора + шаблоны. |
| `docs/documentation_methodology.md` | Общая теория (blameless, RCA, runbook, ADR/RFC, SLO). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
| `docs/legal_questions_for_lawyer.md` | Вопросы юристу (веха 8). |
