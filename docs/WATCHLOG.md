# FINPILOT — Вахтенный журнал (быстрый снимок «где мы сейчас»)

> # [!!!] ПРОТОКОЛ НЕПРЕРЫВНОСТИ — ЧИТАЙ ПЕРВЫМ, ЭТО ОБЯЗАТЕЛЬНО ДЛЯ КАЖДОГО КЛОДА
> **Не копи работу до конца батча. Собирай рабочий архив РАНО и ЧАСТО и отдавай через `present_files`.**
> Токены/контекст кончаются внезапно — если архив не скинут, работу нельзя ни закончить, ни передать
> на вахту, и всё встаёт на часы. Поэтому:
> 1. В начале крупной задачи — оценка объёма + **вероятность уложиться** (высокая/средняя/низкая).
> 2. Готовое отдавай ДО того, как браться за тяжёлый/творческий хвост.
> 3. По ходу переоценивай «успею/нет»; при росте риска — **немедленный чекпоинт-архив**.
> 4. WATCHLOG держи актуальным ПО ХОДУ (не в конце) — это точка передачи на другой аккаунт.
>
> **Полный протокол — `docs/session_continuity.md`. Нарушение = потеря работы и простой. Не пропускай.**

> **Что это и зачем.** Снимок-контекст для мгновенного входа в работу. Василий ведёт продукт на
> **4 аккаунтах Claude (V, J, M, S)** и в нескольких чатах внутри проекта; токены кончаются —
> вахту принимает другой аккаунт/чат. Этот журнал даёт принимающему быструю сводку: **где стоим,
> что сделано, что дальше, на какие грабли не наступать** — чтобы поднять картину с ОДНОГО архива,
> не читая 20k строк кода. (Метафора в тему бренда — штурман у штурвала.)
>
> **Принцип тонкости.** Журнал НЕ дублирует историю. Полная история версий — в `CHANGELOG.md`
> (каждый патч) и `docs/RELEASES.md` (эпохи). Направление и задачи — в `docs/ROADMAP.md`. Здесь
> только выжимка + ссылки.
>
> ## [!] ЖЁСТКОЕ ПРАВИЛО: §3 хранит РОВНО 10 последних версий, не больше
> §3 — это окно из **10 самых свежих версий, и только**. Добавляя новую версию сверху, СРАЗУ удаляй
> самую старую снизу, чтобы их всегда оставалось 10. Без этого журнал быстро разрастается и теряет
> смысл «быстрого снимка». Удалённое не пропадает — вся история живёт в `CHANGELOG.md` и
> `docs/RELEASES.md`. Это правило касается ИМЕННО этого файла: здесь старое именно УДАЛЯЕТСЯ.

> ## [!] Журнал ведётся НЕПРЕРЫВНО, по ходу работы
> Обновляется СРАЗУ по ходу (сделал подшаг → §0; нашёл грабли/факт → §4), а не «в конце сессии» —
> токены могут кончиться в середине батча. В любой момент журнал обязан отражать реальную текущую
> точку. **См. протокол непрерывности — `docs/session_continuity.md` (и баннер вверху файла).**

> **Как пользоваться (Василий).** Новому чату/аккаунту: этот файл (или его текст) + последний архив
> → «продолжай». Копия в чат — с версией в имени: `WATCHLOG finpilot_vX_Y_Z.md`. Архивная копия —
> `docs/WATCHLOG.md` без версии.

> ## ВЕРСИЯ КОДА: `v6.12.0` · **СВЕРКА CSV: КОНТРОЛЬ ПОЛНОТЫ ВСКРЫЛ ПОТЕРЮ ВСЕХ ПРИХОДОВ. CSV был самым массовым каналом (12 614 операций в файле) и не проверялся ничем. Проверка нашла три невидимых дефекта: (1) синоним «поступление» НЕ подстрока заголовка «Поступления» → у Райффайзена терялись ВСЕ приходы (14 строк → 8 операций, человек без доходов в модели); (2) «amount» ловил колонку «(credit)» → вся выписка как доход, расход = 0; (3) строка метаданных принималась за шапку → 0 операций. Плюс: у Тинькоффа был наивный float() вместо _num. ОТКРЫТИЕ: «выписка мамы» — это Альфа-Банк, и её CSV несёт контрольные итоги → сверка 224 805.37 / 221 195.56 сходится. 195 тестов. Дальше: Б3 — обобщить header-driven движок**
> Если архив другой версии — сверь `app/config.py`.

---

## §0. RESUME POINT — отсюда продолжаем

- **[!] ПЕРЕД РАБОТОЙ:** прочитай баннер протокола непрерывности вверху файла и `docs/session_continuity.md` — рано и часто скидывай архив, иначе работа встанет.
- **[!!!] УПАКОВКА АРХИВА (критично — INC-FWD-BUILD-WIPEOUT):** архив ВСЕГДА с корневой обёрткой
  `finpilot_vX_Y_Z_intl/` (имя обёртки = имя zip без `.zip`). Паковать так:
  `mv <дерево> finpilot_vX_Y_Z_intl && zip -r finpilot_vX_Y_Z_intl.zip finpilot_vX_Y_Z_intl`.
  **НИКОГДА** `zip archive.zip .` (содержимое без обёртки) — публикатор ищет обёртку, при её
  отсутствии старый fallback `find | head -1` выхватывал случайную папку (`deploy/`), а
  `rsync --delete` делал репо равным ей → v5.20.0–v5.23.0 стали огрызками (6 файлов вместо ~623).
  Публикатор с v5.24.0 укреплён (fail-loud по маркерам `app/`+`CHANGELOG.md`), но формат обёртки
  соблюдать всё равно. Разбор: `docs/reports/incidents/forward_build_wipeout_incident.md`.
- **[!] СЛЕДУЮЩИЙ ШАГ (= указатель «СЛЕДУЮЩАЯ ЗАДАЧА» в ROADMAP) — веха 6 закрыта по инженерным пунктам.**
  Единственный открытый пункт вехи — **повторная сертификация на датасете v2** (5 итераций по протоколу
  `docs/model/expert_certification/`, финальный гейт; по итогам — решение по floor 1.5–2.0, сейчас
  87.4%, с floor 2.0 было бы 90.2%). **BLOCKED: нужен independent-expert прогон владельца по аккаунтам
  V/M/J/S** — принимающий вахту прочитал весь контекст проекта и загрязнит baseline (смысл ритуала
  независимого эксперта — чистая сессия). После сертификации веха 6 закрывается (нужен блок «Предложения
  Claude», правило 6 ROADMAP) → **веха 7 (React+TypeScript фронт, ADR-001)**, план
  `docs/frontend_migration_plan.md`, юр-требования фронта зафиксированы.
  Параллельно: PNG-превью диаграмм — на Mac («Ожидается»).
- **v6.12.0 (этот батч) — сверка CSV: контроль полноты вскрыл потерю всех приходов.**
  Закрывал главную дыру ретроспективы: CSV — самый массовый канал импорта (12 614 операций в одном
  файле) и не проверялся ничем. Оказалось, проверять есть чем, и проверка сразу нашла три дефекта,
  невидимых глазом (количество операций выглядело правдоподобно):
  (1) **синоним `поступление` НЕ подстрока заголовка `Поступления`** — последняя буква другая: на
  живой выписке Райффайзена 14 строк давали 8 операций, терялись ВСЕ приходы, и в модель попадал
  человек без единого дохода. Синонимы приведены к ОСНОВАМ слов. После фикса все 4 Райф-CSV
  (RU/EN × 2 формата) дают идентичный ответ 14 оп / 14 281.50 — совпадающий с их PDF-версиями.
  (2) **`amount` ловил `Amount in operation currency (credit)`** раньше, чем `credit` → вся выписка
  читалась доходом, расход = 0. Добавлен `_field(..., exclude=...)`.
  (3) **Строка метаданных принималась за шапку**: «Дата открытия счета | 24.12.2018 | Поступления |
  224 805,37» содержит и дату, и «поступления» → 0 операций. `_is_header_row` теперь требует, чтобы
  ни одна ячейка не была ЗНАЧЕНИЕМ (датой/числом). Эту регрессию поймал мой же контроль полноты
  через минуту после того, как я её внёс.
  (4) Тинькофф CSV использовал наивный `float()` вместо `_num` → на EN-формате строка падала в
  `except: continue` и исчезала молча.
  **Контроль полноты:** каждая точка пропуска называет причину, отчёт (`rows`/`parsed`/`skipped`)
  идёт из парсеров в маршрут; `completeness_verdict` отделяет законные пропуски (банк отклонил,
  подвал документа, ноль) от молчаливой потери. Живой файл: 12 788 строк → 12 614 операций, все 174
  пропуска объяснены.
  **ОТКРЫТИЕ:** «выписка мамы», лежавшая в наборе как «универсальный CSV», — это выписка
  **Альфа-Банка**, того самого, про который значилось «нет файлов с операциями». Её CSV несёт
  контрольные итоги в метаданных → `csv_declared` (читает ЯЧЕЙКИ, не сырой текст: между меткой и
  значением пустые ячейки в кавычках) → сверка 224 805.37 / 221 195.56 сходится до копейки.
  Тестов по парсеру 195, регрессий нет.
- **v6.11.0 — property-генератор выписок (Б2) + ретроспектива + протокол аудита.**
  `tools/statement_templates/synth.py`: рендер ВСЕХ реальных шаблонов (ВТБ A/B, Райф RU/EN/без-№,
  Тинькофф CSV, универсальный знаковый и split, 1C cp1251/utf-8) + фаззинг форматирования (nbsp,
  разделители тысяч и их отсутствие, запятая/точка, ₽/RUB/без суффикса, перенос описания в ячейке).
  `tests/test_statement_property.py` (16, маркер `property`): round-trip поле в поле, ~625 выписок за
  прогон; отдельно многостраничные на 60 операций (шапка печатается ОДИН раз → карта колонок должна
  наследоваться, иначе хвост теряется молча). Hypothesis сразу поймал моё неверное ожидание: CSV/1C
  отдают описание дословно, а табличные PDF схлопывают пробелы — это различие форматов, не дефект.
  **ГРАНИЦА МЕТОДА (важно для принимающего вахту):** генератор знает ровно те шаблоны, что и парсер,
  поэтому зелёная синтетика НЕ доказывает, что читаются 200 банков — это замкнутый круг. Единственное
  доказательство на конкретном файле — сверка с контрольными итогами банка.
  **Документы по прямому запросу владельца:** `docs/reports/retrospectives/statement_parser_process.md`
  (хронология v6.7.0→v6.11.0, все дефекты с ценой ошибки, мои собственные ошибки, цифры, дыры,
  ограничения) и `docs/statement_parser_audit_protocol.md` (независимый аудит: holdout-файлы, аудитор
  без доступа к коду, ручной ground truth как единственная проверка описаний/дат, негативные кейсы,
  метрики — главная «тихие ошибки = 0», антипаттерны).
- **v6.10.0 — измеритель точности: сверено 10 из 11 реальных PDF (было 6).**
  Владелец надавил по существу: «как ты докажешь, что парсер работает в 99%? как вообще измеришь
  точность?». Честный ответ: доказать 99% по 200 банкам, имея файлы шести, нельзя ничем — но можно
  сделать так, что молча импортировать неверно невозможно. Выписка сама несёт независимых свидетелей.
  **Добавлено:** (1) итоги Сбера «Пополнение X / Списание Y» из блока «ИТОГО ПО ОПЕРАЦИЯМ ЗА ПЕРИОД»
  (в тексте разнесены по строкам с реквизитами → ищем по метке) — три Сбера стали сверяемыми, и это
  НЕЗАВИСИМО подтвердило фикс кредитного лимита (банк заявляет 3 722,96 = ровно наш результат после
  удаления фиктивных 220 000 ₽); (2) итоги Райфа из таблицы («Всего поступлений»/«Total income») —
  шаблон без «№» не печатает «Обороты» → Райф 5/6 стали сверяемыми; (3) счётчик «Количество операций
  3 4» — проверка полноты набора, ловит пропуск строки при случайно сошедшихся суммах; (4) балансовое
  тождество остаток_конец − остаток_начало (ВТБ, Тинькофф). У Тинькоффа оно ОТДЕЛЬНОЙ арифметикой
  подтвердило, что кэшбэк — приход. **Осознанно НЕ подключено к Сберу:** там тождество не сходится у
  самого банка (−150 → 0 при нулевом нетто) — была бы ложная тревога на исправном парсе; это важный
  урок: контрольные данные банка тоже бывают внутренне противоречивы.
  **Результат:** 10/11 реальных PDF сверены; ВТБ×3 и Тинькофф — по три свидетеля; не сверяется только
  Сбер 5 (банк не печатает итогов). Тесты 37, покрытие 94.5%/92.7%.
  **Грабли этого батча:** два патча подряд молча не применились (`str.replace` без совпадения +
  безусловный `print("готово")`) — F821 всплыл только на flake8. Правило: любой скриптовый патч —
  только с `assert old in text`.
- **v6.9.0 — аудит на реальных выписках, часть 2: Райффайзен, детект банка, сверка в импорте.**
  Владелец прислал ещё одну живую выписку (ВТБ, июнь) и спросил «а Райф-парсер есть?». **Корень трёх моих
  ошибочных диагнозов найден:** стенд угадывал банк по ИМЕНИ файла, а macOS пишет имена в NFD — `'райф' in
  'Райф 1.pdf'.lower()` даёт **False** («й» = «и» + U+0306). Поэтому Райф-парсер на реальных Райф-файлах
  не запускался НИ РАЗУ (они уходили в парсер Тинькоффа → ложный ноль), выписки мамы я записал в
  «Райф-семейство» (это ВТБ), а Альфу/Газпром — в «выписки без парсера» (это справки).
  **Исправлено:** (1) **Райффайзен — колонки по шапке**: три шаблона (RU «Поступления/Расходы», EN
  «Debit/Credit» — ЗЕРКАЛЬНО, третий без колонки «№» со знаковой суммой). Парсер требовал номер в первой
  ячейке → **0 → 14 операций** на двух живых файлах; строка данных теперь по колонке даты (служебная
  «Количество операций 3 4» больше не операция). (2) **Сбер: изменение кредитного лимита шло доходом** —
  «Прочие операции +110 000,00» дважды = **220 000 ₽ фиктивного дохода**, отравленный вход для СППР;
  фильтр нецаш-операций, 18 → 16 операций. (3) **NFC в `_norm`**. (4) **Детект банка по содержимому**
  (`detect_bank`/`detect_pdf_bank`, 11/11 реальных PDF): у ВТБ реквизиты только в подвале последней
  страницы (шапка — картинка) → читаем первые И последние; у Тинькоффа имени банка в тексте нет вовсе →
  структурный признак «Выписка по договору». Маршрут больше не полагается на выпадающий список.
  (5) **Сверка с итогами банка в импорте — вариант B владельца (предупреждать, не блокировать)**:
  `app/services/statement_reconcile.py`, маршрут отдаёт `reconciliation`, UI — зелёный бейдж «Сверено с
  итогами банка» или жёлтое предупреждение. Поддержаны ВТБ (по ДАТЕ ОБРАБОТКИ), Райф («Обороты»/«Turnover»,
  порядок чисел — из порядка колонок), Тинькофф (кэшбэк объявлен ОТДЕЛЬНОЙ строкой: 115 390 + 761 =
  116 151 — ошибка была в контрольной модели, не в парсере). (6) Новые справки: Сбер 8/9, Газпром рус
  («С П Р А В К А» вразрядку) + англ. (CERTIFICATE), Альфа; матчинг ещё и по тексту БЕЗ пробелов (Альфа
  отдаёт «имеетоткрытыйсчёт» слитно). 10/10 справок распознаны, ложных на 12 выписках — 0.
  **Сверка сходится:** ВТБ июнь 54 оп (100 624.00/101 285.09), май 47 (32 156.00/32 410.30), ВТБ 1 — 3
  (267.01), Райф RU 7 (13 670.12/13 842.50), Райф EN 14 (14 281.50/14 281.50), Тинькофф 45
  (116 151.00/109 166.08). Тесты: reconcile (24) + real_formats (60); покрытие 92.7% / 95.2%.
  Инструмент `tools/statement_audit/reconcile.py` — тонкий CLI поверх продуктового модуля.
  **Ландшафт уточнён:** Альфа 2 — настоящая выписка ПУСТОГО счёта (0 операций корректно), Газпром —
  только справки. Парсеров Альфы/Газпрома писать не по чему: нужны их выписки С ОПЕРАЦИЯМИ.
- **v6.8.0 — аудит парсера на реальных выписках: ВТБ по шапке, знак кешбэка, справки.**
  Владелец загрузил настоящие выписки (6 банков, 64 файла) со словами «я уверен, что не работает; здесь
  важна точность 100% — если операции читаются неверно, весь алгоритм бесполезен». Опасение
  подтвердилось: синтетические шаблоны §6.4 я подгонял под парсеры, они и обязаны были проходить.
  **Найдено и исправлено:** (1) **ВТБ: колонки по шапке** (`_vtb_column_map`, `_vtb_row_to_transaction`)
  — у ВТБ ТРИ реальных шаблона со сдвинутыми колонками (там, где «Комиссия», в другом «Описание
  операции»; «Расход» бывает и +, и −). Привязка к индексам: у всей июньской выписки (54 оп) описание
  было заглушкой «Операция» — бралась колонка контрагента (FR-13/FR-14 мертвы), плюс **ландмайна
  молчаливой порчи суммы**: текст описания читался как комиссия и вычитался (`_num('Перевод 1500') =
  1500.0` → 200 ₽ стали бы 1700 ₽); корректность держалась на том, что `_num` случайно падал.
  (2) **Кешбэк в колонке «Комиссия» уходил в расход** (`_vtb_is_credit`): проверено по координатам —
  кешбэк 656 ₽ и комиссия 69.01 ₽ стоят в ОДНОЙ колонке (x=368), направление кодируется только в
  описании; приход недосчитывался ровно на 656 ₽. (3) ВТБ давал 0 операций (нетто = Приход − Расход −
  Комиссия). (4) 1C — cp1251 (`decode_statement_bytes`, utf-8 strict первым: cp1251 никогда не первой,
  она не падает и молча портит UTF-8). (5) Справки (об остатке/реквизиты/о задолженности/о пенсиях)
  распознаются вместо silent-0. **ДОКАЗАТЕЛЬСТВО (§11, три уровня):** прогон + координаты pdfplumber +
  **сверка с контрольными итогами самой выписки**. Итог: июнь 54 оп (приход 100 624.00, расход
  101 285.09), май 47 (32 156.00 / 32 410.30), ВТБ 1 — 3 (267.01) — всё = итогам банка, баланс
  замыкается, описаний-заглушек 0 (было 54). Инструмент `tools/statement_audit/reconcile.py` (сверка по
  ДАТЕ ОБРАБОТКИ — банк считает период так, а операции показывает по дате операции; без этого ложные
  тревоги). Тесты (28) `tests/test_statement_parser_real_formats.py`, фикстуры обезличенные — реальные
  выписки в репо НЕ кладутся (ПДн). Отчёт `docs/reports/testing/statement_parser_real_data_audit.md`.
  **Исправлен мой ошибочный диагноз:** «выписки мамы = Райф, сломан» — это ВТБ, а ноль был артефактом
  стенда, угадывавшего банк по имени файла. Отсюда открытый пункт: автоопределение банка по содержимому
  в проде (сейчас bank_id — из списка на форме, ошибка пользователя = ложный ноль).
- **v6.7.0 — переименование канона → v3_3_0 + §6.4 импорт выписок.**
  Два пункта вехи 6. **(1) Переименование:** `math_model_v3_2_0.md` → `docs/math_model_v3_3_0.md` —
  файл уже нёс контент v3.3.0 (прогноз Holt + G7-остаток), имя приведено в соответствие. Различал
  живые указатели vs исторические: обновил хаб `model/README` (v3.3.0 действующий, v3.2.0 → «Заменена»),
  superseded-указатель `v3_1_0`, таблицу ROADMAP, docstring `invariants.py`, снапшоты `project_meta`;
  исторические ссылки в CHANGELOG/§3 не тронул, ref в WATCHLOG перевёл в bare. **Revision-gate CLEAN**
  (проверял эмпирически: чекер сканирует `.md`, полные пути `docs/...`, замораживает `/reports/`).
  **(2) §6.4 импорт выписок:** эталонные шаблоны вшиты в продукт `app/data/statement_templates/`
  (генератор `tools/statement_templates/build_templates.py` + MANIFEST): CSV Тинькофф/Сбер,
  универсальный знаковый + split Приход/Расход, XLSX, PDF-текст Тинькофф/Сбер, PDF-сетка ВТБ/Райффайзен,
  1C. Синтетические, формат 1-в-1; PDF рендер DejaVuSans (кириллица + ₽), round-trip через pdfplumber
  проверен. 15 golden-тестов `tests/test_statement_templates.py` (точные операции + роутер + universal-
  стратегия на незнакомом банке). **Корпус сразу окупился:** нашёл дефект в собственном образце
  Райффайзена — Debit держится со знаком минус в реальном формате (парсер прав, образец был нереалистичен).
  Отчёт `statement_import_coverage.md`, стратегия §5 обновлена. Веха 6: открыт только гейт сертификации
  (BLOCKED на владельце). flake8=0, регрессий в парсерах нет.
- **v6.6.0 — G7-остаток: остаток целевого бакета → резерв (ADR-009).**
  Замкнут каскад освоения свободного ресурса **долг → цели → резерв**. Целевой бакет кэпится по
  потребности целей (`calculate_goals_si`: `min(доля, _remaining)`); при насыщении целей остаток
  `x_goals_unused = x_goals_total − Σ(goal_allocation)` раньше **испарялся** — не в цели (кэп), не в
  резерв (`Lt` читал номинальный `x_reserve`), не в поток (`Rt` от него не зависит). Теперь перетекает
  в резерв: `x_reserve_effective = x_reserve + x_goals_unused`, `Lt' = (Bliq + x_reserve_effective)/Σe`.
  Резерв — конечный сток (в подушку всегда есть куда влить). **Сохранение денег:** долг_эфф +
  цели_развёрнуто + резерв_эфф = R⁺ (тест `test_no_money_leaks_conservation`). Правки:
  `alternatives.py` (поля `x_goals_unused`/`x_reserve_effective`), `recommendation.py` (эффективный
  резерв + нота о перетоке), `planning.py` (`_effective_signature` — дедуп по истинному эффекту),
  **I10** (стенд, `Lt` на эффективном резерве; I4 не затронут). Замер 12 000 портретов v2 (7512 с
  решёткой): переток у победителя **0.41%**, смена рекомендации **0.16%**, эффективного сплита
  **0.09%**; спасено от испарения — медиана 5 904 ₽, p95 75 502 ₽, максимум **194 395 ₽**. Footprint
  мал, но правило принимается — фикс сохранения денег (в отличие от отклонённого шага 5%, ADR-008).
  Свип v2 после изменения: **0 нарушений I1–I14**. ADR-009 + отчёт
  `goal_remainder_reroute_benchmark.md` + стенд `goal_remainder_reroute_benchmark.py`; канон §5.2/
  §10.4/v3.3.0/§19. Полный fast-контур зелёный (golden движка, grid_step, свип — без регрессий).
  **Сертификация (гейт вехи) — BLOCKED на владельце** (независимый прогон по аккаунтам, см. СЛЕДУЮЩИЙ ШАГ).
- **v6.5.1 — РЕШЕНИЕ по шагу решётки (ADR-008) + три отчёта + мина id закрыта.**
  **Решение владельца: шаг остаётся 10%, `|A| = 66`.** Массовый прогон 2000 портретов v2: победитель
  тот же в **90.9%**, средний прирост полезности **+0.00067** (0.07 п.п.), p95 +0.004, максимум +0.025;
  **главный совет меняется в 0.8%** портретов (13/1619, пограничные res↔obl), денежный сдвиг: медиана
  0 ₽, p95 ≈ 1 086 ₽/мес. Цена перехода: ×3.5 латентности (1.79 → 6.38 мс), смена канона (`|A|=66` в трёх
  файлах модели, диаграммах, глоссарии, SRS, инварианте I1, golden-снапшотах), рассинхрон с защищённой
  ВКР и статьями в работе, полная пересертификация (эталон 87.4% измерен на 10%). Прирост 0.07 п.п. на
  два порядка ниже разброса самих экспертов (коридор 85.6–95.6%). **ADR-008** + отчёт
  `docs/reports/testing/grid_step_5pct_benchmark.md` (закрыл висячую ссылку из `tests/test_grid_step.py` —
  замер сделали в v6.4.0, отчёт не написали; ревизия не поймала: ось битых ссылок смотрит `.md`, а ссылка
  была в докстринге `.py`). **Мина id закрыта:** `f"a{d}{r}{g}"` без разделителя давал 29 дублей из 231
  на шаге 5% — теперь `_alternative_id` (на `steps <= 10` формат ПРЕЖНИЙ, снапшоты живы; мелкий шаг —
  разделители) + **fail-loud** на неуникальность. Стенд правился: сравнивал топ-3 и врал «совет
  ОТЛИЧАЕТСЯ», хотя ТОП-1 идентичен — теперь решает `best`. **Археология INC-NULL-DEADLINE:** D2/D3 —
  ровесники ядра (эпоха v1.x, старейшие дефекты проекта), D1 — с v4.16.20; D3 стал достижим ПУБЛИЧНО
  только в v6.4.0 (`POST /api/demo/analyze`, `months_left: int | None = None` по умолчанию) — окно
  выстрела ≈ сутки. Урок: **старая латентная мина + новая фича = инцидент**.
- **v6.5.0 — бессрочные цели + три латентных дефекта под NOT NULL (ROADMAP §6.3).**
  Канон допускал `deadline: null`, колонка была `NOT NULL` — бессрочная цель обходилась костылём
  «дедлайн через 10 лет». Закрыто сквозь слои: миграция `0029` (batch — SQLite не умеет ALTER COLUMN),
  модель/схемы/B2B-контракт (ослаблен, не сломан), CRUD, ядро (`urgency_of`, `months_left_or_none`),
  UI (поле «Срок» необязательно, в списке «бессрочная»), демо (костыль снят). Канон §11.2
  `math_model_v3_2_0.md`: `τ_s = ∅ ⇒ u_s = 1` — отдельным правилом; бессрочная не участвует в разовом
  закрытии близких целей и дедлайновых траншах. **Снятие NOT NULL вскрыло три дефекта** (near miss,
  в прод не вышло): **D1** `/spending-advice` → детерминированный 500 (`round(None)`, сортировка
  `float` против `None`); **D2** `ORDER BY deadline` без `NULLS LAST` — SQLite ставит бессрочные
  первыми, PostgreSQL последними (тихое расхождение матрицы, замер обеих СУБД в разборе); **D3**
  фолбэк `_months_left → 12.0` объяснял бессрочную цель как «до дедлайна ~12 мес» — **уже работал**
  в демо-витрине. Разбор: `docs/reports/incidents/inc_null_deadline_latent_defects.md`, правило —
  **PIT-010** («ограничение схемы = предохранитель на коде; ослабляешь — сначала обойди всех
  потребителей колонки»). 14 тестов `tests/test_goals_open_ended.py` на матрице SQLite + PostgreSQL 16.
  Хвост v6.4.0 подобран: `flake8 .` = 0 (F841/F541 в стендах — PIT-007). Пин ревизии: migrations 28 → 29.
  **Открытый хвост:** ось ревизии «канон ↔ схема БД» — предложение владельцу (сейчас «канон допускает X,
  схема запрещает X» не ловит ни один гейт).
- **v6.4.0 — прогноз на РЕАЛЬНОЙ истории транзакций + месячная граница показателей + 10 портретов.**
  Костыль (синтетический канал истории) убран: прогноз строится из реальных многомесячных транзакций
  (`build_monthly_history` — скользящие 30-дневные корзины), демо-кейсы получили настоящую историю в БД
  (`_history_transactions`, `_case_data`) → «Валидация» и `/planning` читают ОДИН источник. **Латентный баг
  закрыт:** месячные показатели считались за всю историю → границу ставит `prepare_data(period_days=30)`
  (единственная точка — у неё ровно 3 вызова, всем нужен месяц). Прогноз: демпфированный Holt + помесячная
  капитализация → выпуклая, непостоянная по дельте траектория (INV-FORECAST-STRAIGHTLINE закрыт, канон → v3.3.0).
  +4 портрета (Екатерина/Артём/Наталья/Павел = 10); «Валидация» переработана (спарклайн + `/demo/analyze`).
  Единый промпт эксперта + протокол 5 итераций (`docs/model/expert_certification/`). **Новый тип отчёта
  (правило владельца):** engineering / implementation reasoning — отчёт
  `docs/reports/engineering/v6_4_0_forecast_implementation_reasoning.md` + методичка
  `knowledge/guides/engineering_reasoning_guide.md` + регистрация в `report_types.md`. Плюс ADR-007.
- **v6.3.0 — counterfactual-анализ floor 2.0 (по запросу владельца: «последствия и
  разбор подгонки»).** Инструмент `floor_counterfactual.py` + разбор
  `docs/model/floor_counterfactual_analysis.md`: 68 портретов сменили бы доминанту (в т.ч. 27
  «подушка важнее лавины»), 62 починено / 0 сломано; split-half +2.5/+3.2 п.п., шум отбора ~0.83
  п.п. — прирост НЕ шум, а вкус фиксированной четвёрки экспертов в зоне Lt 1–3; цена на кейсе
  кредитки +2 002 ₽ процентов (+21%). ADR-006 Update: веса аргументов уточнены, решение и триггер
  без изменений; для сертификации отмечен ключевой срез (goals+/debt → reserve при Lt < 2).
  Роли доков без дублей: ADR = решение, методичка = теория, expert_alignment = эксперимент,
  этот анализ = эмпирика последствий. **Новое правило владельца (reporting_policy.md):** совершенно
  новый тип отчёта = сам отчёт + регистрация типа в report_types.md + общая методичка с шаблоном в
  knowledge/guides/. Ретроактивно закрыт прецедент: тип Counterfactual зарегистрирован, методичка
  `knowledge/guides/counterfactual_analysis_guide.md` (жанр, обязательные элементы, шаблон, ошибки).
- **v6.2.0 — модель v3.2.0: слой запаса (G4) + горизонт в полке (G5) + честный ПДН (G7).**
  `app/core/surplus.py`: излишек сверх Lt*·Σe → разовые ходы (Avalanche-погашения >= r_bench с
  выгодой против бенчмарка, дедлайновые цели с инструментом по горизонту, разовый инвесттранш);
  только при Rt >= 0, подушка неприкосновенна — инвариант I14; UI-блок. `instrument_for_horizon`
  (<12 деп / 12–36 деп+ОФЗ / >36 профильный микс). `calculate_dt`: ПДН=1.0 при нулевом доходе с
  платежами (I10 расширен). Канон `math_model_v3_2_0.md` (+§14, v3_1_0 superseded), ссылки
  обновлены (снипеты/хаб/ROADMAP/инварианты). Экспортер: `expert-pack` (4 сухих чанка + бриф
  `docs/model/expert_brief_v2.md` с CSV-протоколом — выданы владельцу для раздачи экспертам) и
  `markdown` (полный каталог 12k — выдан владельцу, в архиве не хранится). Методичка
  `knowledge/guides/overfitting_data_snooping_guide.md` (переобучение/p-hacking, кейс ADR-006).
  Валидация: согласие 87.4% без изменений, свип v2 12k — 0 нарушений I1–I14.
- **v6.1.1 — ADR-006: floor-решение зафиксировано (docs PATCH).** По запросу
  владельца («зафиксируем и оставим как есть, будем смотреть по выборке новых экспертов»):
  floor = 1.0 в v3.1.0, floor 1.5–2.0 — кандидат ровно одного пересмотра по второй сертификации;
  обоснование (подгонка под выборку/состав экспертов, дефектный v1, продуктовая цена, G6) —
  `docs/reports/adr/adr_006_reserve_floor_calibration.md`.
- **v6.1.0 — материализация датасетов, хаб мат-модели, калибровочный эксперимент.**
  В репо положены оба датасета + ответы модели (`knowledge/model_validation/`: portraits_v1/v2
  jsonl.gz, model_outcomes_v3_1_0_on_v1/v2 csv.gz; ~2.9 МБ, лимит владельца 10 МБ; экспортер
  `tools/model_validation/dataset_export.py` с TDD). Хаб `docs/model/` (README: история версий
  модели, оценки экспертов, вердикт по датасету v1, протокол второй сертификации;
  expert_alignment.md: методология, срезы, остатки, потолок). Свип v2 12 000: 0 нарушений I1–I13.
  Грид-эксперимент (`calibration_experiment.py`): floor 2.0 даёт **90.2%**, floor 1.5 — 88.9%,
  Lt* профилей 4/5 — эффекта нет; РЕШЕНИЕ: floor остаётся 1.0 (рекомендация G6; вторая подгонка
  под тот же датасет = оверфит), floor 1.5–2.0 — кандидат второй сертификации. Ретроспектива
  процесса — `docs/reports/retrospectives/model_calibration_retrospective_2026_07.md` (новый тип).
  Снипеты инструкций проекта (`knowledge/project_meta/project_instructions_{v,s}.md`) обновлены
  на канон v3.1.0 — владельцу копипаст. Кейс кредитки прогнан помесячной симуляцией: карта
  закрывается за 5 мес против 25 на минималке (мес 1 — floor-добор подушки + 9.2к в карту,
  мес 2–4 — вся мощность в карту, мес 5 — закрытие и возврат к резерву).
- **v6.0.0 — СТАРТ ВЕХИ 6: независимая экспертиза 12 000×4 → калибровка модели, канон v3.1.0.**
  Экспертиза (заключение: `docs/reports/testing/independent_expert_review_2026_07.md`): ядро честное
  (математика 9/10, 0 нарушений, перепроверено независимо), советник имел дефекты — согласие с
  консенсусом 4 экспертных движков 51.8% при их базовой линии 85.6–96%. Закрыто по TDD: **G1**
  насыщение резерва (Lt* 6/5/4.5/3.5/3 мес), **G2** кризисный модуль `app/core/crisis.py` (балансовый
  ход из ликвидности, сокращение до нуля дефицита, потолок трат, заморозка целей, реструктуризация,
  runway; охват 9697/9697), **G3** гейт «план не увеличивает ПДН» (no_admissible_plan 198 → 0,
  флаг Dt_alert), **G5-минимум** инвест-транш `app/core/investment.py` (полка по профилю + нота АСВ),
  **G6** floor 1 мес лексикографически. Этап 4.0 — только при Rt >= 0. **Согласие 51.9% → 87.4%**
  (коридор экспертов 85.6–95.6; все 5 контрольных кейсов совпали) — отчёт
  `docs/reports/testing/expert_agreement_v3_1_0.md`, стенд `tools/model_validation/` + эталон
  `knowledge/model_validation/joined.csv.gz` (регенерация: генератор **v1** seed 20260702 — только v1!).
  Генератор портретов **v2** (ПДН ~ Beta по ЦБ/НБКИ, аннуитетные сроки, adversarial-слой; v1 бит-в-бит
  сохранён), инварианты I12/I13 + I6-расширение, golden-снапшоты пересняты (floor-эффект), кейс
  Василия с кредиткой — живой тест `TestVasiliiCreditCardCase` (§6.3 закрыт). UI: блок кризис-плана
  + Dt_alert на /planning. Канон: `docs/math_model_v3_1_0.md` (v3_0_0 — superseded-баннер).
  Владельцу: обновить канон в project-инструкциях на v3.1.0. Fast-прогон зелёный, flake8=0.
- **v5.25.0 — мёрж вахты: англ-опрос v2 (USD) вместо ₽-версии v5.19.0.**
  Параллельная вахта пересобрала англоязычный опрос; внесён в канон как конфликт-резолюция.
  Валюта — USD с пере-якорением по **пропорциям кейса** (Q26: доход $4,000 / обязательные $2,500 /
  кредит $3,000 под 20%, мин. $200 / цель $5,000 / резерв $750 / свободно $1,500; согласованы
  Q13/Q43/Q48/Q59; для € числа 1:1), мусорные варианты из выгрузки вычищены, ограничение API
  задокументировано (Q14-ранжирование: «один ответ на столбец» — вручную в редакторе формы).
  Прежний аргумент «₽ ради сопоставимости волн» снят: сопоставимость держат пропорции, ₽-вариант
  остаётся в истории (git + архивы ≤ v5.24.0). Заменены `knowledge/survey_auditory/international/`
  `finpilot_survey_en.gs` (verbatim от вахты, точка входа `buildSurveyForm()`) + README папки
  (инструкция запуска, решения перевода, референс RU↔EN на 62 вопроса, суперсид-нота). Попутный
  fact-fix: из указателей «СЛЕДУЮЩАЯ ЗАДАЧА» (ROADMAP + §0 ниже) убран протухший пункт «первый
  пуш зеркала — манифест на подтверждении» (опубликовано в v5.18.4). Код `app/` не тронут (бамп
  только APP_VERSION); `flake8 .` = 0; revision gate CLEAN.
- **v5.24.0 — публикатор укреплён после аварии wipeout + восстановитель + постмортем.**
  Авария: forward-build стирал репо (v5.20.0–v5.23.0 на гите = 6 файлов). Причина — архивы паковались
  без обёртки, публикатор через `head -1` брал случайную папку, `rsync --delete` сносил проект.
  Данные целы (деревья в архивах полные). **Фикс публикатора:** надёжный поиск корня (обёртка /
  содержимое в корне / единственная папка) + приём только по маркерам `app/`+`CHANGELOG.md`, иначе
  fail-loud (репо не трогается); `head -1` убран; сохранён детект рабочего python. **Добавлено:**
  `tools/publish/finpilot_restore.sh` (откат main на целый тег + снос битых тегов/релизов + чистый
  rebuild), постмортем `incidents/forward_build_wipeout_incident.md` + строка в реестре, PIT в
  `docs/pitfalls.md`. Код `app/` не тронут; `flake8 .` = 0. Владельцу: восстановить репо через
  `finpilot_restore.sh`, затем долить v5.24.0 публикатором.
- **[!] ПОЛИТИКА ДОСТАВКИ (v5.23.0):** вся документация ВНУТРИ продукта. WATCHLOG/отчёты/планы НЕ дублируем
  отдельными `.md` через `present_files` — наружу только архив (`.zip`). Владелец смотрит доки на GitHub.
  Исключение — явный разовый запрос файла. Детали — `session_continuity.md` §4.
- **v5.23.0 — ревизия стала гейтом (инструмент + процесс) + политика «доки внутри продукта».**
  1) **`tools/revision/revision_check.py`** — runnable-гейт: 3 статические проверки docs↔code (битые ссылки
     живой/замороженный + allowlist · утечка legacy мат-модели, контекстно-осознан · счётчики 28/28/106
     против пинов). Exit 0/1. Запуск `python -m tools.revision.revision_check`.
  2) **`tests/test_repo_revision.py`** — pytest-обёртка (4 теста, авто-fast → в fast-тире и CI). Плюс явный
     шаг «Revision gate» в быстром джобе CI (<1с). Негативно проверено: инжект ссылки/legacy → DRIFT.
  3) **`knowledge/guides/repo_revision_methodology.md`** — методология: живое/замороженное, 6 осей, что
     автоматизировано, когда править инструмент а не доки, timewarp (ось 3), когда гонять.
  4) **Политика доставки:** обновлены `session_continuity.md` §4 + WATCHLOG §1 (этот) + `tools/README.md`.
  `app/` не тронут (бамп только APP_VERSION), flake8=0 на новом коде, гейт CLEAN.
  **Флаг владельцу:** project-инструкции всё ещё держат правило #6 (дублировать WATCHLOG — отменено),
  ссылку на удалённый `model_vs_code.md`, кириллическое имя мат-модели — правка в настройках проекта.
  **Осталось по ревизии:** оси 4–6 (диаграммы mermaid↔код · нейминг · дедуп knowledge).
- **v5.22.1 — дельта-ревизия репо, оси 1–3.** Отчёт `docs/reports/audits/repo_revision_2026_07_delta.md`.
  1) **Ось 1 (ссылки):** 22 протухшие ссылки в живых доках исправлены (реорг v5.8.0: отчёты уехали в
     `docs/reports/`, кросс-ссылки гайдов/ADR остались на плоских путях). Замороженная ссылочность (61 хит:
     CHANGELOG, релиз/мёрж/инцидент/аудит-отчёты, science) НЕ тронута — правило «историю не переписываем».
  2) **Ось 2 (актуальность):** ЧИСТО — числа сходятся (таблицы 28 / миграции 28 / пути OpenAPI 106),
     порядок вех консистентен, легаси мат-модели в живые доки не течёт, устаревших версий нет.
  3) **Ось 3 (timewarp):** ЧИСТО — оконный домен 268 fast-тестов × baseline/+90/+180/+370 = 0 падений.
     Мин класса BUG-028 не осталось (`test_expense_record_date` зелёный на всех офсетах).
  **Флаги владельцу (не автоправка):** `project_instructions_v/_s.md` ссылаются на удалённый `model_vs_code.md`
  и кириллическое имя мат-модели — правка нужна в ИСТОЧНИКЕ (project-инструкции в настройках проекта), не в
  снапшоте; PWA-форварды (`PWA_УСТАНОВКА.md`/`icon-512.png`) — плановые ассеты вехи 8. **Осталось:** оси 4–6
  (диаграммы mermaid↔код · нейминг · дедуп knowledge) — следующим проходом.
- **v5.22.0 — план фронта (набросок вехи 8) зафиксирован в репо + фикс указателей ADR-001.**
  1) **`docs/frontend_migration_plan.md`** — направляющий план миграции фронта, помечен как
     **ПРЕДВАРИТЕЛЬНЫЙ НАБРОСОК** (детализируется при входе в веху 8, после вех 6–7). Строится поверх
     канона (стек — ADR-001, эстетика — `ui_visual_design_brief.md`, токены — `ui_ux_design_standard.md`).
     Фиксирует стратегию, ключевые решения с трейд-оффами (миграция: greenfield-to-parity, Jinja не гасим,
     cut-over = флип nginx; типы API из `openapi.json`; cookie-auth → тут закрываются CSRF+SameSite;
     TanStack Query+Context), 6 фаз, рабочий метод (поверхности Claude), отложенные бэк-галки (CSP, CSRF),
     и §6 «что сознательно НЕ детализировано» (YAGNI). Владелец просил пометить набросок явно — сделано.
  2) **[ФИКС ССЫЛОК, часть ревизии] `adr_001_frontend_stack.md`:** «веха 6»→«веха 8» (7 вхождений —
     фронт-веха перенумерована 2026-07-02) + `06_ТЗ_на_современный_UI.md`→`ui_visual_design_brief.md`.
     Решение/обоснование стека НЕ менялись — только фактические указатели.
  Код `app/` не тронут (бамп только APP_VERSION). **Ревизия (оси 1–3) — greenlit владельцем, следующий
  заход:** живые ссылки + сверка канона + timewarp-свип. `flake8` фронта не касается (доки+config).
- **v5.21.0 — база знаний по Claude усилена + приватный скрипт переименован.**
  1) **Методичка `knowledge/guides/claude_infrastructure_methodology.md` +2 раздела** (сверка с доками
     2026-07-05): **§12 «Экосистема Claude»** — пять поверхностей (чат/Проект/Cowork/Claude Code/
     приложения) + таблица «инструмент→для чего→когда в FINPILOT»; лимиты файлов (чат 30 МБ/20 файлов;
     Проект без лимита числа, но 200K = мягкий потолок → RAG ×~10, кэш knowledge не жрёт лимиты; API
     Files до 500 МБ); три следствия (код в Проекте; тезисные `.md` дольше держат «вижу всё» — обоснование
     сжатия; дубликаты вредят вдвойне). **§13 «Claude Code и субагенты»** — механизмы (CLAUDE.md, субагенты,
     Hooks=CI у правок, Skills, MCP), таблица субагентов под стек (planner/reviewer/tester/debugger/
     security-audit/explorer, роутинг моделей в YAML как в model-advisor), паттерны, дисциплина контекста.
     Первоисточники → §14, дополнены (upload-files, sub-agents, Claude Code overview).
  2) **Скилл `model-advisor` +триггеры 12–14:** выбор поверхности, лимиты файлов/RAG (числа только по
     ссылке), предложение Claude Code+субагентов на репо-задачах; роутинг и description дополнены.
  3) **[ПЕРЕИМЕНОВАНИЕ] `tools/publish/finpilot_publish.sh` → `finpilot_publish_private.sh`** —
     явная пара к `finpilot_publish_public.sh` (сразу видно, какой скрипт куда). Ссылки в living-доках
     (`tools/README.md`, `docs/versioning_and_releases.md`) и usage-комментах обоих скриптов обновлены.
     Историческая запись CHANGELOG [5.20.0] не переписана (на тот момент имя было прежним).
  Код `app/` не тронут (бамп только APP_VERSION). `flake8 .` = 0. Дальше — веха 6 по команде владельца.
- **v5.20.0 — публикатор зеркала v2 (тег+релиз), скилл model-advisor, сверка Claude-методички.**
  1) **Публикатор `tools/publish/finpilot_publish_public.sh` v2:** после push зеркало получает
     git-тег `vX.Y.Z` + GitHub Release (идемпотентно, ретраи под РФ-TLS); новый режим `release` —
     дооформить тег/релиз без пересборки. Тексты релизов — ТОЛЬКО из нового private-only
     `tools/publish/public_release_notes.md` (приватные CHANGELOG/WATCHLOG не используются никогда);
     слой `guard_notes` роняет публикацию при следах внутренней кухни (`WATCHLOG|вахт|аккаунт|Claude`+ФИО).
     **Ассеты к релизам запрещены by design** (архив `*_intl.zip` = полное приватное дерево). Механизм
     `mirror_extras/` — файлы только для зеркала; первый: `.github/workflows/codeql.yml` (CodeQL на
     публичном репо бесплатен, в приватный не кладём — без GHAS падал бы).
  2) **Скилл `tools/claude_skills/model-advisor/SKILL.md`** — автосоветник выбора модели/effort:
     11 триггеров → односрочные советы, роутинг-only, лайнап из системного контекста + сверка по докам,
     цены/лимиты не хардкодятся (ссылка на docs). Методичка — `knowledge/guides/claude_infrastructure_methodology.md`.
  3) **Методичка Claude-инфраструктуры сверена с первоисточниками** (новый §12 со ссылками):
     фолбэк Fable→Opus идёт **с уведомлением** (не «молча»); Sonnet 5 (релиз 30.06, сменил 4.6) подтверждён;
     окно подписочных лимитов Fable до 07.07 → далее usage credits. Прецедент **«системный контекст
     Claude может отставать от лайнапа»** (называл Sonnet 4.6 при вышедшем Sonnet 5) закреплён в §8/скилле.
  4) **[FACT-FIX журнала, не переписывание истории]** §2 держала СТАРЫЙ порядок вех (6=Фронт/7=Тест/8=Юр)
     и «мы здесь» на вехе 4 — противоречило §0, баннеру и канону ROADMAP. Приведена к канону
     **6=Тест/7=Юр/8=Фронт/9=Деплой**, веха 5 «Завершена». Заодно в §6 две фронт-привязки
     (`adr_001_frontend_stack`, `ui_visual_design_brief`) переведены «(веха 6)» → «(веха 8)».
  5) **[ГИГИЕНА ГЕЙТА, унаследовано от v5.19.0]** прогон `flake8 .` (скоуп CI, урок §4) вскрыл 27
     нарушений (26×E501 + 1×E741) в `tools/portrait_testing/render_portrait_cards.py` — файл пришёл
     с вахты J и, похоже, проверялся узким `flake8 app/`. Строки >100 — дословные выходные литералы
     карточек/легенды (уходят экспертам); перенос менял бы вывод, а правило #2 запрещает реформат →
     добавлен точечный `per-file-ignores` в `.flake8` (E501/E741 только для этого файла; ядро
     generator/invariants/runner остаётся под гейтом — проверено инъекцией). **Инвариант «flake8
     репо = 0» (PIT-007) восстановлен; файл J НЕ тронут.** Разбор — §4. Если владелец предпочтёт
     полный перенос строк вместо ignore — скажи, переверстаю.
  Ответ владельцу по витрине GitHub Actions — в §4 (находка «GitHub-тулы»). Код `app/` не тронут
  (бамп только APP_VERSION). Дальше — веха 6 по команде владельца.
- **v5.19.0 — внесены материалы вахты S + сухие синт-портреты + англ-опрос (задача 6 закрыта).**
  1) **3 методички вахты S** в `knowledge/guides/`: `claude_infrastructure_methodology.md`,
     `knowledge_capture_protocol.md`, `volume_compression_methodology.md`.
  2) **Синтетические портреты — СУХОЙ рендерер** `tools/portrait_testing/render_portrait_cards.py`:
     раскрывает движковый генератор в человекочитаемые карточки формата 6 эталонных кейсов, но
     **только вход** — БЕЗ вычисленных Rt/Lt/ПДН и вердиктов (объективность: считает эксперт).
     Числа 1:1 из генератора (seed=20260702), обёртка-персона детерминирована по индексу и сводится
     точно к тоталам. В репо — рендерер + выборка `knowledge/survey_auditory/synthetic_portraits_sample.md`
     + легенда типов вахты S `.../synthetic_portraits_boundary.md`. **Полный набор 12 000 карточек (29 МБ)
     и JSONL-фикстура (7.6 МБ) В АРХИВ НЕ КЛАДУТСЯ** — регенерируются рендерером; отдаются владельцу
     через `present_files` для рассылки экспертам.
  3) **Англ-опрос** `knowledge/survey_auditory/international/finpilot_survey_en.gs` (Google Apps Script):
     воспроизводит RU-форму 1:1 — 7 разделов, 62 вопроса, все типы (radio/checkbox/scale/grid/text),
     `Другое:`/required/attention-check сохранены; суммы в ₽ намеренно (сопоставимость волн). README рядом.
  Код `app/` не тронут (бамп только APP_VERSION). Дальше — веха 6 по команде владельца.
- **v5.18.4 — публичное зеркало ОПУБЛИКОВАНО** (`vevdokimovm/finpilot`, v5.18.3, HTTPS+токен).
  Публикатор захардненен: `sanitize_tree` (LICENSE→PolyForm, обезличивание) + GUARD 3/3 (скан ФИО→FAIL),
  docs-allow урезан до DEPLOY+RELEASES. Практика токена/пуша — `docs/mirror_publishing_guide.md`.
- **v5.18.3 — диета-уточнение (приказ владельца после мёржа):** все `.docx` вон из репо (канон — `.md`;
  `user_manual_simple.docx` сконвертирован в `.md`), генерируемое тяжёлое вон (питч-PDF + survey PNG/html/json —
  регенерируются из источников). `naming_convention` под политику «.docx у владельца». Код не тронут.
- **v5.18.2 — ВЕХА 5 ЗАКРЫТА, линии вахт M и J смёржены в единый канон.** База — M (нумерация
  вех + механизм закрытия), из J влиты .md-исходники науки, оригиналы ответов редакций, распакованный
  replication_package, питч RU-PDF+.md, лого 1200px. Код не тронут (был идентичен). Нумерация вех в теле
  ROADMAP приведена к таблице (6=Тест/7=Юр/8=Фронт). Отчёт — `docs/reports/merges/merge_report_v5_18_2.md`.
  **Дальше — веха 6 (тестирование мат-модели, v6.0.0) по команде владельца.**
- **Идёт роадмап §5.8 — пять задач владельца от 2026-07-02** (ревизия ✓ → диаграммы → методичка
  живого роадмапа → предложения Claude). Порядок и статусы — в `docs/ROADMAP.md` §5.8.
- **v5.18.1 — merge-манифест вахты M** (`docs/reports/merges/merge_manifest_v5_18_1.md`):
  карта линии v5.12→v5.18.1, опасные зоны (нумерация вех! не воскрешать диету!), гейты приёмки.
- **v5.18.0 (этот батч) — ЗАКРЫТИЕ ВЕХИ 5: диета −9.6 МБ, вехи переставлены, LEGAL.md, курс, реестр роадмапов.**
  Диета (протокол `docs/reports/releases/archive_diet_v5_18_0.md`): статьи только .pdf (13 docx →
  `manuscripts_index.md`), фигуры-дубли и pdf/png ответов редакций → `responses_digest.md`, питчи-pdf
  регенерируемы, лого 2039K→489K, `.mypy_cache` исключён из zip. Вехи: **6=Тест, 7=Юр, 8=Фронт, 9=Деплой**
  (обоснование в шапке ROADMAP; прежний порядок — `docs/roadmap_archive/`). `LEGAL.md` в корне.
  `knowledge/business/solution_factory_course.md` (питч-курс: CustDev/PSF/PMF, JTBD, TAM/SAM/SOM,
  юнит-экономика, уроки Eventify). Механизм роадмапа: п.6 — веха не закрывается без блока
  «Предложения Claude». Манифест публикации public-зеркала — на подтверждении владельца. Код не тронут.
- **v5.17.0 — портретное тестирование мат-модели: 12 000 портретов, 0 нарушений (§6.2 суб-пункт).**
  `tools/portrait_testing/` (генератор 10 граничных типов + 11 инвариантов канона + раннер с
  отчётом), 7 property-тестов в fast. Свип 2000 (seed 20260702) + контроль 10000 (seed 77):
  **0 нарушений, 0 падений**, ~0.2 мс/портрет. Находка: решётка кусочная 66/11/1 — канон
  (`|A|≤66`) корректен, инвариант строже. Отчёт: `docs/reports/testing/portrait_sweep_2026_07.md`.
  Гейты: flake8 репо=0, mypy новых чисто, смежные ядра зелёные; `app/` не тронут. Грабли: venv
  умирает после mv папки (шебанг) → `python -m pip`; rng-сид кортежем запрещён в 3.12.
- **v5.16.0 — научный трек в базе (`knowledge/science/`, §5.9) + инцидент доставки ЗАКРЫТ.**
  42 файла / ~8.5 МБ: статусы публикаций (принятая МНТК-РИНЦ по справке + КИМ-пакет с replication
  package + 3 подачи), рукописи, ответы редакций, гайды. Private-only (в whitelist зеркала не
  добавлять). ⚠️ DOC-03: нормативная нормализация из статьи КИМ в коде НЕ реализована — кандидат
  v3.1. Инцидент доставки: корень подтверждён (Tor Browser → file-CDN; Safari+VPN качает всё) —
  правило «артефакты в обычном браузере», PIT-009 v2 как страховка. Код/гейты не тронуты.
- **v5.15.0 — все 7 предложений §5.8 п.4 РЕАЛИЗОВАНЫ + протокол доставки v2.**
  Новые доки: `docs/onboarding.md` (вход за час) · `docs/glossary.md` · `docs/api_contract.md`
  + снапшот `docs/api/openapi.json` (**106 путей**, генератор `tools/api_snapshot/dump_openapi.py`,
  flake8 по репо = 0) · `docs/backup_restore.md` (RPO/RTO, age-офсайт РФ) · `docs/pdn_data_map.md`
  (карта ПДн юристу) · `docs/slo.md` (p95-бюджеты, цели Locust) · `diagrams.md` §9 (физический
  деплой). **INC-DELIVERY-STUB → PIT-009 v2:** внутри чисто (sha256, целостность), ломается на пути
  платформа↔Tor; свой вклад (замена файла по выданному пути) устранён; протокол: выданный путь
  неприкасаем, ретрай под новым именем, sha256 в чат, Tor-обходы. Код `app/` не тронут.
- **v5.14.0 — §5.8 п.2 ЗАКРЫТ: диаграммы доведены.** `10_er_database.drawio`
  перегенерирован программно (28 таблиц, 6 кластеров, 11 FK-связей, канон `diagrams.md` §2) +
  постраничный свап страницы ER в мастере; C4-component 06 (+страница мастера) — добавлены
  `app/ingestion` (ADR-005) и `mfa.py`; символы ∈/≠/∅ выпилены из 01/02 (WebKit-грабля);
  7 протухших PNG удалены, чек-лист регенерации на Mac — `docs/diagrams/README.md` §Превью
  (+строка в «Ожидается»). Весь §5.8 теперь закрыт. Код/гейты не тронуты.
- **v5.13.0 — §5.8 п.3 и п.4 ЗАКРЫТЫ (живой роадмап + предложения).**
  (1) Методичка `knowledge/guides/roadmap_methodology.md` внесена в репо — через форк она НЕ доехала:
  проверка `find && echo ЕСТЬ` лгала (exit 0 и без находок) → **PIT-008** заведён (pitfalls + §4 ниже);
  внесена версия линии V + новый **§15 «механизм самоприоритизации»** (перенумерация 15–17 → 16–18).
  (2) **Механизм живого роадмапа** — блок в шапке `docs/ROADMAP.md`: алгоритм выбора (приказ → указатель →
  спец-поток → строгий порядок вех), тай-брейки (хвосты · разблокировка/дедлайн · user-facing · quick win),
  указатель **«СЛЕДУЮЩАЯ ЗАДАЧА»** + дисциплина синхронизации с этим §0. (3) П.4 — 7 предложений
  (onboarding · glossary · API-снапшот · backup_restore · карта ПДн · SLO · deploy-диаграмма) под чекбоксом
  §5.8, решения за владельцем (продублировано в «Ожидается»). Код/гейты не тронуты.
- **v5.12.0 (docs) — §5.8 п.2 диаграммы, ЧАСТИЧНО (источники готовы).** Приоритет —
  правильность источника > полнота превью. **Сделано:** `docs/diagrams.md` (mermaid, канон) приведён к коду —
  ER **9→28 таблиц** (все `__tablename__`, хабы users/households), §1 слои (ingestion-пакет ADR-005 +
  сервисы mfa/notifications/telegram/subscription/referral), §5 внешние (cbr-xml-daily → **cbr.ru** + Telegram),
  §7 auth (MFA/TOTP двухшаговый + JWT-ревокация jti + mass). drawio: «21 альтернатива»→**66** и «шаг 20%»→**10%**
  в 6 схемах; C4-container «5 таблиц»→**28**, «6-этапный»→**9-шаговый**; XML всех drawio валиден.
  **Остаётся (отдельный проход):** регенерация ER-drawio `10_er_database` на 28 таблиц (раскладка),
  C4-component `06` + ingestion/MFA, PNG-превью 01/02 (CDN Chromium блокирует → десктоп). Источник регенерации —
  `diagrams.md` §2. Пункт §5.8 п.2 НЕ закрыт полностью — прогресс отмечен под чекбоксом.
- **v5.11.0 — полная ревизия репозитория (§5.8 п.1, закрыт).** Отчёт —
  `docs/reports/audits/repo_revision_2026_07.md`. Главное:
  - **BUG-028 закрыт:** календарная мина в `test_expense_record_date` (хардкод `2026-01-10`
    против окна 6 мес от `utcnow()` — взорвалась 1 июля без единого коммита). Фикс —
    относительная дата. Разбор: `docs/reports/incidents/bug_028_calendar_fixture_bomb.md`.
  - **«Машина времени»** — `tools/timewarp/warp.py` (сдвиг `utcnow` на `WARP_DAYS` до импорта
    приложения). Свип оконного домена +90/+370: вторая находка — `test_obligation_term` на
    `datetime.now()` (локаль) → переведён на `utcnow()`. Контроль +370: 153/153. Правило дат и
    варп-приёмка — в `docs/QA.md`. Auth/JWT-домен НЕ варпить (iat/exp на реальных часах).
  - **PIT-007 заведён и закрыт:** v5.1.0 добавил `tools/survey_analysis/*.py` docs-батчем без
    `flake8 .` → 12 нарушений накопилось. Исправлены точечно, гейт = 0. Правило: добавил любой
    `.py` — гони полный гейт, «docs-батч» определяется по диффу, не по намерению.
  - Ссылки/легаси: 4 битых пути в README (реорг v5.8.0), `06_ТЗ_…` → `ui_visual_design_brief.md`,
    `FINPILOT_*`-имена в UI-доках → актуальные пути; `ut_00n_*` нейминг; QA.md `ruff` → flake8
    (по факту гейта); `black` удалён из requirements-dev; it_stack «21 альт.» → 66. ROADMAP:
    гниющая «Текущая точка» заменена ссылкой на шапку WATCHLOG, «Ожидается» актуализирован.
- **v5.10.0 — методичка «Процесс разработки FINPILOT»** (`knowledge/guides/development_process_methodology.md`)
  + разбор task-tracking: канон = ROADMAP+WATCHLOG в репо, Confluence отложен. §5.6/§5.4 закрыты.
- **v5.9.0 — банки и выписки (§5.5 закрыт):** реестр банков (СБП 226 на 03.2026), справочник
  форматов (физлица→PDF, юрлица→1C), стратегия universal-парсера (реализация — веха 6).
- **Состояние (проверено НА ЭТОЙ вахте, не по наследству):** fast **957/957 зелёные** (1 skip —
  PG-only реверсивность миграций), варп +370 по оконному домену 153/153, `flake8 .` = 0,
  mypy `app/` — 105 файлов чисто. Бэкенд зрелый: весь P0–P3 и раздел 4.4 (кибербез, бэк-часть)
  закрыты в вехе 4; остатки 4.4 deferred по вехам (Redis→9, CSP→6, CSRF→6). Полный срез
  безопасности — корневой `SECURITY.md`. Миграции 0001–0028, одноголовая цепочка, PG-прогон ✓.

**Ожидается от Василия:** решения юриста (веха 8, `docs/legal_questions_for_lawyer.md`) —
остальное из старого списка получено/закрыто (Telegram-токен ✓, банки ✓ v5.9.0, материалы ✓ v5.1.0).

---

## §1. Протокол вахты + аккаунты

**Аккаунты:** Василий работает на 4 аккаунтах Claude — **V, J, M, S** — несущих вахту по очереди
через этот журнал (плюс несколько чатов внутри одного проекта). Текущую вахту описывает **M**.
Имена нужны только если Василий уточняет «акк V/J/M/S» в разборе; на работу не влияют — журнал
самодостаточен для любого.

**Заступая на вахту:**
1. Прочитай этот файл + `docs/ROADMAP.md` (направление) + `docs/sandbox_runbook.md` (грабли
   песочницы — НЕ диагностируй заново).
2. Распакуй архив, сверь версию в `app/config.py` с §0.
3. Подними venv (`python3 -m venv .venv` + `pip install -r requirements.txt -r requirements-dev.txt`);
   для E2E — `playwright install chromium`.
4. Отчитайся Василию коротко (3-4 строки): версия, текущая веха, следующий шаг.
5. НЕ переделывай закрытое, НЕ уходи с порядка ROADMAP без команды.

**Неся вахту:** обновляй §0/§4 по ходу; разборы оформляй САМ по триггеру (инцидент/расследование/
pitfall — маппинг в §5); в конце батча WATCHLOG живёт ВНУТРИ архива (`docs/WATCHLOG.md`) — наружу
отдаётся только сам архив. Отдельным `.md` через `present_files` журнал НЕ дублируем (политика
«вся документация внутри продукта», v5.23.0; подробно — `docs/session_continuity.md` §4). Исключение —
явный разовый запрос владельца дать файл.

---

## §2. Направление (выжимка ROADMAP)

Полностью — `docs/ROADMAP.md`. **Вехи проекта нумеруются как мажоры (v4-v9), это СОЗНАТЕЛЬНО не
классический SemVer** (мажор = крупный этап, не слом контракта). Внутри вехи: фича=MINOR, фикс=PATCH.

| Веха | Версия | Тема | Статус |
|---|---|---|---|
| 4 | `v4.x.x` | Завершение бэкенда + техдолг | В работе (остатки §4.4 deferred → 8/9) |
| 5 | `v5.x.x` | Организационное (репозитории, доки, бизнес, исследования, методички, банки, наука) | **Завершена** |
| **6** | `v6.x.x` | **Тестирование** (мат-модель + независимая экспертиза + импорт) | **← в работе** (v6.0.0: экспертиза 12000×4, модель → v3.1.0, согласие 87.4%) |
| 7 | `v7.0.0` | Юридическое (документы + соответствие законам) | Запланировано |
| 8 | `v8.0.0` | Фронтенд (React+TS, редизайн, вынос всех возможностей бэка) | Запланировано |
| 9 | `v9.0.0` | Деплой на сервер | Запланировано |

Порядок строгий: бэк → организация → **тестирование → юр → фронт** → деплой. _(Пересмотр 2026-07-02,
приказ владельца: математика замораживается и проверяется до фронта; юр-тексты — контент экранов,
фронт вшивает их сразу; юр-ответы внешние и медленные — запускаем раньше. Прежний порядок — `docs/roadmap_archive/`.)_

---

## §3. Последние 10 версий (новое сверху; РОВНО 10 — добавил новую, удали старую снизу)

> Полная история — `CHANGELOG.md` / `docs/RELEASES.md`. Здесь только 10 свежих (правило в шапке).

- **v6.12.0** — Сверка CSV: контроль полноты вскрыл потерю всех приходов (MINOR). Три невидимых
  дефекта универсального CSV: «поступление» не подстрока «Поступления» (у Райфа терялись ВСЕ приходы,
  14→8 операций); «amount» ловил колонку «(credit)» (вся выписка как доход); строка метаданных
  принималась за шапку (0 операций). Тинькофф: наивный float() → `_num`. Контроль полноты разбора с
  причинами пропуска (12 788 строк → 12 614, все 174 объяснены). Открытие: «выписка мамы» — Альфа-Банк,
  её CSV несёт контрольные итоги → сверка сходится (224 805.37 / 221 195.56). 195 тестов.
- **v6.11.0** — Property-генератор выписок (Б2) + ретроспектива + протокол аудита (MINOR).
  `synth.py` рендерит все реальные шаблоны с фаззингом форматирования; 16 property-тестов, ~625
  синтетических выписок за прогон, многостраничные на 60 операций. Зафиксирована граница метода:
  синтетика не доказывает покрытие банков (генератор знает то же, что парсер) — доказывает только
  сверка с итогами. Отчёты: ретроспектива процесса и протокол независимого аудита (holdout, ручной
  ground truth, «тихие ошибки = 0»).
- **v6.10.0** — Измеритель точности: сверено 10 из 11 реальных PDF, было 6 (MINOR). Итоги Сбера
  («Пополнение»/«Списание») — независимо подтвердили фикс кредитного лимита; итоги Райфа из таблицы
  («Всего поступлений») → Райф 5/6 сверяемы; счётчик «Количество операций 3 4» (полнота набора);
  балансовое тождество для ВТБ/Тинькоффа (у Тинькоффа отдельно подтвердило, что кэшбэк — приход).
  К Сберу тождество НЕ подключено: не сходится у самого банка. Вердикт на нескольких свидетелях.
  Тесты 37, покрытие 94.5%.
- **v6.9.0** — Аудит на реальных выписках ч.2: Райффайзен, детект банка, сверка в импорте (MINOR).
  Райф-парсер БЫЛ, но на живых файлах не запускался: `'райф' in 'Райф 1.pdf'.lower()` = False (NFD).
  У Райфа три шаблона (RU/EN зеркальны, третий без колонки «№») → 0→14 операций. Сбер: «Установка
  кредитного лимита» +110 000 ₽ шла доходом (220 000 ₽ фиктивных) → фильтр. NFC в `_norm`. Детект банка
  по содержимому (11/11) в проде. Сверка с итогами банка в импорте — вариант B (предупреждать, не
  блокировать): `app/services/statement_reconcile.py`, бейдж в UI; ВТБ/Райф/Тинькофф сходятся. Новые
  справки (Сбер 8/9, Газпром рус+англ, Альфа) + матчинг без пробелов. Тесты 24+60, покрытие 92.7/95.2%.
- **v6.8.0** — Аудит парсера на РЕАЛЬНЫХ выписках (MINOR): 6 банков, 64 файла. ВТБ — колонки по шапке
  (три шаблона со сдвигом): описания всей июньской выписки были заглушкой «Операция», текст описания
  вычитался как комиссия (`_num('Перевод 1500')=1500`). Кешбэк 656 ₽ в колонке «Комиссия» уходил в
  расход → знак по семантике описания. ВТБ давал 0 операций (нетто = Приход − Расход − Комиссия). 1C —
  cp1251. Справки распознаются вместо silent-0. Доказано сверкой с итогами банка: июнь 54 оп
  (100 624.00/101 285.09), май 47 (32 156.00/32 410.30), ВТБ 1 — 3 (267.01), баланс замыкается.
  Инструмент `tools/statement_audit/reconcile.py`, тесты (28), отчёт `statement_parser_real_data_audit.md`.
- **v6.7.0** — Переименование канона → v3_3_0 + §6.4 импорт выписок (MINOR): `math_model_v3_2_0.md` →
  `v3_3_0.md` (файл нёс контент v3.3.0), живые указатели обновлены, revision-gate CLEAN. §6.4:
  эталонные шаблоны выписок в продукт `app/data/statement_templates/` (CSV/XLSX/4×PDF/1C, генератор +
  MANIFEST; PDF рендер DejaVuSans, round-trip pdfplumber) + 15 golden-тестов чтения + universal-
  стратегия на незнакомом банке. Корпус нашёл дефект образца Райффайзена (Debit со знаком минус).
  Отчёт `statement_import_coverage.md`. Веха 6: открыт только гейт сертификации (BLOCKED на владельце).
- **v6.6.0** — G7-остаток: остаток целевого бакета → резерв (MINOR, ADR-009): каскад долг→цели→резерв
  замкнут (`x_reserve_effective = x_reserve + x_goals_unused`, `Lt'` на эффективном резерве); при
  насыщении целей остаток раньше испарялся из плана. Замер 12 000 портретов v2: переток у победителя
  0.41%, смена рекомендации 0.16%, спасено от испарения до 194 395 ₽; свип v2 — 0 нарушений I1–I14 (I10
  на эффективном резерве, I4 не затронут). Фикс сохранения денег (долг_эфф+цели+резерв_эфф=R⁺). Стенд
  `goal_remainder_reroute_benchmark.py` + отчёт + 6 тестов; канон §5.2/§10.4/v3.3.0.
- **v6.5.1** — Решение по шагу решётки (ADR-008: остаёмся на 10%, `|A|=66`) + отчёты + мина id (PATCH):
  массовый прогон 2000 портретов (победитель тот же в 90.9%, +0.07 п.п. полезности, главный совет меняется
  в 0.8%); отчёт `grid_step_5pct_benchmark.md` (закрыл висячую ссылку теста); археология INC-NULL-DEADLINE
  (D2/D3 — ровесники ядра v1.x); коллизия id альтернатив закрыта (fail-loud + разделитель на мелком шаге,
  формат на 10% не тронут); стенд перестал врать про «совет ОТЛИЧАЕТСЯ» (решает `best`, не топ-3).
- **v6.5.0** — Бессрочные цели `deadline: null` + три латентных дефекта под NOT NULL (MINOR):
  миграция `0029` (batch), канон §11.2 (`τ=∅ ⇒ u_s=1`), сквозь слои (модель/схемы/B2B/CRUD/ядро/UI/демо);
  **INC-NULL-DEADLINE (near miss):** 500 на `/spending-advice` (`round(None)`, сортировка с None),
  расхождение порядка SQLite↔PG (`NULLS LAST`), ложь «до дедлайна ~12 мес» (работала в демо); PIT-010.
  14 тестов на матрице SQLite+PG16; flake8=0 (подобран хвост v6.4.0); пин migrations 28→29.
- **v6.4.1** — Консистентность прогноза витрина↔прод + фиксация поведения тестами (PATCH):
  «Валидация»/`/demo/analyze` компаундят баланс той же канонной ставкой `r_bench`, что и `/planning`
  (раньше — по ставке депозита, график чуть расходился); +2 регресса через реальные эндпоинты
  (направление прогноза по истории; витрина == планирование по форме — лочит анти-костыль); README 6→10.
> Старее v6.4.1 — в `CHANGELOG.md` (полная история) и `docs/RELEASES.md` (эпохи).

## §4. Заметки и находки (не переоткрывать)

- **Месячные показатели — за текущий месяц, НЕ за всю историю (v6.4.0):** `calculate_income_total`/
  `calculate_expense_total` суммируют все транзакции; границу месяца ставит `prepare_data(period_days=30)`
  (единственная точка — у неё ровно 3 вызова, всем нужен месяц). У кого нужна вся история — явно
  `period_days=None`. Латентный баг «доход = сумма всех месяцев» закрыт.
- **Прогноз — на РЕАЛЬНЫХ транзакциях, костыль убран (v6.4.0):** синтетического канала истории больше нет;
  `build_monthly_history` бинует полный список по скользящим 30-дневным окнам (устойчиво к границам месяцев),
  последний бин = месячный тотал. Демо-кейсы получили реальную многомесячную историю (`_history_transactions`,
  `_case_data`) → «Валидация» и `/planning` читают один источник. Признак костыля был: одна сущность считается
  по-разному в двух местах.
- **`run_planning` требует у каждой цели ключ `id` (v6.4.0):** в проде его проставляет сериализатор;
  при прямом вызове (демо/валидация) — проставлять синтетически (`g{i}`), иначе `KeyError: 'id'`.
- **`goals.deadline` — NOT NULL (v6.4.0):** бессрочные цели на уровне БД пока не поддержаны; в демо —
  дальний горизонт (10 лет). Nullable-дедлайн = отдельная миграция (пункт роадмапа).
- **`build_monthly_history` теперь по СКОЛЬЗЯЩИМ окнам, не календарным (v6.4.0):** тест, кодировавший
  календарное поведение, обновлён (`test_forecast_shape`). Красный тест после осознанной смены поведения —
  сверять «кто прав» (тест или код), а не рефлекторно чинить код.
- **Голый `TestClient(app)` НЕ создаёт таблицы (v6.4.0):** для разовых end-to-end прогонов —
  `Base.metadata.create_all(bind=engine)` на временной SQLite; в тестах это делает фикстура.

- **Эталон экспертизы регенерируется ТОЛЬКО генератором v1 (v6.0.0):** `knowledge/model_validation/joined.csv.gz`
  восстановим строго `PortraitGenerator(seed=20260702, version=1)` — v1 в `tools/portrait_testing/generator.py`
  заморожен бит-в-бит ради этого (self-check стенда: статус 12000/12000). Любые новые прогоны — генератор v2
  (default). Раннер: `--gen-version`. Не «чинить» v1 и не менять его RNG-поток.
- **Инвест-транш — разметка, не четвёртое направление (v6.0.0):** решётка 66 альтернатив и инвариант сумм I4
  не тронуты; транш вычисляется поверх `x_reserve` сверх Lt*·Σe (`app/core/investment.py`). В метрике согласия
  с экспертами транш считается в бакет goals+ (семантика заключения: цели и инвестиции объединены).
- **Floor резерва — лексикографический уровень, не вес (v6.0.0):** сортировка ranked — по (floor_level, utility);
  `best.utility` может быть НЕ максимумом по списку — это норма (инвариант I5 обновлён, I13 добавлен).
  Golden-снапшоты при изменениях модели пересматриваются глазами (floor-эффект задокументирован в сценариях).

- **GitHub-тулы — разбор витрины Actions (2026-07-05, вахта M):** трёхуровневый CI (`ci.yml`: fast/full/deep,
  в full уже `bandit`+`pip-audit`+locust, в deep — `mutmut`+стресс-property) закрывает потребности продукта.
  Из marketplace-предложений **почти всё мимо**: Fortify (платный SAST — `bandit` покрывает Python),
  Jscrambler (JS-обфускация не наш кейс), SLSA (оверкилл до запуска), деплой-шаблоны облаков (RU-деплой на
  VPS+docker, санкционные облака нерелевантны), Django/Pylint/Python-app шаблоны (наш CI сильнее),
  Greetings/Labeler/AI-summary (соло-разработчик). **Что реально стоит включить (бесплатно, БЕЗ CI-минут):**
  1) **Dependabot alerts** — тумблер в Settings→Security обеих реп (и приватной). Именно **alerts**, НЕ
     update-PR: авто-PR конфликтуют с нашим forward-build (rsync `--delete` из архивов перетёр бы мердж).
     Закрывает окно между тегами, когда `pip-audit` (только в full-тире, на тегах) молчит.
  2) **Secret scanning + push protection** — Settings публичного зеркала (бесплатно на паблике).
  3) **CodeQL** — бесплатен ТОЛЬКО на публичных репо → внедрён этим релизом как `mirror_extras/…/codeql.yml`
     (в приватный не кладём: без GHAS воркфлоу падал бы). Портфолио-бейдж + межпроцедурный SAST сверх bandit.
  Вывод: писать новые воркфлоу не нужно — три тумблера в Settings + CodeQL уже в зеркале.

- **PIT-009 (2026-07-02, корень подтверждён — Tor Browser; инцидент ЗАКРЫТ):** качать артефакты в обычном браузере + VPN. Три фейла за день: карточки
  не появились (v5.12.0), «мёртвая заглушка» — кнопка есть, скачивание падает (v5.14.0), пропажа
  файлов владельца «вверх». Протокол: доставка не подтверждена до скачивания; outputs не трогать;
  перевыдача → копия под НОВЫМ именем (выданный путь неприкасаем!) → критический минимум;
  sha256 каждой выдачи в чат; владельцу на Tor — новая цепочка, поштучно, сразу. Разбор:
  `docs/reports/incidents/inc_chat_file_delivery_dead_stub.md`.

**PIT-008 (v5.13.0):** `find && echo ЕСТЬ` печатает «ЕСТЬ» и без находок (exit 0 всегда) — так чуть
не потеряли методичку роадмапа между линиями. Существование проверять `test -f` / непустым выводом.
Детали — `docs/pitfalls.md`.

**Рассинхрон flake8-гейта (app/ vs весь проект) — вскрыт и закрыт в v4.30.2.** Гейт flake8 определён в
ТРЁХ местах, и все гоняют `flake8 .` (весь проект): CI `ci.yml:80`, `Makefile` цель `lint`, pre-commit
hook. А руками каждый батч гоняли `flake8 app/` (узко) — поэтому `app/` держали в нуле, а `tests/`/
`loadtest/` незаметно копили E501/F841 (101 нарушение к v4.30.1). Официального GitHub-репа ещё нет →
полный гейт вживую ни разу не запускался, первый CI-run был бы красным. **Урок: гейт руками гонять ТЕМ
ЖЕ скоупом, что CI** — для flake8 это `flake8 .`, не `flake8 app/`. Выровнено вариантом A (реальное
приведение к 0, не игнор-конфиг): autopep8 --select по коду, точечный `# noqa: E501` по data-литералам
(enforcement нового кода жив), XML-фикстура → конкатенация. При merge-боли откатный путь — per-file-
ignore E501 для tests/ в `.flake8` (вариант C). Заодно убран `black` из pre-commit — прямое нарушение
§2 (латентная merge-мина: `make precommit` переформатировал бы весь проект).
> **Рецидив в v5.20.0 (тот же класс):** вахта J в v5.19.0 внесла `tools/portrait_testing/render_portrait_cards.py`
> с 27 нарушениями (26×E501 + 1×E741) — снова проверяли узким `flake8 app/`, а не `flake8 .` (CI-скоуп,
> `continue-on-error`, поэтому красный лог не заметили). Лечение вариантом C (§4 fallback): `per-file-ignores`
> в `.flake8` — строки >100 это выходные литералы досье-карточек, перенос менял бы текст эксперту, реформат
> запрещён (§2). Инвариант «flake8 репо = 0» восстановлен; файл J не тронут; ядро papки под гейтом (инъекция
> подтвердила). **Урок повторно: перед выдачей ВСЕГДА `flake8 .`, не `flake8 app/` — даже для docs/tooling-батча.**

**Текущая фаза планирования (что согласовано с Василием):**
- Карта версий 4-9 подтверждена; правило «вехи ≠ SemVer» — осознанное решение Василия.
- Фронт-стек React+TS зафиксирован с обоснованием — `docs/reports/adr/adr_001_frontend_stack.md`.
- Техдолг (mypy/flake8, utcnow, ExpenseRecord) РАЗБЛОКИРОВАН: работаем с конечной версией
  последовательно, параллельных правок нет → конфликтов мёржа не будет.
- Принцип заглушек вехи 4: MVP/каркас для того, что в роадмапе и точно нужно фронту; спекулятивное
  не выдумываем (YAGNI).

**Мат-модель — канон `docs/math_model_v3_0_0.md`** (источник истины — код): 66 альтернатив, шаг 10%,
stock-based `Lt = B_liq/E` (месяцы), L_min=0 (фильтр выключен), MC-интервал 80%, инварианты Rt≥0 и
ПДН≤0.40. Если всплывёт спека с 21 альтернативой / шаг 20% / flow-based Lt / 95% — это УСТАРЕВШАЯ
v2.0.2, не источник истины.

**Время — единый контракт (после BUG-026, v4.25.3):** всё «сейчас» в коде идёт через
`app/utils/time.py::utcnow()` (naive UTC). Прямой `datetime.now()` (локаль сервера) в `app/` —
запрещён, он давал 3ч-сдвиг на MSK-VPS. Исключение — намеренные aware-вызовы `datetime.now(timezone.utc)`
в `cbr_rate`/`cbr_fx`/`security` (там нужен aware UTC). Колонки моделей — naive UTC (вариант A, не aware).
Костыль `obligation.replace(tzinfo=None)` оставлен намеренно — гасит aware-вход из Pydantic, это НЕ про
«сейчас». Полная tz-корректность (зона юзера → локальный календарь для дни-недели/внутримесячных
паттернов) — отдельный заход в слое паттернов, там же заведётся поле `tz` у User (сейчас его нет).
Вариант B (миграция timestamp-колонок на `timezone=True`) НЕ нужен для корректности приложения —
только для интеропа БД у вехи деплоя; шов готов (убрать `.replace(tzinfo=None)` в helper). Полное
обоснование развилки A/B (почему B — не требование корректности) — `docs/reports/adr/adr_002_datetime_storage.md`.

**JWT-ревокация (v4.26.0, раздел 4.4) — устройство и контракт.** Логика отзыва централизована в
`app/database/revocation.py`: точечный рубеж — блок-лист `is_token_revoked`/`revoke_token`/`purge_expired`;
массовый — предикат `tokens_invalidated_for(user, iat)` + `bump_tokens_valid_since`; конвертер
`epoch_to_naive_utc` нормализует JWT-таймстампы (int/float/aware-datetime) в naive-UTC. `dependencies`
и `routes_auth` таймстампы НЕ трогают — только зовут модуль (`revoke_token` принимает сырой `exp`).
Рубеж `tokens_valid_since` режется до секунды (iat — секундная гранулярность), сравнение строгий `<` —
немедленный re-login после logout-all проходит. Легаси-токены без `jti`/`iat` остаются валидными
(обратная совместимость, покрыто тестом).

**Инвалидация сессий при смене/сбросе пароля (v4.27.0, раздел 4.4) — устройство и дизайн-выбор.**
Подключена к тому же массовому рубежу `tokens_valid_since`. `change_password`: `bump_tokens_valid_since`
(валит другие устройства) + `_revoke_request_token` (точечно гасит текущий токен по `jti` — закрывает
sub-секундное окно округления рубежа: токен той же секунды иначе пережил бы строгий `<`) + чистка
auth-cookie + событие `password_changed`. `reset_password`: только `bump` (юзер не залогинен; текущего
токена/cookie нет; вход новым паролем проходит сразу — округление рубежа не задевает свежий токен).
Изменения ТОЛЬКО в `routes_auth.py`, миграций нет (механизм из 0027 переиспользован). **Дизайн-выбор
Option A — намеренный, НЕ переоткрывать:** для финпродукта (банковская планка) смена креденшелов валит
ВСЕ сессии, включая текущую — сильнее и ожидаемее «оставить текущую» (Google/GitHub). Option B
(сохранить текущую сессию) сознательно отклонён: требует ре-выпуска токена + нового cookie + чтобы
фронт подхватил новый токен, а фронт — отдельная веха (пока Jinja+vanilla JS). Инвалидация привязана к
УСПЕХУ смены (verify + update прошли); неверный текущий пароль → 400, рубеж не сдвинут. Карта линии для
слияния с другими вахтами — `docs/reports/merges/merge_manifest_v4_27_0.md`.

**Ротация ключа шифрования (v4.28.0, раздел 4.4) — устройство и эксплуатация.** `TokenCipher` под
капотом — `MultiFernet`. Ключи берутся из `settings.token_encryption_keys_list`: `TOKEN_ENCRYPTION_KEYS`
(через запятую, PRIMARY первым) → один `TOKEN_ENCRYPTION_KEY` → derive из `JWT_SECRET`. `encrypt` всегда
шифрует PRIMARY (первый ключ), `decrypt` перебирает весь набор, `rotate` перешифровывает токен primary.
Перешифровка существующих данных — `app/database/reencrypt.py::reencrypt_all`: колонки находятся
**интроспекцией** маппинга (`isinstance(col.type, EncryptedString)`), поэтому новое шифрованное поле
попадёт в перешифровку без правки модуля — не вести ручной список. Переписывает и старо-зашифрованные
(читаются прежним ключом набора), и legacy-незашифрованные (читаются как есть → шифруются primary).
**Эксплуатация ротации без простоя:** (1) сгенерировать ключ `Fernet.generate_key()`, поставить ПЕРВЫМ в
`TOKEN_ENCRYPTION_KEYS`, старый оставить вторым; (2) `python scripts/reencrypt_keys.py`; (3) убрать
старый ключ из набора. Миграций схемы нет — формат хранения тот же, меняется только ключ. `EncryptedString`
сохраняет обратную совместимость (decrypt вернул None → отдаёт значение как есть), так что включение
шифрования на ранее незашифрованных данных безопасно.

**Security-регрессии и старт-гард ключа (v4.29.0, раздел 4.4) — защитная сетка, не ослаблять.**
`tests/test_security_regressions.py` фиксирует инварианты харденинга: (1) security-заголовки на ЛЮБОМ
типе ответа — работает потому, что `SecurityHeadersMiddleware` зарегистрирован ВНЕШНЕ `CSRFMiddleware`
(порядок `add_middleware` в `main.py`: Rate→CSRF→SecurityHeaders→Logging; внешний обрабатывает response
последним), поэтому даже 403 от CSRF проходит через установку заголовков. Не менять порядок middleware
вслепую — заголовки на ошибках/отказах слетят. (2) CSRF-блок на всех изменяющих методах, с исключениями
Bearer/api-key/без-Origin (срабатывает по методу ДО роутинга — тестируется на любом пути). (3) **Старт-гард
ужесточён:** в production обязателен явный `TOKEN_ENCRYPTION_KEY` или `TOKEN_ENCRYPTION_KEYS` — без него
ключ шифрования деривится из `JWT_SECRET`, что лишает смысла ротацию (v4.28.0) и разделение ключей. Это
**намеренное требование деплоя** (`.env.example` обновлён) — не откатывать ради удобства локального прода.

**MFA/TOTP (v4.30.0, раздел 4.4) — устройство и инварианты.** Второй фактор на `pyotp`. Состояние на
User: `mfa_secret` (зашифрован `EncryptedString`) + `mfa_enabled`; пока `mfa_secret` задан, а
`mfa_enabled=False` — это «pending» (enroll сделан, confirm нет). Крипто — `services/mfa.py`, доступ к
данным — `database/mfa_store.py` (разделение как у revocation). **Ключевой инвариант — двухступенчатый
логин:** при `mfa_enabled` `/auth/login` после верного пароля НЕ ставит cookie и НЕ выдаёт сессионный
токен — возвращает `mfa_pending`-токен (purpose-claim, TTL 5 мин); полную сессию выдаёт только
`/auth/mfa/verify` после TOTP/recovery-кода. **Не выдавать сессию в login при включённом MFA** — это
вся суть фичи. Recovery-коды: 10 шт, bcrypt-хеш, одноразовы (`used_at`), показываются один раз при
confirm; и verify, и disable принимают TOTP ИЛИ recovery-код. **Связанный инвариант (purpose-guard):**
`get_current_user` отвергает ЛЮБОЙ токен с claim `purpose` — полная сессия его не имеет. Это держит
mfa_pending (и заодно email_verify/password_reset/telegram_link) вне сессионного доступа; не ослаблять
эту проверку, иначе промежуточный/одноразовый токен станет полноценным входом.

**Консолидация трёх параллельных линий v4.26.0 (правило §5 #15).** JWT-ревокацию делали параллельно
три вахты (M, J, S). Эта версия — их слияние по лучшим практикам, не переписывание: ядро `revocation.py`
(инкапсуляция + предикат + конвертер) и тест-набор — из линии M (самый полный, 17 кейсов, форж токена с
прошлым iat для кросс-сессионного logout-all); DRY-хелпер `_revoke_request_token` в routes — из линии J;
обвязка релиза (CHANGELOG/ROADMAP/WATCHLOG) — из S. PIT-004 (распознать сбой канала) восстановлен по
линии M, PIT-005 (на возобновлении верь архиву) — урок линии J. Модели/`security.py`/миграция 0027 у
всех трёх совпали — мёрджить нечего.

**Консолидация S+J (v4.27.0→v4.28.0).** Линия S наработала только docs-добавки поверх J-базы:
`docs/pitfalls.md` (PIT-006 — `cd X && job &` фонит весь пайплайн вместе с `cd`, приоритет `&` ниже
`&&`) и `SECURITY.md` (строка «Отзыв сессий» в таблице §2 + раздел §3a «Харденинг сверх минимума»).
Сверено `diff` против J-версий: расхождение только в этих аддитивных вставках, иного дрейфа нет —
файлы внесены к J без конфликтов. Код S и J не расходился (S работал по докам). Внесено в архив v4.28.0.

**Известные баги к разбору в вехе 4** (давние заметки Василия): (1) ставка ЦБ всегда ставит дефолт —
реального fetch нет (вероятно cbr.ru 403 → зеркало `cbr-xml-daily.ru`); (2) письма «отправлены», но
на почте пусто (SMTP no-op в dev маскирует). Диагностика до корня, не угадывать.

**Открытые вопросы для вехи 6** (тестирование): график прогноза — у Василия ощущение что баг (всегда
прямая линия от константы-зарплаты); нужен тест-кейс где НЕ прямая. Совет при `Rt<0` — исследовать
(реальный кейс Василия с кредиткой). Шаг 5% вместо 10% — замерить скорость.

**PostgreSQL поднимается в песочнице** (не спихивать на разработчика): `apt install postgresql` БЕЗ
`apt-get update` ПРИ ОТКЛЮЧЁННОМ nodesource (он даёт 403 и травит весь update; Ubuntu-репы рабочие —
`mv /etc/apt/sources.list.d/nodesource.sources{,.disabled}`, затем `apt-get update` уже проходит) →
`pg_ctlcluster 16 main start` → роль/БД через `su postgres -c psql` →
`DATABASE_URL=postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test`.
**PG-хвост ЗАКРЫТ (v4.30.1):** вся цепочка 0001↔0028 прогнана вживую на PG 16 — `upgrade head` ✓,
`downgrade base` ✓, обратно ✓; миграции 0023–0028 аддитивны и реверсивны. Регрессия зафиксирована
`tests/test_migration_reversibility.py` (под PG; скип на SQLite). Больше PG-миграций в очереди нет.

**BUG-027 (v4.30.1) — reverse-каст 0009 на PostgreSQL.** `downgrade()` миграции 0009 кастует
`user_id` varchar(36)→integer для `events`/`recommendations` без `USING` → на PG `DatatypeMismatch`
(SQLite молчит — динамическая типизация). Фикс: `postgresql_using="user_id::integer"` на обе колонки
(на пустой таблице каст — no-op, проходит). Трогает ТОЛЬКО downgrade-ветку 0009, upgrade не изменён →
накатанные базы не задеты. Это была единственная нереверсивная точка цепочки; найдена код-ревью +
живым PG-прогоном, не на проде.

**Сбой канала тул-коллов — методичка `docs/tool_call_channel_failures.md` (v4.30.1).** Развёрнутый
PIT-004: вызов инструмента иногда выходит в ответ ВИДИМЫМ текстом (`court` + сырой `<invoke>`) и ход
обрывается, но песочница жива. Кейсы: завершение хода вызовом, сложная команда (heredoc/кавычки),
**заражение паттерном из истории** (сломанные хвосты в контексте воспроизводятся как текст), короткий
ход между обрывами. Протокол: НЕ завершать ход вызовом (кончать текстом + `present_files`),
игнорировать сломанные образцы из истории, весь батч одним ходом, при обрыве — реран с проверки
состояния (не полный hand-off — это лечится рераном). Не объяснять глюк по пять раз.

**Грабли песочницы** (детали — `docs/sandbox_runbook.md`): полный одновременный прогон всех тестов
рубится лимитом инструмента (~30с → returncode -1) → гонять пофайловыми группами; итоговая строка
pytest не флашится в файл → использовать `--junitxml` + парсить XML + exit code; фоновые процессы не
флашат лог надёжно; многострочные bash с `\` ломаются; браузеры firefox/webkit недоступны (Azure CDN),
chromium есть.

---

## §5. Жёсткие правила (чек-лист «не дрейфовать»)

1. **TDD** — тест → красный → реализация → зелёный, реально прогонять.
2. **`app/core/` не трогать** без согласования (исключение — `spending_advice.py`).
3. **Схемные изменения — на PostgreSQL** (SQLite молчит про FK).
4. **SemVer внутри вехи:** фича=MINOR, фикс=PATCH. Bump `APP_VERSION` + `CHANGELOG.md` каждый батч.
   Вехи (v4-v9) — крупные этапы, не классический SemVer (см. §2).
5. **Без массового реформата** кроме координированных окон (сейчас окно открыто — техдолг закрываем).
6. **ROADMAP — источник приоритетов, строго по порядку.** Не изобретать параллельную нумерацию.
7. **Без эмодзи.** Русская проза + английский код. По-братски, прямо, без канцелярита.
8. **НЕ объявляй невозможным, не проверив.** PostgreSQL/chromium/apt поднимаются здесь. Реально
   внешнее (cbr.ru 403, живой SMTP, деплой) — честно отметить.
9. **Перед «с нуля» — проверь, не сделано ли уже** (бэк часто опережает план).
10. **Вывод тестов — через `--junitxml`** (не `tail` по логу): итоговая строка не флашится надёжно.
    Парсить XML + exit code. (§4, `docs/pitfalls.md`.) **Что гонять и когда (узкий цикл vs полный) —
    `docs/test_run_optimization.md`; механика полного fast — `docs/sandbox_runbook.md` §7.**
11. **НИКОГДА не писать имена в гите / релизах / публичных артефактах** — официально безымянно.
12. **Тестирование мат-модели — СТРОГО вне проекта** (Opus 4.8, без памяти/инструкций → не biased).
13. **E2E добавляются с unit** для изменений фронта/API; CRUD-флоу обязаны иметь E2E.
14. **Разборы — АВТОМАТИЧЕСКИ по триггеру:** дефект в коде → инцидент (`docs/reports/incidents_summary.md`);
    искали, но дефекта нет → расследование (`docs/reports/investigations_summary.md`); повторная ошибка
    воркфлоу → pitfall (`docs/pitfalls.md`); найденный баг → карточка по `docs/reports/bug_report_template.md`.
15. **Слияние разошедшихся линий → консолидация, не переписывание истории.** Полная процедура,
    гейты и антипаттерны — **`docs/reports/merges/merge_and_fork_guide.md`**. Профилактика: синкать аккаунты чаще,
    координировать номера версий (версия закреплена за задачей, не за аккаунтом).
16. **Чекпоинт-дисциплина (непрерывность).** Не копить работу до конца батча: как только есть
    рабочее состояние — собрать архив + WATCHLOG и отдать через `present_files`. В начале крупной
    задачи — оценка объёма и **вероятность уложиться**; по ходу — переоценка, при росте риска
    немедленный чекпоинт. Готовое отдавать прежде тяжёлого хвоста. Протокол — `docs/session_continuity.md`.
17. **Отчёты — по единому диспетчеру** `docs/reports/reporting_policy.md` (триггер → тип → шаблон →
    папка → реестр). Конвенция имён всего — `docs/naming_convention.md`.

---

## §6. Стабильная библиотека (читать из архива `/docs`; НЕ дублировать сюда)

| Файл | Зачем |
|---|---|
| `docs/ROADMAP.md` | **Направление: вехи 4-9, все задачи** (первоисточник приоритетов). |
| `docs/sandbox_runbook.md` | Грабли песочницы + готовые команды (bootstrap, тесты, E2E, PG). |
| `docs/RELEASES.md` | История развития по эпохам (v1→v4). |
| `docs/reports/adr/adr_001_frontend_stack.md` | Обоснование React+TS (веха 8). |
| `docs/reports/adr/adr_002_datetime_storage.md` | Обоснование naive-UTC (вариант A) и отказа от aware-миграции (B). |
| `docs/reports/adr/adr_template.md` | Шаблон для фиксации архитектурных решений. |
| `docs/math_model_v3_0_0.md` | **Каноническая** мат-модель (источник истины по параметрам). |
| `docs/algorithm_stack.md` | Как мат-модель работает по шагам в коде (9-шаговый конвейер). |
| `docs/diagrams.md` | Mermaid-диаграммы (архитектура, ER, конвейер ядра, потоки). |
| `docs/ui_visual_design_brief.md` | Эстетическое ТЗ фронта (веха 8). |
| `docs/engineering_practices.md`, `docs/CONTRIBUTING.md` | Инженерные решения + процесс (TDD, гейты, DoD). |
| `docs/reports/merges/merge_and_fork_guide.md` | **Как сливать/форкать разошедшиеся линии одной версии между аккаунтами** (процедура, гейты, антипаттерны; раскрывает §5 #15). |
| `docs/reports/merges/merge_manifest_v4_27_0.md` | **Прикладная карта линии v4.27.0 для слияния** (surface, зоны конфликта, инварианты I1–I6, чек-лист) — инстанс гайда под этот батч. |
| `docs/test_run_optimization.md` | **Что гонять и когда:** узкий цикл vs полный fast — как не ждать ~9 мин на каждой правке. Механика полного — runbook §7. |
| `docs/ui_ux_design_standard.md` | Полная спецификация UI/UX (токены, состояния, a11y). |
| `SECURITY.md` (корень) | Единый security-док: меры (ссылки на код), что осталось, домены ревью, план реагирования, тезисы. |
| `docs/cybersecurity_finpilot_checklist.md` | Прикладной чек-лист ИБ (что закладывать, приоритеты). |
| `docs/GLOSSARY.md` | Термины (Rt, Lt, Dt, BLR, Sn…). |
| `docs/QA.md`, `docs/reference_profiles.md`, `docs/reports/bug_report_template.md` | QA-уровни, эталоны, формат багов. |
| `docs/reports/incidents_summary.md`, `docs/reports/investigations_summary.md`, `docs/pitfalls.md` | Реестры (инциденты / расследования / частые ошибки). |
| `docs/reports/report_types.md`, `docs/reports/incident_postmortem_guide.md`, `docs/reports/investigation_report_guide.md` | Роутер типов разбора + шаблоны. |
| `docs/documentation_methodology.md` | Общая теория (blameless, RCA, runbook, ADR/RFC, SLO). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
| `docs/legal_questions_for_lawyer.md` | Вопросы юристу (веха 8). |
