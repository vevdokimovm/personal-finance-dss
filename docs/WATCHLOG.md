# FINPILOT — Вахтенный журнал (быстрый снимок «где мы сейчас»)

> **Что это и зачем.** Снимок-контекст для мгновенного входа в работу. Василий ведёт продукт на
> **4 аккаунтах Claude (V, J, M, S)** и в нескольких чатах внутри проекта; токены кончаются —
> вахту принимает другой аккаунт/чат. Этот журнал даёт принимающему быструю сводку: **где стоим,
> что сделано, что дальше, на какие грабли не наступать** — чтобы поднять картину с ОДНОГО архива,
> не читая 20k строк кода. (Метафора в тему бренда — штурман у штурвала.)
>
> **Принцип тонкости.** Журнал НЕ дублирует историю. Полная история версий — в `CHANGELOG.md`
> (каждый патч) и `docs/RELEASES.md` (эпохи). Направление и задачи — в `docs/ROADMAP.md`. Здесь
> только выжимка + ссылки. Держим коротким: §3 — последние ~10 версий, старое выкидываем (оно в
> CHANGELOG).

> ## [!] Журнал ведётся НЕПРЕРЫВНО, по ходу работы
> Обновляется СРАЗУ по ходу (сделал подшаг → §0; нашёл грабли/факт → §4), а не «в конце сессии» —
> токены могут кончиться в середине батча. В любой момент журнал обязан отражать реальную текущую
> точку.

> **Как пользоваться (Василий).** Новому чату/аккаунту: этот файл (или его текст) + последний архив
> → «продолжай». Копия в чат — с версией в имени: `WATCHLOG finpilot_vX_Y_Z.md`. Архивная копия —
> `docs/WATCHLOG.md` без версии.

> ## ВЕРСИЯ КОДА: `v4.19.0` · ВЕХА 4 (бэкенд) · вахту описывает аккаунт J
> Если архив другой версии — сверь `app/config.py`.

---

## §0. RESUME POINT — отсюда продолжаем

- **Версия:** `v4.19.0`. **Веха 4** (завершение бэкенда + техдолг) по `docs/ROADMAP.md`.
- **Состояние:** бэкенд зрелый, гейты зелёные (776 fast-тестов, покрытие >90%, PG-матрица до 0022).
  Весь P0-P3 backend закрыт (household-оверлей, A/B, экспорт, история, категоризация, импорт-трек).
- **Фаза планирования направления ЗАВЕРШЕНА** (релиз v4.18.2, docs-only). Карта версий (вехи 4-9) в
  `docs/ROADMAP.md`; фронт-стек React+TS в `docs/adr_001_frontend_stack.md`; вопросы юристу в
  `legal_questions_for_lawyer.md`. ROADMAP размечен чекбоксами.
- **Веха 4 СТАРТОВАЛА. Закрыто: P2.3 in-app уведомления (бэк)** — v4.19.0. Модель Notification
  (миграция 0023), CRUD, эндпоинты feed/unread-count/{id}/read/read-all, хук в email-рассылку,
  14 тестов. Снят блокер для колокольчика (веха 6).
- **СЛЕДУЮЩИЙ ШАГ — задача 2 вехи 4: `household_id` на `scenarios` и `plan_snapshots`** (хвост P3.7).
  Колонка есть на 5 доменных таблицах, нет на этих двух → семейный план не виден семье. Миграция 0024
  + проброс household_id при создании снапшота/сценария (производные пишутся внутренними механизмами).
  Детали — `docs/ROADMAP.md` §4.1. По TDD.
- **Порядок вехи 4 (бэк):** ~~P2.3 уведомления~~ → household_id на scenarios/plan_snapshots →
  referral rewards (MVP) → Telegram-каркас → монетизация-каркас → баг ставки ЦБ → баг писем →
  техдолг (mypy/flake8 + utcnow→now(UTC) + ExpenseRecord+date).

**Ожидается от Василия:** Telegram-токен от @BotFather (для Telegram-каркаса); список банков/справочник
(веха 5); бизнес-материалы, лого-эталоны, результаты опросника (веха 5).

---

## §1. Протокол вахты + аккаунты

**Аккаунты:** Василий работает на 4 аккаунтах Claude — **V, J, M, S** — несущих вахту по очереди
через этот журнал (плюс несколько чатов внутри одного проекта). Текущую вахту описывает **J**.
Имена нужны только если Василий уточняет «акк V/J/M/S» в разборе; на работу не влияют — журнал
самодостаточен для любого.

**Заступая на вахту:**
1. Прочитай этот файл + `docs/ROADMAP.md` (направление) + `docs/sandbox_runbook.md` (грабли
   песочницы — НЕ диагностируй заново).
2. Распакуй архив, сверь версию в `app/config.py` с §0.
3. Подними venv (`python3 -m venv .venv` + `pip install -r requirements.txt -r requirements-dev.txt`);
   для E2E — `playwright install chromium`.
4. Отчитайся Василию коротко (3-4 строки): версия, текущая веха, следующий шаг.
5. НЕ переделывай закрытое, НЕ уходи с порядка ROADMAP без команды.

**Неся вахту:** обновляй §0/§4 по ходу; разборы оформляй САМ по триггеру (инцидент/расследование/
pitfall — маппинг в §5); в конце батча отдавай журнал ОТДЕЛЬНЫМ файлом через `present_files` (имя
с версией) + копию в архив (`docs/WATCHLOG.md`).

---

## §2. Направление (выжимка ROADMAP)

Полностью — `docs/ROADMAP.md`. **Вехи проекта нумеруются как мажоры (v4-v9), это СОЗНАТЕЛЬНО не
классический SemVer** (мажор = крупный этап, не слом контракта). Внутри вехи: фича=MINOR, фикс=PATCH.

| Веха | Версия | Тема |
|---|---|---|
| **4** | `v4.x.x` | **Завершение бэкенда + техдолг** ← мы здесь |
| 5 | `v5.0.0` | Организационное (репозитории, доки, бизнес, исследования, методички, банки) |
| 6 | `v6.0.0` | Фронтенд (React+TS, редизайн, вынос всех возможностей бэка) |
| 7 | `v7.0.0` | Тестирование (мат-модель 1000+ кейсов + независимый эксперт ВНЕ проекта + импорт) |
| 8 | `v8.0.0` | Юридическое (документы + соответствие законам) |
| 9 | `v9.0.0` | Деплой на сервер |

Порядок строгий: бэк → организация → фронт → тестирование → юр → деплой.

---

## §3. Последние версии (новое сверху; полная история — CHANGELOG.md)

- **v4.19.0** — P2.3 in-app уведомления (бэк, MINOR). Модель Notification + миграция 0023 + CRUD +
  эндпоинты feed/unread-count/read/read-all + хук в email-рассылку. 14 тестов, fast 790 passed.
  Первая кодовая задача вехи 4.
- **v4.18.2** — новый роадмап (вехи 4-9) + тонкий WATCHLOG (954→197) + ADR фронт-стека (React+TS) +
  вопросы юристу. Docs-only, фаза планирования направления. Правило «вехи=мажоры, не SemVer»,
  чекбоксы в ROADMAP, техдолг разблокирован.
- **v4.18.1** — слияние двух параллельных линий (P3.7 household + a11y/WCAG AA + тест-инфра). PATCH,
  консолидация. Гейты на слитом дереве: fast SQLite/PG 776 passed, миграция до 0022, FK ON DELETE
  SET NULL подтверждён. Зафиксирован PIT-002 (override `DATABASE_URL=:memory:` ломает cross-connection
  scope-тесты — среда, не баг).
- **v4.18.0** — P3.7 household-оверлей (backend, MINOR). Модели Household/Membership/Invite + nullable
  `household_id` FK на 7 таблицах (миграция 0022), `_owner_filter` + household-ось, CRUD + `/api/households`.
  Решение: аддитивный оверлей, не workspace-переезд (NULL=приватно — дефолт). Покрытие нового кода 96-100%.
- **v4.17.2** — гигиена репо: полноценный `.gitignore`, правило §6 #16 (слияние разошедшихся линий),
  разбор форка. PATCH, код не тронут.
- **v4.17.1** — сведение параллельной a11y-ветки (палитра WCAG AA + тест-инфра) в канон. PATCH-мёрж.
  color-contrast=0, fast 749 passed, a11y-axe 28 passed.
- **v4.17.0** — P3.5 A/B-тестирование (прод-уровень, MINOR): таблицы experiments + assignments
  (миграция 0021), детерминированное назначение с фиксацией, админ-CRUD, variant-эндпоинт. Без ML.
- **v4.16.35** — реестр pitfalls + правила §14/§15 (авто-репорты, CI/CD как ценность). Docs/process.
- **v4.16.34** — P2.7 обучение категоризации на правках (детерминированно, без ML): `user_category_rules`
  (миграция 0020), `classify_with_rules`, эндпоинты переназначения, ретроактив.
- **v4.16.33** — `docs/RELEASES.md` (курируемая история по эпохам) + единый формат лейблов. Docs.
- **v4.16.32** — `docs/sandbox_runbook.md` в репо + версия в имени отдельного WATCHLOG. Docs/process.
- **v4.16.31** — README переписан + легенда карты тестов. Docs (вахта J).

> До v4.16.31 — см. `CHANGELOG.md` (полная история, 71+ версия) и `docs/RELEASES.md` (эпохи v1→v4).

---

## §4. Заметки и находки (не переоткрывать)

**Текущая фаза планирования (что согласовано с Василием):**
- Карта версий 4-9 подтверждена; правило «вехи ≠ SemVer» — осознанное решение Василия.
- Фронт-стек React+TS зафиксирован с обоснованием — `docs/adr_001_frontend_stack.md`.
- Техдолг (mypy/flake8, utcnow, ExpenseRecord) РАЗБЛОКИРОВАН: работаем с конечной версией
  последовательно, параллельных правок нет → конфликтов мёржа не будет.
- Принцип заглушек вехи 4: MVP/каркас для того, что в роадмапе и точно нужно фронту; спекулятивное
  не выдумываем (YAGNI).

**Мат-модель — канон `docs/math_model_v3_0_0.md`** (источник истины — код): 66 альтернатив, шаг 10%,
stock-based `Lt = B_liq/E` (месяцы), L_min=0 (фильтр выключен), MC-интервал 80%, инварианты Rt≥0 и
ПДН≤0.40. Если всплывёт спека с 21 альтернативой / шаг 20% / flow-based Lt / 95% — это УСТАРЕВШАЯ
v2.0.2, не источник истины.

**Известные баги к разбору в вехе 4** (давние заметки Василия): (1) ставка ЦБ всегда ставит дефолт —
реального fetch нет (вероятно cbr.ru 403 → зеркало `cbr-xml-daily.ru`); (2) письма «отправлены», но
на почте пусто (SMTP no-op в dev маскирует). Диагностика до корня, не угадывать.

**Открытые вопросы для вехи 7** (тестирование): график прогноза — у Василия ощущение что баг (всегда
прямая линия от константы-зарплаты); нужен тест-кейс где НЕ прямая. Совет при `Rt<0` — исследовать
(реальный кейс Василия с кредиткой). Шаг 5% вместо 10% — замерить скорость.

**PostgreSQL поднимается в песочнице** (не спихивать на разработчика): `apt install postgresql` БЕЗ
`apt-get update` (nodesource 403; Ubuntu-репы рабочие) → `pg_ctlcluster 16 main start` → роль/БД через
`su postgres -c psql` → `DATABASE_URL=postgresql+psycopg://finpilot:finpilot@localhost:5432/finpilot_test`.
PG-долга нет (миграции 0018-0022 проверены на PG 16).

**Грабли песочницы** (детали — `docs/sandbox_runbook.md`): полный одновременный прогон всех тестов
рубится лимитом инструмента (~30с → returncode -1) → гонять пофайловыми группами; итоговая строка
pytest не флашится в файл → использовать `--junitxml` + парсить XML + exit code; фоновые процессы не
флашат лог надёжно; многострочные bash с `\` ломаются; браузеры firefox/webkit недоступны (Azure CDN),
chromium есть.

---

## §5. Жёсткие правила (чек-лист «не дрейфовать»)

1. **TDD** — тест → красный → реализация → зелёный, реально прогонять.
2. **`app/core/` не трогать** без согласования (исключение — `spending_advice.py`).
3. **Схемные изменения — на PostgreSQL** (SQLite молчит про FK).
4. **SemVer внутри вехи:** фича=MINOR, фикс=PATCH. Bump `APP_VERSION` + `CHANGELOG.md` каждый батч.
   Вехи (v4-v9) — крупные этапы, не классический SemVer (см. §2).
5. **Без массового реформата** кроме координированных окон (сейчас окно открыто — техдолг закрываем).
6. **ROADMAP — источник приоритетов, строго по порядку.** Не изобретать параллельную нумерацию.
7. **Без эмодзи.** Русская проза + английский код. По-братски, прямо, без канцелярита.
8. **НЕ объявляй невозможным, не проверив.** PostgreSQL/chromium/apt поднимаются здесь. Реально
   внешнее (cbr.ru 403, живой SMTP, деплой) — честно отметить.
9. **Перед «с нуля» — проверь, не сделано ли уже** (бэк часто опережает план).
10. **Вывод тестов — через `--junitxml`** (не `tail` по логу): итоговая строка не флашится надёжно.
    Парсить XML + exit code. (§4, `docs/pitfalls.md`.)
11. **НИКОГДА не писать имена в гите / релизах / публичных артефактах** — официально безымянно.
12. **Тестирование мат-модели — СТРОГО вне проекта** (Opus 4.8, без памяти/инструкций → не biased).
13. **E2E добавляются с unit** для изменений фронта/API; CRUD-флоу обязаны иметь E2E.
14. **Разборы — АВТОМАТИЧЕСКИ по триггеру:** дефект в коде → инцидент (`docs/incidents_summary.md`);
    искали, но дефекта нет → расследование (`docs/investigations_summary.md`); повторная ошибка
    воркфлоу → pitfall (`docs/pitfalls.md`); найденный баг → карточка по `docs/bug_report_template.md`.
15. **Слияние разошедшихся линий → консолидация, не переписывание истории** (метод — §6 #16 старой
    редакции / разбор форка). Профилактика: синкать аккаунты чаще, координировать номера версий.

---

## §6. Стабильная библиотека (читать из архива `/docs`; НЕ дублировать сюда)

| Файл | Зачем |
|---|---|
| `docs/ROADMAP.md` | **Направление: вехи 4-9, все задачи** (первоисточник приоритетов). |
| `docs/sandbox_runbook.md` | Грабли песочницы + готовые команды (bootstrap, тесты, E2E, PG). |
| `docs/RELEASES.md` | История развития по эпохам (v1→v4). |
| `docs/adr_001_frontend_stack.md` | Обоснование React+TS (веха 6). |
| `docs/adr_template.md` | Шаблон для фиксации архитектурных решений. |
| `docs/math_model_v3_0_0.md` | **Каноническая** мат-модель (источник истины по параметрам). |
| `docs/algorithm_stack.md` | Как мат-модель работает по шагам в коде (9-шаговый конвейер). |
| `docs/diagrams.md` | Mermaid-диаграммы (архитектура, ER, конвейер ядра, потоки). |
| `06_ТЗ_на_современный_UI.md` | Эстетическое ТЗ фронта (веха 6). |
| `docs/engineering_practices.md`, `docs/CONTRIBUTING.md` | Инженерные решения + процесс (TDD, гейты, DoD). |
| `docs/ui_ux_design_standard.md` | Полная спецификация UI/UX (токены, состояния, a11y). |
| `docs/security.md` | Срез безопасности по реальному коду. |
| `docs/GLOSSARY.md` | Термины (Rt, Lt, Dt, BLR, Sn…). |
| `docs/QA.md`, `docs/reference_profiles.md`, `docs/bug_report_template.md` | QA-уровни, эталоны, формат багов. |
| `docs/incidents_summary.md`, `docs/investigations_summary.md`, `docs/pitfalls.md` | Реестры (инциденты / расследования / частые ошибки). |
| `docs/report_types.md`, `docs/incident_postmortem_guide.md`, `docs/investigation_report_guide.md` | Роутер типов разбора + шаблоны. |
| `docs/documentation_methodology.md` | Общая теория (blameless, RCA, runbook, ADR/RFC, SLO). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
| `legal_questions_for_lawyer.md` | Вопросы юристу (веха 8). |
