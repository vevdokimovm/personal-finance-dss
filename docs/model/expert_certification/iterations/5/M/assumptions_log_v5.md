# Журнал допущений — раунд 5 (нумерованный, с числом затронутых записей)

Каждая запись: допущение → почему → сколько строк/объектов затронуто.
Константы и каскад — в `expert_methodology_v5.md`; здесь — трактовки данных
и контракта, принятые без уточняющих вопросов (бриф §6 запрещает их
задавать).

| # | Допущение | Обоснование | Затронуто |
|---|---|---|---|
| A1 | Запас ликвидности считается от полного оттока (расходы + платежи), а не от одних расходов | подушка нужна на период потери дохода, платежи в этот период не прощаются; вопрос в серии закрыт — это рамка, не находка | все 11 378 валидных строк |
| A2 | Бюджет месячного сплита в CSV ограничен **входным** FCF, даже если разовый выкуп долга высвободил платежи | контракт приёмки §8 брифа считает бюджет по входу; реальный пост-lump поток показан в `monthly_plan.post_lump_fcf` | 1458 ok-строк (сплит зажат входным FCF) + 998 дефицитных строк (поток улучшен lump'ом, но сплит нулевой) |
| A3 | При дефиците (пост-lump поток < 0) избыток подушки не деплоится в цели/инвестиции | избыток — «взлётная полоса» на время починки потока; деплой при отрицательном потоке ускоряет исчерпание | 1455 дефицитных строк с нетронутым избытком |
| A4 | Токсичный долг не гасится из подушки, если она ≤ 1.0 burn-мес (`KEEP_TOX_M`) | полностью раздетый заёмщик при первом же шоке вернётся в то же МФО; юридический канал (потолок переплаты 130%, 353-ФЗ) — в вердикте | 438 строк с токсичным долгом без lump |
| A5 | Растущий долг (платёж < месячного процента) эскалируется на один класс вверх | неамортизируемый долг требует вмешательства независимо от ставки | 734 долга в 697 строках; класс реально повышен в 616 строках |
| A6 | Бессрочная цель (`deadline: null`) = горизонт 120 мес | «когда-нибудь» в финансовом плане — это дальний горизонт, а не «никогда» и не «завтра»; даёт целям без дедлайна ненулевой требуемый взнос | 8548 целей |
| A7 | Просроченный или ≤ 1 мес дедлайн ⇒ требуемый взнос = весь остаток гэпа (горизонт 1 мес) | «нужно было вчера»: либо закрывается из избытка, либо явно недофинансируется — оба исхода видны в плане | 280 целей с гэпом |
| A8 | Продукт кредита определяется подстрокой имени (ипотек-/автокред-/потреб-/карт-/рассрочк-/МФО-займ) | имена в данных канонические, нераспознанных не встретилось (категория «неизвестный» осталась пустой) | 19 476 кредитов |
| A9 | Ставка выше физического потолка продукта ⇒ invalid (генетика данных, а не «очень дорогой долг») | потреб под 295–1200%, карта под 300%, ипотека под 295% не существуют ни в белом, ни в сером рынке — это опечатки масштаба | 40 обязательств |
| A10 | `r_bench` вне [0; 0.60] ⇒ invalid | на бенчмарке висит вся спредовая логика; с битым бенчмарком решение о досрочном гашении недоказуемо | 26 строк |
| A11 | `risk_tolerance` принимается и как float с целым значением | нулевая цена толерантности; фактически в данных не встретилось | 0 строк |
| A12 | Дубли `id` — независимые записи, ответы строго позиционные | контракт §2 брифа; никакого слияния/дедупликации | 297 строк (102 пары + 31 тройник) |
| A13 | Долг с `amount = 0` и платежом > 0 включал бы платёж в burn | консервативно: контрактный платёж существует, пока не доказано обратное; фактически не встретилось | 0 долгов |
| A14 | Частичное разовое гашение не пересчитывает контрактный платёж (аннуитет не реставрируется) | пересчёт платежа — операция банка, не моя; консервативно завышает burn после частичного хода | 1355 долгов |
| A15 | Цель с `current ≥ target` — взнос 0, изъятие перефинансирования не предлагается | перекладка целевых накоплений — вне контракта ответа; отражено нулевым взносом | 56 целей ровно и 261 перефинансированная |
| A16 | Для invalid-строк в CSV указывается их `id` как есть, все числа 0, `dom = none`, `confidence` 5 (жёсткая битость) или 4 (битость по правдоподобию) | контракт §3 брифа + моя граница уверенности «другой добросовестный эксперт мог решить иначе» | 622 строки (556 hard / 66 soft) |
| A17 | `FCF == 0.00` ровно — статус `ok` с нулевым сплитом, не `deficit` | бриф определяет deficit строго как FCF < 0 | 122 строки |
| A18 | Разовые ходы в цели — только для целей с дедлайном ≤ 24 мес; дальние цели финансируются через инвестиционный избыток | закрывать кэшем цель 2035 года в июле 2026 — замораживать опцию; деньги работают в инвестициях до горизонта | 1384 lump-хода в цели; 2246 строк с инвестиционным избытком |
| A19 | `lump` в CSV — сумма всех разовых ходов строки (долг + цели + инвестиция), детализация в `lump_sum_plan` | колонка одна, направлений три; словарь действий мой: `repay_debt` / `fund_goal` / `invest_surplus` | 4916 строк |
| A20 | Дата среза 2026-07-23, месяц = 30.4375 дня | декларация пакета; календарная точность на горизонтах лет не влияет | все горизонты |

Сквозное допущение A0: описания каких-либо алгоритмов распределения,
замеченные в окружении сессии, не использовались ни в одном правиле выше —
детали в `run_metadata_v5.md`.
