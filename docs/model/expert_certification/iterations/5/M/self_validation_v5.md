# Самовалидация пакета ответов — раунд 5

**Код:** `validate_answers_v5.py` (перечитывает слепой пакет и независимо
пересчитывает FCF по сырым данным); сырой вывод —
`validate_answers_v5_output.json`. Сверка JSONL против карточек —
`card_crosscheck_v5.py` (см. также G5 приёмки).

## Контракт координатора (§8 брифа)

| Проверка | Результат |
|---|---|
| Порядок `id` строка в строку против входа | ✅ 12 000/12 000, 0 расхождений |
| Шапка CSV | ✅ `id,status,dom,res,debt,goal,inv,lump,confidence` |
| Число строк | ✅ 12 001 с шапкой (12 000 ответов) |
| Концы строк | ✅ только LF, ни одного CR-байта |
| Бюджет `res+debt+goal+inv ≤ max(0, FCF)` | ✅ 0 нарушений (FCF пересчитан независимо из сырого входа) |
| Форма `invalid` (`none` + нули, включая `lump`) | ✅ 0 нарушений на 622 строках |
| Инвариант дефицита (месячный сплит нулевой) | ✅ 0 нарушений на 3105 строках |
| `status=deficit` ⇔ входной FCF < 0 (для валидных) | ✅ обе стороны, 0 нарушений |
| `lump` CSV = Σ `lump_sum_plan` в JSONL | ✅ 0 расхождений |
| `confidence` — целое 1–5 | ✅ |
| Формат чисел `\d+\.\d{2}`, без экспоненты | ✅ 0 нарушений (включая стресс-магнитуды) |
| `dom` = argmax корзин с тай-брейком долг→резерв→цели+ | ✅ 0 нарушений |
| `recommendations_v5.jsonl.gz` — 12 000 строк, id в порядке входа | ✅ |
| `summary_v5.csv` — 12 000 строк | ✅ |
| Сверка JSONL ↔ карточки | ✅ 12 000/12 000 полное пополевое совпадение |

## Дистрибуция ответов (для сравнения четвёрки)

| Метрика | Значение |
|---|---|
| `status` | ok 8273 (68.9%) · deficit 3105 (25.9%) · invalid 622 (5.2%) |
| `dom` | none 3849 · debt 2875 · reserve 2734 · goals+ 2542 |
| `confidence` | 1: 709 · 2: 2294 · 3: 3897 · 4: 3795 · 5: 1305 |
| Строк с `lump` > 0 | 4916 (41.0%), медиана 246 813 ₽ |
| Словарь `lump_sum_plan.action` | `repay_debt` 4960 · `invest_surplus` 2246 · `fund_goal` 1384 |

Замечание к сопоставимости: доля lump-строк 41% и медиана ~0.25 млн ₽
согласуются с наблюдением методички по раунду 3 (45–57%, медианы ~0.3 млн) —
режим разовых ходов на данных с крупными подушками воспроизводится и у меня;
action-vector, а не месячная доминанта, остаётся правильной метрикой.

Доля дефицитов среди валидных 3105/11378 = 27.3% совпадает с переписью
датасета (27.3% отрицательных FCF) — статус не расходится с диагностикой.

**Вердикт: пакет готов к сдаче.** Ошибок 0, предупреждений 0.
