# Самопроверка ответов — раунд 4

Программная (`validate_answers_v4.py` → `aux/validation.json`,
`card_crosscheck_v4.py` → `aux/crosscheck.json`), после финальной генерации
всех трёх файлов ответа. **Итог: 0 нарушений по всем проверкам.**

## 1. Формат `expert_allocations_v4.csv`

| Проверка | Нарушений |
|---|---|
| Шапка ровно `id,status,dom,res,debt,goal,inv,lump,confidence` | 0 |
| Ровно 12 000 строк данных | 0 |
| Нет CRLF (только LF) | 0 |
| Нет пробелов в файле | 0 |
| `status` ∈ {ok, deficit, invalid} | 0 |
| `dom` ∈ {debt, reserve, goals+, none} | 0 |
| `res/debt/goal/inv/lump` — целые неотрицательные, без разделителей | 0 |
| `confidence` — целое 1..5 | 0 |

## 2. Позиционный контракт

`id` каждой строки ответа сверен с `id` соответствующей входной строки
(части 1→4, meta-строки пропущены): **12 000/12 000 совпадений, 0 расхождений**.
Дубли id (260 строк) отвечены построчно, без мержа.

## 3. Контроль сохранения сумм (бриф §4.4)

- `res + debt + goal + inv ≤ max(0, ⌊свободный поток⌋)` — поток пересчитан
  валидатором заново из сырого входа: **0 нарушений** на 11 400 валидных строк.
- `lump ≤ bliq` (доступная ликвидность): **0 нарушений**.
- `invalid` ⇒ все суммы 0, `dom=none`: **0 нарушений** на 600 строк.
- `deficit` ⇒ месячные бакеты нулевые: **0 нарушений** на 4 066 строк.
- `dom` = максимум бакетов (тай-брейк debt→reserve→goals+): **0 нарушений**.

## 4. Согласованность трёх файлов ответа

- `summary_v4.csv`: 12 000 строк; id/status/dom/бакеты совпадают с
  `expert_allocations_v4.csv` построчно: **0 расхождений**.
- `recommendations_v4.jsonl.gz`: 12 000 строк; id позиционно совпадают;
  `totals.monthly_total = res+debt+goal+inv` и `totals.lump_total = lump`
  совпадают с CSV: **0 расхождений**; пустых вердиктов: **0**.

## 5. Сверка JSONL ↔ карточки (бриф §4.5)

`card_crosscheck_v4.py`, детерминированная выборка: 12 случайных позиций
(seed 20260718) + первая + последняя = **14 портретов, 368 сравнений полей**
(id, доход, расходы, подушка, риск, бенчмарк, все кредиты: имя/остаток/ставка/
платёж, все цели: имя/target/current/дедлайн). Допуски форматные: деньги
±0.005 ₽, ставки ±0.06 п.п. (карточки округляют до 0.1 п.п.).
**Расхождений данных: 0** — карточки и JSONL описывают один и тот же датасет.

Примечание честности: первый прогон сверки показал 11 «расхождений»
`goal.deadline`, все оказались багом моего парсера карточек (регэксп захватывал
хвостовую точку с запятой «2030-01-05;»). После фикса парсера — 0 расхождений;
данные были корректны с самого начала. Это зафиксировано как проверка самого
валидатора.

## 6. Независимый пересчёт (выборка 40)

Для 38 валидных портретов выборки §9 решение движка пересчитано отдельной
прямолинейной реализацией методологии (`paper_recompute`): совпадение по всем
семи полям (status, dom, res, debt, goal, inv, lump) — **38/38, 0 расхождений**
(2 invalid — пересчёт не применим, класс дефекта подтверждён вручную).
