# Самовалидация — раунд 4

Все проверки программные, поверх готовых артефактов комплекта. Валидатор: `validate_answers_v4.py` (полный прогон), кросс-чек: `card_crosscheck_v4.py`, референс-пересчёт: независимая реализация в рамках §9.

## 1. Формат и контракт CSV (`expert_allocations_v4.csv`)

Строк данных: 12 000 из 12 000; заголовок — ровно `id,status,dom,res,debt,goal,inv,lump,confidence`. CRLF: 0, лишние пробелы: 0. Позиционный контракт: id каждой строки ответа совпадает с id соответствующей строки входа (дубли не мержены) — несовпадений 0. Enum-нарушений 0: status ∈ {ok, deficit, invalid}, dom ∈ {debt, reserve, goals+, none}, confidence ∈ {1..5} целые, суммы — целые неотрицательные.

## 2. Бюджетный контроль

Нарушений `res + debt + goal + inv ≤ max(0, FCF)`: **0 из 12 000**. Нарушений `lump ≤ bliq`: **0**. Форма invalid-строк (`invalid,none,0,0,0,0,0,5`): нарушений 0. Форма deficit-строк (месячные бакеты нулевые, lump допустим): нарушений 0.

## 3. Согласованность трёх файлов ответа

`summary_v4.csv`: 12 000 строк, id и статусы совпадают с основным CSV попозиционно. `recommendations_v4.jsonl.gz`: 12 000 JSON-строк, id попозиционно совпадают, totals в каждой записи равны бакетам основного CSV. Расхождений 0.

## 4. Сверка входа с карточками

`card_crosscheck_v4.py`: 12 000 карточек = 12 000 строк JSONL; программная сверка 6 позиций (1009, 4176, 5375, 5959, 6205 + дефектная 8): 6/6 полных совпадений всех полей, включая маркировку дефекта в карточке `!..!`.

## 5. Независимый референс-пересчёт (§9)

40 портретов (позиции 0, 300, …, 11700) пересчитаны отдельной реализацией «на бумаге», написанной без переиспользования внутренностей движка. Первый прогон выявил расхождение 5/40 — ровно 1 ₽ в `lump` (порядок округления: движок floor'ит каждый разовый ход, референс округлял сумму). Канон зафиксирован в методологии: каждый ход — целые рубли, `lump` — сумма целых; CSV движка канону соответствует и не правился. После выравнивания референса: **40/40 совпадений** по всем полям (`status, dom, res, debt, goal, inv, lump`). Содержательных расхождений (класс долга, состав ходов, доминанта, статус) — 0 на всех 40.

## 6. Стиль кода

flake8 по четырём скриптам комплекта (`engine_v4.py`, `dataset_stats_v4.py`, `validate_answers_v4.py`, `card_crosscheck_v4.py`): 0 замечаний.

## 7. Детерминизм

Повторный прогон `engine_v4.run()` на тех же входных файлах даёт байт-в-байт идентичный `expert_allocations_v4.csv` (случайности в пайплайне нет, дата среза фиксирована константой 2026-07-18).

## Итог

Все внутренние гейты пройдены: полнота 12 000/12 000, нарушений формата 0, нарушений бюджета 0, согласованность трёх файлов ответа полная, кросс-чек карточек 6/6, референс-сверка 40/40, flake8 = 0.
