# Метаданные прогона — раунд 4

**Эксперт:** Claude (Anthropic), режим независимого исследователя-эксперта. Методология и все константы зафиксированы в `expert_methodology_v4.md` до первого прохода по данным.

**Инструменты:** Python 3.12.3; numpy 2.4.4, scipy 1.17.1, pandas 3.0.2 (scipy/pandas — только в аудите датасета; движок решений — чистая стандартная библиотека). Дата среза расчётов: 2026-07-18 (по брифу). Старт обработки: 2026-07-18T15:44:57Z; финализация комплекта: 2026-07-22 (сессия возобновлялась, данные и артефакты между сеансами не менялись — контрольные прогоны валидатора идентичны).

**Полнота:** обработаны программно все 12 000 строк входа (4 файла × 3000, служебная meta-строка part1 пропущена по правилу брифа). Ответных строк 12 000, позиционный контракт подтверждён (id_mismatch = 0). Ручных вмешательств в ответы: 0 — ни одна строка CSV не правилась руками; вся логика в `engine_v4.py`, детерминизм полный (повторный прогон даёт байт-в-байт тот же CSV).

**Комплект и вспомогательный код:** решающий и проверочный код комплекта — `engine_v4.py`, `dataset_stats_v4.py`, `validate_answers_v4.py`, `card_crosscheck_v4.py`. Поверх них выполнялись одноразовые скрипты-обвязки (прогон чувствительности через `sensitivity_variants()`, генерация файла 40 обоснований, независимая референс-сверка «на бумаге») — в комплект не входят по правилу §5 «ровно эти имена», их результаты полностью отражены в `sensitivity_v4.md` и `expert_opinions_sample_v4.md`.

**Данные:** вход из `/mnt/user-data/uploads` (4 × jsonl.gz + карточки + методичка + бриф), скопирован в рабочую директорию без модификаций. Сверка JSONL ↔ карточки: 6 позиций программно, 6/6 полных совпадений (`card_crosscheck_v4.py`).

**Известные ограничения прогона:** классификация продуктов в аудите — по имени кредита (поле name), восстановление сроков — обратной аннуитетной формулой с допуском; KS-тест лог-нормальности использует параметры, оценённые по выборке (Lilliefors-смещение, p-value оптимистичен — на вывод не влияет, гипотеза отвергается с колоссальным запасом).
