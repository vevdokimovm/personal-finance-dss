# Onboarding — вход в проект за один час

> Для нового человека или аккаунта Claude. Одна страница: что это, как поднять, где канон,
> какие правила нельзя нарушать. Всё остальное — по ссылкам.

## Что это

**FINPILOT** — алгоритмическая СППР персональных финансов (рынок РФ). Двойная роль: бакалаврская
ВКР (НИТУ МИСИС) + production SaaS, запуск — конец осени 2026. Планка качества — уровень топовых
финтехов РФ. Бэкенд зрелый (веха 4 закрыта), идёт веха 5 (организационное), впереди фронт (6),
тестирование модели (7), юр (8), деплой (9).

## Поднять локально (10 минут)

> **[!] Проект живёт в `.venv`, не в системном Python.** На новой машине системный `python3`
> может резолвиться в устаревшую версию (найдено на практике: macOS-хост дал 3.10, а
> `numpy==2.4.4` из `requirements-dev.txt` требует ≥3.11 — `pip install` падал только на этом
> пакете, всё остальное молча ставилось глобально в системный Python). Если `.venv` отсутствует,
> ставь его **явно свежим** интерпретатором (`python3.13 -m venv .venv`, не первым `python3` из
> PATH) — не полагайся на то, что «`pip install` без ошибок» значит «окружение верное».
> `.claude/hooks/tests-gate.sh` при отсутствующем `.venv` тихо откатывается на системный
> `python3` — это защита от падения хука, а не санкционированный способ гонять тесты проекта.

1. `python3.13 -m venv .venv && source .venv/bin/activate` (версия — см. предупреждение выше;
   `python3` без номера может оказаться старше, чем требуют зависимости)
2. `pip install -r requirements.txt -r requirements-dev.txt`
3. `uvicorn app.main:app --reload` → http://127.0.0.1:8000 (SQLite из коробки; PG — `DATABASE_URL`).
4. Тесты: `pytest -q` (fast-suite; полный контур — `docs/QA.md`). E2E: `playwright install chromium`.

## Канон и карта документов

| Вопрос | Где ответ |
|---|---|
| Где мы сейчас и что дальше | `docs/WATCHLOG.md` (§0) → `docs/ROADMAP.md` (механизм в шапке) |
| Математика продукта | `docs/math_model_v3_0_0.md` — ЕДИНСТВЕННЫЙ канон (v2.x мертва) |
| Как разрабатываем | `knowledge/guides/development_process_methodology.md` + `docs/engineering_practices.md` |
| Тестирование и гейты | `docs/QA.md` (TDD, 90% покрытия, SQLite+PG матрица, timewarp) |
| Безопасность | корневой `SECURITY.md` |
| Грабли песочницы Claude | `docs/sandbox_runbook.md` — НЕ передиагностируй известное |
| Термины | `docs/glossary.md` |
| API-контракт | `docs/api_contract.md` + снапшот `docs/api/openapi.json` |
| Инциденты/расследования | `docs/reports/` (диспетчер — `docs/reports/reporting_policy.md`) |
| История | `CHANGELOG.md` (каждый патч) · `docs/RELEASES.md` (эпохи) |

## Правила, которые не обсуждаются

1. **TDD**: тест → реализация → green. Гейт покрытия 90% жёсткий; линтеры информативны, тесты блокируют.
2. **Матрица SQLite + PostgreSQL** для любых изменений схемы (SQLite молчит про FK).
3. **`app/core/` не трогать без согласования**; массовый реформат запрещён (мёрж 4 аккаунтов).
4. **SemVer внутри вехи**: фича=MINOR, фикс=PATCH; каждый батч = бамп `APP_VERSION` + `CHANGELOG.md`.
5. **Протокол непрерывности** (`docs/session_continuity.md`): архив рано и часто, WATCHLOG по ходу.
6. **Русская проза, английский код**, без эмодзи, безымянность в публичных артефактах.

## Первый час нового аккаунта Claude

Прочитай `docs/WATCHLOG.md` целиком → сверь версию `app/config.py` → прогони fast-suite →
возьми задачу по механизму приоритизации из шапки `docs/ROADMAP.md`. Не переделывай закрытое.
