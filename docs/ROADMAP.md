# FINPILOT — ROADMAP (вехи 4-9)

> **Что это.** Единственный источник направления движения продукта. Заменяет старый роадмап
> `03_Будущий_роадмап.md` (тот был под структуру P0-P3 до выхода на прод-планку; большая часть
> его пунктов закрыта — см. «Что уже сделано»). Здесь — крупные вехи проекта от завершения
> бэкенда до публичного запуска, с детализацией задач каждой вехи.
>
> **Как пользоваться.** WATCHLOG (`docs/WATCHLOG.md`) держит короткую выжимку «где в роадмапе
> стоим» и ссылается сюда. Подробности по конкретной версии — в `CHANGELOG.md` (каждый патч) и
> `docs/RELEASES.md` (вехи по эпохам). Этот файл — про БУДУЩЕЕ; история — в тех двух.
>
> **Чекбоксы.** `[x]` — сделано, `[ ]` — осталось. Отмечать СРАЗУ при закрытии задачи (как
> WATCHLOG-дисциплина), чтобы статус был наглядным для любого из 4 аккаунтов.
>
> **Текущая точка** (версия, веха, чья вахта) — в шапке `docs/WATCHLOG.md`. Здесь её НЕ дублируем:
> дублированный факт протухает (эта строка успела отстать на веху — урок ревизии v5.11.0).

---

## Правило версий (важно — читать первым)

**Вехи проекта нумеруются как мажорные версии (v4 → v9), и это СОЗНАТЕЛЬНО НЕ классический
SemVer.** В строгом SemVer `MAJOR` = слом обратной совместимости контракта. Здесь мажор =
**крупный этап проекта** (бэкенд / организация / фронт / тестирование / юр / деплой), даже если
этап контракт не ломает. Так нагляднее для одиночной разработки на 4 аккаунтах. Решение принято
Василием осознанно.

Внутри вехи — обычная механика: фича = MINOR (`v4.19.0`), фикс/мелочь = PATCH (`v4.18.2`).
Каждый батч: bump `APP_VERSION` в `app/config.py` + запись в `CHANGELOG.md`.

| Веха | Версия | Тема | Статус |
|---|---|---|---|
| 4 | `v4.x.x` | **Завершение бэкенда + техдолг** | В работе |
| 5 | `v5.0.0` | Организационное (репозитории, доки, бизнес, исследования, методички, банки, наука) | **Завершена** (v5.18.0) |
| 6 | `v6.0.0` | Тестирование (мат-модель 1000+ кейсов + независимый эксперт + импорт + банки) | **Завершена** (v6.26.1): пять раундов сертификации, канон v3.5.0, оценка 85.7/100; дальнейшие раунды на паузе по исчерпанию информативности метода. Парсер — ADR-010: метрология вместо сертификации |
| 7 | `v7.0.0` | Юридическое (документы + соответствие законам) | **Завершена** (v7.0.0): пакет из шести документов + UI-тексты, карта требований L1–L10, реестр обязательств владельца O1–O7, гейт запуска. Исполнение требований — вехи 8–9, заключение юриста — внешнее и параллельное |
| 8 | `v8.0.0` | Фронтенд (редизайн + React+TS + вынос всех возможностей бэка) | **Следующая** |
| 9 | `v9.0.0` | Деплой на сервер | Запланировано |

Порядок строгий: бэк → организация → **тестирование → юр → фронт** → деплой.
_(Пересмотр 2026-07-02, приказ владельца: математика замораживается и проверяется до фронта;
юр-тексты — согласия, оферта, политика, дисклеймеры — это контент экранов, фронт вшивает их
сразу и не переделывается; юр-ответы внешние и медленные — запускаем раньше. Прежний порядок —
`docs/roadmap_archive/`.)_

---

## Механизм живого роадмапа — выбор следующей задачи БЕЗ владельца

> Любой аккаунт (V/J/M/S) детерминированно определяет следующую задачу сам. Теория и обоснование —
> `knowledge/guides/roadmap_methodology.md` (Часть II, §15). Прямое указание владельца в чате всегда
> приоритетнее этого механизма.

**Алгоритм (сверху вниз, первое совпадение):**
1. Явный приказ владельца в текущем чате.
2. Указатель **«СЛЕДУЮЩАЯ ЗАДАЧА»** ниже — если не противоречит чекбоксам (сверь перед стартом).
3. Активный спец-поток (сейчас — нет; последним был §5.8, веха 5 закрыта): первый открытый `[ ]` по порядку.
4. Строгий порядок вех 4→9: первая веха с открытыми пунктами → первая открытая подсекция сверху
   вниз → первый открытый чекбокс.
5. Пункт заблокирован внешним (владелец/юрист/сеть)? Пометь `(BLOCKED: причина)` и возьми следующий.
6. **Закрытие вехи** — только после блока «Предложения Claude» владельцу: что ещё стоило бы
   сделать в рамках вехи, чего владелец не увидел/не назвал (правило 2026-07-02, детали —
   `knowledge/guides/roadmap_methodology.md`). Без блока веха не помечается «Завершена».

**Тай-брейк, когда порядок неочевиден:**
1. Хвосты раньше нового: частично закрытый пункт (с под-чекбоксами) добивается первым.
2. Разблокирует другие задачи или защищает дедлайн осени-2026 — вперёд.
3. User-facing ценность (UX-находки P0/P1) выше внутренней полировки.
4. При равной ценности — quick win (укладывается в один батч).

**Дисциплина указателя:** закрыл батч → обнови «СЛЕДУЮЩАЯ ЗАДАЧА» здесь и «СЛЕДУЮЩИЙ ШАГ» в
`docs/WATCHLOG.md` §0 — они обязаны совпадать. Перед стартом — верификация состояния по диску
(PIT-004), оценка объёма и вероятности уложиться (`docs/session_continuity.md`).

> ## СЛЕДУЮЩАЯ ЗАДАЧА
>
> **СЛЕДУЮЩЕЕ — ОСТАТОК ЮРБЛОКА. MINOR, v8.41.0.**
>
> L5, L6, L7 закрыты в v8.40.0. Осталось: **экран выдачи и отзыва согласий** по
> 403-ответу гейта (L3) — сейчас `ConsentRequiredPanel` показывается по месту отказа,
> но отдельного места, где видно ВСЕ согласия и можно отозвать любое, нет (частично
> есть на профиле — проверить полноту); и **L2** — ни один чекбокс не отмечен заранее,
> проверить не только в регистрации.
>
> После них снимается условие сноса Jinja-остатка (`legal/*`, `contacts`, `validation`),
> который входит в гейт Э6.
>
> **Остаток §8.2 «ГЛАВНОЕ»:** A/B-результаты и аналитика-дашборд — обе админские,
> продукт к запуску не двигают.
>
> **Прочее, не блокирующее:** остаток H3 (`banks_router`/`plaid_router` под `_FIN`
> вместе с фронтом, решение владельца v8.27.0) · 12 неверифицированных гипотез ·
> декоративность джобы линтеров в CI · долг `/code-review` временно́й лаборатории ·
> `ForecastResult` всё ещё рукописный (`/planning/forecast` размечен `-> dict[str, Any]`)
> — следующий кандидат на генерацию из контракта.
>
> **Перед каждой фичей:** эндпоинт отдаёт `dict[str, Any]` — завести схему на бэкенде
> ДО фронта; проверить гейт согласия; заложить 8-10 блокеров от агентов, это норма,
> а не признак плохой работы.

**ПАРСЕР ВЫПИСОК — НА ПАУЗЕ (упёрся в данные, не в код).** Состояние, блокеры и точный запрос файлов:
`docs/statement_parser_open_items.md`. Без новых реальных выписок дальше — только самоуспокоение.

**v8.9.0 — ВНЕОЧЕРЕДНОЙ инфраструктурный батч, НЕ шаг Э5.** По прямой задаче владельца (вне
канонической последовательности ниже): прод-сборка фронта в Docker (`nginx/Dockerfile`,
multi-stage node+nginx), развод маршрутов nginx (SPA vs FastAPI/Jinja), локальная проверка
`docker-compose.prod.yml` на изолированном стенде, чинка `docs/DEPLOY.md` под появление фронта.
Формально деплой — веха 9 (см. строку 40 и §4.4 ниже), эта работа сделана раньше по месту
намеренно, прогресс самой Э5 (домены ниже) от неё не сдвинулся. Подробности — CHANGELOG [8.9.0].

**v8.9.1 — Python 3.12→3.13, PATCH, НЕ шаг Э5.** По прямой задаче владельца: закрыт разрыв
dev↔prod/CI (`.venv` на 3.13 с v8.7.1, `Dockerfile`/CI оставались на 3.12). Подтверждено
`docker build` + прогоном на реальном PostgreSQL 16. Прогресс Э5 не сдвинулся. Подробности —
CHANGELOG [8.9.1].

**v8.9.2 — ревизия канона матмодели по репо + README, PATCH, НЕ шаг Э5.** Владелец поймал
ошибку ассистента (канон назван v3.0.0 вместо реального v3.5.0), разбор вскрыл 28 файлов
с той же ошибкой и что баг уже был найден раньше и брошен без исправления. Хук
`session-context.sh` сделан самообновляющимся. README пересмотрен комплексно (версия,
тест-счётчики, отсутствие React SPA в описании стека). Прогресс Э5 не сдвинулся.
Подробности — CHANGELOG [8.9.2].

**v8.9.3 — доследствие ревизии v8.9.2, PATCH, НЕ шаг Э5.** Владелец перепроверил v8.9.2:
`ROADMAP.md` строка 101 всё ещё называла канон v3.0.0 (первоисточник исходной ошибки —
ревизия искала имя файла, не голое упоминание в прозе); канон подтверждён по коду
(`app/core/ranking.py::is_toxic_debt`, `app/core/crisis.py`); снос старых Jinja-шаблонов
восстановлен явным шагом Э8. Подробности — CHANGELOG [8.9.3].

**v8.9.4 — стабильное имя канона (docs/math_model.md), PATCH, НЕ шаг Э5.** Структурная причина
серии v8.9.2–v8.9.3: имя файла канона версионировалось, требовало ручного обновления ссылок
при каждой рекалибровке. `math_model_v3_5_0.md` → `docs/math_model.md` (стабильно), старые
версии → `docs/model/history/`. `revision_check` CLEAN, канон v3.5.0 подтверждён сквозной
трассировкой рантайма. Подробности — CHANGELOG [8.9.4].

**v8.10.0 — веха 8, Э5: диаграмма Санкея в AllocationPanel, MINOR.** Первая часть первого
пункта Э5 («Санкей — только режим «подробно»»; ползунки «что если» из того же буллета не
начаты). Тогл-кнопка под легендой раскрывает decorative recharts `<Sankey>`, дефолтные
рендереры recharts заменены целиком (магические `#0088fe`/`#333`). `design-critic` +
`a11y-auditor` до сдачи нашли и починили: ложную `aria-expanded`/`aria-controls` на
`aria-hidden`-блоке (→ `aria-pressed`), перепутанные якоря подписей узлов, магические числа в
SVG, `utility.toFixed(2)` без запятой, сплющивание на узких экранах. Осознанно отложено (не
блокирует эту версию, оба — новые пункты ниже): копия «U = {u}»/«СППР», незагружаемый Manrope
в SPA. Подробности — CHANGELOG [8.10.0].

**v8.11.0 — веха 8, Э5: ползунки «что если» в AllocationPanel, MINOR.** Вторая (последняя)
часть первого пункта Э5 — первый пункт закрыт целиком. Составной столбец, легенда и диаграмма
Санкея реагируют на два ползунка (долг/цели, резерв — остаток): один интерактивный артефакт,
как в плане, не рекомендация сверху и песочница снизу отдельно. Результат ищется в уже
посчитанном `ranked[]` (`findMatchingAlternative`), недоступные модели комбинации помечены
явно. `design-critic` (2 запуска, первый упал на сетевой ошибке) + `a11y-auditor` (тоже 2
запуска) до сдачи нашли и починили 3 блокера: столбец был заморожен на `best` (переписано на
общее управляемое состояние), `accent-color` ползунка долга случайно совпадал с цветом «Целей»
(`--c-accent`≡`--c-green`), блок результата был неотличим от фона панели в светлой теме
(`--c-surface-up`≡`--c-surface` там). Подробности — CHANGELOG [8.11.0].

**v8.19.5 — веха 8, Э5: design-critic/a11y-auditor проход по блоку объяснения плана,
PATCH.** Закрывает п. 8.4 — единственный кусок Э5, не прошедший обычный цикл проверки (скелет
v8.14.0). Подробности выше в этом разделе и CHANGELOG [8.19.5].

**v8.20.0 — веха 8, Э5: формулы KaTeX по клику вместо голой «U = {u}», MINOR.**
Закрывает находку design-critic v8.10.0 (CMP-05) — `UtilityFormula`/`UtilityFormulaBody` в
`AllocationPanel` (символьная SAW-формула из `docs/math_model.md` §8 + числа альтернативы,
раскрывается по клику, скрыта по умолчанию), `AlternativesBrowser` — «U = {u}» → «Оценка {u}»
на всех 66 строках. `katex` вынесен в отдельный React.lazy-чанк (не платить ~260 КБ бандла на
каждом визите `/dashboard`/`/planning` ради опциональной детали). Подробности — CHANGELOG
[8.20.0].

**v8.21.0 — веха 8, Э5: шкала с зонами (ПДН/автономия) + дисклеймер 601-ФЗ, MINOR.**
`ZoneScale` в `MetricsGrid` — карточки «Ликвидность» и «ПДН» получили визуальную шкалу
(декоративную, aria-hidden — число уже текстом рядом); при `Dt > 0.5` спокойный дисклеймер о
рисках (Федеральный закон от 29.12.2022 № 601-ФЗ) — новая константа
`DTI_LEGAL_DISCLAIMER_THRESHOLD`. Подробности — CHANGELOG [8.21.0].

**v8.22.0 — веха 8, §8.4: экраны входа/регистрации с нуля, MINOR.** Владелец выбрал первым
quick-win «Красивые системные сообщения — регистрация, ошибки, подтверждения» — разведка
показала, что в SPA не было ни одного экрана входа/регистрации и ни одной формы вообще
(разбор — CHANGELOG [8.22.0]). Построены `LoginPage`/`RegisterPage`/`ForgotPasswordPage`/
`ResetPasswordPage`, единая система тостов (`shared/ui/Toast.tsx`), входная точка в топбаре.
Полный цикл (регистрация→логаут→вход→сброс пароля→вход новым паролем) проверен живьём в
браузере — по ходу пойман и починен реальный баг топбара (stale-if-error в TanStack Query).

Все шесть пунктов плана Э5 (`frontend_milestone8_plan.md`) технически закрыты с v8.21.0.
**v8.24.0 — §8.4: горизонт + ставка «что если» в прогнозе, MINOR.** Объём согласован владельцем
(`AskUserQuestion`): горизонт (select 3/6/12/24 мес.) + сценарная ставка капитализации `r_bench`
(ползунок, дебаунс 500 мс). Параметры самого движка (α/β Holt, σ Monte-Carlo, ширина CI 80%)
сознательно не отданы — канон-заморожены, обычный пользователь не понимает их смысл. До сдачи
`design-critic`/`a11y-auditor` нашли и починили: точку вместо запятой в деньгах, невидимый для
зрячих `isFetching`, поломку вёрстки на узких экранах, непомеченный в самом графике сценарий,
потерю фокуса после кнопки «Сбросить». Побочно починен протухший `docs/api/openapi.json` (пин
`openapi_paths` держал 114 вместо фактических 111 после сноса Jinja) и CJK-канарейка, ловившая
себя на `frontend/dist/`. Проверено живьём в браузере (Chrome DevTools MCP). Детали —
CHANGELOG [8.24.0].

В §8.4 остаётся 1 пункт: «PDF-отчёт со страницы прогнозирования» — отложен владельцем ДО
CRUD-паритета (§8.2, пятёрка чекбоксов выше): отчёт станет полезнее, когда будет отражать
данные, которые пользователь внёс сам, а не только демо-кейсы.

**v8.24.1 (PATCH, подготовка перед CRUD):** гейт согласия на `budgets_router` (был единственным
без него), мягкое удаление + `restore` для бюджета (миграция `0034`, симметрия с остальными
четырьмя сущностями), `extractErrorMessage.ts` разбирает объектный 403 гейта согласия — чтобы
все батчи CRUD ниже сразу шли по рабочему пути ошибок. Детали — CHANGELOG [8.24.1].

**v8.25.0 (MINOR, батч 1 CRUD-паритета — Обязательство + Актив):** создание/правка/удаление
в React с нуля (было read-only) — общий `Modal` (`@radix-ui/react-dialog`, первое применение),
общие стили полей `shared/ui/field.css` (объединены с auth-формами, CMP-01), danger-вариант
`Button`, мягкое удаление с `toast.undo`. До сдачи design-critic (12 блокеров) и a11y-auditor
(3 нарушения AA — фокус после удаления, объявление восстановления, окно отмены) нашли и
починили реальные проблемы: оверлей/поверхность модалки хардкодом мимо токенов, кнопка
«Удалить» неотличима от «Изменить» и красилась в акцентный зелёный на hover, отсутствие
`:disabled`, мусор с плавающей точкой в поле ставки при правке (`0.078 * 100 →
7.800000000000001`), отсутствие валидации обязательных полей, потеря фокуса после удаления
строки. Vitest 187/187 (было 175 до PATCH). Детали — CHANGELOG [8.25.0].

**ОЧЕРЕДЬ (зафиксировано 2026-08-19, владелец — три задачи одним сообщением, порядок как задан):**

**A. Батч дрейфа (3 пункта, один батч, чтобы не плодить церемонии) — ПЕРЕД батчем 2:**
1. **nginx SPA-regex не содержит `dashboard`.** `nginx/templates/finpilot.conf.template` —
   `/dashboard` уходит на FastAPI и отдаёт легаси Jinja с формой бюджета, хотя React-дашборд
   живёт на `/`, а `base.html:31` ведёт именно на `/dashboard`. Почини регекс. Плюс
   автопроверка: каждый корневой роут из `frontend/src/routes/*.tsx` (кроме `__root.tsx`)
   обязан быть покрыт SPA-блоком nginx — тест должен падать на ТЕКУЩЕМ конфиге (red), потом
   фикс (green).
2. **`README.md` висит на бейдже v8.24.1** — второй повтор класса (в v7.7.0 отставание было
   15 версий). Добавить `README.md` в проверку версий `tools/preflight.py` — сейчас сверяет
   только `config.py`/`CHANGELOG.md`/`WATCHLOG.md`/`VERSION`.
3. **Инвариант ПДН ≤ 0.40 подписан тремя РАЗНЫМИ версиями канона одновременно:**
   `dtiThresholds.ts` — v3.5.0, `AlternativesBrowser.tsx:17` — v3.0.0,
   `MetricsGrid.test.tsx:20` — v3.5.0 (канон сейчас v3.9.0). Не проставлять свежий номер —
   убрать номер версии вообще для стабильных инвариантов, ссылаться на `docs/math_model.md`
   §3. Пройти по всему репо, отделить легитимные исторические пометки «введено в каноне vX»
   (не трогать) от описаний ТЕКУЩЕГО состояния (убрать номер). `LegacyModelChecker`
   (`tools/revision/revision_check.py`) ходит только по markdown — расширить на `.ts`/`.tsx`/`.py`.
   **Тесты сначала по каждому из трёх пунктов** (владелец явно потребовал red→green на
   каждом, не только на nginx).

**B. CRUD-паритет §8.2, батч 2 — Операция + Цель — СДЕЛАНО, v8.28.0.** Решения владельца
исполнены как заданы: `description` в форме операции, `mcc`/`currency` не включены; у цели —
`GoalUpdate` не принимает `current_amount` вообще (отдельный роут `POST
/goals/{id}/contributions`, переиспользует `record_goal_contribution` из `crud.py:883` +
новый `add_goal_contribution`, прибавляет, не перезаписывает), для `linked_asset_id` — 409 на
попытку внести прогресс вручную. Бэк заложен в объём, как предупреждал владелец: `PUT`
для обеих сущностей, схемы, 10 новых тестов TDD red→green (28/28 в `test_api_crud.py`).
Фронт — `GoalForm`/`TransactionForm`/`GoalContributionForm` по каркасу батча 1 (`Modal`,
`field.css`, `toast.undo`, `Button variant="danger"`). Детали — CHANGELOG [8.28.0].

**C. Разбор гейта согласия на planning/recommendation/analysis/export — СДЕЛАНО (докс,
2026-08-19), решение принято и исполнено в v8.27.0.** Полный разбор — `docs/legal/
compliance_register.md` §6. Итог: «пятёрка» из CHANGELOG [8.24.1] — корректная формулировка
(4 исходных + budgets, `telemetry` сознательно не считается финансовым роутером — пишет
события, не портрет). Реальный, незадокументированный разрыв — четыре роутера (`analysis`/
`planning`/`recommendation`/`export`) читали тот же портрет без `require_financial_consent`.
**Владелец (роль архитектора) решил гейтить все четыре — исполнено в v8.27.0** (`_FIN` на 10
роутерах), вопрос закрыт.

**D. Роль «независимый эксперт» — СДЕЛАНО, реализована и прогнана, 2026-08-19/20.** Агент
`.claude/agents/independent-expert.md` создан и прогнан в конце этого батча (дрейф + CRUD
батч 2 + compliance-разбор), как поручил владелец. Результат — 16 гипотез разрыва
«докс/канон vs код», сохранён дословно (правило §18) в `docs/research/raw/
12_independent_expert_milestone8_gap_2026-08-19.md`. Верхние по правдоподобности: **H1** — в
React-SPA нет навигационного каркаса вообще (топбар — только email+выход, ни одной ссылки на
`/obligations`/`/goals`/`/banks`/`/profile`, профиль с отзывом согласия недостижим кликом);
**H3** — периметр `_FIN` (после v8.27.0, «10 роутеров») всё ещё пропускает `banks_router`
(импорт выписки пачкой), `POST /demo/clear` (жёсткий SQL delete без гарда на авторизованного),
`notifications_router` (суммы в уведомлениях), `plaid_router`; **H2** — `RegisterPage` обещает
перенос гостевых данных, бэкенд явным комментарием этого не делает.

**E. Adversarial-verify гипотез — НАЧАТ, v8.30.2. H1, H13 и H3 подтверждены; починен только
H3b.** Владелец отдал выбор развилки вахте; порядок выбран по двум критериям — что необратимо
и что блокирует остальное.

*Подтверждено чтением кода (не саммари):*
- **H1 — навигационного каркаса в SPA нет вообще.** `frontend/src/routes/__root.tsx:18-21` —
  топбар это `AuthTopbarLink` + `ThemeToggle` и ничего больше; `<nav>` и `role="navigation"`
  не встречаются в `frontend/src` ни разу; все восемь `<Link>` продукта ведут на
  `/login`/`/register`/`/forgot-password`. Значит `/obligations`, `/goals`, `/banks`,
  `/profile` недостижимы кликом ниоткуда, а в `/profile` живёт отзыв согласия
  (`ProfilePage.tsx:7`, `useWithdrawConsent`) — право по 152-ФЗ реализовано и недоступно.
  Класс тот же, что SEV1 `CONSENT-GATE-NO-UI`.
- **H13 — межстраничные переходы сырыми `<a href>`.** `Hero.tsx:32`, `DashboardEmpty.tsx:17`,
  `PlanningPage.tsx:115` — в SPA это полная перезагрузка документа со сбросом кэша TanStack
  Query. Не отдельная находка, а усугубление H1: это единственные рабочие переходы в продукте,
  и они самые дорогие.
- **H3 — периметр `_FIN` неполон.** `app/api/router.py:34-66`: `banks_router` (:53),
  `demo_router` (:52), `notifications_router` (:56), `plaid_router` (:62) вне гейта.

*Починено в v8.30.2 — только H3b.* `POST /demo/clear` передавал `user_id` авторизованного в
`_clear_all` (`routes_demo.py:40-48`) — жёсткий SQL `delete()` по `Transaction`/`Obligation`/
`Goal`/`LiquidAsset`, то есть один `POST` без тела и без подтверждения безвозвратно сносил весь
портрет живого аккаунта мимо soft-delete/undo (миграция `0034`). Гард поставлен дословно по
близнецу `/demo/load`, который отдавал авторизованному 403 с самого начала. Гостевая песочница
не затронута, 3 теста TDD red→green. Взято первым из всей развилки, потому что это
единственный пункт про необратимую потерю данных, и единственный из H3 без сцепки с фронтом.

- [x] **`notifications_router` под `_FIN` — СДЕЛАНО, v8.32.0 (H3c).** Гейт поставлен
  ПОЭНДПОИНТНО (`feed`/`unread-count`/`{id}/read`/`read-all`), а `POST /notifications/run`
  сознательно оставлен без него: это cron-рассылка от служебного админа для всех
  пользователей, роутерный `_FIN` потребовал бы согласия админа на чужие данные и молча
  остановил бы рассылку. На `/run` стоит регрессионный тест, что гейта там нет. Вместе с
  фронтом, как требует решение владельца от v8.27.0.

- [ ] **Остаток H3 — `banks_router` и `plaid_router` под `_FIN`.**
  СОЗНАТЕЛЬНО не сделан в v8.30.2: по решению владельца от v8.27.0 гейт ставится ТОЛЬКО вместе
  с фронтом, иначе экран `/banks` воспроизводит SEV1 `CONSENT-GATE-NO-UI`. Самый чувствительный
  из трёх — `banks_router`: импорт банковской выписки пачкой (`routes_banks.py:125-157`,
  `bulk_create_transactions`) не требует согласия на финданные, тогда как ручное добавление
  одной операции требует. `notifications_router` кладёт суммы в тело in-app уведомления, письма
  и Telegram (`services/notifications.py:161-169`) вне гейта. `plaid_router` в РФ-сборке отдаёт
  404, но это конфигурация, а не гейт.
- [x] **H16 (households без фронта) — подтверждена и закрыта в v8.36.0.** Бэкенд из
  двенадцати эндпоинтов, фронта ноль строк. Попутно вскрылся второй случай H12: роута
  `/join` не существовало, хотя бэкенд строил на него ссылку внутри приглашения.
- [ ] **Оставшиеся 12 гипотез (H2, H4–H12, H14–H15) — не верифицированы.** Вести проверку по
  ходу профильных батчей, а не отдельным заходом: три самые дорогие уже сняты, а отдельный
  проход даёт отчёт без строчки кода.

Батч 3 (Бюджет, фронт) закрыт в v8.30.0 — CRUD-паритет §8.2 теперь 5/5. Далее по указателю —
навигационный каркас SPA (H1+H13, MINOR), затем остаток §8.2 «ГЛАВНОЕ» (вынос API-only фич на
фронт) или PDF-отчёт §8.4 (полный визуальный вариант, разведка текущего
`/api/planning/export.pdf` уже сделана). После §8.4 — решение владельца: Э6 (юридический контур, другой домен, внешняя
проверка живым юристом, ROADMAP §6.1) или продолжение quick-win'ов.
ADR-016 (лавина долгов, полный помесячный график погашения) закрыт в v8.16.0 — канон
**v3.8.0**, `app/core/amortization.py`, стенд 12 000 портретов чист (0 исключений/зависаний).
Оба пункта ROADMAP §6.6 (барьеры 2 и 3) теперь закрыты целиком; §6.6 не требует продолжения,
если владелец не откроет новое решение. После Э5 — Э6 юр-контур → Э7
CSP/аутентификация/производительность → Э8 закрытие вехи; остаток §8.2а (API-only фичи,
юр-требования L2/L3/L5/L6/L7, микро-визуализации) не потерян, встаёт в очередь после Э5 либо
вперемешку как quick win. Открыто с прошлых батчей,
не блокирует: решение по `docs/reports/decisions/2026-08-02_xz_recompression.md`,
`claude update`, `claude doctor`; **шрифт `--font-body: Manrope` объявлен токеном (tokens.css),
но нигде не загружается в SPA** (ни `@font-face`, ни `<link>` в `index.html` — весь SPA
фактически рендерится системным шрифтом; найдено design-critic в v8.10.0, затрагивает весь
фронт, а не один экран — решить: самостоятельный хостинг woff2 vs CDN, до какой из следующих
Э-партий). Веха 9 забирает L8, Redis rate-limit, CSP без `unsafe-inline` в проде, TLS/HSTS,
прод-секреты, уведомление Роскомнадзора (O1 — подаётся ДО начала обработки, от фронта не
зависит).
движок табличных выписок + путь добавления банка (стратегия §7)**; ранее v6.12.0 — сверка CSV (вскрыла
потерю ВСЕХ приходов Райффайзена и ещё три дефекта). **Очередь: независимый аудит на holdout**
(`docs/statement_parser_audit_protocol.md`) — нужны свежие выписки от владельца; суммы Тинькофф-CSV
сверить по-прежнему не с чем (только полнота разбора); Альфа/Газпром PDF; OCR для сканов. Ранее: v6.11.0 —
property-генератор (Б2) + ретроспектива + протокол независимого аудита. Ранее: v6.10.0 — измеритель точности
(сверено 10 из 11 реальных PDF арифметикой самих банков). **Очередь по парсеру: Б3** — обобщить
header-driven движок в один + словарь банков; сверка CSV; независимый аудит на holdout. Ранее: **v6.9.0 — аудит парсера на
реальных данных, часть 2** (Райффайзен по шапке — было 0 операций на двух файлах, фиктивный доход от
кредитного лимита у Сбера, детект банка по содержимому, сверка с итогами банка в импорте — вариант B).
Очередь по парсеру: контрольные итоги Сбера (разбор по координатам), выписки Альфы и Газпромбанка с
операциями (**нужны файлы от владельца** — в наборе только справки и пустой счёт), OCR для сканов.
Ранее закрыто: G4/G5/G7 (v6.2.0), прогноз на
реальной истории (v6.4.0), замер шага 5% + решение (v6.5.1, ADR-008), nullable-дедлайн (v6.5.0),
**G7-остаток (v6.6.0, ADR-009)**, **переименование модели → v3_3_0 (v6.7.0)**, **§6.4 импорт выписок:
шаблоны + golden-тесты + покрытие (v6.7.0)**. Итерация 2 сертификации закрыта (v6.13.0,
модель v3.4.0, согласие 88.0%); открыт **раунд 3 на датасете v3** (пакет готов, ждёт чистых сессий):
(`model_outcomes_v3_3_0_on_v2`, v6.12.3), **BLOCKED — нужен independent-expert прогон владельца по
аккаунтам V/M/J/S** (принимающий вахту загрязняет baseline). После неё веха 6 закрывается (нужен блок
«Предложения Claude», правило 6) → **веха 7 (React+TypeScript фронт, ADR-001)**, план —
`docs/frontend_migration_plan.md`, юр-требования фронта уже зафиксированы. Параллельно: PNG-превью
диаграмм — на Mac («Ожидается»).

---

## 8.5. Временна́я лаборатория — планирование не только в токенах

- [x] **Замер длительности прогонов + поддерживаемый справочник.** ✓ v8.33.0, по прямому
  требованию владельца. `tools/timing_lab/` (журнал + `record.py`) и
  `docs/timing_reference.md`, таблица в котором обновляется командой
  `python -m tools.timing_lab.record report --update-doc`, а не руками.
  Ключевое свойство: каждая запись несёт load average, потому что длительность без неё
  бессмысленна — один и тот же срез шёл 40.8 с и 83.4 с без правок кода (семь эпизодов
  `INV-MACHINE-LOAD`). Медиана вместо среднего, спокойные прогоны отделены от
  перегруженных, порог перегрузки относительный (выше числа ядер), а не абсолютный.
  Засеяна 29 замерами, снятыми по факту, не оценками.

- [ ] **`/code-review` инструмента лаборатории — ДОЛГ, две попытки, обе упали.**
  Первая (v8.33.0): `rate_limit`, `HTTP 429, req_011CegSMgbm1ascd4828jPAC`.
  Вторая (v8.34.0): `authentication_failed, HTTP 403, Request not allowed — Please run
  /login`. Второй отказ не лимит, а авторизация субагента; `/login` по §4 авто-режима
  делает владелец — цена команды равна всему контексту сессии, и это его решение.
  Инструмент прогнан тестами (16/16), `flake8` и `mypy` чисты, режим `run` проверен
  живым замером, три дефекта найдены и починены СОБСТВЕННЫМ перечитыванием: `or`
  проваливался на нулевой медиане и уводил быстрый набор в сортировке на его худшее
  значение · граница 90 секунд давала «90 с» и «2 мин» у соседних замеров · опечатка
  в команде роняла инструмент трейсбеком вместо записи. Но самопроверка по §2д слабее
  взгляда со стороны, и правило §2в требует ревью каждого нового инструмента.

- [ ] **Пополнять лабораторию по ходу, а не разово.** Замеры имеют смысл как ряд:
  один прогон не отличает регрессию от нагрузки хоста. Практика — гонять тяжёлые
  наборы через `record run`, а не напрямую, тогда журнал наполняется сам.

## 9.0. Гейт перед деплоем: полный прогон ВСЕХ видов тестов, не только `fast`

> **Найдено 2026-08-19**, разбор CI сразу после того как код (v8.20.0–v8.25.1) впервые дошёл
> до реальных тестов на GitHub Actions (11+ прогонов подряд до этого падали на
> заглавный `GLOSSARY.md` (в `docs/`) в `preflight`, ещё до старта pytest — фикс v8.25.1,
> CHANGELOG [8.25.1]).
> Полная опись видов тестов и что каждый проверяет — `docs/testing_infrastructure.md` §1: unit,
> integration, property (Hypothesis), E2E-smoke (chromium, тир `fast`), E2E мультибраузер
> (chromium+firefox+webkit), визуальная регрессия, live-a11y (axe), security (`bandit`+
> `pip-audit`), нагрузочный (`locust`, `loadtest/locustfile.py`), мутационный (`mutmut`).
> Тир `fast` — блокирующий на каждом push/PR, этого достаточно для мержа. Тиры `full`
> (мультибраузер/визуал/a11y/security/locust) и `deep` (мутации/стресс-property) триггерятся
> только тегом `vX.Y.Z`/вручную/по расписанию — и **на каждом известном прогоне в истории
> проекта `full` падал на первом же шаге (мультибраузерный E2E)**, из-за чего всё, что идёт
> после него в том же job (визуал, live-a11y, security, locust), ни разу физически не
> исполнялось. Нагрузочный сценарий существует с v4.16.11 — почти год в репозитории, ни разу не
> подтверждён своим реальным результатом в CI. Плюс сам шаг locust в `ci.yml` завершается
> `|| true` — даже дойдя до выполнения, провалить прогон не может технически, то есть это
> видимость гейта, не гейт.

- [ ] **Перед КАЖДЫМ реальным деплоем (после заморозки кода — фронт и бек зафиксированы, батч
  сдан) — обязательный сквозной прогон ВСЕХ видов тестов, не только `fast`.** Явно прогнать и
  получить зелёный (или осознанно принятый красный с разбором) результат по каждому виду:
  мультибраузерный E2E, визуальная регрессия, live-a11y, security, нагрузочный — с реальным
  порогом, не заглушенный `|| true`, и хотя бы один раз перед первым продовым релизом —
  мутационный + стресс-property (`deep`), дальше штатно по расписанию. Один зелёный `fast` на
  пуше НЕ считается готовностью к деплою.
- [ ] **Убрать декоративность гейта `full`.** _(частично — v8.25.2)_ Снято `|| true` с
  locust-шага (`ci.yml`, раньше провалить сборку физически не мог); всем шести джобам добавлен
  `timeout-minutes` (не было ни у одной — зависание ждало бы 6 часов дефолта GitHub, см.
  `docs/reports/investigations/ci_full_pg_matrix_hang_investigation.md`, INV-CI-PG-HANG).
  **Остаток:** завести способ гонять `full` НЕ только по тегу релиза, а по явному запросу в
  процессе стабилизации перед релизом — иначе упавший на середине E2E-шаг прячет всё, что после
  него, годами (ровно это произошло с v8.19.4–v8.24.1: 11+ прогонов подряд, ни разу не дошедших
  до visual/a11y/security/locust).
  **Второй случай того же класса, найден 2026-09-02 (v8.30.2), НЕ починен:** job «Линтеры»
  (`ci.yml:121`) стоит под `continue-on-error: true`, то есть провалить сборку не может в
  принципе. Это выяснилось не из чтения конфига, а потому что `flake8 .` локально оказался
  красным (exit 1, 163 нарушения — все в `_base/`, в коде проекта ноль) и никто об этом не
  узнал бы: корневой `.gitignore` `_base/` не исключает, дерево уходит в CI, а job молчит.
  Сам `flake8` починен в v8.30.2 (`_base` в `exclude`, теперь exit 0) — но декоративность
  job'а осталась, и это уже третий гейт подряд, обесцененный тем, что всегда красный или
  всегда зелёный (`|| true` на locust, `full` на теге, `continue-on-error` на линтерах).
- [ ] **Разобрать уже собранные находки `full`-тира на v8.20–v8.25.1** (первый реальный прогон,
  собран 2026-08-19, детали пока только в переписке сессии — перенести в
  `docs/reports/investigations/` или `docs/reports/incidents/` по итогам разбора, что из этого
  реальный дефект продукта, а не тестовая инфраструктура): модалка `#goal-form` перехватывает
  клик в firefox/webkit (chromium проходит); `/planning`-тесты таймаутят в firefox (тяжёлая
  страница — 66 альтернатив + Monte-Carlo + KaTeX — либо реальный перф-разрыв, либо тесный
  30-секундный бюджет, не разделено); визуальные baseline-скриншоты, похоже, существуют только
  под chromium — сравнение в firefox/webkit даёт ~100% diff не из-за реальной регрессии вёрстки,
  а из-за отсутствия эталона.
- [ ] **Разобрать долг джобы `Линтеры` (информационная, `continue-on-error: true` — не блокирует
  деплой) — после закрытия всей вехи 8 (фронт), перед реальным деплоем, не раньше.** На
  2026-08-19: flake8 чист (3 строки E501 в `tools/model_validation/model_snapshot.py` починены
  в v8.25.2, без риска); mypy — 3 ошибки в `statement_parser.py`/`statement_reconcile.py`, уже
  отдельный пункт ROADMAP §8.2 («починить 3 ошибки mypy в парсере», ЗАКРЫТ в v8.34.0) — чинить вместе с той
  задачей, не ради галочки в CI; pylint — ~150 находок размазаны по всему `app/`, оценка
  9.62/10, старый стилевой фон всего кодабейса, не регрессия последних версий — разбирать
  отдельным заходом, не в одну сессию, и только если решение будет «стоит того» (сейчас, по
  оценке того же батча v8.25.2 — не стоит, джоба ничего не блокирует).
- [ ] **E. Новый обязательный класс тестов — «полный эмпирический путь реального пользователя»,
  прямое поручение владельца 2026-08-19.** Найдено на примере `CONSENT-GATE-NO-UI`
  (`docs/reports/incidents/consent_gate_no_ui_dead_end_incident.md`, SEV1 near miss): гейт
  согласия на финданные работал корректно на бэкенде 26 дней и 15+ версий, но UI для его
  прохождения не существовал вообще — каждый новый пользователь упирался в тупик без обходного
  пути. Ни design-critic, ни a11y-auditor, ни ручные прогоны в браузере этого не поймали, потому
  что все шли по демо-режиму или аккаунтам, заведённым до введения гейта — путь «с нуля» не
  тестировался НИКЕМ до прямой ручной эмпирической проверки владельцем через отдельный чат.
  **Требуется:** не мок отдельной функции, а реальный HTTP + реальный браузер (Playwright —
  паттерн уже отработан 2026-08-19, backend uvicorn + frontend vite dev + headless chromium,
  сквозные сценарии от чистой регистрации до реального действия, с проверкой РЕАЛЬНЫХ кодов
  ответа, не предположений). Владелец прямо поручил: **придумать и зафиксировать ВСЕ реалистичные
  сценарии** (не только про согласие) — где ещё в продукте может быть разрыв между «задумано
  теоретически, в докс» и «доступно из UI на практике, в коде» — и внести новый блок тестов
  именно на такие сквозные сценарии, не только на согласие. Черновой список сценариев для
  проработки (не исчерпывающий, отправная точка): регистрация → первая финансовая запись
  (закрыто, было находкой); регистрация → первый расчёт плана (`/planning`, гейтится ли
  согласием так же?); гостевой/демо-режим → регистрация с переносом введённых demo-данных
  (`RegisterPage.tsx` обещает перенос — проверить эмпирически, не по тексту UI); истёкшая
  JWT-сессия посреди заполнения длинной формы (что видит пользователь?); email не подтверждён →
  какие функции реально доступны/недоступны на практике, совпадает ли с тем, что подразумевает
  бейдж «не подтверждён» в профиле; отзыв согласия на финданные (только что реализовано,
  v8.25.3) → реальный повторный запрос на списки — действительно ли перезапрашиваются, не только
  по моку. Результат — не только тесты, но и ОТДЕЛЬНАЯ запись каждого нового найденного разрыва
  (если найдётся) тем же способом, что и `CONSENT-GATE-NO-UI` — post-mortem/incident-карточка,
  не молчаливый фикс.

---

## Что уже сделано (контекст, не переоткрывать)

К v4.23.0 закрыто, подробности — в `CHANGELOG.md` / `docs/RELEASES.md`:

- [x] **Весь P0** (деплой-инфра, SMTP-код, ЦБ-парсер, нагрузочное, секреты, cron, бэкапы).
- [x] **P1.1-P1.3, P1.6, P1.7** (a11y механика+контраст WCAG AA, кэш прогноза, rate-limit, Sentry,
  soft-delete бэк).
- [x] **P2.4-P2.7** (spending-advice слой 3, экспорт XLSX/PDF, история планов, обучение категоризации).
- [x] **P3.5** (A/B-фреймворк), **P3.7 backend** (household-оверлей, миграция 0022).
- [x] Импорт-трек: мультибанк CSV/XLSX/PDF (Тинькофф/ВТБ/Сбер/Райффайзен/Альфа), формат 1C, bulk.
- [x] B2B `/v1/analyze`, Plaid (для US/CA, в РФ-сборке отключён).
- [x] Тест-инфраструктура: 776 fast-тестов, покрытие >90%, 3-категорийный CI, property-тесты, PG-матрица.

**Остаточный технический долг** (закрываем в вехе 4 — см. ниже): mypy/flake8, `datetime.utcnow()`,
`ExpenseRecord` без даты.

---

# ВЕХА 4 — Завершение бэкенда + техдолг (`v4.x.x`)

> Цель: добить оставшийся бэк и закрыть техдолг ДО фронта, чтобы на вехе 6 не прыгать туда-сюда.
> Принцип заглушек: MVP/каркас для того, что **уже в роадмапе и точно понадобится фронту**;
> спекулятивное не выдумываем (YAGNI). Всё по TDD.

## 4.1. Бэк-код — реальные дыры

- [x] **P2.3 in-app уведомления (бэк).** Сделано в v4.19.0: модель Notification, миграция 0023,
  CRUD (create/list/count_unread/mark_read/mark_all), эндпоинты feed/unread-count/{id}/read/read-all,
  хук в email-рассылку (где письмо — там in-app), 14 тестов. Снят блокер для колокольчика (веха 8).

- [x] **Хвост P3.7 — `household_id` на производных.** Сделано в v4.20.0. **Коррекция факта:** колонка
  household_id уже была в БД (миграция 0022) и в моделях Scenario/PlanSnapshot — ранняя заметка
  «нет на этих таблицах» была ошибкой grep. Реальные пробелы были другие и закрыты: (1) household_id
  не проставлялся при создании (`save_scenario`/`create_plan_snapshot`) → производные всегда личные;
  (2) `get_plan_snapshots` игнорировал household-ось (ручной фильтр вместо `_owner_filter`). Добавлен
  `resolve_household_id` (валидация членства — чужой household проставить нельзя, fallback на личное),
  проброс через endpoint'ы планирования, починка чтения. 11 тестов. Миграция не потребовалась.

- [x] **P3.4 referral rewards (заглушка/MVP).** Сделано в v4.21.0. Расширён `/referral/me`:
  invite_url (готовая ссылка-приглашение с кодом для шаринга) + milestones (вехи-достижения по числу
  приглашений: 1/3/5/10/25, детерминированы из счётчика, без новых таблиц) + next_milestone (прогресс).
  Логика в `app/services/referral.py`, поле reward зарезервировано под реальные награды (привяжем при
  монетизации — YAGNI). 11 тестов.

- [x] **Telegram-каркас (P3.6).** Сделано в v4.22.0. Привязка аккаунта через одноразовый link-токен
  (deep link `?start=<token>`), команды бота `/start [token]`/`/link`/`/unlink`/`/status`, webhook с
  secret-валидацией, веб-эндпоинты link/status/unlink, хук в рассылку (привязанный telegram получает
  уведомления рядом с email/in-app). Поле `telegram_chat_id` на User (миграция 0024), сервис no-op без
  токена. 21 тест. Реальная отправка/приём webhook — на проде (api.telegram.org из песочницы 403).
  Токен бота — в `.env` (получен, перед продом перевыпустить через /revoke, т.к. был засвечен в чате).

- [x] **Монетизация-каркас.** Сделано в v4.23.0. Поля `plan_tier`/`plan_expires_at` на User
  (миграция 0025), сервис `app/services/subscription.py` (PlanTier enum, effective_tier/is_premium с
  учётом срока — premium истёкший = free, реестр FEATURES + has_feature/available_features), CRUD
  `set_plan`/`grant_premium_days` (накопительное продление), dependency `require_premium` (403 для free),
  эндпоинт `GET /subscription/me`. Без платёжной интеграции — структура под быстрое подключение платно.
  Реестр premium-фич — placeholder-каркас (реальное разделение при запуске монетизации). 16 тестов.

## 4.2. Баги бэка (из заметок Василия — давние, закрываем)

- [x] **Ставка ЦБ не подтягивается.** Закрыто в **v4.24.0** (инцидент INC-CBR-KEYRATE-PARAM). Корень
  оказался НЕ внешним 403, а дефектом кода: cbr.ru ASMX чувствителен к регистру — метод `KeyRate`
  ждёт `<ToDate>` (заглавная T), в коде была строчная `<toDate>` → параметр не распознан → ToDate=
  DateTime.MinValue → пустой диапазон → ноль строк KR → вечный fallback при живом cbr.ru. Фикс
  регистра + durable БД-кэш last-known-good (`cbr_key_rate_cache`, миграция 0026, переживает прод-403
  на датацентровом IP) + устойчивый парсер + самодиагностика (сниппет ответа в `detail`) +
  `refresh_key_rate()` для прогрева. Подтверждено первоисточником cbr.ru. Живая верификация на РФ-IP —
  у владельца. Маскировалось историей INV-CBR-403 (graceful fallback скрыл реальный баг).

- [x] **Письма не приходят.** Закрыто в **v4.25.0** — но это **расследование**, не дефект
  (INV-EMAIL-FIRE-AND-FORGET). Код формирования/отправки корректен (транспорт покрыт 9 тестами).
  «Пусто на почте» в dev — намеренный no-op без SMTP-конфига; фронт честно отдаёт ссылку
  подтверждения прямо в тосте, а не имитирует отправку. Реальный пробел: фоновая отправка
  (`BackgroundTasks`) теряла результат — сбой SMTP на проде был бы виден только в текстовом логе.
  Добавлена наблюдаемая обёртка `email_dispatch.py`: каждая отправка пишет событие
  `email_sent`/`email_skipped`/`email_failed` в `events` (домен вместо адреса — PII). Теперь при
  запуске на проде с реальным SMTP статус отправки виден в аналитике. Живая верификация с реальным
  SMTP — у владельца (P0.2, веха деплоя).

## 4.3. Технический долг (закрываем — всё синхронизировано, параллельных правок нет)

- [x] **P1.4 — mypy/flake8 до нуля.** Закрыто в **v4.25.1**. mypy 14 → 0 (6 файлов; большинство —
  реальные типовые неточности: `TypedDict` для `BANKS` и возврата `_fetch_from_cbr`, defensive-проверки
  `None`, сужение `isinstance`, не косметика). flake8 93 → 0 (87 × E501 + E731/E30x), точечно через
  `autopep8 --select` (не массовый black — §2); повторявшаяся HTML-обёртка писем вынесена в `_CARD_OPEN`.
  611 тестов fast-suite зелёные, регрессий нет.

- [x] **`datetime.utcnow()` → `datetime.now(UTC)`.** Закрыто в **v4.25.2** (вариант A: helper naive UTC).
  Новый `app/utils/time.py::utcnow()` = `now(timezone.utc).replace(tzinfo=None)` — единая точка, поведение
  идентично, колонки/миграции не тронуты (переход на aware — отдельно, ближе к деплою). 78 вхождений в
  17 файлах `app/` + 13 тест-файлов заменены, осиротевшие `datetime`-импорты убраны. +4 теста, fast-suite
  зелёный, DeprecationWarning устранён.

- [x] **Унификация часов: `datetime.now()` → `utcnow()`.** Закрыто в **v4.25.3** (BUG-026, Minor/P0).
  Хвост по времени: v4.25.2 закрыл `utcnow()`, но 13 прямых `datetime.now()` (локальное время сервера)
  остались — на MSK-VPS дают 3-часовой сдвиг с UTC-хранилищем (месячный бакетинг трат, окна «N дней»,
  fallback-даты импорта). Заменены в 5 файлах (`spending`/`statement_parser`/`bank_api`/`obligation`/
  `crud`), контракт naive↔naive. Aware-вызовы в `cbr`/`security` не тронуты. +7 тестов
  (`test_clock_unification.py`), миграций нет, fast зелёный. Полная tz-корректность (зона юзера →
  локальный календарь, поле `tz` у User) — отдельным заходом в слое паттернов.

- [x] **`ExpenseRecord` + поле `date`.** Закрыто в **v4.25.4**. Хранил только `period "YYYY-MM"` —
  блокировало слой внутримесячных/дни-недели паттернов. Добавлено опциональное `date: datetime | None`
  (frozen dataclass, обратно совместимо); при заданной дате `period` выводится из неё (источник истины,
  через `object.__setattr__`). Проброс `date=txn.date` в `spending.py`. `Transaction.date` уже был —
  миграции нет. +5 тестов (`test_expense_record_date.py`), fast зелёный (889).

- [x] **Деприкейшены в тестах.** Закрыто в **v4.25.2** заодно с utcnow: 13 тест-файлов переведены на
  helper `utcnow()`. (`test_time_utils.py` намеренно сверяется с legacy `datetime.utcnow()`.)

## 4.4. Кибербезопасность до прод-уровня

> Состояние: бэкенд-минимум OWASP **закрыт** — bcrypt-хеши (`services/security.py`), Fernet-шифрование
> «в покое» (`EncryptedString`), JWT + account lockout, 4 middleware (CSRF/SecurityHeaders/RateLimit/
> Logging), валидация Pydantic, fail-loud прод-проверка (`validate_production_security`), bandit +
> pip-audit в CI. Полный привязанный к коду срез — `SECURITY.md`. Ниже — остаток до прод-уровня,
> разделённый на **бэкенд-код** (делаем здесь) и **деплой/орг** (веха деплоя — не дублируем, ссылка).

**Бэкенд-код (харденинг — здесь):**
- [ ] **Rate-limit на общий стор (Redis). _(deferred → веха 9: деплой — нужен перед мультиинстансом)_**
  Сейчас счётчик in-memory на инстансе
  (`RateLimitMiddleware._hits`) — на мультиинстансном проде состояние не делится, лимит дырявый.
  Вынести в общий стор для горизонтального масштабирования. До масштабирования (один инстанс) —
  текущего достаточно; делать перед мультиинстансным деплоем.
- [ ] **CSP без `'unsafe-inline'`. _(deferred → веха 8: фронт — inline-JS уедет на React)_**
  Сейчас `script-src 'self' 'unsafe-inline'` — inline-скрипты
  ослабляют защиту от XSS. Перейти на nonce-CSP (или вынести inline-JS). Согласовать с фронтом.
- [x] **JWT: ревокация / выход со всех устройств.** ✓ v4.26.0. Два рубежа поверх stateless-JWT:
  блок-лист по `jti` (`revoked_tokens`) — точечный отзыв на `/auth/logout`; глобальная отсечка
  `users.tokens_valid_since` — `/auth/logout-all` гасит все токены раньше неё одним движением. Проверки
  в `dependencies.get_current_user`, миграция 0027, модуль `app/database/revocation.py`, +10 тестов.
- [x] **Инвалидация сессий при смене/сбросе пароля.** ✓ v4.27.0. Банковская планка (Option A): смена
  креденшелов гасит ВСЕ сессии. `/auth/change-password` сдвигает рубеж `tokens_valid_since` (валит
  другие устройства) + точечно гасит текущий токен по `jti` + чистит auth-cookie (клиент уходит в
  logged-out, переавторизуется). `/auth/reset-password` сдвигает рубеж (старые сессии мертвы, вход
  новым паролем проходит сразу). Бэкенд-only, без координации с фронтом. +7 тестов.
- [ ] **Cookie SameSite + CSRF-токен. _(deferred → веха 8: фронт — double-submit требует клиента;
  `SameSite=lax` + Origin-проверка уже есть)_** Подтвердить `SameSite=Strict/Lax` на auth-cookie; для
  изменяющих запросов рассмотреть double-submit CSRF-токен в дополнение к Origin-проверке (защита, не
  зависящая от наличия Origin у клиента).
- [x] **Ротация ключа шифрования (Fernet MultiFernet).** ✓ v4.28.0. `TokenCipher` → `MultiFernet`: набор
  `TOKEN_ENCRYPTION_KEYS` (PRIMARY первым) шифрует новым, читает старыми. `rotate` +
  `app/database/reencrypt.py::reencrypt_all` (интроспекция всех `EncryptedString`-полей, future-proof) +
  CLI `scripts/reencrypt_keys.py`. Ротация без простоя; обратная совместимость (один ключ/derive/legacy)
  сохранена. +11 тестов.
- [x] **MFA/2FA (TOTP).** ✓ v4.30.0. Второй фактор входа (pyotp). Поток: `/auth/mfa/enroll` (секрет +
  provisioning URI для QR) → `/auth/mfa/confirm` (код активирует, выдаёт 10 recovery-кодов) → при логине
  с включённым MFA выдаётся промежуточный `mfa_pending`-токен вместо сессии → `/auth/mfa/verify` (TOTP
  или одноразовый recovery-код) обменивает его на полную сессию. `/auth/mfa/disable`, `/auth/mfa/status`.
  Секрет — зашифрован (`EncryptedString`), recovery-коды — хеш (bcrypt). Миграция 0028. Заодно закрыта
  дыра: `get_current_user` отвергает любой purpose-токен (mfa_pending/verify/reset/telegram не дают
  сессии). +10 тестов. **Завершает все бэкенд-задачи раздела 4.4.**
- [x] **Security-регрессии в тестах.** ✓ v4.29.0. Новый `tests/test_security_regressions.py` (21 тест):
  security-заголовки на всех типах ответов (200/404/401/422 и CSRF-403 проходит обратно через
  `SecurityHeadersMiddleware`), HSTS только при включении, CSRF-блок на всех изменяющих методах
  (POST/PUT/PATCH/DELETE; Bearer/без-Origin/safe-GET не блокируются), старт-гард. Заодно закрыта дыра:
  `validate_production_security` теперь требует явный `TOKEN_ENCRYPTION_KEY(S)` в production (без него
  ключ деривится из `JWT_SECRET` — нет разделения ключей по назначению, ротация невозможна).

**Деплой/орг (веха деплоя — ссылка, детали в `SECURITY.md` §2/§3b и `docs/DEPLOY.md`):**
- [ ] TLS/HTTPS + HSTS на VPS (артефакты nginx готовы).
- [ ] Реальные прод-секреты (длинные случайные `JWT_SECRET` / `ADMIN_API_KEY` / Fernet-ключ, разные по
  окружениям).
- [ ] Хранение ПДн граждан РФ в РФ + уведомление Роскомнадзора (152-ФЗ).
- [ ] Активация Sentry-DSN (алертинг аномалий) + регулярный `pip-audit` (cadence).

**Порядок вехи 4:** P2.3 уведомления → household-хвост → referral rewards → Telegram-каркас →
монетизация-каркас → баг ЦБ ✓ → баг писем ✓ → техдолг (mypy/flake8 + utcnow + ExpenseRecord + тесты) →
кибербез-харденинг бэка (4.4; деплой-часть — в вехе деплоя).

---
# ВЕХА 5 — Организационное (`v5.0.0`)

> Не код — структура, документация, бизнес-материалы, методички. Цель: всё в одном месте,
> контролируемо, чтобы любой аккаунт и будущая команда поднимали контекст без раскопок.

## 5.1. Репозитории

- [x] **Приватный репо (текущий, куда заливаем сейчас)** — ВСЁ: код, бизнес-инфа, презентации, питчи,
  аналитика, версии. Единое место контроля.
- [x] **Создать публичный репо `finpilot`** — только то, что нужно пользователям / для open-source.
  Продумать правила: что туда заливать, что нет (секреты, бизнес, внутренние разборы — НЕ туда).
- [x] **Сообщать Claude при работе**, какой репо публичный, какой приватный (чтобы не залить лишнее
  в публичный).

## 5.2. Документация продукта

- [x] **Файл «Философия и ценность продукта»** — зачем продукт, для кого, в чём ценность.
- [x] **Папка `docs/` с фул-репортами** — инциденты/расследования/питфолы ЦЕЛИКОМ (не только саммари
  в реестрах). Раньше полные разборы жили вне репо; Василий решил — места почти не добавит, минусов
  нет, пусть лежат в репозитории для самодостаточности.
- [x] **Лого в проект отдельно** — эталоны + промпт генерации в двух вариантах: (а) без исходной
  картинки (б) с картинкой.
- [x] **`.md` анализ конкурентов** (РФ + международный рынок) — суть каждого, функционал, чего у них
  нет, преимущества против FINPILOT, похожие-но-другие по цели.

## 5.3. Бизнес

- [x] **Папка бизнеса** — аналитика, цифры, стратегия, бизнес-модель, питч. Собрать накопленное по
  папкам в одно место.

## 5.4. Исследования и UX

- [x] **Результаты опросника** (385 респондентов) — html, скрипты, картинки + раздел про
  тестирование/интервью/UX-протокол в общий план.
- [x] **Шаблон протокола исследования** — чтобы сырой лист (например, с интервью мамы) автоматически
  трансформировался в отчёт по шаблону, а не оставался необработанным текстом. Claude превращает
  лист → отчёт.
- [x] **Confluence для задач продукта** (опция на подумать) — вместо ведения задач в заметках. Не
  обязательно, зафиксировано как кандидат. _Разобрано в v5.10.0 (§9
  `knowledge/guides/development_process_methodology.md`): решение — оставить ROADMAP+WATCHLOG в репо
  каноном (несущее для мульти-аккаунтной передачи вахты); Confluence отложен как ответ на «живую
  команду людей», при реальной нужде доски — GitHub Projects как слой-представление._

## 5.5. Банки и выписки (исследование + справочник)

> **Закрыто в v5.9.0** (docs-исследование): реестр банков — `knowledge/product/banks_registry.md`;
> справочник форматов + дрейф — `knowledge/product/banks_statements_reference.md`; стратегия покрытия —
> `docs/universal_statement_parser_strategy.md`. Реализация парсера — веха 7.

- [x] **Найти официальный список ~200 банков.** Они проходят аккредитацию/регистрацию (реестр СБП на
  сайте НСПК, лицензии ЦБ) — список публичный. Эквайринговый список СБП (Василий прислал скрин)
  подтверждает масштаб: реально под две сотни.
- [x] **Справочник банков** — банк → какие выписки отдаёт, форматы (PDF/XLS/CSV/1C), особенности,
  пошаговый план «как скачать выписку» для каждого.
- [x] **Алгоритмическое решение покрытия выписок** — НЕ парсер под каждый банк × каждый формат (сотни
  комбинаций, неподъёмно). Universal-парсер с детекцией формата + эвристики на колонки
  (дата/сумма/назначение). Задел уже есть: мультибанк CSV/XLSX + PDF под 5 банков + 1C. Стратегия
  «как покрыть остальные ~190 без ручного парсера на каждый» — расписать здесь, реализация
  тестируется в вехе 7.

## 5.6. Методички (новые)

- [x] **«Подходы к архитектурированию»** — глобальный/фундаментальный подход к первому процессу
  разработки. Два режима: мало времени → MVP/заглушки; много времени → сразу качественно и
  фундаментально. **+ YAGNI** (не выдумывать спекулятивное — то самое напряжение «MVP-first vs
  фундаментально»). **+ как конкретно у НАС в FINPILOT это делается.** + этапы разработки и
  комментарии.
- [x] **«Процесс разработки FINPILOT»** _(готово v5.10.0 → `knowledge/guides/development_process_methodology.md`)_ — формализация того, что мы проходим прямо сейчас: как
  планируем, как принимаем архитектурные решения (как этот цикл согласований), как идёт цикл
  разработки, этапы, как делаем исследования аудитории. То, что Василий назвал «формализуй процесс
  который мы сейчас проходим».
- [x] **«Исследование аудитории»** — как делать: методы, шаблоны протоколов интервью, опросники,
  UX-тестирование, как сырой лист превращать в отчёт.
- [x] **«Принципы разработки»** — YAGNI / TDD / ship-fast vs фундаментально. Либо отдельной методичкой,
  либо влить в философию продукта (решить, куда чище ляжет при реализации).

## 5.7. Юридическое (заготовка)

- [x] **Отдельная инструкция с вопросами юристу** — создана на приоритете 1 до вехи 8
  (`docs/legal_questions_for_lawyer.md`), используется на вехе 8.

## 5.8. Ревизия репозитория и живой роадмап (задачи владельца, 2026-07-02)

- [x] **Полная ревизия файловой системы** — доки против кода, ссылки, нейминг, суть, календарные
  мины в тестах. Закрыто v5.11.0, отчёт — `docs/reports/audits/repo_revision_2026_07.md`
  (тип «Ревизия» заведён в диспетчер отчётов).
- [x] **Обновить все диаграммы + добавить недостающие** _(источники закрыты полностью v5.14.0;
  PNG-превью — регенерация на Mac, чек-лист в `docs/diagrams/README.md` → «Ожидается»)_ —
  инвентаризация устаревания: отчёт ревизии §3. Батчи v5.12.0 + v5.14.0.
  - [x] **Источники (mermaid `diagrams.md`) приведены к коду (v5.12.0):** ER 9→28 таблиц, §1 слои
    (ingestion-пакет ADR-005 + сервисы MFA/subscription/telegram/notifications), §5 внешние
    (cbr-xml-daily → cbr.ru + Telegram), §7 auth (MFA/TOTP двухшаговый + JWT-ревокация jti + mass).
  - [x] **drawio контент-ошибки (v5.12.0):** «21 альтернатива»→66 и «шаг 20%»→10% в 6 схемах;
    C4-container «5 таблиц»→28, «6-этапный»→9-шаговый. XML всех схем валиден.
  - [x] **Регенерация и добавления (v5.14.0):** `10_er_database.drawio` перегенерирован программно —
    28 таблиц, 6 кластеров, 11 FK-связей (канон `diagrams.md` §2); мастер — постраничный свап страницы ER.
    C4-component 06 (+ страница мастера): +`app/ingestion` (engine/providers, ADR-005) + `mfa.py`.
    Символы ∈/≠/∅ выпилены из 01/02 (WebKit-грабля drawio). 7 протухших PNG удалены («устаревшее
    хуже отсутствующего»), чек-лист регенерации — `docs/diagrams/README.md` §Превью.
- [x] **Методичка «живой роадмап» + прикладной механизм приоритизации** _(готово v5.13.0)_ —
  общая методичка `knowledge/guides/roadmap_methodology.md` (Часть I теория + Часть II FINPILOT;
  §15 — механизм) и **живой блок «Механизм живого роадмапа» в шапке этого файла**: алгоритм выбора,
  тай-брейки, указатель «СЛЕДУЮЩАЯ ЗАДАЧА», дисциплина синхронизации с WATCHLOG §0.
- [x] **Предложения Claude: чего не хватает в базе знаний/доках** _(список v5.13.0; владелец
  одобрил все 7; **реализовано v5.15.0**)_: `docs/onboarding.md` · `docs/glossary.md` ·
  `docs/api_contract.md` + снапшот `docs/api/openapi.json` (106 путей, генератор
  `tools/api_snapshot/dump_openapi.py`) · `docs/backup_restore.md` · `docs/pdn_data_map.md` ·
  `docs/slo.md` · физический деплой — `docs/diagrams.md` §9. Исходный список:
  - `docs/onboarding.md` — одна страница входа: порядок чтения для нового разработчика/аккаунта.
  - `docs/glossary.md` — Rt/Lt/Dt/BLR/ПДН/SAW/OCR/СБП/1C-обмен; нужен фронту (веха 8), юристу, новичку.
  - **API-снапшот для фронта** — экспорт `docs/api/openapi.json` + короткий `docs/api_contract.md`;
    веха 6 опирается на зафиксированный контракт, дифф контракта виден в ревью.
  - `docs/backup_restore.md` — PG-дампы: ротация, восстановление, регулярная проверка
    восстановимости (планка финтеха + 152-ФЗ).
  - `docs/pdn_data_map.md` — карта ПДн: какие данные, где лежат, срок, основание
    (юристу к вехе 8).
  - `docs/slo.md` — перф-бюджеты (p95 `/api/planning/calculate` и др.) под нагрузочное вехи 7.
  - **Deployment-диаграмма (физическая)** — nginx/uvicorn/PG/env; добавить при вехе 9.

---
# ВЕХА 6 — Тестирование мат-модели и импорта (`v6.0.0`)

> Фундаментальная и точная проверка ядра. Самый ответственный блок — от него зависит доверие к
> рекомендациям.

## 6.1. Независимая проверка математики (ТРИ шага)

- [x] **(1)–(3) Выполнено протоколом сильнее планового (v6.0.0, июль 2026):** вместо 6 профилей —
  **12 000 синтетических портретов × 4 независимых экспертных движка** (V, M, J, S) + сводная
  независимая экспертная оценка вне проекта. Заключение: ядро честное (математика 9/10, 0 нарушений
  на 12 000, перепроверено независимо), советник имел дефекты G1–G7. Материалы:
  `docs/reports/testing/independent_expert_review_2026_07.md` (заключение),
  `knowledge/model_validation/joined.csv.gz` (эталон, регенерация — генератор v1 seed 20260702).
- [x] **Калибровка по экспертизе → модель v3.1.0** (канон на тот момент `docs/model/history/math_model_v3_1_0.md`): закрыты
  must-fix G1 (насыщение резерва, Lt* 3–6 мес), G2 (кризисный режим при Rt < 0), G3 (гейт «план не
  увеличивает ПДН») + G6 (floor 1 мес) + G5-минимум (инвест-транш с полкой). Согласие с консенсусом:
  **51.9% → 87.4%** при коридоре самих экспертов 85.6–95.6% — модель вошла в коридор
  (`docs/reports/testing/expert_agreement_v3_1_0.md`; стенд `tools/model_validation/`).
- [x] **Продолжение вехи 6 — G4/G5/G7 закрыты в v6.2.0 (модель v3.2.0):** G4 — слой управления
  запасом `app/core/surplus.py` (Avalanche-погашения из излишка → дедлайновые цели → разовый
  инвесттранш; инвариант I14, UI-блок на /planning); полный G5 — `instrument_for_horizon`
  (<12 мес — депозит; 12–36 — депозит+ОФЗ; >36 — профильный микс) в разовых взносах; G7 —
  ПДН при нулевом доходе с платежами = 1.0. Согласие с консенсусом не изменилось (87.4%),
  свип v2 12 000: 0 нарушений I1–I14. Остаток G7 (ре-роутинг остатка целевого бакета,
  ревизия сетки альтернатив) — исследовательская задача, ниже.
- [x] **G7-остаток (исследование) — ✓ v6.6.0 (ADR-009):** ре-роутинг нераспределённого остатка
  целевого бакета в резерв — каскад долг→цели→резерв замкнут, резерв как конечный сток. Стенд-замер
  (12 000 портретов v2): переток у победителя 0.41%, смена рекомендации 0.16%, спасено от испарения до
  194 395 ₽ (`goal_remainder_reroute_benchmark.md`); свип v2 — 0 нарушений I1–I14 (I10 на эффективном
  резерве). Фикс сохранения денег, канон §5.2/§10.4/v3.3.0. **Ревизия сетки альтернатив закрыта:**
  шаг остаётся 10% (`|A| = 66`) — решение владельца, **ADR-008**; замер — `grid_step_5pct_benchmark.md`.
- [x] **Повторная сертификация на датасете v2 — ✓ v6.13.0 (итерация 2 закрыта).** Свежая
  четвёрка независимых движков (V/M/J/S, чистые сессии, 4 × 12 000, бюджет 0 нарушений):
  согласие модели v3.3.0 как сертифицирована — 78.9%; предзарегистрированный в ADR-006
  floor 2.0 подтверждён out-of-sample (сетка 1.5/2.0/2.5/3.0 → 85.7/88.0/88.2/87.6%) →
  **канон v3.4.0** (`RESERVE_FLOOR_MONTHS = 2.0`), согласие **88.0%** внутри коридора
  экспертов 86.8–89.8%. Отчёты: `docs/reports/testing/expert_certification_round2.md`,
  ADR-006 update, `iterations/2/` (comparison, агрегат фидбека, dataset_meta). Стенд:
  `build_joined.py` (новый) + `expert_agreement --version 2`, self-check 12 000/12 000.
- [x] **Сертификация раунда 3 — ✓ v6.15.0 (итерация 3 закрыта, рекалибровки нет, канон v3.4.0).** инфраструктура готова
  в v6.13.0 — генератор v3 (слои A–E: копула/LHS/каталог/adversarial/метаморфические пары,
  `generator_v3.py`, приёмка 25 тестов), канонический размеченный `portraits_v3_seed20260716`,
  слепой пакет `expert_portraits_v3_part1..4` + ключ координатора, бриф
  `docs/model/expert_brief_v3.md` (status=invalid, run_metadata ОБЯЗАТЕЛЕН — пробел р.2).
  **BLOCKED: экспертный прогон владельца чистыми сессиями** (этот чат загрязнён контекстом).
  Стенд раунда 3, сделано в v6.14.0 — **модельная половина закрыта**: outcomes v3 с веткой
  invalid (`portrait_validation`, срез дат параметризован), прогон 12 000 через v3.4.0 без
  падений; D-вердикт 600/600 invalid при 0 ложных срабатываний; метаморфика M1–M5 — 0 нарушений
  статусных инвариантов из 300 пар, M5 доказал однородность степени 1 (сплиты ×10 точно).
  Отчёт: `docs/reports/testing/round3_model_half.md`. Остались (после экспертов):
  build_joined_v3 (сопоставление ПО ПОРЯДКУ строк из-за дублей id, enum + invalid, сверка
  D-вердикта экспертов с манифестом), сравнение lump, порог дорогого долга.
  Итог: согласие 67.5% внутри рассыпавшегося коридора экспертов [64.8; 88.0] (две философии
  долг-vs-резерв); action-adjusted 76.3% (529/703 главной ячейки — то же действие через lump);
  триггер floor НЕ сработал. Гипотезы р.2 разрешены: burn-Lt отвергнута контрфактом (67.5→64.1),
  lump-стенд реализован (model_lump), порог переформулирован. R3-F1 (нож нуля) починен — FLOW_EPS.
  Отчёты: `expert_certification_round3.md`, `iterations/3/` (+ архив итерации 1 занесён).
- [x] **Генератор v4 + бриф v4 + систематизация артефактов — ✓ v6.16.0.** ТЗ раунда 3
  выполнено полностью: продуктовая типизация кредитов (6 продуктов, ПСК-кап 292%),
  k-спека = факт, имя↔сумма цели, точная арифметика границ, киты 5–10 млн отдельно от
  стресса 2e9, семейства C по 400 (мощность ≥385), слой D v2 (составные + near-miss +
  рандомизация), валидные дубли id в слое C, E из 7 отношений с неравномерными
  возмущениями. 35 приёмочных тестов. Каталог артефактов с провенансом находок
  (`artifact_catalog.md`) + методичка тест-сетов в репо + история промптов.
- [x] **Раунды 4 и 5 — экспертные прогоны проведены и закрыты** (v6.17.0 и v6.24.0).
  Р.4: согласие 79.4% action-vector, внедрён G8 → канон v3.5.0. Р.5: 78.63% при коридоре
  экспертов 58.18–79.99, ни одна предзарегистрированная гипотеза не подтвердилась —
  калибровки нет, канон остался v3.5.0 (обоснование — блок «Статус сертификации» в
  `docs/math_model.md`). Веха тестирования матмодели — **на паузе**; условия
  возобновления `docs/model/model_testing_next_iterations.md`, объём работ до потолка
  качества `docs/model/model_completion_plan.md` (85.7 → ≈91.3: 11 чистых сессий и ~27
  батчей за год, полных раундов в текущем формате нужно ноль).
- [x] **1000+ кейсов** — выполнено с запасом: 12 000 портретов в экспертизе v6.0.0 (см. §6.1)
  поверх свипов v5.17.0 (2000 + 10000). Генератор v2 (v6.0.0) откалиброван по фидбеку экспертизы:
  долги через целевой ПДН (corr доход–платежи > 0.3 против ~0.02 у v1), реалистичные аннуитетные
  сроки, дефицит <= ~30% сета, adversarial-типы (pdn_boundary, rate_at_bench, deadline_now,
  kopeck/giant_scale). 6 эталонных user-cases — отдельно для показа людям (остаток ниже).
  - [x] **Инфраструктура + первый свип (пред-работа, v5.17.0, приказ владельца):**
    `tools/portrait_testing/` — генератор синтетических портретов (10 граничных типов),
    11 исполняемых инвариантов канона (|A| кусочно 66/11/1 · Rt'≥0 · ПДН≤0.40 · суммы долей ·
    fail-loud · веса профилей · Avalanche ≥ r_bench по убыванию · детерминизм · SES/Monte-Carlo
    p10≤p50≤p90 · сверка Lt stock-based/Dt · конечность), раннер с отчётом. **Прогнано 2000
    (seed 20260702) + контроль 10000 (seed 77) — 0 нарушений, 0 падений**; 7 property-тестов в
    fast-контуре. Отчёт: `docs/reports/testing/portrait_sweep_2026_07.md`. Остаток пункта —
    реальные портреты (6 эталонных) и разбор вопросов §6.3 в самой вехе 7.
- [x] **Больше профилей риска** — все 5 профилей покрыты экспертизой (риск-срезы в заключении:
  дифференциация реальна, G6-край закрыт floor'ом); property-тесты и инварианты I6/I13 держат канон.

## 6.3. Вопросы по модели (исследовать при тестировании)

- [x] **Совет при отрицательном свободном ресурсе** — закрыто в v6.0.0 кризисным модулем
  (`app/core/crisis.py`, канон §12 v3.1.0): балансовый ход «закрыть кредит из ликвидности» (если
  разворачивает поток при сохранении floor 1 мес), сокращение расходов ровно до нуля дефицита,
  **потолок трат** `max_affordable_expenses` (ответ на «сколько можно тратить»), заморозка целей,
  реструктуризация/рефинансирование/каникулы самого дорогого кредита, запас хода в месяцах.
  Охват: 9697/9697 дефицитных портретов с действиями (recoverable: 387). Рефинансирование —
  возможно и включено (исследование дало «да»). **Кейс Василия разобран и вшит живым тестом**
  `tests/test_crisis.py::TestHighInterestCreditCardCase` (переименован в v8.19.2 —
  правило безымянности публичного зеркала, `docs/public_mirror_manifest.md`): на
  стабильных 20к — дефицит 2к/мес, потолок
  трат 14к/мес, runway 2 мес, реструктуризация кредитки; с донорством (+25к) — поток положительный,
  лавина бьёт в кредитку (39.9% >> r_bench). UI: блок кризис-плана на /planning.
- [x] **Валидация графика прогноза.** ✓ v6.4.0. Оказалось: НЕ баг арифметики, а (1) единичное
  сглаживание по одной точке даёт плоскую линию и (2) роут прогноза выбрасывал историю. Исправлено:
  демпфированный Holt + помесячная капитализация баланса + история из РЕАЛЬНЫХ транзакций
  (`build_monthly_history`, скользящие 30-дневные корзины). Подтверждено end-to-end на 10 портретах —
  форма разная (рост/падение/сезон), константной дельты нет. INV-FORECAST-STRAIGHTLINE закрыт; разбор —
  `docs/reports/investigations/forecast_straight_line_investigation.md`, канон → v3.3.0.
- [x] **Шаг дискретизации.** ✓ v6.4.0 замерено · ✓ v6.5.1 **решено: остаёмся на 10%** (ADR-008).
  Массовый прогон 2000 портретов: в 90.9% победитель тот же, прирост полезности +0.07 п.п. (p95 +0.4),
  главный совет меняется в 0.8% случаев на ~1 000 ₽ — при цене ×3.5 латентности, смены канона (`|A|=66`
  в трёх файлах модели, диаграммах, SRS, инварианте I1, снапшотах), рассинхрона с ВКР и статьями и
  полной пересертификации. Отчёт: `docs/reports/testing/grid_step_5pct_benchmark.md`.
  Попутно закрыта латентная мина: id альтернатив `f"a{d}{r}{g}"` коллидировал при мелком шаге
  (29 дублей из 231 на 5%) — теперь fail-loud + разделитель при `steps > 10`; на канонных 10% формат
  id не менялся (снапшоты живы).
- [x] **Nullable-дедлайн целей (миграция).** ✓ v6.5.0. Миграция `0029` (batch-режим), семантика
  бессрочной цели в каноне (§11.2 v3.2.0: `τ_s = ∅ ⇒ u_s = 1`), сквозь все слои (модель, схемы,
  B2B-контракт, CRUD, ядро, spending-advice, UI), костыль «+10 лет» в демо снят. 14 тестов на матрице
  SQLite + PostgreSQL 16. **Снятие NOT NULL вскрыло три латентных дефекта** (500 на `/spending-advice`,
  расхождение порядка SQLite↔PG, ложь «до дедлайна ~12 мес» — последний уже работал в демо):
  `docs/reports/incidents/inc_null_deadline_latent_defects.md` (INC-NULL-DEADLINE), правило — PIT-010.
- [x] **Переименовать файл модели в `math_model_v3_3_0.md` — ✓ v6.7.0.** Файл содержал контент
  v3.3.0 (прогноз Holt+капитализация, G7-остаток/ADR-009), имя приведено в соответствие. Живые
  указатели обновлены (хаб `model/README`, superseded v3_1_0, таблица ROADMAP, invariants,
  project_meta); revision-gate CLEAN, исторические ссылки в CHANGELOG/§3 не тронуты.

## 6.4. Импорт выписок (тестирование)

- [x] **Вшить эталонные шаблоны PDF/XLS/CSV в продукт — ✓ v6.7.0.** `app/data/statement_templates/`
  (генератор + MANIFEST): по образцу на каждый формат/парсер (Тинькофф/Сбер CSV, универсальный
  знаковый + split, XLSX, PDF-текст Тинькофф/Сбер, PDF-сетка ВТБ/Райффайзен, 1C). Синтетические,
  формат 1-в-1; PDF рендер DejaVuSans (кириллица + ₽), round-trip через pdfplumber.
- [x] **Тесты корректности считывания — ✓ v6.7.0.** `tests/test_statement_templates.py` (15 golden):
  каждый шаблон → точные операции (дата/тип/сумма/описание) + роутер. Корпус сразу нашёл дефект в
  образце Райффайзена (Debit со знаком минус в реальном формате). Отчёт `statement_import_coverage.md`.
- [x] **Б3: общий движок табличных выписок — ✓ v6.12.1.** Карта колонок по шапке была написана
  дважды (ВТБ, Райффайзен) почти дословно → вынесена в `_column_map(table, roles, date_cells)`,
  парсеры банков стали обёртками. Рефакторинг без изменения поведения (163 теста, 7/7 реальных файлов
  сверяются как прежде). Задокументирован путь добавления банка — `docs/universal_statement_parser_
  strategy.md` §7 — вместе с правилами, добытыми на реальных данных: только основы слов в маркерах,
  знак из семантики колонки, банк по содержимому файла. **Новый банк теперь = словарь ролей + резолвер
  + маркеры, а не новый парсер.**
- [x] **Сверка CSV — ✓ v6.12.0.** Главная дыра ретроспективы закрыта: CSV (12 614 операций в файле)
  не проверялся ничем. Контроль полноты разбора вскрыл ТРИ невидимых дефекта: терялись все приходы
  Райффайзена (синоним «поступление» не подстрока «Поступления»), вся выписка читалась доходом
  («amount» ловил колонку «(credit)»), строка метаданных принималась за шапку. Плюс наивный `float()`
  у Тинькоффа → `_num`. Открытие: «выписка мамы» = **Альфа-Банк**, её CSV несёт контрольные итоги →
  сверка 224 805.37 / 221 195.56 сходится. 195 тестов. **Осталось:** суммы Тинькофф-CSV сверить
  по-прежнему не с чем (только полнота разбора).
- [x] **Property-based генератор выписок (Б2) — ✓ v6.11.0.** `tools/statement_templates/synth.py`
  рендерит все реальные шаблоны (ВТБ A/B, Райф RU/EN/без-№, Тинькофф CSV, универсальный, 1C) с
  фаззингом форматирования; 16 property-тестов, ~625 синтетических выписок за прогон, включая
  многостраничные по 60 операций. **Граница метода зафиксирована:** генератор знает те же шаблоны,
  что и парсер, поэтому синтетика доказывает устойчивость ВНУТРИ формата, а не покрытие банков.
  Плюс два документа: `docs/reports/retrospectives/statement_parser_process.md` (ретроспектива всего
  процесса: дефекты, мои ошибки, дыры, ограничения) и `docs/statement_parser_audit_protocol.md`
  (протокол независимого аудита: holdout, ручной ground truth, метрика «тихие ошибки = 0»).
  **Дальше: Б3** — обобщить трижды написанный header-driven движок (ВТБ/Райф/универсальный) в один
  + словарь маркеров банков; закрыть сверку CSV (главная дыра: 12 614 операций Тинькоффа сверить
  нечем); независимый аудит на holdout.
- [x] **Измеритель точности (Б1) — ✓ v6.10.0.** Точность парсера нельзя пообещать — её можно только
  измерить на каждом файле контрольными данными самой выписки. Добавлены свидетели: итоги Сбера
  («Пополнение»/«Списание»), итоги Райфа из таблицы («Всего поступлений») для шаблона без «№», счётчик
  «Количество операций», балансовое тождество (ВТБ/Тинькофф). **Сверено 10 из 11 реальных PDF (было
  6)**; у ВТБ и Тинькоффа по три свидетеля. К Сберу тождество осознанно не подключено — не сходится у
  самого банка. Тесты 37, покрытие 94.5%. **Дальше по плану:** Б2 — property-based генератор выписок
  (round-trip, тысячи кейсов на шаблон, фаззинг краевых случаев); Б3 — обобщить три header-driven
  парсера (ВТБ/Райф/универсальный) в один движок + словарь маркеров банков, чтобы новый банк стоил
  строк в словаре, а не нового парсера.
- [x] **Аудит на реальных выписках, часть 2 — ✓ v6.9.0.** Райффайзен: парсер существовал, но на живых
  Райф-файлах не запускался (стенд угадывал банк по имени, а `'райф' in 'Райф 1.pdf'.lower()` = False —
  macOS пишет имена в NFD). У Райфа три шаблона (RU/EN зеркальны, третий без колонки «№») → 0 → 14
  операций на двух файлах. Сбер: «Установка кредитного лимита» +110 000 ₽ шла доходом (220 000 ₽
  фиктивного дохода) → фильтр нецаш-операций. NFC в `_norm`. **Определение банка по содержимому** в
  проде (11/11 реальных PDF). **Сверка с контрольными итогами банка вплетена в импорт** — вариант B
  (предупреждать, не блокировать): `app/services/statement_reconcile.py` + бейдж в UI; ВТБ/Райф/Тинькофф
  сходятся до копейки. Новые типы справок (Сбер 8/9, Газпром рус+англ, Альфа), матчинг без пробелов.
  Тесты 24 + 60, покрытие 92.7% / 95.2%. **Открыто:** контрольные итоги Сбера (числа не встают рядом с
  меткой «ИТОГО ПО ОПЕРАЦИЯМ» — нужен разбор по координатам); выписки Альфы и Газпромбанка С ОПЕРАЦИЯМИ
  (в наборе только справки и пустой счёт); OCR-канал для PDF-сканов.
- [x] **Аудит парсера на РЕАЛЬНЫХ выписках — ✓ v6.8.0** (по требованию владельца: «точность 100%, это
  основной функционал»). 6 банков, 64 живых файла. Исправлено: ВТБ — колонки по шапке (три реальных
  шаблона со сдвигом; описания всей июньской выписки были заглушкой «Операция», текст описания
  вычитался как комиссия), кешбэк в колонке «Комиссия» уходил в расход, ВТБ давал 0 операций, 1C —
  cp1251, справки распознаются вместо silent-0. Доказано сверкой с контрольными итогами банка (июнь 54
  оп, май 47, ВТБ 1 — 3; баланс замыкается). Инструмент `tools/statement_audit/reconcile.py`, тесты
  (28), отчёт `docs/reports/testing/statement_parser_real_data_audit.md`. **Открыто (следующий батч):**
  автоопределение банка по содержимому в проде, сверка с итогами внутри импорта, парсеры Альфа и
  Газпромбанк, классификация Сбер 8/9, OCR-канал для PDF-сканов.
- [x] **Покрытие ~200 банков — ✓ v6.7.0.** Стратегия вехи 5.5 реализована/подтверждена: универсальный
  парсер читает незнакомый банк (произвольный порядок/названия колонок, RU+EN шапка) без выделенного
  парсера (`TestUniversalCoverageStrategy`) → длинный хвост ~190 банков. MT940/PDF-скан — вне корпуса
  (парсеров нет), отдельный канал.

## 6.5. Волна 0 — карта качества матмодели (после паузы раундов сертификации, решение владельца 2026-08-12)

> Раунды сертификации на паузе (§6, интеграл 85.7/100) не потому что модель не готова, а потому
> что раунды перестали давать информацию по своей цене. Но карта качества
> (`docs/model/model_quality_scorecard.md` §12) называет три пункта, которые дёшево поднимают
> интеграл **без единого раунда и без правки решающего ядра** — «Волна 0»,
> `docs/model/model_completion_plan.md` §4. Владелец сделал точечное исключение из заморозки
> математики (приказ §0 выше) ровно для этих трёх; лавина долгов (§8.4) заморозку не снимает.

- [x] **0.2 — Починка метаморфического набора тестов.** ✓ v8.11.1. `M2_rate_all`/`M3_deadline_all`
  (`tools/portrait_testing/generator_v4.py::E_RELATIONS`) дают L1 = 0 на всех парах — равномерный
  сдвиг сохраняет относительный порядок Avalanche/срочности by design, не находка о модели.
  generator_v4.py/generator_v5.py — замороженные артефакты сертификации (реюз кода между ними
  уже есть, v5 наследует v4; трогать нельзя — сломает бит-в-бит воспроизводимость раундов 4/5,
  ось 8 карты). Фикс — новый генератор/модуль, не правка frozen-файлов. Testing-инфраструктура,
  `app/core/ranking.py` не трогается, без правки `docs/math_model.md`.
- [x] **0.3/0.4 — Вклад критериев SAW + контрфактное объяснение.** ✓ v8.12.0. `app/core/ranking.py` уже
  считает `Rt_norm/Lt_norm/Dt_norm/Si_norm` в `alt["scores"]`, веса профиля — нет, не
  персистируются; добавить `weighted_scores` аддитивно (не меняет `utility`/сортировку).
  `app/core/recommendation.py::explain_alternative()` — добавить текст про доминирующий критерий
  и контрфакт (сравнение с `top3[1]`). Решить: расширять нетипизированный `dict[str, Any]` ответа
  `/planning/calculate`, или наконец завести Pydantic-схему (см. комментарий в
  `frontend/src/entities/plan-summary/model/types.ts`). Поднимает ось «объяснимость» с 74 до
  **85 максимум** — выше нельзя честно без внешнего замера (ни один из 5 раундов не проверял
  объяснения, только решения).
- [x] **0.1 — Инфляционная индексация целей (горизонт > ~3 лет).** ✓ v8.13.0, канон v3.6.0.
  ADR-013, стенд `goal_inflation_benchmark.py` (12 000 портретов, локализация подтверждена). Канон §11.3: $\Delta_s$ считается
  в номинале, цель на 10–30 лет (пенсия, образование детей) занижена. Реально меняет $S_n$ →
  канон `docs/math_model.md` v3.5.0 → **v3.6.0** (MINOR — добавление входной величины в
  существующую формулу, тот же класс, что G4/G5/G7/G8, не архитектурная правка уровня 2.x→3.0.0).
  Нужен ADR (прецедент ADR-006/ADR-009) + бенчмарк на портретах: меняет argmax ранжирования.
- [x] **Пересчитать `docs/model/model_quality_scorecard.md` после Волны 0.** ✓ v8.16.1,
  `model_quality_scorecard.md` §16. 0.1/0.2/0.3/0.4 (Волна 0) + ADR-014/016 закрыты — три оси,
  не зависящие от эксперта, пересчитаны по замеру (не интерполяцией, §0 карты): полнота охвата
  62 → **80** (3 из 7 пунктов закрыты полностью, 1 частично, 1 снят с доски как объявленная
  граница, 2 открыты — Барьер 1, нужен эксперт), адекватность 82 → **86** (инфляция закрыта),
  воспроизводимость 96 → **98** (кросс-платформенная сверка, см. п. 0.5 ниже). Интеграл
  85.74 → **87.18**. Ось «объяснимость» (74) НЕ тронута — ждёт слепой оценки (см. пункт выше),
  самооценка без замера недопустима.
- [x] **0.5 — Кросс-платформенная сверка прогонов (Linux/Intel Mac/CI).** ✓ v8.16.1.
  `tools/model_validation/cross_platform_check.py` — полный `run_planning()` (не только тесты)
  на 12 000 портретов, Intel Mac (x86_64, macOS) и Linux (`python:3.13-slim`, тот же образ, что
  CI). **Агрегатный sha256 идентичен побайтово** на обеих платформах — воспроизводимость по
  seed подтверждена межплатформенно. Отчёт — `docs/reports/testing/cross_platform_check.md`.
  Не проверено (за рамками исходного пункта): Apple Silicon/arm64.
- [x] **Протокол проверки объяснимости — пакет готов к раздаче.** ✓ v8.13.4.
  `docs/model/explainability_protocol.md` + `docs/model/explainability_review_covering_message.md`
  (сопроводительное письмо) + `docs/reports/testing/explainability_review_material_blind.md`
  (40 портретов, 7 веток, без служебной разметки). **Осталось:** саму слепую оценку провести в
  отдельной чистой сессии/аккаунте (эта сессия не может — полная память проекта): скопировать
  письмо + приложить `_blind.md` в чистый аккаунт; результат — короткий отчёт в
  `docs/reports/testing/` (имя — по факту создания), затем пересчёт оси «объяснимость» в
  `model_quality_scorecard.md`.
## 6.6. Расширение охвата предметной области выше 75 — классификация по типу барьера (2026-08-13)

> Карта называет 7 непокрытых зон (`model_quality_scorecard.md` §10). Каждая упирается в РАЗНЫЙ
> тип барьера — не одна и та же причина «нет времени». Разведка кода перед этим разделом —
> `docs/reports/investigations/` (искать по коммиту, отдельного файла на разведку не заводилось,
> итог занесён прямо сюда).

**Барьер 1 — нужен экспертный консенсус (спорная позиция, не факт).**
- Выбор инструментов/диверсификация — конкретные фонды/секторы внутри доли акций
  (`app/core/investment.py::EQUITY_SHARE_BY_PROFILE`) требуют профессионального суждения о
  рынке, не выводятся из логики. **Бриф эксперту (зафиксирован 2026-08-13, не задан):**
  (1) для доли «акции» по каждому профилю 1–5 — какой тип инструмента дефолтно рекомендовать
  частному инвестору РФ в текущих условиях (санкции, заморозка недружественных активов):
  широкий индексный фонд на MOEX или секторальная диверсификация; (2) для доли «облигации» —
  ОФЗ / корпоративные / фонды денежного рынка, минимальные требования к кредитному качеству;
  (3) нужна ли модели ребалансировка портфеля или «купил и держи» в рамках профиля
  достаточно; (4) стоит ли предупреждать о концентрации на одном брокере (по аналогии с уже
  существующей нотой АСВ про разные банки); (5) меняется ли оптимальный набор инструментов на
  ИИС vs обычном счёте (высокооборотные стратегии выгоднее в налоговой обёртке). Нужна
  разметка КАТЕГОРИЙ инструментов (не конкретные бумаги/ISIN) — тот же уровень детализации,
  что уже есть для депозит/облигации/акции.
- Страхование/защита от шоков — рекомендация конкретных страховых продуктов/сумм требует
  знания реального рынка страхования, не изобретается из формул.

**Барьер 2 — было заблокировано явным решением владельца, снято 2026-08-13, закрыто в v8.16.0.**
- [x] Рефинансирование структуры долга (лавина долгов, второй пункт Э5) — «нужна новая,
  несертифицированная математика вне канона» (WATCHLOG v8.8.0). Заморозка от 2026-07-02 для
  этого пункта снята явным решением владельца 2026-08-13, реализовано тем же батчем ✓ v8.16.0,
  **ADR-016** (`docs/reports/adr/adr_016_debt_avalanche_amortization_schedule.md`), канон
  v3.7.0 → **v3.8.0**. `app/core/amortization.py::build_debt_amortization_schedule` — полный
  помесячный график (baseline «без досрочки» vs накопительная лавина, экономия на процентах и
  срок «свободы от долгов»), кап `MAX_HORIZON_MONTHS=360`. **Красная линия (жёстко проверена
  тестом): `Rt`/`Dt`/`crisis_plan` не читают график погашения вообще** — по конструкции кода
  (`amortization.py` не импортируется `ranking.py`/`crisis.py`) +
  `tests/test_crisis.py::test_crisis_plan_unaffected_by_debt_schedule_adr_016`. Стенд на 12 000
  портретах: 0 исключений/зависаний (терминация подтверждена), график посчитан для 23.29%,
  `months_saved` медиана 17, `interest_saved` медиана 125 056 ₽ — замер:
  `docs/reports/testing/debt_avalanche_benchmark.md`.

**Барьер 3 — не нужны ни эксперты, ни данные пользователей, но не спроектировано и разный по
готовности:**

- [x] **Нерегулярный/сезонный доход — «полный модуль» закрыт.** ✓ v8.15.0, ADR-015
  (`docs/reports/adr/adr_015_irregular_income_floor.md`), канон v3.6.0 → **v3.7.0**. Владелец
  выбрал полный модуль (не диагностический индикатор из `model_completion_plan.md` §Волна 2
  п.2.6 — та оценка «3 батча» относилась к более узкой цели). Floor резерва растёт с
  волатильностью дохода ($CV_{\text{income}} > 0.3$ по 8-мес. истории → до +1 мес) — зеркально
  G8 (токсичный долг снижает floor), в обратную сторону; токсичный долг приоритетнее при
  совпадении. **Красная линия (жёстко проверена тестом): $R_t$/$D_t$/кризисный режим (I12) не
  читают историю дохода вообще** — она влияет только на floor, никогда на детектор кризиса.
  Стенд на 12 000 портретах: затронуто 19.05%, сменился победитель 0.97%, **локализация
  подтверждена** (0 портретов без волатильности сменили победителя).
- [x] **Налоговые режимы (вычеты, ИИС) — тип А закрыт реальным расчётом.** ✓ v8.14.2 (ADR-014,
  вариант B — общая нота) → ✓ v8.18.0 (ADR-017 — реальная цифра для типа А).
  `app/core/investment.py::estimate_iis_deduction` — `eligible = min(amount, max(0, 400000 -
  contributed_this_year))`, `deduction = 0.13 * eligible`; новый вход `user_prefs.iis_type`/
  `iis_contributed_this_year` (миграция 0033). Обходит архитектурный риск ADR-008 (лимит года
  как пороговый критерий SAW): расчёт — диагностика ПОСЛЕ ранжирования для уже выбранной
  альтернативы, не входит в utility/сортировку — красная линия подтверждена тестом
  `test_iis_status_does_not_affect_decision_adr_017`, решётка 66 альтернатив не переоткрыта.
  **Осталось (сознательно не сейчас):** тип Б/ИИС-3 численного расчёта не получают — переходные
  положения закона по ИИС-3 не кодируются без риска выдать неверную цифру (ADR-017, вариант D,
  отклонён); полноценный пересчёт эффективной доходности `r_bench`/Avalanche-порога с учётом
  налогового статуса (более глубокий вариант, чем сделан) — по-прежнему отдельное решение, если
  реальная телеметрия (когда включится, п. 0.6) покажет значимую долю пользователей с ИИС-3.
- [x] **Семейный/совместный бюджет — решение принято 2026-08-13: без общего SAW-решения.**
  Архитектура видимости (`household_id`, P3.7, миграция 0022) уже полностью реализована и
  работает — члены household видят общие транзакции/цели/долги друг друга. Вопрос был про
  ДРУГОЕ: агрегировать ли несколько человек в одно ранжирование (общий $R_t$, приоритет между
  людьми). **Владелец: продукт — про личные финансы, семейный профиль — надстройка, не замена.**
  Решение — каждый член household считает свой личный план независимо, тем же SAW-ядром, что и
  сейчас (`ranking.py`/`planning.py` не меняются, канон не расширяется). «Семейное» — это
  агрегированная витрина поверх уже готовых личных планов (сумма показателей для отображения),
  не единое решение о распределении денег. Отклонённые варианты (общий пул денег с приоритетом
  по категории или по роли owner/member) сознательно не выбраны — оба вводят продуктовую
  политику «чьи деньги/цели важнее», которую не с кем провалидировать для доп-фичи. Реализация
  витрины (дашборд-виджет) — фронтенд-задача, встаёт в очередь Э5/Э8.4, матмодели не касается.

**Что дальше.** Барьеры 2 и 3 закрыты целиком: доход и налоги — расширением канона (ADR-014/015),
семейный бюджет — решением без изменения канона, лавина долгов/рефинансирование — новым
модулем вне канона (ADR-016, v8.16.0). Барьер 1 (инструменты/страхование) остаётся заблокирован
внешним рыночным знанием — единственная нерешённая часть §6.6, ждёт владельца.

## 6.7. Идеи из опроса ЦА (2026-08-13) — блокер: партнёрство/доступ к внешнему API, не эксперт и не данные

> Целевая аудитория в опросе называла эти интеграции желательными. Отдельный тип барьера от
> §6.6: не нужен ни эксперт, ни данные — нужен реальный доступ к чужому API, обычно через
> юрлицо/аккредитацию. Прецедент в коде — Plaid (`app/api/routes_plaid.py`): весь код готов,
> выключен флагом, пока не заведены реальные `PLAID_CLIENT_ID`/`PLAID_SECRET`. Рисовать
> UI сейчас, не зная формы реального API партнёра, — плохая ставка: почти наверняка придётся
> переделывать. Первый шаг по каждому пункту — не код, а выяснить, доступен ли вообще API
> продукту такого масштаба.

- [ ] **Вход через Госуслуги (ЕСИА).** Убрал бы трение регистрации/подтверждения личности.
  Требует официальной аккредитации в ЕСИА — процесс с юрлицом, не разовая интеграция.
- [ ] **Данные из личного кабинета ФНС** (налоги, штрафы — автоматический учёт вместо
  ручного ввода). Требует партнёрского доступа к API ФНС — обычно не публично открыт,
  нужно выяснять условия отдельно.
- [x] **Компаунд-иллюстрация для инвестиционного транша.** ✓ v8.19.0.
  `app/core/investment.py::project_compound_growth` — $FV = amount \cdot (1+r)^t$ на
  3 ставках × 3 срока (5%/8%/12% × 5/10/20 лет), не привязана к риск-профилю/инструменту.
  Попутно найден и починен реальный баг из v8.18.0: `iis_deduction_estimate` считался в коде,
  но не был объявлен в Pydantic-схеме ответа — `response_model` тихо резал поле из настоящего
  API-ответа (юнит-тесты на `run_planning()` этого не ловили, минуя сериализацию). Новый
  API-тест через `TestClient` закрывает класс регрессии.

---
# ВЕХА 7 — Юридическое (`v7.0.0`)

> Все документы и соответствие доведены до конца. ОБЯЗАТЕЛЬНО ревью реальным юристом — это не зона
> ответственности разработки.

- [x] **Все юр-документы доведены до конца** (v6.25.0): политика обработки ПДн, пользовательское
  соглашение, политика cookie, три раздельных согласия (ПДн, финансовые данные, рассылка),
  UI-тексты. Плейсхолдеров нет, дата вступления в силу 29.07.2026. Пакет — `docs/legal/`
  (`md/` для сайта, `docx/` официальные экземпляры).
- [x] **Соответствие законам разобрано и переведено в инженерные требования** — `docs/legal/README.md`
  §2: десять требований L1–L10 с адресом в коде и статусом. **Главная дыра — L1:** документы обещают
  три раздельных согласия с фиксацией даты и времени, а в модели одно поле `consent_at` и один флаг;
  нужна таблица согласий с типом, версией текста и моментом отзыва. Забирает веха 8.
- [ ] **Хранение ПДн в РФ + уведомление Роскомнадзора** (L8) — веха 9, зависит от выбора хостера.
- [ ] **Проверка живым юристом** — внешняя и медленная, запускать параллельно, не дожидаясь фронта.
  Вопросы готовы: `docs/legal_questions_for_lawyer.md`.
- [ ] **Сравнить с требованиями Apple App Store** — актуально только при появлении мобильного
  приложения; сейчас не блокирует.

**Контекст по статусу (из обсуждения, не юр-консультация):** продукт на старте бесплатный (без
монетизации — проще). Аккредитация/регистрация как банк НЕ нужна (FINPILOT — СППР, информационная
поддержка, не платежи). Открытый вопрос — статус оператора ПДн (физлицо / самозанятый / ИП) и
минимизация публичности личных данных. Решается с юристом.

---
# ВЕХА 8 — Фронтенд (`v8.0.0`)

> Тотальный редизайн + переход на современный прод-стек + вынос на фронт ВСЕХ возможностей бэка,
> которые сейчас живут только в API. Ощущение Василия: бэк уже мощный (4.x), а фронт застрял на
> уровне 2.x и не показывает пользователю и доли возможностей.

## 5.9. Научный трек в базе знаний (ретро-добавление, приказ владельца 2026-07-02)

- [x] **Папка `knowledge/science/`** — статьи, методички, переписка с редакциями _(готово
  v5.16.0)_: статус публикаций (принятая МНТК/РИНЦ + 4 статьи в работе, вкл. пакет КИМ с
  replication package), финальные рукописи, ответы редакций (БИ-отказ и др.), гайды
  (ГОСТ/Scopus/РИНЦ/степени). 42 файла, ~8.5 МБ, sha-дедуп; индекс и что-не-вошло —
  `knowledge/science/README.md`. **Private-only: в whitelist публичного зеркала не добавлять.**
  ⚠️ DOC-03: нормативная нормализация из статьи КИМ в коде не реализована — кандидат в v3.1
  при принятии статьи.

## 8.1. Стек (зафиксирован — см. `docs/reports/adr/adr_001_frontend_stack.md`)

- [x] **Решение по стеку принято:** React + TypeScript, бэк остаётся как REST API. Полный набор:
  Vite, TanStack Query, TanStack Table, React Hook Form + Zod, Radix UI / shadcn/ui, Recharts / visx,
  KaTeX. Тесты: Vitest + Playwright (chromium). Реализация — через Claude Design + дизайн-агенты.
  Эстетика — по `docs/ui_visual_design_brief.md`.

## 8.2. Базовые задачи фронта

> **Бэк готов и проверен.** Полный прогон 1473/1473 на SQLite, дельта 141/141 на PostgreSQL,
> ревизионный гейт CLEAN (v7.6.0). Оценка готовности к вехе — **86/100**, разбор с осями —
> `docs/reports/audits/full_revision_before_frontend.md`. Недостающие пункты — это состояние
> самой фронтовой инфраструктуры, которую и предстоит создать.
>
> **Три задачи до первой строчки React** (полдня, ни одна не блокирует старт):
> выверить OpenAPI под нужды фронта · ~~починить 3 ошибки mypy в парсере~~ ✓ v8.34.0 ·
> разобрать 53 документа без входящих ссылок.
>
> **Что бэк уже отдаёт фронту готовым:**
> текст дисклеймера 39-ФЗ приходит полем `disclaimer` в самой `RecommendationResponse` (L5) ·
> отказ гейта согласий — 403 с машиночитаемым телом (`code`, `consent_type`, ссылка на документ),
> по нему строится экран согласия · все шесть юр-документов открываются и покрыты тестом (L7) ·
> реестр документов с редакциями — `GET /api/legal/documents`.

- [x] Поднять React+TS проект (Vite), настроить тесты (Vitest + Playwright) _(готово v8.4.0,
  Э2 плана вехи)_: `frontend/` — Vite + React 19 + TS, TanStack Router/Query, Zustand, Radix UI,
  Recharts + KaTeX; Vitest и Playwright настроены и зелёные (build → preview → браузер).
- [x] Токены дизайн-системы, типографическая шкала, табличные цифры везде, где деньги
  _(готово v8.4.0)_: `frontend/src/app/styles/tokens.css`, направление «Спокойный». Именование
  `--c-*`/`--r-*`/`--shadow-*`/`--fs-*`/`--sp-*` — по решению задачи «поверх существующей
  структуры токенов» (docs/ui_visual_direction.md), не `--surface-*`/`--text-*`/`--space-*`/
  `--radius-*` из этого пункта — если семантическое переименование всё ещё нужно, это отдельная
  задача, не часть Э2. `font-variant-numeric: tabular-nums` — глобально на body.
- [x] Перенести существующие экраны (дашборд, операции, обязательства, цели, планирование) на компоненты.
  _(готово v8.7.0. дашборд — v8.5.0 (Э3, пилотный экран); операции/обязательства/цели — v8.6.0
  (Э4 партия 1); планирование — v8.7.0 (Э4 партия 2), переиспользует usePlan/useForecast и виджеты
  дашборда (MetricsGrid/AllocationPanel/ForecastPanel, вынесены в @widgets/* при этой партии).
  Попутно (сверх пятёрки, тем же конвейером): `/banks` (ликвидные активы) и `/profile`
  (только чтение). Все — read-only, без форм создания/редактирования/смены пароля/удаления
  аккаунта — отдельный охват, не часть этого пункта.)_
- [x] **Снести Jinja-шаблоны/роуты экранов с ПОЛНЫМ функциональным паритетом в React** —
  `profile`/`forgot_password`/`reset_password` (`.html` + роуты в `app/main.py`) и мёртвый
  `templates/index.html`. ✓ v8.23.0. Выпал из плана при перенумерации этапов, восстановлен явно
  как шаг Э8 (`docs/frontend_milestone8_plan.md`) — исполнен раньше устойчивого Э7 явным решением
  владельца. Остаток — `dashboard`/`planning`/`transactions`/`obligations`/`goals`/
  `banks` — React-версии этих шести **read-only** (нет форм создания/редактирования), Jinja
  остаётся единственным способом записи — снос отложен до следующего пункта ниже. Разбор ошибки
  первого прохода и условие снова — `docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`.
- [ ] **ГЛАВНОЕ — вынести на фронт всё, что висит только в API:** in-app уведомления (колокольчик),
  рефералка, аналитика-дашборд (админ), история планов, household/семейные бюджеты, A/B-результаты,
  экспорт, spending-advice слои 1-3. **Плюс CRUD-паритет для шести read-only React-экранов**
  (создание операции/обязательства/цели/актива/бюджета, интерактивные контролы плана
  риск-профиль/`l_min`/источник `r_bench`) — без этого нельзя снести Jinja-версии
  `dashboard`/`planning`/`transactions`/`obligations`/`goals`/`banks` (см. пункт выше).
  **CRUD-паритет — пять сущностей, отдельные чекбоксы:** формы во фронте сейчас есть только
  в `pages/auth` (проверено — `useMutation` встречается в двух файлах, обе формы аутентификация,
  не финансовые данные); Jinja-формы создания для этих шести путей закрыты от домена nginx
  с v8.9.0 (`docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`) — то есть у продукта
  сейчас нет действующего способа ввести финансовые данные вообще, ни через React, ни через
  Jinja по обычному трафику.
  - [x] Операция (`transactions`) — создание/правка/удаление. ✓ v8.28.0 — тот же каркас
    (`Modal`/`toast.undo`), `description` в форме, `mcc`/`currency` сознательно не включены.
  - [x] Обязательство (`obligations`) — создание/правка/удаление. ✓ v8.25.0 — мягкое удаление
    с отменой (`toast.undo`), `Modal` на `@radix-ui/react-dialog` (первое применение).
  - [x] Цель (`goals`) — создание/правка/удаление. ✓ v8.28.0 — правка без `current_amount`;
    отдельный роут `POST /goals/{id}/contributions` («внести прогресс», прибавляет, не
    перезаписывает), 409 для целей с `linked_asset_id` (прогресс из баланса актива).
  - [x] Актив (`assets`, экран `banks`) — создание/правка/удаление. ✓ v8.25.0 — тот же каркас,
    что обязательство (тот же батч).
  - [x] Бюджет (`dashboard`) — создание/правка. ✓ v8.30.0 — `BudgetsSection`, upsert по
    `category` (бэк без отдельного PUT, FR-22), категория заблокирована в форме правки.
- [x] **L5, L6, L7 закрыты в v8.40.0.** Дисклеймер 39-ФЗ приходит полем ответа
  `/planning/calculate` и стоит НА экране рекомендаций до распределения; cookie-баннер
  с раздельным выбором, привязанным к редакции политики; футер со ссылками из реестра
  на каждой странице, включая гостевые, плюс «Настройки cookie» для отзыва согласия.
  Схема `LegalDocuments` заведена на бэкенде до фронта. Агенты нашли восемь блокеров —
  среди них: согласие было `primary`, а отказ `ghost` с автофокусом на согласии (тёмный
  паттерн ровно там, где требование его запрещает); баннер с `z-index: 250` рисовался
  поверх модалки, где не кликается; фиксированный баннер накрывал футер, то есть L6 гасил
  L7; при сетевой ошибке футер исчезал целиком, а баннер считал неизвестную версию
  согласием (fail-open в обе стороны). Всё починено до сдачи.
- [ ] **Остаток юр-требований фронта:** экран выдачи и отзыва согласий
  по 403-ответу гейта (L3); ни один чекбокс не отмечен заранее (L2 — проверить, не только
  в регистрации). Пока эти React-экраны не
  готовы, `legal/*`/`contacts`/`validation` остаются на Jinja (частичный снос v8.23.0 их
  сознательно не тронул) — условие и полный список остатка:
  `docs/reports/decisions/2026-08-14_jinja_frontend_removal.md`. Снос Jinja-остатка — часть
  гейта Э6, не отдельная задача после него.
- [x] **Навигационный каркас SPA (H1 + H13).** ✓ v8.31.0. `widgets/app-nav` — постоянная
  навигация по семи экранам в `__root.tsx`, `aria-current` на активном пункте, три сырых
  `<a href>` заменены на `<Link>`. Показывается только авторизованному: гостю все семь
  экранов отдают 401/403, ссылки были бы тупиками ([IA-04]). Регрессионный гейт
  `tests/test_spa_navigation_reachability.py` — новый экран обязан попасть в меню, иначе
  красный (проверено мутацией: убрал `/profile` — упал с точным адресом). E2E
  `frontend/e2e/navigation.spec.ts` — 7 тестов в реальном браузере, включая прямую проверку,
  что переход не перезагружает документ.
  **Попутно починено по находкам design-critic и a11y-auditor:** skip-link (WCAG 2.4.1 —
  до каркаса перед `<main>` было два таб-стопа, стало девять, то есть цена появилась вместе
  с навигацией); фокус-кольцо в светлой теме давало 2.27:1 при норме 3:1, хотя комментарий
  у токена обещал «≥3:1 в обеих темах» — заведён `--c-focus`, в светлой теме 5.07:1 (дефект
  общий, не только каркаса); hover в светлой теме был 1.03:1, то есть физически отсутствовал
  (`--c-surface-up` там равен белому, а каркас лежит на `--c-bg`); вес активного пункта
  700 → 600, иначе перенос строк перестраивался при каждом переходе; `border-radius`
  таблетки схлопывался при фокусе из-за глобального `:focus-visible` той же специфичности,
  импортируемого позже; добавлен фолбэк `forced-colors`.

- [ ] **Остаток находок design-critic по шапке — решение владельца, не вахты.** Требуют
  бренд-решений и потому не сделаны молча: (1) слева в топбаре пусто — нет вордмарка,
  который был бы и якорем «домой»; (2) шапка не sticky, то есть каркас уезжает при первом
  скролле — [IA-02] говорит «постоянный», а он постоянен только формально; (3) навигация
  набрана вторичным токеном и стоит ПОД служебными контролами (email/выход/тема), то есть
  самый частый элемент подчинён самому редкому; (4) на телефоне семь полных подписей
  переносом занимают около трети экрана до первого числа — альтернатива (горизонтальная
  прокрутка меню) отвергнута из-за персоны R01 (56 лет, промахи), но выбор стоит подтвердить;
  (5) `AppNav` возвращает `null` во время загрузки профиля — контент прыгает на холодном
  старте, полноценный фикс требует стратегии предсказания состояния входа, а не CSS.

- [x] **Шесть E2E-тестов красные — разобрано и починено. ✓ v8.31.1.** Четыре из шести
  оказались НЕ гнилью тестов, а следствием реального дефекта продукта: контракт
  (`docs/api/openapi.json`, схема `PlanningCalculateResponse`) не держит `ranked` и `top3`
  в `required`, а рукописный тип фронта объявлял их обязательными — TypeScript молчал,
  `AllocationPanel` звал `.some()` по undefined, и весь дашборд уходил в error boundary
  («Something went wrong!» вместо финансового обзора). Правка типа по контракту сразу
  вскрыла второе место падения (`AlternativesBrowser`) — то, ради чего она и делалась.
  Оставшиеся два — действительно неоднозначные локаторы (`getByText(/Резерв/)` ловил и
  легенду, и подсказку ползунков; `getByText('anna@example.com')` — и топбар, и профиль),
  сужены до нужной области. E2E целиком зелёный впервые: 32/32.

- [ ] **Генерировать `entities/plan-summary` из контракта, а не вести руками.** Корень
  всех находок v8.31.1: `usePlan.ts:19` делает `data as unknown as CalculatePlanResult` —
  каст выбрасывает сгенерированный `PlanningCalculateResponse` и снимает сверку с
  контрактом целиком. Остальные шесть сущностей (`goals`, `obligations`, `assets`,
  `transactions`, `profile`, `auth`) просто реэкспортируют сгенерированный тип, и
  расхождений у них нет — то есть образец в репе уже есть. Пока каст жив, рукописный тип
  может снова разъехаться с контрактом молча.

- [ ] **`Alternative`: все числовые поля контрактно nullable, фронт считает их числами.**
  Найдено api-contract-guard, v8.31.1, НЕ починено (не роняет экран, но искажает данные).
  В схеме `Alternative` в `required` стоит только `id`; `utility`, `Rt_new`, `Lt_new`,
  `Dt_new` объявлены `anyOf[number|null]`. `AlternativesBrowser` кормит их в `formatMoney`/
  `formatNumber`/`formatPercent`, типизированные на `number`. Краха нет — JS приводит
  `null` к нулю, — но в финансовом продукте это значит, что вместо честного «нет данных»
  пользователь молча видит «0 ₽» и «0%». Чинить вместе с пунктом выше: генерация типа
  сделает nullable видимым для TypeScript, и места вылезут сами, как вылезли `top3`.

- [x] **Шесть E2E-тестов красные, и это НЕ регрессия каркаса — доказано базовым прогоном.**
  Найдено 2026-09-03 при сдаче v8.31.0. Тесты откатаны на состоянии ДО батча (`__root.tsx`
  без `AppNav`/`header`/skip-link) — падают ровно так же. Причина, почему никто не видел:
  браузеры Playwright на машине не были установлены вообще (`npx playwright install chromium`
  скачал их впервые в этой сессии), а тир `full` в CI, по §9.0, ни разу в истории проекта не
  доходил до конца. Три класса: `.fp-hero-value` не совпадает по тексту/видимости
  (`dashboard.spec.ts:49,58`, `theme.spec.ts:57`); strict mode violation на
  `getByText(/Резерв/)` — подпись ползунков содержит то же слово (`planning.spec.ts:68`);
  strict mode violation на `getByText('anna@example.com')` — email есть и в топбаре, и в
  профиле (`profile.spec.ts:11`); взаимодействие с селектом темы (`theme.spec.ts:39`).
  Берётся отдельным PATCH: смешивать шесть чужих починок в MINOR про навигацию — размывать
  и батч, и SemVer.

- [x] **История планов (P2.6) вынесена на фронт.** ✓ v8.34.0. Секция на `/planning`:
  сохранение снимка с подписью, список с датой/временем, риск-профилем, свободными
  деньгами, ПДН и распределением, удаление с подтверждением. **Схемы ответа заведены на
  БЭКЕНДЕ в том же батче** (`PlanSnapshotSummary`/`PlanSnapshotDetail`/`PlanHistoryList`
  вместо `dict[str, Any]`) — прямой урок v8.31.1: рукописный тип фронта обещает больше
  контракта, TypeScript молчит, экран падает. Теперь фронт реэкспортирует сгенерированный.

- [x] **`mypy app` чист впервые — три ошибки в парсере выписок починены. ✓ v8.34.0.**
  Давний пункт (полдня «до первой строчки React», открыт с вехи 7). Обе причины были
  настоящими, а не шумом: `BANK_PARSERS` держит парсеры РАЗНОЙ арности (`parse_sber_csv`
  берёт один аргумент, `parse_tinkoff_csv`/`parse_universal_csv` — ещё и `report`), без
  аннотации mypy выводил значение как `object` и справедливо отказывался его вызывать;
  в `statement_reconcile` проверялся только `start`, хотя сравнение шло и с `end` —
  оба берутся из одного match, но связь между ними невыразима, и явная проверка честнее
  молчаливого допущения. 187 тестов парсера зелёные, поведение не менялось.

- [x] **Выгрузка плана (CSV/XLSX/PDF) вынесена на фронт.** ✓ v8.35.0. Секция на
  `/planning`, скачивание через `fetch`+blob вместо ссылки: экспорт за гейтом согласия,
  и ссылка скачала бы 403-JSON файлом с расширением `.csv`. Имя берётся из
  `Content-Disposition` — сервер уже собирает его с датой. E2E ловит настоящее событие
  `download` и проверяет имя.

- [ ] **Остаток находок design-critic по выгрузке — требуют продуктовых решений.**
  Не сделано сознательно, вынесено сюда, а не забыто:
  1. **Три равновесные ghost-кнопки — это не иерархия, а её отсутствие.** Действие одно
     («выгрузить план»), формат — его параметр. Правильная форма: один CTA плюс выбор
     формата (сегменты/split-button), либо CSV по умолчанию и «другие форматы» рядом.
  2. **На всём `/planning` нет ни одного `primary`** — проверено грепом. Экран
     заканчивается серыми пилюлями; выгрузка — естественный терминальный шаг и
     единственный кандидат на акцент. Бриф §8 прямо называет это «робкой палитрой».
  3. ~~**Экспорт не знает про согласие, которое экран УЖЕ выяснил.**~~ **ПРОВЕРЕНО НА
     ДИСКЕ И СНЯТО (2026-09-03).** Агент утверждал, что при отозванном согласии на экране
     окажутся ДВЕ одинаковые панели в 300 пикселях друг от друга. Это невозможно:
     `PlanningPage.tsx:42-57` при 403 от `usePlan`/`useForecast` делает **ранний return**
     с панелью согласия — секции выгрузки и истории в этом состоянии не рендерятся вовсе.
     Собственная обработка 403 в секции экспорта нужна для ДРУГОГО случая: согласие
     отозвано уже после загрузки страницы (в соседней вкладке, по истечении сессии), и
     тогда панель на экране одна. Оставлено как есть.
     🔴 Урок не про агента, а про приёмку его находок: разбор был убедительным и
     конкретным, с номерами строк, — и всё равно неверным, потому что агент не проследил
     ранний return. Находки агентов проверяются на диске так же, как саммари.
  4. Таксономия подписей форматов разъезжается («назначение» у двух, «файл» у третьего);
     лид объясняет устройство сервера вместо пользы; на 380px кнопки встают столбиком
     с зазором 8px — тесно для аудитории 55+; нет `role="group"` на наборе; мёртвый
     класс `fp-plan-export` без правил; две соседние панели одинаковой структуры подряд
     читаются как одна разрезанная.

- [ ] **Restore для снимков плана — чтобы удаление говорило на общем языке продукта.**
  У бюджетов, целей, операций и активов удаление — мягкое, с undo-тостом. У снимков
  restore-эндпоинта нет, поэтому пришлось ставить модалку подтверждения: вернуть
  удалённое пользователю нечем. В продукте теперь два языка удаления, и это расхождение,
  а не выбор (design-critic). Починка — `POST /planning/history/{id}/restore` плюс переход
  секции на `toast.undo`.

- [x] **`shared/lib/date/formatDate` показывал дату на сутки назад. ✓ v8.34.0.** Найдено тестом
  в v8.34.0. Бэкенд отдаёт `isoformat()` без `Z`; JS парсит такую строку как ЛОКАЛЬНОЕ
  время, а `formatDate` затем пинит `timeZone: "UTC"` — двойной сдвиг. Для дат без времени
  верно (там JS парсит как UTC), для дата-время — нет. Затрагивало дедлайн цели, дату
  операции и даты профиля — у всех полей в контракте `format: date-time`. Проверено
  командой: `new Date("2026-06-01T00:00:00")` в Москве даёт «31.05.2026». Починено
  дописыванием `Z`, 6 тестов; пин `timeZone: "UTC"` сохранён намеренно — показывается
  календарная дата пользователя, а не момент времени.

- [ ] Микро-визуализации (прогресс-бары целей, донат распределения плана, спарклайны).
- [ ] Тёмная тема (отдельная палитра, не инверсия), motion (150-250мс, ease-out, `prefers-reduced-motion`),
  тосты + скелетоны вместо `alert()` и пустоты, mobile-адаптив (цели нажатия ≥44px).

## 8.2а. Обязательное ДО запуска — иначе окно закрывается на полгода

> Оба кодовых пункта (0.6, 0.7) закрыты v8.16.1–v8.17.0. Остался ОДИН пункт — не код,
> задача юриста — и именно он теперь единственное, что блокирует активацию телеметрии.
> Стоимость упущения не изменилась: первый раунд на живых данных откладывается, пока
> данные не накопятся заново, если контур не закрыт до запуска.

- [x] **Телеметрия принятия совета** (волна 0, п. 0.6). ✓ v8.17.0, код готов и ДОРМАНТЕН.
  Таблица `plan_advice_events` (миграция 0032, SQLite+PostgreSQL проверены upgrade/downgrade/
  upgrade), `POST /api/telemetry/advice-events` (показ плана) + `PATCH .../{plan_id}/decision`
  (решение пользователя: `accepted`/`modified`/`ignored`), за гейтом `financial_data` (тот же,
  что transactions/obligations/goals/liquid-assets). **Отдано ДО активации:**
  `settings.TELEMETRY_COLLECTION_ENABLED` (default **False**) — оба эндпоинта отвечают 404, пока
  флаг выключен. Правовой контур обезличивания для использования этих данных в сертификации
  модели (152-ФЗ, следующий пункт) — задача юриста, не кода; переключение флага в True —
  решение владельца ПОСЛЕ закрытия контура, не автоматика при деплое. Стоимость упущения при
  запуске без этого была бы полгода ожидания эталона для раунда на живых данных (волна 2) —
  теперь стоимость активации, когда контур закроется, это одна строка конфига, не батч.
- [x] **Снимок модели на момент запуска** (волна 0, п. 0.7). ✓ v8.16.1.
  `tools/model_validation/model_snapshot.py` — канон, конфиг (константы читаются прямо из
  кода, не переписаны вручную) и версия кода в `knowledge/model_validation/
  model_snapshot_v*.md`. Baseline снят сейчас; при фактическом запуске в проде команду нужно
  перезапустить, чтобы зафиксировать финальную конфигурацию (снимок детерминирован по коду,
  перевыпуск дёшев).
- [ ] **Правовой контур обезличивания** для использования пользовательских портретов в
  тестировании (152-ФЗ) — задача юриста, не кода, и её **нельзя сделать задним числом**:
  собирать без контура нельзя, а достроить контур на уже собранных данных не получится.

## 8.3. Онбординг и объяснимость (критично — закрывает реальные вопросы пользователя)

- [ ] **Интерактивный онбординг** — отдельная страница на сайте + файл-документ, с активными ссылками.
- [ ] **Пояснения «как найти выписку»** того/иного формата прямо в интерфейсе (если пользователь не
  знает, где её взять в своём банке).
- [ ] **Объяснить логику дохода** (онбординг должен это закрыть — при проектировании Claude отвечает):
  как алгоритм понимает доход? Что считается зарплатой? Пользователь вбивает вручную с уникальным
  тегом? А если доходов несколько (дивиденды, подарок, бизнес-выручка, репетиторство, две работы,
  родители дали) — не сломается ли алгоритм? Какой порядок действий для пользователя, чтобы алгоритм
  считал всё правильно?
- [ ] **Объяснить разницу целей и активов** — как это в алгоритме, как пользователь должен разграничивать.
  Супер-подробный онбординг (интерактивный на сайте + файл).

## 8.4. Полировка (давние пункты)

- [x] **Формулы с нижними индексами** красиво, в LaTeX-стиле (KaTeX) — закрыто v8.20.0:
  раскрываемая по клику формула оценки в `AllocationPanel` (`UtilityFormula`/
  `UtilityFormulaBody`), символьная запись + числа альтернативы, `katex` в ленивом чанке.
  Детали — CHANGELOG [8.20.0].
- [x] **Design-critic/a11y-auditor проход по объяснению плана в `AllocationPanel`** — закрыто
  v8.19.5: 8 находок design-critic + 2 блокера a11y-auditor (канон денег, иерархия цвета,
  видимые подписи gains/costs, нейтральный маркер, сброс UA-маргинов, max-width, ритм
  разделителя, живая заглушка вместо размонтирования). Визуально проверено в браузере (демо-кейс
  «ekaterina», обе темы) — впервые для этого блока. Детали — CHANGELOG [8.19.5].
- [x] **Красивые виджеты/системные сообщения** — регистрация, ошибки, подтверждения — закрыто
  v8.22.0: экраны входа/регистрации/сброса пароля с нуля (в SPA их не было вообще — см. разбор
  в CHANGELOG [8.22.0]), единая система тостов (`shared/ui/Toast.tsx`, первое применение
  `@radix-ui/react-toast`), `extractErrorMessage` — единый разбор ошибок API. Задел готов для
  переиспользования, когда появятся CRUD-формы вне auth (сейчас в SPA их ещё нет нигде —
  весь остальной фронт read-only).
- [x] **Улучшить UI прогнозирования** — передвижение/выбор параметров движка. ✓ v8.24.0:
  горизонт (select 3/6/12/24 мес.) + сценарная ставка `r_bench` (ползунок «что если», дебаунс).
  Параметры самого движка (α/β Holt, σ Monte-Carlo, ширина CI 80%) сознательно не отданы —
  канон-заморожены. Детали — CHANGELOG [8.24.0].
- [ ] **PDF-отчёт со страницы прогнозирования** — переделать в красивый и реально полезный, в отдельном
  формате, а не распечатка страницы. Реализация PDF — на бэке, но триггер и польза — фронтовые.

Из старого роадмапа в веху 6 вливаются: P1.5 (UI-стандарт), P2.1 (UI рефералки), P2.2 (UI аналитики),
P2.3 (UI-часть уведомлений), P3.1 (i18n UI — вынос строк), P3.2 (PWA).

---
# ВЕХА 9 — Деплой (`v9.0.0`)

> Финальный запуск на сервере. План — в `docs/DEPLOY.md` и `DEPLOYMENT_PLAN.md`.

- [ ] **Деплой по плану:** VPS в РФ (152-ФЗ → данные граждан РФ на серверах в РФ), Docker через зеркала
  (`dh-mirror.gitverse.ru` — Docker Hub заблокирован для РФ), Nginx reverse proxy + Let's Encrypt
  (HTTPS обязателен для финпродукта), прод-секреты в `.env` (fail-loud), бэкапы по расписанию + cron
  для уведомлений/курсов, мониторинг (Sentry DSN).
- [ ] **Ссылки Василия** (проверить актуальность на момент деплоя): домен — `reg.ru` (искать `finpilot`),
  сервер — `selectel.ru`.
- [ ] **На проде закрываются вещи, не тестируемые в песочнице:** живой SMTP (письма реально уходят),
  живой fetch ставки/курсов ЦБ с РФ-IP, поведение под нагрузкой.

---

## Сквозные правила (действуют на всех вехах)

1. **НИКОГДА не писать имена в гите / релизах / публичных артефактах.** Официально безымянно.
   Касается коммитов, тегов, GitHub Releases, публичного репо.
2. **4 аккаунта Claude: V, J, M, S.** Кто на вахте сейчас — в шапке `docs/WATCHLOG.md` (здесь не
   дублируем — протухает). Имена — только для
   разграничения в разборах, на работу не влияют (WATCHLOG самодостаточен для любого).
3. **Вывод тестов — через `--junitxml`, не через `tail` по логу.** Итоговая строка pytest
   (`N passed`) и счётчики НЕ флашатся надёжно в перенаправленный файл (буферизация stdout, грабли
   песочницы). Решение: `pytest --junitxml=/tmp/result.xml`, парсить XML (точное число passed/failed),
   exit code как источник истины. Зафиксировано в `docs/pitfalls.md` и `docs/sandbox_runbook.md`.
4. **Тестирование мат-модели — СТРОГО вне проекта Claude** (Opus 4.8, без памяти/инструкций) —
   иначе эксперт biased. См. веху 7.
5. **TDD обязателен**, `app/core/` без согласования не трогать (исключение — `spending_advice.py`),
   схемные изменения проверять на PostgreSQL, без массового реформата кроме координированных окон,
   без эмодзи, русская проза + английский код.
6. **Долгий фоновый прогон — никогда не `| tail -N` (без `-f`).** `tail -N` без `-f` не стримит:
   читает весь input до EOF и только тогда печатает хвост — в связке с фоновым запуском это
   полностью прячет прогресс именно там, где видимость нужнее (PIT-016). Два законных варианта:
   (а) «сообщи, когда закончится» — голая команда под фоновым запуском без пайпа через `tail`
   вообще, харнесс уведомляет по завершении сам; (б) «покажи прогресс по ходу» — инструмент
   `Monitor` с небуферизованным построчным выводом (`python -u ... | grep --line-buffered ...`).
   Итоговый счёт — всё равно правило §3 выше (`--junitxml`, не парсинг лога).

---

## Прямые вопросы Василия (ответы — для контекста)

- **Доступ к A/B-тестам и доп-аналитике, аккаунт админа.** Сейчас не «аккаунт», а секрет
  `ADMIN_API_KEY` (заголовок `X-Admin-Key`); в dev при пустом ключе открыто, в проде — задаётся в
  `.env`. Удобный визуальный доступ появится в вехе 6 как админ-дашборд (P2.2). До фронта — можно
  дёргать эндпоинты curl-ом.
- **Замедляют ли тесты/CI разработку.** Почти нет, и экономят время: локальные fast-тесты —
  секунды/минуты, ловят регрессии до прода; CI на push крутится в фоне на GitHub, не блокирует;
  полный прогон ~2-3 минуты против стоимости бага в финпродукте. Для уровня «деньги людей» тесты —
  необходимость, не роскошь. (CI у нас GitHub Actions, не VK.)

---

## Идея (не привязана к вехе): роль «независимый эксперт» — гипотезы о багах и разрыве теория/практика

> **Источник:** владелец, 2026-08-19, устная идея по ходу разбора CI-инцидента. Сейчас практика
> ручная: наработки/код отправляются в отдельный чат Claude (веб-версия, тот же аккаунт), оттуда
> приходят гипотезы и промпты для проверки. Идея — сделать это встроенной, не разовой ролью
> проекта, а не формализовывать прямо сейчас (владелец сам не уверен, как лучше организовать —
> см. цитату ниже, записано дословно по сути, не додумано).

**Суть идеи.** Роль/агент, который не проверяет «прошёл ли тест», а строит **гипотезы**: где
именно код может разойтись с тем, что описано теоретически (в докс/каноне/спецификации);
какие крайние случаи не покрыты; что выглядит правильным в коде, но не гарантированно работает
на практике. Владелец прямо сравнивает это с детерминированными тестами, но называет «более
тонкой работой» — не «assert X == Y», а «а что, если...».

**Чем это отличается от того, что уже есть в проекте:**
- `design-critic`/`a11y-auditor`/`api-contract-guard` (`.claude/agents/*.md`) — уже существующие
  роли того же духа (независимый взгляд, не пишут код, ищут разрыв между тем, что задумано, и
  тем, что получилось), но каждый **узко** заточен под одну ось (визуал / доступность / контракт
  API). Идея владельца — шире: разрыв теория↔практика в целом, включая мат-модель, бизнес-логику,
  граничные случаи, а не только эти три оси.
- `finpilot-math-model-guard` (skill) — сверяет КОД с каноном, но по правилам/чек-листу, не
  генерируя новые гипотезы о том, что может сломаться.
- Post-mortem `docs/reports/incidents/ci_blocking_gate_red_41_days.md` (этот же батч) —
  показательный пример ИМЕННО того класса проблемы, который эта роль должна ловить ЗАРАНЕЕ, а не
  постфактум: «локально работает» ≠ «работает на практике везде» — разрыв держался 41 день
  никем не замеченным именно потому, что никто не задавал вопрос «а что, если файловая система
  регистрочувствительна?» до того, как это всплыло сáмо.

**Решения владельца (2026-08-19, по прямому вопросу):**
- **Формат — все три подхода сразу, не выбор одного.** Дословно: «давай объединим все три
  подхода… типо если мне не лень и есть возможность то использовать отдельный чат, а так всегда
  под рукой будут ещё два подхода — скилл и агент. Как раз так получается, что в одном эксперте
  ещё три независимых подхода эксперта». То есть роль «независимый эксперт» — не ОДНА
  реализация, а **три независимых линзы**, каждая со своим режимом доступности:
  1. **Агент** (`.claude/agents/independent-expert.md`, по образцу `design-critic`) — всегда
     под рукой, видит весь код и историю проекта из этой же сессии Claude Code.
  2. **Skill** (по образцу `finpilot-math-model-guard`) — тоже всегда под рукой, но по явному
     триггеру/чек-листу, не свободная генерация гипотез.
  3. **Внешний чат** (веб-версия Claude, отдельная сессия) — используется, когда есть время и
     желание («если мне не лень») — не заменяет два первых, а даёт третье, структурно другое
     мнение: не видит контекст/историю решений этой сессии, поэтому не может «обмануть себя»
     тем же рассуждением, что писало код. Ценность идеи именно в том, что все три подхода
     **независимы друг от друга** (разный контекст/степень отстранённости) — так же, как
     панель из нескольких линз в review-паттернах (perspective-diverse verify), только здесь
     линзы — способ вызова, а не тема.
- **Частота — на веху/крупный этап**, не на каждый батч (в отличие от `design-critic`/
  `a11y-auditor`, которые именно батч-по-батчу).
- **Отсев шумных гипотез — adversarial-verify.** Независимый второй проход пытается опровергнуть
  каждую гипотезу; выживает только то, что не опровергли — тот же паттерн, что в
  `code-review`/workflow-оркестрации (см. skill `code-review`, режим `ultra`), применённый здесь
  вручную/полуручную к гипотезам, а не к найденным багам.

**Статус:** решение по формату/частоте/отсеву принято 2026-08-19. **Реализовать в очереди D выше**
(`.claude/agents/independent-expert.md` минимум — агент-подход, самый быстрый путь к «под рукой»)
— владелец прямо поручил не откладывать: реализовать и прогнать в конце текущего батча (дрейф +
CRUD батч 2 + compliance-разбор), посмотреть результат вживую. Skill-подход и шаблон для
внешнего чата — можно следующим заходом, агент — обязателен в этом батче.

---

## Ожидается от Василия (разблокирует задачи)

- [x] **Telegram-токен** от @BotFather — получен, в `.env` (перед продом перевыпустить через
  /revoke — засвечен в чате). Опционально остаётся Telegram user ID (@userinfobot).
- [x] **Список банков / справочник** — закрыто исследованием v5.9.0 (реестр НСПК/ЦБ, §5.5).
- [ ] **Решения юриста** (веха 7) — по вопросам из `docs/legal_questions_for_lawyer.md`.
- [ ] **PNG-превью диаграмм** — регенерация на Mac (drawio desktop), список из 9 схем в
  `docs/diagrams/README.md` §Превью (в песочнице CDN Chromium заблокирован).
- [x] **Решения по предложениям §5.8 п.4** — владелец: «все предложения замутим» (2026-07-02),
  реализовано v5.15.0: onboarding · glossary ·
  API-снапшот · backup_restore · карта ПДн · SLO · deployment-диаграмма.
- [x] **Бизнес-материалы, лого-эталоны, результаты опросника** — влиты в репозиторий (v5.1.0, §5.1–5.4).

---

## Связанные документы

| Файл | Зачем |
|---|---|
| `docs/WATCHLOG.md` | Быстрый снимок «где стоим» (ссылается сюда за деталями). |
| `CHANGELOG.md` | Полная история по каждому патчу. |
| `docs/RELEASES.md` | История развития по эпохам (средний слой). |
| `docs/reports/adr/adr_001_frontend_stack.md` | Обоснование выбора React+TS (веха 8). |
| `docs/ui_visual_design_brief.md` | Эстетическое ТЗ для фронта (веха 8). |
| `docs/math_model.md` | Каноническая мат-модель (источник истины; v3.4.0/v3.1.0/v3.0.0 — исторические документы). |
| `docs/legal_questions_for_lawyer.md` | Вопросы юристу (веха 7). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
