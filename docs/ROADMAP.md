# FINPILOT — ROADMAP (вехи 4-9)

> **Что это.** Единственный источник направления движения продукта. Заменяет старый роадмап
> `03_Будущий_роадмап.md` (тот был под структуру P0-P3 до выхода на прод-планку; большая часть
> его пунктов закрыта — см. «Что уже сделано»). Здесь — крупные вехи проекта от завершения
> бэкенда до публичного запуска, с детализацией задач каждой вехи.
>
> **Как пользоваться.** WATCHLOG (`docs/WATCHLOG.md`) держит короткую выжимку «где в роадмапе
> стоим» и ссылается сюда. Подробности по конкретной версии — в `CHANGELOG.md` (каждый патч) и
> `docs/RELEASES.md` (вехи по эпохам). Этот файл — про БУДУЩЕЕ; история — в тех двух.
>
> **Чекбоксы.** `[x]` — сделано, `[ ]` — осталось. Отмечать СРАЗУ при закрытии задачи (как
> WATCHLOG-дисциплина), чтобы статус был наглядным для любого из 4 аккаунтов.
>
> **Текущая точка** (версия, веха, чья вахта) — в шапке `docs/WATCHLOG.md`. Здесь её НЕ дублируем:
> дублированный факт протухает (эта строка успела отстать на веху — урок ревизии v5.11.0).

---

## Правило версий (важно — читать первым)

**Вехи проекта нумеруются как мажорные версии (v4 → v9), и это СОЗНАТЕЛЬНО НЕ классический
SemVer.** В строгом SemVer `MAJOR` = слом обратной совместимости контракта. Здесь мажор =
**крупный этап проекта** (бэкенд / организация / фронт / тестирование / юр / деплой), даже если
этап контракт не ломает. Так нагляднее для одиночной разработки на 4 аккаунтах. Решение принято
Василием осознанно.

Внутри вехи — обычная механика: фича = MINOR (`v4.19.0`), фикс/мелочь = PATCH (`v4.18.2`).
Каждый батч: bump `APP_VERSION` в `app/config.py` + запись в `CHANGELOG.md`.

| Веха | Версия | Тема | Статус |
|---|---|---|---|
| 4 | `v4.x.x` | **Завершение бэкенда + техдолг** | В работе |
| 5 | `v5.0.0` | Организационное (репозитории, доки, бизнес, исследования, методички, банки, наука) | **Завершена** (v5.18.0) |
| 6 | `v6.0.0` | Тестирование (мат-модель 1000+ кейсов + независимый эксперт + импорт + банки) | Запланировано |
| 7 | `v7.0.0` | Юридическое (документы + соответствие законам) | Запланировано |
| 8 | `v8.0.0` | Фронтенд (редизайн + React+TS + вынос всех возможностей бэка) | Запланировано |
| 9 | `v9.0.0` | Деплой на сервер | Запланировано |

Порядок строгий: бэк → организация → **тестирование → юр → фронт** → деплой.
_(Пересмотр 2026-07-02, приказ владельца: математика замораживается и проверяется до фронта;
юр-тексты — согласия, оферта, политика, дисклеймеры — это контент экранов, фронт вшивает их
сразу и не переделывается; юр-ответы внешние и медленные — запускаем раньше. Прежний порядок —
`docs/roadmap_archive/`.)_

---

## Механизм живого роадмапа — выбор следующей задачи БЕЗ владельца

> Любой аккаунт (V/J/M/S) детерминированно определяет следующую задачу сам. Теория и обоснование —
> `knowledge/guides/roadmap_methodology.md` (Часть II, §15). Прямое указание владельца в чате всегда
> приоритетнее этого механизма.

**Алгоритм (сверху вниз, первое совпадение):**
1. Явный приказ владельца в текущем чате.
2. Указатель **«СЛЕДУЮЩАЯ ЗАДАЧА»** ниже — если не противоречит чекбоксам (сверь перед стартом).
3. Активный спец-поток (сейчас — нет; последним был §5.8, веха 5 закрыта): первый открытый `[ ]` по порядку.
4. Строгий порядок вех 4→9: первая веха с открытыми пунктами → первая открытая подсекция сверху
   вниз → первый открытый чекбокс.
5. Пункт заблокирован внешним (владелец/юрист/сеть)? Пометь `(BLOCKED: причина)` и возьми следующий.
6. **Закрытие вехи** — только после блока «Предложения Claude» владельцу: что ещё стоило бы
   сделать в рамках вехи, чего владелец не увидел/не назвал (правило 2026-07-02, детали —
   `knowledge/guides/roadmap_methodology.md`). Без блока веха не помечается «Завершена».

**Тай-брейк, когда порядок неочевиден:**
1. Хвосты раньше нового: частично закрытый пункт (с под-чекбоксами) добивается первым.
2. Разблокирует другие задачи или защищает дедлайн осени-2026 — вперёд.
3. User-facing ценность (UX-находки P0/P1) выше внутренней полировки.
4. При равной ценности — quick win (укладывается в один батч).

**Дисциплина указателя:** закрыл батч → обнови «СЛЕДУЮЩАЯ ЗАДАЧА» здесь и «СЛЕДУЮЩИЙ ШАГ» в
`docs/WATCHLOG.md` §0 — они обязаны совпадать. Перед стартом — верификация состояния по диску
(PIT-004), оценка объёма и вероятности уложиться (`docs/session_continuity.md`).

**СЛЕДУЮЩАЯ ЗАДАЧА:** веха 6, продолжение (после v6.0.0 — экспертиза 12000×4 выполнена, модель
откалибрована v3.1.0, согласие 87.4%). Очередь: (1) G4 — слой управления запасом Bliq +
полный G5 (горизонт в инвест-полке) + G7-косметика (§6.1); (2) валидация графика прогноза §6.3
(приоритетная проверка «всегда прямая линия»); (3) шаг дискретизации 5% — замер (§6.3);
(4) повторная сертификация на датасете генератора v2 против >= 3 экспертных движков; (5) §6.4 —
импорт выписок. Параллельно: PNG-превью диаграмм — на Mac («Ожидается»).

---

## Что уже сделано (контекст, не переоткрывать)

К v4.23.0 закрыто, подробности — в `CHANGELOG.md` / `docs/RELEASES.md`:

- [x] **Весь P0** (деплой-инфра, SMTP-код, ЦБ-парсер, нагрузочное, секреты, cron, бэкапы).
- [x] **P1.1-P1.3, P1.6, P1.7** (a11y механика+контраст WCAG AA, кэш прогноза, rate-limit, Sentry,
  soft-delete бэк).
- [x] **P2.4-P2.7** (spending-advice слой 3, экспорт XLSX/PDF, история планов, обучение категоризации).
- [x] **P3.5** (A/B-фреймворк), **P3.7 backend** (household-оверлей, миграция 0022).
- [x] Импорт-трек: мультибанк CSV/XLSX/PDF (Тинькофф/ВТБ/Сбер/Райффайзен/Альфа), формат 1C, bulk.
- [x] B2B `/v1/analyze`, Plaid (для US/CA, в РФ-сборке отключён).
- [x] Тест-инфраструктура: 776 fast-тестов, покрытие >90%, 3-категорийный CI, property-тесты, PG-матрица.

**Остаточный технический долг** (закрываем в вехе 4 — см. ниже): mypy/flake8, `datetime.utcnow()`,
`ExpenseRecord` без даты.

---

# ВЕХА 4 — Завершение бэкенда + техдолг (`v4.x.x`)

> Цель: добить оставшийся бэк и закрыть техдолг ДО фронта, чтобы на вехе 6 не прыгать туда-сюда.
> Принцип заглушек: MVP/каркас для того, что **уже в роадмапе и точно понадобится фронту**;
> спекулятивное не выдумываем (YAGNI). Всё по TDD.

## 4.1. Бэк-код — реальные дыры

- [x] **P2.3 in-app уведомления (бэк).** Сделано в v4.19.0: модель Notification, миграция 0023,
  CRUD (create/list/count_unread/mark_read/mark_all), эндпоинты feed/unread-count/{id}/read/read-all,
  хук в email-рассылку (где письмо — там in-app), 14 тестов. Снят блокер для колокольчика (веха 8).

- [x] **Хвост P3.7 — `household_id` на производных.** Сделано в v4.20.0. **Коррекция факта:** колонка
  household_id уже была в БД (миграция 0022) и в моделях Scenario/PlanSnapshot — ранняя заметка
  «нет на этих таблицах» была ошибкой grep. Реальные пробелы были другие и закрыты: (1) household_id
  не проставлялся при создании (`save_scenario`/`create_plan_snapshot`) → производные всегда личные;
  (2) `get_plan_snapshots` игнорировал household-ось (ручной фильтр вместо `_owner_filter`). Добавлен
  `resolve_household_id` (валидация членства — чужой household проставить нельзя, fallback на личное),
  проброс через endpoint'ы планирования, починка чтения. 11 тестов. Миграция не потребовалась.

- [x] **P3.4 referral rewards (заглушка/MVP).** Сделано в v4.21.0. Расширён `/referral/me`:
  invite_url (готовая ссылка-приглашение с кодом для шаринга) + milestones (вехи-достижения по числу
  приглашений: 1/3/5/10/25, детерминированы из счётчика, без новых таблиц) + next_milestone (прогресс).
  Логика в `app/services/referral.py`, поле reward зарезервировано под реальные награды (привяжем при
  монетизации — YAGNI). 11 тестов.

- [x] **Telegram-каркас (P3.6).** Сделано в v4.22.0. Привязка аккаунта через одноразовый link-токен
  (deep link `?start=<token>`), команды бота `/start [token]`/`/link`/`/unlink`/`/status`, webhook с
  secret-валидацией, веб-эндпоинты link/status/unlink, хук в рассылку (привязанный telegram получает
  уведомления рядом с email/in-app). Поле `telegram_chat_id` на User (миграция 0024), сервис no-op без
  токена. 21 тест. Реальная отправка/приём webhook — на проде (api.telegram.org из песочницы 403).
  Токен бота — в `.env` (получен, перед продом перевыпустить через /revoke, т.к. был засвечен в чате).

- [x] **Монетизация-каркас.** Сделано в v4.23.0. Поля `plan_tier`/`plan_expires_at` на User
  (миграция 0025), сервис `app/services/subscription.py` (PlanTier enum, effective_tier/is_premium с
  учётом срока — premium истёкший = free, реестр FEATURES + has_feature/available_features), CRUD
  `set_plan`/`grant_premium_days` (накопительное продление), dependency `require_premium` (403 для free),
  эндпоинт `GET /subscription/me`. Без платёжной интеграции — структура под быстрое подключение платно.
  Реестр premium-фич — placeholder-каркас (реальное разделение при запуске монетизации). 16 тестов.

## 4.2. Баги бэка (из заметок Василия — давние, закрываем)

- [x] **Ставка ЦБ не подтягивается.** Закрыто в **v4.24.0** (инцидент INC-CBR-KEYRATE-PARAM). Корень
  оказался НЕ внешним 403, а дефектом кода: cbr.ru ASMX чувствителен к регистру — метод `KeyRate`
  ждёт `<ToDate>` (заглавная T), в коде была строчная `<toDate>` → параметр не распознан → ToDate=
  DateTime.MinValue → пустой диапазон → ноль строк KR → вечный fallback при живом cbr.ru. Фикс
  регистра + durable БД-кэш last-known-good (`cbr_key_rate_cache`, миграция 0026, переживает прод-403
  на датацентровом IP) + устойчивый парсер + самодиагностика (сниппет ответа в `detail`) +
  `refresh_key_rate()` для прогрева. Подтверждено первоисточником cbr.ru. Живая верификация на РФ-IP —
  у владельца. Маскировалось историей INV-CBR-403 (graceful fallback скрыл реальный баг).

- [x] **Письма не приходят.** Закрыто в **v4.25.0** — но это **расследование**, не дефект
  (INV-EMAIL-FIRE-AND-FORGET). Код формирования/отправки корректен (транспорт покрыт 9 тестами).
  «Пусто на почте» в dev — намеренный no-op без SMTP-конфига; фронт честно отдаёт ссылку
  подтверждения прямо в тосте, а не имитирует отправку. Реальный пробел: фоновая отправка
  (`BackgroundTasks`) теряла результат — сбой SMTP на проде был бы виден только в текстовом логе.
  Добавлена наблюдаемая обёртка `email_dispatch.py`: каждая отправка пишет событие
  `email_sent`/`email_skipped`/`email_failed` в `events` (домен вместо адреса — PII). Теперь при
  запуске на проде с реальным SMTP статус отправки виден в аналитике. Живая верификация с реальным
  SMTP — у владельца (P0.2, веха деплоя).

## 4.3. Технический долг (закрываем — всё синхронизировано, параллельных правок нет)

- [x] **P1.4 — mypy/flake8 до нуля.** Закрыто в **v4.25.1**. mypy 14 → 0 (6 файлов; большинство —
  реальные типовые неточности: `TypedDict` для `BANKS` и возврата `_fetch_from_cbr`, defensive-проверки
  `None`, сужение `isinstance`, не косметика). flake8 93 → 0 (87 × E501 + E731/E30x), точечно через
  `autopep8 --select` (не массовый black — §2); повторявшаяся HTML-обёртка писем вынесена в `_CARD_OPEN`.
  611 тестов fast-suite зелёные, регрессий нет.

- [x] **`datetime.utcnow()` → `datetime.now(UTC)`.** Закрыто в **v4.25.2** (вариант A: helper naive UTC).
  Новый `app/utils/time.py::utcnow()` = `now(timezone.utc).replace(tzinfo=None)` — единая точка, поведение
  идентично, колонки/миграции не тронуты (переход на aware — отдельно, ближе к деплою). 78 вхождений в
  17 файлах `app/` + 13 тест-файлов заменены, осиротевшие `datetime`-импорты убраны. +4 теста, fast-suite
  зелёный, DeprecationWarning устранён.

- [x] **Унификация часов: `datetime.now()` → `utcnow()`.** Закрыто в **v4.25.3** (BUG-026, Minor/P0).
  Хвост по времени: v4.25.2 закрыл `utcnow()`, но 13 прямых `datetime.now()` (локальное время сервера)
  остались — на MSK-VPS дают 3-часовой сдвиг с UTC-хранилищем (месячный бакетинг трат, окна «N дней»,
  fallback-даты импорта). Заменены в 5 файлах (`spending`/`statement_parser`/`bank_api`/`obligation`/
  `crud`), контракт naive↔naive. Aware-вызовы в `cbr`/`security` не тронуты. +7 тестов
  (`test_clock_unification.py`), миграций нет, fast зелёный. Полная tz-корректность (зона юзера →
  локальный календарь, поле `tz` у User) — отдельным заходом в слое паттернов.

- [x] **`ExpenseRecord` + поле `date`.** Закрыто в **v4.25.4**. Хранил только `period "YYYY-MM"` —
  блокировало слой внутримесячных/дни-недели паттернов. Добавлено опциональное `date: datetime | None`
  (frozen dataclass, обратно совместимо); при заданной дате `period` выводится из неё (источник истины,
  через `object.__setattr__`). Проброс `date=txn.date` в `spending.py`. `Transaction.date` уже был —
  миграции нет. +5 тестов (`test_expense_record_date.py`), fast зелёный (889).

- [x] **Деприкейшены в тестах.** Закрыто в **v4.25.2** заодно с utcnow: 13 тест-файлов переведены на
  helper `utcnow()`. (`test_time_utils.py` намеренно сверяется с legacy `datetime.utcnow()`.)

## 4.4. Кибербезопасность до прод-уровня

> Состояние: бэкенд-минимум OWASP **закрыт** — bcrypt-хеши (`services/security.py`), Fernet-шифрование
> «в покое» (`EncryptedString`), JWT + account lockout, 4 middleware (CSRF/SecurityHeaders/RateLimit/
> Logging), валидация Pydantic, fail-loud прод-проверка (`validate_production_security`), bandit +
> pip-audit в CI. Полный привязанный к коду срез — `SECURITY.md`. Ниже — остаток до прод-уровня,
> разделённый на **бэкенд-код** (делаем здесь) и **деплой/орг** (веха деплоя — не дублируем, ссылка).

**Бэкенд-код (харденинг — здесь):**
- [ ] **Rate-limit на общий стор (Redis). _(deferred → веха 9: деплой — нужен перед мультиинстансом)_**
  Сейчас счётчик in-memory на инстансе
  (`RateLimitMiddleware._hits`) — на мультиинстансном проде состояние не делится, лимит дырявый.
  Вынести в общий стор для горизонтального масштабирования. До масштабирования (один инстанс) —
  текущего достаточно; делать перед мультиинстансным деплоем.
- [ ] **CSP без `'unsafe-inline'`. _(deferred → веха 8: фронт — inline-JS уедет на React)_**
  Сейчас `script-src 'self' 'unsafe-inline'` — inline-скрипты
  ослабляют защиту от XSS. Перейти на nonce-CSP (или вынести inline-JS). Согласовать с фронтом.
- [x] **JWT: ревокация / выход со всех устройств.** ✓ v4.26.0. Два рубежа поверх stateless-JWT:
  блок-лист по `jti` (`revoked_tokens`) — точечный отзыв на `/auth/logout`; глобальная отсечка
  `users.tokens_valid_since` — `/auth/logout-all` гасит все токены раньше неё одним движением. Проверки
  в `dependencies.get_current_user`, миграция 0027, модуль `app/database/revocation.py`, +10 тестов.
- [x] **Инвалидация сессий при смене/сбросе пароля.** ✓ v4.27.0. Банковская планка (Option A): смена
  креденшелов гасит ВСЕ сессии. `/auth/change-password` сдвигает рубеж `tokens_valid_since` (валит
  другие устройства) + точечно гасит текущий токен по `jti` + чистит auth-cookie (клиент уходит в
  logged-out, переавторизуется). `/auth/reset-password` сдвигает рубеж (старые сессии мертвы, вход
  новым паролем проходит сразу). Бэкенд-only, без координации с фронтом. +7 тестов.
- [ ] **Cookie SameSite + CSRF-токен. _(deferred → веха 8: фронт — double-submit требует клиента;
  `SameSite=lax` + Origin-проверка уже есть)_** Подтвердить `SameSite=Strict/Lax` на auth-cookie; для
  изменяющих запросов рассмотреть double-submit CSRF-токен в дополнение к Origin-проверке (защита, не
  зависящая от наличия Origin у клиента).
- [x] **Ротация ключа шифрования (Fernet MultiFernet).** ✓ v4.28.0. `TokenCipher` → `MultiFernet`: набор
  `TOKEN_ENCRYPTION_KEYS` (PRIMARY первым) шифрует новым, читает старыми. `rotate` +
  `app/database/reencrypt.py::reencrypt_all` (интроспекция всех `EncryptedString`-полей, future-proof) +
  CLI `scripts/reencrypt_keys.py`. Ротация без простоя; обратная совместимость (один ключ/derive/legacy)
  сохранена. +11 тестов.
- [x] **MFA/2FA (TOTP).** ✓ v4.30.0. Второй фактор входа (pyotp). Поток: `/auth/mfa/enroll` (секрет +
  provisioning URI для QR) → `/auth/mfa/confirm` (код активирует, выдаёт 10 recovery-кодов) → при логине
  с включённым MFA выдаётся промежуточный `mfa_pending`-токен вместо сессии → `/auth/mfa/verify` (TOTP
  или одноразовый recovery-код) обменивает его на полную сессию. `/auth/mfa/disable`, `/auth/mfa/status`.
  Секрет — зашифрован (`EncryptedString`), recovery-коды — хеш (bcrypt). Миграция 0028. Заодно закрыта
  дыра: `get_current_user` отвергает любой purpose-токен (mfa_pending/verify/reset/telegram не дают
  сессии). +10 тестов. **Завершает все бэкенд-задачи раздела 4.4.**
- [x] **Security-регрессии в тестах.** ✓ v4.29.0. Новый `tests/test_security_regressions.py` (21 тест):
  security-заголовки на всех типах ответов (200/404/401/422 и CSRF-403 проходит обратно через
  `SecurityHeadersMiddleware`), HSTS только при включении, CSRF-блок на всех изменяющих методах
  (POST/PUT/PATCH/DELETE; Bearer/без-Origin/safe-GET не блокируются), старт-гард. Заодно закрыта дыра:
  `validate_production_security` теперь требует явный `TOKEN_ENCRYPTION_KEY(S)` в production (без него
  ключ деривится из `JWT_SECRET` — нет разделения ключей по назначению, ротация невозможна).

**Деплой/орг (веха деплоя — ссылка, детали в `SECURITY.md` §2/§3b и `docs/DEPLOY.md`):**
- [ ] TLS/HTTPS + HSTS на VPS (артефакты nginx готовы).
- [ ] Реальные прод-секреты (длинные случайные `JWT_SECRET` / `ADMIN_API_KEY` / Fernet-ключ, разные по
  окружениям).
- [ ] Хранение ПДн граждан РФ в РФ + уведомление Роскомнадзора (152-ФЗ).
- [ ] Активация Sentry-DSN (алертинг аномалий) + регулярный `pip-audit` (cadence).

**Порядок вехи 4:** P2.3 уведомления → household-хвост → referral rewards → Telegram-каркас →
монетизация-каркас → баг ЦБ ✓ → баг писем ✓ → техдолг (mypy/flake8 + utcnow + ExpenseRecord + тесты) →
кибербез-харденинг бэка (4.4; деплой-часть — в вехе деплоя).

---
# ВЕХА 5 — Организационное (`v5.0.0`)

> Не код — структура, документация, бизнес-материалы, методички. Цель: всё в одном месте,
> контролируемо, чтобы любой аккаунт и будущая команда поднимали контекст без раскопок.

## 5.1. Репозитории

- [x] **Приватный репо (текущий, куда заливаем сейчас)** — ВСЁ: код, бизнес-инфа, презентации, питчи,
  аналитика, версии. Единое место контроля.
- [x] **Создать публичный репо `finpilot`** — только то, что нужно пользователям / для open-source.
  Продумать правила: что туда заливать, что нет (секреты, бизнес, внутренние разборы — НЕ туда).
- [x] **Сообщать Claude при работе**, какой репо публичный, какой приватный (чтобы не залить лишнее
  в публичный).

## 5.2. Документация продукта

- [x] **Файл «Философия и ценность продукта»** — зачем продукт, для кого, в чём ценность.
- [x] **Папка `docs/` с фул-репортами** — инциденты/расследования/питфолы ЦЕЛИКОМ (не только саммари
  в реестрах). Раньше полные разборы жили вне репо; Василий решил — места почти не добавит, минусов
  нет, пусть лежат в репозитории для самодостаточности.
- [x] **Лого в проект отдельно** — эталоны + промпт генерации в двух вариантах: (а) без исходной
  картинки (б) с картинкой.
- [x] **`.md` анализ конкурентов** (РФ + международный рынок) — суть каждого, функционал, чего у них
  нет, преимущества против FINPILOT, похожие-но-другие по цели.

## 5.3. Бизнес

- [x] **Папка бизнеса** — аналитика, цифры, стратегия, бизнес-модель, питч. Собрать накопленное по
  папкам в одно место.

## 5.4. Исследования и UX

- [x] **Результаты опросника** (385 респондентов) — html, скрипты, картинки + раздел про
  тестирование/интервью/UX-протокол в общий план.
- [x] **Шаблон протокола исследования** — чтобы сырой лист (например, с интервью мамы) автоматически
  трансформировался в отчёт по шаблону, а не оставался необработанным текстом. Claude превращает
  лист → отчёт.
- [x] **Confluence для задач продукта** (опция на подумать) — вместо ведения задач в заметках. Не
  обязательно, зафиксировано как кандидат. _Разобрано в v5.10.0 (§9
  `knowledge/guides/development_process_methodology.md`): решение — оставить ROADMAP+WATCHLOG в репо
  каноном (несущее для мульти-аккаунтной передачи вахты); Confluence отложен как ответ на «живую
  команду людей», при реальной нужде доски — GitHub Projects как слой-представление._

## 5.5. Банки и выписки (исследование + справочник)

> **Закрыто в v5.9.0** (docs-исследование): реестр банков — `knowledge/product/banks_registry.md`;
> справочник форматов + дрейф — `knowledge/product/banks_statements_reference.md`; стратегия покрытия —
> `docs/universal_statement_parser_strategy.md`. Реализация парсера — веха 7.

- [x] **Найти официальный список ~200 банков.** Они проходят аккредитацию/регистрацию (реестр СБП на
  сайте НСПК, лицензии ЦБ) — список публичный. Эквайринговый список СБП (Василий прислал скрин)
  подтверждает масштаб: реально под две сотни.
- [x] **Справочник банков** — банк → какие выписки отдаёт, форматы (PDF/XLS/CSV/1C), особенности,
  пошаговый план «как скачать выписку» для каждого.
- [x] **Алгоритмическое решение покрытия выписок** — НЕ парсер под каждый банк × каждый формат (сотни
  комбинаций, неподъёмно). Universal-парсер с детекцией формата + эвристики на колонки
  (дата/сумма/назначение). Задел уже есть: мультибанк CSV/XLSX + PDF под 5 банков + 1C. Стратегия
  «как покрыть остальные ~190 без ручного парсера на каждый» — расписать здесь, реализация
  тестируется в вехе 7.

## 5.6. Методички (новые)

- [x] **«Подходы к архитектурированию»** — глобальный/фундаментальный подход к первому процессу
  разработки. Два режима: мало времени → MVP/заглушки; много времени → сразу качественно и
  фундаментально. **+ YAGNI** (не выдумывать спекулятивное — то самое напряжение «MVP-first vs
  фундаментально»). **+ как конкретно у НАС в FINPILOT это делается.** + этапы разработки и
  комментарии.
- [x] **«Процесс разработки FINPILOT»** _(готово v5.10.0 → `knowledge/guides/development_process_methodology.md`)_ — формализация того, что мы проходим прямо сейчас: как
  планируем, как принимаем архитектурные решения (как этот цикл согласований), как идёт цикл
  разработки, этапы, как делаем исследования аудитории. То, что Василий назвал «формализуй процесс
  который мы сейчас проходим».
- [x] **«Исследование аудитории»** — как делать: методы, шаблоны протоколов интервью, опросники,
  UX-тестирование, как сырой лист превращать в отчёт.
- [x] **«Принципы разработки»** — YAGNI / TDD / ship-fast vs фундаментально. Либо отдельной методичкой,
  либо влить в философию продукта (решить, куда чище ляжет при реализации).

## 5.7. Юридическое (заготовка)

- [x] **Отдельная инструкция с вопросами юристу** — создана на приоритете 1 до вехи 8
  (`docs/legal_questions_for_lawyer.md`), используется на вехе 8.

## 5.8. Ревизия репозитория и живой роадмап (задачи владельца, 2026-07-02)

- [x] **Полная ревизия файловой системы** — доки против кода, ссылки, нейминг, суть, календарные
  мины в тестах. Закрыто v5.11.0, отчёт — `docs/reports/audits/repo_revision_2026_07.md`
  (тип «Ревизия» заведён в диспетчер отчётов).
- [x] **Обновить все диаграммы + добавить недостающие** _(источники закрыты полностью v5.14.0;
  PNG-превью — регенерация на Mac, чек-лист в `docs/diagrams/README.md` → «Ожидается»)_ —
  инвентаризация устаревания: отчёт ревизии §3. Батчи v5.12.0 + v5.14.0.
  - [x] **Источники (mermaid `diagrams.md`) приведены к коду (v5.12.0):** ER 9→28 таблиц, §1 слои
    (ingestion-пакет ADR-005 + сервисы MFA/subscription/telegram/notifications), §5 внешние
    (cbr-xml-daily → cbr.ru + Telegram), §7 auth (MFA/TOTP двухшаговый + JWT-ревокация jti + mass).
  - [x] **drawio контент-ошибки (v5.12.0):** «21 альтернатива»→66 и «шаг 20%»→10% в 6 схемах;
    C4-container «5 таблиц»→28, «6-этапный»→9-шаговый. XML всех схем валиден.
  - [x] **Регенерация и добавления (v5.14.0):** `10_er_database.drawio` перегенерирован программно —
    28 таблиц, 6 кластеров, 11 FK-связей (канон `diagrams.md` §2); мастер — постраничный свап страницы ER.
    C4-component 06 (+ страница мастера): +`app/ingestion` (engine/providers, ADR-005) + `mfa.py`.
    Символы ∈/≠/∅ выпилены из 01/02 (WebKit-грабля drawio). 7 протухших PNG удалены («устаревшее
    хуже отсутствующего»), чек-лист регенерации — `docs/diagrams/README.md` §Превью.
- [x] **Методичка «живой роадмап» + прикладной механизм приоритизации** _(готово v5.13.0)_ —
  общая методичка `knowledge/guides/roadmap_methodology.md` (Часть I теория + Часть II FINPILOT;
  §15 — механизм) и **живой блок «Механизм живого роадмапа» в шапке этого файла**: алгоритм выбора,
  тай-брейки, указатель «СЛЕДУЮЩАЯ ЗАДАЧА», дисциплина синхронизации с WATCHLOG §0.
- [x] **Предложения Claude: чего не хватает в базе знаний/доках** _(список v5.13.0; владелец
  одобрил все 7; **реализовано v5.15.0**)_: `docs/onboarding.md` · `docs/glossary.md` ·
  `docs/api_contract.md` + снапшот `docs/api/openapi.json` (106 путей, генератор
  `tools/api_snapshot/dump_openapi.py`) · `docs/backup_restore.md` · `docs/pdn_data_map.md` ·
  `docs/slo.md` · физический деплой — `docs/diagrams.md` §9. Исходный список:
  - `docs/onboarding.md` — одна страница входа: порядок чтения для нового разработчика/аккаунта.
  - `docs/glossary.md` — Rt/Lt/Dt/BLR/ПДН/SAW/OCR/СБП/1C-обмен; нужен фронту (веха 8), юристу, новичку.
  - **API-снапшот для фронта** — экспорт `docs/api/openapi.json` + короткий `docs/api_contract.md`;
    веха 6 опирается на зафиксированный контракт, дифф контракта виден в ревью.
  - `docs/backup_restore.md` — PG-дампы: ротация, восстановление, регулярная проверка
    восстановимости (планка финтеха + 152-ФЗ).
  - `docs/pdn_data_map.md` — карта ПДн: какие данные, где лежат, срок, основание
    (юристу к вехе 8).
  - `docs/slo.md` — перф-бюджеты (p95 `/api/planning/calculate` и др.) под нагрузочное вехи 7.
  - **Deployment-диаграмма (физическая)** — nginx/uvicorn/PG/env; добавить при вехе 9.

---
# ВЕХА 6 — Тестирование мат-модели и импорта (`v6.0.0`)

> Фундаментальная и точная проверка ядра. Самый ответственный блок — от него зависит доверие к
> рекомендациям.

## 6.1. Независимая проверка математики (ТРИ шага)

- [x] **(1)–(3) Выполнено протоколом сильнее планового (v6.0.0, июль 2026):** вместо 6 профилей —
  **12 000 синтетических портретов × 4 независимых экспертных движка** (V, M, J, S) + сводная
  независимая экспертная оценка вне проекта. Заключение: ядро честное (математика 9/10, 0 нарушений
  на 12 000, перепроверено независимо), советник имел дефекты G1–G7. Материалы:
  `docs/reports/testing/independent_expert_review_2026_07.md` (заключение),
  `knowledge/model_validation/joined.csv.gz` (эталон, регенерация — генератор v1 seed 20260702).
- [x] **Калибровка по экспертизе → модель v3.1.0** (канон `docs/math_model_v3_1_0.md`): закрыты
  must-fix G1 (насыщение резерва, Lt* 3–6 мес), G2 (кризисный режим при Rt < 0), G3 (гейт «план не
  увеличивает ПДН») + G6 (floor 1 мес) + G5-минимум (инвест-транш с полкой). Согласие с консенсусом:
  **51.9% → 87.4%** при коридоре самих экспертов 85.6–95.6% — модель вошла в коридор
  (`docs/reports/testing/expert_agreement_v3_1_0.md`; стенд `tools/model_validation/`).
- [x] **Продолжение вехи 6 — G4/G5/G7 закрыты в v6.2.0 (модель v3.2.0):** G4 — слой управления
  запасом `app/core/surplus.py` (Avalanche-погашения из излишка → дедлайновые цели → разовый
  инвесттранш; инвариант I14, UI-блок на /planning); полный G5 — `instrument_for_horizon`
  (<12 мес — депозит; 12–36 — депозит+ОФЗ; >36 — профильный микс) в разовых взносах; G7 —
  ПДН при нулевом доходе с платежами = 1.0. Согласие с консенсусом не изменилось (87.4%),
  свип v2 12 000: 0 нарушений I1–I14. Остаток G7 (ре-роутинг остатка целевого бакета,
  ревизия сетки альтернатив) — исследовательская задача, ниже.
- [ ] **G7-остаток (исследование):** ре-роутинг нераспределённого остатка целевого бакета в
  резерв (меняет семантику решётки — делать с замером на стенде) и ревизия сетки альтернатив
  (шаг 5% — см. §6.3).
- [ ] **Повторная сертификация на датасете v2:** прогон 12 000 портретов генератора v2 (калиброван:
  ПДН через beta по картине ЦБ/НБКИ, аннуитетные сроки 12–84 мес, adversarial-слой) через >= 3
  независимых экспертных движка тем же протоколом — финальный гейт вехи 6. Материалы готовы
  (v6.1.0): портреты и ответ модели материализованы в `knowledge/model_validation/`
  (portraits_v2 + model_outcomes_v3_1_0_on_v2), карточки — `render_portrait_cards.py`.
  **Кандидат к пересмотру по итогам:** floor резерва 1.5–2.0 мес (грид-эксперимент дал 88.9–90.2%
  против 87.4% на floor 1.0, но применение отложено — вторая подгонка под тот же датасет/состав
  экспертов = оверфит; см. `docs/model/expert_alignment.md` §5).

## 6.2. Масштаб кейсов

- [x] **1000+ кейсов** — выполнено с запасом: 12 000 портретов в экспертизе v6.0.0 (см. §6.1)
  поверх свипов v5.17.0 (2000 + 10000). Генератор v2 (v6.0.0) откалиброван по фидбеку экспертизы:
  долги через целевой ПДН (corr доход–платежи > 0.3 против ~0.02 у v1), реалистичные аннуитетные
  сроки, дефицит <= ~30% сета, adversarial-типы (pdn_boundary, rate_at_bench, deadline_now,
  kopeck/giant_scale). 6 эталонных user-cases — отдельно для показа людям (остаток ниже).
  - [x] **Инфраструктура + первый свип (пред-работа, v5.17.0, приказ владельца):**
    `tools/portrait_testing/` — генератор синтетических портретов (10 граничных типов),
    11 исполняемых инвариантов канона (|A| кусочно 66/11/1 · Rt'≥0 · ПДН≤0.40 · суммы долей ·
    fail-loud · веса профилей · Avalanche ≥ r_bench по убыванию · детерминизм · SES/Monte-Carlo
    p10≤p50≤p90 · сверка Lt stock-based/Dt · конечность), раннер с отчётом. **Прогнано 2000
    (seed 20260702) + контроль 10000 (seed 77) — 0 нарушений, 0 падений**; 7 property-тестов в
    fast-контуре. Отчёт: `docs/reports/testing/portrait_sweep_2026_07.md`. Остаток пункта —
    реальные портреты (6 эталонных) и разбор вопросов §6.3 в самой вехе 7.
- [x] **Больше профилей риска** — все 5 профилей покрыты экспертизой (риск-срезы в заключении:
  дифференциация реальна, G6-край закрыт floor'ом); property-тесты и инварианты I6/I13 держат канон.

## 6.3. Вопросы по модели (исследовать при тестировании)

- [x] **Совет при отрицательном свободном ресурсе** — закрыто в v6.0.0 кризисным модулем
  (`app/core/crisis.py`, канон §12 v3.1.0): балансовый ход «закрыть кредит из ликвидности» (если
  разворачивает поток при сохранении floor 1 мес), сокращение расходов ровно до нуля дефицита,
  **потолок трат** `max_affordable_expenses` (ответ на «сколько можно тратить»), заморозка целей,
  реструктуризация/рефинансирование/каникулы самого дорогого кредита, запас хода в месяцах.
  Охват: 9697/9697 дефицитных портретов с действиями (recoverable: 387). Рефинансирование —
  возможно и включено (исследование дало «да»). **Кейс Василия разобран и вшит живым тестом**
  `tests/test_crisis.py::TestVasiliiCreditCardCase`: на стабильных 20к — дефицит 2к/мес, потолок
  трат 14к/мес, runway 2 мес, реструктуризация кредитки; с донорством (+25к) — поток положительный,
  лавина бьёт в кредитку (39.9% >> r_bench). UI: блок кризис-плана на /planning.
- [x] **Валидация графика прогноза.** ✓ v6.4.0. Оказалось: НЕ баг арифметики, а (1) единичное
  сглаживание по одной точке даёт плоскую линию и (2) роут прогноза выбрасывал историю. Исправлено:
  демпфированный Holt + помесячная капитализация баланса + история из РЕАЛЬНЫХ транзакций
  (`build_monthly_history`, скользящие 30-дневные корзины). Подтверждено end-to-end на 10 портретах —
  форма разная (рост/падение/сезон), константной дельты нет. INV-FORECAST-STRAIGHTLINE закрыт; разбор —
  `docs/reports/investigations/forecast_straight_line_investigation.md`, канон → v3.3.0.
- [x] **Шаг дискретизации.** ✓ v6.4.0 замерено. Параметр `step` в `run_planning`; бенчмарк
  `tools/model_validation/grid_step_benchmark.py` + `tests/test_grid_step.py`. Блокер на 5%: коллизия id
  альтернатив при шаге 5% (231 альтернатива) — задокументирован; на 10% (66) без коллизий.
- [ ] **Nullable-дедлайн целей (миграция).** Канон допускает бессрочные цели (`deadline: null`), но
  колонка `goals.deadline` — NOT NULL. Нужна Alembic-миграция + матрица SQLite/PG + тесты. Пока в демо
  бессрочная цель обходится дальним горизонтом (10 лет). Всплыло в v6.4.0.
- [ ] **Переименовать файл модели в `math_model_v3_3_0.md`.** Обновление прогнозирования (Holt +
  капитализация) внесено как каноничная часть v3.2.0 (раздел «Обновление прогнозирования (v3.3.0)»);
  при переименовании — обновить ссылки.

## 6.4. Импорт выписок (тестирование)

- [ ] **Вшить эталонные шаблоны** PDF/XLS/CSV в продукт.
- [ ] **Тесты корректности считывания** — числа/категории/операции читаются правильно, не путаясь.
- [ ] **Покрытие ~200 банков** — реализация стратегии из вехи 5.5.

---
# ВЕХА 7 — Юридическое (`v7.0.0`)

> Все документы и соответствие доведены до конца. ОБЯЗАТЕЛЬНО ревью реальным юристом — это не зона
> ответственности разработки.

- [ ] **Все юр-документы** доведены до конца (Политика конфиденциальности, оферта, согласие 152-ФЗ,
  политика обработки ПДн, право на удаление) + ревью юристом.
- [ ] **Соответствие законам:** (1) о финансах (2) о персональных данных (152-ФЗ) (3) о кибербезопасности.
- [ ] **Сравнить с требованиями Apple App Store** (или аналогичными) — проверить применимость, если
  будет мобильная подача.
- [ ] **Файл вопросов юристу** (`docs/legal_questions_for_lawyer.md`) — используется здесь. (Сам файл готов.)

**Контекст по статусу (из обсуждения, не юр-консультация):** продукт на старте бесплатный (без
монетизации — проще). Аккредитация/регистрация как банк НЕ нужна (FINPILOT — СППР, информационная
поддержка, не платежи). Открытый вопрос — статус оператора ПДн (физлицо / самозанятый / ИП) и
минимизация публичности личных данных. Решается с юристом.

---
# ВЕХА 8 — Фронтенд (`v8.0.0`)

> Тотальный редизайн + переход на современный прод-стек + вынос на фронт ВСЕХ возможностей бэка,
> которые сейчас живут только в API. Ощущение Василия: бэк уже мощный (4.x), а фронт застрял на
> уровне 2.x и не показывает пользователю и доли возможностей.

## 5.9. Научный трек в базе знаний (ретро-добавление, приказ владельца 2026-07-02)

- [x] **Папка `knowledge/science/`** — статьи, методички, переписка с редакциями _(готово
  v5.16.0)_: статус публикаций (принятая МНТК/РИНЦ + 4 статьи в работе, вкл. пакет КИМ с
  replication package), финальные рукописи, ответы редакций (БИ-отказ и др.), гайды
  (ГОСТ/Scopus/РИНЦ/степени). 42 файла, ~8.5 МБ, sha-дедуп; индекс и что-не-вошло —
  `knowledge/science/README.md`. **Private-only: в whitelist публичного зеркала не добавлять.**
  ⚠️ DOC-03: нормативная нормализация из статьи КИМ в коде не реализована — кандидат в v3.1
  при принятии статьи.

## 8.1. Стек (зафиксирован — см. `docs/reports/adr/adr_001_frontend_stack.md`)

- [x] **Решение по стеку принято:** React + TypeScript, бэк остаётся как REST API. Полный набор:
  Vite, TanStack Query, TanStack Table, React Hook Form + Zod, Radix UI / shadcn/ui, Recharts / visx,
  KaTeX. Тесты: Vitest + Playwright (chromium). Реализация — через Claude Design + дизайн-агенты.
  Эстетика — по `docs/ui_visual_design_brief.md`.

## 8.2. Базовые задачи фронта

- [ ] Поднять React+TS проект (Vite), настроить тесты (Vitest + Playwright).
- [ ] Семантические токены дизайн-системы (`--surface-*`, `--text-*`, `--space-*`, `--radius-*`),
  типографическая шкала, табличные цифры везде, где деньги.
- [ ] Перенести существующие экраны (дашборд, операции, обязательства, цели, планирование) на компоненты.
- [ ] **ГЛАВНОЕ — вынести на фронт всё, что висит только в API:** in-app уведомления (колокольчик),
  рефералка, аналитика-дашборд (админ), история планов, household/семейные бюджеты, A/B-результаты,
  экспорт, spending-advice слои 1-3.
- [ ] Микро-визуализации (прогресс-бары целей, донат распределения плана, спарклайны).
- [ ] Тёмная тема (отдельная палитра, не инверсия), motion (150-250мс, ease-out, `prefers-reduced-motion`),
  тосты + скелетоны вместо `alert()` и пустоты, mobile-адаптив (цели нажатия ≥44px).

## 8.3. Онбординг и объяснимость (критично — закрывает реальные вопросы пользователя)

- [ ] **Интерактивный онбординг** — отдельная страница на сайте + файл-документ, с активными ссылками.
- [ ] **Пояснения «как найти выписку»** того/иного формата прямо в интерфейсе (если пользователь не
  знает, где её взять в своём банке).
- [ ] **Объяснить логику дохода** (онбординг должен это закрыть — при проектировании Claude отвечает):
  как алгоритм понимает доход? Что считается зарплатой? Пользователь вбивает вручную с уникальным
  тегом? А если доходов несколько (дивиденды, подарок, бизнес-выручка, репетиторство, две работы,
  родители дали) — не сломается ли алгоритм? Какой порядок действий для пользователя, чтобы алгоритм
  считал всё правильно?
- [ ] **Объяснить разницу целей и активов** — как это в алгоритме, как пользователь должен разграничивать.
  Супер-подробный онбординг (интерактивный на сайте + файл).

## 8.4. Полировка (давние пункты)

- [ ] **Формулы с нижними индексами** красиво, в LaTeX-стиле (KaTeX) — давно висит, наконец сделать.
- [ ] **Красивые виджеты/системные сообщения** — регистрация, ошибки, подтверждения. Все системные
  сообщения сделать приятными.
- [ ] **Улучшить UI прогнозирования** — передвижение/выбор параметров движка.
- [ ] **PDF-отчёт со страницы прогнозирования** — переделать в красивый и реально полезный, в отдельном
  формате, а не распечатка страницы. Реализация PDF — на бэке, но триггер и польза — фронтовые.

Из старого роадмапа в веху 6 вливаются: P1.5 (UI-стандарт), P2.1 (UI рефералки), P2.2 (UI аналитики),
P2.3 (UI-часть уведомлений), P3.1 (i18n UI — вынос строк), P3.2 (PWA).

---
# ВЕХА 9 — Деплой (`v9.0.0`)

> Финальный запуск на сервере. План — в `docs/DEPLOY.md` и `DEPLOYMENT_PLAN.md`.

- [ ] **Деплой по плану:** VPS в РФ (152-ФЗ → данные граждан РФ на серверах в РФ), Docker через зеркала
  (`dh-mirror.gitverse.ru` — Docker Hub заблокирован для РФ), Nginx reverse proxy + Let's Encrypt
  (HTTPS обязателен для финпродукта), прод-секреты в `.env` (fail-loud), бэкапы по расписанию + cron
  для уведомлений/курсов, мониторинг (Sentry DSN).
- [ ] **Ссылки Василия** (проверить актуальность на момент деплоя): домен — `reg.ru` (искать `finpilot`),
  сервер — `selectel.ru`.
- [ ] **На проде закрываются вещи, не тестируемые в песочнице:** живой SMTP (письма реально уходят),
  живой fetch ставки/курсов ЦБ с РФ-IP, поведение под нагрузкой.

---

## Сквозные правила (действуют на всех вехах)

1. **НИКОГДА не писать имена в гите / релизах / публичных артефактах.** Официально безымянно.
   Касается коммитов, тегов, GitHub Releases, публичного репо.
2. **4 аккаунта Claude: V, J, M, S.** Кто на вахте сейчас — в шапке `docs/WATCHLOG.md` (здесь не
   дублируем — протухает). Имена — только для
   разграничения в разборах, на работу не влияют (WATCHLOG самодостаточен для любого).
3. **Вывод тестов — через `--junitxml`, не через `tail` по логу.** Итоговая строка pytest
   (`N passed`) и счётчики НЕ флашатся надёжно в перенаправленный файл (буферизация stdout, грабли
   песочницы). Решение: `pytest --junitxml=/tmp/result.xml`, парсить XML (точное число passed/failed),
   exit code как источник истины. Зафиксировано в `docs/pitfalls.md` и `docs/sandbox_runbook.md`.
4. **Тестирование мат-модели — СТРОГО вне проекта Claude** (Opus 4.8, без памяти/инструкций) —
   иначе эксперт biased. См. веху 7.
5. **TDD обязателен**, `app/core/` без согласования не трогать (исключение — `spending_advice.py`),
   схемные изменения проверять на PostgreSQL, без массового реформата кроме координированных окон,
   без эмодзи, русская проза + английский код.

---

## Прямые вопросы Василия (ответы — для контекста)

- **Доступ к A/B-тестам и доп-аналитике, аккаунт админа.** Сейчас не «аккаунт», а секрет
  `ADMIN_API_KEY` (заголовок `X-Admin-Key`); в dev при пустом ключе открыто, в проде — задаётся в
  `.env`. Удобный визуальный доступ появится в вехе 6 как админ-дашборд (P2.2). До фронта — можно
  дёргать эндпоинты curl-ом.
- **Замедляют ли тесты/CI разработку.** Почти нет, и экономят время: локальные fast-тесты —
  секунды/минуты, ловят регрессии до прода; CI на push крутится в фоне на GitHub, не блокирует;
  полный прогон ~2-3 минуты против стоимости бага в финпродукте. Для уровня «деньги людей» тесты —
  необходимость, не роскошь. (CI у нас GitHub Actions, не VK.)

---

## Ожидается от Василия (разблокирует задачи)

- [x] **Telegram-токен** от @BotFather — получен, в `.env` (перед продом перевыпустить через
  /revoke — засвечен в чате). Опционально остаётся Telegram user ID (@userinfobot).
- [x] **Список банков / справочник** — закрыто исследованием v5.9.0 (реестр НСПК/ЦБ, §5.5).
- [ ] **Решения юриста** (веха 7) — по вопросам из `docs/legal_questions_for_lawyer.md`.
- [ ] **PNG-превью диаграмм** — регенерация на Mac (drawio desktop), список из 9 схем в
  `docs/diagrams/README.md` §Превью (в песочнице CDN Chromium заблокирован).
- [x] **Решения по предложениям §5.8 п.4** — владелец: «все предложения замутим» (2026-07-02),
  реализовано v5.15.0: onboarding · glossary ·
  API-снапшот · backup_restore · карта ПДн · SLO · deployment-диаграмма.
- [x] **Бизнес-материалы, лого-эталоны, результаты опросника** — влиты в репозиторий (v5.1.0, §5.1–5.4).

---

## Связанные документы

| Файл | Зачем |
|---|---|
| `docs/WATCHLOG.md` | Быстрый снимок «где стоим» (ссылается сюда за деталями). |
| `CHANGELOG.md` | Полная история по каждому патчу. |
| `docs/RELEASES.md` | История развития по эпохам (средний слой). |
| `docs/reports/adr/adr_001_frontend_stack.md` | Обоснование выбора React+TS (веха 8). |
| `docs/ui_visual_design_brief.md` | Эстетическое ТЗ для фронта (веха 8). |
| `docs/math_model_v3_2_0.md` | Каноническая мат-модель (источник истины; v3.1.0/v3.0.0 — исторические документы). |
| `docs/legal_questions_for_lawyer.md` | Вопросы юристу (веха 7). |
| `docs/DEPLOY.md`, `DEPLOYMENT_PLAN.md` | Деплой на VPS (веха 9). |
