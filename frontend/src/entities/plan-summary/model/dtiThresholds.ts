// Жёсткий инвариант канона v3.5.0 (docs/math_model.md): ПДН <= 0.40.
// "Приближение" не задано каноном числом — продуктовый запас в 5 п.п., не
// матмодель (см. finpilot-math-model-guard: канон даёт только сам порог 0.40).
// Общее между MetricsGrid (текущий ПДН) и WhatIfSliders (гипотетический ПДН альтернативы) —
// один и тот же порог модели, не два независимых числа.
export const DTI_THRESHOLD = 0.4;
export const DTI_WARN_THRESHOLD = 0.35;
