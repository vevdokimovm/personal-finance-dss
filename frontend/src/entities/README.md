# entities/

FSD-слой: бизнес-сущности продукта (пользователь, транзакция, обязательство, цель) — типы,
их отображение, элементарные операции. Пусто до Э3 — см. `src/pages/README.md`.

Barrel-файл — только `index.ts` на верхнем уровне каждой сущности, глубже не дублировать.
