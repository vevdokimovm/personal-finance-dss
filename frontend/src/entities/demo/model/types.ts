/**
 * Рукописных типов нет — только реэкспорт сгенерированных из контракта.
 *
 * Схема `DemoCases`/`DemoCase` заведена на бэкенде в этом же батче ДО фронта: эндпоинт
 * был размечен `-> dict[str, Any]` и приезжал как `{[key: string]: unknown}`. Песочница —
 * первый экран человека без своих данных, и рукописный тип означал бы пустые карточки
 * на первом же расхождении (урок v8.31.1).
 */
export type { DemoCases, DemoCase } from "@shared/api/generated";
