/** Рукописных типов нет — реэкспорт сгенерированных из контракта.
 *
 * Схемы `AnalyticsOverview` и `FunnelResponse` заведены на бэкенде в этом же батче
 * ДО фронта: эндпоинты были размечены `-> dict`. */
export type { AnalyticsOverview, FunnelResponse, FunnelStep } from "@shared/api/generated";
