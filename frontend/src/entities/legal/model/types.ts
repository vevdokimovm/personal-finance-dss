/**
 * Рукописных типов нет — только реэкспорт сгенерированных из контракта.
 *
 * Схемы `LegalDocuments`/`LegalDocument` заведены на бэкенде в этом же батче ДО
 * появления фронта. Цена рукописного типа здесь выше обычной: по этим полям строится
 * юридический контур (ссылки в футере L7, дисклеймер 39-ФЗ L5), и расхождение означает
 * отсутствие обязательного по закону элемента, а не косметический сбой.
 */
export type { LegalDocuments, LegalDocument } from "@shared/api/generated";
