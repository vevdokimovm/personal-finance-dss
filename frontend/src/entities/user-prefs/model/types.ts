/**
 * Рукописных типов нет — только реэкспорт сгенерированных из контракта.
 *
 * `/user-prefs` типизирован на бэкенде с самого начала (`app/schemas/user_prefs.py`),
 * так что здесь ничего заводить не пришлось — в отличие от истории планов, рефералки,
 * реестра документов и согласий, где схемы появлялись перед фронтом.
 */
export type { UserPrefsResponse, UserPrefsUpdate } from "@shared/api/generated";
