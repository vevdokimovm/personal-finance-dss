/**
 * Рукописных типов нет — только реэкспорт сгенерированных из контракта.
 *
 * Схемы `StatementUploadResult` и `BankOption` заведены на бэкенде в этом же батче.
 * Ответ импорта несёт семь полей, включая сверку с контрольными итогами выписки —
 * рукописный тип разошёлся бы с ним на первой же правке парсера.
 */
export type { StatementUploadResult, BankOption } from "@shared/api/generated";
