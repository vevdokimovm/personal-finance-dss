# widgets/

FSD-слой: самостоятельные крупные блоки UI, собранные из фич и сущностей (например карточка
метрики, панель распределения, график прогноза). Пусто до Э3 — см. `src/pages/README.md`.

Barrel-файл — только `index.ts` на верхнем уровне каждого виджета, глубже не дублировать.
