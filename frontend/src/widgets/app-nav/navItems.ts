/**
 * Разделы продукта в порядке навигации.
 *
 * Отдельный файл, а не константа рядом с компонентом: `react-refresh/only-export-components`
 * справедливо возражает против модуля, экспортирующего и компонент, и данные — при hot reload
 * такой модуль перезагружается целиком, и состояние компонента теряется на правке константы.
 *
 * Подписи ДОСЛОВНО равны `<h1>` своих экранов — [CMP-03] `docs/ui_ux_design_standard.md`
 * требует одно слово на одно понятие. Поэтому «Кредиты и обязательства», а не короткое
 * «Обязательства» ради умещания в строку: разнобой меню и заголовка — ровно то, что
 * стандарт запрещает.
 */
export const APP_NAV_ITEMS = [
  { to: "/", label: "Финансовый обзор" },
  { to: "/planning", label: "План распределения" },
  { to: "/transactions", label: "Операции" },
  { to: "/obligations", label: "Кредиты и обязательства" },
  { to: "/goals", label: "Цели" },
  { to: "/banks", label: "Ликвидные активы" },
  { to: "/household", label: "Семейный доступ" },
  { to: "/profile", label: "Профиль" },
] as const;

/**
 * Разделы, видимые ТОЛЬКО владельцу продукта (`is_owner`).
 *
 * Отдельным списком, а не флагом внутри общего: у этих пунктов другое условие показа,
 * и смешивать их значило бы каждый раз вспоминать, какой пункт кому виден. Сервер
 * отдаёт этим адресам 403 всем остальным — пункт меню, ведущий в гарантированный
 * отказ, это тупик ([IA-04]).
 */
export const OWNER_NAV_ITEMS = [
  { to: "/insights", label: "Метрики продукта" },
  { to: "/experiments", label: "A/B-эксперименты" },
] as const;
