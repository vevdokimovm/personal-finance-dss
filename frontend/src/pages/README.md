# pages/

FSD-слой: композиция виджетов/фич в конкретный экран. Пусто — поэкранная раскладка сознательно
не детализирована на этапе каркаса (`docs/frontend_migration_plan.md` §6), собирается в Э3
(`docs/frontend_milestone8_plan.md`), начиная с auth-флоу, затем dashboard.

Barrel-файл — только `index.ts` на верхнем уровне каждой страницы, глубже не дублировать.
