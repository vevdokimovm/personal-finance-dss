# features/

FSD-слой: пользовательские сценарии с побочным эффектом (отправить форму, пересчитать план,
переключить тему). Пусто до Э3 — см. `src/pages/README.md`.

Barrel-файл — только `index.ts` на верхнем уровне каждой фичи, глубже не дублировать.
