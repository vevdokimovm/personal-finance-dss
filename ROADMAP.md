# ROADMAP — `personal-finance-dss` (указатель)

> 🔴 **Здесь плана нет намеренно — он в [`docs/ROADMAP.md`](docs/ROADMAP.md).**
>
> У этой репы своя продуктовая конвенция: вся документация живёт в `docs/`
> (`math_model.md`, `WATCHLOG.md`, ADR и прочее), и роадмап — там же.
> Дублировать его сюда нельзя: два источника правды разойдутся молча, а это
> ровно тот класс дефекта, который система ловит с самого начала.
>
> **Зачем тогда этот файл.** `mission-control/scripts/push-inbox.sh` (guard 2)
> отказывает в доставке репе, у которой в **корне** нет `ROADMAP.md` и
> `TASKS.md` — проверка идёт по корневому пути через GitHub API. Без этого
> указателя доставке задач из планировщика некуда приземляться, хотя план
> у репы есть.
>
> Заведён 28.08.2026 при ревизии механизма раздачи задач.

**Актуальный план — [`docs/ROADMAP.md`](docs/ROADMAP.md).**
Вахтенный журнал — [`docs/WATCHLOG.md`](docs/WATCHLOG.md).
