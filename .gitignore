# ── Python ──────────────────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# ── Виртуальные окружения ───────────────────────────────────────────
.venv/
venv/
env/
ENV/

# ── Node (фронтенд) ──────────────────────────────────────────────────
node_modules/

# ── Базы данных (dev/локальные — не коммитятся) ─────────────────────
*.db
*.sqlite
*.sqlite3
sppr.db

# ── Тесты / покрытие / кэши инструментов ────────────────────────────
.coverage
.coverage.*
htmlcov/
.pytest_cache/
.hypothesis/
.mypy_cache/
.ruff_cache/

# Визуальные эталоны платформо-зависимы — генерируются в CI, не коммитятся
tests/full/__screenshots__/

# ── Секреты (никогда в репо; шаблоны .env.example остаются) ──────────
.env
.env.local
.env.*.local

# ── IDE / редакторы ─────────────────────────────────────────────────
.idea/
.vscode/
*.swp
*.swo

# ── ОС ──────────────────────────────────────────────────────────────
# Копии при правке: рядом с оригиналом они выглядят рабочим файлом,
# и правка может уехать в копию — тихо и без ошибки (гейт test_repo_hygiene).
*.bak
*.orig
*.rej

.DS_Store
Thumbs.db

# Аудит действий агента (локальный, не коммитится)
logs/agent-audit.jsonl

# Рабочий каталог mutmut (пилот мутационного тестирования, 16.09.2026): копия дерева,
# пересоздаётся каждым прогоном.
mutants/
.mutmut-cache

# Playwright MCP: служебные снимки и логи браузера агентов (17.09.2026)
.playwright-mcp/
