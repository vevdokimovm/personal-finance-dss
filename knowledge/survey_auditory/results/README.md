# results/ — что здесь и где остальное

Здесь хранятся только **невоспроизводимые** результаты исследования аудитории:

- `results.json` — все числовые результаты анализа в машиночитаемом виде;
- `survey_wave2.docx` — вторая волна (сырой документ-артефакт).

**Отчёт `finpilot_report.html` и 14 PNG-графиков из архива убраны (похудение v5.18.0):**
это производные артефакты — регенерируются детерминированно пайплайном
`tools/survey_analysis/run_analysis.py` из сырых данных `../raw/`
(`survey_responses_385.xlsx`). Анализ и методология — `../survey_analysis.md`;
продуктовые выводы — `../product_additions_from_research.md`.
